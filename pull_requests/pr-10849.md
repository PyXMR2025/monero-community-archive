---
title: 'p2p: support IPv6 subnet bans'
source_url: https://github.com/monero-project/monero/pull/10849
author: Ap4sh
assignees: []
labels:
- pending review
- daemon
created_at: '2026-06-30T22:53:58+00:00'
updated_at: '2026-08-26T16:18:53+00:00'
type: pull_request
status: open
closed_at: null
merged_at: null
---

# Original Description
Adds IPv6 CIDR support to the existing ban paths

This lets `--ban-list`, DNS blocklist entries, daemon `ban` / `unban`, and RPC `set_bans` accept entries like `2001:db8::/32`

Single IPv6 hosts keep using the existing address parser

IPv6 subnets stay as a separate internal ban type, matching the existing IPv4 subnet approach

Nested unbans split the remaining IPv6 ranges, so `unban 2001:db8:abcd::/48` works after `2001:db8::/32` was banned

IPv4-mapped and deprecated IPv4-compatible CIDRs are rejected to avoid alternate spellings for IPv4 ranges

Tests:
- `cmake --build build --target daemon wallet_rpc_server unit_tests test_notifier --parallel 2`
- `build/tests/unit_tests/unit_tests --data-dir tests/data --gtest_filter="ban.*:get_network_address.ipv6subnet"`
- `python3 tests/functional_tests/functional_tests_rpc.py python3 tests/functional_tests build bans`
- `ctest --test-dir build/tests/unit_tests --output-on-failure -j2`

# Discussion History
## jpk68 | 2026-08-25T00:10:24+00:00
Would it be possible for the masked subnet to be cached, so that it doesn't have to be recomputed on every call?

## jpk68 | 2026-08-25T00:13:08+00:00
It looks like this function (as well as a few related ones) are extremely similar to their existing IPv4 counterparts. It might be better to use something like a template so there's less duplicated logic.

## Ap4sh | 2026-08-25T10:11:57+00:00
yep, the canonical subnet is cached in the constructor now, so `matches()` only masks the candidate address

## Ap4sh | 2026-08-25T10:11:59+00:00
agreed, i replaced the duplicated IPv4 and IPv6 subtraction helpers with one traits-based template and added `/0` and `/128` coverage

## jpk68 | 2026-08-25T19:48:09+00:00
This seems to reject `::ffff:a.b.c.d` but not the (deprecated) `::a.b.c.d` form; `::192.0.2.0/120` would be considered valid despite the PR description stating "IPv4-mapped IPv6 CIDRs [are] rejected".

IMO, it would also be good to add a unit test for this.

## jpk68 | 2026-08-25T19:50:06+00:00
Can functions like these also be combined/generalized with the existing IPv4 equivalents?

## Ap4sh | 2026-08-26T10:20:40+00:00
good catch, this now rejects both IPv4-mapped and IPv4-compatible forms, with coverage for `::192.0.2.0/120`

## Ap4sh | 2026-08-26T10:20:41+00:00
yep, the IPv4 and IPv6 block/unblock paths now share templated implementations while keeping the existing public overloads

# Action History
- Created by: Ap4sh | 2026-06-30T22:53:58+00:00
