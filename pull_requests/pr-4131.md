---
title: 'wallet2: store keys encrypted'
source_url: https://github.com/monero-project/monero/pull/4131
author: moneromooo-monero
assignees: []
labels: []
created_at: '2018-07-11T21:28:08+00:00'
updated_at: '2018-08-23T01:33:12+00:00'
type: pull_request
status: merged
closed_at: '2018-08-23T01:33:12+00:00'
merged_at: '2018-08-23T01:33:12+00:00'
---

# Original Description
This avoids leaving the keys in memory due to being manipulated
by the JSON I/O API.

These keys are still in memory in the account object for now.

# Discussion History
## stoffu | 2018-07-12T13:02:31+00:00
How about adding 
```c++
  else
    store_keys(keys_file_name, password, m_watch_only);
```
here to make sure this scheme is enabled when loading the old unencrypted format?


## moneromooo-monero | 2018-07-12T13:06:05+00:00
Good idea.

## moneromooo-monero | 2018-07-12T19:07:09+00:00
done

## moneromooo-monero | 2018-07-12T21:48:50+00:00
and now done only when load success...

## stoffu | 2018-07-17T15:13:20+00:00
```
src/cryptonote_basic/account.cpp:77:124: error: return type 'class epee::wipeable_string' is incomplete
   static epee::wipeable_string get_key_stream(const crypto::chacha_key &base_key, const crypto::chacha_iv &iv, size_t bytes)
```

## stoffu | 2018-07-17T15:20:31+00:00
Got this error:
```
src/cryptonote_basic/account.cpp:87:5: error: no matching function for call to 'chacha20'
    crypto::chacha20(buffer0.data(), buffer0.size(), key, iv, buffer1.data());
    ^~~~~~~~~~~~~~~~
src/crypto/chacha.h:68:15: note: candidate function not viable: 5th argument ('const char *') would lose
      const qualifier
  inline void chacha20(const void* data, std::size_t length, const chacha_key& key, const chacha_iv& iv, char* cipher) {
              ^
```
because `wipeable_string::data()` returns `const char *`. But even with a `char *` overload as in #4116 the compiler seems to still stick with the `const` one, so cast `(char *)` seems necessary here.

## stoffu | 2018-07-17T15:22:56+00:00
Bikeshedding: how about using `boost::value_initialized`?
```c++
KV_SERIALIZE_VAL_POD_AS_BLOB_OPT(m_encryption_iv, (crypto::chacha_iv)boost::value_initialized<crypto::chacha_iv>())
```


## moneromooo-monero | 2018-07-17T16:33:44+00:00
That's one change in a subsequent patch :/ I'll bring it forward.

## moneromooo-monero | 2018-07-17T16:34:56+00:00
I want a known value here (zeroes here).

## moneromooo-monero | 2018-07-17T17:03:05+00:00
Does it work if you cherry pick 4116 first ? It builds here with CLANG without changing constness.

## vtnerd | 2018-07-17T17:11:26+00:00
`boost::value_initialized` does exactly that in this situation (initializes to zeroes). However, `boost::value_initialized` is more or less deprecated in C++11. The expression `const crypto::chacha_iv default_iv{}` performs value initialization (zeroes) or calls the default constructor if specified.

## moneromooo-monero | 2018-07-17T18:08:06+00:00
In fact, technically, I don't care about the values, since they'll be overwritten if not present in the saved data.

## stoffu | 2018-07-17T23:25:13+00:00
Right, it did work for me as well. No cast is needed here.

## stoffu | 2018-07-23T01:35:03+00:00
```c++
m_account.encrypt_keys(key);
```


## stoffu | 2018-07-23T01:35:12+00:00
```c++
m_account.decrypt_keys(key);
```


## stoffu | 2018-07-23T01:36:30+00:00
```c++
#include "wipeable_string.h"
```

## stoffu | 2018-07-23T02:15:11+00:00
After a bit of testing and looking closely, it seems like these `encrypt_keys`/`decrypt_keys` steps here are unnecessary, because the function `store_keys` internally calls `encrypt_keys` against a copy of `m_account`.

## stoffu | 2018-07-23T04:08:25+00:00
Ah, this `#include` was added to `hex.h` in #4116 which this PR depends on.

## moneromooo-monero | 2018-07-23T09:45:23+00:00
I don't think so. We want to leave the keys in "normal" mode here, which is with an encrypted spend key, but unencrypted view key. Since we start with unencrypted keys, encrypt_keys(key); does this, while m_account.encrypt_keys(key); encrypts both. Is that wrong ?

## moneromooo-monero | 2018-07-23T09:46:52+00:00
It's a copy, but made at the start of it, so it needs to start from the expected state, which is encrypted secret spend key.

## stoffu | 2018-07-31T23:13:12+00:00
Looks like this is because wipeable_string doesn't have `operator[]`. 

## stoffu | 2018-08-01T01:11:53+00:00
Maybe `std::move(buf)`?

## stoffu | 2018-08-01T02:12:55+00:00
Would it also make sense to do `m_recovery_key.scrub();` here? Or perhaps it should be done with `create_scope_leave_handler` to be sure.

## stoffu | 2018-08-01T02:44:04+00:00
Why not make this function templated where all of `epee::span<T>`, `std::string` and `epee::wipeable_string` would work with no code changes?

## stoffu | 2018-08-01T02:47:43+00:00
This form of conversion to `std::string` shows up repetitively, so maybe add a member function `std::string wipeable_string::to_string() const`?


## moneromooo-monero | 2018-08-01T08:10:03+00:00
I don't think it's related. The version which was not compiling has to_hex::buffer_unchecked(out.data(), src), not using operator[].

## moneromooo-monero | 2018-08-01T08:10:45+00:00
AFAIK, std::move on returned data is worse than not, as it prevents copy elision.

## moneromooo-monero | 2018-08-01T08:12:08+00:00
Seems like it would be a good idea, yes.

## moneromooo-monero | 2018-08-01T08:17:25+00:00
Because it's simple enough like this as it's a one liner, and you'd still need instanciations anyway. Do you think it'd be better ?

## moneromooo-monero | 2018-08-01T08:17:58+00:00
I'd rather not, because it'd make it easier to leak the "secret" data.
Where did you see these conversions ? There should not be many of them.

## stoffu | 2018-08-01T09:01:22+00:00
Ah, true.

## stoffu | 2018-08-01T09:06:15+00:00
OK, my understanding on the move semantics is weak.

## stoffu | 2018-08-01T09:08:33+00:00
I'm fine with your version. I suggested the above because the number of changed lines is smaller.

## stoffu | 2018-08-01T09:11:40+00:00
I see eight instances of this form. OK, maybe it's not that many.

## moneromooo-monero | 2018-08-01T09:49:59+00:00
This is too many :) Can you list the ones you found please ?

## moneromooo-monero | 2018-08-01T10:55:28+00:00
I moved the seed wiping to a leave handler. I'm not adding the recovery key thing, it'll get done when I get to wiping more stuff later.

## stoffu | 2018-08-01T11:04:18+00:00
language_base.h:
```
L123: MWARNING("Duplicate prefix in " << language_name << " word list: " << std::string(trimmed.data(), trimmed.size()));
L125: throw std::runtime_error("Duplicate prefix in " + language_name + " word list: " + std::string(trimmed.data(), trimmed.size()));
```

simplewallet.cpp:
```
L2884: multisig_keys = m_wallet->decrypt<epee::wipeable_string>(std::string(multisig_keys.data(), multisig_keys.size()), key, true);
```

api/wallet.cpp:
```
L739: return std::string(seed.data(), seed.size()); // TODO
```

wallet_rpc_server.cpp:
```
L1522: res.key = std::string(seed.data(), seed.size()); // send to the network, then wipe RAM :D
```

unit_tests/mnemonics.cpp:
```
L88: seed = std::string(w_seed.data(), w_seed.size());
L111: return_seed = std::string(w_return_seed.data(), w_return_seed.size());
L136: return_seed = std::string(w_return_seed.data(), w_return_seed.size());
```


## stoffu | 2018-08-01T11:14:00+00:00
Ok

## moneromooo-monero | 2018-08-01T14:28:00+00:00
Thanks. Those are all OK, except for the one I marked TODO, as the GUI might not be able to keep its data wiped.

## stoffu | 2018-08-01T14:54:39+00:00
OK. BTW, I'd like to try and see if I can easily activate this encryption feature for the GUI (since most novice users will be using the GUI).

## stoffu | 2018-08-02T02:59:22+00:00
This (along with the corresponding decryption part) needs to check `password && m_ask_password && !m_rpc`. One issue from this bug: 1) Create a wallet with this patch, 2) `set ask-password 0`, 3) `set seed language` which returns "wallet is non-deterministic and has no seed"

## stoffu | 2018-08-02T03:22:34+00:00
Though I'm not sure how to reproduce a deprecated wallet, if I artificially activate this section by replacing `if (m_wallet->is_deprecated())` above with `if (true)`, I observe that the wallet wouldn't print the seed saying that the wallet is not deterministic. This is because `get_seed()` internally calls `is_deterministic()` with no argument, which means that `get_seed()` assumes that the wallet keys are not encrypted. The fix is
```c++
tools::wallet_keys_unlocker unlocker(*m_wallet, m_wallet->ask_password(), password);
m_wallet->get_seed(seed);
```

I find it a bit confusing that `is_deterministic` takes the password as an optional parameter so that it can decrypt the wallet keys, while `get_seed` doesn't take the password parameter and assumes the wallet keys to be unencrypted. How about removing the optional password argument from `is_deterministic` and ensure that the wallet keys are decrypted before each call?


## stoffu | 2018-08-02T03:27:09+00:00
I think it should check if the encryption is enabled:
```c++
if (m_ask_password && !m_rpc)
  encrypt_keys(key);
```
Likewise for the subsequent decryption step:
```c++
if (m_ask_password && !m_rpc)
  decrypt_keys(key);
```
To reproduce this issue: 1) use the release CLI to create a wallet, and set ask-password 0, 2) open the wallet with this patch, 3) close it, and 4) try to open the same wallet file again, which fails.


## stoffu | 2018-08-02T05:34:51+00:00
I believe this needs to be `if (m_ask_password && !m_rpc)`. Also, the current patch would make the rest of the process completely skipped when the encryption mode is disabled. It should instead be:
```c++
  // decrypt keys
  epee::misc_utils::auto_scope_leave_caller keys_reencryptor;
  if (m_ask_password && !m_rpc)
  {
    ...
    keys_reencryptor = epee::misc_utils::create_scope_leave_handler([&, this, chacha_key]() { m_account.encrypt_keys(chacha_key); m_account.decrypt_viewkey(chacha_key); });
  }

  ...multisig key generation...

  // keys are encrypted again
  keys_reencryptor.reset();

  if (!m_wallet_file.empty())
  {
    bool r = store_keys(m_keys_file, password, false);
    ...
  }
```
Note that the lambda expression for `keys_reencryptor` needs to copy capture `chacha_key`.

The same for `finalize_multisig`.

## stoffu | 2018-08-02T06:24:01+00:00
Looks like `clear_ringdb_key` is not called from anywhere. Maybe remove it?

## stoffu | 2018-08-02T06:37:58+00:00
The password input to `finalize_multisig` is mandatory, while `SCOPED_WALLET_UNLOCK` skips the password input when `ask-password` is disabled.

Also, the next commit deletes the `SCOPED_WALLET_UNLOCK();` added to `make_multisig` as above, which makes sense (since `make_multisig` does decryption as needed). Should the same be done for `finalize_multisig`?


## stoffu | 2018-08-02T08:23:34+00:00
I think decrypting the spend key is unnecessary when the wallet is watch-only.

## stoffu | 2018-08-02T10:04:40+00:00
The first argument to `wallet2::init` should be boolean. If we assume the encryptiom to be enabled:
```c++
wallet.init(false, "") ;
```


## stoffu | 2018-08-03T06:41:53+00:00
Doesn't `pod_to_hex` create a temporary `std::string` whose data might stay in the memory? 
How about:
```c++
    const epee::wipeable_string s = epee::to_hex::wipeable_string(epee::as_byte_span(k));
```


## stoffu | 2018-08-03T06:45:32+00:00
I don't think this will make any difference because the view key doesn't get encrypted.

## stoffu | 2018-08-03T07:06:25+00:00
This will crash when `ask-password` is disabled where `pwd_container` would be `boost::none`. Password input is mandatory here.
```c++
  if (!pwd_container)
  {
    pwd_container = get_and_verify_password();
    if (!pwd_container)
      return true;
  }
```

## stoffu | 2018-08-03T07:34:17+00:00
Maybe this is a good place to use `scrubbed_arr<char,sizeof(base_key)+1>` as in `wallet2::setup_keys`?

## stoffu | 2018-08-03T07:48:19+00:00
Doesn't the moving of this code section to above have no effect? If so, it seems better to revert this to minimize changes.

## stoffu | 2018-08-03T08:08:40+00:00
Looks like `std::flush` is necessary, otherwise the text output can get weird:
```
Generated new wallet: 543DAuRYixWJbnNZJRYekwjbi2KMGL79XDnDm1PN2LhiGRQtb6rJRSsjaJ87DtE43oYxVChQD3YyQiai2cQsPtw81zgoXDz
5fec71e61cc9a274afb8bf8596a7971a6357291dedc709b50a0c444f8626c407
View key: **********************************************************************
```


## stoffu | 2018-08-03T08:16:49+00:00
When `ask-password` is disabled, the output text becomes slightly strange:
```
[wallet 55AG7C]: viewkey
Wallet password: 
secret: d042f7012cafc1aecf89b06daed52f9fdbc17ea75e8d03aec36613b4fccb0102
public: e7fb43ed085528d3efce5e2ce054b428bbedcb80b2ed86ef1c090fbeb9fa013b
[wallet 55AG7C]: set ask-password 0
Wallet password: 
[wallet 55AG7C]: viewkey
[wallet 55AG7C]: secret: d042f7012cafc1aecf89b06daed52f9fdbc17ea75e8d03aec36613b4fccb0102
public: e7fb43ed085528d3efce5e2ce054b428bbedcb80b2ed86ef1c090fbeb9fa013b
```
I've tried a few things but couldn't figure out how to fix it.

## stoffu | 2018-08-03T09:11:48+00:00
I see `import_multisig_info` not working:
```
[wallet 5ACZH3]: import_multisig_info msinfo-1 msinfo-2
Wallet password: 
Password needed - use the refresh command
Multisig info imported                                          
```
which was fixed by inserting
```c++
    m_in_manual_refresh.store(true, std::memory_order_relaxed);
    epee::misc_utils::auto_scope_leave_caller scope_exit_handler = epee::misc_utils::create_scope_leave_handler([&](){m_in_manual_refresh.store(false, std::memory_order_relaxed);});
```
before the call to `m_wallet->import_multisig(info)`.


## stoffu | 2018-08-03T09:26:25+00:00
This prevents the command `show_transfers` from recognizing unconfirmed incoming transfers:
```
[wallet 55RKrP]: show_transfers
Password needed - use the refresh command
Error: Failed to get pool state:Password is needed to compute key image for incoming monero
```
Like with `import_multisig_info`, it seems that `m_in_manual_refresh` should be set to `true` while in the command.

## stoffu | 2018-08-04T05:26:38+00:00
This and `get_seed` can both remain `const` as no encryption is happening inside.

## stoffu | 2018-08-04T05:57:39+00:00
Maybe the same should apply to `derive_key()` in account.cpp?

## stoffu | 2018-08-04T10:10:37+00:00
On Windows:
```
C:/msys64/home/vagrant/slave/monero-static-win64/build/contrib/epee/src/mlocker.cpp:31:10: fatal error: sys/mman.h: No such file or directory
```


# Action History
- Created by: moneromooo-monero | 2018-07-11T21:28:08+00:00
- Merged at: 2018-08-23T01:33:12+00:00
