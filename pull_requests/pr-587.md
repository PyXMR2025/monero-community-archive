---
title: Tapes db
source_url: https://github.com/Cuprate/cuprate/pull/587
author: Boog900
assignees: []
labels:
- A-p2p
- A-test-utils
- A-dependency
- A-workspace
- A-consensus
- A-docs
- A-storage
- A-helper
- A-pruning
- A-types
- A-ci
- A-binaries
created_at: '2026-02-21T16:01:49+00:00'
updated_at: '2026-04-18T14:15:02+00:00'
type: pull_request
status: open
closed_at: null
merged_at: null
---

# Original Description
No description

# Discussion History
## redsh4de | 2026-03-24T23:36:00+00:00
Is L188 correct?

L105 uses `CompactAltBlockInfo` instead of `BlockInfo` for the read

## redsh4de | 2026-03-24T23:43:56+00:00
shouldn't this be *num_outputs - 1?

if its 0 indexed, then if there is 1 output, it will try to remove from index 1, which is the 2nd item - doesn't exist

## redsh4de | 2026-03-25T00:21:45+00:00
why are the ki bytes inserted, instead of tx hash? AddTransaction takes in a `Option<TransactionHash>`

## redsh4de | 2026-03-25T00:28:54+00:00
is there a reason why this is not something like `mining_index + 1 + index_offset` like in other places that add a +1?

## redsh4de | 2026-03-25T00:55:25+00:00
why is `hardfork_signal` used for `hf_version`, and `hardfork_version` used for `hf_vote` for alts?

other places use `hardfork_version` for `version`, and hardfork_signal for `vote` (except `alt_block/block.rs` which uses version for vote as well, so this seems intentional but i dont get why)

## redsh4de | 2026-03-25T01:00:54+00:00
wont not using path_with_network (like for fjall below) cause testnet and mainnet write to the same files? in case someone runs a mainnet node, then decides to run a testnet node on the same machine

## redsh4de | 2026-03-25T09:31:53+00:00
main used to have .take() which deletes the tx_hash, i dont see the `remove_tx_from_dynamic_tables` deleting the tx_hash from tx_ids - only key images and outputs

edit: looks like it impacts `add_alt_transaction_blob` in `src/ops/alt_block/tx.rs` as well - sees tx hash exists, skips adding the blob

## redsh4de | 2026-03-25T10:01:00+00:00
main used a while loop to build the hashes sequentially - should this be `FuturesOrdered` instead?

## redsh4de | 2026-03-26T07:25:03+00:00
related to previous: why does L198 use `hardfork_version`, instead of `hardfork_signal` like `ops/block.rs`?

## redsh4de | 2026-03-26T10:20:41+00:00
```suggestion
                    warn!(
                        "Failed to verify alt block: {}, error {}, banning peer.",
                        hex::encode(hash),
                        e
                    );
                    batch.peer_handle.ban_peer(LONG_BAN);
```
I'd add the warn! log for alt blocks as well to match how main does it now

## Boog900 | 2026-03-26T13:20:41+00:00
nice spot, fixed

## Boog900 | 2026-03-26T13:21:27+00:00
fixed

## Boog900 | 2026-03-26T13:23:23+00:00
Because 2 txs can spend the same key image but have different hashes, we can't have double spends in the pool so we use the key images themselves here to prevent this.

## Boog900 | 2026-03-26T13:26:58+00:00
Oops forgot to add the explicit removal when I move to a write batch instead of an atomic tx, should be fixed now

## Boog900 | 2026-03-26T13:27:49+00:00
good spot, I remember thinking I need to use the ordered one as well :)

## Boog900 | 2026-03-26T13:29:10+00:00
nice spot, this is incorrect. 

## Boog900 | 2026-03-26T13:38:42+00:00
Fixed

## Boog900 | 2026-03-26T13:39:05+00:00
This was a mistake, should now be fixed.

## Boog900 | 2026-03-26T13:55:55+00:00
oh wait I see what you mean yeah that should be the tx hash.

## redsh4de | 2026-03-26T14:53:46+00:00
we do now remove `tx_hash` from `tx_ids` but noticed that changes are commited **after** this function runs at `service/write.rs` - since `contains_key()` reads commited state, it would still would return `true`, and skip adding the blob when `alt_transaction_blobs` check might have returned `false`

Would removing the `tx_ids` check here be fine? Seems like the simplest way to handle it

## redsh4de | 2026-03-26T14:56:32+00:00
or by checking the uncommited state

## redsh4de | 2026-03-26T18:14:06+00:00
we add amount to cache on L32 but don't remove it on error

edit: alternatively make sure that amount is inserted in cache only on success to avoid needing a remove-on-err, that would be more correct

```suggestion
        pre_rct_numb_outputs_cache.remove(&amount);
        return Err(e.into());
```

## redsh4de | 2026-03-26T18:15:06+00:00
same here

```suggestion
        pre_rct_numb_outputs_cache.remove(&amount);
        return Err(e.into());
```

## redsh4de | 2026-03-26T20:04:08+00:00
nit, to match L215
```suggestion
        .read_entry(&db.block_infos, block_height.saturating_sub(1) as u64)?
```

## redsh4de | 2026-03-26T20:59:18+00:00
style nit: could port the same defensive check from `chain_height`

unnecessary as genesis block always gets added but good for consistency

```suggestion
    let chain_height = tapes
      .fixed_sized_tape_len(&db.block_infos)
      .expect("Required tape not open");
    
    if chain_height == 0 {
        return Err(BlockchainError::NotFound);
    }
    
    let top_block_height = chain_height - 1;
```

## redsh4de | 2026-03-26T21:01:10+00:00
same here

```suggestion
    let chain_height = crate::ops::blockchain::chain_height(db, &tapes)?;
    
    if chain_height == 0 {
        return Err(BlockchainError::NotFound):
    }
    
```

## redsh4de | 2026-03-26T21:16:41+00:00
![its over literally unshippable](https://github.com/user-attachments/assets/9c1b7a02-caaa-4a6b-9346-ca7bf8653ab5)
```suggestion
            .expect("Required tape not found")
```

## redsh4de | 2026-03-26T21:17:12+00:00
here too
```suggestion
        .expect("Required tape must exist") as usize)
```

## redsh4de | 2026-03-26T22:21:24+00:00
note: with graceful shutdown all errors after tapes commit, but before fjall successfully commits should be distinguished due to us being out of sync after that. `BlockchainError::DatabaseOutOfSync`?

`handler.rs` does panic on error now so not a issue rn, but graceful shutdown changes this

idea: recovery function that replays only the out of sync blocks from tapes into fjall when we get that kind of error instead instead of a full chain nuke on desync

## redsh4de | 2026-03-26T22:43:27+00:00
`KeyNotFound` -> `NotFound`

## Boog900 | 2026-03-27T13:19:34+00:00
An error here is unrecoverable so the cache being invalid doesn't matter IMO.

## Boog900 | 2026-03-30T15:14:56+00:00
Yeah I was going to do that originally, however full fjall nuke is much easier to handle + is not too slow. You can test it by just deleting the fjall DB but keeping the tapes.

## hinto-janai | 2026-04-13T18:53:01+00:00
nit: I don't think you care to change this but in the `storage/` diffs there are many u32/u64/usize `as` ops that could use `cuprate_helper::cast` already in scope.

## hinto-janai | 2026-04-13T19:09:25+00:00
nit:

> rg "usize::MAX, 3"
```
storage/blockchain/src/ops/tx.rs
259:            cuprate_pruning::get_block_pruning_stripe(tx_info.height, usize::MAX, 3).unwrap();

storage/blockchain/src/ops/block.rs
111:        cuprate_pruning::get_block_pruning_stripe(start_height, usize::MAX, 3).unwrap(),
143:            cuprate_pruning::get_block_pruning_stripe(blocks[0].height, usize::MAX, 3).unwrap();
405:    let stripe = cuprate_pruning::get_block_pruning_stripe(block_height, usize::MAX, 3).unwrap();
```

```rust
// cuprate_pruning

pub fn get_default_block_pruning_stripe(start_height) -> u32 {
    get_block_pruning_stripe(
        block_height,
        usize::MAX,
        CRYPTONOTE_PRUNING_LOG_STRIPES
    ).unwrap()
}
```

## hinto-janai | 2026-04-13T19:21:23+00:00
nit: there's probably a lot of inaccurate docs at this point; this function replaces it unconditionally.

## hinto-janai | 2026-04-13T19:31:35+00:00
https://github.com/Cuprate/cuprate/pull/587#discussion_r2997388173

Call stack:
- `cuprated::blockchain::manager::pop_blocks`
- `BlockchainWriteRequest::PopBlocks`
- `cuprate_blockchain::service::write::pop_blocks`
- `cuprate_blockchain::ops::block::pop_blocks`

AFAICT input that can pop the genesis is allowed? I think this will underflow.

## hinto-janai | 2026-04-13T19:37:54+00:00
Shouldn't this be given a `tx_rw: &mut fjall::OwnedWriteBatch`? It is only called from `add_alt_block` which has it open already.

## hinto-janai | 2026-04-13T19:47:14+00:00
```suggestion
        let pre_rct_outputs = fjall.keyspace("pre_rct_outputs", KeyspaceCreateOptions::default)?;
```

## hinto-janai | 2026-04-13T20:14:06+00:00
Why not use bytemuck for this? Why big endian?

## hinto-janai | 2026-04-13T20:30:33+00:00
```suggestion
            Some(Ok(o)) => u64::from_be_bytes(o[8..].try_into().unwrap()) + 1,
            None | Some(Err(e)) => {
                err = Some(e);
                0
}
```

https://github.com/Cuprate/cuprate/pull/587#discussion_r2984946003

If L80 branch is taken then L96 will be `0 - 1`.

## hinto-janai | 2026-04-13T20:38:27+00:00
```suggestion
    pub(crate) alt_block_infos: fjall::Keyspace,
```

## hinto-janai | 2026-04-13T20:38:51+00:00
```suggestion
        let alt_block_infos = fjall.keyspace("alt_block_infos", KeyspaceCreateOptions::default)?;
```

## hinto-janai | 2026-04-13T20:43:39+00:00
Shouldn't `tx_rw` be passed to `flush_alt_blocks`?

## hinto-janai | 2026-04-13T21:15:00+00:00
Optional but perhaps (K,V) and their encoding should be documented since the types/tables are gone, even monerod's DB is easier to understand at a glance now 😭


```suggestion
    /// K = `[u8; 32]` (block hash), V = [`usize`], encoding = little endian.
    pub(crate) block_heights: fjall::Keyspace,
```

## hinto-janai | 2026-04-13T22:31:50+00:00
nit:
```suggestion
            index_dir: path_with_network(&self.fs.slow_data_directory, self.network),
```
`cache_sizes` below can be `Copy` but meh

## hinto-janai | 2026-04-13T22:37:30+00:00
Is there a reason to split this into two? They're both the same path.

## hinto-janai | 2026-04-13T22:50:20+00:00
Shouldn't this be through a `OwnedWriteBatch`?

## Boog900 | 2026-04-15T14:45:50+00:00
They need to be ordered by integer value. I'll add a comment 

## Boog900 | 2026-04-18T14:15:02+00:00
I fixed the obvious ones I saw, lmk if you see others/

# Action History
- Created by: Boog900 | 2026-02-21T16:01:49+00:00
