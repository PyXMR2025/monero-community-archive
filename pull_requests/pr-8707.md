---
title: 'verRctNonSemanticsSimpleCached: fix fragility'
source_url: https://github.com/monero-project/monero/pull/8707
author: jeffro256
assignees: []
labels: []
created_at: '2023-01-14T03:13:11+00:00'
updated_at: '2023-05-30T16:23:07+00:00'
type: pull_request
status: merged
closed_at: '2023-03-18T22:17:18+00:00'
merged_at: '2023-03-18T22:17:18+00:00'
---

# Original Description
edit: this is now also a fix PR

Asserts serialization precondition of `verRctNonSemanticsSimpleCached` as mentioned [here](https://github.com/monero-project/monero/pull/8675#discussion_r1045782676), [here](https://github.com/monero-project/monero/pull/8675#discussion_r1053636419), and [here](https://github.com/monero-project/monero/pull/8675#issuecomment-1379738083).

edit: above assumption was found to be false. This PR fixes the issue with semantics differing from non-cached version where it was thought to be the same.

@vtnerd @SChernykh @kayabaNerve

# Discussion History
## UkoeHB | 2023-01-14T03:23:36+00:00
Instead of an anonymous `namespace` I'd encourage the `static` keyword to promote local reasoning (namespaces are less local than keywords). Not to mention all that non-functional whitespace from the indent... (indenting namespaces is a huge pet peeve for me).

## UkoeHB | 2023-01-14T03:27:13+00:00
Does this need to be a pseudo-singleton? Note that this design locks you into a specific filename. Would be simpler and more future-proof to write a function that takes the file name for the transaction and returns the transaction. And separate functions that take a transaction and return the signatures and signatures hex. At most the additional cost is doing all this once_flag stuff in each `TEST()` case instead of once globally.

## UkoeHB | 2023-01-14T03:32:24+00:00
All I can say is... incredible that this exists for this situation.

## UkoeHB | 2023-01-14T03:44:16+00:00
Not too excited about using a macro, at least what you are doing is simple enough. The main alternative that springs to mind is using a lambda passed to a helper function that replaces this macro.

## jeffro256 | 2023-01-14T06:13:27+00:00
One big benefit of using a macro here is that when/if the unit test fails, we get the line number of that specific instance of the test, instead of the same line number of the `EXPECT_NE` inside the helper function.

## jeffro256 | 2023-01-14T06:49:13+00:00
`expand_transaction_from_bin_file_and_pubkeys` should be the change you're looking for. No more singleton, what do you think?

## jeffro256 | 2023-01-14T06:49:42+00:00
Removed the nonfunctional whitespace and put `static` before the functions

## UkoeHB | 2023-01-14T17:49:44+00:00
It looks better but what is the point of keeping the anonymous namespace in there?

## UkoeHB | 2023-01-14T18:04:00+00:00
Add `.vout.size()`?

## UkoeHB | 2023-01-14T18:05:54+00:00
What you can do is return `bool` on your test helper function, then `EXPECT_TRUE()` in your `TEST()`.

## UkoeHB | 2023-01-14T18:08:03+00:00
unused variable `rs_hex`

## UkoeHB | 2023-01-14T18:11:22+00:00
Afaict this function can just return a `crypto::hash` (`get_rct_sig_hash()`).

## jeffro256 | 2023-01-14T19:27:33+00:00
Yeah it can, but I wanted to it be easier for humans to read. The hex representation is 1-to-1 so it shouldn't affect any outcomes.

## jeffro256 | 2023-01-14T19:27:40+00:00
done

## jeffro256 | 2023-01-14T19:35:21+00:00
Avoiding linking conflicts and not polluting global namespace for tools like Doxygen

## jeffro256 | 2023-01-14T19:40:19+00:00
Especially useful in failure cases.

## UkoeHB | 2023-01-14T19:40:59+00:00
This is better thanks

## UkoeHB | 2023-01-14T19:42:03+00:00
In what circumstance is a human reading this and learning anything by looking at the bytes? Either hashes match or they don't.

## jeffro256 | 2023-01-14T19:43:20+00:00
This is true, but it gets more complicated with `EXPECT_EQ`, `EXPECT_NE`, etc...

## UkoeHB | 2023-01-14T19:44:32+00:00
1) linking conflicts mean there is a code smell (like introducing non-static functions into the global namespace, or badly named functions)
2) why would Doxygen be scraping `.cpp` files?

## jeffro256 | 2023-01-14T19:44:33+00:00
Good point

## SChernykh | 2023-01-14T19:45:23+00:00
You could just do
```
ser_sig.append(reinterpret_cast<const char*>(&tx_hash), sizeof(tx_hash));
```
and then call `get_blob_hash` as usual. Keeping all keccak code in one place is better.

## SChernykh | 2023-01-14T19:47:52+00:00
`verRctNonSemanticsSimpleCached` goes through different code paths the first and subsequent times it encounters a tx, so it should be called two times in a row everywhere to be sure.

## jeffro256 | 2023-01-14T19:51:59+00:00
Yes, the only problem is that this might waste an extra allocation. `keccak_init`, `keccak_update`, and `keccak_finish` are used other places in the codebase besides here, `wallet2.cpp` for instance. I also added a unit test to confirm that `cn_fast_hash` uses keccak. 

## SChernykh | 2023-01-14T19:54:52+00:00
Yes, this code if good enough as is. Also, we shouldn't really care that `cn_fast_hash` is keccak because keccak here is not exposed to the calling code and we can basically use any cryptographically secure hash here.

## jeffro256 | 2023-01-14T19:58:47+00:00
Yeah I just added it in case we migrate away from keccak at any point, a unit test will remind us that we can also upgrade this hashing code instead of us having to remember.

## jeffro256 | 2023-01-14T19:58:57+00:00
done

## UkoeHB | 2023-01-14T20:09:21+00:00
I'd like to discourage non-default use of crypto library functions unless there is a good reason. In this case, just reserve capacity in the string before appending anything, then call to `cn_fast_hash()` like normal. That pattern is very standard. Also you need to define a domain separator (all new uses of hash functions need a domain separator as an MRL policy introduced by sarang).


## UkoeHB | 2023-01-14T20:19:22+00:00
This function feels like a categorization and scoping mess to me. `rctSigs` should not need to depend on `cryptonote::transaction` (notice that this only works due to a transitive include right now, which should be fixed). `rctSigs` should not need to depend on _any_ transaction types or functions... it's a file about ringct signatures.

This caching of transaction signature validation results should be a tool in the scope of transaction validation and the ledger (`cryptonote_core` probably), and should not need to rely on a pseudo-singleton... The cache should be a member variable of the blockchain class, and it should by updated by calling into a free function that does what this function is doing. Notice how that pattern would be easily extensible to seraphis without dumping another singleton into the core seraphis code (actually, today I will write a `try_get_tx_contextual_validation_id()` function that gets the hash you have here for seraphis, then you can easily write a function that does 'try get id -> if id not known by cache then fully validate tx').

## UkoeHB | 2023-01-14T21:03:45+00:00
Wow ok so this does require two allocations without some fancy stringstream ops to copy the underlying stringstream buffer, that's too bad.

You need to include a domain separator:

```
cryptonote_config.h

const constexpr char HASH_KEY_CONTEXTUAL_TX_VALIDATION_ID[] = "contextual_tx_validation_id";
```

```
in this file:

std::string ser_sig_and_txhash;
ser_sig_and_txhash.reserve(sizeof(config::HASH_KEY_CONTEXTUAL_TX_VALIDATION_ID) + ss.tellp() + sizeof(crypto::hash));
ser_sig_and_txhash.append(config::HASH_KEY_CONTEXTUAL_TX_VALIDATION_ID);
ser_sig_and_txhash.append(ss.str());
ser_sig_and_txhash.append(tx_hash.data, sizeof(crypto::hash));
```

## UkoeHB | 2023-01-14T21:15:13+00:00
In those cases you just use an assert with clear message, then `EXPECT_NO_THROW()` in the test body.

## SChernykh | 2023-01-14T21:22:53+00:00
What's the point of a domain separator in a data structure that's not mixed with any other hashes anywhere? At this point it's "do everything by the book" just for the sake of doing by the book. Domain separator is unnecessary here.

## SChernykh | 2023-01-14T21:23:32+00:00
This is a bugfix PR, not a refactoring PR. I agree it should be rewritten, just not in this PR.

## UkoeHB | 2023-01-14T21:26:35+00:00
Doesn't matter if it's unnecessary, do it anyway. Not adding a domain separator just means anyone reviewing this code has to do the extra work of validating that no domain separator is necessary.

## SChernykh | 2023-01-14T21:28:12+00:00
Explain why? The code gets fatter and more complicated (for no real reason as far as I know). I'm against this change.

## UkoeHB | 2023-01-14T21:30:35+00:00
It's a policy for higher-quality cryptographic code.

## SChernykh | 2023-01-14T21:32:46+00:00
I know what it is and why. Anyway, after the latest commit it's much cleaner and just 1 line of code, so good enough.

## UkoeHB | 2023-01-14T21:37:47+00:00
The transcript utility I wrote for seraphis requires a domain separator in the constructor, to make it extra clear when **you're doing it wrong**.

## jeffro256 | 2023-01-14T21:38:02+00:00
Yes, check the latest commit. Should be a good compromise. All allocations happen in `std::stringstream`, and we use best practices of domain separation.

## UkoeHB | 2023-01-14T23:24:45+00:00
It looks like this function expects that `rv` was constructed by a call to `expand_transaction_2()` or equivalent. As a sanity check you could add an assert that `!(rv.message == rct::zero())`. It doesn't guarantee anything, but would make tracking down errors easier if this function is misused.

## UkoeHB | 2023-01-14T23:28:39+00:00
`modifyf` looks like a typo, maybe `modify_func` would work better (that's the pattern I use).

## UkoeHB | 2023-01-14T23:37:25+00:00
Uncomment this now that it's fixed?

## jeffro256 | 2023-01-15T03:35:38+00:00
It's not "fixed" really since right now it's still just testing `do_serialize` uniqueness. I think I'll have both a `do_serialize` uniqueness test and a full implementation mimic uniqueness test

## jeffro256 | 2023-01-15T07:43:42+00:00
Okay I separated tests for `do_serialize()` uniqueness from the whole sig + tx uniqueness.

## iamamyth | 2023-01-15T15:54:13+00:00
Some grammar and clarity issues in this comment. Would suggest:
```
// ::do_serialize(rct::rctSig) omits certain transaction-linked rct::rctSig
// fields which affect the semantics of the underlying signature. Therefore,
// the transaction hash must be included in the cache key so that caching
// a rct::rctSig, modifying its transaction-linked fields, and then performing
// a cache lookup on the modified signature, does not yield a false cache hit. 
```

## iamamyth | 2023-01-15T15:55:16+00:00
Seems strange to have a static function in a header file

## jeffro256 | 2023-01-15T19:19:16+00:00
Should be a static method of `class Blockchain`, no?

## jeffro256 | 2023-01-16T19:47:32+00:00
Actually since we confirmed that the behavior did not change, why don't we refactor it right now?

## UkoeHB | 2023-01-16T20:18:37+00:00
This PR is already getting quite long, best to keep it scoped to 'unit test + bug fix' and follow up with a refactor (refactors should generally only change interfaces and organization of code, not existing behavior).

## jeffro256 | 2023-01-17T05:58:47+00:00
This issue is resolved with the latest change to implementation

## iamamyth | 2023-01-17T18:18:57+00:00
See https://github.com/monero-project/monero/pull/8707#issuecomment-1385838246, the various declaration changes suggest an ownership/scoping problem which should be fixed in this PR.

## jagerman | 2023-01-17T21:59:54+00:00
> Avoiding linking conflicts and not polluting global namespace for tools like Doxygen

You won't get linking conflicts: anonymous namespace and `static` do exactly the same thing (for free functions).

## SChernykh | 2023-02-26T08:53:56+00:00
Wait a minute, I don't see where you add successful results to the cache? Where is `cache.add`?

## jeffro256 | 2023-02-26T16:14:21+00:00
That's a great point. Removed it for debugging and forgot to add it back in haha. Check latest commits

## iamamyth | 2023-02-26T17:18:43+00:00
Typo: `pos-expansion` => `post-expansion`

## iamamyth | 2023-02-26T17:19:24+00:00
Typo: `verifcation` => `verification`

## iamamyth | 2023-02-26T17:45:17+00:00
Could make use of `AssertionResult`, `AssertionSuccess`, and `AssertionFailure` here, e.g.:
```
static testing::AssertionResult modification_changes_do_serialize
(
... og_obj,
... obj_modifier_func
// note removal of expected_change
)
{
  ..
  const std::string og_hex = epee::to_hex::string(epee::strspan<uint8_t>(og_blob));
  const std::string modded_hex = epee::to_hex::string(epee::strspan<uint8_t>(modded_blob));
  if (og_blob == modded_blob)
    return testing::AssertionFailure() <<  "serialized blobs match: modded_blob '" << modded_hex << "' og_blob '" << og_hex << "'";
  else
    return testing::AssertionSuccess() <<  "serialized blobs differ: modded_blob '" << modded_hex << "' og_blob '" << og_hex << "'"; 
```

Then, if you expect a change:
```
EXPECT_TRUE(modification_changes_do_serialize(og_obj, obj_modifier_func))
```

And if you expect no change:
```
EXPECT_FALSE(modification_changes_do_serialize(og_obj, obj_modifier_func))
```

## iamamyth | 2023-02-26T17:46:26+00:00
Typo: `than` => `then`

## iamamyth | 2023-02-26T17:46:49+00:00
Typo: `validty` => `validity`

## jeffro256 | 2023-02-26T17:58:02+00:00
But sometimes I want the modification to *not* result in a serializable change. In that case, using `AssertionFailure` doesn't seem quite right in that case, since matching blobs is the success case.

## iamamyth | 2023-02-26T18:57:06+00:00
`AssertionFailure` and `Success` act like bools with a message attached (this exact use is very clearly documented in the gtest.h file), hence the call to `EXPECT_[False|True]`. I guess you can quibble with google's naming choices but that makes using the gtest library much more difficult.

## iamamyth | 2023-02-26T19:06:15+00:00
I suppose I should note that I think your premise misses the mark a bit:
> But sometimes I want the modification to not result in a serializable change. In that case, using AssertionFailure doesn't seem quite right in that case,

The assertion failing in this case is that "modification changes do serialize". So, the result from the function makes sense. Then the caller decides whether the scenario should or should not change do serialize, i.e. whether the function's result constitutes a test failure or success. Whereas, the current version tries to combine the two separable concerns, causing the boolean value to repeat in the function call and the assertion (i.e. `EXPECT_TRUE(..., true)` and `EXPECT_FALSE(..., false)`).

## UkoeHB | 2023-03-07T23:03:43+00:00
Move this into the public API of `Blockchain` so it can be used by the external verification cache function.

## UkoeHB | 2023-03-07T23:04:17+00:00
This does not need to be a member of `Blockchain`.
1) Make this a free function with a `cache_inout` parameter and an `allowed_current_rct_type` parameter.
  - You can put the function into new `cryptonote_core/tx_verification_utils.*` files under namespace `cryptonote` (the .cpp can include `blockchain.h` to get access to `Blockchain::expand_transaction_2()`). Doing it this way facilitates future support for caching seraphis tx verification results. The namespace isn't ideal, but I think we have to live with this legacy layout.
2) Add a default-initialized `m_rct_ver_result_cache` member variable to `Blockchain`.
3) Pass the `m_rct_ver_result_cache` into this function along with `rct::RCTTypeBulletproofPlus` for the current rct type (ideally this would be deduced from context at the callsite).

## UkoeHB | 2023-03-07T23:11:47+00:00
```suggestion                           \
        const auto sig_modifier_func = [](rct::rctSig& rs) { rs.fieldmodifyclause; }; \
```

## UkoeHB | 2023-03-07T23:13:45+00:00
```suggestion
        const auto sig_modifier_func = [](rct::rctSig& rs) { rs.fieldmodifyclause; };  \
```

## UkoeHB | 2023-03-07T23:18:09+00:00
```suggestion
    cryptonote::transaction& tx,
    const crypto::hash& tx_prefix_hash,
    const rct::ctkeyM& mix_ring
  ) -> bool
```

## UkoeHB | 2023-03-07T23:21:40+00:00
```suggestion
    // Check mix ring in transaction against mr
    if (mr != rv.mixRing)
    {
      MERROR_VER("Failed to check ringct signatures: mismatched pubkeys/mixRing");
      return false;
    }
```

All you need is to add `operator!=` for `rct::ctkey`.

## UkoeHB | 2023-03-07T23:27:17+00:00
In `rct/rctTypes.h`

```cpp
struct ctkey {
        key dest;
        key mask; //C here if public
        bool operator==(const ctkey &other) const { return (dest == other.dest) && (mask == other.mask); }
        bool operator!=(const ctkey &other) const { return !(*this == other); }
    };
```

## UkoeHB | 2023-03-07T23:31:12+00:00
```suggestion
    // Mix ring data is now known to be correctly incorporated into the RCT sig inside tx. Do verification
```

## UkoeHB | 2023-03-09T20:17:13+00:00
Not great this has to be `mutable`, but I guess caching is one of the cases where it makes sense.

## UkoeHB | 2023-03-09T20:21:50+00:00
This should be a local variable of your test to avoid cross-talk between tests.

## UkoeHB | 2023-03-09T20:34:31+00:00
Doing this is typically a sign you need a new function:
```
static bool validate_ring_signatures(const rct::rctSig &ring_sigs,
    const std::vector<cryptonote::txin_v> &tx_inputs,
    const rct::ctkeyM &expected_mix_ring);
```

## UkoeHB | 2023-03-09T20:37:42+00:00
```suggestion
    VER_ASSERT(n_sigs == tx.vin.size(), "Failed to check ringct signatures: mismatched input sigs/vin sizes");
```

## UkoeHB | 2023-03-09T20:40:59+00:00
```suggestion
    const bool error{
            rct::is_rct_clsag(rv.type)
            ? !(nth_vin_image == rct::rct2ki(rv.p.CLSAGs[n].I);
            : (rv.p.MGs[n].II.empty() || !(nth_vin_image == rct::rct2ki(rv.p.MGs[n].II[0]));
        };
```

Possible alternative, not required

## UkoeHB | 2023-03-09T20:51:06+00:00
```suggestion
    const bool key_images_match{
            [&rv, &nth_vin_image, n]()
            {
                if (rct::is_rct_clsag(rv.type) && (nth_vin_image == rct::rct2ki(rv.p.CLSAGs[n].I)))
                    return true;
                if (!rv.p.MGs[n].II.empty() && (nth_vin_image == rct::rct2ki(rv.p.MGs[n].II[0]))
                    return true;
                return false;
            }()
        };
```

Another option

## jeffro256 | 2023-03-13T04:37:21+00:00
We could alternatively make `Blockchain::check_tx_inputs` non-`const`.

## jeffro256 | 2023-03-13T04:52:57+00:00
I don't know if that's necessarily true in this case, however. IMO this function has a fairly well defined scope. This reference is just here to make the function more readable and less unwieldy. 

## jeffro256 | 2023-03-13T04:54:12+00:00
How do like the latest rewrite of this part? It also gives more descriptive error messages while keeping the control flow simple.

## jeffro256 | 2023-03-13T04:54:22+00:00
Agreed

# Action History
- Created by: jeffro256 | 2023-01-14T03:13:11+00:00
- Merged at: 2023-03-18T22:17:18+00:00
