---
title: 'P2P: Change `ClientPool` to `PeerSet`'
source_url: https://github.com/Cuprate/cuprate/pull/337
author: Boog900
assignees: []
labels:
- A-p2p
- A-dependency
- A-workspace
- A-binaries
created_at: '2024-11-03T20:37:03+00:00'
updated_at: '2025-01-14T01:38:44+00:00'
type: pull_request
status: merged
closed_at: '2024-11-20T01:37:52+00:00'
merged_at: '2024-11-20T01:37:52+00:00'
---

# Original Description
### What

Changes the `ClientPool`, a `DashMap` of unused connected peers to the `PeerSet`, a service containing all connected peers.

### Why

We need access to all connected peers for certain RPC requests + it's just clearer to have all connected peers in one place.

### How

A new type `WeakClient`  was created so we can still give out handles from the `PeerSet`



# Discussion History
## Boog900 | 2024-11-05T19:58:20+00:00
The new `WeakClient`

## Boog900 | 2024-11-05T19:59:04+00:00
The `PeerSet`, this now holds connected peers instead of the `ClientPool`

## Boog900 | 2024-11-05T19:59:38+00:00
To keep track of peers that are already downloading blocks/stemming txs we use atomic bools

## hinto-janai | 2024-11-05T22:42:04+00:00
Is there a sanity check anywhere in the pipeline from user input -> this line?

If the config has `u64::MAX` for both values would this allow overflow?

## hinto-janai | 2024-11-05T22:44:06+00:00
```suggestion
/// A request to the [`PeerSet`].
```

## hinto-janai | 2024-11-05T22:48:13+00:00
I think the `returned ...` docs could be moved to the response variants.

## hinto-janai | 2024-11-05T22:52:07+00:00
This is only used for `Receiver`, I think it could be imported?
```suggestion
use tokio::sync::mpsc::Receiver;
```

## hinto-janai | 2024-11-05T23:20:57+00:00
iterator version of `for`
```suggestion
        let most_pow_chain = self
            .peers
            .values()
            .fold((0, 0, [0; 32]), |most_pow_chain, peer| {
                let core_sync_data = peer.client.info.core_sync_data.lock().unwrap();
                let cum_diff = core_sync_data.cumulative_difficulty();
                if cum_diff > most_pow_chain.0 {
                    (
                        cum_diff,
                        u64_to_usize(core_sync_data.current_height),
                        core_sync_data.top_id,
                    )
                } else {
                    most_pow_chain
                }
            });
```

## hinto-janai | 2024-11-05T23:25:15+00:00
used once below
```suggestion
use rand::{seq::index::sample, thread_rng};
```

## hinto-janai | 2024-11-05T23:37:48+00:00
couple alternatives

### for
```suggestion
        let outbound_peers = index::sample(
            &mut thread_rng(),
            self.outbound_peers.len(),
            self.outbound_peers.len(),
        )
        .into_iter()
        .map(|i| self.outbound_peers.get_index(i).unwrap());

        for peer in outbound_peers {
            let client = self.peers.get(peer).unwrap();
            if !client.is_a_stem_peer() {
                return PeerSetResponse::StemPeer(Some(client.stem_peer_guard()));
            }
        }

        PeerSetResponse::StemPeer(None)
```

### iterator
```suggestion
        let stem_peer = index::sample(
            &mut thread_rng(),
            self.outbound_peers.len(),
            self.outbound_peers.len(),
        )
        .into_iter()
        .find_map(|i| {
            let peer = self.outbound_peers.get_index(i).unwrap();
            let client = self.peers.get(peer).unwrap();
            client.is_a_stem_peer().then(|| client.stem_peer_guard())
        });

        PeerSetResponse::StemPeer(stem_peer)
```


## hinto-janai | 2024-11-06T00:47:20+00:00
Nvm it's not in scope.

## hinto-janai | 2024-11-06T00:48:20+00:00
Nvm nvm, it's defined in the same file. Same for `PeerSetResponse` btw.

## Boog900 | 2024-11-10T22:17:05+00:00
changed to `checked_add` here

## Boog900 | 2024-11-10T22:17:40+00:00
It's in scope but it's private 

## Boog900 | 2024-11-10T22:19:02+00:00
Added them on both to make sure these docs are seen.

## hinto-janai | 2024-11-11T22:07:28+00:00
This is small but I think overflows in these types of cases usually saturate in most programs instead of crashing for UX reasons (better to saturate on already unrealistic and irrelevantly large number over crashing on said irrelevant number).

User input causing overflow is unrealistic yet possible if someone does this in the CLI/config:
```
max_inbound_connections = 99999999999999999999
```
which I could imagine at least a few people doing. I think `cuprated` continuing with `MAX` over crashing would be expected, or... `monerod` catches large input during CLI/config in stuff like `--out-peers`, `--limit-rate`, etc.

```
$ ./monerod --out-peers 18446744073709551615
Failed to parse arguments: the argument ('18446744073709551615') for option '--out-peers' is invalid
```
If we're either saturating or early returning during CLI, these `unwrap`s can be removed.

## hinto-janai | 2024-11-11T22:12:36+00:00
Okay but these are now duplicated and can get out of sync. 

Reasoning for comment on the response is that the return type is next to it. Other crates are like this, consensus, address-book, blockchain, txpool, etc.

## Boog900 | 2024-11-20T01:37:47+00:00
Saturating these values will cause memory exhaustion as the channel allocates  I'll keep this as a TODO to add limits when parsing the config.

# Action History
- Created by: Boog900 | 2024-11-03T20:37:03+00:00
- Merged at: 2024-11-20T01:37:52+00:00
