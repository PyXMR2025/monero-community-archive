---
title: 'Blockchain: add alt-block handling '
source_url: https://github.com/Cuprate/cuprate/pull/260
author: Boog900
assignees: []
labels:
- A-test-utils
- A-dependency
- A-workspace
- A-storage
- A-helper
- A-types
created_at: '2024-08-29T17:47:41+00:00'
updated_at: '2024-09-19T15:55:31+00:00'
type: pull_request
status: merged
closed_at: '2024-09-19T15:55:28+00:00'
merged_at: '2024-09-19T15:55:28+00:00'
---

# Original Description
### What

Adds alt block handling to the blockchain storage.

### How
Follows #194


# Discussion History
## Boog900 | 2024-09-08T17:33:27+00:00
I moved this function from `test-utils` as it's also now needed in the DB

## Boog900 | 2024-09-08T17:37:14+00:00
Here is some information on how alt-blocks are stored 

## Boog900 | 2024-09-08T17:38:17+00:00
I also removed this as we panic if the same block gets passed in again 

## Boog900 | 2024-09-08T17:39:13+00:00
I also changed `pop_block` to add the block to the alt-blocks if a `ChainId` is specified 

## hinto-janai | 2024-09-10T00:18:57+00:00
Do you mean doing opt-in features? All modules off by default?

If so, sounds good.

## hinto-janai | 2024-09-10T00:22:14+00:00
Added by mistake I think.

## hinto-janai | 2024-09-10T00:22:37+00:00
```suggestion
    FindFirstUnknown(Vec<[u8; 32]>),

    /// A request for all alt blocks in the chain with the given [`ChainId`].
    AltBlocksInChain(ChainId),
```

## hinto-janai | 2024-09-10T00:23:20+00:00
Spacing between variants helps when there's many variants + comments IMO.

## hinto-janai | 2024-09-10T00:26:25+00:00
Can you add to the test in `storage/blockchain/src/service/tests.rs` that asserts these request/response pairs?

## hinto-janai | 2024-09-10T00:29:24+00:00
Is there a reason why this can't be `u64` where `0` represents `Main`?

## hinto-janai | 2024-09-10T00:32:38+00:00
```suggestion
//! unlikely that we will be tracking [`ChainID`](cuprate_types::ChainId)s that don't immediately connect to the main-chain.
```

## hinto-janai | 2024-09-10T00:33:01+00:00
```suggestion
//! ## Why not use the block's `previous` field?
```

## hinto-janai | 2024-09-10T00:35:56+00:00
```suggestion
//! Although that would be easier, it makes getting a range of block extremely slow, as we have to build the weight cache to verify
//! blocks, roughly 100,000 block headers needed, this cost is too high.
```
nit - not really important:

"was seen as" implies some discussion (by multiple people) lead to the conclusion.

## hinto-janai | 2024-09-10T00:38:42+00:00
nit: I'd prefer no code in `mod.rs` files.

## hinto-janai | 2024-09-10T00:39:56+00:00
```suggestion
/// Adds a [`VerifiedTransactionInformation`] from an alt-block
/// if it is not already in the DB.
```

## hinto-janai | 2024-09-10T00:43:19+00:00
Why/how could an alternate transaction be in the normal transaction table?

## hinto-janai | 2024-09-10T00:44:31+00:00
`_utils` seems redundant, I think it can just be `cuprate_helper::tx`?

## hinto-janai | 2024-09-10T00:49:31+00:00
This should only allocate if `move_to_alt_chain.is_some()`, else `Vec::new()`.

## hinto-janai | 2024-09-10T00:51:11+00:00
Your editor... it always cuts off at the worse places
```suggestion
                // We know the PoW is valid for this block so just set it so it will always verify as valid.
```

## hinto-janai | 2024-09-10T00:53:29+00:00
```suggestion
/// Generate `# Invariant` documentation for internal alt block `fn`'s
/// that should be called directly with caution.
///
/// This is pretty much the same as [`doc_add_block_inner_invariant`],
/// it's not worth the effort to reduce the duplication.
```

## hinto-janai | 2024-09-10T00:58:55+00:00
```suggestion
    // FIXME: turn this function into a try block once stable.
```

## hinto-janai | 2024-09-10T01:00:54+00:00
```suggestion
    // FIXME: turn this function into a try block once stable.
```

## hinto-janai | 2024-09-10T01:01:51+00:00
```suggestion
        let top_block_height =
            crate::ops::blockchain::top_block_height(tables_mut.block_heights())?;

        // pop any blocks that were added as part of a re-org.
        for _ in chain_info.common_ancestor_height..top_block_height {
```

## hinto-janai | 2024-09-10T01:02:09+00:00
Also, shouldn't this be `..=`?

## hinto-janai | 2024-09-10T01:04:07+00:00
```suggestion
    /// Alt-block blobs.
    ///
    /// Contains the raw bytes of all alt-blocks.
    18 => AltBlockBlobs,
    AltBlockHeight => BlockBlob,

    /// Alt-block transaction blobs.
    ///
    /// Contains the raw bytes of alt transactions, if those transactions are not in the main-chain.
    19 => AltTransactionBlobs,
    TxHash => TxBlob,

    /// Alt-block transaction information.
```

## hinto-janai | 2024-09-10T01:11:53+00:00
```suggestion
impl From<RawChainId> for ChainId {
    fn from(value: RawChainId) -> Self {
        Self(NonZero::new(value.0).expect("RawChainId cannot have a value of `0`"))
    }
}
```

## Boog900 | 2024-09-15T17:16:51+00:00
`common_ancestor_height` is one less than the range start so by using the `top_block_height` as the end it makes the count correct, a bit confusing but should be ok.

## Boog900 | 2024-09-15T17:47:53+00:00
An alt block is a block that was mined at the same height as another block, if we add a block to the chain and then another (alt)block gets broadcasted at the same height then they could share txs

## Boog900 | 2024-09-15T17:48:24+00:00
It's such a good place for this function though :) where would you prefer it?

## Boog900 | 2024-09-15T17:49:09+00:00
It could but it would change what this type represents and IMO would be less Rusty

## Boog900 | 2024-09-16T01:48:36+00:00
I added a test that tests all the new requests 

## hinto-janai | 2024-09-16T19:53:17+00:00
`block.rs` seems appropriate.

## hinto-janai | 2024-09-16T19:53:46+00:00
```suggestion
mod block;
mod chain;
mod tx;

pub use block::{
    add_alt_block, get_alt_block, get_alt_block_extended_header_from_height, get_alt_block_hash,
};
pub use chain::{get_alt_chain_history_ranges, update_alt_chain_info};
pub use tx::{add_alt_transaction_blob, get_alt_transaction};
```

## hinto-janai | 2024-09-16T19:58:01+00:00
```suggestion
tx        = ["dep:monero-serai"]
```

## hinto-janai | 2024-09-16T19:58:12+00:00
```suggestion
#[cfg(feature = "tx")]
pub mod tx;
```

## hinto-janai | 2024-09-16T20:10:03+00:00
Asserts the panic doc on `tx_fee`.

`Cargo.toml` needs:
```rust
[dev-dependencies]
curve25519-dalek = { workspace = true }
```

```suggestion
}

#[cfg(test)]
mod test {
    use curve25519_dalek::{edwards::CompressedEdwardsY, EdwardsPoint};
    use monero_serai::transaction::{NotPruned, Output, Timelock, TransactionPrefix};

    use super::*;

    #[test]
    #[should_panic(expected = "called `Option::unwrap()` on a `None` value")]
    fn tx_fee_panic() {
        let input = Input::ToKey {
            amount: Some(u64::MAX),
            key_offsets: vec![],
            key_image: EdwardsPoint::default(),
        };

        let output = Output {
            amount: Some(u64::MAX),
            key: CompressedEdwardsY::default(),
            view_tag: None,
        };

        let tx = Transaction::<NotPruned>::V1 {
            prefix: TransactionPrefix {
                additional_timelock: Timelock::None,
                inputs: vec![input; 2],
                outputs: vec![output],
                extra: vec![],
            },
            signatures: vec![],
        };

        tx_fee(&tx);
    }
}
```

## hinto-janai | 2024-09-16T20:24:44+00:00
`put()` might return `Result<Option<OLD_VALUE>, Error>` in the future.

```suggestion
    tables
        .alt_transaction_blobs_mut()
        .put(&tx.tx_hash, StorableVec::wrap_ref(&tx.tx_blob))?;

    Ok(())
```

## hinto-janai | 2024-09-16T20:44:14+00:00
Ok, I see what this `Vec` is for now. Here's an iterator version that only branches once on `move_to_alt_chain`:

```rust
remove_tx(&block.miner_transaction.hash(), tables)?;

let remove_tx_iter = block.transactions.iter().map(|tx_hash| {
    let (_, tx) = remove_tx(tx_hash, tables)?;
    Ok::<Transaction, RuntimeError>(tx)
});

if let Some(chain_id) = move_to_alt_chain {
    let txs = remove_tx_iter
        .map(|result| {
            let tx = result?;
            Ok(VerifiedTransactionInformation {
                tx_weight: tx.weight(),
                tx_blob: tx.serialize(),
                tx_hash: tx.hash(),
                fee: tx_fee(&tx),
                tx,
            })
        })
        .collect::<Result<Vec<VerifiedTransactionInformation>, RuntimeError>>()?;

    alt_block::add_alt_block(
        &AltBlockInformation {
            block: block.clone(),
            block_blob,
            txs,
            block_hash: block_info.block_hash,
            // We know the PoW is valid for this block so just set it so it will always verify as valid.
            pow_hash: [0; 32],
            height: block_height,
            weight: block_info.weight,
            long_term_weight: block_info.long_term_weight,
            cumulative_difficulty: combine_low_high_bits_to_u128(
                block_info.cumulative_difficulty_low,
                block_info.cumulative_difficulty_high,
            ),
            chain_id,
        },
        tables,
    )?;
} else {
    for result in remove_tx_iter {
        drop(result?);
    }
}

Ok((block_height, block_info.block_hash, block))
```

## hinto-janai | 2024-09-16T20:53:17+00:00
Can we create a well defined `cuprate_traits` crate (or add traits to `cuprate_types`) that encapsulates behavior across slightly different common types? I.e.:

```rust
trait Block {
    fn height(&self) -> u64;
    fn tx_fees(&self) -> u64;

    // other common functions
}

impl Block for monero_serai::Block {/*...*/} 
impl Block for AltBlockInformation {/*...*/}
impl Block for VerifiedBlockInformation {/*...*/}
```
it would reduce mappings between types (this has to be at least 50% of where my time is spent) and duplication of lines like this one.

## hinto-janai | 2024-09-16T21:03:29+00:00
nit: `.map().flatten()` -> `.flat_map()`.

[This lint](https://rust-lang.github.io/rust-clippy/master/index.html#/map_flatten) would trigger here but it isn't because it's `rayon`'s iter functions (I think).

## hinto-janai | 2024-09-16T21:08:38+00:00
We should probably do this now that there's >1 write function.

`const` can be defined somewhere in this file, probably right after `use` statements.

```rust
/// Write functions within this module abort if the write transaction
/// could not be aborted successfully to maintain atomicity.
///
/// This is the panic message if the `abort()` fails.
const TX_RW_ABORT_FAIL: &str =
    "Could not maintain blockchain database atomicity by aborting write transaction";
```
```suggestion
            TxRw::abort(tx_rw).expect(TX_RW_ABORT_FAIL);
```

## hinto-janai | 2024-09-16T21:15:40+00:00
It seems wrong to do direct DB mutation within `service`. Shouldn't this `put()` be in `crate::ops::block::pop_block` anyway?

## hinto-janai | 2024-09-16T21:19:16+00:00
Is this comment true?
```suggestion
        // Add the old main chain blocks back to the main chain.
        for height in (chain_info.common_ancestor_height + 1)..chain_info.chain_height {
            let alt_block = crate::ops::alt_block::get_alt_block(
                &AltBlockHeight {
                    chain_id: chain_id.into(),
                    height,
                },
                &tables_mut,
            )?;

            let verified_block = map_valid_alt_block_to_verified_block(alt_block);

            crate::ops::block::add_block(&verified_block, &mut tables_mut)?;
        }
```



## hinto-janai | 2024-09-16T21:25:49+00:00
Is there a reason things are mostly `usize` instead of `u64`? Here but also across the codebase in general, e.g. DB service messages.

They are required to be `u64` elsewhere (especially in RPC) and these `usize`s permeate and cause `usize_to_u64()` to be everywhere. I understand indices but these aren't that. Can we start using `u64` instead?

## hinto-janai | 2024-09-16T21:27:31+00:00
```suggestion
cuprate-helper            = { path = "../helper", features = ["map", "tx"] }
```

## hinto-janai | 2024-09-16T21:29:36+00:00
`alt_block/block.rs` to be specific

## Boog900 | 2024-09-18T21:00:55+00:00
I added some logic to `update_alt_chain_info` to handle adding blocks in reverse 

## Boog900 | 2024-09-18T21:02:35+00:00
oops, I retrieved the alt_block in an iterator, this is a better solution though 

## Boog900 | 2024-09-18T21:06:44+00:00
It's a trade of between what type is needed more, I used to use `u64`s everywhere but I changed it here: #201 and I do remember there being an improvement.

This is a weight which is often calculated by getting the size of a Vec, which is given in a `usize`. Plus you could change the JSON types to be `usize`s right? 

## Boog900 | 2024-09-18T21:21:47+00:00
IMHO I don't think this would be helpful. When I map between types I am often mapping for the whole type not for a particular function. Also these types are specialized, for example you can't get `tx_fees` from a `monero_serai::Block`.

I would like some more unified "extra data" tx/block types, but I don't think that would solve the issue you mention.



## Boog900 | 2024-09-18T21:26:08+00:00
Changing this actually removed multiple conversions in the DB

## hinto-janai | 2024-09-19T00:36:08+00:00
The types are defined as 64-bit in `monerod`, I don't think defining public fields within public RPC types as `usize` is a good tradeoff. Probably won't affect many machines but not worth it IMO. I'm okay with using the cast functions if these need to be `usize` elsewhere.

## hinto-janai | 2024-09-19T00:46:36+00:00
These invariant comments (all of them) should be removed since the `const` documents the panic reasoning now.

# Action History
- Created by: Boog900 | 2024-08-29T17:47:41+00:00
- Merged at: 2024-09-19T15:55:28+00:00
