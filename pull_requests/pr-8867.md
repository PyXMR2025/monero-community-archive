---
title: Replace serialization functions
source_url: https://github.com/monero-project/monero/pull/8867
author: vtnerd
assignees: []
labels:
- pending review
- serialization
created_at: '2023-05-20T22:52:23+00:00'
updated_at: '2026-08-29T08:31:25+00:00'
type: pull_request
status: open
closed_at: null
merged_at: null
---

# Original Description
This replaces all serialization functions (epee binary, epee json, and zmq json), except the custom binary formats for transaction and block. The design is a DOMless vtable interface for reading and writing (2 separate interfaces). The base vtable class can be used to support multiple formats with minimal executable size OR the derived class can be used directly for performance. The design re-uses existing macros, although the newer macros should be lighter on compiler resources.

Memory consumption issues are handled by having all arrays provide a constraint - either a max element count or a min size per element (i.e. `std::vector<std::string>` ideally has a min size of `sizeof(std::string)` per element to prevent memory issues on unpack).

Most importantly, reading epee binary is ~81% faster and writing epee binary is ~56% faster (I re-used the benchmark by @jeffro256 on a ryzen 3 chip). This is primarily due to the removal of the DOM entirely - everything is done on the objects directly.

Unfortunately this diff is huge, and probably has no chance of ever being merged as-is. So I will try to break things into smaller chunks, with the caveat that if nobody wants the serialization to be replaced, its best to vote within this discussion thread here.

# Discussion History
## iamamyth | 2023-11-07T00:42:28+00:00
Botched rebase?

## vtnerd | 2023-11-07T19:24:13+00:00
Pushed an update to fix this merge issue.

## jeffro256 | 2023-11-09T17:08:23+00:00
Is `negative` supposed to be set here?

## jeffro256 | 2023-11-09T17:14:07+00:00
I guess it doesn't matter if you only use `rapidjson_sax` exactly once 

## vtnerd | 2023-11-09T18:10:10+00:00
No, because the actual value is not negative.

## thomasbuilds | 2026-08-29T07:58:51+00:00
This parses recursively with no depth limit, and `skip_value()` sends unknown fields through it. One anonymous `POST /get_info` with a 350 KB body of the form `{"x":[[[...]]]}` (depth 175000) segfaults the daemon; master rejects the same body cleanly. The nesting has to sit in a field value, a bare `[[[...]]]` body is rejected by `do_start_object` first.

```suggestion
    if (!reader_.Parse<rapidjson::kParseStopWhenDoneFlag | rapidjson::kParseIterativeFlag>(istream, handler))
```

Against the vendored rapidjson, depth 200000 segfaults as written and 2000000 parses cleanly with the flag, successive `Parse` calls still stopping after each value. The iterative parser accepts any depth rather than rejecting it, so `max_read_depth()` on the skip path is still worth adding.

## thomasbuilds | 2026-08-29T07:58:51+00:00
This path sends no reply on a REP socket, so the next `receive` fails with EFSM, `MONERO_THROW` propagates past the loop, and `serve()` exits, leaving ZMQ-RPC dead for all clients until restart. `{"jsonrpc":"2.0","method":"get_height"}` reaches it: `handle_message` returns a bare error code because `params` is absent, so nothing is sent. (Dropping `jsonrpc` instead fails earlier in `request_generic` and does get a `BAD_JSON` reply, so the request has to be well formed to hit this.)

## thomasbuilds | 2026-08-29T07:58:51+00:00
`remaining_added_pool_txids` is missing from this map. It is still declared at :242, still filled in `core_rpc_server.cpp:688`, and still read by wallet2 at `wallet2.cpp:3144-3161`, so those mempool transactions never reach the wallet.

```suggestion
        KV_SERIALIZE_ARRAY(added_pool_txs, min_pool_tx_info) // optional if empty
        KV_SERIALIZE_CONTAINER_POD_AS_BLOB(remaining_added_pool_txids) // optional if empty
        KV_SERIALIZE_CONTAINER_POD_AS_BLOB(removed_pool_txids) // optional if empty
```

## thomasbuilds | 2026-08-29T07:58:51+00:00
`wallet2::import_key_images` (:13199) sends every signed key image in one request (:13261) and asserts the response size matches (:13263), so a wallet with more than 4096 outputs fails to import: 4096 is accepted, 4097 and 5000 return 400, master accepts 5000. `rescan_spent` chunks at 1000.

## thomasbuilds | 2026-08-29T07:58:51+00:00
`binary` defaults to true, so `get_output_distribution` over JSON-RPC reaches this throw mid-write and returns 200 with an empty body. `binary:false` works.

## thomasbuilds | 2026-08-29T07:58:52+00:00
`do_start_array` charges `count * min_element_size` against the remaining buffer while each blob costs `1 + length` bytes on the wire, so `<41>` enforces a 40-byte payload minimum. `too_small_txs_blobs` and `too_small_compact_txs_blobs` build 40-byte blobs and expect a decode error, so both fail on all three test jobs, while the `smallest_*` pair at 41 passes. Raising the constant to 42 would restore the intended boundary, but those two sit close to the limit, the compact one also paying the outer array's charge against `array_space_`, so they want re-running with any new value.

## thomasbuilds | 2026-08-29T07:58:52+00:00
Following the 2023 empty-body thread, measured at bfaebdd: this branch returns 400 on an empty body for 18 of the 29 request types behind the direct endpoints, while master accepts an empty body on all 28 it has (master has no direct `/set_bootstrap_daemon`). The 18 are `/get_transactions`, `/get_alt_blocks_hashes`, `/is_key_image_spent`, `/send_raw_transaction`, `/start_mining`, `/get_peer_list`, `/get_public_nodes`, `/set_log_hash_rate`, `/set_log_level`, `/set_log_categories`, `/get_transaction_pool`, `/set_limit`, `/out_peers`, `/in_peers`, `/get_outs`, `/update`, `/pop_blocks`, `/set_bootstrap_daemon`. The same holds for `{}` where a field is required, master defaults it and this branch throws `missing_key` (`/set_log_level` without `level`, `/get_outs` without `outputs`, `/out_peers` without `out_peers`). Is that the intended end state?

# Action History
- Created by: vtnerd | 2023-05-20T22:52:23+00:00
