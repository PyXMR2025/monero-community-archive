---
title: 'wallet: fix wallet_keys_unlocker'
source_url: https://github.com/monero-project/monero/pull/9860
author: jeffro256
assignees: []
labels:
- wallet
- bugfix
created_at: '2025-03-23T21:20:57+00:00'
updated_at: '2025-09-08T18:08:42+00:00'
type: pull_request
status: merged
closed_at: '2025-09-08T18:08:42+00:00'
merged_at: '2025-09-08T18:08:42+00:00'
---

# Original Description
Fix four issues with `wallet_keys_unlocker`:
1. It won't decrypt if there are unlockers open simulataneously on multiple `wallet2` instances
2. It won't decrypt if the first unlocker was disabled (i.e. `locked=false`), even with a second non-disabled unlocker
3. If a destructor of an earlier unlocker is triggered before the destructor of a later unlocker, it will re-encrypt too early, while the second unlocker is still in scope
4. ~~We shouldn't attempt to encrypt/decrypt for any background wallets, even if not currently syncing~~
5. Calling the 3-parameter constructor with `locked=true` after an unlocker already exists will "double-encrypt"  the spend key after its destructor since the local variable is `locked=false` and the field member is `locked=true`

Also, replace `wallet2::decrypt_account_for_multisig` with `wallet_keys_unlocker`.

To test out the unit tests against the old code, run:
```
git checkout master
git checkout -B test_9860
git checkout 3808431c2e9aead23206cf446617c70d0f6933eb -- tests/unit_tests/wallet_storage.cpp
```

In the file `tests/unit_tests/wallet_storage.cpp`, set the macro `OLD_WALLET_KEYS_UNLOCKER` to `1`, then compile and run unit tests:

```
make -j$(nproc)
./build/Linux/test_9860/release/tests/unit_tests/unit_tests --gtest_filter=wallet_keys_unlocker.*
```

You should see that the tests mostly all fail without the `wallet_keys_unlocker` changes.

# Discussion History
# Action History
- Created by: jeffro256 | 2025-03-23T21:20:57+00:00
- Merged at: 2025-09-08T18:08:42+00:00
