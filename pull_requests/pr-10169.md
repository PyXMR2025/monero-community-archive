---
title: 'functional_tests: Python epee binary lib and test `/get_blocks.bin`'
source_url: https://github.com/monero-project/monero/pull/10169
author: jeffro256
assignees: []
labels:
- pending review
- tests
created_at: '2025-10-14T20:37:40+00:00'
updated_at: '2026-07-18T03:36:44+00:00'
type: pull_request
status: open
closed_at: null
merged_at: null
---

# Original Description
Functional test `blockchain` will pass after #10652  is merged (with requested edits).

# Discussion History
## selsta | 2026-05-30T21:09:59+00:00
```suggestion
        if isinstance(x[0], bool):
```

## selsta | 2026-05-30T21:10:46+00:00
```suggestion
        if len(x) == 0:
            EMPTY_U8_ARRAY_FLAG = SERIALIZE_FLAG_ARRAY | SERIALIZE_TYPE_UINT8
            self.__serialize_fixed_int(EMPTY_U8_ARRAY_FLAG, signed = False, byte_size = 1, include_type = False) #type
            self.__serialize_varint(0) # length
            return
```

## selsta | 2026-05-30T21:15:28+00:00
```suggestion
        if signed:
            sign_bit = 1 << (byte_size * 8 - 1)
            if val & sign_bit:
                val -= 1 << (byte_size * 8)
        return val
```

## selsta | 2026-05-30T21:20:19+00:00
```suggestion
                signed, byte_size = self.__get_auto_int_properties(x)
                self.__serialize_fixed_int(x, signed, byte_size, include_type)
```

and remove

```__serialize_fixed_int_auto```

## selsta | 2026-05-30T21:23:27+00:00
```suggestion
class DaemonBinary:
    @staticmethod
    def hash_list_to_blob(hash_list):
```

## selsta | 2026-06-14T20:37:06+00:00
```suggestion
            self.__serialize_section(x, include_type)
```

or is it intentionally set to True?

## selsta | 2026-06-14T20:42:12+00:00
```suggestion
    def __serialize_string(self, x, include_type):
        if isinstance(x, str):
            x = x.encode()

        if include_type:
            self.outf.write(bytes([SERIALIZE_TYPE_STRING]))

        self.__serialize_varint(len(x))
        self.outf.write(x)
```

Not relevant for get_blocks test but more correct

## selsta | 2026-06-14T20:45:09+00:00
```suggestion
    @classmethod
    def __get_auto_int_properties(cls, x):
        signed = x < 0
        sizes = (1, 2, 4, 8)

        for byte_size in sizes:
            bits = byte_size * 8
            if signed:
                if -(1 << (bits - 1)) <= x <= (1 << (bits - 1)) - 1:
                    return True, byte_size
            else:
                if 0 <= x <= (1 << bits) - 1:
                    return False, byte_size

        raise ValueError("Integer too large to serialize")
```

More robust and supports 8 byte integer

## selsta | 2026-06-14T20:51:05+00:00
```suggestion
    @classmethod
    def __is_maybe_array_like(cls, x):
        return isinstance(x, Sequence) \
            and not isinstance(x, (str, bytes, bytearray)) \
            and not cls.__is_maybe_dict_like(x)
```

also needs

`from collections.abc import Sequence`

## selsta | 2026-06-14T20:51:25+00:00
unused, should this assert something?

## jeffro256 | 2026-07-01T17:07:11+00:00
Probably a copy-and-paste artifact 

## selsta | 2026-07-01T20:22:13+00:00
```suggestion
        N_TO_MINE = 20 + max(0, first_recv_height + 60 - n_blocks)
```

```
Testing the /get_blocks.bin RPC endpoint
First, mining to wallet and creating 2 transactions in the top block
Traceback (most recent call last):
  File "/Users/selsta/dev/monero/tests/functional_tests/blockchain.py", line 448, in <module>
    BlockchainTest().run_test()
    ~~~~~~~~~~~~~~~~~~~~~~~~~^^
  File "/Users/selsta/dev/monero/tests/functional_tests/blockchain.py", line 55, in run_test
    self.test_get_blocks_fast()
    ~~~~~~~~~~~~~~~~~~~~~~~~~^^
  File "/Users/selsta/dev/monero/tests/functional_tests/blockchain.py", line 393, in test_get_blocks_fast
    res = wallet.transfer([dst])
  File "/Users/selsta/dev/monero/utils/python-rpc/framework/wallet.py", line 62, in transfer
    return self.rpc.send_json_rpc_request(transfer)
           ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~^^^^^^^^^^
  File "/Users/selsta/dev/monero/utils/python-rpc/framework/rpc.py", line 77, in send_json_rpc_request
    return self.send_request("/json_rpc", inputs, 'result')
           ~~~~~~~~~~~~~~~~~^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/Users/selsta/dev/monero/utils/python-rpc/framework/rpc.py", line 69, in send_request
    assert 'error' not in res, res
           ^^^^^^^^^^^^^^^^^^
AssertionError: {'error': {'code': -19, 'message': 'not enough outputs to use Please use sweep_dust.'}, 'id': '0', 'jsonrpc': '2.0'}
[TEST FAILED] blockchain
```

## selsta | 2026-07-01T20:22:41+00:00
I double checked, I think this should be without `=True`.

## selsta | 2026-07-01T20:32:29+00:00
```python
        print('Calling /get_blocks.bin (blocks and incremental pool, no new blocks) and testing response...')

        time.sleep(1)
        res = daemon.get_blocks_fast(0, [], requested_info = 2)
        assert res.pool_info_extent == 2
        pool_info_since = res.daemon_time

        wallet.refresh()
        pending_txid = wallet.transfer([dst]).tx_hash
        current_height = daemon.get_height().height

        res = daemon.get_blocks_fast(
            current_height,
            [],
            requested_info = 1,
            pool_info_since = pool_info_since)
        assert not res.get('blocks')
        assert res.pool_info_extent == 1
        assert [tx.tx_hash.hex() for tx in res.added_pool_txs] == [pending_txid]

        daemon.flush_txpool([pending_txid])
```

Test for https://github.com/monero-project/monero/pull/10761/

## jeffro256 | 2026-07-02T00:45:58+00:00
Thank you

# Action History
- Created by: jeffro256 | 2025-10-14T20:37:40+00:00
