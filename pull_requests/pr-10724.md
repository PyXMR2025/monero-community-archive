---
title: 'FCMP++: curve tree builder'
source_url: https://github.com/monero-project/monero/pull/10724
author: j-berman
assignees: []
labels:
- pending review
- fcmp++
created_at: '2026-06-05T10:27:14+00:00'
updated_at: '2026-08-26T11:44:00+00:00'
type: pull_request
status: open
closed_at: null
merged_at: null
---

# Original Description
Builds on top of:
  - #10362

Introduces the `get_tree_extension` function used to "extend" an existing (or empty) curve tree. This is the core tree building function. It accepts as params the number of leaves already in the tree, the current tree edge (the last hashes in every layer), and the new outputs to add to the tree. It uses those params to build a struct that can be used to extend the tree.

This PR also includes a unit test that constructs a tree in memory, extends it, and audits the result. It tests various tree structure combinations in an effort to capture as many edge cases as possible.

For more context on the tree and its place in FCMP++, see [these WIP docs](https://github.com/j-berman/monero/blob/7c6205403ed88924d48eb0dfb0a71140cd7902bb/docs/FCMP%2B%2B_INTEGRATION.md#2-curve-trees-merkle-tree) ([source PR](https://github.com/seraphis-migration/monero/pull/110)).

# Discussion History
## UkoeHB | 2026-06-08T19:14:13+00:00
This comment makes no sense. Why would you be hashing the root layer unless you were making a new root layer that's even higher?

Is it supposed to say this?
```
// If the existing root layer will become a middle layer,
// we need to include the existing root as a normal layer node.
```

## UkoeHB | 2026-06-08T19:47:36+00:00
Need to check/assert `chunk_size == new_child_scalars.size()` or just get it from len instead of passing in.

## UkoeHB | 2026-06-08T19:54:51+00:00
Also here, an assert that `chunk_size` correctly aligns with `new_child_scalars.size() + chunk_start_idx`.

## UkoeHB | 2026-06-08T20:53:22+00:00
Technically `adding_members_to_existing_last_chunk` is a little ambiguous/confusing when the last chunk's last member will change. But that case is implicitly covered by the following `need_old_last_child` check.

Nothing to change, just taking note.

## j-berman | 2026-06-08T21:03:09+00:00
Yep, that's what I was trying to say there

## UkoeHB | 2026-06-09T19:25:45+00:00
Add an assert that `leaf_layer_chunk_width % leaf_tuple_size == 0`.

## UkoeHB | 2026-06-09T19:44:28+00:00
```suggestion
    const std::vector<LayerExtension<C_CHILD>> &child_layer_extensions,
```

## UkoeHB | 2026-06-10T01:13:21+00:00
```suggestion
    // Calculate the number of elems in each layer of the tree based on the number of leaf tuples.
    // Doesn't include the leaf layer.
```

## UkoeHB | 2026-06-10T01:13:34+00:00
```suggestion
    // Calculate how many layers in the tree there are based on the number of leaf tuples.
    // Doesn't include the leaf layer.
```

## UkoeHB | 2026-06-10T01:23:51+00:00
```suggestion
    const std::size_t active_child_layer_idx,
    const std::size_t active_parent_layer_idx)
```
The 'last updated' semantics are confusing and overly-complicated.

## UkoeHB | 2026-06-10T01:31:42+00:00
Didn't know this

## UkoeHB | 2026-06-10T01:44:38+00:00
Not sure if the compiler will always optimize out these `.size()` calls, which need to be evaluated every loop.

Might be inconsequential here, just pointing it out.

## j-berman | 2026-06-10T02:47:39+00:00
Ya other sections of the code actually use `std::deque<bool>` to work around this ([example](https://github.com/monero-project/monero/blob/17891e91575a3350e769c1c28aa0dc5d9404c2d2/src/ringct/rctSigs.cpp#L1501))

## thomasbuilds | 2026-08-26T11:36:02+00:00
```suggestion
        const bool parent_is_c2,
```

## thomasbuilds | 2026-08-26T11:37:07+00:00
```suggestion
std::shared_ptr<CurveTreesV1> init_curve_trees_test(const std::size_t selene_chunk_width,
    const std::size_t helios_chunk_width,
```

## thomasbuilds | 2026-08-26T11:37:35+00:00
```suggestion
```

## thomasbuilds | 2026-08-26T11:38:12+00:00
```suggestion
```

## thomasbuilds | 2026-08-26T11:39:25+00:00
```suggestion
```

## thomasbuilds | 2026-08-26T11:40:52+00:00
Optional: rejects a LastHashes whose size does not match the tree up front instead of only when a hash happens to be needed. The callers in fcmp++-stage build one hash per layer and grow_tree passes with it.

```suggestion
    tree_extension.leaves.start_idx = old_n_leaf_tuples;

    CHECK_AND_ASSERT_THROW_MES((existing_last_hashes.c1_last_hashes.size() + existing_last_hashes.c2_last_hashes.size())
        == this->n_layers(old_n_leaf_tuples), "unexpected number of last hashes");
```

## thomasbuilds | 2026-08-26T11:42:27+00:00
chunk_size here is min(new_child_scalars.size(), chunk_width - start_offset) so it only equals new_child_scalars.size() when everything fits in the first chunk. An assert needs <=.

## thomasbuilds | 2026-08-26T11:43:48+00:00
With these widths the largest extension is 19 chunks so HASH_BATCH_SIZE > 1 (several chunks per tpool task) is only reached with fewer than 10 hardware threads. One extension of a few thousand leaves at prod widths would pin that path.

# Action History
- Created by: j-berman | 2026-06-05T10:27:14+00:00
