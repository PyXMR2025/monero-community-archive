---
title: 'CI: add cargo hack'
source_url: https://github.com/Cuprate/cuprate/pull/170
author: Boog900
assignees: []
labels:
- A-p2p
- A-dependency
- A-workspace
- A-docs
- A-storage
- A-helper
- A-net
- A-pruning
- A-types
- A-ci
- A-rpc
- A-binaries
created_at: '2024-06-18T21:02:23+00:00'
updated_at: '2024-11-01T20:22:18+00:00'
type: pull_request
status: merged
closed_at: '2024-11-01T20:22:15+00:00'
merged_at: '2024-11-01T20:22:15+00:00'
---

# Original Description
adds cargo hack to CI and fixes errors found.



# Discussion History
## Boog900 | 2024-06-27T00:47:38+00:00
`cuprate-database` does not build with `--no-default-features` and I don't think there is a way to fix this without hot swapable backends, so another reason for that :)

## hinto-janai | 2024-06-27T13:47:28+00:00
Should we set `cuprate-database` to always pull in `heed` by default? Then if another backend feature is detected, that is used instead, although `heed` will still be built (but not used).

## hinto-janai | 2024-06-27T13:47:31+00:00
```suggestion
| `cargo hack --workspace --exclude cuprate-database check --feature-powerset --no-dev-deps` | Uses `cargo hack` to check our crates build with different features set |

`cargo hack` can be installed with `cargo` from: https://github.com/taiki-e/cargo-hack.

**Note: in order for some tests to work, you will need to place a [`monerod`](https://www.getmonero.org/downloads/) binary at the root of the repository.**
```

## hinto-janai | 2024-06-27T13:50:56+00:00
1. Windows and macOS should be added too
2. `${{ matrix.os }}` is an empty variable since no matrix is setup

It may be worth putting this in `ci.yml` which already has all the Windows workarounds / dependency stuff.

## Boog900 | 2024-06-30T13:04:44+00:00
We could, initially I didn't want to, but thinking about it more, with hot-swapable backends both `heed` & `redb` would need to be pulled in all the time as well.

I'll add this

## Boog900 | 2024-06-30T13:11:32+00:00
ah that's what I get for blindly copying another workflow as a template :p

This workflow takes awhile to complete (~7 mins), so I don't really want to add it as another step to `ci`, I think just testing linux should be enough, we already test full builds with Windows/ MacOS.

If we do add Windows/ MacOS support, it may make sense to separate the `ci` setup into a separate action so that both the `ci` and `hack` workflows can use it.

## hinto-janai | 2024-10-31T00:43:33+00:00
Could you import this and use it here? It gets used a lot below:
```rust
#[cfg(feature = "epee")]
use crate::misc::PoolInfoExtent;
```

## hinto-janai | 2024-10-31T00:49:55+00:00
Why was this signature changed? It can error because it uses https://doc.cuprate.org/cuprate_epee_encoding/fn.write_varint.html.

## hinto-janai | 2024-10-31T00:58:37+00:00
It's unfortunate these doc links are being removed. Have you considered pulling `tower` in for this since this module pulls in `cuprate-database-service` and that pulls in `tower` anyway?

## hinto-janai | 2024-10-31T00:59:59+00:00
I'm not sure if this is exactly right but would something like this work?
```rust
#[cfg(all(feature = "hex", feature = "serde"))]
use hex as _;
```

## Boog900 | 2024-11-01T00:13:38+00:00
No this uses a different varint format: https://github.com/Cuprate/cuprate/pull/229#discussion_r1690531904

## Boog900 | 2024-11-01T00:19:02+00:00
It would but IMO just enabling serde here is better as the enabled type literally only makes sense with serde 

## hinto-janai | 2024-11-01T13:55:28+00:00
```suggestion
# Used in docs: <https://github.com/Cuprate/cuprate/pull/170#discussion_r1823644357>.
use tower as _;
```

## hinto-janai | 2024-11-01T13:59:32+00:00
Why was this feature was removed? Should `cuprate-blockchain` do the same?

Anyway some docs should be updated:
- https://doc.cuprate.org/cuprate_txpool/#feature-flags
- https://doc.cuprate.org/cuprate_txpool/service/index.html#service



## Boog900 | 2024-11-01T14:49:42+00:00
Because it was annoying to get this to pass CI so I just removed the feature, almost all use cases will use the tower interface anyway, for those that don't I don't think a few extra dependencies would be too bad.

I for parity removed the feature flag from `cuprate-blockchain` as well and updated the docs

# Action History
- Created by: Boog900 | 2024-06-18T21:02:23+00:00
- Merged at: 2024-11-01T20:22:15+00:00
