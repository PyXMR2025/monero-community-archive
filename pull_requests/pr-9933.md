---
title: 'Tx Relay v2: New Relay Logic with threshold-based peer dropping'
source_url: https://github.com/monero-project/monero/pull/9933
author: '0xFFFC0000'
assignees: []
labels:
- pending review
- important
- daemon
created_at: '2025-05-22T10:24:13+00:00'
updated_at: '2026-03-09T17:22:49+00:00'
type: pull_request
status: open
closed_at: null
merged_at: null
---

# Original Description
## Tx Relay v2: Enhanced Transaction Propagation Protocol https://github.com/monero-project/monero/issues/9334

This was made possible thanks to @Boog900’s support in designing and implementing each step.

### Key Improvements & Technical Implementation

#### 1. Inventory-Based Transaction Announcements
**Protocol Messages:**
```cpp
NOTIFY_TX_POOL_INV (ID 2011):
- Broadcasts transaction hashes instead of full TXs
- Contains vector of crypto::hash entries

NOTIFY_REQUEST_TX_POOL_TXS (ID 2012):
- Requests specific TXs by hash
- Enables selective fetching of missing transactions
```

**Workflow**:

- Node advertises new TX hashes via `NOTIFY_TX_POOL_INV`

- Receivers check local mempool & request missing TXs with `NOTIFY_REQUEST_TX_POOL_TXS`

- Only requested transactions are transmitted

**Bandwidth Savings**:

- Reduces initial broadcast size by ~99% (hash vs full TX)

#### Peer Reliability System
Peer Scoring Metrics (`peerinfo_manager`):

- Tracked per connection:
  * Announcements received
  * Successful TX deliveries
  * Missed requests
  * Request/response ratios
  * Timestamps for temporal analysis

- Threshold-based eviction:
  if (missed_requests / total_announcements) > 70%: disconnect_peer()
    
**Failure Detection**:

- Tracks 3 consecutive failures for any TX request

- Uses sliding window (last 30 requests) for fairness

- Penalizes peers for:

  * Unresponsive behavior

  * Invalid transaction responses

#### Components:
```
[Request Manager] <-handles-> [TX Request Queue]
[TX Request Queue] <-tracks-> [Peer UUIDs]
[TX Handler] <-manages-> [Timeouts]
```

#### Configuration

```cpp
 // cryptonote_config.h
#define P2P_DEFAULT_REQUEST_TIMEOUT 5      // Seconds
#define P2P_REQUEST_FAILURE_THRESHOLD 70   // Percentage
```


# Discussion History
## SChernykh | 2025-05-22T13:01:42+00:00
16 MB is 524288 32-byte hashes. Do we really need to request 500k transactions at a time? I think a few thousand will be max, so 1 MB will be more than enough.

## SChernykh | 2025-05-22T13:03:02+00:00
If v2 adds only notify_tx messages, I would call this flag `P2P_SUPPORT_FLAG_NOTIFY_TX`

## nahuhh | 2025-05-22T15:56:47+00:00
Just a note: the default txpool config is something like 648mb, or about max 432k txs

## SChernykh | 2025-05-22T17:26:06+00:00
Maybe this limit makes sense then. But it doesn't have to be this big anyway - peers can send multiple messages if there are too many transactions.

## Boog900 | 2025-05-24T16:11:56+00:00
These messages should contain no where near the number of txs as there are in the pool. I would say 1 MB would be good, if we really wanted to push it we could go with the amount of txs needed to reach the 100 MB packet limit in `NOTIFY_NEW_TRANSACTIONS`: 100_000_000 / 1_500 = 66_666, 66_666 * 32 = 2_133_333. So 2 MB.

`NOTIFY_GET_TXPOOL_COMPLEMENT` which contains the ID of every tx in our pool itself has a 4 MB limit.

## Boog900 | 2025-05-24T16:18:52+00:00
I would say `P2P_SUPPORT_FLAG_TX_RELAY_V2`, the "old" message is called `NOTIFY_NEW_TRANSACTIONS`.

## 0xFFFC0000 | 2025-05-24T16:20:14+00:00
Thanks to everyone participating in this discussion. I used 16MB as placeholders, since the tx-relay-v2 parameters will, of course, be a discussion topic when reviewing. I agree that @Boog900’s suggestion is a reasonable approach. Other limitations will kick in even before approaching 16MB.

## 0xFFFC0000 | 2025-05-24T16:22:54+00:00
`P2P_SUPPORT_FLAG_TX_RELAY_V2` seems more reasonable. I have nothing against `P2P_SUPPORT_FLAG_NOTIFY_TX`, but `P2P_SUPPORT_FLAG_NOTIFY_TX` name does not show that we are using totally separate method of tx relay.

I will update it in next push. 

## Boog900 | 2025-05-24T16:28:50+00:00
IMHO these messages do not need padding, the old method sent the txs whole meaning any size differences could be used to tell which tx(s) are being sent. The only thing you will be able to tell with the new method is the amount of txs being sent. Also this message is only used to fluff txs not to stem.

## Boog900 | 2025-05-24T16:30:29+00:00
naming this field `t` instead of `txs` could save a couple bytes per broadcast, free savings that we might as well make use of.

## Boog900 | 2025-05-24T16:30:48+00:00
same here with the field name. 

## Boog900 | 2025-05-24T16:45:05+00:00


because this is handling part of the fluff protocol we might need to include padding here? The old code in levin_notify seems to be adding padding for certain network zones for fluff txs.

@vtnerd thoughts, if padding is needed here?


## nahuhh | 2025-05-24T16:47:24+00:00
Is the padding still there?

@boog900 thoughts on padding the hashes? 
My thought was that its unnecessary. Padding txs makes sense (privacy. Different txs have different sizes) but not the hashes, which should be(?) uniform in size

## 0xFFFC0000 | 2025-05-24T16:50:15+00:00
> Is the padding still there?
> 
> @Boog900 thoughts on padding the hashes? My thought was that its unnecessary. Padding txs makes sense (privacy. Different txs have different sizes) but not the hashes, which should be(?) uniform in size

Yes, we do still have padding in v2. It is quite easy to disable it too, if necessary. The granularity of padding is 128 bytes. 

## Boog900 | 2025-05-24T17:20:03+00:00
The serialized message stores all hashes in a single string, so this is incorrect.

```suggestion
        size_t bytes = 9 /* header */ + 4 /* 1 + 'txs' */ + tools::get_varint_data(request.txs.size() * 32).size() + request.txs.size() * 32;
```

Although even that is incorrect, it uses the consensus (COCOA BEANS) varint, not the epee varint and misses the field tag's size. This is the same as the `make_tx_message` padding calculation. 

@vtnerd does this slight difference in calculation of the size vs the real size matter for the padding? 

## Boog900 | 2025-05-24T17:32:10+00:00
~~IMO this should take into account if we actually requested the tx from the peer. Currently the peer can send us an inv, we request it, they send a different tx and to us it looks like they have sent as many as we requested, when in reality they haven't.~~

Ignore, we count the number of missed txs directly instead of doing requested vs responded.

## Boog900 | 2025-05-24T17:41:00+00:00
Currently from what I can see a peer has 30 seconds to bomb our node with as many tx invs as possible and we have to track each one. I think we should add a limit on the amount of inflight requests we make to a single peer.

So if we have more than like 50 requests to a single peer we should ignore all future invs from that peer until that number drops.

## Boog900 | 2025-05-24T17:45:35+00:00
I don't think we need to: https://github.com/monero-project/monero/pull/9933#discussion_r2105874480

## Boog900 | 2025-05-24T17:49:13+00:00
Also if we are padding this one we also should pad `NOTIFY_REQUEST_TX_POOL_TXS` as that will leak data too. 

## vtnerd | 2025-05-25T01:25:23+00:00
C++ doesn't define defaults for these, just remove them and the compiler will generate errors.

## vtnerd | 2025-05-25T01:26:21+00:00
There has to be a way to implement this without some arbitrary thread tucked away. Admittedly I'm being lazy and not going through everything, but this just has to be the result of poor design choices.

## vtnerd | 2025-05-25T01:32:21+00:00
This was duplicated from the thread above, so removing.

## vtnerd | 2025-05-25T01:38:12+00:00
This `class` shouldn't exist - you can fetch by `uuid`/`connection_id` already. Instead, the information should be tucked into the `connection_context` object.

This implementation creates several problems:
  * You have to separately prune the map when connections close
  * Everything EXCEPT "tx inventory" is stored in the `connection_context` object

## vtnerd | 2025-05-25T01:38:45+00:00
Do we really need to track all of this information? Is it useful for debugging somehow?

## vtnerd | 2025-05-25T01:43:13+00:00
Just make this `std::ostream operator<<(...)`. Its more efficient.

## vtnerd | 2025-05-25T01:46:20+00:00
You should be using an `unordered_map<id, data>` instead of an `unordered_set` and then all of these `find_if` calls become efficient hash lookups instead of log(n) iteration. Another reason to use the built-in connection tracking, which does this (relatively) efficiently already.

## vtnerd | 2025-05-25T01:48:24+00:00
I got a bit lazy and didn't see if it was being used outside of `std::ostream` cases. So safely ignore if not always in use.

## vtnerd | 2025-05-25T01:56:42+00:00
If you need to do something periodically, [use the "idle" callback](https://github.com/monero-project/monero/blob/125622d5bdc42cf552be5c25009bd9ab52c0a7ca/src/cryptonote_protocol/cryptonote_protocol_handler.inl#L1677). It gets called once a second.

## 0xFFFC0000 | 2025-05-25T02:10:35+00:00
Yes, I know C++ doesn’t define them, it is left over that I didn’t want to touch. Since don’t want anyone to abuse this class in future. 

## 0xFFFC0000 | 2025-05-25T02:14:31+00:00
> This just has to be the result of poor design choices.

That is inaccurate statement. I don’t want to block existing operations. I know already idle gets called once a second. But doing stuff there means blocking it. 

## 0xFFFC0000 | 2025-05-25T02:18:38+00:00
Yes. That is the problem with our existing code. Used haphazard, temporary methods. And trying to fix later.  No industry grade professional engineer is thinking about keeping few bytes, that can be used later, as problem. 

Instead I am keeping all the info in central, well defined place. That can be used later. Possibly with a better way to statistically score peers and remove them if the score drops. 

## 0xFFFC0000 | 2025-05-25T02:39:50+00:00
That is inaccurate statement IMHO. unordered_set and unordered_map are using same underlying data structure. For example, IIRC libc++ is using redblack tree to implement both. I think you are mistaken `std::unordered_set`  with `std::set`


Both std::unordered_set and std::unordered_map offer average-case O(1) time for insert, find, and erase (hash-based) operations

## 0xFFFC0000 | 2025-05-25T02:46:03+00:00
I disagree. Connection context is mostly networking / connection related information. I am trying to have peer abstraction here without the networking information. 

Eventually I want to have well defined statistical scoring system that would score the peer based on its performance. If this was more networking related information, your proposal would’ve made more sense. But at this abstraction layer I disagree with your proposal. 

## 0xFFFC0000 | 2025-05-25T02:50:48+00:00


I grant you that use of `std::find_if` is wrong here. Which is leftover from first version I wrote. But `std::unordered_set` is okay. 

## vtnerd | 2025-05-25T07:18:44+00:00
Yes, I know they use the same underlying code. This has nothing to do with my comment. You have a natural case for `map<uuid, struct>` but instead are using `set` with `shared_ptr`. Use the `uuid` as a key, and use the `struct` (with uuid removed) as the value without `shared_ptr`.

And none of this matters, as all of this should be shoved into the `connection_context`.

## vtnerd | 2025-05-25T07:29:55+00:00
> Yes. That is the problem with our existing code. Used haphazard, temporary methods. And trying to fix later.

This is somewhat vague criticism. Do other systems of similar size have permanent (never needing maintenance) code? Zero bugs or something?

> No industry grade professional engineer is thinking about keeping few bytes, that can be used later, as problem.

I never said it would be a memory issue; we sometimes track information of limited use in debugging. These field dumps can just clutter the log further in the worse case. I'm on the fence whether these fields help with debugging. If they helped you at some point, then perhaps they are useful.

I find that that tracing when particular events occur is more useful. Because you don't really know ordering or timing with these kinds of dumps, just that event X occurred Y times.

## vtnerd | 2025-05-25T07:34:42+00:00
There is already a scoring system in the `connection_context`. Are you looking to replace it?

And I fail to see how keeping a _separate_ `map` of connections will be an overall improvement. This new map is tracking data foreach connection, thats why the map key is a connection `uuid`. Just use the map that is already there. This isn't integrated well.

## vtnerd | 2025-05-25T07:47:08+00:00
GitHub was too slick for me. Editing one modified both. Here's what I original said:

* Padding mostly helps with Tor/I2P when noise is OFF. In this PR, only number of hashes is potentially leaked, so I don't see a massive need for including it.
* Padding this way should be avoided in favor of what is done in my [SSL patch](https://github.com/monero-project/monero/pull/8996/files). See `levin_base.h` and `levin_base.cpp`. The inner epee format is self-describing so you can safely write random bytes after the payload, and they will get dropped/ignored. This is quicker, more efficient, and less error prone than doing it with the `_` field.
  * The SSL will deprecate this padding code if merged first, so might as well just import it.

## vtnerd | 2025-05-25T07:51:57+00:00
At a glance it looks like this `std::thread` "blocks" the ASIO threads with locks. Is that any better? The `std::thread` could be blocked for a while by the ASIO thread (or vice-versa)?

Just use the idle thread timer, it's there specifically for this purpose. As long as the locks are released within a reasonable time-frame it'll be fine. You might be able to create a strand implementation to prevent all blocking, but that'd be a bit more work I think.

## vtnerd | 2025-05-25T07:59:56+00:00
And to clarify a bit, the design makes more sense if the `shared_ptr` was being used outside of this class. But the way its being used here doesn't seem helpful.

## vtnerd | 2025-05-25T08:21:39+00:00
This is why I promoted the usage of the technique the SSL PR used - it's more accurate, quicker, and easier to just pad the end of the levin message as the extra trailing data gets dropped. Arguably this feature should be dropped entirely (see below).

Assuming this implementation is kept, it's possibly leaking data, but it's unlikely to matter. This feature is only useful if the connection is encrypted, which currently occurs only over Tor/I2P, both of which are already padded to disrupt traffic shaping analysis. In other words, Tor/I2P already do what we want here.

When/if SSL gets merged, this should be re-thought as SSL provides no automatic padding, which is why I added randomized padding (at the levin "layer") in that patch. It pads a randomized amount up to 8KiB, but perhaps it should just "round up" like Tor/I2P do (if it works for those networks, surely it works for us).

## Boog900 | 2025-05-25T16:39:50+00:00
 I agree padding is not necessary for the new messages. Although here is where we are sending the full txs in response to a request for them.
 
 The old protocol looked like this:
 
 node --- txs (maybe padded) --> node
 
 the new protocol:
 
 node --- inv  (not  padded) --> node
 node <-- request  (not padded) --- node
 node --- txs  (not padded) --> node
 
  my question was about the last step. IMO we should pad to match the old protocol. Although if we never fluff over Tor/I2p this isn't really necessary.  

## nahuhh | 2025-05-25T17:59:38+00:00
> Although if we never fluff over Tor/I2p this isn't really necessary.


We do fluff (immediately) to tor/i2p AIUI (when disable_noise is on)

## Boog900 | 2025-05-25T23:34:06+00:00
Hmm yeah it looks like we do, I guess we need to add padding here then. 

## 0xFFFC0000 | 2025-05-26T14:30:12+00:00
Your analysis about not using `shared_ptr` is **incorrect**. This data is being used from multiple threads (potentially), and should support cases where the container ( whether `set` or `map` ) moves or reorganizes the data. So you need a `shared_ptr` here. Otherwise you will get a concurrency bug. 

> You have a natural case for map<uuid, struct> but instead are using set with shared_ptr

I think my explanation was a little bit confusing. I don't want to expose any internal approach to outside and want to abstract as much as possible. Yes, of course you can `unordered_map<uuid, struct>` here, but I don't want expose any complexity outside since I have to handle concurrency too ( plus abstracting away the details make the code much more readable / maintainable, contrary to have what we have in few places ). 

## 0xFFFC0000 | 2025-05-26T14:33:53+00:00
Your criticism about using `connection_context` is reasonable. 

## 0xFFFC0000 | 2025-05-26T14:44:24+00:00
> You might be able to create a strand implementation to prevent all blocking, but that'd be a bit more work I think. 

This is a better approach IMHO. Let me look up what I can do. 

All of your comments will be addressed in the next update.

## 0xFFFC0000 | 2025-05-26T14:44:31+00:00
All of your comments will be addressed in the next update.


## 0xFFFC0000 | 2025-05-26T14:44:41+00:00
All of your comments will be addressed in the next update.


## 0xFFFC0000 | 2025-07-16T11:30:37+00:00
Fixed. Feel free to resolve it.

## vtnerd | 2025-07-16T17:11:02+00:00
A `shared_ptr` was not useful originally, but may be useful after the ongoing re-design.

## 0xFFFC0000 | 2025-07-16T17:12:53+00:00
@vtnerd  thanks, will take that into account.

## 0xFFFC0000 | 2025-07-26T14:14:38+00:00
Fixed. Feel free to resolve it. 

https://github.com/monero-project/monero/compare/f2061bf8976e3872e7d227dd5ee365213a317a4c..d024d369d590a0bd6488757266fee0c45df217c7

## 0xFFFC0000 | 2025-07-26T14:18:48+00:00
Fixed. Feel free to resolve it.

https://github.com/monero-project/monero/compare/d024d369d590a0bd6488757266fee0c45df217c7..3fcd5c70cf964b4f283c92538c81f2f2aed84c53

## 0xFFFC0000 | 2025-07-26T14:22:26+00:00
Fixed. Feel free to resolve it.

https://github.com/monero-project/monero/compare/f2061bf8976e3872e7d227dd5ee365213a317a4c..d024d369d590a0bd6488757266fee0c45df217c7

## 0xFFFC0000 | 2025-07-26T15:09:01+00:00
Added padding here. Fixed. Feel free to resolve it.

https://github.com/monero-project/monero/compare/3fcd5c70cf964b4f283c92538c81f2f2aed84c53..f345aea15430f7bbc79e6b613baba9a6bd245bf8

## 0xFFFC0000 | 2025-07-26T15:09:17+00:00
Removed the padding here. Fixed. Feel free to resolve it.

https://github.com/monero-project/monero/compare/3fcd5c70cf964b4f283c92538c81f2f2aed84c53..f345aea15430f7bbc79e6b613baba9a6bd245bf8

## 0xFFFC0000 | 2025-07-26T15:37:07+00:00
Feel free to resolve this, we can ignore this comment since we have removed the padding: 

https://github.com/monero-project/monero/pull/9933#discussion_r2233028720

## 0xFFFC0000 | 2025-07-26T15:50:14+00:00
Thanks. Feel free to resolve this.

## 0xFFFC0000 | 2025-09-09T16:22:11+00:00
Feel free to resolve this; fixed in updated push. 

## 0xFFFC0000 | 2025-09-09T16:24:26+00:00
Feel free to resolve this; fixed in updated push, and addressed all the criteria you mentioned. 

I implemented this feature using `on_idle`. I did a few experiments and found `on_idle` robust. 

About the concurrency mechanism, I kept the locking, since it is read/write (lightweight) locking. The issue with the `strand` is that I had to create another `io_context` ( and a thread to run it ). The `cryptonote_protocol_handler` does not have any `io_context` by itself.

## 0xFFFC0000 | 2025-09-09T16:25:23+00:00
Feel free to resolve it.  I moved everything inside `connection_context`, and got rid of this class.

## 0xFFFC0000 | 2025-09-09T16:26:39+00:00
> You should be using an `unordered_map<id, data>` instead of an `unordered_set` and then all of these `find_if` calls become efficient hash lookups instead of log(n) iteration. Another reason to use the built-in connection tracking, which does this (relatively) efficiently already.

Addressed in updated push. Everything moved to `connection_context`, so no need to discuss this further. Thanks

## 0xFFFC0000 | 2025-09-09T18:49:02+00:00
Addressed in the new push. Feel free to resolve this issue if you consider the implementation adequate. 

## vtnerd | 2025-09-21T16:00:46+00:00
Could these be `atomic<size_t>` instead? The above functions seems to indicate so. The benefit is that the reads will be faster as no write to memory has to be made (whereas a lock implies writing to memory to acquire lock).

## vtnerd | 2025-09-21T16:10:22+00:00
I just looked, and unfortunately I never fully commented on this type of padding. 

My [SSL patch](https://github.com/monero-project/monero/pull/8996) has a much cleaner solution to the problem. See `levin_base.h` and `levin_base.cpp`. The function writes zeroes after the payload which gets ignored be the epee binary parser. It's much faster and more accurate than what's attempted here.

If SSL get merged first, this basically has to be removed as dead code, and if SSL gets merged first this will be removed, etc.

## vtnerd | 2025-09-21T16:11:20+00:00
Is this leftover from earlier version?

## vtnerd | 2025-09-21T16:16:33+00:00
These functions should be in a separate cpp. No good reason to attempt inlining.

## vtnerd | 2025-09-21T16:17:36+00:00
I see little benefit to these aliases, especially considering that the codebase has been moving to explictly calling them (by their full `std::` name).

## vtnerd | 2025-09-21T16:19:06+00:00
Should there be a lock here? Presumably it was left out because a recursive call could be made, however, `m_requested_txs` can be modified by another thread ... ?

I think you need to make a copy of every request into a vector, and then feed it to the callback. Or even easier, just return the vector.

## vtnerd | 2025-09-21T22:36:10+00:00
Why mark this as mutable? Why can't ``fly()` be not-const?

## vtnerd | 2025-09-21T22:37:04+00:00
Avoid this `using namespace` in a header, we don't want to drag in everything from that namesake into the global namespace.

## vtnerd | 2025-09-21T22:37:59+00:00
Templated function does the same thing, and is somewhat preferable here.

## vtnerd | 2025-09-21T22:44:45+00:00
These two includes look unnecessary here too (but perhaps are needed within `request_manager.h`).

## vtnerd | 2025-09-21T22:45:51+00:00
This should be `const`.

## vtnerd | 2025-09-21T23:11:06+00:00
The store only happens once during init; `epee::once_a_time` should store the timer value instead of a callback function.

## vtnerd | 2025-09-21T23:25:56+00:00
I would delay creating `tx_hashes` until inside of this function. This removes some memory needed for the `fluffy_notify` and `dandelionpp_notify` callbacks inside of ASIO land. This also removes the duplication in `send_txs` where the hash is computing in two different switch statements.

## vtnerd | 2025-09-22T00:12:33+00:00
Switching to `std::chrono::steady_clock::time_point` has several advantages when computing time differences (better type checking, etc).

## vtnerd | 2025-09-22T00:15:25+00:00
Why not declare this inside of `request_manager` - its never referenced outside of that class.

## vtnerd | 2025-09-22T00:21:02+00:00
`std::time_t` has no defined type or interval (which is why I proposed `std::chrono` elsewhere).

## vtnerd | 2025-09-22T00:24:41+00:00
You need a better name than this in the global namespace.

## vtnerd | 2025-09-22T00:26:14+00:00
Creating a `request` is roughly as cheap as a `boost::make_tuple` call. Just try the insert, and then return the bool if it succeeded. This reduces the lookups by 1.

## vtnerd | 2025-09-22T00:34:09+00:00
This line should be removed, it's not needed.

## vtnerd | 2025-09-22T00:35:16+00:00
This should be checking whether the entire map is empty, is that intended? Don't see how this is useful.

## vtnerd | 2025-09-22T00:37:25+00:00
This also should have a more useful name; too generic.

## vtnerd | 2025-09-22T00:45:20+00:00
Why is this a `shared_ptr`? I don't see where this shared with any other object, and it doesn't need to be a `unique_ptr` either.

## vtnerd | 2025-09-22T00:57:42+00:00
Do not perform a `::all` lookup here. An attacker could issue this command at any time, and get transactions still in the D++ stem phase. This is particularly important if an attacker managed to blackhole a transaction.

There also needs to be a lookup in the blockchain for the tx, as it could've moved. If the tx is silently not returned, the caller could drop the connection unnecessarily.

## vtnerd | 2025-09-22T01:06:56+00:00
This should be a unilateral remove by `tx_hash` (i.e. drop `m_connection_id` from call)?

## vtnerd | 2025-09-22T01:08:41+00:00
This should be declared after `tmp` since its pointing into it.

## vtnerd | 2025-09-22T01:15:17+00:00
This should be in the `for_connection` call above; this entire if statement is duplicated from the above check.

## 0xFFFC0000 | 2025-10-08T19:23:30+00:00
I see. Thanks. I will leave this for now. As you asked, if SSL-Patch gets in first, I will remove this. I am fine either way. 

## 0xFFFC0000 | 2025-10-08T19:24:56+00:00
Because I am storing them inside boost::multi_index_container. If I want to remove the mutable, I have to use:

 ```
 auto& idx = get_requests_by_peer_and_tx(container);
 auto it = idx.find(std::make_tuple(peer_id, tx_hash));
 if (it != idx.end()) {
     idx.modify(it, [](request& r) { r.in_flight = true; });
 }
 ```

 Which I would say is quite cumbersome. `mutable` is quite safe here. Since `in_flight` value does not have any impact on the key.

## 0xFFFC0000 | 2025-10-08T19:25:37+00:00
`cryptonote_protocol_handler.h` needs ‘request_manager’, but you were right about `boost::uuid`.

## 0xFFFC0000 | 2025-10-08T19:26:38+00:00
`m_interval_peer_request_checker` should not be const since we are changing it. I have a command line flag so the user can use a custom value of `m_interval_peer_request_checker`

## 0xFFFC0000 | 2025-10-08T19:27:00+00:00
We are assigning it with command-line option. Not compile time. So, it's not possible to make this change. `once_a_time` needs compile-time argument.


## 0xFFFC0000 | 2025-10-08T19:27:26+00:00
I have to change the `run` API and send an instance of the object. Since the `run` method is static, and no way to access `fluff_notify` object fields, unless I pass a pointer to that object. If that's what you prefer, I can do that.

## 0xFFFC0000 | 2025-10-08T19:27:53+00:00
Just to keep separate concerns and make it more readable, in a sense. I can move it to `request_manager` if you prefer.

## j-berman | 2025-10-09T20:40:00+00:00
Might be in the chain here too

## j-berman | 2025-10-09T21:12:43+00:00
I'm not 100% certain if this will have issues with the connection code, but perhaps you could easily just send batches of the txs here. I.e. iterate over `txs` some slice at a time, calling `post_notify` with that slice. And then just make sure each slice is some max limit below the max packet size. On first pass, it doesn't look like there would be issues with this approach in the connection code, but it could use a deeper look.

UPDATE: here's how I sent batches of txs for the pool tx complement response: https://github.com/seraphis-migration/monero/pull/168

## 0xFFFC0000 | 2025-10-29T14:33:53+00:00
Fixed: https://github.com/monero-project/monero/pull/9933/commits/e8ed4da9cc2a52142f7294dafc0cfda41de05d16

## 0xFFFC0000 | 2025-10-29T14:34:49+00:00
Fixed: https://github.com/monero-project/monero/pull/9933/commits/64121e9df24f60b071971edef4a80d9120ce5a8e

## 0xFFFC0000 | 2025-10-29T14:34:55+00:00
Fixed: https://github.com/monero-project/monero/pull/9933/commits/64121e9df24f60b071971edef4a80d9120ce5a8e

## 0xFFFC0000 | 2025-10-29T14:35:22+00:00
Fixed: https://github.com/monero-project/monero/pull/9933/commits/2875352e33ed4b401a90daff6d8e3a71dbd8cda5

## 0xFFFC0000 | 2025-10-29T14:36:10+00:00
Fixd: https://github.com/monero-project/monero/pull/9933/commits/652b4189710dcacf461e85c5f6f8f928caeff52a#diff-1d03e4b2c6d1df00db873e159c99573f4c9c7a4ff7cbc85ebdf28a1965c08fa2R96

## 0xFFFC0000 | 2025-10-29T14:36:14+00:00
Fixd: https://github.com/monero-project/monero/pull/9933/commits/652b4189710dcacf461e85c5f6f8f928caeff52a#diff-1d03e4b2c6d1df00db873e159c99573f4c9c7a4ff7cbc85ebdf28a1965c08fa2R96 

## 0xFFFC0000 | 2025-10-29T14:36:24+00:00
Fixd: https://github.com/monero-project/monero/pull/9933/commits/652b4189710dcacf461e85c5f6f8f928caeff52a

## 0xFFFC0000 | 2025-10-29T14:36:33+00:00
Fixd: https://github.com/monero-project/monero/pull/9933/commits/652b4189710dcacf461e85c5f6f8f928caeff52a

## 0xFFFC0000 | 2025-10-29T14:36:43+00:00
Fixd: https://github.com/monero-project/monero/pull/9933/commits/652b4189710dcacf461e85c5f6f8f928caeff52a

## 0xFFFC0000 | 2025-10-29T14:37:25+00:00
Fixd: https://github.com/monero-project/monero/pull/9933/commits/652b4189710dcacf461e85c5f6f8f928caeff52a

## 0xFFFC0000 | 2025-10-29T14:38:47+00:00
Fixed: https://github.com/monero-project/monero/pull/9933/commits/652b4189710dcacf461e85c5f6f8f928caeff52a

## 0xFFFC0000 | 2025-10-29T14:41:43+00:00
After erasing in-flight requests for a given transaction (`tx_hash`), these lines **refresh the index** (`by_tx_hash`) to ensure it reflects the current state of `m_requested_txs`.  

## 0xFFFC0000 | 2025-10-29T14:41:51+00:00
https://github.com/monero-project/monero/pull/9933#discussion_r2473535129

## 0xFFFC0000 | 2025-10-29T14:50:53+00:00
You need it as `shared_ptr`
```
monero/src/p2p/net_node.inl: In instantiation of ‘int nodetool::node_server<t_payload_net_handler>::handle_handshake(int, typename COMMAND_HANDSHAKE::request&, typename COMMAND_HANDSHAKE::response&, p2p_connection_context&) [with t_payload_net_handler = cryptonote::t_cryptonote_protocol_handler<cryptonote::core>; typename COMMAND_HANDSHAKE::request = epee::misc_utils::struct_init<nodetool::COMMAND_HANDSHAKE_T<cryptonote::CORE_SYNC_DATA>::request_t>; COMMAND_HANDSHAKE = nodetool::COMMAND_HANDSHAKE_T<cryptonote::CORE_SYNC_DATA>; typename COMMAND_HANDSHAKE::response = epee::misc_utils::struct_init<nodetool::COMMAND_HANDSHAKE_T<cryptonote::CORE_SYNC_DATA>::response_t>; p2p_connection_context = nodetool::p2p_connection_context_t<cryptonote::cryptonote_connection_context>]’:
monero/src/rpc/instanciations.cpp:36:37:   required from here
   36 | namespace nodetool { template class node_server<cryptonote::t_cryptonote_protocol_handler<cryptonote::core>>; }
      |                                     ^~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
monero/src/p2p/net_node.inl:2600:40: error: use of deleted function ‘nodetool::p2p_connection_context_t<cryptonote::cryptonote_connection_context>::p2p_connection_context_t(const nodetool::p2p_connection_context_t<cryptonote::cryptonote_connection_context>&)’
 2600 |       try_ping(arg.node_data, context, [peer_id_l, port_l, context, this]()
      |                                        ^~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
 2601 |       {
      |       ~
      
```      

Some code (in this case, in try_ping or a lambda) tries to copy the context object, but the compiler cannot generate a copy constructor due to the non-copyable member.

## 0xFFFC0000 | 2025-10-29T14:54:36+00:00
Thanks. Fixed: https://github.com/monero-project/monero/pull/9933/commits/2fba2462cc86c8f32162ed06c089a818fa4aae32

## 0xFFFC0000 | 2025-10-29T15:00:41+00:00
I need both to do a lookup.

## 0xFFFC0000 | 2025-10-29T15:02:58+00:00
Fixed: https://github.com/monero-project/monero/pull/9933/commits/6466070de9227cb2793bcb5483fd7b0f201fe165

## 0xFFFC0000 | 2025-10-29T15:07:49+00:00
Thanks. Good catch. 

Fixed: https://github.com/monero-project/monero/pull/9933/commits/afff91006143a127dce9ab295ad4e525e0efa24f

## vtnerd | 2026-03-01T02:03:53+00:00
I don't understand why this was moved to the header. It's marginally faster if inlined?

And if we're going to be put it here, upgrade this to `constexpr` I guess?

## navidR | 2026-03-02T08:20:47+00:00
Thanks. Addressed by making it `constexpr`. 

## vtnerd | 2026-03-03T15:58:48+00:00
Why was it moved to the header though? This isn't going to be executed as `constexpr`, as it requires a runtime value, so why move it to the header?

## navidR | 2026-03-03T20:43:34+00:00
You are right about your concern. But there is one minor case that I think you are missing. That minor case might not be very important. But still it is valid. Look at these: 

 ```
 // src/cryptonote_protocol/cryptonote_protocol_handler.inl:188
 static const std::size_t MAX_N_TXS =
  get_command_max_bytes(NOTIFY_NEW_TRANSACTIONS::ID) / get_max_tx_size();
  ```

  and

  ```
  // src/cryptonote_protocol/cryptonote_protocol_handler.inl:195
  static const std::size_t MAX_N_TX_HASHES =
  get_command_max_bytes(NOTIFY_TX_POOL_HASH::ID) / sizeof(crypto::hash) * 95 / 100;
  ```
  
The point in both cases `get_command_max_bytes` ( not the whole expression, just that function) will be evaluated at compile time.

These are optimizable with `constexpr`, guaranteed. I know with constant-folding, the compiler will try to optimize these even without `constexpr`, but that is relying on compiler behaviour. 


We are getting into the nitty-gritty of the C++ standard here. So I leave the final decision for you. If you want to move it to `.cpp`, let me know. 

## vtnerd | 2026-03-06T00:42:14+00:00
See this [patch](https://github.com/seraphis-migration/monero/pull/297) in seraphis branches. You'll need to add something similar here - the message should be sending txes in fluff mode.

## navidR | 2026-03-06T00:48:58+00:00
Thanks. Applied here: https://github.com/monero-project/monero/pull/9933/changes/9ac0d39083244c1bac0eaec1defbd3f6f33392b1#diff-d32229ecf55d414dcca43b6918ad711e1ffd313ef851492ecba1865323007f0fR1000

## jeffro256 | 2026-03-07T16:32:47+00:00
Why not take a similar approach to https://github.com/seraphis-migration/monero/pull/168?

## j-berman | 2026-03-09T17:22:49+00:00
Linking https://github.com/seraphis-migration/monero/pull/288#discussion_r2906786976

# Action History
- Created by: 0xFFFC0000 | 2025-05-22T10:24:13+00:00
