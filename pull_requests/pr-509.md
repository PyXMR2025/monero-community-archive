---
title: Add Tor support to Cuprate (Arti, Tor Daemon, Dandelion router)
source_url: https://github.com/Cuprate/cuprate/pull/509
author: SyntheticBird45
assignees: []
labels:
- A-p2p
- A-dependency
- A-workspace
- A-consensus
- A-net
- A-ci
- A-rpc
- A-binaries
created_at: '2025-06-10T22:03:14+00:00'
updated_at: '2025-09-30T04:11:03+00:00'
type: pull_request
status: merged
closed_at: '2025-08-04T14:31:25+00:00'
merged_at: '2025-08-04T14:31:25+00:00'
---

# Original Description
### What

Part of [ongoing Tor support development](https://ccs.getmonero.org/proposals/syntheticbird_cuprate_arti_2_months.html) for Cuprate

Follows: https://github.com/Cuprate/cuprate/pull/446 and https://github.com/Cuprate/cuprate/pull/481

### Changelog

In `workspace`:
- New dependencies: `arti_client`, `Cuprate/tokio-socks.git`, `tor-cell`, `tor-config-path`, `tor-hsservice`, `tor-persist`, `tor-proto`, `tor-rtcompat` (yes nothing was exported).
- In `deny.toml`, whitelisted `Unlicense` license for `arti_client`.

In `cuprate-p2p-transport`:
- Implemented `Transport<ClearNet>` and `Transport<Tor>` for `Arti`. New `ArtiClientConfig`, `ArtiServerConfig` configuration. New `OnionListener` for accepting inbound connections from arti's generated onion service.
- Implemented `Transport<Tor>` for `Daemon`. New `DaemonClientConfig`, `DaemonServerConfig` configuration. New `DaemonInboundStream` listening for incoming TCP  connections from the tor daemon.
- `DisabledListener` as a polyfill for transports with inbound disabled, such as `Transport<ClearNet> for Arti` and in the future `Transport<ClearNet> for Socks5`.

In `cuprate-p2p-core`:
- Removed `Default` and `Debug` bound from `Transport::ClientConfig` and `Transport::ServerConfig`.
- Removed `Clone` bound from `Transport::ServerConfig`.

In `cuprate-p2p`:
- Changed some function visibility to `pub(super)` instead of `pub`.

In `cuprate-wire`:
- Added `borsh` dependency and `BorshSerialize` and `BorshDeserialize` derived implementation to `OnionAddr` for `BorshNetworkZone` requirement in address book.

In `cuprated`:
- New `tor` module containing the initialization of Arti, config helpers and context structure `TorContext` to pass down to p2p initialization function and other helpers.
- New `config/tor` module containing the `[tor]` configuration table. It define tor daemon related variables, as well as arti settings.
- Added `enable_inbound` field to `ClearNetConfig` to disable incoming listener by default.
- Added `proxy` field to `ClearNetConfig` for enabling clearnet over arti and in the future proxy urls.
- Added `TorNetConfig` for setting `Tor` network zone parameters such as listening ports, enabling arti inbound server, or setting an anonymous inbound onion address from an external daemon.
- Modified `initialize_zones_p2p` to now start Tor network zone and use the correct transport depending on user configuration.
- In `txpool/*`, generalized `DiffuseService`, `OutboundPeerStream` and `ConcreteDandelionRouter` for `Z: NetworkZone`. Created a new `MainDandelionRouter` service that will route local txs to a Tor router instead of clearnet if available. Adapted initialization to the changes.

# Discussion History
## Boog900 | 2025-06-18T12:43:41+00:00
why have you pinned these versions?

## Boog900 | 2025-06-18T12:44:17+00:00
wrong group

## Boog900 | 2025-06-18T12:47:38+00:00
this should just be 0, we can't advertise RPC over Tor P2P

## Boog900 | 2025-06-18T12:48:35+00:00
The imports are in the wrong group

## Boog900 | 2025-06-18T12:49:55+00:00
This has been moved, it should be moved back. Also i think the comment is wrong now: `Setting this to 0 will disable incoming P2P connections for this zone.` 

## Boog900 | 2025-06-18T12:54:35+00:00
should this be joined with `inbound_onion` and put in `Shared`?

## Boog900 | 2025-06-18T12:55:33+00:00
is it a good idea to do this for default, I wonder if it is better to leave this on for the people who wouldn't notice. 

## Boog900 | 2025-06-18T15:33:38+00:00
this should be much higher IMO


## Boog900 | 2025-06-18T15:36:33+00:00
I didn't understand this at first, but I get it, if you enable pow, it will only require pow if the onion service is under load.

Maybe change it to:
```suggestion
        /// If set, Arti will enforce an EquiX PoW to be resolved for
        /// other nodes to complete a rendez-vous request when under
        /// heavy load.
```

## Boog900 | 2025-06-18T15:51:39+00:00
can probably be removed (discussed in DM)

## Boog900 | 2025-06-18T15:52:27+00:00
can we combine these into 1 field, with a `SocketAddr`? also should this mention p2p?

## Boog900 | 2025-06-18T15:52:55+00:00
```suggestion
```

## Boog900 | 2025-06-18T15:53:11+00:00
also the docs need updating I think

## Boog900 | 2025-06-18T15:54:36+00:00
```suggestion
```

## Boog900 | 2025-06-18T16:03:30+00:00
should we add a comment here and on daemon that these values are only used when the mode is set to the corresponding one?

## Boog900 | 2025-06-18T16:38:13+00:00
import group

## Boog900 | 2025-06-18T16:46:16+00:00
IMO `TorMode::Off` should cause an exit here, which also means we no longer need the `tcp` closure 

## Boog900 | 2025-06-18T16:47:51+00:00
where is the `enable` field for the P2P Tor config checked? 

## Boog900 | 2025-06-18T16:56:11+00:00
import groups

## Boog900 | 2025-06-18T17:13:44+00:00
`to_lowercase` `Tor` :)

Do you think it might be good idea to make `proxy` an enum?

## Boog900 | 2025-06-18T17:18:14+00:00
bit of nit but these can be combined:
```suggestion
    let (Some(bootstrapped_client), Some(client_config)) = (ctx.bootstrapped_client, ctx.arti_client_config) else {
        panic!("Arti client should be initialized");
    };
```

## Boog900 | 2025-06-18T17:19:34+00:00
should this instead check the bool field?

## Boog900 | 2025-06-18T17:19:52+00:00
group 

## Boog900 | 2025-06-18T17:25:27+00:00
is it possible to instead list the crates to allow?

## SyntheticBird45 | 2025-06-30T19:08:29+00:00
Honestly no. That would be confusing af.

## SyntheticBird45 | 2025-06-30T19:13:27+00:00
There is an ethical question at hand. We should definitely avoid additional behavior without consent. I would be pretty pissed off if my software suddenly tried to advertise me as being reachable and worse that IPs inbounding towards me without me realizing it. This should be an opt-in thing, this also force people to RTFM before enabling it.

## SyntheticBird45 | 2025-06-30T19:14:03+00:00
Do you have defaults in mind ?

## SyntheticBird45 | 2025-06-30T19:16:18+00:00
Kept in (discussed in DM). Aftermatch of https://cfp.twed.org/mk5/talk/AQBMB3/ presentation. Isolated circuits can yield a throughput/performance benefit

## SyntheticBird45 | 2025-06-30T19:24:38+00:00
I think so it would be ideal

## SyntheticBird45 | 2025-06-30T20:27:11+00:00
> `to_lowercase` `Tor` :)

lol

> Do you think it might be good idea to make proxy an enum?

It might be but I prefer to think about it in the SOCKS5 PR that comes just after (with docs PR) because fwiw I haven't decided if we should just support SOCKS5 and not SOCKS5H or 4. And therefore if we should rely on some URI library with serde support. we could handroll too. I prefer to think of `String` as a placeholder atm.

## SyntheticBird45 | 2025-06-30T20:29:17+00:00
I don't like having for conditions for one panic on the same line. If it panic I'll wonder what was uninitialized.

## SyntheticBird45 | 2025-06-30T20:31:28+00:00
It should check bool and warn if anonymous_inbound is empty

## SyntheticBird45 | 2025-06-30T20:39:55+00:00
There are quite a lot of them. Every subcrates of arti is `Unlicense`

## Boog900 | 2025-07-14T14:33:17+00:00
does this require someone to always supply an inbound addr for daemon mode?

## Boog900 | 2025-07-14T14:34:40+00:00
why? they are both for changing if we accept inbound connections 

## Boog900 | 2025-07-14T14:35:41+00:00
I think this section needs to be updated as it isn't just for arti anymore 

## Boog900 | 2025-07-14T14:36:34+00:00
`max_inbound_connections` should be like 128

## Boog900 | 2025-07-14T14:45:43+00:00
the enum I was thinking would be something like:

```rust
enum {
    Tor,
    Socks(String)
}
```

with the socks being a catch all, this is a nit but does improve the code over string matching :shrug: 

## Boog900 | 2025-07-14T14:46:23+00:00
well then the comments should be different and mention the field IMO

## Boog900 | 2025-07-14T14:50:44+00:00
if `inbound_onion` is false then a server config will be created here, is that intended?

## SyntheticBird45 | 2025-07-18T22:19:31+00:00
I am afraid it is. Will fix that.

## SyntheticBird45 | 2025-07-18T22:22:56+00:00
I agree. thx for clarifying 

## SyntheticBird45 | 2025-07-20T13:45:07+00:00
`enable_inbound` would have:
```
Enable IPv4 inbound server.

The inbound server will listen on port `p2p.clear_net.p2p_port`.
Settings this to `false` will disable incoming IPv4 P2P connections.
```
`inbound_onion` would have:
```
Enable Tor inbound onion server.

In Arti mode, setting this to `true` will enable Arti's onion serice for accepting inbound Tor P2P connections. They keypair and therefore onion address is generated randomly on first run.

In Daemon mode, setting this to `true` will enable a TCP server listening for inbound connections from your Tor daemon. Refer to the `tor.anonymous_inbound` and `tor.listening_addr` field for onion address and listening configuration.

The server will listen on port `p2p.tor_net.p2p_port`
```

Now if we put it in `Shared`:
```
Enable inbound server for this zone.

The inbound server will listen on the `p2p_port` field of this zone.
Setting this to `false` will disable incoming P2P connections for this zone.
```

We lose so many information for arguably not that much deduplication gain if you consider you have to document it elsewhere. People will immediately see that the comments are duplicated, the beginners will not understand, try to run and realize it don't work. The more knowledgeable will try to seek for documentation elsewhere. "Where is this documentation?" They will be roaming around the configuration file. The best is that the relevant field you are using is redirecting you towards concerned and mandatory fields. The same way that when you read a man page there is always a [`See also`](https://www.man7.org/linux/man-pages/man5/hosts.5.html) section. `enable_inbound` DO NOT work on its own under tor zone, unlike clearnet. You can't use the same documentation for a field that have a different behavior.

I'll just profit from this comment to say that the user book should never imo hold **exclusive** essential documentation for making cuprate work. You do not know if someone is working on an headless environment with no access to a browser elsewhere or if the website is prohibited for some reason.

## Boog900 | 2025-07-25T19:00:54+00:00
this comment needs updating 

## Boog900 | 2025-07-25T19:08:23+00:00
`valid_onion` will still be true if `inbound_onion` is false 

## SyntheticBird45 | 2025-08-04T12:47:15+00:00
Why this change?

# Action History
- Created by: SyntheticBird45 | 2025-06-10T22:03:14+00:00
- Merged at: 2025-08-04T14:31:25+00:00
