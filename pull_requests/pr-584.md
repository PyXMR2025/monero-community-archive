---
title: 'cuprated: event-driven block synchronization'
source_url: https://github.com/Cuprate/cuprate/pull/584
author: redsh4de
assignees: []
labels:
- A-p2p
- A-consensus
- A-binaries
created_at: '2026-02-15T16:27:27+00:00'
updated_at: '2026-03-15T19:44:47+00:00'
type: pull_request
status: merged
closed_at: '2026-03-15T19:44:47+00:00'
merged_at: '2026-03-15T19:44:47+00:00'
---

# Original Description
### What
Make blockchain synchronization event-driven instead of polling every 30s. This makes new sync initiation near-instant as it respond to relevant events. Closes #532.

### Why
The syncer previously polled every 30 seconds to check if we are behind our peers. This meant up to a 30-second delay before reacting to network's cumulative difficulty changes. Also unnecessary work when nothing had changed

### Where
- `p2p/p2p-core/src/client.rs` - new `PeerSyncCallback` struct in its own submodule
- `p2p/p2p-core/src/client/handshaker.rs`, `builder.rs`, `request_handler.rs`, `timeout_monitor.rs` - plumbing
- `p2p/p2p/src/lib.rs`, `inbound_server.rs`, `connection_maintainer.rs` - notify syncer on handshake and 0 peer -> 1+ peer transitions
- `consensus/context/src/lib.rs` - add a read function for our cumulative difficulty for use in the callback
- `binaries/cuprated/src/main.rs`, `p2p.rs`, `p2p/request_handler.rs`, `blockchain/manager.rs`, `blockchain/syncer.rs` - wire everything together, add loop label for clarity for `breaks`

### How
Replace the 30-second polling interval with a `PeerSyncCallback` struct that wraps a `tokio::sync::Notify` with three waking paths:
- Filtered wake (`call`): When a peer reports new sync data, wake the syncer only if the peer's cumulative difficulty exceeds ours.
- Unconditional wake (`wake_unconditionally`): When we receive an orphan block, wake the syncer immediately.
- First-peers wake (`wake_on_first_peers`): When transitioning from 0 to 1+ connected peers, wake the syncer immediately. Returns `bool` so handshake sites can skip the filtered `call` and not double-notify. Uses a guard that the syncer re-arms (`wake_on_first_peers_arm`) each time it enters the `NoPeers` state, so the mechanism works across repeated complete disconnect -> connect cycles.
  
The callback also tracks incoming blocks via a watch<u32> counter (`incoming_block_in_flight` / `incoming_block_done`). Before starting the block downloader loop, the syncer waits for any incoming blocks to finish processing. After that:
- If we had fully synced once already: the syncer parks, assuming incoming blocks kept us synced.
- Before initial sync completes: the syncer skips the park and proceeds directly to the downloader, just in case.

Sync status checking is extracted into `wait_until_behind`, which loops over `check_sync_status`, and parks on `Synced` or `NoPeers` until we are confirmed behind. On `NoPeers`, `first_sync_done` resets so the syncer doesn't optimistically park after reconnecting from a network loss

The `synced_notify` for the Tor P2P zone start is now fired only once on initial sync completion rather than repeatedly.

# Discussion History
## Boog900 | 2026-03-04T16:29:50+00:00
I think this closure should be changed, currently it takes in the cumulative difficulty and returns if that is above ours.

Instead I think it should take in a reference to the whole core sync data from the peer and return nothing. In the closure it should keep a Notify signal.

This means we should be able to move the `PeerSyncCallback` from the lower level network crate.

It also means we can use the peers top_hash to check if we are currently handling that block by using this static: https://github.com/Cuprate/cuprate/blob/adbded5ffa2cd472dea20e227466d6573588e6af/binaries/cuprated/src/blockchain/interface.rs#L73

This should allow us to remove the watch channel for if we are handling blocks. 



## redsh4de | 2026-03-04T21:29:41+00:00
Thank you for your perspective, this is a much cleaner approach overall

For the incoming block part, i think a watcher of some kind that gives a grace period like in the flowchart for incoming blocks to start arriving is still needed, as it would benefit cases where it might take a while for incoming blocks to arrive. If the block is still in-flight and not in BLOCKS_BEING_HANDLED yet, we might end up starting a block downloader loop for 1-2 blocks, where the block may end up arriving during or after anyway

Thoughts? Can try some kind of hybrid approach, will look into approaches tomorrow that could be used

## Boog900 | 2026-03-05T20:11:39+00:00
> If the block is still in-flight and not in BLOCKS_BEING_HANDLED yet, we might end up starting a block downloader loop for 1-2 blocks, where the block may end up arriving during or after anyway

Although this _could_ happen this should be very rare, the only way I can see it happening is a new connection exactly as a block is being sent around the network.

Nodes shouldn't send us a core sync data update before sending the new block that caused the core sync update.

# Action History
- Created by: redsh4de | 2026-02-15T16:27:27+00:00
- Merged at: 2026-03-15T19:44:47+00:00
