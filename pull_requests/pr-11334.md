---
title: 'multisig: erase all copies of a nonce at the same time'
source_url: https://github.com/monero-project/monero/pull/11334
author: UkoeHB
assignees: []
labels:
- pending review
- wallet
- needs backport
created_at: '2026-09-15T22:29:07+00:00'
updated_at: '2026-09-18T09:08:34+00:00'
type: pull_request
status: open
closed_at: null
merged_at: null
---

# Original Description
EDIT: #11331

An old version of this saved nonces in a separate field of the wallet cache, but [LLM review](https://github.com/xmrack/monero-review/issues/651) pointed out nonce lifetimes are tied to specific enotes via `R = a*Hp(K)`. The current solution avoids this lifetime ambiguity without making the whole thing even more complex.

# Discussion History
## jpk68 | 2026-09-16T00:27:43+00:00
Should the version be bumped here as well?

## thomasbuilds | 2026-09-16T13:39:36+00:00
`m_multisig_k` only gets rows from `export_multisig`, so this fails whenever the wallet holds transfers with no nonce row: outputs received after the last export, a wallet that imports before its first export, every wallet right after the v3 upgrade (the table starts empty), and a soft rescan while rescan info is pending (`clear_soft` empties the table). From the loop in `import_multisig` it fails the import outright (`Mismatched sizes of multisig_k and info`); during the post-import rescan `refresh()` retries, the output is then skipped as "already exists", and the import reports success with that output left without a key image (or fails after three retries). Nothing here reads `m_multisig_k` anymore.
```suggestion
```

## thomasbuilds | 2026-09-16T13:39:36+00:00
No nonce row is the normal state for an output received after the last export (and for every output right after the v3 upgrade). Master reports `multisig_export_needed`; this says "idx out of range".
```suggestion
  CHECK_AND_ASSERT_THROW_MES(idx < m_transfers.size(), "idx out of range");
  THROW_WALLET_EXCEPTION_IF(idx >= m_multisig_k.size(), tools::error::multisig_export_needed);
```

## thomasbuilds | 2026-09-16T13:39:37+00:00
Every multisig `transfer` right after the v3 upgrade throws `std::out_of_range` here (`vector::_M_range_check: __n (which is 0) >= this->size() (which is 0)`), the table being empty until the wallet exports again; once the check above is gone, spending an output received after the last export fails the same way.
```suggestion
      if (idx >= m_multisig_k.size() || m_multisig_k[idx].empty())
        continue;
      std::vector<rct::key> &multisig_k = m_multisig_k[idx];
```

## thomasbuilds | 2026-09-16T13:39:37+00:00
Rows beyond `m_transfers.size()` (left by a detach) are dropped unwiped.
```suggestion
  for (size_t n = m_transfers.size(); n < m_multisig_k.size(); ++n)
    memwipe(m_multisig_k[n].data(), m_multisig_k[n].size() * sizeof(m_multisig_k[n][0]));
  m_multisig_k.resize(m_transfers.size());
```

## thomasbuilds | 2026-09-16T13:39:37+00:00
The version<3 branch only runs for new-format caches; a cache loaded through the boost fallback keeps its legacy `td.m_multisig_k` scalars and writes them back into every v3 save.
```suggestion
    if (!m_transfers[i].m_multisig_k.empty())
      memwipe(m_transfers[i].m_multisig_k.data(), m_transfers[i].m_multisig_k.size() * sizeof(rct::key));
    m_transfers[i].m_multisig_k.clear();
    if (i >= m_multisig_k.size()) { continue; }
```

## thomasbuilds | 2026-09-17T06:20:52+00:00
With `break`, a cache loaded through the boost fallback (empty table) only gets transfer 0 wiped; every other transfer keeps its legacy `m_multisig_k`.
```suggestion
    if (i >= m_multisig_k.size()) { continue; }
```

# Action History
- Created by: UkoeHB | 2026-09-15T22:29:07+00:00
