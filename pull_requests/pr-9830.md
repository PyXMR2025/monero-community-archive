---
title: 'wallet: add multiple-recipient URI support to `parse_uri` and `make_uri`'
source_url: https://github.com/monero-project/monero/pull/9830
author: fxrstor
assignees: []
labels:
- pending review
- wallet
created_at: '2025-03-09T14:22:10+00:00'
updated_at: '2026-01-05T01:02:34+00:00'
type: pull_request
status: open
closed_at: null
merged_at: null
---

# Original Description
Closes #7731 
This PR aims to implement the [format suggested by jeffro256](https://github.com/monero-project/monero/issues/7731#issuecomment-3239824513).
```
monero-uri = "monero:" monero-address [ "?" first-param { "&" monero-param } ]
monero-address = main-address / sub-address / integrated-address
first-param = version-param / monero-param
monero-param = address-param / legacy-amount-param / amount-param / label-param / tx-desc-param
address-param = "address=" monero-address
legacy-amount-param = "tx_amount=" amount
amount-param = "amount=" amount
label-param = "label=" label-string
tx-desc-param = "tx_description=" label-string
version-param = "version=" version
amount = decimal-number [amount-unit]
amount-unit = xmr-unit / crypto-unit / fiat-unit
decimal-number = *DIGIT ["." *DIGIT]
version = "2.0"
label-string = *(unreserved / pct-encoded / ":" / "@")
xmr-unit = "XMR"
crypto-unit = "BTC" / "ETH"
fiat-unit = "USD" / "EUR"
```

The new format is backwards compatible with the old one. `version` key in query segregates the new format and any URI containing the version key will be rejected by legacy `parse_uri`. Single recipient URIs (with xmr as currency) generated by new `make_uri` can be parsed by legacy `parse_uri` safely.

# Discussion History
## Tzadiko | 2025-03-18T16:26:50+00:00
NIT:

```cpp
std::vector<std::string> addresses, recipient_names;
std::vector<uint64_t> amounts;
```

## Tzadiko | 2025-03-18T16:27:22+00:00
NIT: `address_index` is clearer than `j`.

## Tzadiko | 2025-03-18T16:31:53+00:00
I know this isn't part of your change, but I'm curious why these two can contradict. In other words, is there a case where has_payment_id is false and payment_id_uri is not empty?

## Tzadiko | 2025-03-18T16:44:25+00:00
NIT: Theres a lot of parameters here, as well as parse_uri. Consider moving them into a struct called `payment_details` or somethign of the sort, which houses these parameters as member variables. 

You can then change this API to 

```cpp
struct MultipleUriDetail
{
    std::vector<std::string> addresses;
    // ... Other
};
// ...
std::string make_uri(const MultipleUriDetail&);
```


## Tzadiko | 2025-03-18T16:44:56+00:00
Both the existing parse_uri and make_uri are virtual. Consider whether these need to be make virtual as well. If not, why not?

## Tzadiko | 2025-03-18T16:46:05+00:00
Missing the `t` after `conver`

## Tzadiko | 2025-03-18T16:49:20+00:00
Use `.append` instead of `+=`.

+= operator typically creates a new string object by copying both the original string and the string to be appended. This can be less efficient than append() because it involves additional memory allocation and copying. 

## Tzadiko | 2025-03-18T16:51:45+00:00
NVM I see why it's not virtual after reading more of the code. It just forwards to the virtual implementation.

## Tzadiko | 2025-03-18T16:55:55+00:00
`ptr == nullptr`

## Tzadiko | 2025-03-18T16:58:31+00:00
NIT: Not a fan of magic numbers. Consider

```cpp
const int short_payment_id_length = 16;
const int long_payment_id_length = 64;
```


## Tzadiko | 2025-03-18T17:08:39+00:00
NIT: 'recipient name count does not match address count' is probably more informative / actionable to the average user since it says why the recipient name count is wrong. There are error messages of this similar phrasing above too that should probably be addressed. 

## Tzadiko | 2025-03-18T17:09:10+00:00
Unsure what 'Zero may be used as a filler' means. Consider rephrasing.

## Tzadiko | 2025-03-18T17:11:00+00:00
This is moreso a message for a developer, instead of a message for a user. The user (especially a non-technical one) will not have any idea what this (an overload) means. Consider rephrasing.

## Tzadiko | 2025-03-18T17:12:17+00:00
I'm personally not a fan of the naming `_v2`. If a new developer comes and reads this, they will have no idea which one to use (v1 or v2). V2 may imply that V1 is no longer used, or that V2 is 'better' than V1. Consider renaming this to `on_make_uri_multiple` or something similar. You'll need to make this change to similar methods like `on_parse_uri_v2` and lines like these: https://github.com/monero-project/monero/pull/9830/files#diff-ffc587ad00e4573484c0430da9e42b7690f664de49df6f52b1a5be9b48833605R129

## Tzadiko | 2025-03-18T17:13:47+00:00
Why std::? I haven't seen this on anything other than MSVC, which isn't supported by Monero.

## Tzadiko | 2025-03-18T17:15:42+00:00
Can you make this into multiple tests? Youre testing quite a bit here, and each comment before the assert suggests something else is being tested. You can create a test fixture and use it to pass data that's common to each test to.

## fxrstor | 2025-03-19T01:34:30+00:00
Yes. The developer doesn't know if he gets a muti recipient URI. So, the v1 function fails. v2 can process both URIs.  Hence, it is also “better” than the v1. Plus, the v1 function is just there for ensuring backwards compatibility so the change doesn't break old clients. Take a look at old PR (#9756)

## fxrstor | 2025-03-19T01:37:03+00:00
Please elaborate. I don't understand.

## Tzadiko | 2025-03-19T02:15:54+00:00
Sure, so you have six groupings of asserts in one test method (test_multi_uri). You have lines 252 to 261, lines 269 to 279, lines 290, lines 296, lines 310 to 311, and lines 316 to 319. Each of these groupings as a comment marked "Case: ...". So clearly there are 7 cases being tested. Each case should be its own test with a unique test name. Otherwise, if this massive test fails, it will be hard to determine which part failed (despite a line number being associated with the failure message). 

If some of these tests share data, build a test fixture that contains said data and use it accross multiple tests to avoid code duplication. More on test fixtures here: https://docs.pytest.org/en/6.2.x/fixture.html

## Tzadiko | 2025-03-19T02:22:55+00:00
Im not sure if you'll ever hit this condition, unless there's a bug. Nonetheless I suggest you change it to something like "Multiple recipient addresses parsed out of transaction intended for one recipient.". You're moreso telling the user that there is a bug in your parse_uri moreso than there is an error that they themselves can fix. Hence, Im not sure how valuable this check even is. I want someone else to chime in.

## fxrstor | 2025-03-19T08:41:16+00:00
I think it's fine right now imo. There is no requirement of an additional data-type. Plus, it also adds an extra negligible work to create and assign the structure. What do you think?

## fxrstor | 2025-03-19T09:40:58+00:00
Suggested by SNeedleWoods at #9756 

## fxrstor | 2025-03-19T09:44:56+00:00
OK. So you mean to split the `test_multi_uri` function into multiple functions, because it is vast and combines multiple test cases into one making it complex? 
Yes, those tests also share some variables. So you mean to pass the variables via test fixtures in order to avoid code redundancy. 

> Otherwise, if this massive test fails, it will be hard to determine which part failed (despite a line number being associated with the failure message).

It's easy, at least for me (as far I've seen) to determine which part fails, due to Python giving the line number. Not sure what you meant by `hard` but the suggestions seems to be good, helping the code to be more readable. Please let me know if this is exactly what you meant.

## fxrstor | 2025-03-19T09:48:59+00:00
![image](https://github.com/user-attachments/assets/f96a617c-3de4-4230-bb86-9c069a9c06be)
@Tzadiko 

## fxrstor | 2025-03-19T09:50:31+00:00
![image](https://github.com/user-attachments/assets/f96a617c-3de4-4230-bb86-9c069a9c06be)
SNeedlewoods (see #9756) said that they don't want to have "non-standard" types as arguments for methods in the Wallet API. FYI, we had a type for simplifying things as you said but later to the comments, I removed it and modified the code so it only accepts standard types. What do you think?

## Tzadiko | 2025-03-20T14:00:58+00:00
In practice, many codebases use uint32_t without the std:: prefix for brevity, especially when working with C-compatible code or when the project includes <stdint.h>. They usually use std::uint32_t when they include `<cstdint>`. Just use uint32_t to conform to other parts of the code. Notice other parts of our code don't use `std::uint32_t`. They're both the same type. Proof here: https://godbolt.org/z/xYexErzWG

## Tzadiko | 2025-03-20T14:02:31+00:00
I don't think my comment here should hold up the PR, since it's not a change you made here, but I'd like to hear from someone else about this too.

## Tzadiko | 2025-03-20T14:15:12+00:00
Just because net_utils has a mispelled function doesn't mean you should double down on the misspelling.

## Tzadiko | 2025-03-20T14:26:05+00:00
I don't think it's better, it's just different. One takes in a list of items and the other takes in a single item. They communicate different intents. I'd be confused if I had one address, one amount, and one recipient name (etc) but the API that implies it is more current, and should hence be preferred (v2), asked for several of them. That's why I think the postfix `_multi` would be a better choice than `_v2`

## Tzadiko | 2025-03-20T14:31:11+00:00
> So you mean to split the test_multi_uri function into multiple functions, because it is vast and combines multiple test cases into one making it complex?

Yes

> So you mean to pass the variables via test fixtures in order to avoid code redundancy.

Yes

> It's easy, at least for me (as far I've seen) to determine which part fails, due to Python giving the line number.

Let me share an example, I was looking at functional_tests_rpc.py yesterday. It calles a transfer test in transfer.py. transfer.py's `transfer` test is massive and failed on line number 177. Naturally I looked at the function call closest to this line failure to see what the issue was. I ended up spending 20 minutes looking at how this function call may have been affected by my changeset only to then notice the variable that was being asserted was from a call 20+ lines before this assert, that had several other asserts before it. So yes, while you may have the line number, it greatly reduces confusion and the need to navigate around the test when there is one thing under test per test, instead of many things.




## Tzadiko | 2025-03-20T14:32:59+00:00
Why was this marked as resolved without being addressed? Please only mark as resolved after you've pushed changes.

EDIT: Can resolve now.

## Tzadiko | 2025-03-20T14:35:47+00:00
Why was this marked as resolved without being addressed? Please only mark as resolved after you've pushed changes.

## Tzadiko | 2025-03-20T14:35:57+00:00
Why was this marked as resolved without being addressed? Please only mark as resolved after you've pushed changes.

## Tzadiko | 2025-03-20T14:39:25+00:00
I think it's a bit of common sense. Review your changed error messages, anything that doesn't explicitly tell you what is wrong / is unactionable is a bit worthless. For example, `Incorrect tx_amount count` doesn't tell you what's wrong with the transaction count (that the sizes and amounts are not equal). Something like `tx_amount count of X is not equal to address count of Y` (fill in X and Y). 

## Tzadiko | 2025-03-20T14:42:00+00:00
> I think it's fine right now imo. 

Yeah, just leave as is.

No need to tag me in every comment, please. It just spams my email. If it's been over 24 hours and you don't hear back, feel free to tag me once.

I replied to this in another comment.

## fxrstor | 2025-03-20T14:42:08+00:00
I think I'll need someone else to also comment on this. 

## Tzadiko | 2025-03-20T14:46:57+00:00
Why was this marked as resolved without being addressed? Please only mark as resolved after you've pushed changes.

## Tzadiko | 2025-03-20T14:47:13+00:00
Why was this marked as resolved without being addressed? Please only mark as resolved after you've pushed changes.

## Tzadiko | 2025-03-20T15:57:30+00:00
This doesn't output what you think it does. This is doing `const char*` + `size_t` which is dangerous. You need to do:

`std::string{"The counts of addresses ("} + std::to_string(addresses.size())` ...

Or use boost::format / similar string formatting. See example as to why your code doesn't work: https://godbolt.org/z/1axqzrEdK

## Tzadiko | 2025-03-20T15:59:30+00:00
Issue here too, same as previously mentioned.

## Tzadiko | 2025-03-20T16:00:34+00:00
I think you mean "Please don't specify amounts if ..." instead of "Please use zero values". Although I'm not personally sure what not specifying amounts does.

## Tzadiko | 2025-03-20T16:02:27+00:00
In monero/tests/ there is a README.md file that lists the dependencies one needs to install to run the tests. I would add pytest there. 

## Tzadiko | 2025-03-20T16:02:48+00:00
Looks better now. Thanks

## Tzadiko | 2025-03-20T16:11:30+00:00
I think the CMakeLists.txt in tests/functional_tests/ may also need to have `import pytest;` added

![image](https://github.com/user-attachments/assets/2035e833-cceb-4e78-b170-be25ea63140b)


## iamamyth | 2025-03-25T23:06:49+00:00
As I recall, the version suffix exists because there was a stated intent to deprecate and eventually eliminate the prior endpoint, in favor of the new endpoint. If the plan changes to support both indefinitely, then I agree, v2 would be a poor naming choice.

## iamamyth | 2025-03-25T23:10:14+00:00
I'd rather you just eliminate this piece if the feature is still supported, it's an obvious one line change and the commented version only brings confusion.

## fxrstor | 2025-03-26T01:47:49+00:00
Yes because the user never knows what type of uri is coming, it could be single recipient or multi. In case of multi uri, the legacy function fails. The newer functions work on both cases. If we continue on supporting both legacy and newer functions, it would introduce complexities in the code. First the user has to check if the uri is multi by calling the legacy function, then if it is, then call the newer function which handles the data in a slightly different way using vectors.

## selsta | 2025-04-13T17:54:05+00:00
Please check here and in other places in this file that the indentation is 2 spaces, if you change it back to 2 spaces the diff will get slightly smaller in some places.

## selsta | 2025-04-13T17:55:32+00:00
Is this neccessary?

## selsta | 2025-04-23T14:13:11+00:00
```suggestion
    error = (boost::format("The counts of addresses (%1%), amounts (%2%), and recipient names (%3%) do not match.") % addresses.size() % amounts.size() % recipient_names.size()).str();
```

The current syntax appears incorrect. Please also update the other places that use `boost::format`.

https://www.boost.org/doc/libs/1_88_0/libs/format/doc/format.html

## selsta | 2025-04-23T14:15:01+00:00
```suggestion
    bool parse_uri(const std::string &uri, std::vector<std::string> &addresses, std::vector<uint64_t> &amounts, std::vector<std::string> &recipient_names, std::string &tx_description, std::vector<std::string> &unknown_parameters, std::string &error) override;
```

So that the signature matches with the existing one. Please do the same thing with `make_uri`.

## selsta | 2025-04-27T17:03:23+00:00
Did you test if this correctly sets the description afterwards?

## selsta | 2025-04-27T17:06:10+00:00
Should `amounts_split.erase()` be added here like with addresses?

## fxrstor | 2025-04-28T15:30:45+00:00
Yes. If there is a trailing semicolon, you will get an extra empty‐string token in the `amounts_split` vector which will cause the next if condition to fail. Thanks for pointing.

## fxrstor | 2025-04-29T15:07:54+00:00
> Did you test if this correctly sets the description afterwards?

How am I supposed to test that?

## selsta | 2025-04-29T15:53:39+00:00
Compile it, and open moneo-wallet-cli and make a transaction with a URI containing a description and see if it sets the description.

## fxrstor | 2025-05-01T10:21:53+00:00
> Compile it, and open moneo-wallet-cli and make a transaction with a URI containing a description and see if it sets the description.

I'm not really sure how I can see the description. I've tried googling and asked chatGPT but it was unsure.
This is what it says when I send a transaction to a wallet in testnet.
![image](https://github.com/user-attachments/assets/6d970d16-3d71-4351-8d1f-c30e3eaca103)
![image](https://github.com/user-attachments/assets/3114d118-3a48-45a9-adb7-2ebbac764f78)
It behaves the same as the original wallet-cli.

## fxrstor | 2025-05-01T14:29:13+00:00
@selsta I have modified the transfer_main code in simplewallet FYI.

## selsta | 2025-05-03T15:48:05+00:00
Modified the code but did you test what I wrote?

## fxrstor | 2025-05-03T16:02:08+00:00
Yeah. I linked the image, see it.

## selsta | 2025-05-03T16:04:13+00:00
I was confused because it appears the whole URI gets saved in the description field instead of just the description, or am I misunderstanding the image?

## fxrstor | 2025-05-03T16:16:09+00:00
> I was confused because it appears the whole URI gets saved in the description field instead of just the description, or am I misunderstanding the image?

I have no idea. That is what happens in the original app too iirc. I made so it achieves the same behavior as the original app. I think the description should come under `Tx Note`.

## fxrstor | 2025-05-03T16:54:32+00:00
@selsta Should we push the description to `Tx Note` field?

## selsta | 2025-05-03T16:56:53+00:00
I would say yes, it does not make sense as it is.

## fxrstor | 2025-05-03T17:04:12+00:00
@selsta Alright but in the Destination field, should we display the full URI or just the address itself (the original app displays the full URI)? I wonder what happens to unknown parameters? Where are they stored?

## nahuhh | 2025-05-03T17:18:18+00:00
My 2c.

- Destination should only be the first field or `[address=]`
- tx note should only be the `tx_description=`
- the full uri should not be saved anywhere 



## selsta | 2025-05-03T18:22:24+00:00
Agree with @nahuhh 

## fxrstor | 2025-05-05T06:38:37+00:00
![image](https://github.com/user-attachments/assets/c9863662-bd40-4bcd-8da9-e49f201e06cf)
Yes, it sets the description now (using `set_tx_note`). It doesn't store the full uri anywhere, now prints the address instead of the full uri like it used to do before. Note: the image has debug prints but I've removed them.

## nahuhh | 2025-05-05T13:07:40+00:00
The amount is still in the destination field << perhaps it should be for multiuri?

Please show a tx using multi-uri

## fxrstor | 2025-05-05T14:07:47+00:00
> The amount is still in the destination field

It is afaik. It's the way how it formats (`show_transfers`) in the original wallet-cli. I'll see what I can do.
![image](https://github.com/user-attachments/assets/2a588d72-0050-4a6a-9d30-160b47002378)


## fxrstor | 2025-05-05T14:31:49+00:00
@nahuhh Can you elaborate what I need to do?

## nahuhh | 2025-05-05T15:03:20+00:00
For a multiURI, showing the amount per address looks fine. 

for a single transfer, probably redundant to show. But not a dealbreaker if this was existing behavior 

## fxrstor | 2025-05-05T16:35:14+00:00
> For a multiURI, showing the amount per address looks fine.
> 
> for a single transfer, probably redundant to show. But not a dealbreaker if this was existing behavior

![image](https://github.com/user-attachments/assets/606d0209-3ce7-4e7d-b675-7808b4186dea)
Removed it. Is it fine?

## jeffro256 | 2025-08-30T17:57:39+00:00
These new methods, including `parse_uri()`, do not need to be member functions of `wallet2`. I would recommend making them free functions. If they aren't to be free functions, at the very least mark the member functions as `static`, please. 

## jeffro256 | 2025-08-30T18:01:44+00:00
This is why we shouldn't have `make_uri()`/`parse_uri()` be member functions of `wallet2`: you need to have initialized a `wallet2` instance for no reason. 

## jeffro256 | 2025-08-30T18:02:00+00:00
Same point here 

## jeffro256 | 2025-08-30T18:39:10+00:00
I don't like the format of `<all addresses>?<all amounts>&<all messages>`, etc. Take a look at Bitcoin Unlimited's (node for Bcash) multiple address URI scheme: https://github.com/BitcoinUnlimited/BUIP/blob/master/086.md. The address, amount, and labels are next to each other for each output, which makes for more readable URIs. More importantly however, this allows the field for each output to remain undefined if desired. See this example for an actual use case: https://github.com/BitcoinUnlimited/BUIP/blob/master/086.md#example-1-bar-transaction. Here you can see that the amount field for a tip output at a bar is left empty, which triggers the client code to prompt the user to enter a desired amount. Overall, this type of URI format is *way* more flexible and future-proof than cramming values into a list based on field type, assuming the same structure for every single output in a transaction. 

## selsta | 2025-08-30T18:42:18+00:00
FWIW this is the format we decided on. I don't remember why exactly.

https://github.com/monero-project/monero/blob/master/docs/URI_SCHEME.md

## jeffro256 | 2025-08-30T19:38:35+00:00
Is it too late to change it if no one has a working implementation yet AFAIK?

## selsta | 2025-08-30T19:41:21+00:00
https://github.com/monero-project/monero/issues/7731#issuecomment-848102482

Here is some background on when it was decided on. I'm not sure if anyone actually implemented this.

## jeffro256 | 2025-08-30T19:52:53+00:00
Seems like @trasherdk and @benevanoff wanted a similar approach to what I am bringing up here: packing info per-output, instead of packing per-value-type.

## jeffro256 | 2025-08-30T19:54:40+00:00
I don't know if I'd worry much about the byte-length of the URIs unless it becomes a practical problem. Seems a bit like a premature optimization, IDK. QR codes can handle fairly large URIs. 

## FiatDemise | 2025-08-31T18:59:48+00:00
> Seems like @trasherdk and @benevanoff wanted a similar approach to what I am bringing up here: packing info per-output, instead of packing per-value-type.

As far as I know, XMRChat is the first and only one to use this, and we don't have anything in prod yet. We're fine with whatever format you prefer. Just hope it can be done quickly as we'd like to implement this soon.

## BroccomaglosAdolfas | 2025-09-05T20:17:08+00:00
This variable (path_part) is unused

## iamamyth | 2025-09-10T19:43:48+00:00
I think the design goal here was for the multi-payment scheme to match the original single-payment scheme in case of a single payment, i.e. it merely extends the existing scheme, rather than replacing it, making for a graceful upgrade (in particular, newly generated single-payment URIs work fine with existing consumers). That said, one could argue a "graceful" upgrade creates a false continuity, as multi-payments might parse OK with software that only supports single payments, making for user headaches. In any case, looking at the current tests, something has gone way off the rails, because the tests now include an `output` parameter, which they shouldn't.

If you want to change the scheme, such that it's not compatible with the old one, it should be a clean break (i.e. a new scheme name), otherwise you end up violating the core principle of URI namespacing.

## fxrstor | 2025-09-11T02:34:47+00:00
I get what you're saying. The solution is to change the scheme name so the old clients don't accidentally parse the new multi recipient URI. I wonder what should be the new URI scheme name if that's what you mean? Additionally, should the new functions parse and generate old scheme URIs if there's it's a single recipient? 

## iamamyth | 2025-09-11T17:24:28+00:00
We can't decide on minor issues like the scheme name before resolving the major question of whether to split the scheme at all. So, it seems any work should be on hold for now. I see two decent options:

1. Extend the scheme to preserve the single-payment format and intentionally differ from it for multi-payment URIs in a manner that makes it highly unlikely multi-payment URIs introduce false compatibility.
2. Keep the current single-payment URI scheme as-is, and introduce a new, multi-payment URI scheme.

In either case, I think single-payment URIs should work as-is (i.e. no changes to the single-payment use case, anything that previously worked will still work); otherwise, a lot of software breaks for a minor upgrade.

## fxrstor | 2025-09-11T17:33:54+00:00
Can we not modify the old functions to reject the URI as multi-recipient if it contains any `output` parameter? If this is possible, this would eliminate the problem of false compatibility if I'm not wrong?

## jeffro256 | 2025-09-11T22:19:55+00:00
> I think the design goal here was for the multi-payment scheme to match the original single-payment scheme in case of a single payment, i.e. it merely extends the existing scheme, rather than replacing it, making for a graceful upgrade (in particular, newly generated single-payment URIs work fine with existing consumers). 

[The scheme I proposed](https://github.com/monero-project/monero/issues/7731#issuecomment-3239824513) achieves this goal. AFAICT, the current PR does not implement this scheme.

## jeffro256 | 2025-09-11T22:22:07+00:00
> We can't decide on minor issues like the scheme name before resolving the major question of whether to split the scheme at all. So, it seems any work should be on hold for now. I see two decent options:
> 
>     1. Extend the scheme to preserve the single-payment format and intentionally differ from it for multi-payment URIs in a manner that makes it highly unlikely multi-payment URIs introduce false compatibility.
> 
>     2. Keep the current single-payment URI scheme as-is, and introduce a new, multi-payment URI scheme.
> 
> 
> In either case, I think single-payment URIs should work as-is (i.e. no changes to the single-payment use case, anything that previously worked will still work); otherwise, a lot of software breaks for a minor upgrade.

I vote for 1. The multi-recipient format should be backwards compatible for a single recipient.

## jeffro256 | 2025-10-13T19:03:53+00:00
I'm fairly certain that this part is breaking payment ID handling, I would revert it back.

## jeffro256 | 2025-10-13T19:09:28+00:00
Support for unencrypted long payment IDs should be dropped.

## jeffro256 | 2025-10-13T19:15:58+00:00
Need a namespace here. I recommend `tools::wallet`

## jeffro256 | 2025-10-13T19:17:53+00:00
There's not really a reason for `amount` to be 128-bit since amounts for a single transaction output are only 64-bits. You can do a lot of simplification if you drop support for 128-bit amounts. If someone really wants to receive more than 18.4 million XMR in a single transaction, they can make a URI with two amount fields lol.

## jeffro256 | 2025-10-13T19:20:30+00:00
You can also use [`boost::to_upper()`](https://www.boost.org/doc/libs/1_41_0/doc/html/boost/algorithm/to_upper.html) here.

## jeffro256 | 2025-10-13T19:29:40+00:00
I would add an overload which returns a vector of `cryptonote::tx_destination_entry`, a short payment ID, and the nettype in place of a vector of address strings and amounts, since the very next move of parsing a URI is most likely going to be parsing the addresses (like is the case in `simplewallet`).

## fxrstor | 2025-10-14T02:18:31+00:00
uint64_t is ok for Monero and other assets but when it comes to Ethereum, it uses 18 decimals. 64 bits won't cut it. Even 20 ETH in wei exceeds 2^64-1, that's why I intend to have uint128 so there wouldn't be any overflows.

## plowsof | 2025-11-21T13:56:31+00:00
"add multiple-URI support to `parse_uri` and `make_uri`" but glancing over some of these function names im wondering what.... so, sending monero to multiple addresses why currency table? scale and decimals? suffix to currency? i get supporting both atomic units but im very confused

## fxrstor | 2025-11-26T15:54:32+00:00
Sorry, it was a typo that went unnoticed for... quite a long time. The [scheme that jeffro suggested](https://github.com/monero-project/monero/issues/7731#issuecomment-3239824513) supported multiple currencies in the uri.

# Action History
- Created by: fxrstor | 2025-03-09T14:22:10+00:00
