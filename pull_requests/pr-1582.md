---
title: background mining
source_url: https://github.com/monero-project/monero/pull/1582
author: revler1082
assignees: []
labels: []
created_at: '2017-01-16T04:33:11+00:00'
updated_at: '2017-02-14T19:13:58+00:00'
type: pull_request
status: merged
closed_at: '2017-02-12T21:11:56+00:00'
merged_at: '2017-02-12T21:11:56+00:00'
---

# Original Description
my extremely poor attempt at background mining, definitely not ready for prime-time.
mostly throwing it up here for feedback/proof of concept/etc.

only works for linux, only tested on ubuntu.
"start_mining [addr] [thread_count] true"

feel free to point out any and all stupidity/terribleness, thanks everyone :)


# Discussion History
## vtnerd | 2017-01-16T23:01:35+00:00
Incorrect description

## vtnerd | 2017-01-16T23:03:54+00:00
This is only going to work on systems with this /proc layout ... should the process arguments be `#ifndef` on systems where this is known in advance to not be available (i.e. WIN32, etc.)?

## vtnerd | 2017-01-16T23:04:29+00:00
Is this check useful?

## vtnerd | 2017-01-16T23:06:03+00:00
Should the remainder of these arguments be in a `if (is_background_mining_enabled())` block? Also, if background mining is disabled, should it verify that none of these arguments appear?

## vtnerd | 2017-01-16T23:18:52+00:00
These double bools are confusing. Can't this function just return true when mining should start, and return false in all other situations? The function is already logging errors.

## vtnerd | 2017-01-16T23:19:43+00:00
Same here, except `return true` should be used on error.

## vtnerd | 2017-01-16T23:20:10+00:00
All of the functions through this section should be returning the `uint64_t` or `uint8_t` by value.

Also, a number of these functions are not using any member variables. I recommend either marking them as static, moving them to the cpp entirely (remove from header), or dropping the first parameter `cpu_data_start` and grabbing it as a member variable of the function.

## vtnerd | 2017-01-16T23:52:14+00:00
These might not be available on all `!Win32` systems ...

## vtnerd | 2017-01-16T23:53:09+00:00
These should be the same values returned by the posix system call `times`, which hopefully works across all `!Win32` systems. There is also `getrusage`, which returns seconds/microseconds instead of clock ticks.

## vtnerd | 2017-01-16T23:58:59+00:00
I like using the member variable accessor here (and similar places throughout patch) ... can't unintentionally modify the variable ...

## vtnerd | 2017-01-17T00:00:54+00:00
Only `m_cpu_data_start` is needed as a member variable. `m_cpu_data_end` can be created on the stack when polling, and then copied into `m_cpu_data_start` when finished.

## vtnerd | 2017-01-17T00:01:19+00:00
Can these constants by in the cpp? Do they need to be exposed in the header for some reason?

## vtnerd | 2017-01-17T00:11:36+00:00
I don't follow this section - were other variables supposed to be checked? This looks to be checking the same 2 variables 3 times.

## vtnerd | 2017-01-17T00:13:03+00:00
Can these be `constexpr const` values instead? I don't see an advantage to using the c-preprocessor in this context.

## vtnerd | 2017-01-17T00:24:28+00:00
I recommending removing cases (in this patch) of built-in types being passed by const reference, and passing-by-value instead. Hopefully the remainder of the codebase can be move towards that goal as well ...

## vtnerd | 2017-01-17T01:05:43+00:00
I wanted to call this out separately ... this means by default background mining initiates when the users system is 3% idle ... ? That seems fairly low to me, but any default value is arbitrary I suppose.

## vtnerd | 2017-01-19T04:08:23+00:00
Did a tab sneak in here?

## vtnerd | 2017-01-19T04:13:10+00:00
Isn't this set before starting any threads? If so, drop volatile.

## vtnerd | 2017-01-19T04:19:23+00:00
I think you want `std::chrono::steady_clock`. Before the loop have `std::chrono::steady_clock::time_point`, then here you can do `if (std::chrono::steady_clock::now() - last_check) < std::chrono::seconds(...INTERVAL_IN_SECONDS))`.

In fact the constructor for `std::chrono::seconds` is contexpr, so  the constant itself can be wrapped in a `std::chrono::seconds` object, and `IN_SECONDS` becomes superfluous.

Although its likely that this check should be removed (see comments below).

## vtnerd | 2017-01-19T04:32:30+00:00
Why reset these? I'm guessing the issue is that the mining threads are still active for a short period of time, which yields incorrect idle / processing values. If that's the case, the short sleep above is entirely due to the race condition between setting the flag and waiting for the mining threads to stop.

I would move the short sleep and the fresh check for process time to this area right here. It should be _slightly_ more obvious why the short sleep and additional process time check is present. Then a sleep equal to the check time can be done at the top of the loop `boost::this_thread::sleep_for(BACKGROUND_MINING_MINER_MONITOR_INVERVAL)`.

Ideally this would block until all mining threads stop so the result is more accurate. For this purpose, I guess this is good enough ish ?

## vtnerd | 2017-01-19T04:32:58+00:00
`std::atomic<bool>`

## vtnerd | 2017-01-19T04:36:09+00:00
This does not seem like a variable that we need. But if it must be added, make it a `std::atomic`.

## vtnerd | 2017-01-19T04:50:07+00:00
FILETIME is just a 64-bit value split into two 32-bit values. No idea why, must be an implementation detail. The code below is just converting to a standard 64-bit value.

I would clean up some of the commenting here; hopefully as a group we understand the code being comitted into the project.

## vtnerd | 2017-01-19T05:07:15+00:00
Shouldn't this sleep be skipped when mining? And Is this variable even necessary? Seems like it should be sleeping at the constant `BACKGROUND_MINING_MINER_MONITOR_INVERVAL` rate.

Also what if this were:
```c++
bool is_checking_idle_usage = false;

...
else if (m_is_background_mining_enabled) {
    if (!m_is_background_mining_started) {
        const boost::lock_guard<boost::mutex> lock{m_background_mining};
        if (m_stop)
            break;

        if (!m_is_background_mining_started) {
            wait_for_idle();
           if (m_stop)
               break;

            is_checking_idle_usage = true;
            m_is_background_mining_started = true;
        }
    }
}
...

if (is_checking_idle_usage) {
    if (not_idle()) {
        is_checking_idle_usage = false;
        m_is_background_mining_started = false;
    }
}
```

So all worker threads would block at the mutex while _one of_ the worker threads checked for idle usage. Once it succeeded, it would be the sole thread to check for the stopping condition. The motivation would be to prevent an additional thread from being created, with the downside that one mining thread has to do a few extra system calls after each mining attempt. Perhaps the check could be done every couple attempts, if the system calls were a little expensive.

## vtnerd | 2017-01-19T05:11:00+00:00
Might be a useful static function local to this cpp file since the expression is used 4 times.

## vtnerd | 2017-01-19T06:14:18+00:00
This (semi) substantial thread change is by no means "mandatory". We can get background mining in first, then I can attempt another patch at this for a separate review. I'm not sure on the process for that, so @moneromooo-monero  or @fluffypony would have to comment on that.

## revler1082 | 2017-01-19T16:54:10+00:00
Hmm, that sleep there is how I throttle the miner threads. 
The BACKGROUND_MINING_MINER_MONITOR_INVERVAL constant is the time between when the monitoring thread re-checks miner cpu usage. Right now I think I made them the same, but I think in the future the amount of time between checking whether to start will be greater than checking whether to stop. 

The mutex version is definitely an interesting idea, and I have no problem refactoring to whatever you guys agree is better. 


## revler1082 | 2017-01-19T17:37:51+00:00
Thanks, should improve accuracy!

## revler1082 | 2017-01-19T17:38:22+00:00
Yes sir

## revler1082 | 2017-01-19T17:47:03+00:00
Hmm, I guess I want to start tracking from when mining starts. Say I don't reset it when mining pauses, a bunch of time passes, mining resumes, process usage will be lower than what's going on because I'm taking into account all of the down time when mining wasn't happening?

## revler1082 | 2017-01-19T17:48:44+00:00
Will do

## revler1082 | 2017-01-19T17:51:36+00:00
Just thinking future-wise, since the function is there, what if someone calls set_is_background_mining_enabled(false) after the threads are created/started? Ideally the background logic would be the only thing to stop and mining would continue full speed.

## vtnerd | 2017-01-19T19:38:43+00:00
Why is the sleep interval at the top of this loop for a shorter time period than the entire `MONITOR_INTERVAL`? Why is the logic to explicitly wait one short sleep cycle before grabbing the initial value for `previous_process_time`?

The way this is currently written, it appears the goal is to wait a short interval _after_ enabling background mining before setting the `previous_process_time` value. Look at every other branch - if the entire `MONITOR_INTERVAL` has not elapsed the loop is `continue`d to sleep a short time interval again. Only one branch does anything different, and it is the case where `previous_process_time` is set for the first time after mining is enabled.

Is the intent to wait after enabling mining before setting `previous_process_time`? Or ...? What I am suggesting is basically:

```c++
while (!m_stop) {
    boost::this_thread::sleep_for(BACKGROUND_MINING_MONITOR_INTERVAL);
    if (m_is_background_mining_started) {
        if (percentage(...) + percentage(...) < get_idle_threshold()) {
            m_is_background_mining_started = false;
            ...
        } else {
            ...
        }
    } else {
        if (get_idle_threshold() <= percentage(...)) {
            m_is_background_mining_started = true;
            boost::this_thread::sleep_for(std::chrono::milliseconds(100));
            // this is the only case where something is done between MONITOR_INTERVALs
            ...
            if (!get_process_time(previous_process_time))
            ...
        } else {
            ...
        }
    }
}
```

## revler1082 | 2017-01-19T20:06:00+00:00
I wanted to keep the program responsive since I can't be sure what the
monitor interval will end up being. Even at 10 seconds, a user issuing a
stop mining command might have to wait the whole 10 seconds for the
background thread to end. That was my intent anyway, lol. We can definitely
change it if you guys want.

## vtnerd | 2017-01-19T20:19:49+00:00
I missed that there was a separate configuration option for targetting CPU level. The description explains it clearly, but the internal name is similar to the idle threshold value. Instead of variations on "mining threshold", how about variations of "mining target"? I hate to do this nitpick, but I think it would help with code clarity a bit (and its a search + replace to do).

As for the mutex idea, don't do it if you are not comfortable implemented the logic for it. Updating to the lock method should not be a difficult refactor.

## vtnerd | 2017-01-19T20:23:01+00:00
Well, drop `volatile` and make it `std::atomic`. Although I wonder - should  the setter be `public`? If it were no longer public, it could be guaranteed that the `miner` class would have to be updated for a threading issue to become a concern (and thus the `std::atomic` wrapper could be dropped). Reading from an atomic isn't "free", so if its unnecessary ...

## vtnerd | 2017-01-20T22:21:18+00:00
I really think you are better off moving the "setter" for background mining to private space. The thread is not launched by the setter, so `miner::start` would have to be called afterward (unless it was initialized as enabled, then disabled, then re-enabled). Toggling background mining can always be a future improvement.

## vtnerd | 2017-01-20T22:23:42+00:00
I was confusing earlier due to the length of my comments, but I was suggesting that this be moved to the `else` block where background mining starts. It just makes the logic easier to read. And it would answer my question as to whether the sleep between enabling background mining and grabbing these values is intentional.

## revler1082 | 2017-01-20T22:24:01+00:00
So private setter and remove is_enabled checks in the background thread?

## vtnerd | 2017-01-20T22:27:17+00:00
I don't see why this check is necessary ... ?

## vtnerd | 2017-01-20T22:29:19+00:00
That would be my recommendation. Because if the command line did not specify background mining, this thread never starts (I think). If that's the case, then the setter does not work as exactly intended in some situations.

## revler1082 | 2017-01-20T22:30:30+00:00
If **m_is_background_mining_started_cond** is notified by the stop() call, control breaks from the **while( !m_is_background_mining_started )** loop. I could let the function run through one more time, but I figured it's just more responsive to stop on the spot rather then calculate a few more hashes.

## revler1082 | 2017-01-20T22:38:34+00:00
If the behavior were correct though (i.e. enabling bg mining after start works as expected), would that be preferential, or should I still go with the above? 

## vtnerd | 2017-01-22T17:53:55+00:00
I forgot about the second `while` loop here.

## vtnerd | 2017-01-22T18:03:16+00:00
I would just leave this for a future modification. The easiest way to handle "toggling" of background mining is force everything through the `start` function. You have to `stop` mining first, then change its mode. A bit clunky, but its the easiest logic to get correct. And the details for specifying this have to be worked out - can the mode be modified through a RPC call? Or just by local command line, etc. ? 

Without "toggling" the mode you can already forcibly start/stop background mining without taking down the daemon, which seems convenient enough for a first go.

## vtnerd | 2017-01-27T15:39:44+00:00
Re-use `on_ac_power` here ?

## vtnerd | 2017-01-27T15:41:22+00:00
There is no point in doing the other checks if `on_ac_power` is false here. Something like:
```c++
if (m_is_background_mining)
{
    // ... stuff ...
}
else if (ac_line_status())
{
    // ...other checks...
}
```
just to bail quicker to sleep.

## vtnerd | 2017-01-27T15:49:19+00:00
Another thing I should've noticed earlier - should this return an enum? `{ kFailure, kAcPower, kBatteryPower}` ? If someone enables background mining on a system that fails to grab ac status, it will just continually check with little hope of ever being resolved automagically. Same thing with the idle stats.

... something that can be marked as a future improvement via // TODO ...

## vtnerd | 2017-01-27T16:00:28+00:00
Just thought about this ... I would set `m_is_background_mining_started = false;` somewhere up here. If `mining::stop` then `mining::start`, I think the idle logic should be re-run. Or is it intended to immediately start again?

## vtnerd | 2017-01-27T16:02:38+00:00
Ok, I will try again, last time I promise. This block is attempting to grab the process times when background mining is first started. Can it be moved to the section where background mining is started and done unconditionally? I.e. `m_background_mining_start = true; get_system_times(...);`.

When taken with my other suggestion, the idle logic will always have to run before the if block is entered.

## revler1082 | 2017-01-27T18:52:40+00:00
Haha, no worries at all, please always feel free to point out everything. I will be moving the code per your recommendation, but in case you're interested, my motivation was to more accurately capture the processes' CPU usage, which my code doesn't even assure. I was simply going by if I let some time pass some mining will probably occur in the interim. I probably have this wrong, but this was my thought process. 
![image](https://cloud.githubusercontent.com/assets/852762/22383451/cfe3e160-e497-11e6-8a25-8c46d1c86e1c.png)


## vtnerd | 2017-01-27T19:40:32+00:00
This is why I advocated moving the initial `get_process_times` to the block below - an explicit sleep in that portion would be more obvious to the intent. I was previously unsure if it was intentional, and asked in a typical rambling manner. 

The other benefit of moving below is a sleep time targetted specifically for the miner threads. Do not make this based on any user input obviously - its already racy. Also comment that this is needed because of the CPU targetting, otherwise it would be unnecessary (I think).


## moneromooo-monero | 2017-02-03T18:45:13+00:00
I'd rather avoid changing logs back to a severityless version, unless you  have a good reason for it.

## moneromooo-monero | 2017-02-03T18:50:08+00:00
Exception are generally  best caught by const ref

## moneromooo-monero | 2017-02-03T18:50:32+00:00
MGINFO will be visible by default. Consider MDEBUG for debug stuff.

## moneromooo-monero | 2017-02-03T18:52:19+00:00
Adding __func__ everywhere seems unneeded. Logs already have file:line. If you want function too, you can set MONERO_LOG_FORMAT to have "%func" in addition to its default (see top of contrib/epee/src/mlog.cpp)

## moneromooo-monero | 2017-02-03T18:53:07+00:00
Looks like a syscall failure will cause immediate call again. Is this intended (or am I reading this wrong) ?

## moneromooo-monero | 2017-02-03T18:55:06+00:00
And that's really a debug, or a trace if it gets spammed every 5ms

## moneromooo-monero | 2017-02-03T18:59:08+00:00
"line" ? live ? other ?

## revler1082 | 2017-02-04T18:16:11+00:00
It will go back to the top and sleep again so won't be immediate I believe. 

## revler1082 | 2017-02-04T18:17:55+00:00
Apologies, I'm not understanding this one?

## moneromooo-monero | 2017-02-05T11:00:31+00:00
I'm not either. What does "line" mean here ? Is it a typo ? Did you mean "ac_live_status" ?

## revler1082 | 2017-02-05T14:04:45+00:00
lol, I implemented the battery functionality for Windows first, and the structure that gets populated by the power query has [ACLineStatus as the flag for plug in/out](https://msdn.microsoft.com/en-us/library/windows/desktop/aa373232(v=vs.85).aspx), so I just stuck with that. 

What would you like it changed to?



## deleted_user | 2017-02-06T11:38:29+00:00
battery_power_status?

Because in the UK we'd say 'mains power' rather than 'AC power', whereas battery power is universally understood.

## moneromooo-monero | 2017-02-06T21:42:27+00:00
If it's common, I don't mind. I just never saw "line" being used as "power", so I was surprised, that's all :)

# Action History
- Created by: revler1082 | 2017-01-16T04:33:11+00:00
- Merged at: 2017-02-12T21:11:56+00:00
