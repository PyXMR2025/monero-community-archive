---
title: 'cuprated: --dry-run options with check config parse, ports and paths'
source_url: https://github.com/Cuprate/cuprate/pull/563
author: iskyd
assignees: []
labels:
- C-feature
- A-binaries
created_at: '2025-11-05T23:00:57+00:00'
updated_at: '2025-11-25T15:03:45+00:00'
type: pull_request
status: merged
closed_at: '2025-11-25T15:03:45+00:00'
merged_at: '2025-11-25T15:03:45+00:00'
---

# Original Description
cuprated: adding --dry-run option

### What
Closes: [#<442>](https://github.com/Cuprate/cuprate/issues/442)

This PR add the --dry-run option to check config deserialization, ports availability and path permissions.
There is another pr in the same scope (https://github.com/Cuprate/cuprate/pull/461). This PR takes care of reviews and added file permissions check.

# Discussion History
## hinto-janai | 2025-11-06T15:11:46+00:00
Since this is used a lot it could be imported: `use anyhow::bail;`

## hinto-janai | 2025-11-06T15:13:36+00:00
So that files aren't passed in the future.
```suggestion
    /// Checks if a directory exists and has proper read/write permissions.
    fn check_dir_permissions(path: &Path) -> Result<(), anyhow::Error> {
```

## hinto-janai | 2025-11-06T15:19:53+00:00
`cuprated` at this point hasn't made these directories so it errors here:

```
> cargo r --bin cuprated -- --dry-run --skip-config-warning

Port 0.0.0.0:18080 available.
Error: Path does not exist /home/hinto/.local/share/cuprate
Error Path does not exist /home/hinto/.cache/cuprate
Checks failed.
```

 maybe these checks would be better done with `std::fs::OpenOptions`.

## hinto-janai | 2025-11-06T15:22:12+00:00
To note that this function never returns:
```suggestion
    pub fn dry_run_check(self) -> ! {
```

The end of the function can be:
```rust
let code = if errors.is_empty() {
    println!("All checks passed successfully!");
    0
} else {
    eprintln_red("Checks failed.");
    1
};

std::process::exit(code)
```

## iskyd | 2025-11-06T16:51:41+00:00
@hinto-janai could we consider creating both directories if they don't yet exist during the dry run check?

## hinto-janai | 2025-11-07T00:36:11+00:00
Probably fine although it would be nice if `--dry-run` itself didn't have side effects, I think you could check if the parent directory has the right permissions instead of creating a file.

## iskyd | 2025-11-07T08:41:52+00:00
@hinto-janai we can just check parent permissions even if that doesn't guarantee that the new dirs will have the proper permissions.
We can also check if the dir already exists than check it otherwise check the parent.
Not sure how to properly check permissions without creating a temporary file, it would be unreliable.


## iskyd | 2025-11-07T08:54:51+00:00
Also I think we need to consider that you can choose to don't give write permissions to the parent dir.
I think to properly check permissions we need the directories to be created.

## hinto-janai | 2025-11-11T19:02:20+00:00
Ok, it may be simpler to just create files/directories.

I'm surprised `std::fs::Permissions` lacks thorough checks for this.

## hinto-janai | 2025-11-17T21:13:10+00:00
nit: should be grouped with other 3rd party crates.

## hinto-janai | 2025-11-17T21:23:06+00:00
`self.rpc.{unrestricted,restricted}.{address,port}`, `self.tor.arti.directory_path`, and `self.tor.daemon.{address,listening_addr}` need to be checked, maybe only if they are enabled.

## iskyd | 2025-11-19T19:01:41+00:00
@hinto-janai i have updated the pr adding the check for rpc both restricted and unrestricted if enabled, for the tor listening address and added check permissions for arti directory path.
Why should we also add tor.daemon.address? From the docs I read "The IP address and port of the external Tor daemon to use for outgoing connections.", it seems is not something we bind to. Sorry if I am missing something.


## hinto-janai | 2025-11-19T20:51:31+00:00
> it seems is not something we bind to

Yup, my bad.

## hinto-janai | 2025-11-19T20:59:35+00:00
```suggestion
        if self.rpc.restricted.enable {
            let port = self.rpc.restricted.port;
            let ip = self.rpc.restricted.address;

            match Self::check_port(ip, port) {
                Ok(()) => println!("RPC restricted {ip}:{port} available."),
                Err(e) => {
                    eprintln_red(&format!("Error: {e}"));
                    errors.push(e);
                }
            }
        }

        if self.rpc.unrestricted.enable {
            let port = self.rpc.unrestricted.port;
            let ip = self.rpc.unrestricted.address;

            match Self::check_port(ip, port) {
                Ok(()) => println!("RPC unrestricted {ip}:{port} available."),
                Err(e) => {
                    eprintln_red(&format!("Error: {e}"));
                    errors.push(e);
                }
            }
        }
```

## hinto-janai | 2025-11-19T21:01:48+00:00
This can be:
```suggestion
        let mut error = false;
```

## iskyd | 2025-11-20T13:23:06+00:00
@hinto-janai we cannot directly use self.rpc.restricted.port since check_port takes an u16 while self.rpc.restricted.port is also a DefaultOrCustom.

## hinto-janai | 2025-11-20T21:13:39+00:00
`restricted_rpc_port` and `unrestricted_rpc_port` from `crate::config::rpc` should work.

# Action History
- Created by: iskyd | 2025-11-05T23:00:57+00:00
- Merged at: 2025-11-25T15:03:45+00:00
