---
title: Starting monero-wallet-rpc with offline --daemon-address interferes with wallet
  functionality, even when overriden
source_url: https://github.com/monero-project/monero/issues/10246
author: woodser
assignees: []
labels: []
created_at: '2025-12-02T20:44:24+00:00'
updated_at: '2025-12-03T22:00:03+00:00'
type: issue
status: open
closed_at: null
---

# Original Description
The following issue is observed:

- Start monero-wallet-rpc with `--daemon-address` set to an offline address, e.g. "offline_server_uri" (this prevents monero-wallet-rpc from syncing automatically when a new wallet is initialized).
- Initialize a wallet (via RPC).
- Manually set an online daemon connection via the `set_daemon` RPC call, e.g. `localhost:28081`.
- The resulting wallet fails to function at least as a multisig wallet. After making the wallet multisig and synchronizing with participants, creating a transaction yields the error: "No transaction created". This error does not happen if monero-wallet-rpc is initialized with an online --daemon-address.

This issue requests that calling `set_daemon` fully overrides the startup daemon connection, so the resulting wallet functions correctly at least as a multisig wallet.

# Discussion History
## nahuhh | 2025-12-03T01:06:22+00:00
There are some other weird quirks, such as open_wallet overriding the set_daemon and using the --daemon-address

probably a workaround is to avoid setting a daemon via runtime flag, and instead to only set via set_daemon. untested

## plowsof | 2025-12-03T11:05:57+00:00
could include usage of disabling `auto_refresh` and sending `refresh` manually in the workaround if thats the goal, could be messy 

## woodser | 2025-12-03T16:23:00+00:00
Disabling `auto_refresh` before initializing the wallet is working as a workaround. Thanks.

# Action History
- Created by: woodser | 2025-12-02T20:44:24+00:00
