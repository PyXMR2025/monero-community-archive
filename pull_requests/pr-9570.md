---
title: 'contrib: fix serialization not calling load when only child is opt'
source_url: https://github.com/monero-project/monero/pull/9570
author: '0xFFFC0000'
assignees: []
labels:
- invalid
created_at: '2024-11-14T19:41:22+00:00'
updated_at: '2024-11-16T21:18:54+00:00'
type: pull_request
status: closed
closed_at: '2024-11-15T22:56:43+00:00'
merged_at: null
---

# Original Description
The problem is `monero-wallet-rpc` does not save the state of the wallet when closing the wallet.

As you can see in the image below, the value of `autosave_current` is `false`. When in fact it should be `true` \[1\]. ( The command I am running is default command ` curl http://localhost:18082/json_rpc -d '{"jsonrpc":"2.0","id":"0","method":"close_wallet"}' -H 'Content-Type: application/json'` )

![image](https://github.com/user-attachments/assets/10f547ae-d29b-4f11-a7a1-7f66094ada61)

The reason for this bug is when the _only_ child of the key-value serialization object is optional ( as is the case with the `close_wallet` ), we don't call the function `_load` of the object. This line \[2\] returns `null` and therefore we don't call `_load` method, which initializes the object.  Whether the child section is null or not, we want to call `_load` method, to make sure the correct initialization.

1. https://github.com/monero-project/monero/blob/893916ad091a92e765ce3241b94e706ad012b62a/src/wallet/wallet_rpc_server_commands_defs.h#L2199
2. https://github.com/monero-project/monero/blob/893916ad091a92e765ce3241b94e706ad012b62a/contrib/epee/include/serialization/keyvalue_serialization_overloads.h#L80

# Discussion History
## Boog900 | 2024-11-15T22:33:19+00:00
This is a bad test, this test requires that the `params` field in `ParentObjWithOptChild` is optional (it is not specified as optional).

This test should make `load_t_from_json` error as the `params` field is missing and it is not an optional field. 

## 0xFFFC0000 | 2024-11-15T22:37:10+00:00
I see what you saying, but the point is we don't have ( or require ) param in like substantial of portion of our RPC commands. 

But if we are not able to initialize the `params` it can have serious consequences. Like the one I explained for `close_wallet`.

1. https://www.getmonero.org/resources/developer-guides/wallet-rpc.html#close_wallet

## Boog900 | 2024-11-15T22:39:42+00:00
`params` here is not the same as the `params` in the JSON-RPC it is just the name of the field in the test struct.

`params` in the real JSON-RPC code should be an optional field, like what #9573 does.

## 0xFFFC0000 | 2024-11-15T22:56:18+00:00
Yes, you are right. I am closing this PR. and re-opening our #9573 PR.

# Action History
- Created by: 0xFFFC0000 | 2024-11-14T19:41:22+00:00
- Closed at: 2024-11-15T22:56:43+00:00
