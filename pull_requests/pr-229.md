---
title: 'rpc: custom epee for misc/bin types'
source_url: https://github.com/Cuprate/cuprate/pull/229
author: hinto-janai
assignees: []
labels:
- A-dependency
- A-workspace
- A-docs
- A-types
- A-rpc
created_at: '2024-07-12T20:10:00+00:00'
updated_at: '2024-07-25T19:36:32+00:00'
type: pull_request
status: merged
closed_at: '2024-07-25T15:46:41+00:00'
merged_at: '2024-07-25T15:46:41+00:00'
---

# Original Description
### What
Implement `cuprate_epee_encoding` manually for some types in `cuprate-rpc-types`.

Requires https://github.com/Cuprate/cuprate/pull/227.

### Why
These types have custom epee (de)serialization in `monerod`.


# Discussion History
## hinto-janai | 2024-07-13T01:08:51+00:00
@Boog900 Can you add support for a custom `EpeeObject::write_fields` in the epee macro?

All the custom epee stuff need context from `self`, so the macro's custom `write_field` doesn't work. E.g. here we need access to `self.in_pool`, then serialize fields based off that. Original code: https://github.com/monero-project/monero/blob/cc73fe71162d564ffda8e549b79a350bca53c454/src/rpc/core_rpc_server_commands_defs.h#L242-L259.

This forces us to do manual epee which gets pretty messy because of all the related boilerplate.

## hinto-janai | 2024-07-13T01:09:31+00:00
How should this work? We sometimes serialize 10 fields, sometimes 12.

## Boog900 | 2024-07-13T01:20:45+00:00
Sorry for not realizing this earlier but this type is already defined in `cuprate-wire`:

https://github.com/Cuprate/cuprate/blob/0910c0a231b4e0ef48b6cf0b9d7d48a44cc557a7/net/wire/src/p2p/common.rs#L223-L234

I don't really want to move it to `cuprate-types` but that may be the best option?

## Boog900 | 2024-07-13T01:23:50+00:00
This can be dynamic, however it could be less than 10, epee wont serialize empty container.

## Boog900 | 2024-07-13T01:26:53+00:00
I would make this type an enum of 2 structs (epee objects) and call `write_fields`/ `number_of_fields` on whatever object is being written 

## Boog900 | 2024-07-13T01:29:00+00:00
Also is this type even a bin type or just JSON?

## hinto-janai | 2024-07-14T19:56:02+00:00
Ah my bad, this is only JSON.

## hinto-janai | 2024-07-18T19:34:55+00:00
## Custom epee 1/2
These data types require custom epee.

## hinto-janai | 2024-07-18T19:35:17+00:00
## Custom epee 2/2
These data types require custom epee.

## hinto-janai | 2024-07-18T19:37:33+00:00
Sidenote: are the custom epee implementations correct in this PR?

Is `if $field.should_write() { write() }` required? Should it be `write_field`? or just `write`?

## hinto-janai | 2024-07-18T19:39:45+00:00
For later: `cuprate_epee_encoding` doesn't blanket impl `Vec<impl EpeeValue>` so although this really should be `Vec<KeyImageSpentStatus>`, it's `Vec<u8>` for now.

## hinto-janai | 2024-07-18T19:41:03+00:00
Invariant documented in `lib.rs` - some types will be unreachable and/or not impl for wrong context, e.g. `TxEntry` here is only used in a JSON endpoint, so its epee impl just panics.

This is a workaround because the macro requires all types impl serde + epee.

## hinto-janai | 2024-07-18T19:41:39+00:00
With custom epee impls, we actually need to use some of these macros individually elsewhere, so no `__` prefix.

## hinto-janai | 2024-07-18T19:45:44+00:00
`Distribution::CompressedBinary` has a `compressed_data` field that gets populated _during_ serialization: https://github.com/monero-project/monero/blob/cc73fe71162d564ffda8e549b79a350bca53c454/src/rpc/core_rpc_server_commands_defs.h#L2487.

I think this is a bit weird since this is a field in a data structure, it should be provided by the constructor themselves - i.e. I think the handler should handle this, otherwise we have to comment something like:

```markdown
# Invariant
Leave the `compressed_data` field empty, the serialization will handle it...?
```

## hinto-janai | 2024-07-18T19:47:04+00:00
`enum Status` now has a catch-all and is no longer `Copy`.

Turns out `monerod` emits a lot of `status` strings other than the ones listed...

## Boog900 | 2024-07-22T01:23:45+00:00
It may be a good idea to split each variant fields into a separate structs, the builder would stay the same however it would significantly simplify writing.

## Boog900 | 2024-07-22T18:39:42+00:00
For the JSON version of this type we will need a custom function that writes this type out right?

I think you could use the `serde` from/into attributes to go between a type that has all fields and the type we expose to users.

However if you would rather the handler handle this then I am ok with that

## Boog900 | 2024-07-22T18:40:22+00:00
If this is used in the core wallet(s) then I would want to support them 

## Boog900 | 2024-07-22T18:43:03+00:00
only `write_field` needs to be called, `should_write` is checked in that function.

Everything else looks ok

## Boog900 | 2024-07-22T18:45:17+00:00
blanket impl `Vec<impl EpeeValue>` we can't do this until specialization btw.

## Boog900 | 2024-07-22T18:45:59+00:00
If this is used in the core wallet(s) then I would want to support them 

## hinto-janai | 2024-07-22T21:04:44+00:00
> For the JSON version of this type we will need a custom function that writes this type out right?

Probably yes, but because of the binary string, not the compression.

There is no JSON compressed type, since it must be `binary && compress`: https://github.com/monero-project/monero/blob/cc73fe71162d564ffda8e549b79a350bca53c454/src/rpc/core_rpc_server_commands_defs.h#L2481-L2504.

> However if you would rather the handler handle this then I am ok with that

Or maybe we could hide the (de)compression in the (de)serialization? I.e. there is no `compressed_data` field, only `distribution`. It is always uncompressed from the caller's perspective because the epee (de)serialization handles it internally.

## Boog900 | 2024-07-22T21:32:14+00:00
is this correct? these should be in `read_epee_field!` right?

## hinto-janai | 2024-07-22T23:40:50+00:00
I think this was incorrect before actually, since these fields are only _sometimes_ serialized (e.g. `struct GetBlocksResponsePoolInfoNone`), they won't always exist.

In that case, the fields are left as `None` where `finish()` will do the rest of the work, right?

## Boog900 | 2024-07-24T00:42:21+00:00
> they won't always exist

This should be fine in `read_epee_field` right? there is no requirement for the field the exist there.

`add_field` is called after every field name has been read, by putting these lines after `read_epee_field` we are tying to read these fields after each field has been read which isn't correct. 

> In that case, the fields are left as None where finish() will do the rest of the work, right?

This is what would happen if they were in  `read_epee_field`

## Boog900 | 2024-07-24T00:44:36+00:00

> Or maybe we could hide the (de)compression in the (de)serialization?

Yeah this is what I was getting at, up to you what you think would be better


## hinto-janai | 2024-07-24T19:39:34+00:00
Fields were put back in `read_epee_field!`.

## hinto-janai | 2024-07-24T19:40:29+00:00
(De)compression functions.

Ref: https://github.com/monero-project/monero/blob/cc73fe71162d564ffda8e549b79a350bca53c454/src/rpc/core_rpc_server_commands_defs.h#L45-L72.

## hinto-janai | 2024-07-24T19:41:37+00:00
`distribution` is now always uncompressed from the constructor's perspective.

It gets (de)compressed during (de)serialization in `epee/serde`.

## Boog900 | 2024-07-24T22:29:28+00:00
Monero has 2 varint formats, the one we are using is the epee varint, monerod seems to be using the consensus serialisation varint, which is defined in `monero-serai`: https://github.com/serai-dex/serai/blob/6f34c2ff77ef8a9f8839dbf63f35dcd6dc33f4b3/networks/monero/io/src/lib.rs#L60

## Boog900 | 2024-07-24T22:44:43+00:00
might need to leave this as a todo until #201

## hinto-janai | 2024-07-24T23:53:03+00:00
Changed bodies to `todo!()` for now.

# Action History
- Created by: hinto-janai | 2024-07-12T20:10:00+00:00
- Merged at: 2024-07-25T15:46:41+00:00
