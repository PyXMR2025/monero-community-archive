---
title: Adding initial support for broadcasting transactions over Tor
source_url: https://github.com/monero-project/monero/pull/4988
author: vtnerd
assignees: []
labels: []
created_at: '2018-12-16T23:23:34+00:00'
updated_at: '2019-10-05T02:00:48+00:00'
type: pull_request
status: merged
closed_at: '2019-01-29T14:47:35+00:00'
merged_at: '2019-01-29T14:47:35+00:00'
---

# Original Description
  - Support for ".onion" in --add-exclusive-node and --add-peer
  - Add --anonymizing-proxy for outbound Tor connections
  - Add --anonymous-inbounds for inbound Tor connections
  - Support for sharing ".onion" addresses over Tor connections
  - Support for broadcasting transactions received over RPC exclusively
    over Tor (else broadcast over public IP when Tor not enabled).

# Discussion History
## coneiric | 2018-12-17T06:38:59+00:00
Had to specify `boost::range::join` to compile, like:

```diff
diff --git a/src/p2p/net_peerlist.cpp b/src/p2p/net_peerlist.cpp
index 54fcec1ef..eebbde5a6 100644
--- a/src/p2p/net_peerlist.cpp
+++ b/src/p2p/net_peerlist.cpp
@@ -153,9 +153,9 @@ namespace nodetool
   template<typename Archive>
   void serialize(Archive& a, peerlist_join elem, unsigned ver)
   {
-    save_peers(a, boost::join(elem.ours.white, elem.other.white));
-    save_peers(a, boost::join(elem.ours.gray, elem.other.gray));
-    save_peers(a, boost::join(elem.ours.anchor, elem.other.anchor));
+    save_peers(a, boost::range::join(elem.ours.white, elem.other.white));
+    save_peers(a, boost::range::join(elem.ours.gray, elem.other.gray));
+    save_peers(a, boost::range::join(elem.ours.anchor, elem.other.anchor));
   }
 
   boost::optional<peerlist_storage> peerlist_storage::open(std::istream& src, const bool new_format)
```

## coneiric | 2018-12-17T06:41:39+00:00
My two cents, is there a way to do this without `Boost.Spirit`? I was admonished in another project for the security review overhead that `Boost.Spirit` adds.

## coneiric | 2018-12-17T06:47:37+00:00
Maybe just delete this chunk outright?

## moneromooo-monero | 2018-12-17T18:36:34+00:00
Why are these all prefixed by k ?

## moneromooo-monero | 2018-12-17T18:39:57+00:00
I think we can lose the backward compat baggage now, just supporting addr should be enough.

## moneromooo-monero | 2018-12-17T19:49:02+00:00
s/lk/kl/ (though it doesn't seem to be used for actual conversion so innocuous for now)

## moneromooo-monero | 2018-12-17T19:52:10+00:00
I'd rather an exception, I can see that ending up with peer sent addresses causing exits.

## moneromooo-monero | 2018-12-17T19:53:53+00:00
Are they always full length ? If not, it means you waste space, and peer lists send ~5k of them at once IIRC.

## SRCoughlin | 2018-12-17T21:55:23+00:00
Minor: this will be coming in from `args` so should there be any formatting cleanup?

## moneromooo-monero | 2018-12-17T22:18:35+00:00
Some other code uses those with buffer[x]+1, without checking whether it's in this list. Can we get a pointer to where these are, if another one needs adding later ?

## moneromooo-monero | 2018-12-17T22:27:56+00:00
What is "unknown" in this context ? Doesn't resolve ? Other ?

## moneromooo-monero | 2018-12-17T22:33:03+00:00
That doesn't seem do do any kind of max operation.

## moneromooo-monero | 2018-12-17T22:33:43+00:00
What is the difference between this and span ?

## coneiric | 2018-12-18T00:48:45+00:00
This check fails:

```
/monero/tests/unit_tests/net.cpp:311: Failure
Value of: host.at(net::tor_address::buffer_size() - 1)
  Actual: '\xA0' (160)
Expected: 0u
Which is: 0
---
311             EXPECT_EQ(0u, host.at(net::tor_address::buffer_size() - 1));
(gdb) p host
$1 = "<unknown tor host>\000U\000\001\360\031\361UUU\000\000\300\253\307VUU\000\000\250\323\216VUU\000\000 \337\377\377\377\177\000\000}\026RVUU\000\000\300"
```

## vtnerd | 2018-12-18T02:51:56+00:00
What version of boost are you using? Was it a namespace collision (ambiguity) or a missing keyword type thing? `boost::join` should be available since 1.45+ (1.58 currently required).

## vtnerd | 2018-12-18T02:53:25+00:00
You mean the overhead of reviewing that library itself? I'm _very_ familiar with spirit source for various reasons (provided a couple of small patches), so I feel fairly comfortable using it. I can see why others aren't. FWIW spirit has been put into `contrib/epee/src/http_auth.cpp`. I was thinking of all replacing `boost::lexical_cast` with spirit for performance reasons in epee.

## vtnerd | 2018-12-18T03:09:28+00:00
This is the result of uninitialized memory. Asan doesn't check for that, crud. Will need to start doing build profiles with msan. This patch as-is isn't exploitable, but serialized tor addresses can be rejected on load. I will force-push a change, which is kind of rough because it ruins the review process. I'll copy this version into a new branch with the same top hash for verifying between the two.

## moneromooo-monero | 2018-12-18T14:06:39+00:00
Why = 0 ? This funky init system makes it really unclear whether it gets 0 or rand<>.

## vtnerd | 2018-12-18T22:07:38+00:00
Good point, this will have passed enough network forks. Removed.

## vtnerd | 2018-12-18T22:39:14+00:00
Poorly named, I guess. This is used to retrieve the "max incoming" or "max outgoing" connections for tor and i2p. I will rename this. 

## vtnerd | 2018-12-18T22:48:59+00:00
I assume you meant the usage of `boost::string_ref`.

Internally its identical - a pointer (only to `const char*`) and a `size_t` length. `boost::string_ref` has implicit conversions from `std::string` and null terminated c-strings, so its primarily convenience over span. It also has `std::string` like find functions, `substr`, etc. I use it where typically `std::string` would be used, the data is ascii/utf8, and null-termination isn't required. It allows c-string/string literals to be passed without allocation.

It could be argued that we should be looking for convenience functions for span ... ? I noticed you did that in another review, but that data was typically binary (I think).

## coneiric | 2018-12-19T21:49:57+00:00
> What version of boost are you using?

Using latest boost.

> Was it a namespace collision (ambiguity) or a missing keyword type thing?

Compiler complained about an ambiguous call, collision with the string version of `boost::join`.

## coneiric | 2018-12-19T21:57:04+00:00
> I'm very familiar with spirit source for various reasons (provided a couple of small patches), so I feel fairly comfortable using it.

No worries, if you feel comfortable using it, and more experienced devs don't have a problem with it.

> FWIW spirit has been put into contrib/epee/src/http_auth.cpp

I saw that as well, and it raised my spidey senses. Since it was outside the scope of this PR, I didn't mention it. Vinnie Falco from the Beast project mentioned to me that having Spirit as a dependency adds a lot of review overhead; likely because of the size of the library, and that it deals with serialization (a notoriously hairy domain for security issues). FWIW, that reasoning makes sense to me, and would also serve well as an argument to remove Spirit use from Monero entirely.

## vtnerd | 2018-12-20T04:40:22+00:00
Fixed.

## vtnerd | 2018-12-20T04:41:03+00:00
I've been going by Google style guide for some reason. I have updated to standard camel case, which portions of the codebase uses. Other place use all caps.

## vtnerd | 2018-12-20T04:42:03+00:00
There aren't any other error codes defined by the spec. I could map all unknown error value to a specific enum? 

## vtnerd | 2018-12-20T04:42:27+00:00
That was my intent, deleted.

## vtnerd | 2018-12-20T04:43:04+00:00
I wonder how that other header leaked in newer versions of Boost. Fixed.

## vtnerd | 2018-12-21T17:30:32+00:00
I updated the serialization behavior of both epee and boost. They both now store the host without the null-terminator or any null trailing bytes. Further space savings could be done with base32->bytes conversion (which would compress the size of the struct too!). I didn't think base32 conversion was worth doing right now - thoughts?

## moneromooo-monero | 2018-12-21T19:46:50+00:00
I'm fine with boost_ref, as long as I know why.

## moneromooo-monero | 2018-12-21T19:47:31+00:00
I meant a pointer to wherever the existing ones are defined, so any future ones can be spotted/added.

## moneromooo-monero | 2018-12-21T19:55:28+00:00
Peers share lists of 5k peers on a timer (not sure how often atm). If there are few Tor addresses in the list, it might not be worth changing (and that reminds me those shared lists should have a quota of tor/public addresses).

## vtnerd | 2018-12-21T23:07:32+00:00
Initially I had all members initialized this way instead of an initializer list.

## vtnerd | 2018-12-23T06:00:32+00:00
The Tor daemon doesn't provide the remote address to inbound Tor connections. The `net::tor_address::unknown()` (and the default constructor which is an alias), are used with inbound Tor connections. On debug logging they show up as `<unknown tor host>` in the peerlist. This function returns true when the Tor address is in that state.

## vtnerd | 2018-12-23T06:01:10+00:00
Ah ok, will do.

## vtnerd | 2018-12-23T06:04:14+00:00
The tor peerlist is only shared with tor peers, and ipv4 peerlist is only shared with ipv4 peers. The primary reason was extreme precaution against data leakage. One small benefit is that these types of "caps" are applied independently.

## vtnerd | 2018-12-23T06:05:39+00:00
This should be stripped out in release builds. This is a private constructor, and therefore is an internal check. In the last version I used a `std::min` call so that even if this check is violated in a release binary, it just truncates the host.

## vtnerd | 2018-12-23T06:07:33+00:00
Do you mean for case insensitive, etc? I thought about it, but it didn't seem like it would ruin anything to have strict checking. Is there some other cleanup I've missed?

## moneromooo-monero | 2018-12-26T18:48:31+00:00
These'd be more straightforward as v4a fwiw.

## moneromooo-monero | 2018-12-26T18:53:31+00:00
Is the io services being different expected, and in what case ?

## moneromooo-monero | 2018-12-26T18:59:10+00:00
Fair enough, but then you should probably change the message a few lines above, which says sync is starting.

## moneromooo-monero | 2018-12-26T19:04:47+00:00
Interesting. That could do some good to the levin stuff.

## moneromooo-monero | 2018-12-26T19:23:14+00:00
This relies on buffer_ being 1 byte elements, maybe add a static_assert since you have one for the sizeof above ?

## moneromooo-monero | 2018-12-26T19:27:04+00:00
Badly named maybe. Also doesn't have a Monero copyright notice (at least one other file above also doesn't).

## moneromooo-monero | 2018-12-26T19:44:04+00:00
That's strncpy fwiw (not asking to change if you prefer it that way)

## moneromooo-monero | 2018-12-26T20:03:15+00:00
SOCKS also works as a non-Tor proxy, no ?

## moneromooo-monero | 2018-12-28T22:31:40+00:00
Should this not be a network_address (ie, a proxy host may be an IPv6 one, there's a pending patch to add IPv6 to monero as well) ?

## moneromooo-monero | 2018-12-28T22:37:46+00:00
Why the extra indent ? :)

## moneromooo-monero | 2018-12-28T22:44:52+00:00
Indent hiccup

## moneromooo-monero | 2018-12-28T22:58:46+00:00
Doesn't seem to be any changes apart from the move ?

## moneromooo-monero | 2018-12-28T23:05:33+00:00
Since the rest of the errors return false, maybe it's best to either check for existence (and return), or use operator[] if it's supposed to always exist.

## moneromooo-monero | 2018-12-28T23:11:41+00:00
Same comment about checking

## moneromooo-monero | 2018-12-28T23:14:14+00:00
Why this change ? If it's because L2 in util.cpp, feel free to make it a warning or error.

## moneromooo-monero | 2018-12-28T23:31:55+00:00
Is that a leftover TODO note ?

## moneromooo-monero | 2018-12-29T00:03:21+00:00
That doesn't seem right.  You probably want to do this (the existing code) for the public zone, then have tor/other try one or two extra peers. If you're unlucky, trying all zones might get you most of your peers on tor, where you can't sync.

## moneromooo-monero | 2018-12-29T00:07:24+00:00
Since you can have incoming connections from Tor where you can't sync, you probably want to keep this to the sync-able zones (if, you want the warning if all zones but tor have 0 max).

## moneromooo-monero | 2018-12-29T00:13:44+00:00
When a log is associated with a peer, I try (when I remember) to use MWARNING(context << "blah blah"); as this makes logs easier to read and grep.

## vtnerd | 2018-12-29T02:13:59+00:00
This cannot occur anywhere in the code; this is purely defensive programming. Worth an error log, instead of silently dropping?

## vtnerd | 2018-12-29T02:18:07+00:00
I don't recall why I moved this, reverted.

## vtnerd | 2018-12-29T02:27:56+00:00
The peer counts are unique per zone. The existing command line arguments for connection counts only affect the public zone. Tor and I2P have separate max in/out connections that are specified with the `--proxy` and `--anonymous-inbound` arguments. I considered working "within" the existing command line arguments, but it wasn't obvious how to max counts between the zones.

## vtnerd | 2018-12-29T02:28:08+00:00
Changed.

## moneromooo-monero | 2018-12-29T13:24:04+00:00
I think I totally misread, it looks right on re-reading. Just the ANCHOR define is shared, but that might be ok.

## moneromooo-monero | 2018-12-29T13:25:18+00:00
Can't hurt, yes.

## moneromooo-monero | 2018-12-30T18:35:38+00:00
Should the zone not come from the requester's connection ? Not that it matters that much since this is permanently disabled though.

## moneromooo-monero | 2018-12-30T18:39:27+00:00
This should loop through the zones. The caller already ensures that for blocks, only public zone peers are added to the list. As it is, it'll silently drop peers not in the public list, which will make confusing bugs when it gets used for something else.

## moneromooo-monero | 2018-12-30T18:40:49+00:00
I think this filtering doesn't belong here at all. Can it be done in the caller instead, where the concept of txes and blocks belong ? Alternatively, passing some optional function for "is this one ok to send to ?" would do, and the caller can place the block/tx check here.

## moneromooo-monero | 2018-12-30T18:44:49+00:00
Is there a new case where it can be set to > 0xffff, or is it just a drive sanity check ?

## moneromooo-monero | 2018-12-30T19:38:18+00:00
return false (quit idle thread), bug in the original (https://github.com/monero-project/monero/pull/5029)

## moneromooo-monero | 2018-12-30T19:38:34+00:00
return true (call again)

## moneromooo-monero | 2018-12-30T20:03:50+00:00
Actually, I think it's not OK. It means we'll always (often) connect to the same tor service, and thus be a lot easier to get our privacy broken.

## moneromooo-monero | 2018-12-31T00:08:36+00:00
Please don't add random cosmetic changes (there are a few others above, and at least one below, but this one is smack in the middle of no other changes)

## moneromooo-monero | 2018-12-31T00:12:59+00:00
Should that be >= since the buffer contains the NUL and the above saves the length without the NUL ?

## moneromooo-monero | 2018-12-31T00:17:53+00:00
What does this do ? It doesn't seem to be documented, and the impl runs "$complextype.invoke", which doesn't help me.

## moneromooo-monero | 2018-12-31T00:23:25+00:00
Why all these changes ? It's really hard to tell what behaviour changed. What does the new code change, and do better ?

## vtnerd | 2019-01-02T06:15:19+00:00
Added log statement.

## vtnerd | 2019-01-02T06:15:44+00:00
Added `static_assert`.

## vtnerd | 2019-01-02T06:16:27+00:00
Renamed to parse.h/cpp. Added copyright notices to 3 files that were missing them.

## vtnerd | 2019-01-02T06:18:19+00:00
It does, but in this context I wasn't sure if it was worth adding an IPv4 section since proxying IPv4 traffic hasn't been added yet.

## vtnerd | 2019-01-02T06:19:24+00:00
I changed this to a `boost::asio::ip::tcp::endpoint` object. The object can store an IPv4 or IPv6 address, and ultimately the socks object needs this type anyway (and not Monero's internal type).

## vtnerd | 2019-01-02T06:19:32+00:00
Removed.

## vtnerd | 2019-01-02T06:19:39+00:00
Removed.

## vtnerd | 2019-01-02T06:20:50+00:00
The function in epee isn't really needed because the call into boost is nearly identical anyway. Seemed like a waste of a function call. However, I reverted the change because it shouldn't be in this PR.

## vtnerd | 2019-01-02T06:21:10+00:00
Yes it was leftover. Removed.

## vtnerd | 2019-01-02T06:23:03+00:00
Updated.

## vtnerd | 2019-01-02T06:23:11+00:00
Updated.

## vtnerd | 2019-01-02T06:24:30+00:00
The type over the protocol is `uint32`. I made this change for something that was later reverted before review, so I reverted since it shouldn't be in this patch.

## vtnerd | 2019-01-02T06:28:00+00:00
Take a peek at this change. The type was changed to `std::vector` and move by value into the callee which sorts the data so it matches the order of the enabled zones.

## vtnerd | 2019-01-02T06:32:31+00:00
Um, yeah sure. Done.

## vtnerd | 2019-01-02T06:32:56+00:00
Yes, changed.

## vtnerd | 2019-01-02T06:35:51+00:00
I don't understand why the behavior would be different for Tor than IP. There is a peerlist per zone too - only the serialized file is combined. I might need to go back and review this closer.

## moneromooo-monero | 2019-01-02T14:47:45+00:00
Anchor peers are fixed over several runs of the daemon. If you use those for Tor, it means your node will always connect to the same Tor hidden services. This might make it easier to track someone over time since you have extra info at the hidden service's end. Maybe this is negligible given Tor, but using non-anchor peers here removes that possibility.

## moneromooo-monero | 2019-01-02T14:52:06+00:00
I don't understand that reply. You seem to be implying something changed. If it is so, the change wasn't pushed. My point was that if relay_notify_to_list is used for something-that-are-not-blocks later, this code will silently drop the data, and looping through the zones now will still work since the caller already ensures it only relays to public peers.

## vtnerd | 2019-01-02T19:37:04+00:00
The function is listed in the [`boost::serialization` documentation](https://www.boost.org/doc/libs/1_69_0/libs/serialization/doc/serialization.html#splitting). There is also a macro - is there a preference to using that?

The function simply calls `save` or `load` based on the `Archive::is_saving` type/value. So this is forwarding to the two functions above depending on the context.

## vtnerd | 2019-01-02T20:02:16+00:00
There is now a peerlist per zone but there is still a single peerlist file saved to disk. The file format has not changed. Peers in an active zones are moved to a per-zone `peerlist_manager` (still doing its main "role") whereas peers from an inactive zone remain in `peerlist_storage`. When serializing to disk, all of the peer information from `peerlist_manager`s, including the anchors, need to be collected for serialization. So a function was added for this purpose. The existing function for retrieving just the gray and white peers could be left untouched, but there is an opportunity to merge both cases into one templated function.

I considered making a file per-zone, but this implementation is more flexible. The zone is never serialized and is based on the network address type, so the zones can be changed between runs easily (although that seems unlikely).

## danrmiller | 2019-01-02T20:42:06+00:00
Which extra information will the hidden service have about connecting nodes? Timing, height, first-seen transactions, inventory of transactions seen, and connected nodes? I wonder because tor's approach to entry nodes is that perhaps counter-intuitively, but based on research, connecting to the same entry tor node each time lowers your probability of profiling. I know there is a difference connecting to tor circuit nodes and monero p2p network nodes, but I wonder if a similar effect would apply or not?

https://www.torproject.org/docs/faq.html.en#EntryGuards

## moneromooo-monero | 2019-01-03T00:18:03+00:00
I meant the whole file got rewritten, not the file format in particular.

## vtnerd | 2019-01-03T01:32:11+00:00
Fixed.

## vtnerd | 2019-01-03T01:32:59+00:00
Sorry, I planned on pushing an update yesterday but I re-thought the tx_broadcast hiding comment. I'll push an update after finally testing those changes.

## vtnerd | 2019-01-03T01:33:33+00:00
This entire function is going to be removed. Only relaying to selective list is being used after this change.

## vtnerd | 2019-01-03T01:34:16+00:00
I cheated on this a bit, since this commands are also filtered outside of the public zone. I did further updating on this, it wasn't too much work.

## vtnerd | 2019-01-03T01:35:27+00:00
Yup, I will do this and drop the force setting of the last byte to NULL.

## vtnerd | 2019-01-03T02:00:02+00:00
The entire file was not re-written - primarily the bits to to/from a serialized peerlist. The "old" peerlist stuff was removed as it was unused. Perhaps that should be dropped from this commit to make it more obvious?

The biggest, perhaps non-obvious change, is that `boost::range::join` was used to write active and inactive peers in one step without touching either vector (no mallocs). So the output-to-a-file logic pushed a number of these changes.

EDIT:
I also re-arranged the code so that the serialization could be unit tested. Perhaps you are referencing those changes? 

## vtnerd | 2019-01-03T05:23:42+00:00
I actually wanted the log statement to emit, but I think it is best to move this new block of code up. Done.

## vtnerd | 2019-01-03T05:25:53+00:00
The source is a `boost::string_ref` which can be a subset of a string instance, and therefore does not have to be null-terminated.

## vtnerd | 2019-01-03T07:48:50+00:00
Reverted this to only inspect the public zone.

## moneromooo-monero | 2019-01-04T01:30:59+00:00
In this case, the connection would be to the same hidden service. Tor will decide about the entry guard. But good point, maybe it is not actually a problem, and even a good thing...

## moneromooo-monero | 2019-01-04T01:33:14+00:00
New file, with new code that handles peers differently than before. That's all I meant.

## vtnerd | 2019-01-04T21:57:41+00:00
I left the anchor behavior untouched for now. I'm not sure of the best approach, but this can be tweaked separately later.

## moneromooo-monero | 2019-01-05T10:57:31+00:00
What is the intent of that sorting ? It seems like it's not needed at first glance.

## vtnerd | 2019-01-05T18:32:22+00:00
Instead of doing a find per connection, this puts the connections in the same order as the zones in the `std::map` so that it can iterate through both. The negative is that if the first connection has a "bad" zone, then all of the notifications are skipped, even if the remainder of the list has valid zones.

Alternatively, I could "reset" if the end iterator is hit (and skip that connection) with bit more logic or just do a find per connection.

## moneromooo-monero | 2019-01-05T23:01:32+00:00
Skipping connections we intended to not skip sounds bad :) So this is just a speedup then ? If so, it doesn't seem worth a bug if the case you mention happens.

## vtnerd | 2019-01-05T23:18:33+00:00
This only happens if the caller has a bug - they are requesting a relay to a non-existent zone/connection. Some minor defensive programming can't hurt - I didn't think of this alternate case actually until writing that last description.

## moneromooo-monero | 2019-01-06T00:54:33+00:00
Ah, OK, then that's the last of my comments then.


## jtgrassie | 2019-01-21T16:17:20+00:00
parameters declared as list not vector

## moneromooo-monero | 2019-01-24T00:21:32+00:00
I'm guessing "tor" is not a valid hidden service address. I've never set one up. It made sense as network-type. Also, you probably want <bind-ip>[:<port>] since port seems optional as per the example.


## moneromooo-monero | 2019-01-24T00:22:01+00:00
Probably needs to be const socket_stats& if the function's const.

## vtnerd | 2019-01-28T21:59:01+00:00
Weirdly enough, it doesn't have to be per C++ rules, but arguably should be. I will make it so.

## vtnerd | 2019-01-28T21:59:12+00:00
I caught the Tor typo in the last round of edits, and will update the port thing too.

## vtnerd | 2019-01-29T05:01:25+00:00
This can't return a `const` instance because it uses a non-const function on the handler. Instead, I made `get_stats` not-const. It can be upgraded/updated in the future.

## moneromooo-monero | 2019-01-29T09:37:56+00:00
strncpy doesn't need its source to be either.

## moneromooo-monero | 2019-01-29T09:39:57+00:00
simultaneously

## vtnerd | 2019-01-29T14:54:28+00:00
Fixed.

## vtnerd | 2019-01-29T15:00:48+00:00
Yikes, just got merged. Will have to update this separately.

# Action History
- Created by: vtnerd | 2018-12-16T23:23:34+00:00
- Merged at: 2019-01-29T14:47:35+00:00
