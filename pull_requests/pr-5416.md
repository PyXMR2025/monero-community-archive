---
title: 'Wallet: Option to export data to ASCII'
source_url: https://github.com/monero-project/monero/pull/5416
author: tmoravec
assignees: []
labels: []
created_at: '2019-04-10T09:37:07+00:00'
updated_at: '2019-08-27T20:03:07+00:00'
type: pull_request
status: merged
closed_at: '2019-08-27T20:03:07+00:00'
merged_at: '2019-08-27T20:03:07+00:00'
---

# Original Description
New CLI wallet variable: export-format with options "raw" (the default),
or "hex". "Raw" behaves as before, "hex" forces the wallet to convert
data to ASCII.

On import, we try to decode ASCII first, and if that fails, fall back to
raw in a sort of primitive auto detect.

Implements https://github.com/monero-project/monero/issues/2859

# Discussion History
## moneromooo-monero | 2019-04-10T10:08:30+00:00
That's going to be a lot of memcpy. Not sure it's worth doing anything about given there's a disk write later though.

## moneromooo-monero | 2019-04-10T10:09:43+00:00
That already seems to be a hex dump of the data.

## moneromooo-monero | 2019-04-10T10:13:17+00:00
Could use some std::move here, the strings are likely large

## moneromooo-monero | 2019-04-10T10:15:20+00:00
What I had in mind was more trying to parse, and then falling back to checking for hex. The patch checks whether the input is valid hex first, which might have some false positives. Admittedly probably not many, but it depends on what is being saved. Like for instance one thing above is saving a hex dump of something, so loading one of those files saved with current monero would actually load the wrong data.
In any case, if we decide it's not worth bothering about, I wonder whether this should go to the epee code itself, as an optional parameter. It might be useful elsewhere.


## moneromooo-monero | 2019-04-10T10:17:46+00:00
Can you move this to wherever other options are ? It makes things simpler :)

## tmoravec | 2019-04-10T12:03:39+00:00
Good catch, thanks. Fixed.

## tmoravec | 2019-04-10T12:06:52+00:00
Good idea. Adding.

## tmoravec | 2019-04-10T12:10:20+00:00
They are really all over the place, but I'm picking something better :) .

## tmoravec | 2019-04-10T12:21:40+00:00
Turns out there's several such cases - get_tx_proof, get_spend_proof, and get_reserve_proof all return base58 encoded strings.

Now we have a bunch of occasions where we call `m_wallet->save_to_file` and a bunch of occasions where we call `epee::file_io_utils::save_string_to_file`. Maybe adding a new argument for `save_to_file` like `is_hex` or `is_ascii` would be more appropriate?

## tmoravec | 2019-04-10T12:27:24+00:00
Problem with trying to parse the binary data first is that this function has no idea how to do that.  We'd need to either supply a callback (convoluted and difficult to understand) or update all code that reads from files, which is error-prone and easy to forget in the future.

The mentioned case is IMO a bug, I think we should not convert to hex what's already ASCII. Fixing it in that place.

As for moving the whole implementation to epee, honestly, I've thought it's a third-party library - pushing to upstream and maintaining a fork in the meantime is pretty annoying. If we can just edit epee, it might be a better option. I'll be happy to follow your decision :) .

## tmoravec | 2019-04-10T12:31:39+00:00
TBH I can't think of a solution that doesn't require copying of every chunk. We could copy _only_ that chunk in every iteration if we didn't do it in place but that would be at the cost of less readable code. And as you said, the bottleneck is likely in the disk write anyway.

## moneromooo-monero | 2019-04-10T13:58:31+00:00
Yes, let's leave it like that. I can think of a solution in O(N) but it makes things more complicated. We can always change it later if it turns out there is a significant slowdown.

## moneromooo-monero | 2019-04-10T14:00:22+00:00
It's only a problem if you make the decision inside the read function :)
epee is only used in monero AFAIK.


## moneromooo-monero | 2019-04-10T14:02:05+00:00
You added the getter at the right place already, the options are pairs of functions next to each other :)

## moneromooo-monero | 2019-04-16T18:12:49+00:00
The magic is written with an extra \n. This code does not remove the \n, nor check it. It seems to be implicitely skipped by the whitespace cleanup above. Is this intended ?

## tmoravec | 2019-04-16T18:25:00+00:00
Yes, it's intentional. At this point, the `\n` should have been removed by the whitespace cleanup above. 

The `\n` is there just to make it look nicer when pasted somewhere. I was even contemplating adding the line of dashes like with openssl certificates but it seemed excessive.

## tmoravec | 2019-04-16T18:34:13+00:00
This question has been worked around; marking this conversation as resolved.

## tmoravec | 2019-04-16T19:45:12+00:00
Fixed.

## moneromooo-monero | 2019-04-16T20:05:13+00:00
Can this be "m_wallet->export_format() == Hex ? "Hex" : "Raw"" ? Doesn't depend on enum values this way.

## moneromooo-monero | 2019-04-16T20:07:15+00:00
Why does this one (and the matching load) use the new function ? This new argument is really a "please don't do your thing".

## moneromooo-monero | 2019-04-16T20:09:07+00:00
This does not look like it's saved along with the other settings. If you reload a wallet, it'll be reset.

## tmoravec | 2019-04-17T06:27:04+00:00
Good point. Fixing.

## tmoravec | 2019-04-17T06:45:48+00:00
Good catch. Adding.

## tmoravec | 2019-04-17T14:12:32+00:00
The point is to _only_ use the new functions. 

Imagine I'm a new developer coming to this file. I want to save stuff to a file. So I have a look around how does the rest of the code do it. And I find the old version. So I do the same thing because I have no idea there's the new one, too. And I introduce a bug because the new data is not converted to hex. It's really only about consistency.

## moneromooo-monero | 2019-04-17T14:20:57+00:00
Sounds like rationalization, but OK :)

## moneromooo-monero | 2019-04-17T14:23:25+00:00
The default should be raw or you're going to switch existing wallets on load.

## moneromooo-monero | 2019-04-17T14:25:53+00:00
<s>For very small files, this sounds like a buffer overflow (unless insert just does nothing, I've not looked at its implementation, but in any case an apparent bug.)</s> nevermind, I was wrong.


## moneromooo-monero | 2019-04-17T14:26:59+00:00
That comment is no longer correct

## moneromooo-monero | 2019-04-17T14:29:05+00:00
You probably want to check for an odd size here.

## vtnerd | 2019-04-18T20:41:42+00:00
Bikeshedding - the codebase in here is typically using underscore + lower case.

## vtnerd | 2019-04-18T20:43:44+00:00
`!is_hex` ?

## vtnerd | 2019-04-18T20:57:47+00:00
This and the existing ones should be `const char foo[]` instead of a runtime initialized `std::string` that can cause initialization order issues. @moneromooo-monero just this one now or "normalize" to `std::string` and change them all in some future patch? I don't see any harm in making this `const char[]` now.

## vtnerd | 2019-04-18T21:02:08+00:00
`if (boost::algorithm::iequals(args[1], "hex"))`, etc., covers all the variants.

## vtnerd | 2019-04-18T21:06:07+00:00
`base58` or `base64` will have smaller output. `base58` is provided internally. `EVP_EncodeBlock` and `EVP_DecodeBlock` from OpenSSL provide to/from `base64` with newlines every 64 characters. The OpenSSL decode function is also useful because it strips all whitespace.

## vtnerd | 2019-04-18T21:09:45+00:00
It should be faster to allocate an entirely new string (with `reserve`/`resize` in one shot), then copy the portions into it. This technique is constantly doing `memmove`'s with the _same_ bytes (i.e. the last bytes of the string are being moved N times), and I don't see an easy way for the compiler to optimize that away.

The other technique is to resize the existing string, and do the `memmove` from-the-back-first manually while adding the newlines.

This will definitely matter for large files, but I'm not sure how large the files will get for the use cases.

## vtnerd | 2019-04-18T21:13:25+00:00
This check should be bumped up before the copy + string manipulation so that "raw" files are not being copied.

## vtnerd | 2019-04-18T21:14:15+00:00
The hex string parsing should be doing a validation too (making this unnecessary) ? 

## moneromooo-monero | 2019-04-18T22:06:34+00:00
It can be done in a later patch. Same for optimizing the data copying etc I think.

## moneromooo-monero | 2019-04-18T22:07:20+00:00
This means "if it's already hex, do nothing". Though it should be "printable", not "hex" really.

## tmoravec | 2019-04-28T18:25:43+00:00
Good catch, thanks.

## tmoravec | 2019-04-28T18:27:56+00:00
Thanks, fixed.

## tmoravec | 2019-04-28T18:34:39+00:00
Calling `validate_hex` makes it faster to fail, a pure usability improvement (provided the difference is noticeable). Not sure if it's worth complicating the code for this, but it seems easy enough. If you insist on removing this, I don't have a problem doing it.

## tmoravec | 2019-04-28T18:36:51+00:00
The wallet in particular is written like this. E.g.

```c++
      enum RefreshType {
        RefreshFull,
        RefreshOptimizeCoinbase,
        RefreshNoCoinbase,
        RefreshDefault = RefreshOptimizeCoinbase,
      };

      enum AskPasswordType {
        AskPasswordNever = 0,
        AskPasswordOnAction = 1,
        AskPasswordToDecrypt = 2,
      };

      enum BackgroundMiningSetupType {
        BackgroundMiningMaybe = 0,
        BackgroundMiningYes = 1,
        BackgroundMiningNo = 2,
      };

      enum ExportFormat {
        Raw = 0,
        Hex,
      };
```

## tmoravec | 2019-04-28T18:38:04+00:00
Yup, `is_printable` sounds like a better name. Changing.

## tmoravec | 2019-04-28T18:40:58+00:00
I have forgotten that we already depend on `boost::algorithm` :) . You are right that it's a better option.

## tmoravec | 2019-04-28T18:48:26+00:00
The problem is that it's easy to add some leading whitespace or newlines when copy/pasting. We can of course strip all leading whitespace, then check for the magic string, and then strip all remaining whitespace, but it complicates the code quite a bit. 

Of course, copying a file in the memory needlessly is bad. However, from the UX perspective, searching for the path, and then pasting or typing it takes seconds. This copy takes something like milliseconds presumably. If this was some tight code in handling the blockchain, the story would be different of course.

In either case, if you disagree with my reasoning, let me know and I can change the code.

## tmoravec | 2019-04-28T18:54:15+00:00
Sadly, the wallet does not use OpenSSL currently, and adding such dependency for something so trivial doesn't make sense.

Using `base58` is a good idea though, IMO better than the hexdump we've done previously. Let me try to implement it. Thanks!

## tmoravec | 2019-04-28T19:16:28+00:00
Frankly, I don't understand your comment. But this check is going away anyway with base58 (as vtnerd suggested).

## moneromooo-monero | 2019-04-28T19:46:35+00:00
I meant if (size & 1) return error; or similar.

## tmoravec | 2019-04-29T09:12:59+00:00
With base58 this is going away regardless.

## tmoravec | 2019-04-29T19:02:38+00:00
All right, reimplemented. Not the most readable piece of code but it should get the job done :) .

## moneromooo-monero | 2019-04-30T10:44:52+00:00
These should return true

## moneromooo-monero | 2019-04-30T10:47:44+00:00
If you're using base58 now, a number of places should change from "hex". Especially the settings.

## tmoravec | 2019-04-30T12:33:05+00:00
I don't think the end user cares about the underlying encoding so it would make sense to use something less technical. How about `binary` and `text`? 

## moneromooo-monero | 2019-04-30T19:04:59+00:00
Sure.

## moneromooo-monero | 2019-04-30T19:07:00+00:00
Maybe ASCII, since text implies, well, text, not seemingly random letters. But whatever you think is appropriate.

## vtnerd | 2019-04-30T22:59:31+00:00
The wallet links against `libcrypto` and `libssl` transitively from `epee` SSL code.

## vtnerd | 2019-04-30T23:02:13+00:00
The dependency to OpenSSL could be dropped  if the wallet switches to another SSL library, drops SSL altogether, or builds SSL as an optional build option. The third scenario seems the most likely, but the code would require some serious work to get to that.

## vtnerd | 2019-05-01T01:50:41+00:00
There isn't much complication:
```c++
const auto magic = std::find_if_not(data.begin(), data.end(), boost::algorithm::is_space());
if (!boost::algorithm::starts_with(boost::make_iterator_range(magic, data.end()), HEX_OUTPUT_MAGIC))
{
  ...
}
data.erase(data.begin(), magic + HEX_OUTPUT_MAGIC.size());
```

## tmoravec | 2019-05-05T12:18:15+00:00
Ok.

## tmoravec | 2019-05-05T12:26:55+00:00
All right, makes sense. Will update it all, thanks.

## tmoravec | 2019-05-05T13:22:10+00:00
Okay you win :) . Updated.

## vtnerd | 2019-08-19T18:09:15+00:00
`name`, `header`, and `openssl_data` leak if `std::string` constructor throws. Unfortunately I do not see a C++ functor for use with `unique_ptr` in our code base. Its not difficult to add one. Add a todo if you don't have time to add and test now as the case is not likely to trigger.

## vtnerd | 2019-08-19T18:10:43+00:00
This file typically uses underscores instead of camel case - @moneromooo-monero change now or later?

## moneromooo-monero | 2019-08-19T18:19:40+00:00
I don't really care, we have both I think.

## tmoravec | 2019-08-20T06:32:24+00:00
In this file in particular, there's CamelCase:

```c++
    enum RefreshType {
      RefreshFull,
      RefreshOptimizeCoinbase,
      RefreshNoCoinbase,
      RefreshDefault = RefreshOptimizeCoinbase,
    };

    enum AskPasswordType {
      AskPasswordNever = 0,
      AskPasswordOnAction = 1,
      AskPasswordToDecrypt = 2,
    };

    enum BackgroundMiningSetupType {
      BackgroundMiningMaybe = 0,
      BackgroundMiningYes = 1,
      BackgroundMiningNo = 2,
    };

    enum ExportFormat {
      Binary = 0,
      Ascii,
    };
```

## tmoravec | 2019-08-20T07:56:18+00:00
Good catch about the exception. I can either:

1. Just slap on the todo.
2. Simply wrap it in a try/catch block
3. Add the suggested functor. I'm not sure I 100% understood what you mean - something like this? Where do you think I should put such a helper function?

```c++
template <typename... Targs>
std::unique_ptr<std::string> safe_string(Targs... args)
{
    try
    {
        return std::make_unique<std::string>(std::string(args...));
    }
    catch (...)
    {
        return std::make_unique<std::string>(nullptr);
    }
}
```

## vtnerd | 2019-08-20T15:19:16+00:00
No I meant add the `unique_ptr` for the `OPENSSL_free` calls:
```c++
struct openssl_free
{
    void operator()(void* ptr) const noexcept
    {
        if (ptr)
            OPENSSL_free(ptr);
     }
};
...
std::unique_ptr<char, openssl_free> name;
std::unique_ptr<char, openssl_free> header;
std::unique_ptr<unsigned char, openssl_free> openssl_data;
...
```
The annoying part are the out parameters, because it requires a temporary stack value that gets copied into the `std::unique_ptr` before doing any throwing operation. Having a `try { } catch (...) {}` around the construction is probably the easiest. The solution you had would work too, although I'm not sure how often the function would be used.

## tmoravec | 2019-08-21T06:59:41+00:00
I see, thanks. I'd go with the easy solution of try/catch block around the constructor because the other solutions have their own drawback - either needlessly complex and generic for a single use purpose, or the temporary stack values.

## moneromooo-monero | 2019-08-22T13:56:15+00:00
That looks like a merge error

## tmoravec | 2019-08-22T14:04:37+00:00
Good catch, thanks. Fixed.

# Action History
- Created by: tmoravec | 2019-04-10T09:37:07+00:00
- Merged at: 2019-08-27T20:03:07+00:00
