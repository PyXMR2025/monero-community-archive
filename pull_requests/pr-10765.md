---
title: Polyseed support for 'wallet2', Wallet API, CLI wallet app, wallet RPC server
source_url: https://github.com/monero-project/monero/pull/10765
author: rbrunner7
assignees: []
labels:
- pending review
- wallet
created_at: '2026-06-14T13:35:35+00:00'
updated_at: '2026-06-30T19:13:44+00:00'
type: pull_request
status: open
closed_at: null
merged_at: null
---

# Original Description
This (finally) implements support for Polyseed as created by @tevador in `wallet2`, the Wallet API, the CLI wallet app and the wallet RPC server.

Substantial parts come, with some changes, from [this patch](https://github.com/MrCyjaneK/monero_c/blob/develop/patches/monero/0007-polyseed.patch) authored by @tobtoht and refined by @MrCyjaneK for the [monero-c](https://github.com/MrCyjaneK/monero_c) library. As part of that library the code from that patch is in active use in a number of third-party wallet apps for quite a while now.

The code in `simplewallet` for the CLI wallet app and in `wallet_rpc_server` is my work.

A bug regarding the patch in connection with background sync of Polyseed wallets that I learned about from a discussion in a Matrix channel (the seed info gets lost) is corrected in a clean way now.

It may be that the Polyseed methods in the Wallet API are still quite basic and not yet sufficient to implement really comprehensive support in an app like the Monero GUI wallet app, but without actually trying it's hard to find out what's missing, if anything, and I did not try to go beyond what said patch already brings.

# Discussion History
## selsta | 2026-06-14T18:50:52+00:00
This function appears both broken and incomplete.

## selsta | 2026-06-14T18:56:08+00:00
```suggestion
        std::string mnemonic_language = get_mnemonic_language(false);
```

otherwise it sets polyseed language but prints legacy seed.

## selsta | 2026-06-14T18:56:43+00:00
Also here, it did the legacacy `get_seed` but printed it as a Polyseed. Maybe a separate `print_legacy_seed()` fuction would be useful.

## selsta | 2026-06-14T18:59:21+00:00
```suggestion
          lang = polyseed.decode(seed.c_str());
```

From polyseed documentation

> * @param str is the mnemonic phrase as a C-style string. Must not be NULL.

`data()` would work fine with C++17, but this makes the intent more clear.

## selsta | 2026-06-14T19:04:22+00:00
```suggestion
        auto lang = polyseed.decode(seed.c_str());
```

## selsta | 2026-06-14T19:10:02+00:00
```suggestion
  crypto::ElectrumWords::get_language_list(language_list, false, is_polyseed);
```

## selsta | 2026-06-14T19:17:00+00:00
```suggestion
    bool words_to_bytes_ex(const epee::wipeable_string &words, crypto::secret_key& legacy_recovery_key,
      std::string &language_name, bool &is_polyseed, polyseed::data &polyseed)
```

This makes it more clear that this result is only relevant for legacy path.

## selsta | 2026-06-14T19:19:40+00:00
This does not apply passphrase / seed offset logic, so maybe it should be removed?

## jeffro256 | 2026-06-15T00:18:33+00:00
Use `#pragma once`

## jeffro256 | 2026-06-15T00:18:36+00:00
This is an internal header, and shouldn't be included

## jeffro256 | 2026-06-15T00:19:03+00:00
This should be `#include <polyseed.h>` if you are going to include the header. But the whole purpose of a wrapper API is to hide the other API which is being wrapped, correct?

## jeffro256 | 2026-06-15T00:21:58+00:00
Polyseed code shouldn't need knowledge of `crypto.h` and our previous method of deriving the master secret key from a seed.

## jeffro256 | 2026-06-15T00:22:10+00:00
`data` isn't a great name

## jeffro256 | 2026-06-15T00:22:44+00:00
It is not 2023

## jeffro256 | 2026-06-15T00:23:00+00:00
Did tevador write this C++ API?

## jeffro256 | 2026-06-15T00:25:56+00:00
If you are taking a polyseed status and message parameter, do you intend to display a human-readable string for the code?

## jeffro256 | 2026-06-15T00:34:30+00:00
Why use `utf8proc` over Boost Locale, which the wallet already pulls in as a dependency?

## jeffro256 | 2026-06-15T00:35:14+00:00
Pleeeeeease no

## jeffro256 | 2026-06-15T00:36:31+00:00
`libcryptonote_basic` should NOT depend on `libpolyseed_wrapper`. 

## jeffro256 | 2026-06-15T00:38:32+00:00
Why not `KV_SERIALIZE_OPT()`?

## jeffro256 | 2026-06-15T00:43:09+00:00
I think that this could be done better. Separating languages based on a explicitly or implicitly passed `polyseed` parameter always gives you bad choices. If defaulted to `true` like it is here, it breaks old code. If defaulted to `false`, then new code must pass `polyseed=true` in perpetuity, which is clunky. In my opinion, the RPC should return all languages for both seeds, with the overlapping entries merged. An exception later on will be generated if trying to use the wrong language with the wrong seed format, as would be expected. 

## jeffro256 | 2026-06-15T00:45:15+00:00
We would need to check that this doesn't break downstream projects which automatically generate and manage wallet instances opaquely, e.g. Haveno, Xmrbazaar multisig, etc.

## jeffro256 | 2026-06-15T00:45:44+00:00
I'd prefer if `polyseed` was a subdirectory under `mnemonics`. 

## jeffro256 | 2026-06-15T00:49:33+00:00
Would be nice if you could share code paths with the other `wallet2::generate()` overload, to reduce code duplication

## jeffro256 | 2026-06-15T00:51:06+00:00
Surely, Polyseed can be detected from other seed formats, correct? This needn't be its own field. 

## jeffro256 | 2026-06-15T00:52:35+00:00
Putting Polyseed support under the `ElectrumnWords` namespace is kind of funky...

## jeffro256 | 2026-06-15T01:02:53+00:00
Passphrase should not be stored / serialized next to the account keys. It defeats whole purpose of a passphrase.

## jeffro256 | 2026-06-15T01:06:31+00:00
```suggestion
                if (m_data)
                    polyseed_free(m_data);
```

## jeffro256 | 2026-06-15T01:07:58+00:00
This method impls should be in the .cpp file

## jeffro256 | 2026-06-15T01:10:12+00:00
Shouldn't the default seed type be set to Polyseed somewhere in this file, just like the RPC?

## selsta | 2026-06-15T01:39:42+00:00
It might be relevant that we build Boost Locale without icu support.

## tobtoht | 2026-06-15T11:01:59+00:00
Yes, this was unfortunately inherited from the patch. The daemon and other utilities should not depend on polyseed (and `utf8proc`).

## tobtoht | 2026-06-15T11:04:16+00:00
https://github.com/tevador/polyseed-examples/blob/master/cpp/polyseed.hpp

## tobtoht | 2026-06-15T11:05:32+00:00
Yes, this would need ICU and ICU is a beast.

## rbrunner7 | 2026-06-19T08:15:49+00:00
Right. And implementing it correctly would not be easy because the Polyseed C code does not export some functions that are needed to do it properly.

It's unused. I will simply remove it.

## rbrunner7 | 2026-06-19T08:21:45+00:00
You are right. Upgrading from a "legacy format" wallet has to to work with a legacy seed, i.e. deciding on a new language for that.

Corrected.

## rbrunner7 | 2026-06-19T08:21:59+00:00
Yes, it has to print a legacy seed here.

Corrected.

## rbrunner7 | 2026-06-19T08:27:42+00:00
Switched.

## rbrunner7 | 2026-06-19T08:33:07+00:00
Good catch. Seems I never tested that ...

Corrected.

## rbrunner7 | 2026-06-19T08:38:43+00:00
Switched.

## rbrunner7 | 2026-06-19T08:48:15+00:00
The way it's done now, and the way I propose to keep it (see comment to the next issue), `dst` will contain a key in both cases, legacy and Polyseed.

## rbrunner7 | 2026-06-19T08:48:46+00:00
Well, both the `words_to_bytes` and `words_to_bytes_ex` methods don't know, and don't care, about any seed offsetting that you want to do later, for both legacy *and* Polyseeds. Thus I think it does make sense to give back a key already, in both cases.

## rbrunner7 | 2026-06-19T11:10:40+00:00
Done.

## rbrunner7 | 2026-06-19T11:15:41+00:00
You mean `lang.h`? It defines `polyseed_lang` which is referenced in the code. Maybe connected with the following question about API and wrapping?

## rbrunner7 | 2026-06-19T11:23:33+00:00
`polyseed.hpp` is originally from Tevador, as part of their *polyseed-examples* repository, see [here](https://github.com/tevador/polyseed-examples/blob/master/cpp/polyseed.hpp). Then @tobtoht took it, added some methods, and made it part of their "patch" for `monero_c`. Finally I copied it with only small changes.

Yes, it's not a clean wrapper; you could describe it probably as "Do enough things in C++ style that using the Polyseed C library becomes comfortable enough".

It's quite pragmatic, and certainly not a beauty, but it works.

Rewriting it as a full wrapper would probably take some effort. What's your take, do you see that as necessary in the interest of code quality? Internal code quality, as that stuff is not part of any API.

## rbrunner7 | 2026-06-19T11:33:01+00:00
That's part of something that you could call a small hack. We have a Polyseed in the sense of `polyseed_storage` as part of the account keys [here](https://github.com/rbrunner7/monero/blob/b935b987366901600aac6914a2f9e2d0f36c1fc4/src/cryptonote_basic/account.h#L49). It's a `crypto::secret_key` there so there is no problem for sure with serialization, and to avoid that we have to import any Polyseed header into the account code.

So nothing to do with any real key management, and deriving secrets, but `crypto::secret_key` is just convenient here and has the right size in memory.

Again, a question whether we see this small hack as acceptable, or bad enough to be in need of replacement.

## rbrunner7 | 2026-06-19T11:35:24+00:00
Agreed :) Inherited from the "patch", original Tevador naming, maybe copied a bit too uncritically, and now it's the question whether it's bad enough that we have to improve.

## rbrunner7 | 2026-06-19T11:40:03+00:00
Corrected the headers of 4 files in the /src/polyseed folder to 2026.

## rbrunner7 | 2026-06-19T11:41:18+00:00
I resolve this, because I now link to this in a comment of mine as well.

## rbrunner7 | 2026-06-19T11:50:00+00:00
Yes, see [here](https://github.com/rbrunner7/monero/blob/b935b987366901600aac6914a2f9e2d0f36c1fc4/src/polyseed/polyseed.cpp#L134)

## rbrunner7 | 2026-06-19T11:52:46+00:00
Not sure whether relevant here, but we need ICU on Windows so the Monero software can treat filenames with non-ASCII or even Unicode characters in it; Linux and Mac don't have this problem.

## rbrunner7 | 2026-06-19T11:55:05+00:00
Can somebody please elaborate what's the problem here, and if it's is a problem serious enough, how a solution would probably look?

## rbrunner7 | 2026-06-19T12:10:10+00:00
For reference, here the Polyseed languages:

0 : English
1 : 日本語
2 : 한국어
3 : español
4 : français
5 : italiano
6 : čeština
7 : português
8 : 中文(简体)
9 : 中文(繁體)

and here the languages for legacy seeds:

0 : Deutsch
1 : English
2 : Español
3 : Français
4 : Italiano
5 : Nederlands
6 : Português
7 : Русский
8 : 日本語
9 : 简体中文 (中国)
10 : Esperanto
11 : Lojban

It can be seen that unifying is not straight-forward, because some languages have different names. And we would probably have to give back some enum in the resulting language list, or two booleans "For Polyseed yes/no" and "For legacy seed yes/no" because otherwise the caller could not handle the list properly.

Seems to me that we hope for a quick transition to Polyseed, and that this is an RPC call where I doubt it's widely used, so I doubt it's worthwhile to really try some "unification" here.

> If defaulted to true like it is here, it breaks old code.

Well, seems to me we agreed to the possibility to break old code, quite in general, by defaulting to Polyseed instead of staying with legacy seeds as default, with Polyseed needing activation somehow. If you create a new wallet, it will have a Polyseed, and some software might stumble if it asks later for the seeds and does not see enough words. This default here is merely a consequence of "Polyseed is default".


## tobtoht | 2026-06-19T12:20:30+00:00
We use Boost Locale for this and IIRC Boost Locale works without ICU on Windows.

## rbrunner7 | 2026-06-19T12:29:21+00:00
Not sure I follow. This boolean just records "This wallet was created with a Polyseed" in a clear and convenient way. I am not sure we could already easily check this in another way; looking into the account keys object might not be that.

## rbrunner7 | 2026-06-19T12:33:55+00:00
Done.

## rbrunner7 | 2026-06-19T12:39:44+00:00
You mean only a declaration here, like it's done for `create`, `load` and `decode` already, and the implementation in the .cpp file?

Will give quite a number of 2-code-line implementations, but will be cleaner of course ...

## rbrunner7 | 2026-06-19T12:45:06+00:00
Maybe things changed in the meantime. [This PR](https://github.com/monero-project/monero/pull/3313) of mine back in 2018 brought ICU as new dependency.

## tobtoht | 2026-06-19T12:56:50+00:00
Can you check if unicode file paths (still) work on Windows with a `v0.18.5.0` release build? We build Boost without ICU: https://github.com/monero-project/monero/blob/release-v0.18/contrib/depends/packages/boost.mk#L38

## tobtoht | 2026-06-19T12:59:01+00:00
By linking `cryptonote_basic` with `polyseed_wrapper`, `polyseed` and `utf8proc` become a dependency for any code that depends on `cryptonote_basic`. This includes the daemon.

Polyseed isn't needed for the daemon, so this would be an unnecessary expansion of its threat surface.

You currently need to link `polyseed_wrapper` because `create_from_polyseed` takes a `const polyseed::data&`. You could refactor such that the private spend key is generated elsewhere and is passed as a `const crypto::secret_key&` instead.

## tobtoht | 2026-06-19T13:05:54+00:00
https://www.boost.org/doc/libs/latest/libs/locale/doc/html/using_localization_backends.html

From this, I'd assume it's using the Win32 API.

## rbrunner7 | 2026-06-19T13:32:13+00:00
I checked v0.18.5.0 on Windows 11 - seems to (still) work. Maybe the Boost people also got tired about ICU and solved the problem in some other way ...

People all around ok if we do use `utf8proc` for Polyseed and create a new dependency this way?

## tobtoht | 2026-06-19T13:35:30+00:00
>and made it part of their "patch" for monero_c

I'm not sure how the patch ended up in `monero_c`. I sent it to r4v3r23 for testing in ANONERO and it has since been copied to other projects, which I could have predicted would happen.

```
2023-10-23 <tobtoht> just be warned that if I have to make any serialization changes you will have to maintain this patch until seraphis
2023-10-23 <r4v3r23> will you be basing feather's polyseed on this patch?
2023-10-23 <tobtoht> yes, but not until upstream patch gets ACKd
2023-10-23 <r4v3r23> ok so well just follow your changes if/when they happen
2023-10-23 <tobtoht> in the event of serialization changes, it could break old wallet files or prevent you from obtaining their polyseed
2023-10-23 <tobtoht> so you'll have to maintain current serialization
```

 I regret not attempting to upstream it before sending it out. Lesson learned.

## tobtoht | 2026-06-19T14:16:07+00:00
I'd definitely prefer not to add a new third-party dependency if we can avoid it (especially not on a C library).

We could have Polyseed not do unicode normalization. We don't do this for legacy seeds (even though we should) and this hasn't resulted in any bug reports in the span of a decade (that I know of). This approach requires changes to the Polyseed library. It seems like the lazy route and is technically not correct. 

(Do people just not use seeds in other languages? I find it curious that we haven't seen more issues reported with legacy seeds. Related: BIP39 [strongly recommends against](https://github.com/bitcoin/bips/blob/master/bip-0039.mediawiki#wordlists) using non-English wordlists.) 

Given the relatively narrow subset of unicode we actually need for this, we could attempt to implement the mappings ourselves. On second thought, I have no idea how hairy normalization gets for Chinese, Korean, etc. It's something I'd like to explore in the future to get a better understanding of.

Neither alternative feels particularly enticing though, so I think adding `utf8proc` for now is fine.

## rbrunner7 | 2026-06-19T14:26:17+00:00
Just an info bit: The Polyseed library really "is asking for it" by doing the linguistically and culturally correct thing, use a special Unicode separator for seeds in Japanese instead of blank. If you don't handle this somehow, you won't be able to cut these Japanese seeds into words again.

## rbrunner7 | 2026-06-26T12:03:14+00:00
I wasn't noticing when I was busy copying things over from the "patch", but yeah, a new top-level project subdirectory just for the handful of Polyseed files is a bit much.

Moved as you propose.

## rbrunner7 | 2026-06-26T14:06:54+00:00
Done: `libcryptonote_basic` does not depend on `libpolyseed_wrapper` anymore after refactoring.

However, as something that is also a bit questionable, but where I did not see a good way around it: Now it's the other way round, `libpolyseed_wrapper` depends on / links with `libcryptonote_basic`.

## rbrunner7 | 2026-06-28T11:40:30+00:00
After my PR no longer includes storing of the passphrase used for a Polyseed (will write more about this soon as a general PR comment) this is no longer necessary, and I removed it.

## rbrunner7 | 2026-06-28T11:45:12+00:00
After no longer storing passphrases used for Polyseeds, I could remove `seed_offset` again: Can't give back over RPC what isn't there.

## rbrunner7 | 2026-06-28T11:50:12+00:00
Agree in the meantime. As mentioned already in another comment, will soon write about that in a general comment. 

In any case, the member `m_passphrase` is gone again, and the new `m_monero_c_passphrase` that I added now is a pure compatibility vehicle not used by the core software itself, see comment in `account.h`.

## rbrunner7 | 2026-06-28T12:04:21+00:00
Finally done.

## rbrunner7 | 2026-06-28T12:08:24+00:00
Not sure. It's part of the "patch" that I just copied over. I guess the idea is that wallet app devs that use the Wallet API decide "higher up" which methods they want to call, to get which type of wallet, so a default of some sort would not really be necessary.

Anyway, I suspect that once the Monero GUI wallet app gets moved to Polyseed, we will have to add and/or modify one or the other Wallet API method anyway, but it does not become clear until really done.

## SNeedlewoods | 2026-06-29T19:02:51+00:00
Nit: unnecessary newline

## SNeedlewoods | 2026-06-29T19:03:06+00:00
AFAICT this is just copied from the patch, but I don't see `UTF8PROC_LUMP` being used here,
 so in case the `_LUMP` is not used in some hidden place, the comment is misleading.

## SNeedlewoods | 2026-06-29T19:03:19+00:00
Sorry, what's the reason to increase this?
(I see the wallet-rpc changed, but what changed in the core-rpc?)

## SNeedlewoods | 2026-06-29T19:03:45+00:00
Why not remove this entire `if` block and just show the same as `seed` command in this case?
Again not super important, but to me it seems a little off, that if you have a wallet with legacy seed and use `legacy_seed` command, it tells you that's not possible.

## SNeedlewoods | 2026-06-29T19:04:48+00:00
Ah okay, my question from earlier, "Why not just show legacy seed for legacy wallet?". Then this would also need to change to something like:
``` suggestion
                           m_wallet->is_polyseed() ? tr("Display a Polyseed as a legacy seed") : tr("Display your wallets seed"));
```
Not sure if it's worth it, I'll let you decide.

## SNeedlewoods | 2026-06-29T19:06:22+00:00
Sorry.
The style police would prefer consistency in the placement of `&`.
The majority of the surrounding code places the `&` prefixed to the variable name. (Same for this method in `api/wallet.h` and `api/wallet2_api.h`)

Another style police complaint: The indentation of this method and other ones below is 2 spaces, while the majority of the Wallet API uses 4 spaces.

## SNeedlewoods | 2026-06-29T19:06:44+00:00
Not sure why `seed_words_epee` gets set to `seed_words` instead of empty `wipeable_string` here, AFAICT `seed_words` is the outparam, so in the case if for some reason it's set to a long string of garbage, this seems like a waste?

## SNeedlewoods | 2026-06-29T19:07:02+00:00
In the updated Wallet API #9464 I added two more args to `WalletManager`s create/restore methods: 
```
bool create_address_file = false,
bool unattended = true
```
Looking at the comments in my PR reminded me I should change it to use the same comment for all the methods, currently it's a wild mix. Thinking maybe something like the following is okay:
(Not saying that's what you should use, just to give you context about what these params are used for. To be clear `create_address_file` is not that important IMO, but `unattended` is needed)
```      
     * \param  create_address_file - Whether to create <wallet_name>.address.txt file, containing the primary address (Default: false)
     *                               NOTE: gets ignored for stagenet and testnet and will create .address.txt file even if set to false 
     * \param  unattended          - Whether the wallet is unattended (Default: true)
     *                               (meaning it stores keys unencrypted, e.g. GUI is unattended and uses password for verification only, CLI is _not_ unattended and needs a password to decrypt the keys)
```

## SNeedlewoods | 2026-06-29T19:07:10+00:00
The `unattended` would get put here
``` suggestion
    WalletImpl * wallet = new WalletImpl(nettype, kdf_rounds, unattended);
```
and `create_address_file` would get passed to `createFromPolyseed()` below.

## SNeedlewoods | 2026-06-29T19:07:22+00:00
``` suggestion
Wallet * createWalletFromPolyseed(const std::string &path,
```
`virtual` should only be used in `wallet2_api.h`.
Context: https://github.com/monero-project/monero/pull/9925#issuecomment-2922932059

# Action History
- Created by: rbrunner7 | 2026-06-14T13:35:35+00:00
