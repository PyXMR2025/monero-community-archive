---
title: 'wallet2: fix `store_to()` and `change_password()`'
source_url: https://github.com/monero-project/monero/pull/8937
author: jeffro256
assignees: []
labels: []
created_at: '2023-07-07T17:57:50+00:00'
updated_at: '2023-09-22T00:32:44+00:00'
type: pull_request
status: merged
closed_at: '2023-09-15T03:18:05+00:00'
merged_at: '2023-09-15T03:18:05+00:00'
---

# Original Description
Resolves https://github.com/monero-project/monero/issues/8932 and:
2. Not storing cache when new path is different from old in `store_to()` and
3. Detecting same path when new path contains entire string of old path in `store_to()` and
4. Changing your password / decrypting your keys (in this method or others) and providing a bad original password and getting no error and
5. Changing your password *and* storing to a new file

# Discussion History
## woodser | 2023-07-08T23:02:26+00:00
The old files should be removed only after the new files are stored in case there's any problem storing the new files.

## woodser | 2023-07-08T23:04:33+00:00
Comment with code is unclear. Perhaps a leftover?

## woodser | 2023-07-08T23:06:38+00:00
IIRC whitespace changes are to be avoided so these lines would have no diff.

## vtnerd | 2023-07-09T15:55:33+00:00
Is this necessary? Wouldn't the temp file just be `whatever.new.new`?

## woodser | 2023-07-10T11:37:45+00:00
This end comment is not necessary and breaks convention from the rest of the code. Personally I find it distracting.

## woodser | 2023-07-10T12:04:23+00:00
As far as I can tell, the list is ordered correctly according to the logic, so this comment doesn't need to be added?

## woodser | 2023-07-10T13:17:31+00:00
This error is being triggered when changing the password of an in-memory wallet, since `change_password` calls this function.

## jeffro256 | 2023-07-10T14:28:58+00:00
How about the latest commit?

## woodser | 2023-07-10T15:19:42+00:00
It's fixed with the last commit.

## jeffro256 | 2023-07-13T21:08:00+00:00
Well it could overwrite that file otherwise if it already existed. It's probably not a big deal either way, but might prevent some loss from a buggy script seeing a ".new" file and trying to move it (e.g.)

## woodser | 2023-07-17T21:09:57+00:00
I'm getting a compiler warning that this function is unused. It can be deleted?

## jeffro256 | 2023-07-18T04:17:09+00:00
Yes it can

## j-berman | 2023-08-23T08:04:58+00:00
Seems this could be a breaking change if someone has already chosen to end their wallet cache with ".new" though. I agree it would have made sense to restrict it from the start, but considering it's been out in the wild for years it makes sense to keep allowing it

## j-berman | 2023-08-23T08:25:53+00:00
Nit: "both" is a little unclear because there are 3 params. Could explicitly say `path` and `password`

## j-berman | 2023-08-23T08:44:05+00:00
I now get very uncomfortable with uninitialized booleans :)

## j-berman | 2023-08-23T09:02:18+00:00
Testing closing + opening the new file would be nice here (untested on my end)

```suggestion
    const auto files_are_expected = [&]()
    {
        EXPECT_FALSE(is_file_exist(interm_wallet_file.string()));
        EXPECT_FALSE(is_file_exist(interm_wallet_file.string() + ".keys"));
        EXPECT_TRUE(is_file_exist(target_wallet_file.string()));
        EXPECT_TRUE(is_file_exist(target_wallet_file.string() + ".keys"));
    };

    {
        tools::wallet2 w;
        w.load(interm_wallet_file.string(), password);
        const std::string primary_address = w.get_address_as_str();
        EXPECT_EQ(WALLET_00fd416a_PRIMARY_ADDRESS, primary_address);
        w.store_to(target_wallet_file.string(), password);
        files_are_expected();
    }

    files_are_expected();

    {
        tools::wallet2 w;
        w.load(target_wallet_file.string(), password);
        const std::string primary_address = w.get_address_as_str();
        EXPECT_EQ(WALLET_00fd416a_PRIMARY_ADDRESS, primary_address);
        files_are_expected();
    }

    files_are_expected();
```

## j-berman | 2023-08-23T09:08:41+00:00
```suggestion
    {
        tools::wallet2 w;
        w.generate("", password);
        w.store_to(target_wallet_file.string(), password);
        
        EXPECT_TRUE(is_file_exist(target_wallet_file.string()));
        EXPECT_TRUE(is_file_exist(target_wallet_file.string() + ".keys"));
    }

    EXPECT_TRUE(is_file_exist(target_wallet_file.string()));
    EXPECT_TRUE(is_file_exist(target_wallet_file.string() + ".keys"));
    
    {
        tools::wallet2 w;
        w.load(target_wallet_file.string(), password);
        
        EXPECT_TRUE(is_file_exist(target_wallet_file.string()));
        EXPECT_TRUE(is_file_exist(target_wallet_file.string() + ".keys"));
    }

    EXPECT_TRUE(is_file_exist(target_wallet_file.string()));
    EXPECT_TRUE(is_file_exist(target_wallet_file.string() + ".keys"));
```

## jeffro256 | 2023-08-23T16:49:44+00:00
Alright I removed that check

# Action History
- Created by: jeffro256 | 2023-07-07T17:57:50+00:00
- Merged at: 2023-09-15T03:18:05+00:00
