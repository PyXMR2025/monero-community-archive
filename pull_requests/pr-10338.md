---
title: 'crypto: derive key image generator & separate {un}biased hash to ec'
source_url: https://github.com/monero-project/monero/pull/10338
author: j-berman
assignees: []
labels:
- pending review
- cryptography
- fcmp++
created_at: '2026-02-25T02:38:35+00:00'
updated_at: '2026-07-12T22:27:04+00:00'
type: pull_request
status: open
closed_at: null
merged_at: null
---

# Original Description
In https://github.com/monero-project/research-lab/issues/142 , @kayabaNerve noted the potential bias in Monero's hash-to-point function, and proposed introducing a cleaner *unbiased* hash-to-point function with the FCMP++ upgrade to maximize bit security. This proposal was discussed further in [this MRL meeting](https://github.com/monero-project/meta/issues/1261), where @kayabaNerve provided [additional context](https://github.com/monero-project/research-lab/issues/142#issuecomment-3956284024) for why this property is even more desirable with FCMP++/Carrot to avoid the ["burning bug"](https://web.getmonero.org/2018/09/25/a-post-mortum-of-the-burning-bug.html) in potential circumstances specific to FCMP++/Carrot and not RingCT.

With that general context in mind, this PR introduces 3 new core crypto functions:

- `biased_hash_to_ec` (previously `hash_to_ec`)
  - This change makes it clear that legacy code uses the "biased" implementation, which should help clearly separate it from the upgraded "unbiased" implementation and help avoid accidental usage for new code. 
- `unbiased_hash_to_ec`
  - Used to generate key images in FCMP++ / Carrot code (current iteration needs [this PR](https://github.com/monero-oxide/monero-oxide/pull/182) also).
- `derive_key_image_generator`
  - This new helper function is used in FCMP++ code for generating `I`, which is a member of a leaf that enters the FCMP++ "curve tree," and is used to derive the key image (key images are `x * I`, where x is the one-time priv key used to spend an output). See references to `I` in [the FCMP++ paper](https://github.com/kayabaNerve/fcmp-plus-plus-paper/blob/develop/fcmp%2B%2B.pdf) ([specific commit](https://github.com/kayabaNerve/fcmp-plus-plus-paper/blob/6f01fd3296b17f90b5c97c738cc3320e3369ad49/fcmp%2B%2B.pdf)).
  ______

Cypher Stack and Trail of bits audited an earlier state of this PR.

Cypher Stack recommended using a domain-separating tag for our hash to point in [Section 9.3.3](https://github.com/cypherstack/fcmp-impl-audit/blob/d7de33d08c48a3c249629e47f71ea72c938a3404/FCMP_Code_Implementation_Audit.pdf). Thus, I implemented and used the same `blake2_monero` used for SAL from [this PR](https://github.com/monero-oxide/monero-oxide/pull/176) by @UkoeHB. The Rust side needs [this PR](https://github.com/monero-oxide/monero-oxide/pull/182) to match, which also implements the same tests as this current PR (@UkoeHB suggested to include hard-coded tests consistent on the C++ side and Rust side).

Both Cypher Stack and Trail of Bits affirmed the rationale for this PR, and the implementation of the unbiased hash-to-point (neither reviewed the code that includes the DST from `blake2_monero`). I will share the Trail of Bits audit once public.


# Discussion History
## UkoeHB | 2026-07-08T19:56:06+00:00
This is missing an optional 32-byte key input.

## UkoeHB | 2026-07-09T00:16:48+00:00
```suggestion
	/// Accepts an optional 32-byte key.
	int blake2b_monero(void *out, size_t outlen, const void *in, size_t inlen,
		const void *key);
```

## UkoeHB | 2026-07-09T00:17:35+00:00
```suggestion
int blake2b_monero(void *out, size_t outlen, const void *in, size_t inlen,
	const void *key) {
```

## j-berman | 2026-07-09T01:41:48+00:00
The other `blake2b` functions that accept a `key` param all support `keylen <= BLAKE2B_KEYBYTES`, same with the [reference impl](https://github.com/BLAKE2/BLAKE2/blob/ed1974ea83433eba7b2d95c5dcd9ac33cb847913/ref/blake2b-ref.c#L120-L126). So long as we're adding functionality for an optional `key` here, I think it's reasonable to support the `keylen` param too to match the other functions in the API. Callers can easily just set `keylen = 32`

## UkoeHB | 2026-07-09T02:04:28+00:00
`Blake2bMonero` on the rust side is a baked-in 32-byte optional key, as are the Carrot functions.

## UkoeHB | 2026-07-09T02:08:30+00:00
I think there is a benefit to standardizing/constraining the API, since a broader API implies via Hyrum's Law that anything the API allows *might* be in use. We have always only used 32-byte keys and don't have a reason to change that. It's fine to require *expand only with good reason*.

## j-berman | 2026-07-09T02:36:03+00:00
If someone does want to use the `blake2b_monero` function in the future with a key with `keylen != 32` but they don't modify the function to support it, the function won't necessarily return an error nor explicitly indicate failure/incorrect behavior (aside from the comment). It seems like a footgun to leave like that to me.

I see it as more likely that happens than a caller calling it with a `key` they intended to be 32 bytes, but they pass an incorrect non-zero value for the `keylen`.

The Rust side uses a `[u8; 32]` type rather than a `void *`, so isn't prone to this particular issue

## UkoeHB | 2026-07-09T02:48:14+00:00
> If someone does want to use the blake2b_monero function in the future with a key with keylen != 32 but they don't modify the function to support it, the function won't necessarily return an error nor explicitly indicate failure/incorrect behavior (aside from the comment). 

In that case I'd support asserting keylen == 32.

## j-berman | 2026-07-09T10:48:35+00:00
Done

## jeffro256 | 2026-07-09T15:33:00+00:00
`block` is not wiped if `blake2b_update()` fails

## jeffro256 | 2026-07-09T15:44:21+00:00
Suggested diff:

```diff
diff --git a/src/crypto/blake2b.c b/src/crypto/blake2b.c
index 5e2c51e36..4b4c7828e 100644
--- a/src/crypto/blake2b.c
+++ b/src/crypto/blake2b.c
@@ -279,11 +279,13 @@ int blake2b_init_key(blake2b_state *S, size_t outlen, const void *key, size_t ke
 
        {
                uint8_t block[BLAKE2B_BLOCKBYTES];
+               int r;
                memset(block, 0, BLAKE2B_BLOCKBYTES);
                memcpy(block, key, keylen);
-               blake2b_update(S, block, BLAKE2B_BLOCKBYTES);
+               r = blake2b_update(S, block, BLAKE2B_BLOCKBYTES);
                /* Burn the key from stack */
                clear_internal_memory(block, BLAKE2B_BLOCKBYTES);
+               (void) r; assert(0 == r);
        }
        return 0;
 }
@@ -565,13 +567,13 @@ int blake2b_monero(void *out, size_t outlen, const void *in, size_t inlen,
 
        if (NULL != key) {
                uint8_t block[BLAKE2B_BLOCKBYTES];
+               int r;
                memset(block, 0, BLAKE2B_BLOCKBYTES);
                memcpy(block, key, keylen);
-               if (blake2b_update(&S, block, BLAKE2B_BLOCKBYTES) < 0) {
-                       goto fail;
-               }
+               r = blake2b_update(&S, block, BLAKE2B_BLOCKBYTES);
                /* Burn the key from stack */
                clear_internal_memory(block, BLAKE2B_BLOCKBYTES);
+               (void) r; assert(0 == r);
        }
 
        if (blake2b_update(&S, in, inlen) < 0) {

```

## jeffro256 | 2026-07-09T20:09:13+00:00
Just FYI, `src/carrot_core/hash_functions.cpp` in #9559 was refactored to use this

## j-berman | 2026-07-09T21:19:26+00:00
> block is not wiped if blake2b_update() fails

Handled in the latest. Figure it makes sense for the caller to fail if `blake2b_update` fails.

## jeffro256 | 2026-07-10T20:01:39+00:00
```suggestion
		if (r) {
			return r;
		}
```

## jeffro256 | 2026-07-10T20:04:54+00:00
I recommend putting this statement underneath the variable declarations in this block

## jeffro256 | 2026-07-10T20:06:28+00:00
```suggestion
		ret = blake2b_update(&S, block, BLAKE2B_BLOCKBYTES);
		/* Burn the key from stack */
		clear_internal_memory(block, BLAKE2B_BLOCKBYTES);
		if (ret) {
			goto fail;
		}
```

## jeffro256 | 2026-07-10T20:08:20+00:00
```suggestion
    unsigned char hash[64];
```

## jeffro256 | 2026-07-10T20:08:35+00:00
```suggestion
    ge_fromfe_frombytes_vartime(&first, hash);
```

## jeffro256 | 2026-07-10T20:08:49+00:00
```suggestion
    ge_fromfe_frombytes_vartime(&second, hash + 32);
```

## jeffro256 | 2026-07-10T20:13:06+00:00
Integrating this would be outside the scope of the PR, but is there anywhere in the daemon code where the `derive_key_image_generator()` would be called, and then immediately followed up with a multiplication or point conversion (Wei)? In these cases, we may be able to speed up tree building by returning a `ge_p3` instead of a `crypto::ec_point`, avoiding one compression and decompression per tx output. What would actually be in-scope for this PR would be factoring out (and maybe exposing) that `ge_p3` overload of  `unbiased_hash_to_ec()`, much like how `biased_hash_to_ec()` does.

## j-berman | 2026-07-12T22:08:53+00:00
Disagree with this suggestion, this suggestion deviates from the rest of the function which all returns -1 on any error, not the return value of a specific function

## j-berman | 2026-07-12T22:09:25+00:00
Why?

## j-berman | 2026-07-12T22:12:59+00:00
Also disagree with this for the same/similar reason as the other: the file uses a pattern of returning -1 on any failure. I think it's reasonable either to maintain that pattern, or modify the pattern for all other functions too/all other places. And I think for purposes of this PR, path of least resistance here is maintaining the pattern (and perhaps a future PR could modify all other spots)

## j-berman | 2026-07-12T22:24:51+00:00
Yep, there is, nice spot. That's an optimization on the table that I think will be easier to implement on the PR that calls `derive_key_image_generator`

## j-berman | 2026-07-12T22:27:03+00:00
Done in latest (as well as the other 2 related suggestions)

# Action History
- Created by: j-berman | 2026-02-25T02:38:35+00:00
