---
title: 'wallet: fix warning signed unsigned comparison'
source_url: https://github.com/monero-project/monero/pull/10744
author: SNeedlewoods
assignees: []
labels:
- wallet
created_at: '2026-06-08T18:54:44+00:00'
updated_at: '2026-06-14T15:59:46+00:00'
type: pull_request
status: merged
closed_at: '2026-06-14T15:59:46+00:00'
merged_at: '2026-06-14T15:59:46+00:00'
---

# Original Description
https://github.com/monero-project/monero/actions/runs/27014746716/job/79726796084#step:6:645

# Discussion History
## iamamyth | 2026-06-09T18:16:23+00:00
If you're going to apply this cast, it seems reasonable to switch this to an else and store the value in a local to clarify the semantics (and guard against mistakes): Height has to be greater than value subtracted from height, otherwise the result isn't a valid block height (0 not valid).

## SNeedlewoods | 2026-06-10T11:44:02+00:00
Do you mean something like this?
``` diff
diff --git a/src/wallet/wallet2.cpp b/src/wallet/wallet2.cpp
index d0e42d2e4..135276d62 100644
--- a/src/wallet/wallet2.cpp
+++ b/src/wallet/wallet2.cpp
@@ -12800,12 +12800,16 @@ uint64_t wallet2::get_approximate_blockchain_height() const
   const time_t now = time(NULL);
   if (now > fork_time)
     approx_blockchain_height += (now - fork_time) / seconds_per_block;
-  else if (approx_blockchain_height > static_cast<uint64_t>((fork_time - now) / seconds_per_block))
-    approx_blockchain_height -= (fork_time - now) / seconds_per_block;
   else
   {
-    LOG_ERROR("Failed to approximate blockchain height from future fork block: " << approx_blockchain_height);
-    return 0;
+    uin64_t blocks_until_next_fork = static_cast<uint64_t>((fork_time - now) / seconds_per_block);
+    if (approx_blockchain_height > blocks_until_next_fork)
+      approx_blockchain_height -= blocks_until_next_fork;
+    else
+    {
+      LOG_ERROR("Failed to approximate blockchain height from future fork block: " << approx_blockchain_height);
+      return 0;
+    }
   }
   // testnet and stagenet got some huge rollbacks, so the estimation is way off
   const uint64_t approximate_rolled_back_blocks = m_nettype == TESTNET ? 26600 : m_nettype == STAGENET ? 48600 : 33600;
```

## iamamyth | 2026-06-10T17:32:29+00:00
Yes, that looks good.

## selsta | 2026-06-10T18:50:12+00:00
```suggestion
    const uint64_t blocks_until_next_fork = static_cast<uint64_t>((fork_time - now) / seconds_per_block);
```

## selsta | 2026-06-10T18:53:22+00:00
While you are at it, you might also want to cast here even if this does not warn with our compiler settings, so that the code is more symmetric with adding uint64_t to uint64_t in both branches.

# Action History
- Created by: SNeedlewoods | 2026-06-08T18:54:44+00:00
- Merged at: 2026-06-14T15:59:46+00:00
