---
title: Add randomized delay when forwarding txes from i2p/tor -> ipv4/6
source_url: https://github.com/monero-project/monero/pull/6354
author: vtnerd
assignees: []
labels: []
created_at: '2020-02-25T05:59:43+00:00'
updated_at: '2024-04-01T00:35:15+00:00'
type: pull_request
status: merged
closed_at: '2020-08-09T13:43:12+00:00'
merged_at: '2020-08-09T13:43:11+00:00'
---

# Original Description
The last bullet point on the CCS for transaction privacy is a randomized delay before a node takes an incoming transaction from i2p/tor and sends it over ipv4/6. My thought was an attempt to conceal the bandwidth/timing analysis of i2p/tor. If the the i2p/tor hidden service only has one incoming connection, this delay possibly/probably does nothing. But if the hidden service has 2+ incoming connections sending white noise, the transaction could've been received over any of the white noise connections.

This creates another mempool state.

@SarangNoether might be interested.

# Discussion History
## vtnerd | 2020-02-25T06:03:36+00:00
This drops a mempool meta bit flag that has never been in a release, but has been in `master`. I realized it was more efficient to use `kept_by_block` instead. Anyone running from `master` that has a `is_local` flag in their pool will see incorrect behavior when running this change. I think its better to clean this up before a release, but ... ?

## xiphon | 2020-02-26T01:57:53+00:00
```suggestion
        if (kept != kept_by_block)
```

## xiphon | 2020-02-26T02:05:15+00:00
Seems reasonable to introduce 
```c++
bool kept_by_block() const noexcept;
```


## moneromooo-monero | 2020-02-26T18:28:03+00:00
And grep well since anything that does "if (foo.kept_by_block)" will be turned into "if (true)".

## moneromooo-monero | 2020-02-26T18:29:18+00:00
<s>Are you saying kept_by_block is new ? It's not. Or are you talking about something else here ?</s> nvm, the relevant part is not what's displayed in this bit of code.

## vtnerd | 2020-02-26T21:45:25+00:00
I will definitely make this change, provided the DB change to `master` is acceptable.

## vtnerd | 2020-02-26T21:45:53+00:00
I decided to keep this as-is for whatever reason, but seems reasonable to change.

## vtnerd | 2020-03-10T22:49:09+00:00
Didn't see any objections (or any comments at all), so this function was added and used where appropriate.

## moneromooo-monero | 2020-04-07T01:12:37+00:00
tbh, this is really dodgy. Does this really need to be obfuscated like this ?

## vtnerd | 2020-04-12T02:17:36+00:00
The code could continue using the inefficient bit mechanism, or could "break" the existing txpool between releases (this code breaks the txpool for users running master branch only).

## moneromooo-monero | 2020-04-13T12:18:20+00:00
Why do we care about efficient bit twiddling when it's about I/O ?

## vtnerd | 2020-04-13T16:43:10+00:00
Its less wasted space and less CPU cycles to just store/retrieve this value as the enum value.

## moneromooo-monero | 2020-04-14T11:18:53+00:00
Well, whatever. If you prefer saving a few cycles hidden next to massive amounts of them for confusing code, fine.

## vtnerd | 2020-04-14T15:50:13+00:00
No, my preference was to break the existing txpool (pain should be minimal) to do: `m_state = uint8_t(tx_relay);`.

I think in the end, I will drop this. This creates a hard dependency on the enum value and DB value. How about something like:

```c++
const uin8_t state =
  uint8_t(do_not_relay) +
  uint8_t(kept_by_block) << 1 +
  uint8_t(forward) << 2;

switch (state)
{
  default:
  case 0:
    return relay_method::fluff;
  case 1:
    return relay_method::none;
  case 2:
    return relay_method::block;
  case 4:
    return relay_method::forward:
}
```
which also handles the incorrect DB state, and maps it to fluff. Or the if-else ladder (which maps the incorrect DB state to some other value - but I doubt that matters anyway).

## moneromooo-monero | 2020-04-16T14:57:43+00:00
I much prefer this since I find it much clearer. It looks equivalent to the even simpler:
```
if (forward)
  return relay_method::forward;
if (kept_by_block)
  return relay_method::block;
if (do_not_relay)
  return relay_method::none;
return fluff;
```

## moneromooo-monero | 2020-04-16T15:03:53+00:00
Amusingly, it looks close to what you had before. The thing I was finding objectionable was not the set of tests, it was the case on enum+1.

## vtnerd | 2020-04-17T21:00:27+00:00
Yes, the original version was iffy because it required knowledge of the enum values to read it correctly. And a change in enum values could break the function. This version is similar looking, but everything is self-contained within the function.

FWIW, I disassembled this version and gcc 9 was able to optimize the function into a single comparison for the "error" case. Otherwise it used adds, shifts and a 64-bit constant to put the enum value in the return register. Roughly half of the instructions could be done in parallel by the CPU backend.

The if-ladder is less x64-64 instructions but 3-total comparison instructions. The compiler can still generate the same ASM with the switch version, but chose one with more math instructions and less comparisons.

## moneromooo-monero | 2020-04-19T12:52:59+00:00
For clarity, I really don't care about a dozen cycles saved for this. What I care about is the complexity of the C code.

## vtnerd | 2020-04-21T23:50:26+00:00
This merge here was a little more than usual, might be worth inspecting.

## vtnerd | 2020-04-21T23:51:31+00:00
New test.

## vtnerd | 2020-04-21T23:51:37+00:00
New test.

## vtnerd | 2020-04-21T23:51:53+00:00
Changed (see other comment).

## vtnerd | 2020-04-21T23:52:01+00:00
Changed (see other comment).

## vtnerd | 2020-04-21T23:54:21+00:00
See removal of `tx_relay = relay_method::fluff`. @moneromooo-monero asked about this in the Dandelion++ review, and the original code was/is incorrect. I thought it was appropriate to mark the txes as "fluff" because the fluff routine was being used. However, if someone disables i2p/tor white noise, its only using the fluff algorithm, and isn't intended to be marked as public. So the "state" change is not made here. This is verified in the unit tests (see other comments).

## vtnerd | 2020-04-22T00:20:10+00:00
Another reasonable big change. Transactions in the forward state get relayed over Dandelion++ stems when the timer expires.

## vtnerd | 2020-04-22T00:30:38+00:00
This code block here should block the PR from getting merged. My initial thought is to reset the timer for expired `forward` and `stem` txes transactions a bit further in the future to the standard (longer) re-relay timeout. I.e. when the initial forward/stem timer expires, the `txpool` needs to protect the txes from being relayed again too soon. Putting it here will guard against other changes to the codebase.

After Dandelion++, the `set_relayed` method is called asynchronously following a `get_relayable_transactions` call. The `get_relayable_transactions` method could be called again, and core could think it needs to relay `forward` transactions again 1 second later. This cannot happen with Dandelion++ stem, because the tx gets bumped to `fluff`, and is therefore marked as `fluff` synchronously (only Dandelion++ stem is delayed, as it asynchronously gets queued for inspecting local fluffing status).

## vtnerd | 2020-04-22T00:35:25+00:00
And @moneromooo-monero I ruined tabs again just for you. I switched to emacs recently :/ will fix.

## vtnerd | 2020-04-23T23:54:25+00:00
Marking the above comment as resolved, see comment here about the change to the PR.

## vtnerd | 2020-04-23T23:56:18+00:00
Marked this change earlier with poor tabbing (which is now marked as resolved), and re-marking to highlight the somewhat complex rebase since earlier reviews.

## vtnerd | 2020-04-24T02:35:32+00:00
See the change here, and associated comment above.

## vtnerd | 2020-04-24T02:36:21+00:00
Changed within this PR (see other comment within this review in `levin_notify.cpp`). All similar changes were for same reason.

## moneromooo-monero | 2020-07-12T10:52:09+00:00
Why is this moved before the lock ? It's fairly innocuous, but might result in an extra allocation, which you usually hate :)

## moneromooo-monero | 2020-07-12T10:54:00+00:00
Should none be unconditionally returning true, since they're not to be relayed ?

## vtnerd | 2020-08-07T22:13:11+00:00
The `return true` after this chunk is within the callback given to `for_all_txpool_txes`, so it should continue to the next transaction. If you meant should the entire function unconditionally `return true`, there is a long standing `TODO` to investigate whether `true` should always be returned. I have yet to investigate this longstanding `TODO`.

## vtnerd | 2020-08-07T22:18:09+00:00
I don't see where the extra allocation would come from - the `std::vector` gets reserved either way. The only difference is that the allocation for `std::vector` occurs before acquiring the lock. This typically hits another resource atomic/lock thingy within the allocator anyway, so its probably not mattering much.

## vtnerd | 2020-08-08T00:36:07+00:00
Sorry just got your comment, misread it previously. Yes, I will add an explicit `return` for the `none` label.

## vtnerd | 2020-08-08T00:37:35+00:00
Although to clarify - `get_relay_method()` returns `relay_method::none` when `meta.do_not_relay == true` which is already filtered above, but another explicit check cannot hurt.

## moneromooo-monero | 2020-08-08T20:23:27+00:00
Since the reserve is before the locks, another thread might add a tx to the txpool betewen the reserve and the locking.

## moneromooo-monero | 2020-08-08T20:24:32+00:00
That said, it's trivial and of no consequence really. I'm ok with it as is too if there's even a slight reason for the move.

## moneromooo-monero | 2020-08-08T20:26:24+00:00
If it's a case that cannot happen, then it's fine. I meant just to avoid going into the subsequent age test. I did not realize this case was impossible in practice.

## vtnerd | 2020-08-09T04:36:51+00:00
This was a good catch. It was a call on `m_blockchain` directly, so I think its worth putting back inside the lock. Updated.

## vtnerd | 2020-08-09T04:37:54+00:00
I added a `return true` for both `default` and `relay_method::none` cases. It seemed worth adding for the former, so why not.

# Action History
- Created by: vtnerd | 2020-02-25T05:59:43+00:00
- Merged at: 2020-08-09T13:43:11+00:00
