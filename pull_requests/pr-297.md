---
title: 'cuprated: internal signatures required for RPC'
source_url: https://github.com/Cuprate/cuprate/pull/297
author: hinto-janai
assignees: []
labels:
- A-p2p
- A-consensus
- A-storage
- A-types
- A-binaries
created_at: '2024-09-23T22:42:02+00:00'
updated_at: '2024-10-18T00:25:48+00:00'
type: pull_request
status: merged
closed_at: '2024-10-08T21:57:09+00:00'
merged_at: '2024-10-08T21:57:09+00:00'
---

# Original Description
### What
Adds the `{function, request, response}` signatures needed by `cuprated`'s RPC system to the `{blockchain, txpool, p2p, blockchain_context}` APIs.

Closes https://github.com/Cuprate/cuprate/issues/279.

This covers most (but not all) signatures needed for all RPC requests (JSON-RPC, binary, other).

# Discussion History
## hinto-janai | 2024-10-03T00:23:48+00:00
### Misc
We actually need access to `restricted` within the handlers which means there's no reason to separate `CupratedRpcHandler` into a `state: CupratedRpcHandlerState` anymore for cheaper cloning since we need the whole thing.

You can ignore all the changes in `binaries/cuprated/src/rpc/{json,bin,other}.rs`, they're just updating the input from `CupratedRpcHandlerState` -> `CupratedRpcHandler`.

## hinto-janai | 2024-10-03T00:26:58+00:00
### Misc

You can also mostly ignore `binaries/cuprated/src/rpc/request/`, these are just convenience function that just call + unwrap request/responses so there isn't this type of code everywhere:
```rust
let Response::Variant(data) = blockchain_read
    .ready()
    .await?
    .call(Request::Variant)
    .await?
else {
    unreachable!();
};

Ok(data)
```

## hinto-janai | 2024-10-03T00:28:11+00:00
### Misc
Renamed these to match other variants.

## hinto-janai | 2024-10-03T00:29:02+00:00
### Misc
Not true anymore, we need `Clone` on `CupratedRpcHandler` and thus the `DatabaseWriteHandle`.

## hinto-janai | 2024-10-03T00:33:20+00:00
### Misc
The services that use `tower::BoxError` don't work with `anyhow::Error` when using `?`, can we swap them to `anyhow`?

The services are:
- address book
- blockchain context
- transaction pool 

## hinto-janai | 2024-10-03T00:35:07+00:00
### Signatures (blockchain manager)
Since this doesn't exist in-tree yet, there are just temporary types so signatures line up.

## hinto-janai | 2024-10-03T00:35:58+00:00
### Signatures (blockchain context request)
Added to the existing `BlockChainContextRequest`.

## hinto-janai | 2024-10-03T00:36:14+00:00
### Signatures (blockchain context response)
Added to the existing `BlockChainContextResponse`.

## hinto-janai | 2024-10-03T00:37:17+00:00
### Signatures (address book request)
Added to the existing `AddressBookRequest`.

`IsPeerBanned` can be replaced by `GetBan` (see below)

## hinto-janai | 2024-10-03T00:38:33+00:00
### Signatures (address book response)
Added to the existing `AddressBookResponse`.

RPC's `GetBan` needs the UNIX time where a peer is unbanned, since the peer might not be banned, this call can also function as a `IsPeerBanned` with `unban_instant.is_some()`.

## hinto-janai | 2024-10-03T00:40:37+00:00
### Signatures (txpool request/request)
Added to the existing `TxpoolRead{Request,Response}`.

## hinto-janai | 2024-10-03T00:41:34+00:00
### Signatures (blockchain request)
Added to the existing `BlockchainReadRequest`.

## hinto-janai | 2024-10-03T00:41:52+00:00
### Signatures (blockchain response)
Added to the existing `BlockchainResponse`.

## hinto-janai | 2024-10-03T00:44:10+00:00
### Types (address book)
Some of the requests/responses in this PR require defining some types.

A lot of these are 1-1 with the RPC types but I decided on making more specific versions because:
- Pulling in `cuprate-rpc-types` everywhere seems wrong
- The RPC types are JSON-like, so a `[u8; 32]` field will be something like `String`, adding these types lets us use the canonical type internally

Some of the types are defined in the crate where the request/response is defined, many are defined in `cuprate-types`.

## hinto-janai | 2024-10-03T00:44:52+00:00
### Types (txpool)

## hinto-janai | 2024-10-03T00:45:39+00:00
### Types (everything else)

## hinto-janai | 2024-10-03T00:49:15+00:00
### TODO
All the new request/responses are fully typed but they will all panic with `todo!()`.

The exception is `AddressBookRequest::GetBan` which I added a new function for.

Handlers can be added in a different PR.

## hinto-janai | 2024-10-03T00:50:15+00:00
### TODO
Why does this return a `HashMap` when the request is `CurrentRxVm`?

I might be misremembering but I think somewhere in RPC we need access to a RandomX VM, would calling `.last()` on this `HashMap` always give us the latest in a `Some`?


## hinto-janai | 2024-10-03T01:03:18+00:00
@Boog900 some questions about https://github.com/Cuprate/cuprate/issues/279#issuecomment-2351034124:

> The transaction pool is going to have a specific interface for requests that mutate the pool, just to keep in mind, it wont directly use the write handle.

- What should be here in `txpool_write`?
- Is "specific interface" just going to be a different `tower::Service`?
- Why does the txpool need this special interface?

## Boog900 | 2024-10-03T22:20:41+00:00
The blockchain manager isn't a service, it could be, I don't think it should be. see: https://github.com/Cuprate/cuprate/pull/274#discussion_r1786893092

These requests/responses will just be free functions.

## Boog900 | 2024-10-03T22:22:15+00:00
This should be add block, this also happens to be the one request I already have in the blockchain manager PR (`handle_incoming_block`).

## Boog900 | 2024-10-03T22:27:15+00:00
> What should be here in txpool_write?
> Is "specific interface" just going to be a different tower::Service?

Yes, so a handle to that other service.

> Why does the txpool need this special interface?

To synchronise tx-pool state so we don't end up verifying the same tx across multiple connections before adding them to the pool. There are other operations that require more synchronization than a database write handle can provide. 

## Boog900 | 2024-10-03T22:29:14+00:00
Yeah, there are other as well, probably should be done in a separate PR

## Boog900 | 2024-10-03T22:34:31+00:00
> Why does this return a HashMap when the request is CurrentRxVm?

It should probably be `CurrentRxVms`, it needs to return more than one so we can compute PoW concurrently.

> I might be misremembering but I think somewhere in RPC we need access to a RandomX VM

You will probably just need to generate a new VM - I don't think you will need the current active one, I may be wrong though I just can't see why you would.

## Boog900 | 2024-10-03T22:36:00+00:00
Yeah separating them is a good idea IMO

## Boog900 | 2024-10-03T22:38:05+00:00
Those types seem weird though, wouldn't this make more sense: 

```suggestion
//! Data structures related to bans.

use std::time::{Duration, Instant};

use crate::NetZoneAddress;

/// Data within [`crate::services::AddressBookRequest::SetBan`].
pub struct SetBan<A: NetZoneAddress> {
    pub address: A,
    pub ban: Option<Duration>,
}

/// Data within [`crate::services::AddressBookResponse::GetBans`].
pub struct BanState<A: NetZoneAddress> {
    pub address: A,
    pub banned: Option<Instant>,
}
```

## Boog900 | 2024-10-03T22:40:16+00:00
This does need to be removed for the tx-pool handle, however the database is still only written to in 1 place, the blockchain manager.

You shouldn't need to hold a write handle to the blockchain in the RPC

## Boog900 | 2024-10-03T22:44:07+00:00
`Difficulty` is contained in the blockchain context so shouldn't need a new request (my bad for missing this).

`MinerData` will not be able to be a single request. Looking at its data it would need a request to the blockchain cache and to the tx-pool, both of these requests are already there/in this PR so this could just be removed I think 

## hinto-janai | 2024-10-04T20:18:29+00:00
OK, can these be converted after both this and #274 are merged? Are all the signatures here possible?

## hinto-janai | 2024-10-04T20:36:34+00:00
Does it have the same semantics though? It adds a block to our local chain _and_ broadcasts it?

The usecase is [`submit_block`](https://github.com/monero-project/monero/blob/9866a0e9021e2422d8055731a586083eb5e2be67/src/rpc/core_rpc_server.cpp#L2218).

## hinto-janai | 2024-10-07T23:46:24+00:00
TODO: replace with real interface when public.

## Boog900 | 2024-10-08T17:32:41+00:00
yep

## Boog900 | 2024-10-08T19:16:58+00:00
```suggestion
    /// - If [`Some`], the peer is banned until this [`Instant`]
    /// - If [`None`], the peer is not currently banned
```

# Action History
- Created by: hinto-janai | 2024-09-23T22:42:02+00:00
- Merged at: 2024-10-08T21:57:09+00:00
