---
title: 'p2p: Implement P2P Bucket data structure'
source_url: https://github.com/Cuprate/cuprate/pull/329
author: SyntheticBird45
assignees: []
labels:
- A-p2p
- A-dependency
- A-workspace
- A-docs
- A-books
- A-book-architecture
created_at: '2024-10-28T19:51:31+00:00'
updated_at: '2024-11-02T00:48:39+00:00'
type: pull_request
status: merged
closed_at: '2024-11-02T00:45:56+00:00'
merged_at: '2024-11-02T00:45:56+00:00'
---

# Original Description
### What

See: https://github.com/Cuprate/cuprate/issues/178

This PR implements a "Bucket" data structure that is a collection of data that discriminates its items into "buckets" (vector of size N) following a defined function.

- Implements Bucket data structure and Bucketable trait
- Implements Bucketable for Ipv4Addr
- Added the crate to the workspace dependencies
- Added `arrayvec` as a dependency

### Why

The end goal is to use this structure to store diverse IPs of different /16 subnets.

# Discussion History
## Boog900 | 2024-10-30T00:41:11+00:00
Can you revert the changes to the unrelated lines please 

## Boog900 | 2024-10-30T00:41:46+00:00
This will need license etc, can you copy from another crate and edit appropriately 

## Boog900 | 2024-10-30T00:44:39+00:00
for now can you remove this round_robin pop impl please.

It should be done as a separate struct which impls `Iterator`, however until we need it I would just remove this.

## SyntheticBird45 | 2024-10-30T00:52:34+00:00
I'm actually planning on passing to the Address book PR after that. Shouldn't it be preferable to make that iterator structure in this PR instead ?

## hinto-janai | 2024-10-30T00:53:54+00:00
```suggestion
arrayvec              = { version = "0.7.6", default-features = false }
```

## hinto-janai | 2024-10-30T00:54:41+00:00
```suggestion
//! Bucket data structure
//!
//! A collection data structure that discriminates its unique items and place them into "buckets".
//!
//! The item must implement the [`Bucketable`] trait that defines how to create the discriminant
//! from the item type. The data structure will internally contain any item into "buckets" or vectors
//! of sized capacity `N` that regroup all the stored items with this specific discriminant.
//!
//! A practical example of this data structure is for storing `N` amount of IP discriminated by their subnets.
//! You can store in each "buckets" corresponding to a `/16` subnet up to `N` IPs of that subnet.
```

## hinto-janai | 2024-10-30T00:55:34+00:00
```suggestion
/// A discriminant that can be computed from the type.
```

## hinto-janai | 2024-10-30T00:56:26+00:00
Could add a doc-test to enforce this invariant.

## Boog900 | 2024-10-30T00:58:40+00:00
The bucket structure is more for making sure we store a wide range of IPs, not that we select a wide range of IPs, having this round_robin makes it more likely for consecutive subnets to be all the peers we connect to.

Just randomly selecting peers across the whole structure would be better IMO, but this can be considered later.

## hinto-janai | 2024-10-30T01:00:03+00:00
```suggestion
            // Push the item if it doesn't exist.
            if !vec.contains(&item) {
                vec.push(item);
            }
```

## SyntheticBird45 | 2024-10-30T01:02:18+00:00
Fair enough.

## hinto-janai | 2024-10-30T01:02:53+00:00
An example usage test-doc would be nice.

## hinto-janai | 2024-10-30T01:09:06+00:00
```suggestion
            vec.iter()
                .enumerate()
                .filter_map(|(i, v)| (item == v).then_some(i))
                .last()
                .map(|index| vec.swap_remove(index))
```

## hinto-janai | 2024-10-30T01:09:33+00:00
```suggestion
    /// We are discriminating by `/16` subnets.
```

## SyntheticBird45 | 2024-10-30T12:25:23+00:00
If I remove the current pop there is no method to actually extract an IP address from the storage. Do you want me to make the `storage` field public ? or adapt the current `pop` to take a random item (i know this contradict your statement of needing an iterator but I'm going to implement anyway in the next PR since well I'll need it)? 

## Boog900 | 2024-10-30T22:06:44+00:00
Yeah adding a `pop` method that selects  a random peer would be good - although it should leave the peer in structure and just return a reference  

## Boog900 | 2024-11-01T22:46:21+00:00
It won't panic right?

## Boog900 | 2024-11-01T22:47:40+00:00
```suggestion
/// A collection data structure discriminating its unique items
/// with a specified method. Limiting the amount of items stored
// with that discriminant to the const `N`.
pub struct BucketMap<const N: usize, I: Bucketable> {
    /// The storage of the bucket
    storage: BTreeMap<I::Discriminant, ArrayVec<I, N>>,
}
```

## SyntheticBird45 | 2024-11-01T23:12:55+00:00
Yes, It will. 
Yes, It is a bad API, because it imply you have to use `len_bucket()` before use.
Yes, I would like to replace it with `try_push` or `push` but return a result or option.

## Boog900 | 2024-11-01T23:16:24+00:00
Ah it's an array vec, yes it should return an option, it should not panic 

## Boog900 | 2024-11-01T23:50:21+00:00
ready for: #335 

```suggestion
arrayvec              = { version = "0.7", default-features = false }
```

## Boog900 | 2024-11-01T23:54:04+00:00
```suggestion
        self.storage
            .values()
            .map(|bucket| bucket.len())
            .sum()
    }
```

## Boog900 | 2024-11-02T00:02:18+00:00
I think this could probably be made more efficient, maybe by using `find_map` instead of `filter_map`

# Action History
- Created by: SyntheticBird45 | 2024-10-28T19:51:31+00:00
- Merged at: 2024-11-02T00:45:56+00:00
