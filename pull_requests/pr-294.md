---
title: 'monerod: update systemd config'
source_url: https://github.com/monero-project/monero-docs/pull/294
author: thomasbuilds
assignees: []
labels: []
created_at: '2026-03-31T12:45:52+00:00'
updated_at: '2026-04-04T08:34:56+00:00'
type: pull_request
status: open
closed_at: null
merged_at: null
---

# Original Description
This pull request updates the example `systemd` service configuration for running `monerod` to simplify the setup and improve reliability. The changes focus on making the service easier to manage and more robust, especially in how it starts and interacts with the system.

Service configuration improvements:

* Changed the `Type` from `forking` to `simple` and removed the use of `--detach` and the `PIDFile`, so `systemd` manages the process directly without needing to track a separate daemonized process.
* Added `Requires=network-online.target` to ensure the service only starts after the network is fully online, improving startup reliability.
* Updated the `ExecStart` command to remove unnecessary options and add `--non-interactive` for better automation and compatibility with `systemd`.
* Removed the `RuntimeDirectory` directive, simplifying the configuration since the service no longer needs to manage a PID file.

# Discussion History
## nahuhh | 2026-03-31T13:06:01+00:00
Any reason you chose `Wants=` instead of `Requires=`?

## nahuhh | 2026-03-31T13:07:39+00:00
Under no normal circumstances should the daemon take 5mins to gracefully stop. I'd give it no more than 60sec (though 15sec would be more realistic)

## thomasbuilds | 2026-03-31T13:24:04+00:00
I checked and it seems `Requires=` would cause temporary network issues to stop the service entirely

## thomasbuilds | 2026-03-31T13:25:46+00:00
Yes you are right, let's do 30 like for restart?

## nahuhh | 2026-03-31T13:28:32+00:00
Sounds good

## thomasbuilds | 2026-03-31T19:19:50+00:00
Upon further investigation you're right

## nahuhh | 2026-03-31T19:58:03+00:00
this change matches the upstream example config: https://github.com/monero-project/monero/blob/master/utils%2Fsystemd%2Fmonerod.service#L12-L13

`--detach` method matches sethforprivacy's config: https://sethforprivacy.com/guides/run-a-monero-node-advanced/#install-monerod-systemd-script

I don't particularly have a preference, but i also dont use systemd for my own node



## thomasbuilds | 2026-04-01T06:48:41+00:00
sethforprivacy's config still uses `Type=forking` though.
I've had more reliable behavior with Type=simple and should also be recommended approach imo

## plowsof | 2026-04-01T07:05:02+00:00
from some random comment i found @ https://superuser.com/a/1274913

> a) If the service starts and keeps running, and the prompt does not return until you press Control-C or stop the service in some other way: then Type = simple (or Type = exec if your version of systemd has it) is the right choice.

IF we omit `--non-interactive` type should be simple so upstream is wrong according to this

> b) If the prompt returns but the service keeps running in the background (i.e. the service daemonizes itself on its own), then Type = forking is the right choice.

i.e. we start monerod with `--non-interactive`



## plowsof | 2026-04-01T07:11:34+00:00
edit to the above^ `--detach` daemonizes monerod, not `--non-interactive`, afaict this PR removes `--detach` so `simple` is correct now. with `--detach` monerod has the behaviour matching `b)`

so thats why seth is using fork, as he has `--detach`

## thomasbuilds | 2026-04-01T07:15:15+00:00
`--non-interactive` just disables the interactive console and the process still runs in the foreground. It's `--detach` that actually daemonizes monerod, which would require `Type=forking`. 
Btw Type=exec should be used instead of simple because seems like simple is legacy

## plowsof | 2026-04-01T07:20:15+00:00
this is the only thing that concerns me, id have to find reasoning for adding this, what the default is when this isn't added, and what happens after 30 seconds.

we do not want peoples databases to be corrupted should monerod be unresponsive/busy

## plowsof | 2026-04-01T07:21:34+00:00
?

## thomasbuilds | 2026-04-01T07:32:53+00:00
I removed it because `/run/monero/monerod.sock` wasn't used anywhere

## thomasbuilds | 2026-04-01T07:33:31+00:00
I added back anyways if you change your mind we can still remove it

## plowsof | 2026-04-01T07:35:13+00:00
reading https://www.freedesktop.org/software/systemd/man/latest/systemd.service.html?#TimeoutStopSec=

we do not have any `ExecStop=` defined so this is ignored / `SIGTERM` is sent blindly anyway. am i reviewing AI slop or do yo have a reason for adding this? the default seems to be 90 seconds.

we can not have an ExecStop defined like "./monerod exit" because user may have a custom rpc port 

```suggestion
```

## plowsof | 2026-04-01T07:37:34+00:00
?????

## plowsof | 2026-04-01T07:43:08+00:00
the PID file not being created anymore should make this change OK. ive not ran the systemd file to confirm yet 

## plowsof | 2026-04-01T07:45:59+00:00
"So, if your systemd is new enough to support the Type = exec, you might prefer using it over Type = simple for better error checking. But if you need the boot to proceed as quickly as possible, and rapid detection of the start-up failure of your service is not essential, and/or you wish to be compatible with older versions of systemd, you might still use Type = simple."

backwards compatibility + less thinking , i'd rather keep `simple`

## thomasbuilds | 2026-04-01T07:54:17+00:00
`Type=exec` has been available since systemd 240 (2018) so compatibility shouldn't be a concern on any reasonably modern system. It's also technically more correct here imo because unlike `simple`, it waits for the actual `exec()` call before marking the service started, meaning failed starts are detected immediately rather than silently.

## thomasbuilds | 2026-04-01T07:56:32+00:00
You decide though tell me if I need to change back to simple

## thomasbuilds | 2026-04-01T08:00:09+00:00
Yes I've just ran it and it works so I'll remove it

## plowsof | 2026-04-01T08:08:28+00:00
keep simple, matches upstream also. "waits for the actual exec()" - if your monerod binary is missing .... for any other problem that effects 99% of people - monerod will start and then exit e.g. port in use.

## thomasbuilds | 2026-04-01T08:12:22+00:00
ok I'll commit change

# Action History
- Created by: thomasbuilds | 2026-03-31T12:45:52+00:00
