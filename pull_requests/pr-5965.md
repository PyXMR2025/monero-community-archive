---
title: Split Anonymity Mempool
source_url: https://github.com/monero-project/monero/pull/5965
author: vtnerd
assignees: []
labels: []
created_at: '2019-10-07T14:10:57+00:00'
updated_at: '2021-01-04T04:53:10+00:00'
type: pull_request
status: merged
closed_at: '2019-12-12T19:33:24+00:00'
merged_at: '2019-12-12T19:33:24+00:00'
---

# Original Description
The first two commits are from [PR 5921](https://github.com/monero-project/monero/pull/5921) and from [PR 5944](https://github.com/monero-project/monero/pull/5944) respectively. So inspect the last commit only for changes relevant to this PR.

This is a large chunk of my last CCS (arguably Dandelion++ needs to be implemented for full collection). The mempool now has a separate flag for `anonymity` which indicates that the tx originated at a particular node that had i2p/tor enabled. Unfortunately this means the harddisk will temporarily store information indicating that a particular txid originated at a node. I didn't see a way around this, because the "stem" transactions (Dandelion++ relaying) and "anonymity" transactions (origin relaying) must be kept separate for timeout purposes, etc.

Also take close inspection on how I changed/merged `kept_by_block` and `do_not_relay` into a single enum. I believe this to be the desired behavior, but I had to manually scrub/audit parts of the code multiple times. All of the core tests still pass.

I will be probing this for more tests and just remembered that the markdown file for anonymity networks needs to be updated. But feedback on the high-level approach will be valuable.

# Discussion History
## moneromooo-monero | 2019-10-08T11:09:03+00:00
Why do this rather than add it as a relay_method& (or relay_method*) parameter ? It causes that hack you mention elsewhere, while complicating existing calls.

## moneromooo-monero | 2019-10-08T11:10:19+00:00
same

## moneromooo-monero | 2019-10-11T11:37:22+00:00
Sending those at the same time might allow a peer connected to both tor and clearnet to deduce they're the same ?

## moneromooo-monero | 2019-10-11T11:37:51+00:00
Why ? This will prevent further calls if it fails once. That does not seem like a good idea.

## moneromooo-monero | 2019-10-11T11:45:10+00:00
That seems quite an obfuscated way of looping through the array.

## moneromooo-monero | 2019-10-11T11:45:43+00:00
This and plenty of others like it just annoyingly spam the diff for no good reason.

## moneromooo-monero | 2019-10-11T11:57:10+00:00
That would likely not be needed if you didn't change the return value of many functions from bool.

## moneromooo-monero | 2019-10-11T11:59:03+00:00
Also received over tor/i2p, or are those with the "anonymity" enum ?
AFAIK, If you've got at least one tor/i2p connection, all txes go through at (except those in a block). So "flood" is really the same as "trying to send over i2p/tor", which is the comment for anonymity. This is very confusing.

## moneromooo-monero | 2019-10-18T12:46:01+00:00
This needs to be moved below the use of the data or it risks being unmapped.

## moneromooo-monero | 2019-10-18T12:54:50+00:00
Possibly handled elsewhere, but this looks like it might cause double addition to the txpool, possibly detecting double spends with itself. Delete this if I find it's handled elsewhere.

## moneromooo-monero | 2019-10-18T13:08:36+00:00
Why get the blob first ? It's a lot larger than the meta, which allows earlying out for most txes without having to get the blob. Granted, pruning will not happen often, but it still seems to puff up the diff with no obvious reason.

## moneromooo-monero | 2019-10-18T13:13:29+00:00
Would you mind adding a comment describing what this does ? AFAICT, if the tx being added is already present but was not broadcast already, it errors out. Is that right ? I assume "present but not broadcast" means stem phase ? If so, there could be a loop in the stem by chance and this would trigger (maybe OK ?)

## moneromooo-monero | 2019-10-18T13:17:36+00:00
Maybe indent it ?

## moneromooo-monero | 2019-10-18T13:24:15+00:00
Does this really change anything ?

## moneromooo-monero | 2019-10-18T13:28:06+00:00
This seems to change semantics for do_not_relay txes (I assume the change is meant for stem phase txes).

## moneromooo-monero | 2019-10-18T13:28:29+00:00
same

## moneromooo-monero | 2019-10-18T13:29:58+00:00
This should error noisily I think since the meta was found above, it's a fairly bad bug if this fails.

## moneromooo-monero | 2019-10-18T13:31:02+00:00
AFAICT, stem phase txes will be added to the block template, which seems wrong.

## moneromooo-monero | 2019-10-18T13:42:17+00:00
There is no need to make this diff hard to read. AFAICT this is just reindenting :/ If not, then it worked in making this hard to read.

## moneromooo-monero | 2019-10-18T13:46:10+00:00
The callers do not check, so this return change is a nop.

## moneromooo-monero | 2019-10-18T13:48:36+00:00
This is very confusing. I've been assuming it was stem phase txes so far.
Can you make it like local or something that actually says what it is ?

## moneromooo-monero | 2019-10-18T14:02:31+00:00
Why ? These two have no good reason AFAICT.

## moneromooo-monero | 2019-10-18T14:04:27+00:00
Those random indenting and slight parameter name changes take a few seconds each line to see what the difference is, and it's just wasting time.

## moneromooo-monero | 2019-10-18T14:05:39+00:00
The function did not change, so this hunk seems to be about temp stuff that got reverted.

## moneromooo-monero | 2019-10-18T21:36:27+00:00
Those values seem odd. Did you mean 1/2/4 (ie, bitflags) ? Technically ok though.

## moneromooo-monero | 2019-10-18T21:40:19+00:00
leftover ? If not, consider a MDEBUG/MERROR.

## moneromooo-monero | 2019-10-18T21:41:01+00:00
same

## vtnerd | 2019-10-18T23:19:52+00:00
Crap, don't know what I was thinking here. Banging  out the test code was not fun.

Will update to `1 << 0`, `1 << 2`, etc.

## vtnerd | 2019-10-18T23:20:49+00:00
Yeah meant to drop these. The core tests are kind of funky because they don't auto print errors like GTEST. I will switch to MERROR actually, because it may help in the future.

## vtnerd | 2019-10-18T23:22:57+00:00
So `anonymity` != `stem`, which might be part of the confusion. The next PR in progress now, adds `stem` and `fluff`.

`anonymity` means the tx originated at the node, and that node had i2p/tor enabled. It has to be different than `stem`, because txes received over clearnet `stem` need to be relayed the same way (or fluffed) but not relayed over i2p/tor.

## vtnerd | 2019-10-22T14:34:53+00:00
I've become some kind of haskell purist. Out parameters, especially an enum out parameter, makes me squeamish. I had `boost::optional<relay_method>` at one point, but this seemed slightly better. I didn't think think it was a big deal because in C/C++ you have to handle the `default` case anyway since the value can be munged.

I'll revert to out parameter if you think its better from a consistency perspective on the enum values, but its not really going to simplify any of the calls. Most (all?) of the existing calls have to be more complicated to check the out parameter in some way. There a few cases where a `switch (...)` will become a multi-line setup too.


## vtnerd | 2019-10-22T14:36:33+00:00
I was trying to make the names consistent across the codebase but guess I am reverting.

## moneromooo-monero | 2019-10-22T14:39:39+00:00
I was thinking of all the changes to "if (txpool_has_tx)" calls. If you pass a relay_method*=NULL, none change, and you can get your data if you need it. It just feels wrong to me, but if others prefer it too, I guess it'd be ok (in that case, maybe renaming it get_txpool_something would be better to match the new return).

## vtnerd | 2019-10-22T14:39:46+00:00
Good point. This is a pain in the butt to handle. I need to add a separate "timer" somewhere for this in core. It should probably be randomized too. Rats.

## vtnerd | 2019-10-22T14:40:16+00:00
Reverted

## vtnerd | 2019-10-22T14:43:20+00:00
Given the 6 lines of code, I hardly thing this is obfuscated. But Ill do something different here.

## vtnerd | 2019-10-23T04:16:39+00:00
The enum records how the tx was sent out of the daemon last. So a tx **received** over i2p/tor is labeled with `flood` in this PR, and with dandelion++ added is marked with `flood` or `fluff` depending on the dandelion++ state.

The `anonymity` flag - should it change to `origin`? I thought you mentioned that somewhere but I don't see it in the comment stack.

## vtnerd | 2019-10-23T04:19:41+00:00
Found the comment I just posted to -

how about `origin` ?

## vtnerd | 2019-10-23T04:20:43+00:00
In almost all cases I changed the parameter list and in this case the name of the function before reverting. I failed to revert when looking at the diff against master.

## vtnerd | 2019-10-23T04:23:01+00:00
The else was removed with a `continue` placed above. So this block was shifted as a result. Perhaps the changes, and the `continue` in particular, wasn't obvious?

## vtnerd | 2019-10-23T05:59:03+00:00
Actually that was weird of me to do that way, now a loop that uses indexed arrays.

## vtnerd | 2019-10-23T06:05:55+00:00
I will add a comment here and above. Yes, the intent is to accept a duplicate key-image if the tx originated at the node or was received via stem. Once its received by any other method, it should switch to standard mode. If the tx is received twice over stem, it will be switched to "fluff" elsewhere. This check should pass until its fluffed.

## vtnerd | 2019-10-23T06:14:20+00:00
This is a `struct` with a `std::vector` and a `std::string` which should have an implicitly declared move constructor which moves both types.

## vtnerd | 2019-10-23T06:21:14+00:00
I couldn't come up with a better name btw. "Broadcast" is intended to mean "send to many participants" opposed to "limited set or none".

## moneromooo-monero | 2019-10-23T10:30:02+00:00
Understood. IIRC I suggested "local", since "Received by RPC, trying to send over i2p/tor".

## moneromooo-monero | 2019-10-23T10:33:51+00:00
If the tx originated at the node, you should not receive a duplicate key image unless the user tries to double spend, in which case you likely want to error out. It's the right thing to do for stem though, so these two cases need separating. I think broadcast is a good name. If you're looking for another, maybe public or unrestricted.

## moneromooo-monero | 2019-10-23T10:34:32+00:00
Oh for some reason I had been thinking it was a key_image. Nevermind.

## moneromooo-monero | 2019-10-23T10:36:48+00:00
I saw the functional change, but all the unnecessary diffs in this PR just make it a pain to review for no good reason. Like this unneeded shift. Takes time where none should be needed.

## moneromooo-monero | 2019-10-23T10:39:16+00:00
For the type ? Seems OK too. Though relay_method is also descriptive. For the particular enum, local seems to match well.

## vtnerd | 2019-11-09T02:03:13+00:00
I decided to make this change, it doesn't look too bad so far. Most of the `get_txpool_blob` stuff can now skip a LMDB lookup too.

## vtnerd | 2019-11-09T02:04:39+00:00
I had to think a bit to re-work this one. The `send_tx` function now has a `delay` boolean to toggle a randomized delay. Its a bit of a hack, but it was the easiest to implement. The original leak would only occur if a public and "private" tx relay timer expired in the same 1 second interval.

## vtnerd | 2019-11-09T02:05:01+00:00
Removed.

## vtnerd | 2019-11-09T02:05:10+00:00
Changed to `local`.

## vtnerd | 2019-11-09T02:05:26+00:00
Moved.

## vtnerd | 2019-11-09T02:07:11+00:00
My updated core tests caught a bug here. The expression is now:

```c++
if (m_mempool.have_tx(tx_hash, std::addressof(existing_tx_relay)) && matches_category(existing_tx_relay, relay_category::broadcasted))
```

A new transaction should only be filtered out if its supposed to be publicly visible. Previously it was filtering if the same relay method was used, which is incorrect for stem transactions (not-yet-seen-in-this-PR) which must appear as newly received.

## vtnerd | 2019-11-09T02:10:28+00:00
When writing the comments I realized that there were bugs in both portions. The boolean logic was inverted what it needed to be. The core tests I wrote didn't catch this because of a second bug in the mempool `add_tx` function. Yikes.

Please re-review this, and all changes in tx_pool.

## vtnerd | 2019-11-09T02:11:40+00:00
A tab snuck in there. Now fixed.

## vtnerd | 2019-11-09T02:14:07+00:00
Added an error log statement.

## vtnerd | 2019-11-09T02:21:52+00:00
Also updated this.

## vtnerd | 2019-11-09T03:37:00+00:00
I'm not sure why I flipped these retrievals. I think I altered how `relay_method` was handled by metadata or something after the first change. Flipped back.

## vtnerd | 2019-11-09T03:42:26+00:00
This behavior change was intentional, although I'm not sure its correct. This function is only used by the ZMQ rpc code to return whether a list of key-images are spent. This is now hiding `do_not_relay` txes from the ZMQ rpc.

Is the ZMQ rpc considered `!restricted`? I think it is actually ... ?

## vtnerd | 2019-11-09T03:43:59+00:00
I'm wondering "out loud" here because it does seem funky to "hide" a key-image from a ZMQ rpc call that could've just provided it. OTOH, it doesn't seem natural to respond to `do_not_relay` txes but not stem, etc.

## vtnerd | 2019-11-09T04:08:26+00:00
You are/were correct, but I'm not sure why the comment was placed here. Perhaps because this check passes and then the tx is added to `m_txs_by_fee_and_receive_time`? This was another bug :/

My current approach is to add to `m_txs_by_fee_and_receive_time` for consistency, and then filter out on in the block_template function since the metadata is already being retrieved.  The primary purpose of this member variable is for block templates, so it could probably be omitted entirely (although that would cause spurious messages about it missing to come up).

## vtnerd | 2019-11-09T04:11:30+00:00
This is an internal (to `tx_pool`) function but I am still mulling over the expected behavior. `do_not_relay` wasn't thought of being private, yet I wouldn't expect setting `do_not_relay` to have _less_ privacy than if I had left that field false. That's basically whats driving my thoughts on this comment and on the one about about ZMQ. 

## vtnerd | 2019-11-09T04:34:53+00:00
Crap, I'm thinking reverting to existing `do_not_relay` behavior is the best option (at least here). Maintaining privacy with that flag means broadcasting a mined block over Tor. Will have to re-think some of these checks, yet again.

## moneromooo-monero | 2019-11-11T15:26:12+00:00
I think it's unrestricted in practice since only the first RPC code uses that variable. You may choose to add a restricted mode later or not if it make sense.

## vtnerd | 2019-11-16T04:31:29+00:00
I scrapped everything I worked on, because it was pointless after Dandelion++ is added. So I decided to [PR diffusion / Dandelion++ fluff](https://github.com/monero-project/monero/pull/6146) immediately to fix this issue. This section of code will be updated with comments.

## vtnerd | 2019-11-24T05:52:31+00:00
So the actual change was a bit different - the return value has been reverted to `bool` and the `relay_method::not_found` value has been too. Instead, this takes a `relay_category` argument which filters the return value (i.e. it is attempting to filter based on "split" mempool).

## vtnerd | 2019-11-24T05:53:13+00:00
The same change was made here - a `relay_category` filter is now a required argument. The overload that returned a `cryptonote::blobdata` was brought back too.

## vtnerd | 2019-11-24T05:57:01+00:00
This is now `if (m_mempool.have_tx(tx_hash, relay_category::legacy))`. See other comment about `legacy` category.

## vtnerd | 2019-11-24T06:01:25+00:00
So this currently uses `relay_category::broadcasted` still, which will now filter out `do_not_relay` transactions. This is for the ZMQ interface only so impact should be minimal.

## vtnerd | 2019-11-24T06:03:06+00:00
This now uses `relay_category::legacy`. This category returns false for `local` and `stem` transactions but still returns true for `do_not_relay`. I couldn't think of a better name. Hiding `do_not_relay` txes are funky because peers will notice them in a block which is not notified in a private manner.

## vtnerd | 2019-11-24T06:04:17+00:00
This no longer applies because the filtering logic made it possible to keep everything in a single `if` statement.

## vtnerd | 2019-11-24T06:04:55+00:00
I left this as-is for now - should the callers check? Is there harm in changing this now?

## moneromooo-monero | 2019-11-24T16:26:14+00:00
None, I'm fine if you leave it this way.

## moneromooo-monero | 2019-11-26T13:49:33+00:00
Pedantic, but could be a & (or are you concerned about alignment ?)

## moneromooo-monero | 2019-11-26T13:56:14+00:00
This change (and the lack of change to the call in remove_stuck_transactions) mean that an unbroadcast tx that fails to mine will stay in the txpool forever. That might arguable be intended. Was it ? Same for validate and init in the same file.

## moneromooo-monero | 2019-11-26T14:02:10+00:00
Could have had a const bool kepy_by_block = tx_relay == relay_method::block above to save all the diffs below.

## moneromooo-monero | 2019-11-26T14:03:44+00:00
Why was this reordered ? The ordering means that key images get inserted only if the DB write succeeds. Otherwise, you need to remove key images on DB commit failure since hte key images are in memory, not the db txn.

## moneromooo-monero | 2019-11-26T14:06:52+00:00
Leftover from the merging of error code and method ?

## moneromooo-monero | 2019-11-26T14:07:55+00:00
That switch looks a lot more complex that the if that there before, and does not do anything else once the return gets reverted.

## moneromooo-monero | 2019-11-26T14:09:38+00:00
I see now why the reordering above. I guess the reordering should be undone, and a "is_conflicting" function added that does those checks without adding.

## moneromooo-monero | 2019-11-26T14:12:26+00:00
What is the semantic difference between "unrelayed" and "sensitive" here ? You might already have answered that somewhere, sorry :)

## moneromooo-monero | 2019-11-26T14:13:28+00:00
This tx_infos reserve call likely wants the same include_sensitive_data/unrelated flag as key_image_infos.

## moneromooo-monero | 2019-11-26T14:21:31+00:00
That makes things less clear.

## vtnerd | 2019-11-27T05:04:42+00:00
That error message is cruft but the return was intended. I suppose in this context it is irrelevant - I'll switch back to an `if`.

## vtnerd | 2019-11-27T05:08:58+00:00
This call changes the status of the tx in-place. A stem tx is converted to a fluff tx, etc. This also updates the timestamp (verified in new core tests) so that information isn't leaked either. Everything is replaced.

This had to be after `insert_key_images` because that call is doing a DB lookup to find current tx status. The core tests now go crazy if this is inverted.

## vtnerd | 2019-11-27T05:11:43+00:00
I'm not sure if I understand correctly - the DB is **not** throwing an exception when a tx is already in the pool, it is replacing it. The code currently in the DB uses a flag which is ignored for the properties on that `dbi` handle. Its something I intended to PR at some point to clarify the code a bit.

## vtnerd | 2019-11-27T05:13:51+00:00
`include_sensitive` now includes `do_not_relay`, `stem`, and any `local` tx  that originated at that node. The latter two are relayed immediately but still considered sensitive for the RPC calls. So this was a bit of bikeshedding and slight behavior change, but this is a source of potential leaks I think.

## vtnerd | 2019-11-27T05:15:14+00:00
Yikes, missed the one above. I would've "cached" it on the stack had I noticed that ... updating.

## vtnerd | 2019-11-27T05:17:45+00:00
Ok, will do.

## moneromooo-monero | 2019-11-27T15:22:28+00:00
The DB code can throw for various other things though. Anyway, I don't mind the reordering if it's needed and the original requirement of keeping the db and the in-memory key image map is maintained.

## vtnerd | 2019-11-28T16:25:09+00:00
This was a missed keystroke, fixed.

## vtnerd | 2019-11-28T16:26:24+00:00
Failed to revert this, now it should be reverted properly.

## vtnerd | 2019-11-28T16:48:50+00:00
Yes, I think this was a mistake. `remove_stuck_transactions`, `validate`, and `init` all now use `relay_category::all`. I also tried to re-review all changes to default behavior, some other thoughts:

There are some RPCs that no longer return information from txes in `do_not_relay` status where they did previously. I am not sure if this is the best change, but its also not obvious why some RPC calls have a `!request_has_rpc_origin || !restricted` check and others do not. So, those with the check will return **all** information (if the checks pass), whereas the other RPC calls will always filter out information.

## moneromooo-monero | 2019-11-28T18:08:44+00:00
Are the ones without the check restricted calls ? Those are caught in the header file in the name-to-function map.

## moneromooo-monero | 2019-12-02T19:17:26+00:00
Leftover conflict marker

## vtnerd | 2019-12-03T04:30:04+00:00
What diff is this from? It does not appear in the most up-to-date version.

# Action History
- Created by: vtnerd | 2019-10-07T14:10:57+00:00
- Merged at: 2019-12-12T19:33:24+00:00
