---
title: Reserve proofs
source_url: https://github.com/monero-project/monero-gui/pull/1508
author: cryptochangements34
assignees: []
labels:
- invalid
created_at: '2018-07-13T22:06:06+00:00'
updated_at: '2019-02-01T14:54:35+00:00'
type: pull_request
status: closed
closed_at: '2019-02-01T14:54:35+00:00'
merged_at: null
---

# Original Description
Add a new tab under "advanced" called "Reserve proofs" where the user can create and verify reserve proofs which prove that a user owns at least X amount of Monero

# Discussion History
## stoffu | 2018-07-18T00:19:03+00:00
Shouldn't this be "W"?

## stoffu | 2018-07-18T00:25:40+00:00
Typo: `"ReserveProof"`

## stoffu | 2018-07-18T00:29:50+00:00
I'd suggest these changes as well:
```diff
--- a/main.qml
+++ b/main.qml
@@ -130,7 +130,8 @@ ApplicationWindow {
             else if(middlePanel.state === "Receive") middlePanel.state = "History"
             else if(middlePanel.state === "History") middlePanel.state = "Mining"
             else if(middlePanel.state === "Mining") middlePanel.state = "TxKey"
-            else if(middlePanel.state === "TxKey") middlePanel.state = "SharedRingDB"
+            else if(middlePanel.state === "TxKey") middlePanel.state = "ReserveProof"
+            else if(middlePanel.state === "ReserveProof") middlePanel.state = "SharedRingDB"
             else if(middlePanel.state === "SharedRingDB") middlePanel.state = "Sign"
             else if(middlePanel.state === "Sign") middlePanel.state = "Settings"
         } else if(seq === "Ctrl+Shift+Backtab" || seq === "Alt+Shift+Backtab") {
@@ -148,7 +149,8 @@ ApplicationWindow {
             */
             if(middlePanel.state === "Settings") middlePanel.state = "Sign"
             else if(middlePanel.state === "Sign") middlePanel.state = "SharedRingDB"
-            else if(middlePanel.state === "SharedRingDB") middlePanel.state = "TxKey"
+            else if(middlePanel.state === "SharedRingDB") middlePanel.state = "ReserveProof"
+            else if(middlePanel.state === "ReserveProof") middlePanel.state = "TxKey"
             else if(middlePanel.state === "TxKey") middlePanel.state = "Mining"
             else if(middlePanel.state === "Mining") middlePanel.state = "History"
             else if(middlePanel.state === "History") middlePanel.state = "Receive"
```


## stoffu | 2018-07-18T00:33:35+00:00
Typo: "that you own ..."

## stoffu | 2018-07-18T00:41:18+00:00
The amount needs to be parsed to account for the decimal points:
```diff
-                  informationPopup.text  = appWindow.currentWallet.getReserveProof(amountLine.text, getMessageLine.text);
+                  informationPopup.text  = appWindow.currentWallet.getReserveProof(walletManager.amountFromString(amountLine.text), getMessageLine.text);
```


## stoffu | 2018-07-18T00:56:56+00:00
Two-space indentation here as opposed to four-space indentation elsewhere

## stoffu | 2018-07-18T01:05:18+00:00
Two-space indentation

## stoffu | 2018-07-18T01:07:04+00:00
The proof can optionally be generated for the entire balance. I'd suggest a patch below to do this:
```diff
--- a/pages/ReserveProof.qml
+++ b/pages/ReserveProof.qml
@@ -88,6 +88,8 @@ Rectangle {
                     readOnly: false
                     width: mainLayout.editWidth
                     Layout.fillWidth: true
+                    inlineButtonText: qsTr("All") + translationManager.emptyString
+                    inlineButton.onClicked: amountLine.text = "(all)"
                     validator: DoubleValidator {
                         bottom: 0.0
                         top: 18446744.073709551615
@@ -115,11 +117,12 @@ Rectangle {
                 anchors.topMargin: 17 * scaleRatio
                 width: 60 * scaleRatio
                 text: qsTr("Generate") + translationManager.emptyString
-                enabled: amountLine.text > 0
+                enabled: amountLine.text === "(all)" || amountLine.text > 0
                 onClicked: {
                   informationPopup.title  = qsTr("Reserve proof") + translationManager.emptyString;
                   console.log("message: " + getMessageLine.text);
-                  informationPopup.text  = appWindow.currentWallet.getReserveProof(amountLine.text, getMessageLine.text);
+                  var all = amountLine.text === "(all)";
+                  informationPopup.text  = appWindow.currentWallet.getReserveProof(all, all ? 0 : walletManager.amountFromString(amountLine.text), getMessageLine.text);
                   informationPopup.onCloseCallback = null
                   informationPopup.open()
                 }
--- a/src/libwalletqt/Wallet.cpp
+++ b/src/libwalletqt/Wallet.cpp
@@ -605,19 +605,27 @@ Q_INVOKABLE QString Wallet::checkSpendProof(const QString &txid, const QString &
     return QString::fromStdString(result);
 }
 
-QString Wallet::getReserveProof(quint64 amount, const QString &message)
+QString Wallet::getReserveProof(bool all, quint64 amount, const QString &message)
 {
-    std::string proof = m_walletImpl->getReserveProof(false, 0, amount, message.toStdString());
+    std::string proof = m_walletImpl->getReserveProof(all, 0, amount, message.toStdString());
     return QString::fromStdString(proof);
 }
--- a/src/libwalletqt/Wallet.h
+++ b/src/libwalletqt/Wallet.h
@@ -258,8 +258,8 @@ public:
     Q_INVOKABLE QString checkTxProof(const QString &txid, const QString &address, const QString &message, const QString &signature);
     Q_INVOKABLE QString getSpendProof(const QString &txid, const QString &message) const;
     Q_INVOKABLE QString checkSpendProof(const QString &txid, const QString &message, const QString &signature) const;
-    Q_INVOKABLE QString getReserveProof(quint64 amount, const QString &message);
+    Q_INVOKABLE QString getReserveProof(bool all, quint64 amount, const QString &message);
     // Rescan spent outputs
     Q_INVOKABLE bool rescanSpent();
```


## stoffu | 2018-07-18T01:12:21+00:00
The result should display the total proved amount and its spent amount.
```diff
--- a/pages/ReserveProof.qml
+++ b/pages/ReserveProof.qml
@@ -192,12 +195,17 @@ Rectangle {
                 enabled: TxUtils.checkAddress(addressLine.text, appWindow.persistentSettings.nettype)
                 onClicked: {
                   informationPopup.title  = qsTr("Proof Result") + translationManager.emptyString;
-                  var result;
-                  if(appWindow.currentWallet.checkReserveProof(addressLine.text, proveMessageLine.text, signatureLine.text))
-                      result = "True Proof";
-                  else
-                      result = "False Proof";
-                  informationPopup.text  = result
+                  var result = appWindow.currentWallet.checkReserveProof(addressLine.text, proveMessageLine.text, signatureLine.text);
+                  var results = result.split("|");
+                  if (results[0] === "true") {
+                      if (results[1] === "true") {
+                          informationPopup.text = "Good signature: total " + walletManager.displayAmount(results[2]) + ", spent " + walletManager.displayAmount(results[3]);
+                      } else {
+                          informationPopup.text = "Bad signature";
+                      }
+                  } else {
+                      informationPopup.text = "Error: " + results[1];
+                  }
                   informationPopup.onCloseCallback = null
                   informationPopup.open();
                 }
--- a/src/libwalletqt/Wallet.cpp
+++ b/src/libwalletqt/Wallet.cpp
-bool Wallet::checkReserveProof(const QString &address, const QString &message, const QString &signature)
+QString Wallet::checkReserveProof(const QString &address, const QString &message, const QString &signature)
 {
     bool good;
     uint64_t total;
     uint64_t spent;
-    m_walletImpl->checkReserveProof(address.toStdString(), message.toStdString(), signature.toStdString(), good, total, spent);
-    return good;
+    bool success = m_walletImpl->checkReserveProof(address.toStdString(), message.toStdString(), signature.toStdString(), good, total, spent);
+    std::string result = std::string(success ? "true" : "false") + "|";
+    if (success) {
+        result += good ? "true" : "false";
+        if (good)
+            result += "|" + std::to_string(total) + "|" + std::to_string(spent);
+    } else {
+        result += m_walletImpl->errorString();
+    }
+    return QString::fromStdString(result);
 }
--- a/src/libwalletqt/Wallet.h
+++ b/src/libwalletqt/Wallet.h
@@ -258,8 +258,8 @@ public:
     Q_INVOKABLE QString checkTxProof(const QString &txid, const QString &address, const QString &message, const QString &signature);
     Q_INVOKABLE QString getSpendProof(const QString &txid, const QString &message) const;
     Q_INVOKABLE QString checkSpendProof(const QString &txid, const QString &message, const QString &signature) const;
-    Q_INVOKABLE bool checkReserveProof(const QString &address, const QString &message, const QString &signature);
+    Q_INVOKABLE QString checkReserveProof(const QString &address, const QString &message, const QString &signature);
     // Rescan spent outputs
     Q_INVOKABLE bool rescanSpent();
```


## cryptochangements34 | 2018-07-18T11:51:22+00:00
Yes, thanks

## sanderfoobar | 2019-02-01T14:46:24+00:00
Fixed in #1927

## sanderfoobar | 2019-02-01T14:46:27+00:00
Fixed in #1927

## sanderfoobar | 2019-02-01T14:46:30+00:00
Fixed in #1927

## sanderfoobar | 2019-02-01T14:46:34+00:00
Fixed in #1927

## sanderfoobar | 2019-02-01T14:46:37+00:00
Fixed in #1927

## sanderfoobar | 2019-02-01T14:46:40+00:00
Fixed in #1927

## sanderfoobar | 2019-02-01T14:46:44+00:00
Fixed in #1927

## sanderfoobar | 2019-02-01T14:46:47+00:00
Fixed in #1927

# Action History
- Created by: cryptochangements34 | 2018-07-13T22:06:06+00:00
- Closed at: 2019-02-01T14:54:35+00:00
