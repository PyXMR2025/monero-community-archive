---
title: Destroy Rust FFI types correctly
source_url: https://github.com/seraphis-migration/monero/pull/39
author: j-berman
assignees: []
labels: []
created_at: '2025-05-03T01:27:23+00:00'
updated_at: '2025-06-23T07:40:34+00:00'
type: pull_request
status: merged
closed_at: '2025-06-23T07:40:34+00:00'
merged_at: '2025-06-23T07:40:33+00:00'
---

# Original Description
From Rust docs [`into_raw`](https://doc.rust-lang.org/std/boxed/struct.Box.html#method.into_raw):

> After calling this function, the caller is responsible for the memory previously managed by the Box. In particular, the caller should properly destroy T and release the memory, taking into account the [memory layout](https://doc.rust-lang.org/std/boxed/index.html#memory-layout) used by Box. The easiest way to do this is to convert the raw pointer back into a Box with the [Box::from_raw](https://doc.rust-lang.org/std/boxed/struct.Box.html#method.from_raw) function, allowing the Box destructor to perform the cleanup.

As such, I don't believe calling `free()` on the C side is guaranteed to properly release the memory previously managed by the Box. So this PR's solution is to free the raw pointer by sending the pointer back over the FFI and destroying via Rust's [`from_raw`](https://doc.rust-lang.org/std/boxed/struct.Box.html#method.from_raw).

This PR also brings stronger type protection on the C++ side, since it wouldn't just be `const uint8_t *` for tons of different types.

Would be good to get the crustaceans to take a look at this too probably.

# Discussion History
## jeffro256 | 2025-06-04T22:58:35+00:00
I prefer `HeliosBranchBlindNative`, but that's a purely personal preference. 

## jeffro256 | 2025-06-04T23:00:38+00:00
I would prefer a term like `generate_helios_branch_blind`, which is a bit more descriptive than "new", but again, personal choice. 

## jeffro256 | 2025-06-04T23:04:11+00:00
I would rename this to `destroy_helios_branch_blind` to signify that's it's not just freeing memory, but performing actions on the Rust side to cleanup this object. Basically mirroring the difference between "free" and "delete" in C++. Out of the last 3, this is the naming change I feel most strongly about.

## jeffro256 | 2025-06-04T23:35:33+00:00
Personally, I might drop the `new_fn` since for some types it doesn't really make sense to have a default value. What is the "default tree root"? What is the default outputs blind? 

## jeffro256 | 2025-06-04T23:40:45+00:00
I'm not quite sure I like the `ffi` namespace here, it's sort of backwards. Ditto for the name of the header file. The types defined here are supposed to be user-facing, hence why we put effort into making them memory-safe. The actual types used in the Rust FFI aren't in *any* namespace (since it's a C header). The user of the library doesn't really care that the backend of the library is Rust, so long as they don't cause memory leaks and other UB. Idk, just feels backwards to me. 

## jeffro256 | 2025-06-04T23:43:44+00:00
This doesn't break the strict aliasing rule since there's an exception for accessing any objects though `char` and `unsigned char` pointers.

## jeffro256 | 2025-06-04T23:45:33+00:00
This is also why I argued earlier for the Rust FFI functions returning a `NULL` pointer on an error, instead of returning an int and setting the pointer by reference. Makes passing it to a `std::unique_ptr` more ergonomic. We could use this [errno crate](https://docs.rs/errno/latest/errno/) for setting `errno`, just like a real C stdlib function, if passing error codes is useful for recovering for that specific function. 

## j-berman | 2025-06-05T00:31:51+00:00
I think for other functions, like `path_new` for example (or whatever it ends up being called), we can definitely expect multiple error cases (failed point decompression, output idx too high). I don't love the idea of adding another dependency to handle something that could be handled pretty simply in imo a clear functional way. And so considering that, we'd expect a separate "constructor" fn approach somewhere. And as long as we do expect it, why not generalize the approach in this way to smoothly support more error cases for functions like this in the future?

I hear you on there not being a default in those cases, but does that matter? I'll just do as you propose if you feel strongly on it.

## j-berman | 2025-06-05T00:57:19+00:00
Removed the `ffi` namespace and moved to `fcmp_pp_types.{h,cpp}` in the latest. I separated the implementation so `fcmp_pp_types.h` can be included in another file without needing needing to link the impl if it's not needed.

## j-berman | 2025-06-05T01:00:01+00:00
Kept this one as `Unsafe` because I think it's a more useful deterrent against using it directly on the C++ side, changed the others

## j-berman | 2025-06-05T02:59:34+00:00
The latest commit ([4636f99](https://github.com/seraphis-migration/monero/pull/39/commits/4636f997cbf8483ab35bc31459f33f04e861b0da)) removes all usage of uint8_t* for Helios branch blinds. Have a feeling you'll have some thoughts on that approach too. Will stop here before proceeding.

## jeffro256 | 2025-06-05T13:06:24+00:00
Yeah looks good!

## jeffro256 | 2025-06-07T05:28:59+00:00
The `unsafe` block isn't needed here b/c the function is marked `unsafe`

## jeffro256 | 2025-06-07T06:10:49+00:00
Same here

## jeffro256 | 2025-06-07T06:11:04+00:00
This function should be marked `unsafe`

## jeffro256 | 2025-06-07T06:15:47+00:00
Same `unsafe` comment here

## jeffro256 | 2025-06-07T06:16:00+00:00
And here 

## jeffro256 | 2025-06-07T06:22:47+00:00
```suggestion
    using raw_t##Shared = std::shared_ptr<raw_t##Unsafe>;            \
```

I'd like the `shared_ptr` name to be differentiable from the `unique_ptr` name. Also allows us to have unique *and* shared variants for the same raw pointer type. 

## jeffro256 | 2025-06-07T06:24:43+00:00
I personally don't see the point in putting these functions in the macro. It makes the macros harder to read, it makes the function definitions less clear, and it really isn't any less writing. 

## jeffro256 | 2025-06-07T06:25:18+00:00
nice

## jeffro256 | 2025-06-07T06:27:50+00:00
Just this line should be reverted back to `const wallet2_basic::transfer_details &td,` 

## jeffro256 | 2025-06-07T06:34:33+00:00
Could these take `epee::span<const SeleneBranchBlind>` and `epee::span<const HeliosBranchBlind>` instead too? They could just as easily to converted to `SeleneBranchBlindSliceUnsafe`, but would avoid extra copies/movies in `tx_builder.cpp`. 

## jeffro256 | 2025-06-07T06:41:33+00:00
Now that the SA/L specific stuff is removed, should we call this just `FcmpProveInputUnsafe`? 

## jeffro256 | 2025-06-07T06:42:21+00:00
Same comment here

## j-berman | 2025-06-07T07:25:26+00:00
Good catch

## j-berman | 2025-06-16T23:03:18+00:00
It's just moving unique pointers in `tx_builder.cpp` / I don't see a way to improve this approach with `epee::span`. I'm open to it though, can you show the code you'd change in `tx_builder.cpp`?

## j-berman | 2025-06-16T23:05:00+00:00
Renamed to `FcmpPpProveMembershipInput...` to keep it tied to FCMP++

## jeffro256 | 2025-06-23T07:39:50+00:00
Maybe I'll do it in a future PR, not super important. 

# Action History
- Created by: j-berman | 2025-05-03T01:27:23+00:00
- Merged at: 2025-06-23T07:40:33+00:00
