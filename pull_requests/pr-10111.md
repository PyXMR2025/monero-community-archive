---
title: '[DO NOT MERGE] crypto: fast `fe_batch_invert` using Montgomery''s trick'
source_url: https://github.com/monero-project/monero/pull/10111
author: j-berman
assignees: []
labels:
- cryptography
- audit pending
created_at: '2025-09-26T02:34:59+00:00'
updated_at: '2026-05-01T20:49:41+00:00'
type: pull_request
status: open
closed_at: null
merged_at: null
---

# Original Description
Idea by @kayabaNerve to speed up field element inversions when we need to do multiple inversions at one time. This is a significant perf optimization used in FCMP++ tree building.

The perf test shows that batch inverting 1000 elements is ~98% faster than inverting each one individually.

# Discussion History
## vtnerd | 2025-09-27T15:58:25+00:00
Should the function be labeled `vartime` due to this branch?

## vtnerd | 2025-09-27T16:01:08+00:00
Check for `nullptr`? Could easily use `std::vector` ...?

## vtnerd | 2025-09-27T16:01:35+00:00
Same here.

## vtnerd | 2025-09-27T16:02:11+00:00
`throw std::bad_alloc` is probably more appropriate, as this has to allocate memory for the `std::string` after just failing to `malloc`.

Again, not sure why `std::vector` isn't used instead.

## kayabaNerve | 2025-09-27T16:22:30+00:00
Time variable to the width, which generally isn't a secret, shouldn't be an issue. You'd have to argue that the amount of inputs/amounts is a secret and should be constant-time to them accordingly (or whatever defines the width here).

## jeffro256 | 2025-09-27T16:48:46+00:00
It's worth noting that in-memory representations of `fe` might not be equal even if the `fe_tobyte()` canonical representations would be the same. They happen to be in this case, and it might be worth keeping this test as-is? But it's conceivable that some correct change to e.g. `fe_mul()` down the line might cause this line to fail. I would recommend keeping this line, but documenting it in case it fails later. 

## kayabaNerve | 2025-09-27T17:04:58+00:00
ref10 in general doesn't guarantee they're reduced. That caused annoyances with the FCMP++ code...

(Adding on to what you said, as you identified and raised)

## j-berman | 2025-09-27T17:13:36+00:00
Used a vector instead of a pointer in the latest. I think I did that originally and then changed it, I don't remember why. Good spot

## j-berman | 2025-09-27T17:14:12+00:00
Added a warning in the latest, also linking to an `fe_reduce` implementation that could be used to update the test

## jeffro256 | 2025-09-27T18:29:13+00:00
```suggestion
    if constexpr (batched)
```

For good measure, to help compiler optimize the branch out, and to signal to reader that that is what it's doing

## j-berman | 2025-09-28T17:07:53+00:00
CI failure reminded me why I changed it from using a vector...

```
/Applications/Xcode_16.4.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX.sdk/usr/include/c++/v1/__memory/allocator.h:168:84: error: object expression of non-scalar type 'int[10]' cannot be used in a pseudo-destructor expression
  168 |   _LIBCPP_DEPRECATED_IN_CXX17 _LIBCPP_HIDE_FROM_ABI void destroy(pointer __p) { __p->~_Tp(); }
```

I figured using a raw pointer was the simplest way to deal with this. Do you see a better way?

## j-berman | 2025-09-28T17:09:20+00:00
I use this pattern in FCMP++ integration code, not just in tests here, so it would definitely be good to improve if you see a better way

## j-berman | 2025-09-29T16:17:50+00:00
Reverted to using a raw pointer in the latest. Open to a better approach

## vtnerd | 2025-09-29T16:42:08+00:00
Oops.

I think the only way is to wrap in another object. 

## vtnerd | 2025-09-29T16:43:22+00:00
`std::unique_ptr` mostly works here, but you have to match it with the correct allocation call. (`new []` array).

## vtnerd | 2025-09-29T17:32:32+00:00
>I use this pattern in FCMP++ integration code, not just in tests here, so it would definitely be good to improve if you see a better way.

Hopefully the code doesn't leak on exceptions (like these tests kind of do).

## j-berman | 2025-09-29T17:36:02+00:00
How does the latest look? I'd update the integration code with that pattern too (it does currently leak on exceptions)

## j-berman | 2025-10-01T00:07:46+00:00
I implemented an `fe_equals` over here: https://github.com/j-berman/monero/commit/2a56788b8be63b9593a43574cd425b01dec72df1

Will make a follow-up PR to this one

## vtnerd | 2025-11-26T20:33:55+00:00
I think this line right here prevents aliasing on in+out. Should a runtime check be added? Or at least an assert? I recall the other crypto functions being resilient against aliasing.

## jeffro256 | 2025-11-26T20:46:51+00:00
This is a good point.

## jeffro256 | 2025-11-26T20:47:38+00:00
I would recommend documenting it in a comment in the header file, plus adding an `assert()` statement that the memory spaces aren't overlapping. 

## jeffro256 | 2025-11-26T21:07:35+00:00
Another note: this only tests field elements mod $l$, not mod $q$. Since $l < q$, this should always be complete, but you're missing out on testing field elements in range $[l, q)$. 

## jeffro256 | 2025-11-26T21:36:01+00:00
You could randomly generate 32 bytes and clear the top bit. The probability that you generate an integer greater than or equal to $q$ is $19 / 2^{255}$, which is negligible if our random number generator is working correctly. 

## jeffro256 | 2025-11-26T21:37:53+00:00
Oh nevermind actually, you're using the compressed Y value of the point, not the secret key to that point. 

## jeffro256 | 2025-12-10T20:50:09+00:00
You could apply this diff:

```diff
diff --git a/src/crypto/crypto-ops.c b/src/crypto/crypto-ops.c
index ccb59de3f..5c003fef3 100644
--- a/src/crypto/crypto-ops.c
+++ b/src/crypto/crypto-ops.c
@@ -320,6 +320,17 @@ int fe_batch_invert(fe *out, const fe *in, const int n) {
     return 0;
   }
 
+  assert(out);
+  assert(in);
+#ifndef NDEBUG
+  {
+    // Overlap of `out` and `in` sections not allowed
+    const fe *pmin = out < in ? out : in;
+    const fe *pmax = out > in ? out : in;
+    assert(pmin + n < pmax);
+  }
+#endif
+
   // Step 1: collect initial muls
   fe_copy(out[0], in[0]);
   for (int i = 1; i < n; ++i) {
diff --git a/src/crypto/crypto-ops.h b/src/crypto/crypto-ops.h
index 6488fd99d..797637c33 100644
--- a/src/crypto/crypto-ops.h
+++ b/src/crypto/crypto-ops.h
@@ -164,6 +164,13 @@ void ge_sub(ge_p1p1 *r, const ge_p3 *p, const ge_cached *q);
 void fe_add(fe h, const fe f, const fe g);
 void fe_tobytes(unsigned char *, const fe);
 void fe_invert(fe out, const fe z);
+/**
+@brief: out[i] = 1/in[i] for i in [0, n). Uses Montgomery's trick
+@return: 0 on success, some other value otherwise
+
+Unlike other crypto functions, `out` and `in` memory sections CANNOT be aliased.
+If `out` and `in` overlap, it will cause undefined output.
+**/
 int fe_batch_invert(fe *out, const fe *in, const int n);
 void fe_mul(fe out, const fe, const fe);
 void fe_0(fe h);

```

## jeffro256 | 2025-12-10T20:50:49+00:00
Returns an `int` even though only value possibly returned is `0`.

## j-berman | 2025-12-15T20:23:21+00:00
Applied that diff in the latest

## j-berman | 2026-02-18T00:08:22+00:00
I went ahead and implemented `fe_equals` in this PR

# Action History
- Created by: j-berman | 2025-09-26T02:34:59+00:00
