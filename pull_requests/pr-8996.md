---
title: Add SSL support to P2P
source_url: https://github.com/monero-project/monero/pull/8996
author: vtnerd
assignees: []
labels:
- pending review
- daemon
created_at: '2023-09-19T23:24:27+00:00'
updated_at: '2026-08-21T19:00:31+00:00'
type: pull_request
status: open
closed_at: null
merged_at: null
---

# Original Description
First, my apologies to @moneromooo-monero for NACKing his/her SSL proposal years ago, only for me to bring it back.

--------------------------------------

This does exactly as the title suggests, with options to disable P2P encryption entirely, and an option to re-use a SSL certificate between "runs". The default generates a new SSL certificate each `monerod` run, so that the node cannot be tracked across IP address changes.

Nodes _do not_ trust encryption information from peers. Instead, every peer is assumed to be in autodetect mode, unless overridden on the CLI or via handshake/ping messages directly from the peer.

**Possibly bad**:
If a node chooses to re-use SSL certificates, a change in certificates will cause connection failures until the node is removed from the white+gray lists OR the node makes a direct connection and provides the new SSL certificate.

-------------------------------

EDIT:
I will also attempt some unit test changes; there is enough to review here that I expect this diff to be up a while.

# Discussion History
## vtnerd | 2023-11-16T17:32:19+00:00
This is a bug, as `ssl_support_t` doesn't strictly define enum values.

## j-berman | 2024-04-18T19:39:34+00:00
It looks as though if monerod generates a new temporary cert every run, that the node won't share the fingerprint with its peers. Therefore this should only affect nodes that have a persistent cert and then change it, ya?

## j-berman | 2024-04-18T19:48:35+00:00
Nits:

```suggestion
    const command_line::arg_descriptor<std::vector<std::string> > arg_p2p_add_peer   = {"add-peer", "Manually add peer to local peerlist. Append \",<sha-256 fingerprint>\" to make authenticated connection. " 
```

Same for other references to the fingerprint

## j-berman | 2024-04-18T22:59:22+00:00
nit: the whitespace has tabs here and doesn't line up in my editor

```suggestion
                               m_ssl(ssl_support_t::e_ssl_support_disabled),
```

## j-berman | 2024-04-19T00:23:09+00:00
This is unused

## j-berman | 2025-10-23T22:06:16+00:00
```suggestion
`encryption_mode = 2` AND `ssl_finger = <binary sha256 of cert>` in the
```

## j-berman | 2025-10-23T23:06:48+00:00
Free with `X509_free` when done?

## j-berman | 2025-10-24T00:27:53+00:00
Thoughts on using `new_connection_l->client_handshake` here? That handshake function handles waiting, cleaning up on failure, synchronization. Reusing that code seems like one less thing to need to worry about here

OTOH, that code also shuts down and closes the socket on failure, which could complicate things a little here.

Your synchronization looks good because you wrap the callbacks in the strand. I don't have a strong preference so long as this works.

## j-berman | 2025-10-24T00:40:14+00:00
Looks like you want to make sure the locally scoped timer isn't still running before you enter this function

## j-berman | 2025-10-24T01:03:39+00:00
Nit: whitespace

## j-berman | 2025-10-24T02:14:55+00:00
Checking my understanding: because the padding doesn't get serialized into `_` as part of the NOTIFY_NEW_TRANSACTIONS request anymore, and instead gets stuffed into the buffer, the `_` field is now empty even though this request should have been padded. And the reason you don't pad into a `_` field anymore is because you want padding on every message, rather than strictly NOTIFY_NEW_TRANSACTIONS

It would be nice if there was an explicit way to see/test the padding at this level, but not worth changing the internals for it

A comment explaining how these `_with_padding` tests are just testing smooth notifications even with `pad` true would be solid, since they're no longer explicitly testing the padding

## j-berman | 2025-10-24T02:34:34+00:00
Just to check my understanding: when de-serializing a message, the de-serializer is unaffected by these padded bytes because these are just dangling bytes that have no associated key, ya? The de-serializer can safely just ignore them

## j-berman | 2025-10-24T03:30:18+00:00
Looks like this was meant to be an else for `if (command_line::get_arg(vm, arg_p2p_persistent_cert))`

## vtnerd | 2025-10-24T18:59:56+00:00
Yes, the epee parser always ignored extra data after the root object. The "noise" mode in Tor uses this "feature" already - each message is padded to a specific length with possible trailing. So we have a working example at least.

Any future parser already has to support this feature due to the noise mode.

## vtnerd | 2025-10-24T19:35:47+00:00
The documentation for `SSL_get_certificate` states:

> Returned objects are owned by the SSL object and should not be freed by users of these functions.

So the `dflt_SSL` `unique_ptr` should cleanup the memory for this object.

## vtnerd | 2025-10-24T23:46:04+00:00
Correct, if a fingerprint is sent in the peerlist exchange, it is removed by the receiving node. The fingerprint is also exchanged in the handshake, iff the user has a fixed certificate. In this situation, the peer caches it and is equivalent to a unique ip/port pair in terms of treatment and recovery.

## vtnerd | 2025-10-24T23:47:20+00:00
Fixed.

## vtnerd | 2025-10-24T23:49:32+00:00
I think I fixed this. Always annoying in editors.

## vtnerd | 2025-10-24T23:50:01+00:00
Removed.

## vtnerd | 2025-10-25T00:02:19+00:00
Commented the best I could in the test file.

## vtnerd | 2025-10-25T00:14:46+00:00
I changed this to `else if (zone.first == epee::net_utils::zone::public_)` with an updating message saying "Not using encryption for p2p".

## vtnerd | 2025-10-25T00:17:17+00:00
Leaving this open for now, as I need to investigate further.

## vtnerd | 2025-10-25T00:22:49+00:00
Good catch, added `sd_deadline->cancel()` in the `if (ec)` section.

## vtnerd | 2025-10-25T00:52:35+00:00
Looked into it. The `client_handshake` function isn't asynchronous, so I just rolled this here. For better worse. Unlikely to change, but not marking as resolved either.

## selsta | 2026-04-16T16:13:39+00:00
```suggestion
  ["--rpc-login", "md5_lover:Z1ON0101", "--add-exclusive-node", "127.0.0,1:18282,no_encryption", "--add-exclusive-node", "127.0.0.1:18283"],
```

## vtnerd | 2026-04-16T21:16:35+00:00
Fixed.

## selsta | 2026-06-02T00:40:08+00:00
Should this perform a fresh TCP connect, like the synchronous path does?

## selsta | 2026-06-02T00:44:23+00:00
Should `set_ssl_enabled()` be added here?

## selsta | 2026-06-02T00:54:51+00:00
Does using `e_ssl_support_enabled` here make the inbound P2P listener require TLS? The new Levin docs say the responder must always be in autodetect SSL mode so it can handle both encrypted and unencrypted initiators.

## selsta | 2026-06-02T00:59:11+00:00
```suggestion
      pad_bytes = randombytes_uniform(P2P_MAX_LEVIN_PAD_BYTES + 1);
```

or update the `pad == true` comment to `0..8191` instead of `0..8192`.

## selsta | 2026-06-02T01:01:22+00:00
```suggestion
      int res = transport.send(to_send.finalize_notify(command, context.should_pad()), conn_id);
```

also here?

## selsta | 2026-06-02T01:07:13+00:00
```suggestion
static bool parse_address(string_address& out, std::string adr)
{
  namespace net = epee::net_utils;

  net::ssl_options_t e2e_mode{net::ssl_support_t::e_ssl_support_autodetect};

  const auto comma = adr.find(',');
  if (comma == std::string::npos)
  {
    out = {std::move(adr), std::move(e2e_mode)};
    return true;
  }

  const std::string option = adr.substr(comma + 1);
  adr.erase(comma);

  if (adr.empty() || option.empty())
    return false;

  if (option == "no_encryption")
  {
    out = {
      std::move(adr),
      net::ssl_options_t{net::ssl_support_t::e_ssl_support_disabled}
    };
    return true;
  }

  std::vector<std::uint8_t> fingerprint;
  try
  {
    fingerprint = epee::from_hex_locale::to_vector(boost::string_ref{option});
  }
  catch (const std::exception&)
  {
    return false;
  }

  if (fingerprint.size() != 32)
    return false;

  e2e_mode = net::ssl_options_t{{std::move(fingerprint)}, {}};
  out = {std::move(adr), std::move(e2e_mode)};
  return true;
}
```

more strict parsing

## selsta | 2026-06-02T01:13:48+00:00
Should this assert here check for `autodetect` or `enabled`?

## selsta | 2026-06-02T01:14:23+00:00
```suggestion
        daemon3.in_peers(0)
```

## vtnerd | 2026-06-05T17:27:28+00:00
This was a bug, good catch.

Looks like I mixed this up with what the client should be doing when a fingerprint is provided.

## vtnerd | 2026-06-05T17:31:57+00:00
Changed the comment.

## vtnerd | 2026-06-05T17:44:14+00:00
Good catch, I think the suggestion is better.

## vtnerd | 2026-06-05T17:46:22+00:00
Fixed.

## vtnerd | 2026-06-05T22:07:05+00:00
The test is currently checking for `autodetect`. The code never moves the state to `enabled`, that state should be exclusive to when a user specifies a fingerprint of when the connection (via handshake) signals it is using a persistent cert.

This can be seen in the [status printer](https://github.com/monero-project/monero/pull/8996/changes#diff-eb68bc70cb6aeb0af3d4d6ebb9cfc2512547dc8590ae4b78f427dfe255b6e1c0R665) where autodetect and enabled have different labels printed.

## vtnerd | 2026-06-05T22:38:28+00:00
Yes, this does appear to be useful/needed.

## vtnerd | 2026-06-05T22:40:08+00:00
I'm probably going to do this, but it won't be included in the SSL+beta-stressnet PR.

## vtnerd | 2026-06-07T17:17:15+00:00
It shouldn't matter afaik. The `async_connect` routine automatically closes existing sockets.

## selsta | 2026-06-11T21:20:55+00:00
```suggestion
    return connections_.erase(new_connection) != 0;
```

to avoid later calling `cb` later from a stale async path

## selsta | 2026-06-11T21:29:59+00:00
I checked Boost 1.90 source for `basic_socket::async_connect`, it only opens the socket if `!is_open()` and does not close/reset an already-open socket. The synchronous autodetect path explicitly does `shutdown()` + `close()` before retrying without SSL.

## vtnerd | 2026-06-11T22:07:22+00:00
Isn't this an existing issue (that should never happen)?

## selsta | 2026-06-11T22:23:54+00:00
If it matches existing behavior (and there isn't more risk with the new code) then this can be ignored.

## vtnerd | 2026-06-12T02:01:11+00:00
The prior pattern was:

```c++
    connections_mutex.lock();
    connections_.erase(new_connection_l);
    connections_mutex.unlock();
```

where the result of the erase was ignored. I moved this into a separate function, and the result still doesn't matter, but the `nullptr` check is _somewhat_ useful.

I should be able to remove the `connections_` map code from the async connect entirely. I kept it because the existing async_connect code had it.

## selsta | 2026-07-15T19:06:15+00:00
This needs to be conditional on `store_ssl_keys()` succeeding, otherwise a non-persistent fingerprint is shared.

## selsta | 2026-07-15T19:12:13+00:00
```suggestion
    node_data.encryption_mode = zone.m_ssl_finger.empty() ? get_encryption_mode(zone.m_ssl_support) : emode_ssl_enabled;
```

Otherwise `get_p2p_encryption()` returns early and ignores the fingerprint.

## selsta | 2026-07-15T19:18:34+00:00
`encryption_mode` and `cert_finger` are not populated here. `encryption_mode` is indeterminate and `cert_finger` is empty. `COMMAND_TIMED_SYNC` replaces the outgoing peer's stored encryption state with corrupt data.

## vtnerd | 2026-08-20T01:02:47+00:00
Good catch, fixed.

## selsta | 2026-08-20T11:32:55+00:00
`PING` and `REQUEST_SUPPORT_FLAGS` allow only 4096 bytes, so `P2P_MAX_LEVIN_PAD_BYTES` can randomly exceeds command limits.

## selsta | 2026-08-20T11:35:38+00:00
`--p2p-disable-encryption` is ignored here.

## vtnerd | 2026-08-20T15:46:07+00:00
Yes, I stepped through all of the logic again, and this is defintely needed as `m_ssl_support` is typically `autodetect` so that the server is in autodetect mode (otherwise SSL is forcibly required by the node).

## vtnerd | 2026-08-20T16:44:38+00:00
Actually this should be fine AFAIK, but it's not obvious how.

Nearly every location uses brace/zero initialization instead of default initialization. The one place that default initializes correctly sets `encryption_mode` and `cert_finger`. So this patch is fine as-is.

Despite this, I am switching to brace initialization everywhere, and adding a default constructor.

## vtnerd | 2026-08-21T17:47:33+00:00
I added a fix where the randomized padding is capped at the message max size. Most messages are capped at the 8 KiB global padding max.

## vtnerd | 2026-08-21T17:47:50+00:00
Great catch. This was fixed too.

# Action History
- Created by: vtnerd | 2023-09-19T23:24:27+00:00
