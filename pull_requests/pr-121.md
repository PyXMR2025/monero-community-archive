---
title: P2P Client Pool
source_url: https://github.com/Cuprate/cuprate/pull/121
author: Boog900
assignees: []
labels:
- A-p2p
created_at: '2024-04-30T01:09:37+00:00'
updated_at: '2024-06-16T23:37:55+00:00'
type: pull_request
status: merged
closed_at: '2024-05-17T13:52:51+00:00'
merged_at: '2024-05-17T13:52:51+00:00'
---

# Original Description
This PR adds the `ClientPool` and connection maintainer task.

## Client Pool

The client pool replaces what the peer-set was supposed to be. The `PeerSet` was a `tower::Service` that held the connected peers, allowing the routing methods to retrieve peers from it. However it had notable overhead and giving peers back to it was not simple.

The client pool instead is a wrapper around a `DashMap` this should make it quicker to retrieve peers from than the peer set. Although it does make it a bit more complicated to remove disconnected peers from, which is handled by the `disconnect_monitor` task.

A worry with both designs `PeerSet` and `ClientPool` is that only one router can hold a peer at once, i.e while a peer is being used it can not be used by another router. If this becomes a problem we can change it so we can share peers, however I do not see this being a big issue.
 

# Discussion History
## hinto-janai | 2024-05-16T16:31:04+00:00
May need to re-add features to `tracing-subscriber`.
```suggestion
cuprate-test-utils = { path = "../../test-utils" }
tracing-subscriber = { workspace = true }
```

## hinto-janai | 2024-05-16T16:41:43+00:00
Is there a reason to not leave wrapper types up to the caller?

I see it is `Arc<Self>` everywhere else though, is `&Self` never going to be used?

## hinto-janai | 2024-05-16T16:43:40+00:00
```suggestion
    /// Adds a [`Client`] to the pool, the client must have previously been taken from the
    /// pool.
    ///
    /// See [`ClientPool::add_new_client`] to add a [`Client`] which was not taken from the pool before.
    ///
    /// # Panics
    /// This function panics if `client` already exists in the pool.
    fn add_client(&self, client: Client<N>) {
```

## hinto-janai | 2024-05-16T16:46:53+00:00
```suggestion
    /// Adds a _new_ [`Client`] to the pool, this client should be a new connection, and not already
    /// from the pool.
    ///
    /// # Panics
    /// This function panics if `client` already exists in the pool.
    pub fn add_new_client(&self, client: Client<N>) {
```

## hinto-janai | 2024-05-16T16:47:33+00:00
```suggestion
    /// Remove a [`Client`] from the pool.
    ///
    /// [`None`] is returned if the client did not exist in the pool.
    fn remove_client(&self, peer: &InternalPeerID<N::Addr>) -> Option<Client<N>> {
```

## hinto-janai | 2024-05-16T16:49:16+00:00
```suggestion
    /// Borrows a [`Client`] from the pool.
    ///
    /// The [`Client`] is wrapped in [`ClientPoolDropGuard`] which
    /// will return the client to the pool when it's dropped.
    ///
    /// See [`Self::borrow_clients`] for borrowing multiple clients.
    pub fn borrow_client(
```

## hinto-janai | 2024-05-16T16:51:49+00:00
```suggestion
    /// Borrows multiple [`Client`]s from the pool.
    ///
    /// Note that the returned iterator is not guaranteed to contain every peer asked for.
    ///
    /// See [`Self::borrow_client`] for borrowing a single client.
```

## hinto-janai | 2024-05-16T16:52:53+00:00
```suggestion
//! This module contains the [`disconnect_monitor`] task, which monitors connected peers for disconnection
//! and then removes them from the [`ClientPool`] if they do.
```

## hinto-janai | 2024-05-16T16:54:24+00:00
```suggestion
    /// A set of outbound clients, as these allow accesses/mutation from different threads,
    /// a peer ID in here does not mean the peer is necessarily in `clients` as it could have been removed
    /// by another thread. However, if the peer is in both here and `clients` it is definitely
    /// an outbound peer.
    outbound_clients: DashSet<InternalPeerID<N::Addr>>,
```

## hinto-janai | 2024-05-16T17:04:11+00:00
```suggestion
    /// The [`Client`].
    ///
    /// This is set to [`Some`] when this guard is created, then
    /// ### [`take`](Option::take)n and returned to the pool when dropped.
    pub(super) client: Option<Client<N>>,
```

## hinto-janai | 2024-05-16T17:05:43+00:00
Might be worth using `num/num_traits` if the bounded number pattern occurs often.

## hinto-janai | 2024-05-16T17:10:11+00:00
Is `svc` a common abbreviation for `service` in this type of code?

Not asking this to be changed, just curious.

## hinto-janai | 2024-05-16T17:13:17+00:00
```suggestion
{
    /// Create a new [`Self`].
    ///
    /// # Panics
    /// This function panics is [`P2PConfig::gray_peers_percent`] is not `0.0..=1.0`. 
    pub fn new(
```

## hinto-janai | 2024-05-16T17:14:11+00:00
```suggestion
        assert!(!seeds.is_empty(), "No seed nodes available to get peers from");
```

## hinto-janai | 2024-05-16T17:14:56+00:00
Why not take `config` by value? It's also `Copy`.

## hinto-janai | 2024-05-16T17:16:31+00:00
```suggestion
        if allowed_errors == 0 {
            Err(OutboundConnectorError::FailedToConnectToSeeds)
        } else {
            Ok(())
        }
```

## hinto-janai | 2024-05-16T17:19:04+00:00
Is there anything asserting `max_outbound_connections >= outbound_connections`?

`P2PConfig::allowed_extra_connections` will panic if not.

## hinto-janai | 2024-05-16T17:19:26+00:00
```suggestion
    /// Returns the amount of outbound connections still available.
    pub fn allowed_extra_connections(&self) -> usize {
        self.max_outbound_connections - self.outbound_connections
    }
```

## hinto-janai | 2024-05-16T17:21:18+00:00
```suggestion
    /// Handles a free permit, by either connecting to a new peer or by removing a permit if we are above the
    /// minimum number of outbound connections.
```

## hinto-janai | 2024-05-16T17:23:09+00:00
There's `tracing::debug` usage below mixed with `info`, on purpose?

## hinto-janai | 2024-05-16T17:26:50+00:00
Is this still needed?

## Boog900 | 2024-05-16T23:29:20+00:00
I don't think this is used, must be left over from an old test 

## Boog900 | 2024-05-16T23:31:00+00:00
the disconnect monitor needs a handle to the pool, so it needs to be in an Arc

## Boog900 | 2024-05-16T23:54:14+00:00
`is_empty` is not stable here yet 

## Boog900 | 2024-05-16T23:58:28+00:00
I don't think this doc needs to be duplicated here, it's already on the config 

## Boog900 | 2024-05-16T23:59:07+00:00
I have done it before, I have seen it elsewhere, I think, as well.

## Boog900 | 2024-05-17T00:00:27+00:00
We could, for another time though 

## Boog900 | 2024-05-17T00:01:45+00:00
yeah, not everything is public, it will be removed though in a future PR 

## hinto-janai | 2024-05-17T01:16:24+00:00
Same as last time, up to you.

Could be in the config but usually panic docs are located on the function that asserts the invariant. Btw I made a typo here `is` -> `if`.

## hinto-janai | 2024-05-17T01:27:28+00:00
nit
```suggestion
impl<N, A, C> OutboundConnectionKeeper<N, A, C>
where
    N: NetworkZone,
    A: AddressBook<N>,
    C: Service<ConnectRequest<N>, Response = Client<N>, Error = HandshakeError>,
    C::Future: Send + 'static,
{
```

## hinto-janai | 2024-05-17T01:35:17+00:00
Btw this is because `cargo doc` formats it as-is:

![image](https://github.com/Cuprate/cuprate/assets/101352116/162689f9-d577-41a3-a6af-33db31662633)

## Boog900 | 2024-05-17T13:51:53+00:00
I am going to leave it as is for now, eventually we could look at doing this: https://github.com/Cuprate/cuprate/pull/121#discussion_r1603740823

# Action History
- Created by: Boog900 | 2024-04-30T01:09:37+00:00
- Merged at: 2024-05-17T13:52:51+00:00
