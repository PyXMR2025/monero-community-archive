---
title: Memwipe
source_url: https://github.com/monero-project/monero/pull/2860
author: moneromooo-monero
assignees: []
labels: []
created_at: '2017-11-24T16:34:48+00:00'
updated_at: '2017-12-16T21:19:45+00:00'
type: pull_request
status: merged
closed_at: '2017-12-16T21:19:45+00:00'
merged_at: '2017-12-16T21:19:45+00:00'
---

# Original Description
This is the part of https://github.com/monero-project/monero/pull/2731 that can be merged now, whether or not the Bitcoin PRNG gets merged.

# Discussion History
## jroelofs | 2017-11-24T17:15:48+00:00
Mind quoting the relevant bit of the standard? IIRC using `data()` this way has been "blessed" since c++11.

## jroelofs | 2017-11-24T17:20:16+00:00
Everything done with `foo` here after `free()` is UB. Even without optimizations turned on, the compiler has liberty to kneecap you here and eliminate what it sees as dead code after it.

Better sink the `free()` down to the bottom of the function.

## jroelofs | 2017-11-24T17:22:22+00:00
> putting the function in its own file for other platforms

LTO makes that a non-starter. Glad they chose another strategy.

## moneromooo-monero | 2017-11-24T17:31:12+00:00
I don't have the standard handy. I'm going by http://www.cplusplus.com/reference/string/string/data, which says, in part, "A program shall not alter any of the characters in this sequence". Note that the function returns a const char*, not a char*. Maybe the "since c++11" is the important part here ?

## moneromooo-monero | 2017-11-24T17:33:24+00:00
Everything done with the dereference of foo. And that's kind of the point of the test. I suppose it may be useless...

## jroelofs | 2017-11-24T18:33:48+00:00
We don't care what's in *foo after it has been freed, so long as just before freeing it has been wiped. The test won't be useless if the free gets moved down to the right spot.

 As-is, this is a use-after-free, which tools like asan/valgrind will rightfully error out on.

## moneromooo-monero | 2017-11-24T18:38:05+00:00
Heh, quite true, though something like memset + memcmp then free will never fail even if the optmizer would otherwise optimize the memset away. A valgrind report is fine and expected here, that's not a problem. But I'll remove the test anyway, since it's UB indeed.

## jroelofs | 2017-11-24T18:48:57+00:00
21.4.7.1 c++11 [1]:

> const charT* data() const noexcept;
> 1 Returns: A pointer p such that p + i == &operator[](i) for each i in [0,size()].
> 2 Complexity: constant time.
> 3 Requires: The program shall not alter any of the values stored in the character array.

21.3.6 in c++98 [2]:

> const charT * data () const ;
> 3 Returns: If size() is nonzero, the member returns a pointer to the initial element of an array whose first size()
elements equal the corresponding elements of the string controlled by *this. If size() is zero, the member
returns a non-null pointer that is copyable and can have zero added to it.
> 4 Requires: The program shall not alter any of the values stored in the character array. Nor shall the program
treat the returned value as a valid pointer value after any subsequent call to a non- const member function of
basic_string that designates the same object as this.

~~TL;DR: using string::data() like this is fine.~~

Edit: I'm wrong.

1: http://www.open-std.org/jtc1/sc22/wg21/docs/papers/2013/n3690.pdf

2: http://www.open-std.org/jtc1/sc22/wg21/docs/papers/2005/n1905.pdf

## jroelofs | 2017-11-24T18:52:07+00:00
Minus the optimization barriers that are in there via `SCARECROW` :)

The test is good to have though, since it explicitly checks that it was zeroed, and not assigned with random garbage (which might be another strategy for implementing memwipe).

## moneromooo-monero | 2017-11-24T19:09:44+00:00
I don't get it. Both seem to say "The program shall not alter any of the values stored in the character array."

## moneromooo-monero | 2017-11-24T19:10:31+00:00
Well, we don't really care how it's wiped, as long as it is.

## jroelofs | 2017-11-24T19:19:58+00:00
Wait, shit. I got the two of them confused. You're right.

Probably have to resort to `&pass[0]`.

## jroelofs | 2017-11-24T19:24:47+00:00
👍

## jroelofs | 2017-11-24T19:28:40+00:00
(was getting vector::data() and string::data() confused, btw)

## jroelofs | 2017-11-24T22:16:39+00:00
FWIW, even comparing pointers after freeing one of them is UB. I think you're conflating memory management with zeroing here. While we care that the free'd memory has been zeroed out, that's not something that C allows you to test. That said, we don't need to worry about malloc/free here... this is sufficient:

```
  char foo[] = "foo";
  volatile char *wipe = memwipe(foo, sizeof(foo));
  ASSERT_EQ(wipe, foo);

  // Use a volatile ptr so the compiler cannot elide the checks:
  volatile char *vfoo = foo;
  ASSERT_EQ(vfoo[0], '0');
  ASSERT_EQ(vfoo[1], '0');
  ASSERT_EQ(vfoo[2], '0');
  ASSERT_EQ(vfoo[3], '0');
```



## moneromooo-monero | 2017-11-24T22:23:48+00:00
Are you sure comparing pointers after free is UB ?

Your example defeats the point of the test because those accesses will likely cause any optimization pass to not remove the memset in the first place.


## moneromooo-monero | 2017-11-24T22:31:21+00:00
I did find references saying it is indeed UB. Bastards. Oh well.

## jroelofs | 2017-11-24T22:41:04+00:00
Wearing my compiler engineer hat, yes, it is UB :)

Short of inspecting the assembly, I do not know of any way to check for the presence of the optimization barrier in memwipe's implementation.

## moneromooo-monero | 2017-11-24T22:45:06+00:00
Fair enough. I'll leave it as its latest form, as it will only access through a valid pointer. If the foo pointer is somehow changed, it'll go to the "cannot test" branch, so we can't lose I think.

## moneromooo-monero | 2017-11-24T22:47:03+00:00
Oh, codesourcery... thank you all for your past work as a whole ^_^

## jroelofs | 2017-11-24T22:50:13+00:00
We lose when the compiler realizes that the pointer test is UB, and decides that it's always going to take the false branch. std::launder might help here, but we won't get that until c++17 :/

## moneromooo-monero | 2017-11-24T23:08:47+00:00
Ah, good point :/

## moneromooo-monero | 2017-11-24T23:20:38+00:00
<s>Though we still don't lose, we just fail to win ^_^</s> nevermind, I guess it could do all sorts of worse things. So we still lose :/

## jroelofs | 2017-11-24T23:28:07+00:00
Or if it shoots out nasal demons, sends all your btc to `1bitcoineateraddressdontsendf59kue`, and formats your hdd :P

You really can't make any assumptions about what happens in the presence of UB.

## vtnerd | 2017-11-25T03:43:51+00:00
Do not cast away the constness here. `std::string` prior to C++11 was allowed to do CoW, and libstdc++ had such an implementation. I _believe_ gcc prior to 5.0 (which is still supported by monero) shipped with the older ABI, which still is likely doing CoW on those platforms. In that situation, this `memwipe` call could be modifying an aliased `std::string`; the non-const version of `operator[]` must be used to force an immediate copy if necessary. Also, the contents of a CoW `std::string` cannot be wiped with certainty without a custom allocator, which is what Bitcoin does (or at least used to do).

FYI, a mutable `std::string::data()` will be available for C++17, and apparently its omission has been oversight.

## moneromooo-monero | 2017-11-25T09:01:29+00:00
Sounds like I'll just make a small class based on std::vector then.

## moneromooo-monero | 2017-11-25T13:36:15+00:00
How about this. We save foo into an intptr_t before the free. Then we compare later. Still UB ?

```
static void test(bool wipe)
{
  char *foo = (char*)malloc(4);
  ASSERT_TRUE(foo != NULL);
  strcpy(foo, "bar");
  void *bar = wipe ? memwipe(foo, 3) : memset(foo, 0, 3);
  ASSERT_EQ(foo, bar);
  intptr_t foop = (intptr_t)foo;
  free(foo);
  char *quux = (char*)malloc(4); // same size, just after free, so we're likely to get the same, depending on the allocator
  if ((intptr_t)quux == foop)
  {
    MDEBUG(std::hex << std::setw(8) << std::setfill('0') << *(uint32_t*)quux);
    if (wipe) ASSERT_TRUE(!memcmp(quux, "\0\0\0", 3));
  }
  else MWARNING("We did not get the same location, cannot check");
  free(quux);
}
```

## jroelofs | 2017-11-25T14:57:16+00:00
I believe this still depends on UB. The compiler can see right through the fact that you're trying to catch malloc giving you the same memory again.

## jroelofs | 2017-11-25T23:13:55+00:00
This returns a copy now, instead of a reference. Is that intentional?

## jroelofs | 2017-11-25T23:19:34+00:00
```
if (!wipefunc)
  abort()
wipefunc(...)
```

?

## jroelofs | 2017-11-25T23:20:08+00:00
likewise?

## vtnerd | 2017-11-26T03:02:19+00:00
Why the indirection to this function? Is this less likely to be optimized away?

## moneromooo-monero | 2017-11-26T08:39:08+00:00
It was, it was otherwise failing to perform some move, but it might be OK now that I added some move ctors around, I'll check whether it can be changed back.

## moneromooo-monero | 2017-11-26T08:39:37+00:00
I guess I can.

## moneromooo-monero | 2017-11-26T08:40:48+00:00
Because I had to put wipeable_function in epee, and memwipe's in common, and common depends on epee. Though sadly some stuff in epee cheekily uses some stuff in src...

## vtnerd | 2017-11-26T15:20:13+00:00
No I meant why does `memory_cleanse` exist - its only being called from one function internally to this cpp.

## vtnerd | 2017-11-26T15:22:28+00:00
`std::unique_ptr<char[]>`

## vtnerd | 2017-11-26T15:26:11+00:00
The initializer should be used to construct the member instead of the body.

## vtnerd | 2017-11-26T15:27:54+00:00
This is creating a temporary copy that _may_ get wiped depending on C++11 compliance (a fair assumption as a I stated in the general comments). Anyway it is currently two lines to implement the code without the temporary, and can be refactored so that `const std::string` uses the same internal construction function as this constructor. i.e. create a private function that take a ptr,length and have the `const std::string &` overload and this constructor call it. The `std::string&&` could also use it before wiping the incoming string.

## vtnerd | 2017-11-26T15:31:43+00:00
This comment somewhat indicates that perhaps the `std::string` overloads should be removed. Creating this class was due to the potential limitations of `std::string` ...

Also, I would recommend `other = std::string{};` so that the caller is left with an object that appears moved instead of an object whose contents have been erased. 

## vtnerd | 2017-11-26T15:35:14+00:00
`data()`, `size()`, and `empty()` can all be marked with `noexcept`.

## vtnerd | 2017-11-26T15:35:34+00:00
Definitely add `noexcept` to the comparison operators.

## vtnerd | 2017-11-26T15:39:09+00:00
The following exhibit the behavior you want without having to provide an implementation:
```c++
wipeable_string() = default;
wipeable_string(const wipeable_string&) = default;
wipeable_string(wipeable_string&&) = default;
wipeable_string& operator=(const wipeable_string&) = default;
wipeable_string& operator=(wipeable_string&&) = default;
```

Although if you take my other recommendation to drop `std::vector` then a replacement for the copy and assignment functions will be required.

## vtnerd | 2017-11-26T15:41:45+00:00
Why is this modifiable? Its set in exactly once place. Is this to prevent linking against another library?

## vtnerd | 2017-11-26T16:00:01+00:00
You've already had to intercept every size operation on the `std::vector`, why not manage a `std::unique_ptr<char[]>` directly instead? It will also be more obvious when allocations are occurring. The negative is that copy and move assignments will have to be implemented (currently the defaults will work). There amount of code in this function will be nearly identical.

## vtnerd | 2017-11-26T16:01:06+00:00
I don't see where `push_back` is being used so I would just drop it.

## vtnerd | 2017-11-26T16:03:12+00:00
`push_back` in `std::vector` typically doubles the capacity so that its not constantly re-allocating the buffer. The current implementation that uses `std::vector` might be doing this internally in the reserve calls, but if you switch to a `std::unique_ptr<char[]>` buffer that logic would have to be done here. Also strongly consider my recommendation to dropping the method entirely.

## vtnerd | 2017-11-26T16:07:18+00:00
This is now busted, `userpass` has been moved.

## jroelofs | 2017-11-26T16:08:12+00:00
Yes, it makes things harder for the compiler.  We're already paying the price of an "unnecessary" `memset`, and the overhead of an extra call should be negligible.

## jroelofs | 2017-11-26T16:19:40+00:00
Seems like it's meant to break a dependence between the two libraries.

## moneromooo-monero | 2017-11-26T20:39:21+00:00
Ah, memory_cleanse is used in the "use Bitcoin PRNG" patchset. I just PR'd this separately as a building block because it's separate and doesn't have another option.

## moneromooo-monero | 2017-11-26T20:40:12+00:00
It's used in password.cpp IIRC (when typing a character)

## moneromooo-monero | 2017-11-26T20:40:52+00:00
It's to prevent a dependency from epee to common.

## moneromooo-monero | 2017-11-26T20:44:38+00:00
I do not understand this. Could you please try to explain in another way how a std::unique_ptr\<char\> is better than a std::vector\<char\> here ?

## moneromooo-monero | 2017-11-26T20:45:40+00:00
Oooh, thanks!

## vtnerd | 2017-11-27T05:59:49+00:00
Is it currently worth the effort? This implementation is only wiping memory, and is not locking the password in RAM to prevent copying to swap/disk. The only place where a password is being used in the `epee` namespace is in the RPC server code which keeps the password in memory for the entire operation of the process. There is no advantage to wiping this value - the operating system will never return this memory address unsanitized (it would be a big security violation).

If you wanted to lock the memory into RAM, then things change a bit because the user can specify a RPC password via prompt (and therefore never have it on disk). Although its not clear how many people are doing this.

## jroelofs | 2017-11-27T14:44:27+00:00
Locking things to ram was going to be one of my next PRs... baby steps.

## jroelofs | 2017-11-27T14:56:35+00:00
`unique_ptr<char[]>` gives you more explicit control over the lifetime of memory allocated. With vector you have to be really careful that you don't trigger resize/deallocation without wiping appropriately.


## jroelofs | 2017-11-27T14:59:34+00:00
Also, by using a smart ptr instead of one of the containers, later we'll be able to pool them and memlock the pool.

## moneromooo-monero | 2017-11-27T21:50:03+00:00
The string&& one needs to stay bewcause of the login::parse method in src/common/password.cpp, which gets a string&& and says it's wiped. Though I suppose one could just wipe it there instead. But in any case it's a std::string. It's called with things like command_line::get_arg on a string.

## moneromooo-monero | 2017-11-27T22:20:48+00:00
It's also called from the chacha code.

## moneromooo-monero | 2017-11-27T22:22:23+00:00
I started, but I'm more confident I did it right with std::vector than I'd be with std::unique_ptr, for which the lifetime looks confusing to me, so I've left it as is. If you want to change it later, feel free ^_^

# Action History
- Created by: moneromooo-monero | 2017-11-24T16:34:48+00:00
- Merged at: 2017-12-16T21:19:45+00:00
