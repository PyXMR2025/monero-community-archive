---
title: carrot_core
source_url: https://github.com/monero-project/monero/pull/9559
author: jeffro256
assignees: []
labels:
- pending review
- carrot
created_at: '2024-11-08T20:56:33+00:00'
updated_at: '2026-08-28T08:54:26+00:00'
type: pull_request
status: open
closed_at: null
merged_at: null
---

# Original Description
This PR is to implement all Carrot cryptography, as well as enote creation logic and enote scanning logic. This PR should exclude very Monero-specific integration details, except for domain separators. The `carrot_core` library is largely meant to be a stand-alone library, and thus only includes headers from `src/crypto`. 

https://github.com/jeffro256/carrot/blob/master/carrot.md

- [x]  Non-legacy carrot enote creation
- [x]  Non-legacy carrot enote scanning
- [x] Tests for new enotes addressed to legacy cryptonote accounts/addresses
- [x] Transaction output set finalization logic
- [x] Efficient implementation for X25519 unclamped scalar multiplication
- [x] Hardware device interface
- [x] Switch commitments (walmart version)
- [x] Internal self-send messages
- [x] Fix Debian 10 build
- [x] Unbind the special janus anchor derivation from the account spend pubkey to make hybrid accounts easier
- [x] Rewrite ARM mx25519 implementation to support unclamped scalar multiplications
- [x] ~~Deterministic FCMP++ output rerandomizations to support some refund address schemes~~
- [x] ~~Low-dependency payment proposal to enote finalization C interface for hardware wallets~~
- [x] ~~Payment proofs to internal enotes which A) allow for burning bug validation and B) don't reveal $s_{vb}$~~
- [x] Refactor for explicit unclamped mx25519 API: https://github.com/tevador/mx25519/pull/15

Depends on #9826, #9827, #9828 , #10108, #10111, #10338, #10963, #10964, #10965, #11155

# Discussion History
## UkoeHB | 2026-06-17T18:41:07+00:00
`ec_scalar`

## UkoeHB | 2026-06-17T18:53:57+00:00
`// returns x G + y T`

## UkoeHB | 2026-06-17T18:58:23+00:00
Maybe make this `L` or `g_l` to make it more clearly a global at use-sites. Or put it in a namespace `consts`.

## thomasbuilds | 2026-08-19T07:21:14+00:00
and here?

## thomasbuilds | 2026-08-25T10:51:01+00:00
Spec 8.1 step 18 (prime-order subgroup check on K^j_s') is not enforced on the receiver path?

```suggestion
        return false;

    if (!verify_point_is_in_main_subgroup(address_spend_pubkey_out))
        return false;
```

## thomasbuilds | 2026-08-25T10:57:24+00:00
2c45ac024 is on tevador/mx25519 master (merged via PR 15) so this can point at the canonical repo?

```suggestion
	url = https://github.com/tevador/mx25519.git
```

## jeffro256 | 2026-08-27T17:46:05+00:00
This will get resolved in #10964 

## thomasbuilds | 2026-08-28T08:23:00+00:00
`enote_type_out` (declared at 288) is never assigned so on success the caller keeps whatever was on the stack. `sender_scan_main_address` / `sender_scan_subaddress` pass only because that byte happens to be 0 == `PAYMENT` (Valgrind flags the uninitialised read).

```suggestion
    else if (recovered_enote_type != CarrotEnoteType::PAYMENT)
        return false;

    enote_type_out = recovered_enote_type;
    return true;
```

## thomasbuilds | 2026-08-28T08:23:00+00:00
Probable cause of the 34 macOS failures: since #10960 `std::hash<crypto::public_key>` is SipHash keyed in `init_random()` (`constructor(101)`), and Mach-O ignores constructor priorities, so `people::alice/bob/eve` are generated here at static init with the zero key; `init_random()` then re-keys and every `get_index_for_address_spend_pubkey` lookup misses, so `can_open_fcmp_onetime_address` returns false. Proposed fix: generate the people from a gtest global `Environment` instead of at static init; patch below, applies to d002730e9. Any other `unordered_*` keyed by a `crypto::*` type and filled at static init has the same problem on macOS

<details><summary>patch</summary>

```diff
diff --git a/tests/unit_tests/carrot_mock_helpers.h b/tests/unit_tests/carrot_mock_helpers.h
index 3e060a88a..a521e9efd 100644
--- a/tests/unit_tests/carrot_mock_helpers.h
+++ b/tests/unit_tests/carrot_mock_helpers.h
@@ -212,9 +212,13 @@ CarrotPaymentProposalSelfSendV1 convert_selfsend_payment_proposal_v1(const crypt
 //----------------------------------------------------------------------------------------------------------------------
 namespace people
 {
-extern const mock_carrot_and_legacy_keys alice;
-extern const mock_carrot_and_legacy_keys bob;
-extern const mock_carrot_and_legacy_keys eve;
+// Generated by a gtest global Environment, NOT at static-init time: before init_random() runs, the RNG is unseeded
+// and std::hash<crypto::*> (SipHash keyed by crypto_siphash_key) changes value, which makes any unordered_map built
+// that early unreadable afterwards. Mach-O has no constructor priorities, so on macOS the test TUs' globals are
+// initialized before init_random().
+extern mock_carrot_and_legacy_keys alice;
+extern mock_carrot_and_legacy_keys bob;
+extern mock_carrot_and_legacy_keys eve;
 //----------------------------------------------------------------------------------------------------------------------
 } //namespace people
 } //namespace mock
diff --git a/tests/unit_tests/carrot_mock_helpers.cpp b/tests/unit_tests/carrot_mock_helpers.cpp
index b03fd69a0..a3f55c78c 100644
--- a/tests/unit_tests/carrot_mock_helpers.cpp
+++ b/tests/unit_tests/carrot_mock_helpers.cpp
@@ -38,6 +38,7 @@
 #include "ringct/rctOps.h"
 
 //third party headers
+#include "gtest/gtest.h"
 
 //standard headers
 
@@ -48,14 +49,6 @@ namespace
 {
 //----------------------------------------------------------------------------------------------------------------------
 //----------------------------------------------------------------------------------------------------------------------
-static carrot::mock::mock_carrot_and_legacy_keys gen_default_person()
-{
-    carrot::mock::mock_carrot_and_legacy_keys guy;
-    guy.generate();
-    return guy;
-}
-//----------------------------------------------------------------------------------------------------------------------
-//----------------------------------------------------------------------------------------------------------------------
 } //anonymous namespace
 
 namespace carrot
@@ -672,10 +665,25 @@ CarrotPaymentProposalSelfSendV1 convert_selfsend_payment_proposal_v1(const crypt
 //----------------------------------------------------------------------------------------------------------------------
 namespace people
 {
-const mock_carrot_and_legacy_keys alice = gen_default_person();
-const mock_carrot_and_legacy_keys bob = gen_default_person();
-const mock_carrot_and_legacy_keys eve = gen_default_person();
-}
+mock_carrot_and_legacy_keys alice;
+mock_carrot_and_legacy_keys bob;
+mock_carrot_and_legacy_keys eve;
+
+namespace
+{
+struct people_environment final : public ::testing::Environment
+{
+    void SetUp() override
+    {
+        alice.generate();
+        bob.generate();
+        eve.generate();
+    }
+};
+[[maybe_unused]] const ::testing::Environment *const people_environment_registration
+    = ::testing::AddGlobalTestEnvironment(new people_environment);
+} //anonymous namespace
+} //namespace people
 //----------------------------------------------------------------------------------------------------------------------
 } //namespace mock
 } //namespace carrot
\ No newline at end of file
```
</details>

## thomasbuilds | 2026-08-28T08:23:00+00:00
Cannot pass as written: the `_sender` overloads reject anything but `PAYMENT` (scan.cpp:319) and check only normal Janus anchors (scan.h:123) while Alice's change enote is a special enote. Delete it or scan Alice's enote through the receiver path rather than leaving it commented out?

## thomasbuilds | 2026-08-28T08:23:00+00:00
This pins only the doubly-protected case: `K^j_s'` = identity is rejected both by enote_utils.cpp:212 and by `ge_p3_to_x25519` (:225). The subgroup check at :217 is the only thing rejecting the other small-order points and nothing exercises it: replacing it with a plain `ge_frombytes_vartime` leaves all 92 tests of the PR's carrot/x25519 suites green, while `D_e` = 0 with `K^j_s'` = (0, -1) (`ecff...7f`) scans successfully whenever `d_e'` is odd. Add that case with a fixed odd-`d_e'` anchor (`gen_janus_anchor()` at :3117 would catch a regression only half the time).

## thomasbuilds | 2026-08-28T08:23:00+00:00
`alice_selfsend_type` (708) is unused: the type is hard-coded `CHANGE` here, so the 12 tests at 834-903 run the CHANGE case twice and never a PAYMENT self-send beside a normal payment (that path works when exercised by hand). `.is_subaddress` is also missing, so the `sub2*` cases build a subaddress self-send with `is_subaddress = false`.

## thomasbuilds | 2026-08-28T08:23:00+00:00
Same as carrot_core.cpp:753-758: `alice_selfsend_type` (136) unused, `CHANGE` hard-coded (257-291 run it twice), `.is_subaddress` missing. Line 181 also sets `enote_ephemeral_privkey` explicitly (see payment_proposal.cpp:259-261); the test should not need it.

## thomasbuilds | 2026-08-28T08:23:00+00:00
Inconsistent with `get_enote_ephemeral_privkey` (206-207): this getter tolerates an explicit `d_e` equal to the normal payment's, that one rejects any explicit `d_e`. With the proposal carrot_legacy.cpp:181 builds, `get_output_enote_proposals` succeeds but `get_enote_ephemeral_privkey(ss, &normal, ...)` throws. Apply one rule in both.

## thomasbuilds | 2026-08-28T08:23:00+00:00
`rand_idx` starts at 0, so (0,0), which `mock_carrot_and_legacy_keys::subaddress` maps to the main address, comes up ~1 % of the time and the "subaddress" test silently becomes a main-address test (same at 439-440, 585-586, 712-713, 725-726); `mock::gen_subaddress_index()` already guarantees non-zero. Likewise `tx_inout_proof_special_2out_1selfsend_*` pick Bob's address type with `rand<int>() & 1` (2621, 2748); run both shapes deterministically.

## thomasbuilds | 2026-08-28T08:23:00+00:00
No test calls `get_coinbase_output_enotes`, directly or indirectly. (Exercised by hand it behaves; the one asymmetry with `get_output_enote_proposals` is that an empty proposal list yields an empty set instead of throwing `too_few_outputs`.)

## thomasbuilds | 2026-08-28T08:23:00+00:00
The scalar labelled "-1" is `p - 1`, not `l - 1` (comment only).

## thomasbuilds | 2026-08-28T08:23:01+00:00
An empty `epee::span<const crypto::public_key>{}` says what is meant better than a one-element `null_pkey` list

## thomasbuilds | 2026-08-28T08:23:01+00:00
Comment says `l * K^j_v` but this checks a generic point (copy-paste).

## thomasbuilds | 2026-08-28T08:23:01+00:00
`K^o_ext` is compressed by `make_carrot_sender_extension_pubkey` and immediately decompressed again with `ge_frombytes_vartime` (same at 350-358). Keeping the `ge_p3` from the double-scalar-mult, as `try_recover_address_spend_pubkey` does (558-586), saves a compression plus a variable-time decompression of a secret-derived point on the sender path.

## thomasbuilds | 2026-08-28T08:23:01+00:00
Spec 8.1 step 18 (K^j_s' must be in the prime-order subgroup) is enforced here, inside the D_e recomputation, which the external receiver reaches through the normal Janus check; the internal `s_vb` scan never calls this, so it has no equivalent check. A comment saying so would help.

# Action History
- Created by: jeffro256 | 2024-11-08T20:56:33+00:00
