---
title: 'database: impl `service` fn bodies '
source_url: https://github.com/Cuprate/cuprate/pull/113
author: hinto-janai
assignees: []
labels:
- A-storage
created_at: '2024-04-23T01:01:35+00:00'
updated_at: '2024-05-27T01:07:58+00:00'
type: pull_request
status: merged
closed_at: '2024-05-01T17:52:20+00:00'
merged_at: '2024-05-01T17:52:20+00:00'
---

# Original Description
## Part 22
https://github.com/Cuprate/cuprate/pull/104 <- Previous Next -> https://github.com/Cuprate/cuprate/pull/117

- Implements the `service` function signatures introduced in https://github.com/Cuprate/cuprate/pull/101 using the database functions introduced in https://github.com/Cuprate/cuprate/pull/102.

Requires https://github.com/Cuprate/cuprate/pull/102 to be merged first.

# Discussion History
## hinto-janai | 2024-04-23T19:00:02+00:00
@Boog900 Re: https://github.com/Cuprate/cuprate/pull/102#discussion_r1554580888

LMDB docs:
> A thread can only use one transaction at a time, plus any child transactions. Each transaction belongs to one thread.

And `heed` (issue 20):

> A thread is a single unit of execution. So is a transaction. It is nonsensical to associate them other than 1:1.

So we'll be opening a read transaction + table(s) per reader thread.

And although `redb`'s is `Send + Sync`, it seems like the read transaction + table per thread model is correct there too:

https://github.com/cberner/redb/blob/0d0e11ea901a3fe8a7ec4ac0b2b4aee121d11461/benches/lmdb_benchmark.rs#L220-L225

## Boog900 | 2024-04-23T19:29:14+00:00
If we open RO LMDB txs with `MDB_NOTLS` we can impl Send for our read only txs.

We can then use a [thread local](https://docs.rs/thread_local/latest/src/thread_local/lib.rs.html#103-111) which will hold the tables, so we don't need to keep opening txs and tables.


## hinto-janai | 2024-04-23T20:18:08+00:00
Are you sure 1 TX per thread is not how reads scale? Issue 20 seems to answer the `MDB_NOTLS` question:
> yes but a txn can only be used by one thread at a time, regardless. concurrent use of a single txn from multiple threads will cause memory corruption.

I believe it's mostly for moving the transaction, not sharing a reference across many threads.

This does break read atomicity though, AFAIK this is prevented in `monerod` with a spinlock, we could use a `RwLock` (only inside `service`) that syncs the reader-pool + 1 writer.

## Boog900 | 2024-04-23T20:46:51+00:00
> Are you sure 1 TX per thread is not how reads scale

We would still have 1 tx per thread but currently this is doing 1 tx per _block height_, using a thread local allows us to keep 1 tx per thread.

> This does break read atomicity though, AFAIK this is prevented in monerod with a spinlock, we could use a RwLock (only inside service) that syncs the reader-pool + 1 writer.

I am not really worried about this too much, the syncer will have a consistent view as it waits on writes so the worse event that we could get is an RPC (or other interface) request during a reorg when one tx sees one chain and the other sees another, the chance of this happening though are very low.

What we could do to combat this:

- Keep an atomic counter that gets incremented on a reorg (no request to do this yet)
- When a tx is added to the thread local record the atomics value with the tx. i.e (Tx, u64)
- after retrieving all values loop over the thread local and check all the txs have the same value.
- If they differ, try again.

It doesn't matter if they differ with the current atomic value just that they all agree on the state.

This does still allow for 2 txs to disagree on the top block but I can't see an issue with this.


## hinto-janai | 2024-04-23T21:35:41+00:00
> We would still have 1 tx per thread but currently this is doing 1 tx per block height, using a thread local allows us to keep 1 tx per thread.

Ahhh okay I see the mistake. `heed` has a `read-txn-no-tls` feature for `MDB_NOTLS` I'll enable.

> the syncer will have a consistent view as it waits on writes

IMO the correctness shouldn't depend on external code using it correctly.

> the worse event that we could get is an RPC (or other interface) request during a reorg when one tx sees one chain and the other sees another

Wouldn't a normal `add_block()` call be bad?
```
Reader 1 (same request as reader 2)
   |
   v      Writer
 tx_ro      |
   |        v        Reader 2
   |  tx_rw + commit   |
   |                   v
    \                tx_ro <- different state than reader 1
      \________________|
            |
collecting from different database states
```

> What we could do to combat this:

Do you think a counter + retries would allow more reader throughput than a `RwLock`? Is it worth taking away simplicity? Each function will now need `loop { do(); maybe_break }` style code instead of `lock(); do()`.

Could we use `RwLock` for now and implement a counter later if there's bottlenecks?

## Boog900 | 2024-04-23T21:59:41+00:00
> Wouldn't a normal add_block() call be bad?

An add block only adds new data and does not change old data, the worst that can happen is one tx does not see the new data and the request fails, which would have happened anyway, even with locking, if that same request came a couple seconds earlier.

> Do you think a counter + retries would allow more reader throughput than a RwLock

Yes I do, I don't know how significant the difference would be but I don't really want to add a lock for this.

> Each function will now need loop { do(); maybe_break }

Or you could make it recurse, which would be fine as this event is extremely unlikely.

> Could we use RwLock for now and implement a counter later if there's bottlenecks?

I would rather just not handle this now than add a lock.



## hinto-janai | 2024-04-26T00:31:17+00:00
Should the `DatabaseReadHandle` (the read service) give access to the actual database?

It lets anybody outside of `cuprate_database` directly use the database instead of going through `service`.

## hinto-janai | 2024-04-26T00:37:26+00:00
Context: `ReadRequest::Outputs`, we're mapping `cuprate_database::types::Output` -> `cuprate_types::OutputOnChain`.

How do we recover this `Timelock`? I don't think we store the value directly anywhere.

We could deserialize the parent transaction from `TxBlob` then get the timelock, but that seems expensive to do for every output?

## hinto-janai | 2024-04-26T00:37:43+00:00
Is `key` correct here?

## hinto-janai | 2024-04-26T00:39:22+00:00
What is the valid response to `ReadRequest::NumberOutputsWithAmounts` where the `amount` doesn't exist, i.e. there are 0 outputs with `amount`.

Should it be `Err(KeyNotFound)` or `Ok(0)`?

## hinto-janai | 2024-04-26T00:42:38+00:00
`Response::CheckKIsNotSpent(true)` means the key image doesn't exist, right?

## hinto-janai | 2024-04-26T00:49:41+00:00
Think adding an `enum cuprate_types::Exist { Does, DoesNot }` or similar would be nice btw.

## Boog900 | 2024-04-26T21:04:17+00:00
we could, probably should put a warning on it though 

## Boog900 | 2024-04-26T21:07:18+00:00
From the `Tx Unlock Time` table: https://github.com/Cuprate/cuprate/tree/db-schema/database#tx-unlock-time

## Boog900 | 2024-04-26T21:07:42+00:00
yep

## Boog900 | 2024-04-26T21:08:24+00:00
It should be `Ok(0)`

## Boog900 | 2024-04-26T21:10:11+00:00
IIRC I switched it to this in the document consensus PR so for now just put it as a big TODO to make sure, if this is incorrect all txs will fail verification anyway.

## hinto-janai | 2024-04-26T21:42:33+00:00
Ahhh...

## hinto-janai | 2024-04-27T00:43:23+00:00
Since this gets tested everywhere, I added helper struct for `#[cfg(test)]` to be more type-friendly.

## hinto-janai | 2024-04-27T00:47:01+00:00
Since `redb`'s transaction/tables are `Send + Sync`, it would be a waste to wrap them in `ThreadLocal` when sending to `rayon`.

So there is a `set_tx_ro!()`, `set_tables!()` and `get_tx_ro_and_tables!()` that branch on the backend, setting/getting either: `ThreadLocal<T>` (heed) or `&T` (redb).

Not sure if there's a cleaner way to do this, but it works. 

## hinto-janai | 2024-04-27T01:04:52+00:00
Tests all `Request`'s lead to expected `Response`'s, which is currently tested on...

## hinto-janai | 2024-04-27T01:05:17+00:00
...all the blocks we have currently in `test-utils/`.

## hinto-janai | 2024-04-27T01:08:23+00:00
If adding a block fails we should panic if we cannot maintain atomicity, correct?

Otherwise the database may proceed with half-inserted data. 

## hinto-janai | 2024-04-27T01:10:54+00:00
This is kinda awkward but I'm not sure where else to put this.

This is needed since we need to map `Output` -> `OutputOnChain` in:
- Reader service's `output()` response function
- Reader service's test to make sure the `Response` is returning correct `OutputOnChain`'s.

## hinto-janai | 2024-04-27T01:13:17+00:00
This is safe?

The feature makes `heed::RoTxn: Send`, and I believe `heed::Database` is `Send` for all of our cases.

## Boog900 | 2024-04-28T14:19:40+00:00
This is making `heed::RoTxn` `Sync`, which I don't think is safe.

For our current use cases this is ok as we aren't using them between threads however this would be a soundness hole. 

I only really see 2 ways of making this sound: add a mutex on the inner tx or mark every method on `HeedTableRo` unsafe. Both are not great.

## Boog900 | 2024-04-28T14:23:28+00:00
this needs to branch on the value of the timelock - right now it is setting them all to a time based lock.

https://github.com/serai-dex/serai/blob/bc1dec79917d37d326ac3d9bc571a64131b0424a/coins/monero/src/transaction.rs#L139

## Boog900 | 2024-04-28T14:38:42+00:00
This needs to handle getting RCT outputs as as well, currently it is only getting pre-rct outputs 

## Boog900 | 2024-04-28T14:39:42+00:00
I think we need to handle RCT outputs here as well  

## Boog900 | 2024-04-28T14:43:15+00:00
yes, however does failing to abort cause data to half write?

## hinto-janai | 2024-04-28T17:12:06+00:00
We're using `read-txn-no-tls` which only allows `Send`. `sync-read-txn` is removed in the version of `heed` we're using. 
 
![image](https://github.com/Cuprate/cuprate/assets/101352116/f741a78b-36aa-4079-bd36-b1d320b4e3df)

![image](https://github.com/Cuprate/cuprate/assets/101352116/12963568-b2c3-4e21-b04c-57693cf6889c)

Sidenote but this would be a good excuse to add `static_assertions` so we assert traits (among other things).

## Boog900 | 2024-04-28T17:23:17+00:00
I should have been more clear, but this is what `HeedTableRo` looks like:

```rust
pub(super) struct HeedTableRo<'tx, T: Table> {
    /// An already opened database table.
    pub(super) db: HeedDb<T::Key, T::Value>,
    /// The associated read-only transaction that opened this table.
    pub(super) tx_ro: &'tx heed::RoTxn<'tx>,
}
```

So impl'ing `Send` for `HeedTableRo` means you are making `heed::RoTxn` `Sync` (in this struct). You could create 2 `HeedTableRo` from the same tx and send one to another thread, then you would have the same tx shared between 2 threads.

## hinto-janai | 2024-04-28T17:37:53+00:00
- `heed`'s abort is infallible and panics if the tx was already commited (API doesn't let us do this so it's unreachable)
- `redb`'s abort is fallible: https://docs.rs/redb/2.0.0/src/redb/transactions.rs.html#939

The above eventually calls https://docs.rs/redb/2.0.0/src/redb/tree_store/page_store/page_manager.rs.html#600-642 which always returns `Ok(())` or panics. So, regardless we will panic if data cannot be rolled back. I think we should keep panics here too as to rely on the signature and not the impl.

## hinto-janai | 2024-04-28T19:18:37+00:00
Ahhh I see, `&T: Send` only if `T: Sync`. I think Mutex is how `redb` does it?

I know this is rough but in our case (non-contended mutex) it's 3-5% slower. I think it's okay unless you preferred something else, although this would allow us to `impl Sync` for our tables too? No more `ThreadLocal`?

```rust
use heed::{types::*, *};
use std::{hint::black_box, sync::Mutex, time::Instant};

fn main() {
    let dir = tempfile::tempdir().unwrap();
    let env = unsafe { EnvOpenOptions::new().open(dir.path()).unwrap() };

    let mut tx_rw = env.write_txn().unwrap();
    let db: Database<Str, Str> = env.create_database(&mut tx_rw, None).unwrap();

    db.put(&mut tx_rw, "a", "").unwrap();
    tx_rw.commit().unwrap();

    const RANGE: std::ops::Range<usize> = 0..10_000_000;

    loop {
        let now = Instant::now();
        for _ in RANGE {
            let tx_ro = black_box(Mutex::new(env.read_txn().unwrap()));
            let _get = black_box(db.get(&tx_ro.lock().unwrap(), "zero")).unwrap();
        }
        println!("mutex: {}", now.elapsed().as_secs_f32());

        let now = Instant::now();
        for _ in RANGE {
            let tx_ro = black_box(env.read_txn()).unwrap();
            let _get = black_box(db.get(&tx_ro, "zero")).unwrap();
        }
        println!("tx_ro: {}", now.elapsed().as_secs_f32());
    }
}

```
```
mutex: 0.8341315
tx_ro: 0.76794577
mutex: 0.7981665
tx_ro: 0.7648049
mutex: 0.7917462
tx_ro: 0.76621574
```

## hinto-janai | 2024-04-28T20:07:49+00:00
If RCT, what number are we supposed to return? The total amount of RCT outputs? `tables.rct_outputs().len()`?

## Boog900 | 2024-04-28T23:54:02+00:00
> although this would allow us to impl Sync for our tables too? No more ThreadLocal?

We would then have contention between threads retrieving data, only one will be able to at a time.

>  I think Mutex is how redb does it

Does this mean only one thread can get data an once? if so I would recommend putting that in a thread local as well 

>  3-5% slower

Not awful, still annoying though, probably our best option  

## Boog900 | 2024-04-28T23:54:33+00:00
yes total amount of RCT outputs

## hinto-janai | 2024-04-29T01:10:39+00:00
`add_tx()` takes in a `TransactionVerificationData`, is there a cheaper way to map our `block.block.miner_tx: Transaction` to it?

`fee` should be `tx.rct_signatures.base.fee`?

## hinto-janai | 2024-04-29T01:14:12+00:00
Clippy gives scary warnings without this allow, pretty sure it's a false positive?

## hinto-janai | 2024-04-29T01:15:44+00:00
Only happens after wrapping it in `Mutex` btw, weird.

## Boog900 | 2024-04-29T12:45:45+00:00
It's because the struct is:
```rust
pub(super) struct HeedTableRo<'tx, T: Table> {
    /// An already opened database table.
    pub(super) db: HeedDb<T::Key, T::Value>,
    /// The associated read-only transaction that opened this table.
    pub(super) tx_ro: Mutex<&'tx heed::RoTxn<'tx>>,
}
```
It needs to be:

 ```rust
 pub(super) struct HeedTableRo<'tx, T: Table> {
    /// An already opened database table.
    pub(super) db: HeedDb<T::Key, T::Value>,
    /// The associated read-only transaction that opened this table.
    pub(super) tx_ro: &'tx Mutex<heed::RoTxn<'tx>>,
}
 ```
 
 Otherwise the mutex isn't actually doing anything. This means you will have to do the same changes that you did to wrap write txs in a refcell :(

## hinto-janai | 2024-04-30T00:15:36+00:00
For future reference:

Discussion in matrix lead to the solution of creating and using a private, separate [`struct UnsafeSendable<T>(T)`](https://github.com/Cuprate/cuprate/pull/113/commits/06a2a869a7d0cc61e115e938578b437173886028) that implements `Send`.

We use it for `HeedTableRo` in [`service`](https://github.com/Cuprate/cuprate/pull/113/commits/08d5715ba96ce663e5c825dee9dcabfe54830d10) where our use-case (inside `ThreadLocal<T: Send>`) is safe.  

## hinto-janai | 2024-04-30T00:38:15+00:00
When returning outputs, how are we supposed to differentiate post-RCT miner outputs? `id_to_output_on_chain()` here checks `if amount == 0 { /* rct */ } else { /*...*/ }` but that skips miner outputs, no?

## hinto-janai | 2024-04-30T00:40:50+00:00
Same here.

## hinto-janai | 2024-04-30T00:42:57+00:00
I think this change is correct? It was causing problems in tests (1st transaction added had index of 1).

## Boog900 | 2024-04-30T01:15:11+00:00
miner txs are stored under 0 as well even though they have a none zero amount so this is ok

## Boog900 | 2024-04-30T01:18:57+00:00
I don't think so, this is shadowing the correct index at the top of this function, was that intentional?

## hinto-janai | 2024-04-30T16:19:44+00:00
Ah... no.

## hinto-janai | 2024-04-30T19:54:18+00:00
`trait DatabaseRo` is now `unsafe`.

## hinto-janai | 2024-04-30T19:55:58+00:00
`TablesIter` is a supertrait of `Tables`, needed since we need those functions sometimes.

Kind of confusing, could separate and do `impl Tables + TablesIter` if you prefer, or rename to something else?

## hinto-janai | 2024-04-30T19:58:08+00:00
Added `miner_tx` before other transactions.

Re https://github.com/Cuprate/cuprate/pull/113#discussion_r1582465439: still not sure if `fee` is correct or if there's a cheaper way to map `Transaction -> TransactionVerificationData`.

## hinto-janai | 2024-04-30T20:01:17+00:00
How `tx_ro` + `tables` will work in the `service` reader thread-pool.

- Both get wrapped in `ThreadLocal`
- `tx_ro` is `Send`
- For `redb`, `tables` is `Send`, normal `ThreadLocal::get_or_try` is used
- For `heed`, `tables` is `!Send`, but gets wrapped in `UnsafeSendable`

usage is...

## hinto-janai | 2024-04-30T20:04:06+00:00
...here.

- Prep the `tx_ro` + `tables` before entering `rayon`
- Set & use those `ThreadLocal`'s within

`tables` here is either `&impl Tables` or `UnsafeSendable::<impl Tables>::as_ref()`.

## hinto-janai | 2024-04-30T20:06:48+00:00
`UnsafeSendable` impl.

The inner `T` is private so the caller must use `unsafe fn new()` and document their usage.

## Boog900 | 2024-04-30T22:23:37+00:00
This is not the same as `HeedTableRo` as write txs are not Send, right?

This is still fine, the comment should probably say that the send bound only applies to read tables 

## Boog900 | 2024-04-30T22:24:43+00:00
I wouldn't say this is a soundness hole against `Sync`:
```suggestion
/// other threads - this would be a soundness hole against `HeedTableRo`.
```

## Boog900 | 2024-04-30T22:31:13+00:00
would these functions be better in `ops/output`?

## Boog900 | 2024-04-30T22:34:15+00:00
fee should just be 0, it might be good to split the inputs to that function up as I don't think we need to clone the tx or calculated it's weight

## Boog900 | 2024-04-30T22:38:27+00:00
Couldn't we just store both redb & heed tables in `UnsafeSendable` the overhead should be nothing and it means we don't need to cfg if this high up in the abstraction. Also means this can be a function I think 

## Boog900 | 2024-04-30T22:38:56+00:00
```suggestion
                        // then 1 transaction could be used to open multiple tables, then sent to
                        // other threads - this would be a soundness hole against `HeedTableRo`.
```

## Boog900 | 2024-04-30T22:39:34+00:00
Did you mean safely here?

```suggestion
                        // Instead, we are safely using `UnsafeSendable` in `service`'s reader
                        // thread-pool as we are pairing our usage with `ThreadLocal` - only 1 thread
                        // will ever access a transaction at a time. This is an INVARIANT.
```

## Boog900 | 2024-04-30T22:41:32+00:00
I think this is meant to return the chain height right?

## Boog900 | 2024-04-30T22:44:54+00:00
I don't think this type needs to be serde

## hinto-janai | 2024-05-01T00:47:51+00:00
Sure, although making it a function means we have to name everything:
```rust
fn get_tables<'a, EnvInner, TxRo, TxRw, Tables>(
    env_inner: &'a EnvInner,
    tx_ro: &'a TxRo,
    tables: ThreadLocal<UnsafeSendable<Tables>>,
) -> Result<&'a Tables, RuntimeError>
where
    EnvInner: crate::EnvInner<'a, TxRo, TxRw>,
    TxRo: crate::TxRo<'a>,
    TxRw: crate::TxRw<'a>,
    Tables: crate::tables::TablesIter,
{/*...*/}
```
not sure how to tell the compiler the returned value's type == `Tables` here.
```
error[E0308]: mismatched types
   --> database/src/service/read.rs:285:82
    |
246 | fn get_tables<'a, EnvInner, TxRo, TxRw, Tables>(
    |                                         ------ expected this type parameter
...
285 |                 Ok(t) => Ok(unsafe { crate::unsafe_sendable::UnsafeSendable::new(t) }),
    |                                      ------------------------------------------- ^ expected type parameter `Tables`, found associated type
    |                                      |
    |                                      arguments to this function are incorrect
    |
    = note: expected type parameter `Tables`
              found associated type `impl tables::TablesIter`
```
Can it just be a macro?

## Boog900 | 2024-05-01T17:23:24+00:00
@hinto-janai I don't think this was changed?

## hinto-janai | 2024-05-01T17:31:17+00:00
Ah, only the `get_tables!()` doc was updated.

I fixed the above and de-duplicated the docs.

## Boog900 | 2024-05-01T17:42:14+00:00
> Can it just be a macro?

yeah, alright  

## Boog900 | 2024-05-01T17:48:18+00:00
I don't think this line is needed anymore 

```suggestion
```

# Action History
- Created by: hinto-janai | 2024-04-23T01:01:35+00:00
- Merged at: 2024-05-01T17:52:20+00:00
