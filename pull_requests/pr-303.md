---
title: 'cuprated: P2P protocol request handler'
source_url: https://github.com/Cuprate/cuprate/pull/303
author: Boog900
assignees: []
labels:
- A-p2p
- A-dependency
- A-workspace
- A-storage
- A-net
- A-types
- A-binaries
created_at: '2024-10-04T01:46:45+00:00'
updated_at: '2024-12-03T20:21:13+00:00'
type: pull_request
status: merged
closed_at: '2024-12-03T20:21:05+00:00'
merged_at: '2024-12-03T20:21:05+00:00'
---

# Original Description

### What
Implements the P2P protocol handler




# Discussion History
## Boog900 | 2024-11-18T16:43:19+00:00
Here's that maker that produces `P2pProtocolRequestHandler`

## Boog900 | 2024-11-18T16:43:42+00:00
Here's the actual P2P protocol request handler 

## hinto-janai | 2024-11-28T01:55:30+00:00
Would this request being named `TxsInBlock` be better?

## hinto-janai | 2024-11-28T02:00:52+00:00
Would these semantics be better represented with `Option<NonZero<usize>>`? Then where it is currently used:

```rust
if start_height.is_none() {
    anyhow::bail!("The peers chain has a different genesis block than ours.");
}
```

## hinto-janai | 2024-11-28T02:04:35+00:00
I see where/how these are being used, would it be possible to change these to `BTreeMap<[u8; 32], usize>` or `Vec<([u8; 32], usize)>` throughout the call-sites?

## hinto-janai | 2024-11-28T02:14:16+00:00
```suggestion
bytes        = { workspace = true }
```

## hinto-janai | 2024-11-28T02:14:47+00:00
```suggestion

//---------------------------------------------------------------------------------------------------- `get_block_blob_with_tx_indexes`
```

## hinto-janai | 2024-11-28T02:28:13+00:00
1/2
```suggestion
    let miner_tx_idx = tables.block_infos().get(block_height)?.mining_tx_index;
```

## hinto-janai | 2024-11-28T02:28:23+00:00
2/2
```suggestion
    monero_serai::io::write_varint(&block_txs.len(), &mut block)
        .expect("The number of txs per block will not exceed u64::MAX");

    let block_txs_bytes = bytemuck::must_cast_slice(&block_txs);
```

## hinto-janai | 2024-11-28T02:34:17+00:00
`rustfmt` also accepts this formatting.
```suggestion
    let idx = block_ids.partition_point(|block_id| {
        match block_exists(block_id, table_block_heights) {
            Ok(exists) => exists & chronological_order,
            Err(e) => {
                err.get_or_insert(e);
                // if this happens the search is scrapped, just return `false` back.
                false
            }
        }
    });
```

## hinto-janai | 2024-11-28T22:53:18+00:00
Are these needed? They are already in-scope.
```suggestion
    let blockchain_height = crate::ops::blockchain::chain_height(tables.block_heights())?;
```

## hinto-janai | 2024-11-28T23:05:13+00:00
`async fn get_chain` in `binaries/cuprated/src/p2p/request_handler.rs` will eventually call this, is it always guaranteed `!block_ids.is_empty()`? If peers can send an empty request I think it will panic at this line.


## hinto-janai | 2024-11-28T23:07:11+00:00
If this PR is merged with main this can be:
```suggestion
        .collect::<DbResult<(Vec<_>, Vec<_>)>>()?;
```

## hinto-janai | 2024-11-28T23:07:32+00:00
```suggestion
        .collect::<DbResult<_>>()?;
```

## hinto-janai | 2024-11-28T23:11:21+00:00
If renamed:
```suggestion
/// [`BlockchainReadRequest::TxsInBlock`]
fn txs_in_block(
    env: &ConcreteEnv,
    block_hash: [u8; 32],
    txs: Vec<u64>,
```

## hinto-janai | 2024-11-28T23:17:52+00:00
+1 on no comments here, I think they could be bundled if like this, I usually separate when there's a bunch of docs, do you have a preference?

```suggestion
    pub blockchain_read_handle: BlockchainReadHandle,
    pub blockchain_context_service: BlockChainContextService,
    pub txpool_read_handle: TxpoolReadHandle,
```

## hinto-janai | 2024-11-28T23:18:50+00:00
Same here, up to you
```suggestion
    peer_information: PeerInformation<N>,
    blockchain_read_handle: BlockchainReadHandle,
    blockchain_context_service: BlockChainContextService,
    txpool_read_handle: TxpoolReadHandle,
    incoming_tx_handler: IncomingTxHandler,
```

## hinto-janai | 2024-11-28T23:27:25+00:00
Would it be better to impl `From<ByteArrayVec<N>> for Vec<[u8; N]>` then this?:
```suggestion
    let block_hashes: Vec<[u8; 32]> = request.blocks.into();
```
There's more `drop()`s below as well.

## hinto-janai | 2024-11-28T23:29:53+00:00
```suggestion
        unreachable!();
```

## hinto-janai | 2024-11-28T23:30:02+00:00
```suggestion
        unreachable!();
```

## hinto-janai | 2024-11-28T23:33:22+00:00
```suggestion
        unreachable!();
```

## hinto-janai | 2024-11-28T23:38:11+00:00
From https://github.com/Cuprate/cuprate/pull/308#pullrequestreview-2463412554:

> [binaries/cuprated/src/rpc/request/*.rs](https://github.com/Cuprate/cuprate/tree/main/binaries/cuprated/src/rpc/request) which are fn versions of our tower::Service's Request -> Response to reduce noise

Since `cuprated` will be using `Service`s like this in general, should I move this folder? Maybe `cuprated/src/service/*.rs`? I think this will reduce noise significantly through `cuprated`, example here would be:
```rust
let context = blockchain_context::context(&mut blockchain_context_service)
    .await
    .expect(PANIC_CRITICAL_SERVICE_ERROR)
    .unchecked_blockchain_context();
```

## hinto-janai | 2024-11-28T23:41:30+00:00
```suggestion
    let NewTransactions { txs, .. } = request;
```

## Boog900 | 2024-12-02T16:39:02+00:00
It's more efficient to keep them like this as the data is sent straight over the wire - we don't use the response in Cuprate.

Needing to build a map or join the values in a vec to then un-join causes unnecessary allocations.

## Boog900 | 2024-12-02T16:53:05+00:00
these are the types not behind a `ThreadLocal`, unlike the others

## Boog900 | 2024-12-02T20:35:33+00:00
The request could have more `Bytes` fields so we still need to make sure the whole request is dropped. 

> Would it be better to impl From<ByteArrayVec<N>> for Vec<[u8; N]> then this?:

Maybe, however the into requires copying the data so doesn't need to be owned - I think a custom function would be better and can be be done in #202

## Boog900 | 2024-12-02T21:05:29+00:00
The line above early returns if `idx == block_ids.len()` which handles this right?

## Boog900 | 2024-12-02T21:07:00+00:00
I have plans to rework this service to make getting the context not require a `Service` request, so for now I don't think so.

## hinto-janai | 2024-12-03T00:25:24+00:00
If `usize` is better here that's ok too, just noting that the `usize // 0 == special_semantics` -> `Option<NonZero<T>>` pattern exists to move the invariant into the type system, also:
```rust
fn main() {
    assert_eq!(size_of::<Option<usize>>(), 16);
    assert_eq!(size_of::<Option<std::num::NonZero<usize>>>(), 8);
}
```
Patch for `Option<NonZero<usize>>`:
```diff
diff --git a/binaries/cuprated/src/p2p/request_handler.rs b/binaries/cuprated/src/p2p/request_handler.rs
index 620c9df..d8fef7f 100644
--- a/binaries/cuprated/src/p2p/request_handler.rs
+++ b/binaries/cuprated/src/p2p/request_handler.rs
@@ -238,7 +238,7 @@ async fn get_chain(
         split_u128_into_low_high_bits(cumulative_difficulty);
 
     Ok(ProtocolResponse::GetChain(ChainResponse {
-        start_height: usize_to_u64(start_height),
+        start_height: usize_to_u64(std::num::NonZero::<usize>::get(start_height)),
         total_height: usize_to_u64(chain_height),
         cumulative_difficulty_low64,
         cumulative_difficulty_top64,
diff --git a/storage/blockchain/src/service/read.rs b/storage/blockchain/src/service/read.rs
index f3e0a65..0263f42 100644
--- a/storage/blockchain/src/service/read.rs
+++ b/storage/blockchain/src/service/read.rs
@@ -634,7 +634,7 @@ fn next_chain_entry(
     };
 
     Ok(BlockchainResponse::NextChainEntry {
-        start_height: Some(first_known_height),
+        start_height: std::num::NonZero::<usize>::new(first_known_height),
         chain_height,
         block_ids,
         block_weights,
diff --git a/types/src/blockchain.rs b/types/src/blockchain.rs
index 58a23b2..7518935 100644
--- a/types/src/blockchain.rs
+++ b/types/src/blockchain.rs
@@ -288,7 +288,7 @@ pub enum BlockchainResponse {
     /// If all blocks were unknown `start_height` will be [`None`], the other fields will be meaningless.
     NextChainEntry {
         /// The start height of this entry, [`None`] if we could not find the split point.
-        start_height: Option<usize>,
+        start_height: Option<std::num::NonZero<usize>>,
         /// The current chain height.
         chain_height: usize,
         /// The next block hashes in the entry.
```
Patch for `usize`:
```diff
diff --git a/binaries/cuprated/src/p2p/request_handler.rs b/binaries/cuprated/src/p2p/request_handler.rs
index 620c9df..adc9be1 100644
--- a/binaries/cuprated/src/p2p/request_handler.rs
+++ b/binaries/cuprated/src/p2p/request_handler.rs
@@ -230,9 +230,9 @@ async fn get_chain(
         unreachable!();
     };
 
-    let Some(start_height) = start_height else {
+    if start_height == 0 {
         anyhow::bail!("The peers chain has a different genesis block than ours.");
-    };
+    }
 
     let (cumulative_difficulty_low64, cumulative_difficulty_top64) =
         split_u128_into_low_high_bits(cumulative_difficulty);
diff --git a/storage/blockchain/src/service/read.rs b/storage/blockchain/src/service/read.rs
index f3e0a65..f14e50e 100644
--- a/storage/blockchain/src/service/read.rs
+++ b/storage/blockchain/src/service/read.rs
@@ -600,7 +600,7 @@ fn next_chain_entry(
     // This will happen if we have a different genesis block.
     if idx == block_ids.len() {
         return Ok(BlockchainResponse::NextChainEntry {
-            start_height: None,
+            start_height: 0,
             chain_height: 0,
             block_ids: vec![],
             block_weights: vec![],
@@ -634,7 +634,7 @@ fn next_chain_entry(
     };
 
     Ok(BlockchainResponse::NextChainEntry {
-        start_height: Some(first_known_height),
+        start_height: first_known_height,
         chain_height,
         block_ids,
         block_weights,
diff --git a/types/src/blockchain.rs b/types/src/blockchain.rs
index 58a23b2..e1a6dc9 100644
--- a/types/src/blockchain.rs
+++ b/types/src/blockchain.rs
@@ -285,10 +285,10 @@ pub enum BlockchainResponse {
 
     /// Response to [`BlockchainReadRequest::NextChainEntry`].
     ///
-    /// If all blocks were unknown `start_height` will be [`None`], the other fields will be meaningless.
+    /// If all blocks were unknown `start_height` will be `0`, the other fields will be meaningless.
     NextChainEntry {
-        /// The start height of this entry, [`None`] if we could not find the split point.
-        start_height: Option<usize>,
+        /// The start height of this entry, `0` if we could not find the split point.
+        start_height: usize,
         /// The current chain height.
         chain_height: usize,
         /// The next block hashes in the entry.
```

## hinto-janai | 2024-12-03T00:27:33+00:00
```suggestion
/// The inner response for a request for txs.
```

## hinto-janai | 2024-12-03T00:28:39+00:00
```suggestion
/// chronologically ordered chains this will return the index of the first known.
```

# Action History
- Created by: Boog900 | 2024-10-04T01:46:45+00:00
- Merged at: 2024-12-03T20:21:05+00:00
