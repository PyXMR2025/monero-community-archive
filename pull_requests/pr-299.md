---
title: 'P2P: remove peer sync service'
source_url: https://github.com/Cuprate/cuprate/pull/299
author: Boog900
assignees: []
labels:
- A-p2p
- A-dependency
- A-workspace
created_at: '2024-09-25T20:58:49+00:00'
updated_at: '2024-09-30T21:15:51+00:00'
type: pull_request
status: merged
closed_at: '2024-09-30T21:15:48+00:00'
merged_at: '2024-09-30T21:15:48+00:00'
---

# Original Description
### What

Removes the peer sync service and instead keeps core sync data stored in `PeerInformation`.

### Why

Removes the need for a separate service.



# Discussion History
## Boog900 | 2024-09-26T18:48:55+00:00
This is the new way for the block downloader to get peers to sync from.

This function is horribly expensive, however in a future PR I have some ideas on how to make it cheaper 

## hinto-janai | 2024-09-27T23:47:48+00:00
On purpose? `pruning_seed` is already in scope.

## hinto-janai | 2024-09-27T23:49:22+00:00
Should this be removed?

## hinto-janai | 2024-09-27T23:58:58+00:00
Is it expensive because of the `Mutex`? Maybe add a `FIXME: ...`.

Also, would the below have the same lifetime semantics? I don't think `Captures` is needed anymore right?

```rust
    /// Borrows multiple [`Client`]s from the pool.
    ///
    /// Note that the returned iterator is not guaranteed to contain every peer asked for.
    ///
    /// See [`Self::borrow_client`] for borrowing a single client.
    pub fn borrow_clients<'a>(
        self: &'a Arc<Self>,
        peers: impl Iterator<Item = InternalPeerID<N::Addr>> + 'a,
    ) -> impl Iterator<Item = ClientPoolDropGuard<N>> + '_ {
        peers.filter_map(|peer| self.borrow_client(&peer))
    }

    /// Borrows all [`Client`]s from the pool that have claimed a higher cumulative difficulty than
    /// the amount passed in.
    ///
    /// The [`Client`]s are wrapped in [`ClientPoolDropGuard`] which
    /// will return the clients to the pool when they are dropped.
    pub fn clients_with_more_cumulative_difficulty(
        self: &Arc<Self>,
        cumulative_difficulty: u128,
    ) -> Vec<ClientPoolDropGuard<N>> {
        let peers = self.clients.iter().filter_map(|element| {
            let peer_sync_info = element.value().info.core_sync_data.lock().unwrap();

            if peer_sync_info.cumulative_difficulty() > cumulative_difficulty {
                Some(*element.key())
            } else {
                None
            }
        });

        self.borrow_clients(peers).collect()
    }
}
```

## hinto-janai | 2024-09-28T00:03:01+00:00
If this lock isn't temporary I would really prefer if it were accompanied with a brief description of its scope; how, where, and who the lock should be used by.

## Boog900 | 2024-09-30T18:54:02+00:00
> Is it expensive because of the Mutex?

Yeah and the `DashMap` which is a bunch of `RwLocks`

> Also, would the below have the same lifetime semantics? I don't think Captures is needed anymore right?

It's not however I would rather keep it as it is as the current way allows more uses.

## Boog900 | 2024-09-30T18:54:33+00:00
oops ..

# Action History
- Created by: Boog900 | 2024-09-25T20:58:49+00:00
- Merged at: 2024-09-30T21:15:48+00:00
