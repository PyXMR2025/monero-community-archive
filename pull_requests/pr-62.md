---
title: 'fcmp++: store indexes for outputs in tree instead of complete `OutputContext`,
  saving 65 bytes/output'
source_url: https://github.com/seraphis-migration/monero/pull/62
author: j-berman
assignees: []
labels: []
created_at: '2025-06-26T01:21:19+00:00'
updated_at: '2025-07-28T22:53:14+00:00'
type: pull_request
status: merged
closed_at: '2025-07-28T22:53:14+00:00'
merged_at: '2025-07-28T22:53:14+00:00'
---

# Original Description
Builds on #49 

For every leaf in the tree, stores `{leaf_idx,output_id}` (16 bytes) instead of `{leaf_idx,output_context}` (81 bytes), saving 65 bytes per leaf. We now read the db for the complete `output_context` when it's needed, using the `output_id` (forward sync does not need to read the `leaves` table for `OutputContext`s).

This saves ~10 GB for both pruned and unpruned nodes for the current tree (150mn outputs * 65 bytes).

Note to @nahuhh: existing testnet nodes cannot run this code, they would break. However, an updated node running this code should be able to sync from scratch pointing to a testnet node not running this code.

### Some comments on the code

I had to modify the curve trees unit test's `grow_tree_db` function to add tx output data to the db, so that it can be read back from the db during `trim_tree` and `audit_tree`.

I removed the `trim_tree_then_regrow` unit test because it broke, and I figure fixing the test is more involved than the test is worth (e.g. moving `BlockchainLMDB::trim_block()` out of `BlockchainLMDB::remove_block()` is probably not ideal), and the `_test_reorg_handling` functional test also tests trimming then regrowing.

Note that`get_path_by_global_output_id` reads for output data twice: once in beginning of its fn, and then again at the end inside `get_path`.

I figure this is ok for now because:
1) `get_path_by_global_output_id` is not a commonly called fn, used only to serve the `COMMAND_RPC_GET_PATH_BY_GLOBAL_OUTPUT_ID_BIN` endpoint.
2) The duplication should not be a significant slowdown on those operations even if read from disk.
3) LMDB takes advantage of the OS buffer cache ([1](https://www.symas.com/mdb), [2](https://tldp.org/LDP/sag/html/buffer-cache.html)), which means the duplicate reads may end up being read from memory anyway.

I think this fairly simple PR is a solid win that makes sense to take as is for now.


# Discussion History
## jeffro256 | 2025-07-24T18:38:38+00:00
Maybe I'm missing something. Isn't this saving (`sizeof(OutputContext::output_idx)` + `sizeof(OutputContext::torsion_checked)` + (`sizeof(OutputPair::output_pubkey)` + `sizeof(OutputPair::commitment(`)) - `sizeof(mdb_leaf::output_id)` = (8 + 1 + (32 + 32)) - 8 = 65 bytes per output, not 57?

## j-berman | 2025-07-24T19:11:36+00:00
Whoops, yep! Nice catch

## jeffro256 | 2025-07-24T20:15:23+00:00
Why not collect this information from the `output_amounts` table? It has the output pubkey and amount commitment stored, and you're passed the global output ID. It would be way, way faster, with fewer reads, fewer indirections, no transaction deserializations, and no additional transparent amount commitment multiplies. I guess the only piece of information that you're missing is `torsion_checked`, hmm.....

What do you think of this: instead of checking for torsion only on carrot-output transactions, what if we do it for every single output in v2 transactions on or after hard fork v17? We then always assume that v1 output pubkeys have not been checked for torsion (v1 amount commitments need not be checked for torsion since we calculate them), but all v2 output pubkeys and amount commitments with output ID >= first output ID on HF v17 can be treated as checked for torsion. We can find the first output ID on HF v17 by looking at the `bi_cum_rct` field of `mdb_block_info` at the height of the first v17 block.

## j-berman | 2025-07-24T21:18:35+00:00
This is the *global* output id, so it would be a few more reads before being able to read `output_amounts` using a global output id (`output_txs` -> `tx_indicies` -> `tx_outputs` -> `output_amounts`)

## jeffro256 | 2025-07-25T00:31:28+00:00
By global output ID, do you mean the index used in ring signatures lookup AKA the keys to the `output_amounts` table in the DB? Or is there some other "global index" for outputs that I'm missing?

If we're talking about the same method of indexing, why go `output_txs` -> `tx_indicies` -> `tx_outputs` -> `output_amounts` when you can just go directly to `output_amounts`?

## j-berman | 2025-07-25T02:40:17+00:00
> Or is there some other "global index" for outputs that I'm missing

There are "amount output id's" and "global output id's"

The "amount output id" is the unique id assigned to an output of a particular amount (e.g. there might be one pre-RCT output with amount 100000 with "amount output id" 5, and then a post-RCT output of amount 0 with "amount output id" 5 as well).

Ring members (and outputs in `output_amounts`) are indexed by the amount and amount output id's, because the amount is available on the ring.

The "global output id" is the globally unique id assigned to every output as they enter the chain. That's what this function is using, and is what's used for outputs inserted in to the tree (otherwise we would need to additionally store the amount too)

## jeffro256 | 2025-07-25T17:34:14+00:00
Ahhhh, yeah I forgot about those. Personally, I won't call those "global output IDs" though, since that term is already used for what you call "amount output IDs". See:

- https://github.com/monero-project/monero/blob/fbc242d52d89d9c8021194cd4faae657c94d5a31/src/blockchain_db/lmdb/db_lmdb.cpp#L4221
- https://github.com/monero-project/monero/blob/fbc242d52d89d9c8021194cd4faae657c94d5a31/src/cryptonote_core/blockchain.h#L545
- https://github.com/monero-project/monero/blob/fbc242d52d89d9c8021194cd4faae657c94d5a31/src/cryptonote_core/blockchain.h#L566
- https://github.com/monero-project/monero/blob/fbc242d52d89d9c8021194cd4faae657c94d5a31/src/cryptonote_core/blockchain.h#L585-L598
- https://github.com/monero-project/monero/blob/fbc242d52d89d9c8021194cd4faae657c94d5a31/src/rpc/core_rpc_server_commands_defs.h#L507
- https://github.com/monero-project/monero/blob/fbc242d52d89d9c8021194cd4faae657c94d5a31/src/wallet/wallet2.h#L340
- https://github.com/monero-project/monero/blob/fbc242d52d89d9c8021194cd4faae657c94d5a31/src/wallet/wallet2.h#L396
- https://github.com/monero-project/monero/blob/fbc242d52d89d9c8021194cd4faae657c94d5a31/src/wallet/wallet2.cpp#L9001

There are a handful places in `BlockchainLMDB` that refer to the DB-specific output IDs as "global" which makes the whole situation more confusing lol. 

> ... (otherwise we would need to additionally store the amount too)

I think that it may be worth it in this case. We can get `OutputContext`s with one DB read, we can skip amount commitment torsion checking if the cleartext amount != 0, and we would still be saving 57 bytes / output. 

## j-berman | 2025-07-25T17:51:00+00:00
> There are a handful places in BlockchainLMDB that refer to the DB-specific output IDs as "global" which makes the whole situation more confusing lol.

Yes :/

> I think that it may be worth it in this case. We can get OutputContexts with one DB read, we can skip amount commitment torsion checking if the cleartext amount != 0, and we would still be saving 57 bytes / output.

I don't think it's worth it because this function is also a rarely called function, only used for small contained reads (last path when trimming the tree, last path when serving wallets first request on restore, serving the path by global output ID RPC), and the DB reads in this PR shouldn't be *that* slow regardless

## j-berman | 2025-07-25T17:57:46+00:00
Long term, I think it would probably make more sense to restructure the db to stop storing future outputs indexed by amount + amount output ID in the `output_amounts` table, and to instead index by global output ID. There isn't a need to query by amount + amount output ID after FCMP++, it will only be useful for verifying rings before the fork.

I figure this PR is a reasonable route to v1 as is. Because the above would be a pretty significant re-working I think.

# Action History
- Created by: j-berman | 2025-06-26T01:21:19+00:00
- Merged at: 2025-07-28T22:53:14+00:00
