---
title: Add a test for an inbound handshake from monerod
source_url: https://github.com/Cuprate/cuprate/pull/75
author: Boog900
assignees: []
labels:
- A-p2p
- A-test-utils
created_at: '2024-02-25T14:01:20+00:00'
updated_at: '2024-05-27T01:10:02+00:00'
type: pull_request
status: merged
closed_at: '2024-02-25T21:21:26+00:00'
merged_at: '2024-02-25T21:21:25+00:00'
---

# Original Description
No description

# Discussion History
## hinto-janai | 2024-02-25T17:53:44+00:00
I think CI will cache this? [`db_directory` is in `target/`](https://github.com/Cuprate/cuprate/actions/runs/8038494435/job/21954497396#step:11:360), so either `db_location` needs to be set elsewhere or this has to be removed.

## hinto-janai | 2024-02-25T17:57:55+00:00
```suggestion
    let db_location = tempfile::tempdir().unwrap();
```
`tempfile` could be added to the workspace too, I think we'll be using it elsewhere.

Helps the `remove_dir_all` problem too, since this directory usually gets wiped on OS reboot. 

## hinto-janai | 2024-02-25T18:01:48+00:00
```suggestion
        .arg(format!(r"--data-dir='{}'", db_location.display()))
```
Not sure if necessary, but `tempfile` might return a PATH with spaces.

## hinto-janai | 2024-02-25T18:02:30+00:00
```suggestion
        .arg(format!("--p2p-bind-port={p2p_port}"))
        .arg(format!("--rpc-bind-port={rpc_port}"))
        .arg(format!("--zmq-rpc-bind-port={zmq_port}"))
```

## Boog900 | 2024-02-25T19:47:32+00:00
I moved the folder out of target after the fail

## Boog900 | 2024-02-25T20:08:56+00:00
I couldn't get `tempfile` to work - it wasn't using `/tmp` and wasn't dlelting the dir when finished like to docs say so I just went with stds `temp_dir`

## Boog900 | 2024-02-25T20:12:08+00:00
Not using `tempfile` so I don't think this is necessary 

## hinto-janai | 2024-02-25T20:23:06+00:00
`tempfile::tempdir()` returns the same directory as `temp_dir` since it uses it internally. 

It's just a test though so `temp_dir` is ok.

## Boog900 | 2024-02-25T20:24:41+00:00
thats what the docs say but it was putting the dir in `monero-p2p`

## hinto-janai | 2024-02-25T20:28:51+00:00
`temp_dir` is the same with `TMPDIR='/path/with space'` (I don't do that but someone might)

## hinto-janai | 2024-02-25T20:34:16+00:00
Hmm I doubt `tempfile` is incorrect, it just calls [`temp_dir`](https://docs.rs/tempfile/latest/src/tempfile/lib.rs.html#597). Maybe some CI env problem.

## Boog900 | 2024-02-25T21:01:22+00:00
This issue was because of 
```rust
 .arg(format!(r"--data-dir='{}'", db_location.display()))
 ```
 this was making monerod put the file in the same folder that we are currently in

## Boog900 | 2024-02-25T21:01:58+00:00
I tested this with a space and it works without `` 

## hinto-janai | 2024-02-25T22:19:12+00:00
https://doc.rust-lang.org/std/process/struct.Command.html#method.arg
> This means that shell syntax like quotes, escaped characters, word splitting, glob patterns, variable substitution, etc. have no effect.

Yeah nvm I was wrong, too used to shell.

# Action History
- Created by: Boog900 | 2024-02-25T14:01:20+00:00
- Merged at: 2024-02-25T21:21:25+00:00
