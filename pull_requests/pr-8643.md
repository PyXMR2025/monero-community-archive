---
title: 'p2p: fix exclusive node DNS resolution for certain hosts'
source_url: https://github.com/monero-project/monero/pull/8643
author: jeffro256
assignees: []
labels: []
created_at: '2022-11-16T22:07:54+00:00'
updated_at: '2023-01-11T21:10:22+00:00'
type: pull_request
status: merged
closed_at: '2023-01-11T16:39:57+00:00'
merged_at: '2023-01-11T16:39:57+00:00'
---

# Original Description
Fixes #8633. The function `append_net_address` did not parse hostname + port addresses (e.g. `bar:29080`) correctly if the hostname did not contain a `.` character.

# Discussion History
## vtnerd | 2022-11-17T15:24:51+00:00
This really should be a lambda. Its about the same number of characters and isn't hijacking the preprocessor. If the concern is over line numbers then `SCOPED_TRACE(addr);` can track what failed, but even then it should be obvious based on the `exp_host` and `exp_port` output.

## vtnerd | 2022-11-17T15:29:51+00:00
I hate bikeshedding, but this really should be `TEST(get_network_address, ipv6)` to be consistent with the other test functions. This is helpful when filtering or identifying tests quickly.

## vtnerd | 2022-11-17T15:31:38+00:00
These could be in a separate `TEST(get_network_address, hostname)` test function. Or just merge everything into one function, its not that much work. Again this helps with quick identification and filtering.

Although, I'm not sure that these tests matter that much given that there is nothing special for `onion` or `i2p` addresses. This isn't strictly an internal blackbox either; `get_network_address_and_port` has `std::string` out parameters that don't provide anything unique for those special TLDs.

## vtnerd | 2022-11-17T15:37:54+00:00
`address.find_first_of(':') != std::string::npos` is a bit better, its a builtin function to the type and breaks on first character instead of always scanning the entire string. The above `if` can be bumped to `find_first_of` too, this was added in C++11 and I'm not sure the original author was aware of the new function.

## vtnerd | 2022-11-17T15:42:14+00:00
This is the main one worth doing, use the builtins!

## vtnerd | 2022-11-17T15:43:06+00:00
Wait this is a `>1`, let me check the spec on that.

## jeffro256 | 2022-11-17T15:45:37+00:00
That doesn't do the same though: this line is asking for *more than* 1 match since all valid IPv6 address must have at least 2 colons. 

## vtnerd | 2022-11-17T15:49:43+00:00
Damn, I needed my coffee. Sorry man. `find_first_of` takes a list of characters, etc. Yikes. And at least two `::` are clearly needed.

The other comments are trivial bikeshedding, but helpful during maintenance.


## vtnerd | 2022-11-17T15:51:07+00:00
> That doesn't do the same though: this line is asking for more than 1 match since all valid IPv6 address must have at least 2 colons.

I was replying to myself during this thread because I couldn't see your responses in real time. So, I was simultaneously writing the same thing. Weirdly quick on your part though to respond! I need to set better email filtering/alerts myself.

## jeffro256 | 2022-11-17T15:54:23+00:00
Haha no worries! You just so happened to catch me during a rare time where I was reading my emails lol

## jeffro256 | 2022-11-17T18:08:19+00:00
Nah that isn't bikeshedding, it does make the tests easier to categorize when they fail.

# Action History
- Created by: jeffro256 | 2022-11-16T22:07:54+00:00
- Merged at: 2023-01-11T16:39:57+00:00
