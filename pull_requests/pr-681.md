---
title: 'pruning: add pruning option'
source_url: https://github.com/Cuprate/cuprate/pull/681
author: p4xxus
assignees: []
labels:
- A-p2p
- A-dependency
- A-workspace
- A-storage
- A-net
- A-pruning
- A-types
- A-binaries
created_at: '2026-08-11T19:41:36+00:00'
updated_at: '2026-09-03T19:10:49+00:00'
type: pull_request
status: open
closed_at: null
merged_at: null
---

# Original Description
<!--
If your pull request is long and/or has sections
that need clarifying, consider leaving a review on
your own PR with comments explaining the changes.
-->

### What
<!--
If applicable, close a related issue with: 

Fixes #<BUG_ISSUE_NUMBER>

...or...

Closes #<FEATURE_ISSUE_NUMBER>
-->

Closes #657 

<!-- Describe the pull request in detail. -->
- a new metadata file stores the pruning seed, which is accessible via the `db` struct
- a new db entry called `prunable_tip` contains the transactions of the top 5500 blocks by TxId to blob
- if pruned:
  - new blocks are added in the prunable tip
  - retrieval of txs and blocks searches in this tip

### Why
<!-- If applicable, describe why this pull request exists. -->
to use less space for a monero node

### Where
<!-- If applicable, describe the places this pull request affects. -->
- DB: 2 new fields (`prunable_tip` (`Keyspace`) and `metadata` (`Metadata`)), updated `prunable_blob` to use optional tape for prunable part
- config options
- cli argument
- adding and retrieving blocks as well as transactions
- `ContextCache`
- persited file structure: new metadata file containing pruning seed and db version (= 0)

### How
<!-- If applicable, describe how this pull request works. -->
- a new metadata file stores the pruning seed
  - the seed is accessible via the `db` struct
  - it's populated via a config option in the config file or via a cli arg
- if the metadata file says not prune but the config / cli arg says prune, create a random seed, add the top 5500 blocks to the prunable tip and delete the other tapes

incoming block:
- if we prune:
  - only add the blob to the desired stripe
  - remove old txs in the `prunable_tip` so after adding the new blocks we're back to 5500 blocks
  - add the new blocks to the `prunable_tip`

retrieval of tx (`read_prunable_tape`):
- if we prune:
  - if the tx is in the top 5500 blocks, look in the prunable_tip
  - otherwise, return NotFound error

I used Gemini Pro 3.1 to find bugs and general help. It was thoroughly reviewed though.

# Discussion History
## Boog900 | 2026-08-11T20:25:12+00:00
```suggestion
        /// Whether to prune the blockchain. This reduces the size of the blockchain by removing most transaction proof data.
```

## Boog900 | 2026-08-11T20:26:12+00:00
Can we make this the type from the cuprate_pruning crate

## Boog900 | 2026-08-11T20:26:41+00:00
and here

## Boog900 | 2026-08-11T20:29:28+00:00
`PruningSeed::decompress` handles the 0 case

## Boog900 | 2026-08-11T20:36:24+00:00
This is not needed as we can just use the `pruning_seed` from the other function

## Boog900 | 2026-08-11T20:38:39+00:00
This isn't needed too

## Boog900 | 2026-08-11T20:40:10+00:00
The service interface is a lot of overhead for this, IMO it should just be stored in the database struct with a getter function.

## Boog900 | 2026-08-11T20:41:20+00:00
This breaks atomic writes, you should move the logic from `add_blocks_to_prunable_tip` into `add_block_to_dynamic_tables`

## Boog900 | 2026-08-11T20:46:07+00:00
IMO this should be broken up into 2 ...

## Boog900 | 2026-08-11T20:58:10+00:00
This should be moved to: `add_block_to_dynamic_tables`. We should calculate the `tx_id_to_remove_from..tx_id_to_remove_to` outside the function call and pass it in like we do for `numb_transactions`. Here is how you can do it with just the tapes:

```rust
tapes.read_entry(
                    &db.block_infos,
                    usize_to_u64(/*height we want to remove from*/),
                )?.map_or(0, |prev| prev.mining_tx_index)
..
tapes.read_entry(
                    &db.block_infos,
                    usize_to_u64(/*lowest height we want to keep*/),
                )?.map_or(0, |prev| prev.mining_tx_index)
```



## Boog900 | 2026-08-11T20:59:55+00:00
This should be put into `add_tx_info_to_dynamic_tables`. That already has the `tx_id`. We should just add every tx if pruning. When writing blocks we wont every write more than `CRYPTONOTE_PRUNING_TIP_BLOCKS`

## p4xxus | 2026-08-12T19:16:54+00:00
isn't this much more inefficient?
Currently I'm removing from the lowest TxId in the `prunable_tip` up until the calculated one, having in total basically 2 db lookup (block and TxId retrieval, although it can be brought down to 1 by reading in block_infos as you said here). 

If I'd do as you suggested, I would need to calculate the lowest height we want to keep for each block, (I can still delete from the lowest txid upwards), thus having 1 DB lookup per Block.
Or am I missing sth?

## p4xxus | 2026-08-12T20:36:11+00:00
I don't quite understand. How can I get access here to the pruning seed via another way?

## Boog900 | 2026-08-13T10:26:29+00:00
hmm yeah `add_block_to_dynamic_tables` takes each block individually. I was thinking we can just calculate the whole range to remove across all blocks. 

I think we should add a new function `add_blocks_to_dynamic_tables` that just calls `add_block_to_dynamic_tables` but does this range removal first and then call that new function in the `write_blocks` function.

When rebuilding fjall you will still need to use the current `add_block_to_dynamic_tables` but that should be ok as we don't necessarily have the pruning data anyway (see here: https://github.com/Cuprate/cuprate/issues/657 `Make consistent`). 

## Boog900 | 2026-08-13T10:33:03+00:00
Like this:
```rust
impl BlockchainDatabase {
    pub fn pruning_seed(&self) -> PruningSeed {
    }
}
```

# Action History
- Created by: p4xxus | 2026-08-11T19:41:36+00:00
