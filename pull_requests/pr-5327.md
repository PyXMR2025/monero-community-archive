---
title: 'New interactive daemon command ''print_net_stats'': Global traffic stats'
source_url: https://github.com/monero-project/monero/pull/5327
author: rbrunner7
assignees: []
labels: []
created_at: '2019-03-21T11:02:04+00:00'
updated_at: '2019-04-01T15:32:14+00:00'
type: pull_request
status: merged
closed_at: '2019-04-01T15:32:14+00:00'
merged_at: '2019-04-01T15:32:14+00:00'
---

# Original Description
The Monero daemon lacks so far an interactive command to show info about total network traffic since startup. (The `print_cn` command only print details per connection, and only about currently existing ones, not about terminated ones.)

This PR implements a new command `print_net_stats` to finally provide such a feature.

Sample output:

    Received 7354450 bytes (7.01 MB) in 4682 packets, average 1.39 kB/s = 0.02% of the limit of 8.00 MB/s
    Sent 49976456 bytes (47.66 MB) in 4705 packets, average 9.47 kB/s = 0.46% of the limit of 2.00 MB/s

The interactive command is based on new RPC commands that could be used e.g. to automatically monitor daemons and make sure that traffic does not go "through the roof".

# Discussion History
## moneromooo-monero | 2019-03-21T11:09:17+00:00
Is this needed ? The loop below should work without this special case (probably changed a bit).

## moneromooo-monero | 2019-03-21T11:13:20+00:00
Does adding in and out make sense ?

## moneromooo-monero | 2019-03-21T11:14:02+00:00
Make it restricted

## moneromooo-monero | 2019-03-21T11:15:23+00:00
Why would you have two of them ?

## moneromooo-monero | 2019-03-21T11:16:00+00:00
bump RPC minor version

## rbrunner7 | 2019-03-21T15:47:19+00:00
Done.

## rbrunner7 | 2019-03-21T15:47:41+00:00
Done.

## rbrunner7 | 2019-03-21T15:51:06+00:00
Not sure what you mean. Why I made both `on_get_net_stats_json` and `on_get_net_stats`? If yes, I just made both because I saw that other calls are duplicated in the same way.

## rbrunner7 | 2019-03-21T15:55:58+00:00
I mainly added total in+out to make clear what the percent numbers on the same line refer to. I'm torn whether it's better to keep it for that purpose or zap it because you are right and it's not a very expressive number ...

## rbrunner7 | 2019-03-21T15:59:18+00:00
Yeah, that special case sure is not elegant, but if I want to format numbers below 1024 in a different way i.e. without 2 decimals I think I need to have an "if" somewhere, and it does not matter much where it is. But maybe I am just lacking a good idea?

## moneromooo-monero | 2019-03-21T17:18:32+00:00
Which ones ? AFAIK it's only get_info (and I'm planning to merge them at some point).

## moneromooo-monero | 2019-03-21T17:18:49+00:00
Ah, OK.

## moneromooo-monero | 2019-03-21T17:30:48+00:00
I felt like trying it :P 

<pre>
#include <stdio.h>
#include <stdlib.h>


int main(int argc, const char **argv)
{
  double L;
  static const char *units[] = { "B", "kB", "MB", "GB", "TB"};

  if (argc <= 1)
  {
    printf("parameter needed\n");
    return 1;
  }
  int unit_idx = 0;
  L = strtoull(argv[1], NULL, 10);
  int p = 0;
  while (L >= 1024)
  {
    L /= 1024;
    ++unit_idx;
    p = 2;
  }
  printf("%.*f %s\n", p, (float)L, units[unit_idx]);

  return 0;
}

</pre>


## vtnerd | 2019-03-21T20:40:13+00:00
`std::to_string(bytes)` is slightly preferable in this context.

## vtnerd | 2019-03-21T20:51:26+00:00
I think the overload should be dropped (as per @moneromooo-monero comment), but if somehow the duplication needs to remain have this call into the other function. There's no need to literally duplicate.

Also, the `restricted` case should apply to this as well, otherwise its a "backdoor" into getting the data.

## vtnerd | 2019-03-21T20:53:46+00:00
How is this field useful? Is it required for each API call or something? I guess its basically useless convention?

## vtnerd | 2019-03-21T20:56:14+00:00
Some good shedding - why is this variable all caps? Its not a macro.

And as per the comment, base-2 prefixes are `KiB`, `MiB`, etc. I don't see a harm in using that here to start making things more correct.

Although typically network speeds use base-10 ....

## vtnerd | 2019-03-21T20:57:08+00:00
I suspect that `(boost::format(%.2f %s) % bytes_d % SIZES[div]).str()` would have less allocations since format can incorporate the extra string.

## vtnerd | 2019-03-21T21:11:36+00:00
I don't know if its worth the effort but:

```c++
std::string get_human_readable(const std::uint64_t bytes)
{
    struct byte_map
    {
        const char* const format;
        const std::uint64_t bytes;
    };

    static constexpr const byte_map sizes[] =
    {
        {"%.0f B", 1024},
        {"%.2f KiB", 1024 * 1024},
        {"%.2f MiB", std::uint64_t(1024) * 1024 * 1024},
        {"%.2f GiB", std::uint64_t(1024) * 1024 * 1024 * 1024},
        {"%.2f TiB", std::uint64_t(1024) * 1024 * 1024 * 1024 * 1024}
    };

    struct bytes_less
    {
        bool operator()(const byte_map& lhs, const byte_map& rhs) const noexcept
        {
            return lhs.bytes < rhs.bytes;
        }
    };

    const auto size = std::upper_bound(
        std::begin(sizes), std::end(sizes) - 1, byte_map{"", bytes}, bytes_less{}
    );
    const std::uint64_t divisor = size->bytes / 1024;
    return (boost::format(size->format) % (double(bytes) / divisor)).str();
}
```
would reduce the math cycles being done.

EDIT: Changed the code so the entire struct/constant is in the function, because why not.

EDIT2: Ok this one you can trust, since I actually tested it. Had to use upper_bound and change constants.

## vtnerd | 2019-03-22T03:17:43+00:00
I'm not sure what to make of that total number either - its an odd stat.

## rbrunner7 | 2019-03-24T13:48:22+00:00
Ok. I deleted the complete third line. Should be easy enough from the first two to see in which direction there is more traffic.

## rbrunner7 | 2019-03-24T13:54:00+00:00
You are of course right. I jumped at a conclusion after seeing that single "get_info" example.

I deleted the second i.e. the "_json" method.

## rbrunner7 | 2019-03-24T14:10:39+00:00
Hmm, yes, pretty clever how you use the capabilities of `printf` here :)

There seems to be no corresponding trick with `boost::format` however, and I don't think the problem is bad enough to merit a fallback to pure C ...

## rbrunner7 | 2019-03-24T14:12:28+00:00
Duplication deleted completely. It was nothing behind it except me jumping to conclusions ...

## moneromooo-monero | 2019-03-24T14:19:38+00:00
It's convention. It's sometimes used to convey "OK, but you may want to know about this" information, or when the daemon is busy. The wallet RPC does not have such a field.

## moneromooo-monero | 2019-03-24T14:21:38+00:00
Sure. Also it did not check for overflow in case of petabytes etc :)

## rbrunner7 | 2019-03-24T14:22:28+00:00
I switched from `SIZES` to `sizes`.

About your proposal to start the "migration" to proper 10-based units with this: I am more a follower of the "make one big switch with everything at once" strategy than "migrate bit-by-bit until finally you get everything over" ...

And maybe this switch would be more controversial than both of us would ever expect :)

## rbrunner7 | 2019-03-24T14:25:08+00:00
Sounds plausible. Adopted.

## rbrunner7 | 2019-03-24T14:27:32+00:00
Yes, I just did for my new call what everybody did with the existing calls already, so while it may or may not make sense it's at least kept consistent.

## rbrunner7 | 2019-03-24T14:29:04+00:00
Changed.

## rbrunner7 | 2019-03-24T14:37:22+00:00
Wow ... this leaves me somewhat speechless.

Well, if we ever need a *high speed* version of that method, and added complexity and probably also added temporary memory consumption no problem, I guess this could be it ...

## vtnerd | 2019-03-24T15:02:36+00:00
There is no temporary memory consumption. Everything is done in place. I've disassembled this, so I am very confident in that.

And what complexity? This is standard STL usage with a bit of math.

## rbrunner7 | 2019-03-24T15:57:33+00:00
Well then. I use your method now because it would be waste of your effort otherwise, and it does have a certain elegance.

But frankly, if you compare a single dead-simple loop with something that needs a `struct` definition, a static lookup table, another `struct` definition with one method, and some clever / non-trivial iteration over a map using all this, and then you ask "What [added] complexity?" somebody who does not eat, drink, and dream about STL probably won't understand you.



## vtnerd | 2019-03-28T02:44:45+00:00
If the struct comparator + `upper_bound` was abhorrent, a loop over the struct elements would suffice. Logarithmic bounded lookups will not make much difference with a list this small, might even be a slight kill on perf. The bigger change is the pre-computation of the ranges.

And usage of `sort`, `binary_search`, `lower_bound`, and `upper_bound` in some ways seems fundamental to C++. I have found them to be useful tools across several contexts/problems.

> and some clever / non-trivial iteration over a map using all this, and then you ask "What [added] complexity?" somebody who does not eat, drink, and dream about STL probably won't understand you.

No map is ever created; a sorted array/vector is a binary tree with the root in the middle and its children split halfway on either "side", etc. The iteration with `upper_bound` is somewhat trivial, it is a binary search using fixed memory locations.

# Action History
- Created by: rbrunner7 | 2019-03-21T11:02:04+00:00
- Merged at: 2019-04-01T15:32:14+00:00
