---
title: 'RPC: Stream timeout (hardening pt.1)'
source_url: https://github.com/Cuprate/cuprate/pull/675
author: SyntheticBird45
assignees: []
labels:
- A-dependency
- A-workspace
- A-binaries
created_at: '2026-08-07T22:09:06+00:00'
updated_at: '2026-08-14T21:11:04+00:00'
type: pull_request
status: merged
closed_at: '2026-08-14T21:11:04+00:00'
merged_at: '2026-08-14T21:11:04+00:00'
---

# Original Description
> [!NOTE]
> This PR has been split. Some of the reviews reference code that has been removed from the branch.

# What

This PR replace the `axum::serve` usage in `cuprated` by a custom hyper serving loop under an `RpcServer` struct. By doing so we can wrap connection's `TcpStream` into a timeount wrapper struct called `WriteTimeout`. This allows us to kick connections to which we cannot send bytes after a configurable amount of time.

This PR also introduce a header read and body read timeout. The first will abort the connection. The latter will first respond with a `400 Bad Request` response and then abort the connection.

This is an important mitigation against inactive/keep-alive/unidirectional spamming connections.

# Discussion History
## Boog900 | 2026-08-09T21:09:12+00:00
This needs to be `IpAddr` not `SocketAddr`

## Boog900 | 2026-08-09T21:11:23+00:00
The docs need updating 

## Boog900 | 2026-08-09T21:14:26+00:00
Can we remove this

## Boog900 | 2026-08-09T21:14:50+00:00
Then can this just build manually 

## Boog900 | 2026-08-09T21:17:06+00:00
this needs to be removed

## Boog900 | 2026-08-10T11:32:18+00:00
10 is low IMO, I think 30 would be better 

## Boog900 | 2026-08-10T12:59:29+00:00
This needs to be moved up and out of this `if let`

## Boog900 | 2026-08-10T13:01:15+00:00
IMO the timer should not be reset after every _byte_ has been read, it should reset once clear

## SyntheticBird45 | 2026-08-10T14:45:32+00:00
ok

## redsh4de | 2026-08-10T17:45:37+00:00
this line needs to be moved back in the spawn after awaiting the run, otherwise it will log on startup every time after spawn

```suggestion
        launch_ctx.task_executor.spawn(async move {
            if let Err(e) = rpc_server.run(shutdown_token).await {
                error!(restricted, "RPC server error: {e:#}");
            }

            info!(restricted, "RPC server shut down.");
        });
```

## redsh4de | 2026-08-11T23:43:26+00:00
can't panic here, will kill the node

```suggestion
                        let Ok(bytes) = axum::body::to_bytes(body, usize::MAX).await else {
                              return Ok(Response::builder()
                                  .status(StatusCode::BAD_REQUEST)
                                  .body(Body::empty())
                                  .unwrap());
                         };
```

## redsh4de | 2026-08-12T00:01:16+00:00
We can apply something like this in order to cover incomplete requests

```suggestion
/// The accounting of an in-flight request, debited from the IP address'
/// budgets once the request is served or dropped.
struct Debit {
    /// The instant the request entered the service.
    start: Instant,
    ip: IpAddr,
    cache: Arc<RpcRateLimitCache>,
}

impl Debit {
    /// Debit the response, consuming this `Debit`: its
    /// [`Drop`] pays for the processing time.
    fn settle(self, response: Response<Body>) -> Response<Body> {
        // A response of known size is debited in full, the node
        // has already paid to produce it.
        if let Some(size) = response.body().size_hint().exact() {
            self.cache
                .debit_size(&self.ip, i64::try_from(size).unwrap_or(i64::MAX));

            return response;
        }

        // Otherwise the bytes are counted as they stream out.
        let (ip, cache) = (self.ip, Arc::clone(&self.cache));
        response.map(|body| Body::new(DebitBody::new(body, cache, ip)))
    }
}

impl Drop for Debit {
    fn drop(&mut self) {
        let millis = i64::try_from(self.start.elapsed().as_millis()).unwrap_or(i64::MAX);
        self.cache.debit_time(&self.ip, millis);
    }
}

pin_project! {
    #[project = ResponseFutureProj]
    pub(super) enum ResponseFuture<F> {
        /// The request was rate limited: respond `429 Too Many Requests`.
        RateLimited,
        /// The request is being served, its processing time and response size
        /// measured and debited from the IP address' budgets.
        Future {
            #[pin]
            future: F,
            debit: Option<Debit>,
        },
    }
}

impl<F, E> Future for ResponseFuture<F>
where
    F: Future<Output = Result<Response<Body>, E>>,
{
    type Output = Result<Response<Body>, E>;

    fn poll(self: Pin<&mut Self>, cx: &mut Context<'_>) -> Poll<Self::Output> {
        match self.project() {
            ResponseFutureProj::RateLimited => Poll::Ready(Ok(Response::builder()
                .status(StatusCode::TOO_MANY_REQUESTS)
                .body(Body::empty())
                .unwrap())),
            ResponseFutureProj::Future { future, debit } => {
                let response = ready!(future.poll(cx))?;

                // The request has finished processing. Time to debit its budget.
                Poll::Ready(Ok(match debit.take() {
                    Some(debit) => debit.settle(response),
                    None => response,
                }))
            }
        }
    }
}
```

## redsh4de | 2026-08-12T08:09:39+00:00
why not have 0 mean no limits?

itd be conflicting if rpc is enabled but nobody can make requests imo

## redsh4de | 2026-08-12T08:14:07+00:00
multiplication before division

```suggestion
        let refill_size = budget.income_size.saturating_mul(elapsed) / 1000;
        let refill_time = budget.income_time.saturating_mul(elapsed) / 1000;
```

## redsh4de | 2026-08-12T08:47:14+00:00
server could die on a failed accept:

```suggestion
                    let (socket, remote_addr) = match res {
                          Ok(res) => res,
                          Err(e) => {
                              // A failed accept must not take the server down.
                              warn!("Failed to accept RPC connection: {e}");
                              tokio::time::sleep(ACCEPT_BACKOFF).await;
                              continue;
                          }
                      };
```

## SyntheticBird45 | 2026-08-12T09:43:38+00:00
Because I am retarded and I forgot to remove that doc back when I was frustrated with 0 to max conversions. Will fix that

## SyntheticBird45 | 2026-08-12T10:23:49+00:00
I don't really like this. This adds another struct and that becomes confusing with DebitBody imo. This is also confusing to me. You are saying this would account for incomplete request but this is only debiting the response. I don't see a difference in logic here.

## redsh4de | 2026-08-12T10:34:09+00:00
Yeah the logic is the same on request completion

the difference is when the request is not completed: if the client drops mid-request we never reach `cache.debit_time`, and `DebitBody` is never built either, so nothing gets charged. The `Drop` impl takes care of that

naming could be better though

## SyntheticBird45 | 2026-08-12T11:30:37+00:00
oh got it. make sense

## redsh4de | 2026-08-13T17:31:19+00:00
preserves graceful shutdown

```suggestion
                    self.serve(socket, remote_addr, shutdown_token.clone());
                },
                Some(res) = self.rpc_tasks.join_next() => {
                    if let Err(err) = res {
                        error!("RPC connection task failed: {err:#}");
                    }
                }
                () = shutdown_token.cancelled() => {
                    break;
                }
            }
        }

        while let Some(res) = self.rpc_tasks.join_next().await {
            if let Err(err) = res {
                error!("RPC connection task failed: {err:#}");
            }
        }

        Ok(())
    }

    fn serve(
        &mut self,
        socket: TcpStream,
        remote_addr: SocketAddr,
        shutdown_token: CancellationToken,
    ) {
        let rpc = self.rpc.clone();
        let send_timeout = self.send_timeout;
        let header_read_timeout = self.header_read_timeout;

        self.rpc_tasks.spawn(async move {
            let socket = WriteTimeout::new(socket, send_timeout);

            let mut builder = http1::Builder::new();
            builder
                .timer(TokioTimer::new())
                .header_read_timeout(header_read_timeout);

            let connection =
                builder.serve_connection(TokioIo::new(socket), TowerToHyperService::new(rpc));
            tokio::pin!(connection);

            tokio::select! {
                result = &mut connection => {
                    if let Err(err) = result {
                        error!("Failed to serve RPC connection: {err:#}");
                    }
                }
                () = shutdown_token.cancelled() => {
                    connection.as_mut().graceful_shutdown();

                    if let Err(err) = connection.await {
                        error!("Failed to shut down RPC connection: {err:#}");
                    }
                }
            }
```

## redsh4de | 2026-08-14T12:20:16+00:00
Using `written == len` means the deadline never re-arms once the socket send buffer fills, so the whole remaining body has to drain within `send_timeout`. Slow clients get cut off mid-response


```suggestion
                    if written > 0 {
```

## redsh4de | 2026-08-14T12:30:56+00:00
mb, these should prob be debug as they are remotely triggerable, can cause a log flood


```suggestion
                        debug!("Failed to serve RPC connection: {err:#}");
```

## redsh4de | 2026-08-14T12:31:46+00:00
```suggestion
                        debug!("Failed to shut down RPC connection: {err:#}");
```

## redsh4de | 2026-08-14T12:33:22+00:00
`nano` -> `nanos` in the last examples across all 3

## redsh4de | 2026-08-14T12:36:28+00:00
```suggestion
            warn!("RPC tasks survived shutdown signal for more than {RPC_SHUTDOWN_TIMEOUT:?} seconds... Dropping connections anyway.");
```

## Boog900 | 2026-08-14T13:00:16+00:00
That is the whole point, we want slow clients to disconnect. Otherwise a client can juts read 1 byte and the timer will reset.

## Boog900 | 2026-08-14T20:32:45+00:00
Do we really want to leave the node running here?

## Boog900 | 2026-08-14T20:34:11+00:00
We should remove the http2 feature on axum

## SyntheticBird45 | 2026-08-14T20:45:12+00:00
Not quite sure either but if we want to shutdown on listener bind failure then we would have to move TcpListener outside of this task, which would transform init_rpc_servers into an async fn. Since the task is stored in task_executor I believe it is more suited for the graceful shutdown pr to join all tasks and shutdown on failure.

## Boog900 | 2026-08-14T21:09:45+00:00
we can leave it as a TODO

# Action History
- Created by: SyntheticBird45 | 2026-08-07T22:09:06+00:00
- Merged at: 2026-08-14T21:11:04+00:00
