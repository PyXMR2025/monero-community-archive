---
title: 'net/i2p: support human-readable hostnames'
source_url: https://github.com/monero-project/monero/pull/10458
author: nahuhh
assignees: []
labels:
- pending review
- networking
created_at: '2026-04-24T22:10:51+00:00'
updated_at: '2026-07-27T16:33:56+00:00'
type: pull_request
status: open
closed_at: null
merged_at: null
---

# Original Description
mirror of jpk68's [codeberg branch](https://codeberg.org/jpk68/monero/src/branch/i2p-addresses)

(Review comments will be responded to on matrix)

from https://codeberg.org/jpk68/monero/pulls/3 :
> Adds support for standard, human-readable I2P hostnames in addition to Base32 ones. It effectively fixes [this TODO](https://github.com/monero-project/monero/blob/master/src/net/i2p_address.cpp#L47C9-L47C46), and is intended to be built off of by a later [SAM](https://i2p.net/en/docs/api/samv3/) integration PR. Some improvements have also been made to general hostname validation logic, in terms of conforming to [naming rules](https://i2p.net/en/docs/overview/naming/#naming-rules) and the like.
> 
> An example: `foo.i2p` may be used instead of `xmrdoc6k4licya7zsnba5s7vugzo72blcpukkvqno75o4ruhaqla.b32.i2p`.
> 
> To my knowledge, the changes made here do not render existing I2P documentation for Monero (i.e. [this](https://github.com/monero-project/monero/blob/master/docs/ANONYMITY_NETWORKS.md)) outdated; I do plan on updating the docs on a later date, however modifications can instead be made sooner if necessary.



# Discussion History
## vtnerd | 2026-04-25T00:42:02+00:00
Unnecessary change, drop it.

## vtnerd | 2026-04-25T00:42:09+00:00
Also unnecessary.

## vtnerd | 2026-04-25T00:42:20+00:00
Drop this spurious change too.

## vtnerd | 2026-04-25T00:43:19+00:00
If you're going to drop this, might want to add a runtime check somewhere that it doesn't exceed `host_` size.

## vtnerd | 2026-04-25T01:15:08+00:00
Nevermind, it's in the other function. In that case, I would still static assert that the buffer size is larger than `i2p_max_length` and `b32_length`

## nahuhh | 2026-04-25T11:03:02+00:00
Are these brackets meant to mean optional? If so, square them

```suggestion
        //! \return `x[.b32].i2p` or `x[.b32].i2p:z` if `port() != 0`.
```


## nahuhh | 2026-04-25T11:04:22+00:00
```suggestion
}
```


## selsta | 2026-05-27T18:28:36+00:00
This and later should be checked without `tld_i2p`, like `host_check_b32`.

## selsta | 2026-05-27T18:38:02+00:00
https://en.cppreference.com/cpp/string/byte/islower

unsafe without first converting to unsigned char, alternatively you could do

```
('a' <= c && c <= 'z') || ('0' <= c && c <= '9') || c == '.' || c == '-'
``

## selsta | 2026-05-27T18:39:59+00:00
this appears to be missing `sizeof(tld_b32)` now

## selsta | 2026-05-27T18:44:32+00:00
This is currently unreachable because `host_check()` already handles `host.ends_with(tld_b32)`. Should it be removed or comment updated that it's a defensive check?

## selsta | 2026-05-27T18:47:40+00:00
```suggestion
        constexpr const unsigned human_max_length = 63;
```

maybe better?

## selsta | 2026-05-27T18:50:50+00:00
```suggestion
EXPECT_TRUE(net::i2p_address::make(std::string(human_max_length, 'a') + ".i2p").has_value());
EXPECT_TRUE(net::i2p_address::make(std::string(human_max_length + 1, 'a') + ".i2p").has_error());
```

## selsta | 2026-05-27T18:53:43+00:00
```suggestion
    EXPECT_TRUE(net::i2p_address::make("m..ero.b32.i2p.i2p").has_error());
    EXPECT_TRUE(net::i2p_address::make("monero.i2p-").has_error());
    EXPECT_TRUE(net::i2p_address::make("monero..i2p").has_error());
    EXPECT_TRUE(net::i2p_address::make("a-.b.i2p").has_error());
    EXPECT_TRUE(net::i2p_address::make("a.-b.i2p").has_error());
    EXPECT_TRUE(net::i2p_address::make("a.b.i2p").has_value());
    EXPECT_TRUE(net::i2p_address::make("abc-123.i2p").has_value());
```

more edge cases

## selsta | 2026-05-27T19:00:31+00:00
If you move the max size from .cpp to the .h (like with Tor) you can avoid hardcoding the size

## selsta | 2026-05-28T16:12:50+00:00
still needs

```
#include <cctype>
```

## iamamyth | 2026-05-28T22:21:10+00:00
`islower` depends on locale, and `isdigit` may introduce certain oddities as well (https://en.cppreference.com/cpp/string/byte/isdigit):
```
some implementations (e.g. Microsoft in [1252 codepage](https://en.wikipedia.org/wiki/Windows-1252)) may classify additional single-byte characters as digits.)
```

## iamamyth | 2026-05-28T22:23:08+00:00
Also, there's wisdom in selsta's initial comment: Why introduce a function in place of a trivial conditional? Doing so only brings the possibility of mistakes, with no real benefit, as seen here.

## iamamyth | 2026-05-28T22:23:32+00:00
The prior line just checked empty.

## iamamyth | 2026-05-28T22:35:40+00:00
The naming here seems likely to cause misapprehension: TLD has a precise meaning which makes each TLD its own namespace (`.i2p` is a TLD, `.b32.i2p` is a domain, not a TLD); if one were to just look at the variable names, it would be reasonable, but incorrect, to assume that members of `tld_i2p` are not members of `tld_b32`.

## jpk68 | 2026-07-09T21:16:39+00:00
Added a check for this.

## jpk68 | 2026-07-09T21:18:03+00:00
This is a good point, however the `tld` wording was already used in the original code, so it might be better to keep it as is.

## jpk68 | 2026-07-09T21:18:25+00:00
Fixed.

## selsta | 2026-07-10T11:11:02+00:00
```
EXPECT_TRUE(net::i2p_address::make("example.b32.i2p").has_error());
```

I would add one explicit test for this to make it clear that we reject human readable addresses that end with .b32.i2p

## selsta | 2026-07-10T11:12:12+00:00
```
const auto address = net::i2p_address::make("Example.I2P");
ASSERT_TRUE(address.has_value());
EXPECT_STREQ("example.i2p", address->host_str());
```

also I'd add one test for this

## iamamyth | 2026-07-27T16:04:04+00:00
This check needs to happen prior to `remove_suffix` (near the start of the function), probably makes sense to just have it right below the other suffix check.

## iamamyth | 2026-07-27T16:04:38+00:00
This comment wasn't ever addressed, and the suggested test would have caught the above bug I referenced.

## iamamyth | 2026-07-27T16:07:23+00:00
The multi-char pair testing could be part of the above loop, which would make it a lot more efficient, though I don't care all too much, given the relatively infrequent use of the function.

## iamamyth | 2026-07-27T16:09:48+00:00
Minor addendum: If you don't feel like folding these `find`s into the loop, I'm happy to create a follow-on PR, as you already have fairly thorough tests for this scenario.

## jpk68 | 2026-07-27T16:33:56+00:00
Yeah, that would work. Thanks :)

# Action History
- Created by: nahuhh | 2026-04-24T22:10:51+00:00
