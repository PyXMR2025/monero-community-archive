---
title: 'P2P: Peer sync state svc'
source_url: https://github.com/Cuprate/cuprate/pull/128
author: Boog900
assignees: []
labels:
- A-p2p
created_at: '2024-05-15T00:33:48+00:00'
updated_at: '2024-05-27T01:09:36+00:00'
type: pull_request
status: merged
closed_at: '2024-05-22T00:56:39+00:00'
merged_at: '2024-05-22T00:56:39+00:00'
---

# Original Description
This PR adds the sync state service, which keeps track of what peers claim their chain is at, so we know if we are behind. It then allows getting a list of peers IDs who claim they are ahead

# Discussion History
## hinto-janai | 2024-05-19T22:04:45+00:00
punctuation:
```suggestion
/// A service that keeps track of our peers blockchains.
///
/// This is the service that handles:
/// 1. Finding out if we need to sync
/// 1. Giving the peers that should be synced _from_, to the requester
pub struct PeerSyncSvc<N: NetworkZone> {
```

## hinto-janai | 2024-05-19T22:10:08+00:00
Should this doc be on the public `fn new()` instead?

## hinto-janai | 2024-05-19T22:24:56+00:00
nit: saving 1 letter with `cum_dif` is not worth it imo

https://github.com/search?q=repo%3Amonero-project%2Fmonero%20cum_dif&type=code

## hinto-janai | 2024-05-19T22:27:00+00:00
Up to you on this one, I like taking out predicates when there's multiple or are long (both here). 
```suggestion
        let claimed_cum_diff_higher =
            self.new_height_watcher.borrow().cumulative_difficulty < new_cumulative_difficulty;
        let peer_disconnected = || {
            self.last_peer_in_watcher_handle
                .as_ref()
                .is_some_and(|handle| handle.is_closed())
        };
        if claimed_cum_diff_higher || peer_disconnected() {
```

## hinto-janai | 2024-05-19T22:28:28+00:00
Not sure if needed.
```suggestion
            tracing::debug!(
                "Updating sync watcher channel with new highest seen cumulative difficulty: {new_cumulative_difficulty}"
            );
```

## hinto-janai | 2024-05-19T22:29:56+00:00
Should this be handled? What does it mean when the `Receiver` is dropped?

## hinto-janai | 2024-05-19T22:33:38+00:00
Is `blockchain_height` the current height? I assumed so but `CRYPTONOTE_MAX_BLOCK_HEIGHT` is passed elsewhere.
```suggestion
    /// Returns `true` if a peer with this pruning seed should have a non-pruned version of a block.
    pub fn has_full_block(&self, height: u64, blockchain_height: u64) -> bool {
```

## hinto-janai | 2024-05-19T22:35:34+00:00
```suggestion
    /// Returns `true` if a peer with this pruning seed should have a non-pruned version of a block.
    pub fn has_full_block(&self, height: u64, blockchain_height: u64) -> bool {
```

## hinto-janai | 2024-05-19T22:45:41+00:00
```suggestion
        match get_block_pruning_stripe(height, blockchain_height, self.log_stripes) {
            Some(block_stripe) => self.stripe == block_stripe,
            None => true,
        }
```

## hinto-janai | 2024-05-19T22:53:09+00:00
punctuation:
```suggestion
//! This module contains a [`PeerSyncSvc`], which keeps track of the claimed chain states of connected peers.
//! This allows checking if we are behind and getting a list of peers who claim they are ahead.
```

## Boog900 | 2024-05-21T00:24:47+00:00
I am going to leave this is as, I don't think it's too bad

## Boog900 | 2024-05-21T00:27:56+00:00
It means there are no handles left, which means the network has been dropped and we are shutting down.

I don't think we need to propagate this error to the caller as they will handle shutdown themselves, which will shut this down when all handles to it are dropped.

## Boog900 | 2024-05-21T00:30:02+00:00
It's the height of whatever peer we are doing calculations for.

Using a value of `CRYPTONOTE_MAX_BLOCK_HEIGHT` just means we don't take into account the tip blocks which are non-pruned 

## Boog900 | 2024-05-21T00:30:37+00:00
ngl this was not intentional 

## hinto-janai | 2024-05-21T17:26:47+00:00
```suggestion
    /// Returns a list of peers that claim to have a higher cumulative difficulty than `current_cum_diff`.
    fn peers_to_sync_from(
        &self,
        current_cum_diff: u128,
```

## hinto-janai | 2024-05-21T17:26:52+00:00
```suggestion
            .range((current_cum_diff + 1)..)
```

## hinto-janai | 2024-05-21T17:27:01+00:00
```suggestion
        if let Some((old_cum_diff, _)) = self.peers.get_mut(&peer_id) {
            match (*old_cum_diff).cmp(&new_cumulative_difficulty) {
```

## hinto-janai | 2024-05-21T17:27:20+00:00
```suggestion
            let old_cum_diff_peers = self.cumulative_difficulties.get_mut(old_cum_diff).unwrap();
            old_cum_diff_peers.remove(&peer_id);
            if old_cum_diff_peers.is_empty() {
                // If this was the last peer remove the whole entry for this cumulative difficulty.
                self.cumulative_difficulties.remove(old_cum_diff);
            }
            // update the cumulative difficulty
            *old_cum_diff = new_cumulative_difficulty;
```

## hinto-janai | 2024-05-21T17:28:07+00:00
This diff was not applied btw

# Action History
- Created by: Boog900 | 2024-05-15T00:33:48+00:00
- Merged at: 2024-05-22T00:56:39+00:00
