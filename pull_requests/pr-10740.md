---
title: use byte-safe isalpha in epee json value parser
source_url: https://github.com/monero-project/monero/pull/10740
author: alhudz
assignees: []
labels:
- pending review
- epee
created_at: '2026-06-08T09:32:00+00:00'
updated_at: '2026-06-10T04:32:35+00:00'
type: pull_request
status: open
closed_at: null
merged_at: null
---

# Original Description
1. the json value parser detects `null`/`true`/`false` with the libc `isalpha(*it)` on a raw `char`, so a non-ASCII value byte is passed as a negative `int`, outside the `unsigned char`/`EOF` range `isalpha` is defined for, and the result is locale dependent.
2. `epee::misc_utils::parse` already has byte-safe `isspace`/`isdigit` over `lut`, and `lut` already sets the alpha bit, so the global call was the odd one out.

Added a matching `parse::isalpha` and switched the three value-keyword sites to it.

`repro`: `load_from_json` on `{"b": <0xc3>(}` (a value starting with a non-ASCII byte), reachable from RPC/HTTP and P2P JSON.
`expected`: rejected, the byte is not a keyword start.
`actual`: `isalpha((char)0xc3)` evaluated out of range (UB) and locale dependent.
`fix`: classify with the `lut` alpha bit, matching the adjacent `parse::isdigit`.

Test added covering `true`/`false`/`null` and the non-ASCII byte.


# Discussion History
## iamamyth | 2026-06-08T16:42:40+00:00
Would recommend test descriptions/comments focus on the desired observed behavior, rather than implementation details (branching being an implementation detail). For example, this one could be shortened to:
```
// Keyword parsing rejects non-ASCII characters
```

## iamamyth | 2026-06-08T16:49:14+00:00
Same here about implementation details (`isalpha`), you can simply drop the final clause and maybe change the non-ASCII clarification:
```
// A value beginning with a non-ASCII byte (most significant bit == 1) is not
// a keyword and must be rejected.

## alhudz | 2026-06-08T17:00:47+00:00
Good point, dropped the implementation detail. Reworked the header to just describe what parses.

## alhudz | 2026-06-08T17:00:54+00:00
Done, used your wording and dropped the isalpha clause.

# Action History
- Created by: alhudz | 2026-06-08T09:32:00+00:00
