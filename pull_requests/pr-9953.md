---
title: 'wallet: immediate lookahead table expansion + set_subaddresss_lookahead RPC
  endpoint'
source_url: https://github.com/monero-project/monero/pull/9953
author: nahuhh
assignees: []
labels:
- wallet
created_at: '2025-06-12T17:45:44+00:00'
updated_at: '2025-07-10T15:25:13+00:00'
type: pull_request
status: merged
closed_at: '2025-07-10T12:20:53+00:00'
merged_at: '2025-07-10T12:20:53+00:00'
---

# Original Description
replaces #8981

~~no changes~~
changes from original:
- added header to fix test failure `#include <boost/optional/optional_io.hpp>`
- updates & fixes by @j-berman 
- updates & fixes by @jeffro256 

resolves #8980
resolves #8954
resolves #7364

> Basically the current behavior is that if you make a wallet with the default settings in the cli and then do set subaddress-lookahead 50:300, then the subaddress pubkey table hasnt actually expanded and you aren't looking any further when you scan. Instead, the table will get expanded to this look ahead only after either a new enote is found with the old lookahead or if the user requests a new subaddress with address new.
>
> This patch makes sure that the wallet adjusts the pubkey table immediately after sending the set command if an increase in the lookahead is requested.
>
> This patch also adds a new endpoint to the wallet-rpc program, /set_subaddress_lookahead which allows the user to expand the subaddress lookahead table via remote command. ~~As mentioned in the original ticket, the change does not persist to disk when the wallet closes and changing the wallet-rpc program to save those cached values looks like kinda a pain but I hope that the requestor finds that something is better than nothing for now.~~

# Discussion History
## j-berman | 2025-06-26T06:20:01+00:00
This currently doesn't handle overflows (e.g. `m_subaddress_labels.size()+major` can overflow), and is also slower than it needs to be by deriving a single subaddress every iteration (calling `create_one_off_subaddress` -> `get_subaddress_spend_public_key` each iteration will de-compress the public spend key every iteration inside `rct::addKeys`).

This should solve the above issues and in my (biased) view is easier to verify:

```suggestion
  const uint32_t old_major_lookahead = m_subaddress_lookahead_major;
  const uint32_t old_minor_lookahead = m_subaddress_lookahead_minor;

  m_subaddress_lookahead_major = major;
  m_subaddress_lookahead_minor = minor;

  if (old_major_lookahead >= major && old_minor_lookahead >= minor)
    return;

  // Expand the subaddresses map so that outputs received to the higher lookaheads will be identified in the scan loop
  hw::device &hwdev = m_account.get_device();
  cryptonote::subaddress_index index2;
  const uint32_t major_end = get_subaddress_clamped_sum(m_subaddress_labels.size(), major);
  for (index2.major = 0; index2.major < major_end; ++index2.major)
  {
    // The existing n minor subaddresses already set for this account (the lookahead expands past this point)
    const uint32_t n_minor_labels = m_subaddress_labels.size() > index2.major ? m_subaddress_labels[index2.major].size() : 0;

    // The expected existing n minor subaddresses already allocated for this account
    const uint32_t begin = (n_minor_labels || index2.major < old_major_lookahead) ? get_subaddress_clamped_sum(n_minor_labels, old_minor_lookahead) : 0;
    // The expected new n minor subaddresses allocated for this account
    const uint32_t end = get_subaddress_clamped_sum(n_minor_labels, minor);

    if (begin >= end)
      continue;

    const std::vector<crypto::public_key> pkeys = hwdev.get_subaddress_spend_public_keys(m_account.get_keys(), index2.major, begin, end);
    for (index2.minor = begin; index2.minor < end; ++index2.minor)
    {
      const crypto::public_key &D = pkeys.at(index2.minor - begin);
      m_subaddresses[D] = index2;
    }
  }
```

## plowsof | 2025-06-26T16:27:18+00:00
tested on cli and working fine:

starting with a `1:10` lookahead. sending to above expanded , then decreasing idx until i find the max idx

| lookahead | max idx |
|---------------|-------------|
| 1:10          | 9|           
| | 18|                     
| | 27| 
| 1:20 | 47|
|1:1000 | 1047 |

**update** i was using old code, set lookahead expands by the expected value, thanks for debugging @j-berman 
- ~~note how it starts at idx 9 as its indexed from 0, but, reference wallet cant see subaddress idx 0, but this is expected.~~
- ~~but increments thereafter, only by (lookahead-1) which is odd. i expected a full +lookahead~~  
- expansion is immediate. :heavy_check_mark: 

## j-berman | 2025-06-26T21:34:05+00:00
```suggestion
    // The existing minor addresses already set for this account
    const uint32_t n_minor_subaddrs = this->get_num_subaddresses(index2.major);

    // The subaddress lookahead is expected to expand from the max index in expand_subaddresses
    const uint32_t n_max_idx = n_minor_subaddrs > 0 ? (n_minor_subaddrs - 1) : 0;
    const uint32_t begin = (n_minor_subaddrs || index2.major < old_major_lookahead) ? get_subaddress_clamped_sum(n_max_idx, old_minor_lookahead) : 0;
    // The expected new n minor subaddresses allocated for this account
    const uint32_t end = get_subaddress_clamped_sum(n_max_idx, minor);
```

## j-berman | 2025-06-26T21:37:15+00:00
Some helpful additional tests:

```suggestion
}

TEST_F(WalletSubaddress, ExpandThenSetMinorIncreaseOnly)
{
  // We assume we are starting with the default setup state
  EXPECT_EQ(50, w1.get_subaddress_lookahead().first);
  EXPECT_EQ(200, w1.get_subaddress_lookahead().second);

  // Helper function to check max subaddrs allocated
  const auto check_expected_max = [&](const cryptonote::subaddress_index exp_max)
  {
    for (uint32_t i = 0; i <= exp_max.minor; ++i)
    {
      auto subaddr = w1.get_subaddress({exp_max.major, i});
      EXPECT_NE(boost::none, w1.get_subaddress_index(subaddr));
    }
    auto subaddr = w1.get_subaddress({exp_max.major, exp_max.minor + 1});
    EXPECT_EQ(boost::none, w1.get_subaddress_index(subaddr));
  };

  // We assume we start with subaddrs for minor indexes 0 to 199
  check_expected_max({0,199});
  check_expected_max({1,199});
  check_expected_max({49,199});
  EXPECT_EQ(w1.get_num_subaddresses(50), 0);

  // Mock receive to {0,150}, so expand from there
  w1.expand_subaddresses({0,150});
  // We should now have subaddresses for minor indexes 0 to 349
  check_expected_max({0,349});
  check_expected_max({1,199});
  check_expected_max({49,199});
  EXPECT_EQ(w1.get_num_subaddresses(50), 0);

  // Now set the minor lookahead 100 higher
  w1.set_subaddress_lookahead(50, 200+100);
  // We should have subaddresses for minor indexes 0 to 449
  check_expected_max({0,449});
  check_expected_max({1,299});
  check_expected_max({49,299});
  EXPECT_EQ(w1.get_num_subaddresses(50), 0);
}

TEST_F(WalletSubaddress, ExpandThenSetMajorIncreaseOnly)
{
  // We assume we are starting with the default setup state
  EXPECT_EQ(50, w1.get_subaddress_lookahead().first);
  EXPECT_EQ(200, w1.get_subaddress_lookahead().second);

  // Helper function to check max subaddrs allocated
  const auto check_expected_max = [&](const cryptonote::subaddress_index exp_max)
  {
    for (uint32_t i = 0; i <= exp_max.minor; ++i)
    {
      auto subaddr = w1.get_subaddress({exp_max.major, i});
      EXPECT_NE(boost::none, w1.get_subaddress_index(subaddr));
    }
    auto subaddr = w1.get_subaddress({exp_max.major, exp_max.minor + 1});
    EXPECT_EQ(boost::none, w1.get_subaddress_index(subaddr));
  };

  // We assume we start with subaddrs for minor indexes 0 to 199
  check_expected_max({0,199});
  check_expected_max({1,199});
  check_expected_max({49,199});
  EXPECT_EQ(w1.get_num_subaddresses(50), 0);

  // Mock receive to {40,0}, so expand from there
  w1.expand_subaddresses({40,0});
  check_expected_max({0,199});
  check_expected_max({1,199});
  check_expected_max({40,199});
  check_expected_max({89,199});
  EXPECT_EQ(w1.get_num_subaddresses(90), 0);

  // Now set the major lookahead 10 higher
  w1.set_subaddress_lookahead(50+10, 200);
  check_expected_max({0,199});
  check_expected_max({1,199});
  check_expected_max({40,199});
  check_expected_max({99,199});
  EXPECT_EQ(w1.get_num_subaddresses(100), 0);
}

TEST_F(WalletSubaddress, ExpandThenSetIncreaseBoth)
{
  // We assume we are starting with the default setup state
  EXPECT_EQ(50, w1.get_subaddress_lookahead().first);
  EXPECT_EQ(200, w1.get_subaddress_lookahead().second);

  // Helper function to check max subaddrs allocated
  const auto check_expected_max = [&](const cryptonote::subaddress_index exp_max)
  {
    for (uint32_t i = 0; i <= exp_max.minor; ++i)
    {
      auto subaddr = w1.get_subaddress({exp_max.major, i});
      EXPECT_NE(boost::none, w1.get_subaddress_index(subaddr));
    }
    auto subaddr = w1.get_subaddress({exp_max.major, exp_max.minor + 1});
    EXPECT_EQ(boost::none, w1.get_subaddress_index(subaddr));
  };

  // We assume we start with subaddrs for minor indexes 0 to 199
  check_expected_max({0,199});
  check_expected_max({1,199});
  check_expected_max({49,199});
  EXPECT_EQ(w1.get_num_subaddresses(50), 0);

  // Mock receive to {40,150}, so expand from there
  w1.expand_subaddresses({40,150});
  check_expected_max({0,199});
  check_expected_max({1,199});
  check_expected_max({40,349});
  check_expected_max({89,199});
  EXPECT_EQ(w1.get_num_subaddresses(90), 0);

  // Now set the major lookahead 10 higher and minor 100 higher
  w1.set_subaddress_lookahead(50+10, 200+100);
  check_expected_max({0,299});
  check_expected_max({1,299});
  check_expected_max({40,449});
  check_expected_max({99,299});
  EXPECT_EQ(w1.get_num_subaddresses(100), 0);
}

```

## j-berman | 2025-06-26T22:46:52+00:00
```suggestion
  const uint32_t max_major_idx = this->get_num_subaddress_accounts() > 0 ? (this->get_num_subaddress_accounts() - 1) : 0;
  const uint32_t major_end = get_subaddress_clamped_sum(max_major_idx, major);
  for (index2.major = 0; index2.major < major_end; ++index2.major)
  {
    // The existing minor addresses already set for this account
    const uint32_t n_minor_subaddrs = this->get_num_subaddresses(index2.major);

    // The subaddress lookahead is expected to expand from the max index in expand_subaddresses
    const uint32_t max_minor_idx = n_minor_subaddrs > 0 ? (n_minor_subaddrs - 1) : 0;
    const uint32_t begin = (n_minor_subaddrs || index2.major < old_major_lookahead) ? get_subaddress_clamped_sum(max_minor_idx, old_minor_lookahead) : 0;
    // The expected new n minor subaddresses allocated for this account
    const uint32_t end = get_subaddress_clamped_sum(max_minor_idx, minor);
```

## j-berman | 2025-06-26T22:50:28+00:00
```suggestion
// Helper function to check max subaddrs allocated
static void check_expected_max(const tools::wallet2 &w1, const cryptonote::subaddress_index exp_max)
{
  for (uint32_t i = 0; i <= exp_max.minor; ++i)
  {
    auto subaddr = w1.get_subaddress({exp_max.major, i});
    EXPECT_NE(boost::none, w1.get_subaddress_index(subaddr));
  }
  auto subaddr = w1.get_subaddress({exp_max.major, exp_max.minor + 1});
  EXPECT_EQ(boost::none, w1.get_subaddress_index(subaddr));
};

static void expect_default_wallet_state(const tools::wallet2 &w1)
{
  // these tests assume we are starting with the default setup state
  EXPECT_EQ(2, w1.get_num_subaddress_accounts());
  EXPECT_EQ(50, w1.get_subaddress_lookahead().first);
  EXPECT_EQ(200, w1.get_subaddress_lookahead().second);

  // We assume we start with subaddrs for minor indexes 0 to 199
  check_expected_max(w1, {0,199});
  check_expected_max(w1, {1,199});
  check_expected_max(w1, {49,199});
  check_expected_max(w1, {50,199}); // 50 because the test starts with accounts 0 and 1 already allocated
  EXPECT_EQ(boost::none, w1.get_subaddress_index(w1.get_subaddress({51,0})));
}

TEST_F(WalletSubaddress, SetLookahead)
{
  expect_default_wallet_state(w1);
  // get_subaddress_index looks up keys in the private m_subaddresses dictionary so we will use it to test if a key is properly being scanned for
  cryptonote::subaddress_index test_idx = {50, 199};
  auto subaddr = w1.get_subaddress(test_idx);
  EXPECT_NE(boost::none, w1.get_subaddress_index(subaddr));
  // fist test expanding the major lookahead
  w1.set_subaddress_lookahead(100, 200);
  EXPECT_EQ(100, w1.get_subaddress_lookahead().first);
  EXPECT_EQ(200, w1.get_subaddress_lookahead().second);
  check_expected_max(w1, {100, 199});
  // next test expanding the minor lookahead
  w1.set_subaddress_lookahead(100, 300);
  EXPECT_EQ(100, w1.get_subaddress_lookahead().first);
  EXPECT_EQ(300, w1.get_subaddress_lookahead().second);
  check_expected_max(w1, {100, 299});
}

TEST_F(WalletSubaddress, ExpandThenSetMinorIncreaseOnly)
{
  expect_default_wallet_state(w1);

  // Mock receive to {0,150}, so expand from there
  w1.expand_subaddresses({0,150});
  // We should now have subaddresses for minor indexes 0 to 349
  check_expected_max(w1, {0,349});
  check_expected_max(w1, {1,199});
  check_expected_max(w1, {49,199});
  check_expected_max(w1, {50,199});
  EXPECT_EQ(boost::none, w1.get_subaddress_index(w1.get_subaddress({51,0})));

  // Now set the minor lookahead 100 higher
  w1.set_subaddress_lookahead(50, 200+100);
  // We should have subaddresses for minor indexes 0 to 449
  check_expected_max(w1, {0,449});
  check_expected_max(w1, {1,299});
  check_expected_max(w1, {49,299});
  check_expected_max(w1, {50,299});
  EXPECT_EQ(boost::none, w1.get_subaddress_index(w1.get_subaddress({51,0})));
}

TEST_F(WalletSubaddress, ExpandThenSetMajorIncreaseOnly)
{
  expect_default_wallet_state(w1);

  // Mock receive to {40,0}, so expand from there
  w1.expand_subaddresses({40,0});
  check_expected_max(w1, {0,199});
  check_expected_max(w1, {1,199});
  check_expected_max(w1, {40,199});
  check_expected_max(w1, {89,199});
  EXPECT_EQ(boost::none, w1.get_subaddress_index(w1.get_subaddress({90,0})));

  // Now set the major lookahead 10 higher
  w1.set_subaddress_lookahead(50+10, 200);
  check_expected_max(w1, {0,199});
  check_expected_max(w1, {1,199});
  check_expected_max(w1, {40,199});
  check_expected_max(w1, {99,199});
  EXPECT_EQ(boost::none, w1.get_subaddress_index(w1.get_subaddress({100,0})));
}

TEST_F(WalletSubaddress, ExpandThenSetIncreaseBoth)
{
  expect_default_wallet_state(w1);

  // Mock receive to {40,150}, so expand from there
  w1.expand_subaddresses({40,150});
  check_expected_max(w1, {0,199});
  check_expected_max(w1, {1,199});
  check_expected_max(w1, {40,349});
  check_expected_max(w1, {89,199});
  EXPECT_EQ(boost::none, w1.get_subaddress_index(w1.get_subaddress({90,0})));

  // Now set the major lookahead 10 higher and minor 100 higher
  w1.set_subaddress_lookahead(50+10, 200+100);
  check_expected_max(w1, {0,299});
  check_expected_max(w1, {1,299});
  check_expected_max(w1, {40,449});
  check_expected_max(w1, {99,299});
  EXPECT_EQ(boost::none, w1.get_subaddress_index(w1.get_subaddress({100,0})));
}
```

## jeffro256 | 2025-06-30T16:28:50+00:00
Why not use `this->expand_subaddresses()`? (Before/conditional on setting `m_subaddress_lookahead_major` and `m_subaddress_lookahead_minor` above)

## j-berman | 2025-06-30T18:24:53+00:00
`expand_subaddresses` expects a user to have received to a specific subaddr index, and then expands the subaddress map (**and adds subaddress labels**) from there. UI's populate subaddresses based on the labels that are set. So we don't want to add any new labels from setting this lookahead. We could have `expand_subaddresses` reuse this logic from here, but I think this approach is reasonable.

## jeffro256 | 2025-06-30T18:53:44+00:00
I will need to double-check, but I think a lot of the `wallet2` balance calls will break if the subaddress map is populated, but not the subaddress labels. See: https://github.com/monero-project/monero/pull/9934#issuecomment-2945876887. A lot of higher-level calling code iterates through the subaddresses `wallet2` provides using the `get_num_subaddresses()` and `get_num_subaddress_accounts()` calls, which query `m_subaddress_labels`, not `m_subaddresses`.

## j-berman | 2025-06-30T18:59:41+00:00
`expand_subaddresses` already does expand the subaddresses map past the subaddress labels the same way that this new logic does (see the new unit tests), so the wallet2 balance calls would be currently broken if true

## jeffro256 | 2025-06-30T22:28:33+00:00
Okay good point. On first receive during `refresh`, `should_expand` will return `true` for that receive address index if the subaddress lookahead was already set previously, in which case the subaddress labels will be filled up to that index. So basically the account/subaddress counts will be "wrong" until the first receive, but those would have 0 balance in them anyways. You also can't lookup the subaddress by index until it's first receive, which is funky, but that's already the case for lookahead addresses. I still don't like the code duplication though...

## jeffro256 | 2025-06-30T22:31:33+00:00
The RPC minor version should be bumped.

## nahuhh | 2025-07-01T14:43:51+00:00
[done](https://github.com/monero-project/monero/compare/6b441a1782d9ecc2248145fa87efe3e099a6db43..cd6c0e9503eccd9c42d2385eadcf2141dad7955e), thanks

## jeffro256 | 2025-07-07T22:03:07+00:00
These should be `uint32_t`, not `uint64_t`. 

# Action History
- Created by: nahuhh | 2025-06-12T17:45:44+00:00
- Merged at: 2025-07-10T12:20:53+00:00
