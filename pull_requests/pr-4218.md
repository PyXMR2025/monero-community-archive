---
title: Cryptonight variant 2
source_url: https://github.com/monero-project/monero/pull/4218
author: SChernykh
assignees: []
labels: []
created_at: '2018-08-03T09:47:47+00:00'
updated_at: '2018-10-15T04:22:51+00:00'
type: pull_request
status: merged
closed_at: '2018-09-11T13:22:51+00:00'
merged_at: '2018-09-11T13:22:51+00:00'
---

# Original Description
Contains two modifications to improve ASIC resistance: shuffle and integer math.

Shuffle makes use of the whole 64-byte cache line instead of 16 bytes only, making Cryptonight 4 times more demanding for memory bandwidth.

Integer math adds 64:32 bit integer division followed by 64 bit integer square root, adding large and unavoidable computational latency to the main loop.

More or less complete description of these changes and performance numbers are here: https://github.com/SChernykh/xmr-stak-cpu/blob/master/README.md

Discussion that preceded this pull request: https://github.com/SChernykh/xmr-stak-cpu/issues/1

# Discussion History
## moneromooo-monero | 2018-08-03T12:59:34+00:00
Please make these  >= 2, we don't want to revert for v3 without thinking about it first (and v3 will be the same as v2 anyway, there'll be just one day between the two).

## moneromooo-monero | 2018-08-03T13:03:06+00:00
I did not realize this would actually use floating point. Is it stable across the whole 64 bit range on various platforms ?

## vtnerd | 2018-08-03T13:59:19+00:00
This currently alters the CPU / GPU performance ratios. The CPU has 64-byte cachelines while most GPUs seem to have 32-byte cachelines. This is also touching more cachelines per iteration, giving a further performance edge to CPUs.

Not arguing for or against this altereration, but I thought it was worth pointing out for those that may be interested.

## SChernykh | 2018-08-03T14:00:54+00:00
It's stable on any system that supports IEEE-754. Assuming that underlying hardware is IEEE-754 compliant, this code works like this:
- sqrt_input is 48-bit integer, it's converted to double without rounding and loosing precision
- sqrt(z) does rounding, but it's safe here: the result is less than 2^24 and absolute error is less than 2^-28 because **double** precision is used
- z is then truncated to get z1, but absolute error is too small to make this truncation incorrect

This was the theory, but I also tested this code (both portable and SSE2 versions) and it gives correct results for **all** integers less than 2^48.

I can publish the testing program so everyone else could confirm it. It's actually easy to write a testing program independently. The definition of integer square root is simple: INT_SQRT(N) is an integer number M such that both inequalities hold: M^2 <= N and (M+1)^2 > N. This number always exists if N >= 0 and there is always only one such number.

P.S. I can rewrite the fallback implementation (the last one in slow-hash.c) to use only integers to calculate the square root. It will become slower though.

## vtnerd | 2018-08-03T14:02:59+00:00
No need for the aliasing violation, just do `division_result = {0, 0};`. Detecting multi-wide initialization is trivial for the compiler.

## vtnerd | 2018-08-03T14:04:30+00:00
This isn't portable. This is an aliasing violation that can result in misaligned access errors on some CPUs.

## vtnerd | 2018-08-03T14:22:58+00:00
IEEE-754 behavior isn't required by the C or C++ standard. There is a static check in `numeric_limits<T>` for IEEE. I doubt this check fails frequently though.

> * sqrt(z) does rounding, but it's safe here: the result is less than 2^24 and absolute error is less than 2^-28 because double precision is used
> * z is then truncated to get z1, but absolute error is too small to make this truncation incorrect

I might be missing something, but this feels awfully close to a +/-1 bug with rounding, where the float itself is `3.999999` or `4.00000`. I would need to brush up on my floating point behavior. @hyc ?

EDIT: changed "defined" to "required" above because that was my intent.

## SChernykh | 2018-08-03T14:24:13+00:00
CPU/GPU performance ratio remains pretty much intact, we already have a lot of performance numbers: https://github.com/SChernykh/xmr-stak-cpu#performance

## SChernykh | 2018-08-03T14:49:31+00:00
base_ptr is actually a scratchpad pointer here. It's allocated by malloc/VirtualAlloc/mmap. It will be 8-byte aligned in the worst case. offset is always a multiple of 16, so misaligned access is not possible here.

## SChernykh | 2018-08-03T15:02:06+00:00
IEEE-754 is not required, but even without it this code is far away to getting incorrect rounding: worst case is when sqrt(2^48 - 1) is calculated: https://onlinegdb.com/r1AmAyzrX

Rounding error is never larger than 1 ULP by definition, and the distance to the next integer is 16 ULP in this case.

## vtnerd | 2018-08-04T20:11:40+00:00
Yikes, its reliant on the code below. This is still an aliasing violation, but AFAIK will work on our targeted compilers.

## SChernykh | 2018-08-04T20:27:36+00:00
It's safe as long as compiled code passes all tests. This PR has test vectors to catch any bug.

## moneromooo-monero | 2018-08-24T11:36:16+00:00
Probably faster (and simpler) to init them to 0 regardless.

## moneromooo-monero | 2018-08-24T11:41:41+00:00
Maybe pedantic, but it'd be prefereable to lose the ending ; in VARIANT2_INTEGER_MATH_DIVISION_STEP so that statements like the above look like clean C :)

## moneromooo-monero | 2018-08-24T11:50:55+00:00
Might be worth a #warning

## moneromooo-monero | 2018-08-24T11:52:53+00:00
I think there's some asm that does both / and % at once. Maybe the compiler's clever and will work it out though ?

## moneromooo-monero | 2018-08-24T11:56:12+00:00
Do you have docs about these ? All I find is not very helpful. In particular, a lot of code calls _mm_sqrt_sd with identical parameters.

## moneromooo-monero | 2018-08-24T11:57:48+00:00
What is the intent of that or ? It seems to constrain the result to low 32 bits, which doesn't seem to be a problem given the code below, but why ?

## SChernykh | 2018-08-24T12:00:32+00:00
Both Visual Studio's cl compiler and gcc are clever enough, they use single div instruction.

## SChernykh | 2018-08-24T12:02:00+00:00
> __m128d _mm_sqrt_sd (__m128d a, __m128d b)
Compute the square root of the lower double-precision (64-bit) floating-point element in b, store the result in the lower element of dst, and copy the upper element from a to the upper element of dst.

From Intel documentation.

## SChernykh | 2018-08-24T12:03:52+00:00
To make sure divisor is > 2^32 and is an odd number (not divisible by 2). When divisor is very small, div instruction gets much slower on CPU because quotient gets large, and CPU calculates quotient bit by bit. And I want the remainder to be also full 31-32 bits which is not the case with small divisors.

## SChernykh | 2018-08-24T12:08:27+00:00
We have 200 bytes from initial keccak, it could be anything from those 200 bytes here if someone doesn't like zeroes. I actually have non-zero init values here in my latest code.

## moneromooo-monero | 2018-08-24T12:23:29+00:00
OK. I just meant, the if (variant >= 2) is unneeded.

## moneromooo-monero | 2018-08-24T12:24:47+00:00
Ah, I'd found "passed through from a", which was confusing but that makes sense now. Thanks.

## moneromooo-monero | 2018-08-24T12:25:02+00:00
Interesting. Thanks for the explanation.

## Equim-chan | 2018-08-27T06:41:02+00:00
Can the `(((int64_t)(r2 + s + (1ULL << 32)) < 0) ? 1 : 0)` branch really be covered? I'm implementing this in Go and found this cannot be covered while I'm using the same test code. https://codecov.io/gh/Equim-chan/cryptonight/src/8fbc190cb5d6928c1c7f68dd916261bc3892f023/v2_sqrt.go#L21

## SChernykh | 2018-08-27T06:44:20+00:00
It's casted to int64_t first then compared to 0, so yes. This code will need comments because it's not obvious how it works. I'm not sure how it should be written in Go, but it works in plain C.

## SChernykh | 2018-08-27T06:48:52+00:00
This code essentially checks these inequalities:
1) (r/2+2^32)^2 <= sqrt_input+2^64
2) ((r+1)/2+2^32)^2 > sqrt_input+2^64

And corrects r if needed. But it does some bit manipulations to stay within 64 bit integer types and to work both for even and odd values of r.

## SChernykh | 2018-08-27T07:22:39+00:00
You can rewrite the last two lines as
```
    const uint64_t r2 = (uint64_t)(s) * (s + b) + (r << 32); \
    r += ((r2 + b > sqrt_input) ? -1 : 0) + ((r2 + (1ULL << 32) < sqrt_input - s) ? 1 : 0); \
```
It should work too: I've tested it for all edge cases and it passed the test.

## SChernykh | 2018-08-27T08:21:16+00:00
@Equim-chan I've submitted this code without casts to int64_t, so you should be able to port it to Go without issues now.

## Equim-chan | 2018-08-27T08:45:04+00:00
The logic may make sense, but the test just doesn't cover that.

I tried this:

```diff
diff --git a/src/crypto/variant2_int_sqrt.h b/src/crypto/variant2_int_sqrt.h
index 211f0c3c..7aae4827 100644
--- a/src/crypto/variant2_int_sqrt.h
+++ b/src/crypto/variant2_int_sqrt.h
@@ -9,7 +9,7 @@
     const uint64_t s = r >> 1; \
     const uint64_t b = r & 1; \
     const uint64_t r2 = (uint64_t)(s) * (s + b) + (r << 32) - sqrt_input; \
-    r += (((int64_t)(r2 + b) > 0) ? -1 : 0) + (((int64_t)(r2 + s + (1ULL << 32)) < 0) ? 1 : 0); \
+    r += (((int64_t)(r2 + b) > 0) ? -1 : 0); \
   } while(0)
```

and it still passes hash-slow-2 and hash-variant2-int-sqrt test, indicating that it is not covered.

## psychocrypt | 2018-08-29T20:38:19+00:00
This an the next line can be handled within one `memcpy` call.

```C++
memcpy(b + AES_BLOCK_SIZE, state.hs.b + 64, AES_BLOCK_SIZE);
```

## psychocrypt | 2018-08-29T20:40:53+00:00
the `16` should be `AES_BLOCK_SIZE`

## psychocrypt | 2018-08-29T20:48:40+00:00
Nobody will ever see that there is a test for the integer sqrt version available even if there is a comment in the code. This test shoud be enabled by default and the reference that this test is disabled should be removed from the code. 

The needed time for this test should be small enough to have this test enabled by default.

## SChernykh | 2018-08-29T21:00:38+00:00
It's much slower than floating point versions. There are 7116134816 edge cases to test - it won't be possible to test them all. I will add a code that runs reference implementation for 1% of them, including 0, 2^64-1 and other extreme cases. It should still catch all implementation bugs.

## SChernykh | 2018-08-29T21:01:53+00:00
Indeed. I didn't notice that one.

# Action History
- Created by: SChernykh | 2018-08-03T09:47:47+00:00
- Merged at: 2018-09-11T13:22:51+00:00
