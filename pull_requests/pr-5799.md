---
title: 'daemon: automatic public nodes discovering and bootstrap daemon switching'
source_url: https://github.com/monero-project/monero/pull/5799
author: xiphon
assignees: []
labels: []
created_at: '2019-08-09T10:50:19+00:00'
updated_at: '2019-09-04T14:27:35+00:00'
type: pull_request
status: merged
closed_at: '2019-09-04T14:27:34+00:00'
merged_at: '2019-09-04T14:27:34+00:00'
---

# Original Description
Implemented `--bootstrap-daemon-address auto` mode.

Allows to sync and use the wallet while the daemon is syncing with the network.

* **While the daemon is syncing**
it will automatically select random public node (node that is running with `--public-node` mode enabled) to serve incoming requests (i.e. will use it as a remote node). If currently selected public node fails, the daemon will switch to another randomly chosen public node.
* **Once the daemon is fully synced**
it will use the local blockchain to serve incoming requests, i.e. operating in normal mode.

# Discussion History
## vtnerd | 2019-08-10T06:29:53+00:00
`#include <list>` does not appear to be needed.

## vtnerd | 2019-08-10T06:33:10+00:00
How about creating a cpp for a lot of these functions? Don't need to be put everything in headers.

## vtnerd | 2019-08-10T06:33:27+00:00
This function cannot be marked as `noexcept` due to `invoke_http_json`.

## vtnerd | 2019-08-10T06:34:41+00:00
I _think_ we've been trying to list all members (even if they have default constructors). It does make things more consistent in case the type changes.

## vtnerd | 2019-08-10T06:35:06+00:00
This one can be `noexcept`.

## vtnerd | 2019-08-10T06:35:33+00:00
Again, I think this `noexcept` needs to be dropped.

## vtnerd | 2019-08-10T06:36:49+00:00
This can't be `noexcept` because`crypto::rand_idx` eventually acquires a lock which can throw.

## vtnerd | 2019-08-10T06:38:37+00:00
This function can be `static` and possibly should be in some peerlist class.

## vtnerd | 2019-08-10T06:39:15+00:00
The log functions and `get_public_peerlist` can throw, so `noexcept` has to be dropped here too.

## vtnerd | 2019-08-10T06:40:38+00:00
Does `get_random_public_node` need to be public? Can it be put into an anonymous namespace in a cpp and used by this function solely? It appears that its only used here.

## vtnerd | 2019-08-10T06:46:45+00:00
The http client has everything you need. Check `is_connected()` here, and in `handle_result` should disconnect on failure.

## vtnerd | 2019-08-10T06:52:29+00:00
...`noexcept` that probably needs to be dropped...

## vtnerd | 2019-08-10T06:52:38+00:00
Another `noexcept` that needs to be dropped.

## vtnerd | 2019-08-10T06:53:02+00:00
Again, drop the `noexcept`.

## vtnerd | 2019-08-10T06:55:28+00:00
These `AUTO_VAL_INIT` calls need to be slowly deprecated. Just use brace `{}` initialization.

## vtnerd | 2019-08-10T07:00:45+00:00
There is an `invoke_http_json_rpc` function ... is this version different? The only thing that may be different is how the ID is initialized, but I don't see a practical reason for duplicating this.

## vtnerd | 2019-08-10T07:05:10+00:00
Is this copy really needed? The HTTP server has a `get_host()` and `get_port()` call if the information is needed.

## vtnerd | 2019-08-10T07:06:31+00:00
Take `credentials` by `&&` then move.

## vtnerd | 2019-08-10T07:07:20+00:00
Take `credentials` by copy then move.

## vtnerd | 2019-08-10T07:09:35+00:00
Why not `address != "auto"`? The performance is identical, but this form is less common.

## vtnerd | 2019-08-12T15:20:05+00:00
This is now aliasing `target_height`, but its not obvious why ... was this some technique to prevent further modifications below? Why not declare `top_height` on line 1530 and drop `target_height`? This would also elminate one additional `m_core.get_current_blockchain_height()` call which has locks, etc., and isn't cheap.

## vtnerd | 2019-08-12T15:22:52+00:00
This looks like a decent candidate for `boost::optional<uint64_t>` return value.

## vtnerd | 2019-08-12T15:26:41+00:00
The lifetime/ownership of this is unfortunate (but acceptable for its only current use case), no unit tests were added, and there is little code (outside of the randomized peer selection which can still be in a separate file) ... should the bulk of this logic just be included in the `core_rpc_server.cpp`?

## xiphon | 2019-08-12T15:49:34+00:00
Yep, and `#include <iterator>` as well

## xiphon | 2019-08-12T16:02:19+00:00
I agree

## xiphon | 2019-08-12T16:23:45+00:00
I might do that, though listing members with default constructors seems questionable to me. Could you elaborate a bit more on that?

## xiphon | 2019-08-12T16:27:47+00:00
> Does `get_random_public_node` need to be public?

It is private.

> Can it be put into an anonymous namespace in a cpp and used by this function solely?

Would like to keep it as-is: private class function. Will move it to cpp though (with all the other stuff).

## xiphon | 2019-08-12T17:09:31+00:00
Nope, i changed the logic.

Now i want to skip public nodes that are not synced with the network (having `height` less than known `target_height`).

## xiphon | 2019-08-12T17:13:23+00:00
>  I don't see a practical reason for duplicating this.

is a leftover from the current codebase, will replace it with `invoke_http_json_rpc` call.

https://github.com/monero-project/monero/blob/d4d5a4433d550499921d8dd5c1bacee66c7d116b/src/rpc/core_rpc_server.cpp#L1539

## xiphon | 2019-08-12T17:45:46+00:00
What issue do you want to address?

## xiphon | 2019-08-12T17:45:48+00:00
What issue do you want to address?

# Action History
- Created by: xiphon | 2019-08-09T10:50:19+00:00
- Merged at: 2019-09-04T14:27:34+00:00
