---
title: 'device/trezor: webusb transport added, cmake fixes'
source_url: https://github.com/monero-project/monero/pull/4824
author: ph4r05
assignees: []
labels: []
created_at: '2018-11-08T15:20:31+00:00'
updated_at: '2019-04-03T19:22:30+00:00'
type: pull_request
status: merged
closed_at: '2018-11-26T18:27:42+00:00'
merged_at: '2018-11-26T18:27:42+00:00'
---

# Original Description
- webusb transport based on libusb added. Provides direct access to Trezor via USB, no need for Trezor bridge.
- protocol message handler improved, no recursion used. Ready for upcoming integration tests.
- libusb (for docker) bumped from v1.0.9 to v1.0.22, newer version required for webusb transport, for device enumeration.  
- cmake improvements and fixes. As reported in #4810 by @TheCharlatan. Cmake Trezor checks are moved to a dedicated CheckTrezor.cmake file. In case of a problem Trezor is excluded from build.
- ifdefs made consistent to Ledger. 

# Discussion History
## sedited | 2018-11-09T16:17:14+00:00
I think a message to show that support is disabled might make sense as well. 

## ph4r05 | 2018-11-11T14:08:56+00:00
Makes sense, its added now

## moneromooo-monero | 2018-11-19T15:20:04+00:00
That seems like an odd name for the variable, referencing trezor when it's about libusb ?

## moneromooo-monero | 2018-11-19T15:24:49+00:00
This will presumably leave that data in memory when done. I'm guessing fixing that isn't trivial so OK to go in, but maybe make a note of it for later.

## moneromooo-monero | 2018-11-19T15:25:05+00:00
Same

## moneromooo-monero | 2018-11-19T15:31:02+00:00
That cast seems superfluous, what is its intent ?

## moneromooo-monero | 2018-11-19T15:35:52+00:00
Where does this output to ? Maybe it should be set if the relevant category is set to DEBUG or so ?

## moneromooo-monero | 2018-11-19T15:39:44+00:00
get_libub_ports can throw on error, so you won't get libusb_exit/free_device_list called in that case.

## moneromooo-monero | 2018-11-19T15:40:03+00:00
Same here about exceptions.

## moneromooo-monero | 2018-11-19T15:41:56+00:00
That seems to silently allow more close than necessary. Moaning loudly might help find any bug there.

## moneromooo-monero | 2018-11-19T15:45:47+00:00
Those two seem like they could be const.

## moneromooo-monero | 2018-11-19T15:46:07+00:00
const ?

## ph4r05 | 2018-11-19T21:08:59+00:00
The idea of `libdevice_trezor_lib` and `libdevice_trezor_hdr` was to contain other optional libraries for Trezor but for now it contains only LibUSB. At this point, I can remove it completely and just use `${LibUSB_INCLUDE_DIRS}` and `${LibUSB_LIBRARIES}`

## ph4r05 | 2018-11-19T21:20:15+00:00
This is tough one indeed. Protobuf generated code uses `std::string` to represent these fields. I will add comments here. It may be needed to change the Trezor protocol to use byte arrays for secrets in the future. 

## ph4r05 | 2018-11-20T12:36:45+00:00
I've added MERROR

## ph4r05 | 2018-11-20T12:39:40+00:00
I've checked the docs and it is:

```
LIBUSB_LOG_LEVEL_NONE (0) : no messages ever printed by the library (default)
LIBUSB_LOG_LEVEL_ERROR (1) : error messages are printed to stderr
LIBUSB_LOG_LEVEL_WARNING (2) : warning and error messages are printed to stderr
LIBUSB_LOG_LEVEL_INFO (3) : informational messages are printed to stdout, warning and error messages are printed to stderr
LIBUSB_LOG_LEVEL_DEBUG (4) : debug and informational messages are printed to stdout, warnings and errors to stderr
```

Now I set the debugging like this:
```
#if ELPP_TRACE_LOG || ELPP_DEBUG_LOG || ELPP_INFO_LOG
    libusb_set_debug(ctx, 3);
#elif ELPP_WARNING_LOG
    libusb_set_debug(ctx, 2);
#elif ELPP_ERROR_LOG
    libusb_set_debug(ctx, 1);
#endif
```

The question is whether to keep level 3 as it writes to stdout.

## ph4r05 | 2018-11-20T12:39:55+00:00
Fixed (should not be there)

## moneromooo-monero | 2018-11-20T13:10:13+00:00
That won't do it right. You want something like:

near the top of the file:
#undef MONERO_DEFAULT_LOG_CATEGORY
#define MONERO_DEFAULT_LOG_CATEGORY "device.trezor,transport"

Then:
if (ELPP->vRegistry()->allowed(el::Level::Debug, MONERO_DEFAULT_LOG_CATEGORY))
  libusb_set_debug(ctx, 3);
else if (ELPP->vRegistry()->allowed(el::Level::Warning, MONERO_DEFAULT_LOG_CATEGORY))
  libusb_set_debug(ctx, 2);
else if (ELPP->vRegistry()->allowed(el::Level::Error, MONERO_DEFAULT_LOG_CATEGORY))
  libusb_set_debug(ctx, 1);

## moneromooo-monero | 2018-11-20T13:14:50+00:00
Might be a tad dangerous since this does not check return code, and if an error is returned, t->m_port_numbers will hold whatever it was holding prior to the call. This is a local so it looks OK in this particular call though.

## moneromooo-monero | 2018-11-20T13:15:15+00:00
So this is pushed even if get_libusb_ports fails ?

## ph4r05 | 2018-11-20T13:24:01+00:00
The idea is to ignore the fail of `get_libusb_ports` (fail is logged as error). In that case the device complete address will not be determined but it does not seem like a fatal error (problem to distinguish 2 Trezor devices in the same tree). User just wont be able to address particular device on the USB tree but in the most cases it will just work as typically there is only one Trezor connected.

Does it sound OK?

## moneromooo-monero | 2018-11-20T13:30:29+00:00
Sure. If it's on purpose and you know it'll do the right thing, it's fine.

## moneromooo-monero | 2018-11-20T13:30:49+00:00
Might be worth a comment though.

## ph4r05 | 2018-11-20T13:35:48+00:00
Ah thanks! Good to know - its changed according to this now.

## ph4r05 | 2018-11-20T13:44:39+00:00
Added

## moneromooo-monero | 2018-11-20T14:15:04+00:00
Should probably return here instead of destroying things again. And maybe simplify the refcount code.

## ph4r05 | 2018-11-20T14:25:08+00:00
Giving it a second thought: `if (m_conn_count <= 0){` is just a safe condition. Code should not allow going to negative numbers as the close function eventually calls `m_conn_count = 0;` and the only place counter is decreased is in 

```
if (m_conn_count > 0){
    m_conn_count -= 1;
}
```

In these cases I always use `<=0` as more general option in case something goes wrong in the future, new bug and so on. 

> And maybe simplify the refcount code.

Idea how? :)

## ph4r05 | 2018-11-20T14:25:29+00:00
Ditto as comment below

## moneromooo-monero | 2018-11-20T14:30:14+00:00
Sure, but this code is supposed to catch things like open/close/close, right ?
If this happens, then:
open -> m_conn_count set to 1, usb stuff gets initialized
first close -> m_conn_count > 0, so gets set to 0, usb stuff gets shutdown
second close -> m_conn_count == 0, so usb stuff gets shutdown again

## ph4r05 | 2018-11-20T14:32:22+00:00
aah you are right! of course. fixing...

## moneromooo-monero | 2018-11-20T14:52:46+00:00
That's better, though I'd -=1 only if > 0, or you end up with somthing like close/open doing weird things in open (but then it's a programming error that's caused an error log already, so maybe we don't care that much now).

## moneromooo-monero | 2018-11-20T14:53:29+00:00
A '.' is usually used to separate subcategories. It's not *needed*, but is confusing.

## jtgrassie | 2018-11-21T13:56:56+00:00
This can fail. On my system for example the default python 3 is `python3.6`. Some people it may simply be `python` symlinked also.

## jtgrassie | 2018-11-21T13:57:53+00:00
Protobuf can be found but that doesnt mean the python-protobuf module is installed. 

## ph4r05 | 2018-11-21T13:58:37+00:00
In that case Trezor support is disabled and build succeeds anyway. 
As a workaround you can set `PYTHON3` env var.

## ph4r05 | 2018-11-21T13:59:08+00:00
As I commented in the issue, `python-protobuf` is not required. It is not a dependency, in this PR.

## ph4r05 | 2018-11-21T13:59:56+00:00
Anyway, as I said, if something goes wrong the whole Trezor support is disabled. User is then free to resolve the issue or ignore it. 

## jtgrassie | 2018-11-21T14:04:15+00:00
Yes I'm well aware of how to workaround it. Point is I shouldn't have to work around it.

## jtgrassie | 2018-11-21T14:06:06+00:00
> python-protobuf is not required

Why `if(Protobuf_FOUND` then?

## ph4r05 | 2018-11-21T14:06:32+00:00
#4822 says you want to disable Trezor build if something is wrong. So in this regard this PR fixes the issue. If you want more robust mechanism wait for #4839.

## ph4r05 | 2018-11-21T14:18:10+00:00
@jtgrassie I've updated #4839 with more robust Python check (better cmake). That should make it pretty robust. And still, there is a workaround if all `find_module` fails. This PR is already approved for merge so I don't want to add more changes at this point. 

## ph4r05 | 2018-11-21T14:19:06+00:00
`find_package(Protobuf)` has nothing to do with `python-protobuf`.

https://cmake.org/cmake/help/v3.8/module/FindProtobuf.html

"Locate and configure the Google Protocol Buffers library."

## ph4r05 | 2018-11-21T14:20:28+00:00
I am mainly interested in `Protobuf_PROTOC_EXECUTABLE`, `PROTOBUF_INCLUDE_DIRS` - as seen below. Python calls protoc binary, but does not use anything related to `python-protobuf`

## jtgrassie | 2018-11-21T14:26:15+00:00
I was literally just about to comment that the python search in #4839 was dependent on cmake 3.12+. TBH though, having the python / protobuf dependency at all is unnecessary. Nicer if the maintainer of trezor support just runs and commits the generated c++ output, thus removing the need for python or protobuf to build in trezor support.

## jtgrassie | 2018-11-21T14:36:28+00:00
Gotcha. OK, so I still recommend removing the python requirement for trezor builds (e.g. couldn't what you are using python for not be done just as easily in cmake alone?).

## ph4r05 | 2018-11-21T14:38:10+00:00
> I was literally just about to comment that the python search in #4839 was dependent on cmake 3.12+.

Yep I realized this module is new so I've added also the simple one for cmake 3.0+

> BH though, having the python / protobuf dependency at all is unnecessary. Nicer if the maintainer of Trezor support just runs and commits the generated c++ output, thus removing the need for python or protobuf to build in Trezor support.

I would say this is a matter of personal preferences. We discussed this with @moneromooo-monero and came to the conclusion that generating the messages during build time is quite easy so we can do it like this. 

Having generated code in the repository is a bit ugly and against best practices. The generated code is also quite huge and impossible to review manually so it is maybe better to generate it during compilation with a trusted compiler. 

Protobuf library is required also in runtime so I think if somebody wants to build trezor support it is not that much of a hassle to install protobuf compiler and python, which is almost omnipresent nowadays.

## ph4r05 | 2018-11-21T14:49:35+00:00
I was thinking about it but python script performs additional modifications to generated message files. Python provides quite nice flexibility in this. I find cmake way for this complex and currently not worth the effort as any default python3 installation will do the job OR python2 with `backports.tempfile` package.

I mean, it is still a soft requirement. It seems to me that python is quite common to be considered a problematic dependency nowadays. Are there users who want to have Trezor support enabled and don't have python installed? Or is it a problem to install? 

Removing python dependency has zero to low priority for me. My ToDo list is just too big :) If you want to remove it give it a try with PR.

# Action History
- Created by: ph4r05 | 2018-11-08T15:20:31+00:00
- Merged at: 2018-11-26T18:27:42+00:00
