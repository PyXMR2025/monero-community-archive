---
title: P2p changes
source_url: https://github.com/Cuprate/cuprate/pull/118
author: Boog900
assignees: []
labels:
- A-p2p
created_at: '2024-04-26T20:20:01+00:00'
updated_at: '2024-05-27T01:09:35+00:00'
type: pull_request
status: merged
closed_at: '2024-05-02T22:58:22+00:00'
merged_at: '2024-05-02T22:58:22+00:00'
---

# Original Description
No description

# Discussion History
## hinto-janai | 2024-04-30T20:32:46+00:00
Not sure what the use-case is here, just confirming these are semantically different.

`windows()` returns an empty iterator if `bytes.len() < T::SIZE`, `chunks()` returns a remainder iteration?

## hinto-janai | 2024-04-30T20:47:51+00:00
Why is `len() / N` and here `at * N`?

Also, I think if the behavior is the same, linking to the original docs would be better. If not, there's a `Bytes` reference left and `` formatting wasn't copied over.

## hinto-janai | 2024-04-30T20:58:07+00:00
nit:
```suggestion
bitflags! {
    impl PeerSupportFlags: u32 {
        const FLUFFY_BLOCKS = 0b0000_0001;
        const _ = !0;
    }
}
```

## hinto-janai | 2024-04-30T21:02:09+00:00
Should other code use this name too? `cumulative_difficulty_low64` instead of `cumulative_difficulty_low`?

## hinto-janai | 2024-04-30T21:03:15+00:00
`cuprate_helper::map::combine_low_high_bits_to_u128()`?

## hinto-janai | 2024-04-30T21:16:56+00:00
Suggestion: less indentation.

```rust
for _ in 0..3 {
    if let Some(needed_height) = block_needed {
        let (_, addresses_with_block) = self.pruning_seeds.iter().find(|(seed, _)| {
            // TODO: factor in peer blockchain height?
            seed.get_next_unpruned_block(needed_height, CRYPTONOTE_MAX_BLOCK_HEIGHT)
                .expect("Block needed is higher than max block allowed.")
                == needed_height
        })?;
        let n = r.gen_range(0..addresses_with_block.len());
        let peer = addresses_with_block[n];
        if must_keep_peers.contains(&peer) {
            continue;
        }
        return self.remove_peer(&peer);
    }

    let len = self.len();

    if len == 0 {
        return None;
    }

    let n = r.gen_range(0..len);
    let (&key, _) = self.peers.get_index(n).unwrap();
    if !must_keep_peers.contains(&key) {
        return self.remove_peer(&key);
    }
}
```

## hinto-janai | 2024-04-30T21:18:31+00:00
nit:
```suggestion
    /// A known address.
    KnownAddr(A),
    /// An unknown address (probably an inbound anonymity network connection).
    Unknown(u64),
```

## hinto-janai | 2024-04-30T21:19:01+00:00
nit:
```suggestion
            InternalPeerID::Unknown(id) => f.write_str(&format!("Unknown, ID: {id}")),
```

## hinto-janai | 2024-04-30T21:22:23+00:00
```suggestion
    /// A permit for the semaphore, will be [`Some`] after `poll_ready` returns ready.
    permit: Option<OwnedSemaphorePermit>,
```

## hinto-janai | 2024-04-30T21:24:35+00:00
```suggestion
        let permit = ready!(self.semaphore.poll_acquire(cx))
            .expect("Client semaphore should not be closed!");
```

## hinto-janai | 2024-04-30T21:26:36+00:00
This one's longer... There's other code like this too not in the diff, it's fine but I think this is what `expect()` is for.
```suggestion
        let permit = self
            .permit
            .take()
            .expect("poll_ready did not return ready before call to call");
```

## hinto-janai | 2024-04-30T21:54:54+00:00
Suggestion: less indentation.

```rust
if req.request.needs_response() {
    self.state = State::WaitingForResponse {
        request_id: req.request.id(),
        tx: req.response_channel,
        _req_permit: req.permit,
    };

    self.send_message_to_peer(req.request.into()).await?;
    // Set the timeout after sending the message, TODO: Is this a good idea.
    self.request_timeout = Some(Box::pin(sleep(REQUEST_TIMEOUT)));
    return Ok(());
}

// INVARIANT: This function cannot exit early without sending a response back down the
// response channel.
let res = self.send_message_to_peer(req.request.into()).await;
// send the response now, the request does not need a response from the peer.
if let Err(e) = res {
    let err_str = e.to_string();
    let _ = req.response_channel.send(Err(err_str.clone().into()));
    return Err(e);
}

// We still need to respond even if the response is this.
let _ = req.response_channel.send(Ok(PeerResponse::NA));
Ok(())
```

## hinto-janai | 2024-04-30T21:56:51+00:00
```suggestion
        // If the message is definitely a request then there is no way it can be a response to
        // our request.
```

## hinto-janai | 2024-04-30T22:02:49+00:00
```suggestion
//! This is where outbound connections are created.
```

## hinto-janai | 2024-04-30T22:03:30+00:00
nit:
```suggestion
    /// The peer's address.
```

## Boog900 | 2024-04-30T22:58:07+00:00
We check above this that the len is a multiple of `T::SIZE`

## Boog900 | 2024-04-30T23:00:54+00:00
This is a byte array vec, i.e. a Vec<[u8; N]> with a backing representation of `Bytes` which is pretty much a Vec<u8> so all operations are scaled by N to account for this.

## Boog900 | 2024-04-30T23:01:58+00:00
no need IMO, I only did it here to match `cumulative_difficulty_top64`

## Boog900 | 2024-04-30T23:02:51+00:00
This crate doesn't currently pull this crate in so I would rather keep this as is.

## Boog900 | 2024-04-30T23:10:30+00:00
True, I changed this, not really a big issue though so I don't really want to go through the code to find these

## hinto-janai | 2024-05-01T00:11:06+00:00
Ah, I didn't edit https://github.com/Cuprate/cuprate/pull/118#discussion_r1585534711 in time I guess - I introduced a bug here, it should be reverted.

If this doesn't `continue` then the code below will execute, this is different behavior than before. Should be:
```rust
if must_keep_peers.contains(&peer) {
    continue;
}

return self.remove_peer(&peer);
```

## Boog900 | 2024-05-01T00:24:58+00:00
Ah, good spot!

# Action History
- Created by: Boog900 | 2024-04-26T20:20:01+00:00
- Merged at: 2024-05-02T22:58:22+00:00
