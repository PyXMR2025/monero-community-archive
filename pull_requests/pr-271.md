---
title: cryptonight in pure Rust
source_url: https://github.com/Cuprate/cuprate/pull/271
author: dimalinux
assignees: []
labels:
- A-dependency
- A-workspace
- A-cryptonight
created_at: '2024-09-05T23:33:27+00:00'
updated_at: '2024-10-08T15:03:57+00:00'
type: pull_request
status: merged
closed_at: '2024-10-08T15:03:57+00:00'
merged_at: '2024-10-08T15:03:57+00:00'
---

# Original Description
This PR ports the Cryptonote slow hash to Rust eliminating the C code. It doesn't use unsafe and, for better or worse, tries to follow the C code reasonably closely.

Closes #28


# Discussion History
## hinto-janai | 2024-09-06T00:41:44+00:00
Some of these can be replaced with `{ workspace = true }`, e.g. `hex = { workspace = true }`.

Also is there a reason `pre` and `alpha` versions are used instead of the normal releases?

## hinto-janai | 2024-09-06T00:51:20+00:00
Forgot to remove I think.

## hinto-janai | 2024-09-06T00:53:04+00:00
I don't think core needs to be explicit imported.

```suggestion
mod cnaes;
```

## hinto-janai | 2024-09-06T00:55:32+00:00
Are these macros meant to be public? This makes them usable to users of the crate, not just internally.

## dimalinux | 2024-09-06T06:33:19+00:00
I removed that line. Not sure how it got in there.

## dimalinux | 2024-09-06T08:09:15+00:00
I downgraded them. I think those "pre" and "alpha" dependencies may have been imported using RustRover instead of `cargo add`.  I moved `hex` to the `dev-dependencies` section, but it was having issues finding the `encode` method if I tried setting the version from the workspace. I was able to update the existing `thiserror` dependency to get its version from the workspace though.

## dimalinux | 2024-09-06T08:09:39+00:00
Yeah, removed.

## dimalinux | 2024-09-06T08:29:33+00:00
Importing macros between modules of the same crate was trickier than I expected. I'll reduce their scope tomorrow. I did document those macros with the thought that, if the rest of the code base is dealing with u8 arrays (with subarrays) a lot, the macros might be useful project-wide.

## Boog900 | 2024-09-06T14:44:35+00:00
This could be a function:

```suggestion
#[inline]
fn subarray<const N: usize, T>(arr: &[T], start: usize) -> &[T; N] {
    arr[start..(start + N)].try_into().unwrap()
}
```

## Boog900 | 2024-09-06T14:47:47+00:00
same here:
```suggestion
#[inline]
fn subarray_copy<const N: usize, T: Copy>(arr: &[T], start: usize) -> [T; N] {
    *subarray(arr, start)
}
```

## Boog900 | 2024-09-06T14:50:03+00:00
```suggestion
#[inline]
fn subarray_mut<const N: usize, T>(arr: &mut [T], start: usize) -> &mut [T; N] {
    arr[start..(start + N)].as_mut().try_into().unwrap()
}
```

## Boog900 | 2024-09-06T14:59:04+00:00
We roughly order imports: std, outside crates, cuprate crates, current crate.

## Boog900 | 2024-09-06T15:06:33+00:00
I realised this is just for `u8`s so you could remove the generic `T` if you wanted. 

## dimalinux | 2024-09-06T21:23:06+00:00
It's fixed now.

## dimalinux | 2024-09-07T01:58:34+00:00
@Boog900 Can we add automation for this (not in this PR)?  Does this look like the `.rustfmt.toml` options that correspond to the desired ordering?

```
group_imports = "StdExternalCrate"
reorder_modules = true # default
reorder_impl_items = true
imports_granularity = "crate"
```
The code now is formatted with those lines above. I just added a `.rustfmt.toml` file to the crate for formatting, but didn't check the file in.

## dimalinux | 2024-09-07T04:00:26+00:00
I converted all 3 macros to functions and kept the elements generic. There was one place in the v4 hash code that `subarray_mut` was useful on an array of `u32`.

## dimalinux | 2024-09-07T04:01:59+00:00
Converted.

## dimalinux | 2024-09-07T04:02:41+00:00
Converted.

## dimalinux | 2024-09-07T04:38:27+00:00
They are not even macros anymore, so I'll resolve this convo.

## hinto-janai | 2024-09-12T00:39:11+00:00
> I moved hex to the dev-dependencies section, but it was having issues finding the encode method

This is because `default-features = false`, this works:

```suggestion
hex = { workspace = true, features = ["std"] }
```

## hinto-janai | 2024-09-12T00:40:40+00:00
It may be worth storing the old `cryptonight/` as `old_cryptonight/` just in case like https://github.com/Cuprate/cuprate/pull/100.

@Boog900 any thoughts?

## hinto-janai | 2024-09-12T00:48:00+00:00
Just some tests that assert the panic docs.
```suggestion
    #[test]
    #[should_panic(expected = "range end index 4 out of range for slice of length 1")]
    fn subarray_panic() {
        let array = [1_u8];
        let _: &[u8; 3] = subarray(&array, 1);
    }

    #[test]
    #[should_panic(expected = "range end index 4 out of range for slice of length 1")]
    fn subarray_copy_panic() {
        let array = [1_u8];
        let _: [u8; 3] = subarray_copy(&array, 1);
    }

    #[test]
    #[should_panic(expected = "range end index 4 out of range for slice of length 1")]
    fn subarray_mut_panic() {
        let mut array = [1_u8];
        let _: &mut [u8; 3] = subarray_mut(&mut array, 1);
    }
}
```

## hinto-janai | 2024-09-12T00:51:46+00:00
`///` is preferred to `//` even on private items as:

1. `cargo doc` lints will run on it
1. It shows up on `cargo doc --document-private-items`

Applies to other `//` comments throughout the PR as well.

## hinto-janai | 2024-09-12T01:06:35+00:00
You can un-indent this block by early returning.

```suggestion
    if !matches!(variant, Variant::V2 | Variant::R) {
        return;
    }

    let chunk1_start = offset ^ 0x10;
    let chunk2_start = offset ^ 0x20;
    let chunk3_start = offset ^ 0x30;

    let chunk1: &[u8; AES_BLOCK_SIZE] = subarray(long_state, chunk1_start);
    let chunk2: &[u8; AES_BLOCK_SIZE] = subarray(long_state, chunk2_start);
    let chunk3: &[u8; AES_BLOCK_SIZE] = subarray(long_state, chunk3_start);

    let mut chunk1_old = block_to_u64le(chunk1);
    let chunk2_old = block_to_u64le(chunk2);
    let chunk3_old = block_to_u64le(chunk3);

    let b1 = block_to_u64le(subarray(b, 16));

    let chunk1 = &mut long_state[chunk1_start..chunk1_start + 16];
    chunk1[0..8].copy_from_slice(&(chunk3_old[0].wrapping_add(b1[0]).to_le_bytes()));
    chunk1[8..16].copy_from_slice(&(chunk3_old[1].wrapping_add(b1[1]).to_le_bytes()));

    let a0 = block_to_u64le(a);

    let chunk3 = &mut long_state[chunk3_start..chunk3_start + 16];
    chunk3[0..8].copy_from_slice(&(chunk2_old[0].wrapping_add(a0[0])).to_le_bytes());
    chunk3[8..16].copy_from_slice(&(chunk2_old[1].wrapping_add(a0[1])).to_le_bytes());

    let b0 = block_to_u64le(subarray(b, 0));
    let chunk2 = &mut long_state[chunk2_start..chunk2_start + 16];
    chunk2[0..8].copy_from_slice(&(chunk1_old[0].wrapping_add(b0[0])).to_le_bytes());
    chunk2[8..16].copy_from_slice(&(chunk1_old[1].wrapping_add(b0[1])).to_le_bytes());

    if variant == Variant::R {
        let mut out_copy = block_to_u64le(c1);

        chunk1_old[0] ^= chunk2_old[0];
        chunk1_old[1] ^= chunk2_old[1];
        out_copy[0] ^= chunk3_old[0];
        out_copy[1] ^= chunk3_old[1];
        out_copy[0] ^= chunk1_old[0];
        out_copy[1] ^= chunk1_old[1];

        c1[0..8].copy_from_slice(&out_copy[0].to_le_bytes());
        c1[8..16].copy_from_slice(&out_copy[1].to_le_bytes());
    }
```

## hinto-janai | 2024-09-12T01:06:59+00:00
```suggestion
#[expect(
    clippy::cast_sign_loss,
    clippy::cast_precision_loss,
    clippy::cast_possible_truncation
)]
```

## hinto-janai | 2024-09-12T01:08:50+00:00
Un-indent.

```suggestion
    const U32_MASK: u64 = u32::MAX as u64;

    if variant != Variant::V2 {
        return;
    }

    let tmpx = *division_result ^ (*sqrt_result << 32);
    *c2 = (u64::from_le_bytes(*c2) ^ tmpx).to_le_bytes();

    let c1_64 = block_to_u64le(c1);
    let mut divisor = c1_64[0];
    let dividend = c1_64[1];

    divisor = ((divisor + ((*sqrt_result << 1) & U32_MASK)) | 0x80000001) & U32_MASK;
    *division_result = ((dividend / divisor) & U32_MASK).wrapping_add((dividend % divisor) << 32);

    let sqrt_input = c1_64[0].wrapping_add(*division_result);
    *sqrt_result = variant2_integer_math_sqrt(sqrt_input);
```

## hinto-janai | 2024-09-12T01:10:40+00:00
Non-capturing closure can just be a `fn`.
```suggestion
        fn test(
            c2_hex: &str,
            c1_hex: &str,
            division_result: u64,
            sqrt_result: u64,
            c2_hex_end: &str,
            division_result_end: u64,
            sqrt_result_end: u64,
        ) {
            let mut c2: [u8; 16] = hex_to_array(c2_hex);
            let c1: [u8; 16] = hex_to_array(c1_hex);
            let mut division_result = division_result;
            let mut sqrt_result = sqrt_result;

            variant2_integer_math(
                subarray_mut(&mut c2, 0),
                &c1,
                &mut division_result,
                &mut sqrt_result,
                Variant::V2,
            );

            assert_eq!(hex::encode(c2), c2_hex_end);
            assert_eq!(division_result, division_result_end);
            assert_eq!(sqrt_result, sqrt_result_end);
        }

        test(
```

## hinto-janai | 2024-09-12T01:11:28+00:00
Same here.

```suggestion
        fn test(
            c1_hex: &str,
            a_hex: &str,
            b_hex: &str,
            offset: usize,
            variant: Variant,
            c1_hex_end: &str,
            long_state_end_hash: &str,
        ) {
            let mut c1: [u8; AES_BLOCK_SIZE] = hex_to_array(c1_hex);
            let a: [u8; AES_BLOCK_SIZE] = hex_to_array(a_hex);
            let b: [u8; AES_BLOCK_SIZE * 2] = hex_to_array(b_hex);

            let mut long_state = vec![0_u8; MEMORY];
            let long_state: &mut [u8; MEMORY] = subarray_mut(&mut long_state, 0);
            for (i, byte) in long_state.iter_mut().enumerate() {
                *byte = u8::try_from(i & 0xFF).unwrap();
            }

            variant2_shuffle_add(&mut c1, &a, &b, long_state, offset, variant);
            assert_eq!(hex::encode(c1), c1_hex_end);
            let hash = Groestl256::digest(long_state.as_slice());
            assert_eq!(hex::encode(hash), long_state_end_hash);
        }

        test(
```

## hinto-janai | 2024-09-12T01:12:48+00:00
```suggestion
        fn test(key_hex: &str, expected_out: &str) {
            let key = hex_to_array(key_hex);
            let expanded_key = key_extend(&key);
            assert_eq!(expected_out, hex::encode(expanded_key));
        }

        test(
```

## hinto-janai | 2024-09-12T01:13:20+00:00
```suggestion
        fn test(key_hex: &str, input_hex: &str, expected_out: &str) {
            let key: [u8; 32] = hex_to_array(key_hex);
            let extended_key = key_extend(&key);
            let mut block: [u8; 16] = hex_to_array(input_hex);

            aesb_pseudo_round(&mut block, &extended_key);
            assert_eq!(expected_out, hex::encode(block));
        }
```

## hinto-janai | 2024-09-12T01:18:07+00:00
Extra parentheses aren't needed in Rust, only needed for separating operations.
```suggestion
        while (latency[0] < TOTAL_LATENCY
            || latency[1] < TOTAL_LATENCY
            || latency[2] < TOTAL_LATENCY
            || latency[3] < TOTAL_LATENCY)
            && num_retries < 64
```

## hinto-janai | 2024-09-12T01:19:38+00:00
```suggestion
            if matches!(opcode, Add | Sub | Xor) && a == b {
```

## hinto-janai | 2024-09-12T01:20:22+00:00
```suggestion
            if opcode != Mul
                && inst_data[a] & 0xFFFF00
                    == ((opcode as usize) << 8) + ((inst_data[b] & 255) << 16)
```

## hinto-janai | 2024-09-12T01:21:46+00:00
```suggestion
                    if alu_busy[next_latency][i] {
                        continue;
                    }

                    if opcode == Add && alu_busy[next_latency + 1][i] {
                        continue;
                    }

                    if is_rotation[opcode as usize]
                        && next_latency < (rotate_count * OP_LATENCY[opcode as usize])
                    {
                        continue;
                    }

                    alu_index = i as isize;
                    break;
```

## hinto-janai | 2024-09-12T01:29:37+00:00
Ah, ok, I see you took the closure tests from here from... @Boog900 https://github.com/Cuprate/cuprate/commit/55b7699a82fb48bfbfd4fabe5e9f3a6b9697bcac.

They can/should still be `fn` though.

## dimalinux | 2024-09-13T05:57:56+00:00
It's always available in git if we need to pull it back in. This replacement is feature complete and keeps the same API.

If you decide that you want it back in, let me know and I'll add it back as `old_cryptonight`.

## dimalinux | 2024-09-13T06:25:31+00:00
I fixed them throughout the crate. It would be nice if there was a clippy option to flag those, but I couldn't find one.

## dimalinux | 2024-09-13T06:48:52+00:00
> They can/should still be fn though.

I'm not sure I understand that last sentence. I left the unit tests in `lib.rs` as they were, as the external API here hasn't changed. These tests are using the expected values from the regression suite in the C++ repo. The extra commas were added by a cargo fmt or clippy command, not because I was trying to introduce extra changes.

The underlying `cn_slow_hash`  that implements all the versions also has a bunch of new tests generated for it which used the C code to get the expected values.

## dimalinux | 2024-09-13T07:47:23+00:00
I understand the comment now. I converted the preexisting `test` lambdas to nested `test` functions in `lib.rs`. I had modeled the other tests in this crate on these existing tests, that's why they were all using a lambda named `test` instead of a nested function.

## Boog900 | 2024-09-15T00:29:08+00:00
`group_imports` is currently unstable and we would need an extra option: `StdExternalWorkspaceCrate`.

We could enable the others, but I would have to look at the open issues as they are unstable. 

## Boog900 | 2024-09-15T00:40:39+00:00
See this comment: https://github.com/Cuprate/cuprate/pull/271#issuecomment-2351252391

I don't think we need to keep the old cryptonight around however I think making a fuzzing tool that works on both the new and old code would be good.

## dimalinux | 2024-09-15T01:31:54+00:00
All 4 versions of the hash have already had significant fuzzing run on them. As soon as I had a working version, but before I eliminated the C code, all the `cryptonight_hash_*` functions where running both versions of the code and asserting that they returned identical results before returning the hash value.  I also had code generating randomized inputs and testing in loops. I even ran it overnight once with no problems.
```rust
        for _ in 0..COUNT {
            let mut rng = rand::thread_rng();
            let length = rng.gen_range(43..=1024);
            let mut input = vec![0_u8; length];
            rng.fill(&mut input[..]);

            let _ = cryptonight_hash_v0(&input);
            let _ = cryptonight_hash_v1(&input).unwrap();
            let _ = cryptonight_hash_v2(&input);
            let _ = cryptonight_hash_r(&input, rng.random::<u64>());
        }
 ```
That fuzz test won't work anymore though, as those hashes are no longer running both implementations and comparing the results. And the not-very-significant changes for PR feedback haven't been fuzzed.

## dimalinux | 2024-09-17T07:04:29+00:00
Here's the requested fuzzing tool:
https://github.com/dimalinux/cryptonight-fuzzer

## hinto-janai | 2024-09-19T00:54:14+00:00
Were these loops unrolled? How much faster is this?

## Boog900 | 2024-09-20T20:44:39+00:00
I got an overflow here in debug mode

## dimalinux | 2024-09-20T21:46:50+00:00
Yeah, that should have been using `wrapping_add`. All this hash code was designed with intentional integer overflow and I was probably overcautious of not adding `wrapping_add` in places I didn't think overflow could be triggered. If you saved the hash input, please share it. I added a contrived unit test directly to `variant2_integer_math` that would trigger the previous code.

## dimalinux | 2024-09-20T22:09:28+00:00
Yes, I unrolled the loops and switched to u32 xor operations. I didn't record the exact numbers, but I was using the fuzzer on the release build with debug symbols enabled and trimmed down the infinite loop in `main` to just a few cycles. It took `round_fwd` down from about 70% down to 53% of the total.
![image](https://github.com/user-attachments/assets/16053b55-5731-48c5-bcd9-739aa1b40f57)


## Boog900 | 2024-09-20T22:53:55+00:00
Sadly I don't, the panic didn't cause it to print the hash input 

# Action History
- Created by: dimalinux | 2024-09-05T23:33:27+00:00
- Merged at: 2024-10-08T15:03:57+00:00
