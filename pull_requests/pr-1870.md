---
title: 'openalias: usability improvements on addressbook/transfer page'
source_url: https://github.com/monero-project/monero-gui/pull/1870
author: mmbyday
assignees: []
labels: []
created_at: '2018-12-28T17:33:33+00:00'
updated_at: '2019-02-14T17:15:44+00:00'
type: pull_request
status: merged
closed_at: '2019-02-14T17:15:44+00:00'
merged_at: '2019-02-14T17:15:44+00:00'
---

# Original Description
resolves #1072

- [x] sending : adds placeholderText to indicate openalias support
- [x] sending tab address validation :  resolved by PR #1849 
- [x] transaction history : adds openalias to description field, which is displayed in transaction history
- [x] address book : add openalias resolve button, update placeholderText, adde openalias address validation, and saves openalias address to description field.

![getxmr](https://user-images.githubusercontent.com/40871101/50523184-88957300-0a83-11e9-8509-bde85118a3fa.gif)

# Discussion History
## selsta | 2018-12-28T18:40:37+00:00
Would it be possible avoid code duplication here?

## mmbyday | 2018-12-28T19:32:48+00:00
Do you mean resolveOpenAlias vs isValidOpenAliasAddress? Not really as they do different things.

## selsta | 2018-12-28T19:34:58+00:00
```
                var result = walletManager.resolveOpenAlias(addressLine.text)
	                if (result) {
	                    var parts = result.split("|")
	                    if (parts.length == 2) {
	                        var address_ok = walletManager.addressValid(parts[1], appWindow.persistentSettings.nettype)
	                        if (parts[0] === "true") {
	                            if (address_ok) {
	                                // prepend openalias to description
	                                descriptionLine.text = descriptionLine.text ? addressLine.text + " " + descriptionLine.text : addressLine.text
	                                addressLine.text = parts[1]
	                                addressLine.cursorPosition = 0
	                            }
	                            else
	                                oa_message(qsTr("No valid address found at this OpenAlias address"))
	                        }
	                        else if (parts[0] === "false") {
	                              if (address_ok) {
	                                  addressLine.text = parts[1]
	                                  addressLine.cursorPosition = 0
	                                  oa_message(qsTr("Address found, but the DNSSEC signatures could not be verified, so this address may be spoofed"))
	                              }
	                              else
	                              {
	                                  oa_message(qsTr("No valid address found at this OpenAlias address, but the DNSSEC signatures could not be verified, so this may be spoofed"))
	                              }
	                        }
	                        else {
	                            oa_message(qsTr("Internal error"))
	                        }
	                    }
	                    else {
	                        oa_message(qsTr("Internal error"))
	                    }
	                }
	                else {
	                    oa_message(qsTr("No address found"))
	                }
```

This whole code block is in `pages/Transfer.qml` and `pages/AddressBook.qml` now. Although I’m not sure if you can avoid this easily.

## mmbyday | 2018-12-30T06:09:30+00:00
Not worth it in this PR. Transfer page is changing quite a bit still. Better after. In a future PR, this could be reorganized and refactored into a component. Also this definitely could be refactored to be more readable.

## sanderfoobar | 2019-02-01T15:00:50+00:00
`small: true` here would be nice :)

## mmbyday | 2019-02-01T18:56:53+00:00
Thanks :)

# Action History
- Created by: mmbyday | 2018-12-28T17:33:33+00:00
- Merged at: 2019-02-14T17:15:44+00:00
