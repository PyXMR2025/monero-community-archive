---
title: 'wallet: Improve Fee Priority Code for Clarity'
source_url: https://github.com/monero-project/monero/pull/9838
author: Tzadiko
assignees: []
labels:
- wallet
created_at: '2025-03-16T17:42:54+00:00'
updated_at: '2025-08-20T09:22:44+00:00'
type: pull_request
status: merged
closed_at: '2025-07-10T12:45:50+00:00'
merged_at: '2025-07-10T12:45:50+00:00'
---

# Original Description
Hello,

I'm working on https://github.com/monero-project/monero/issues/9799 . After speaking to @nahuhh , I'd like to contribute in two steps. The first step is to clean the existing code, and then addressing the issue. This makes reviewing my changeset easier.

In this changeset there are no changes to functionality. I added fee_priority .h and .cpp. Here I added the enumeration FeePriority to replace the integer values which were confusing to understand when trying to address the original issue linked above.

I will now be looking at addressing the original issue linked, working off this 'clean up' branch.

# Discussion History
## 0xFFFC0000 | 2025-03-18T04:03:56+00:00
if (req.priority > 4) {
         LOG ERROR;
         er.message = "Invalid priority value. Must be between 0 and 4.";
     }

## Tzadiko | 2025-03-18T04:50:20+00:00
Thanks, please see the commit where I add this: [41b71b9](https://github.com/monero-project/monero/pull/9838/commits/41b71b902a3c240e917b0b13fe1d12a2e0370061)

## vtnerd | 2025-05-12T16:21:33+00:00
How many places is this used? Can the usage of `allowed_priority_strings` just be updated to use the other `static` `array`?

If you keep it, make it `constexpr`.

## vtnerd | 2025-05-12T16:22:07+00:00
Only `src/wallet/api/` uses camel case, everything else uses snake case. Please update everything to snake case.

## vtnerd | 2025-05-12T16:25:30+00:00
Small nitpick, but I don't see a reason for a `class`. Maybe a `namespace`.

## vtnerd | 2025-05-12T16:29:53+00:00
These two should definitely be marked `constexpr`. Make sure no initialization order fiasco stuff happens.

Also - why put them in `private` and `return` with a function when it's `const` anyway?  Just expose the inner type directly.

## vtnerd | 2025-05-12T16:35:23+00:00
Clamp here?

## vtnerd | 2025-05-12T16:38:01+00:00
Again, as before this should arguably by a `namespace` instead of a `class`.

## vtnerd | 2025-05-12T16:39:51+00:00
This change caught a type mismatch here, perhaps a `boost::numeric_cast<unsigned>(as_integer(m_default_priority))`?

You could also make `as_integer_clamp` (or some better name) which takes a templated return type, and then clamp. This is arguably better than wrapping (which is what the code does now).

## vtnerd | 2025-05-12T16:40:45+00:00
This looks like another potential use for `numeric_cast` + `from_integral`.

## vtnerd | 2025-05-12T16:41:08+00:00
Clamp here (where clamp does an upper range clamp)?

## vtnerd | 2025-05-12T16:44:13+00:00
Good opportunity to see if all of this can be made `constexpr` (it should be possible fairly easily).

## vtnerd | 2025-05-12T16:46:38+00:00
`#include <iosfwd>` should be here too.

## vtnerd | 2025-05-12T16:49:39+00:00
Given the name, I would expect this to do an upper-bound clamping too.

## vtnerd | 2025-05-12T16:50:38+00:00
Should this call an (upgraded) `clamp` so that `at` doesn't throw when exceeds the bounding condition?

## vtnerd | 2025-05-12T19:51:52+00:00
Should the `priority < 1 || priority > 4` check be updated above? If another priority is added to the `enum` list, that check could get missed.

## vtnerd | 2025-05-12T19:53:04+00:00
Why even have `allowed_prioriry_strings.size()` if you just do a call to `ToString`?

## vtnerd | 2025-05-12T22:23:59+00:00
Seems more appropriate todo a `as_integral` conversion first, then check that value against `index < 0 && std::size(fee_steps) <= std::size(fee_steps)` etc. Otherwise this check has to be updated every time a new fee algorithm enum is added. Basically, its just more difficult to verify correctness as I have to know the value of `HardforkV8`, etc., to know this works.

## vtnerd | 2025-05-12T23:01:32+00:00
The `double` changes seems unrelated and unnecessary (revert).

## Tzadiko | 2025-05-14T00:11:52+00:00
I prefer to keep it as a class incase I need to add state to the class later. I'll leave as is since this is a nit.

## Tzadiko | 2025-05-14T00:14:55+00:00
Done

## Tzadiko | 2025-05-14T00:16:38+00:00
Done

## Tzadiko | 2025-05-14T00:28:02+00:00
It's used in 6 places, so I'd rather keep the changeset small by just replacing how it's created.

## Tzadiko | 2025-05-14T00:35:02+00:00
Done

## Tzadiko | 2025-05-14T02:18:38+00:00
Done

## Tzadiko | 2025-05-14T02:26:20+00:00
I'm used to writing utilities as classes. I get that you can have them as free function in namespace scope, and that's valid too. Maybe moreso of a habit to 'sub-group' logical units in classes (i.e. namespace -> class -> method, instead of namespace -> method).

## Tzadiko | 2025-05-14T02:53:35+00:00
What is the scope of 'all of this'? You want me to mark get_fee_multiplier as constexpr? What's the point? Are any of these methods called in a constexpr context?

## Tzadiko | 2025-05-14T02:56:07+00:00
The upper bound is clamped by virtue of it being an enum, so not sure what you mean. Do you mean I add another value to fee_priority and call it UpperLimit, so I have

enum class Something
{
    Default
    Unimportant
    ...
    Priority
    Upperbound // Upper clamp
}

I don't think it's a good idea, since it'll be adding more complexity to fee_priority, which already is confusing to understand (we're mapping fee_priority to an actionable fee_priority which is a remnant of how the code was done before.

## Tzadiko | 2025-05-14T03:01:06+00:00
Im not sure why I'd need a clamp here. There are five enum values and five strings. They map 1-to-1. I'm assuming you're referring to a case where an enumeration is added later but the `string_` is not changed, in that case there is the opportunity to throw. I'm not sure what a clamp would do there though. I added a comment for the next person to add to the string_ member variable when a fee_priority enum is added.

## Tzadiko | 2025-05-14T03:05:08+00:00
I changed it to use fee_priority enum.

## Tzadiko | 2025-05-14T03:08:21+00:00
Removed

## Tzadiko | 2025-05-14T03:23:48+00:00
Done

## Tzadiko | 2025-05-14T03:59:40+00:00
Done boost approach.

## Tzadiko | 2025-05-14T03:59:42+00:00
Done boost approach.

## Tzadiko | 2025-05-14T04:07:22+00:00
std::size() is a template function and relies on template type deduction. Since it requires the type of the array to be deducible, it won't work if the array is of an anonymous type. Changed the type to a struct (adding the struct fee_step in its place) and made fee_steps an array of type fee_step.

## Tzadiko | 2025-05-14T04:08:26+00:00
There is clamping now for the upperbound when going from integer type to enum in FromIntegral.

## vtnerd | 2025-05-19T23:46:39+00:00
The values in global `static` memory should be `constexpr` where possible to avoid the initialization order fiasco. This doesn't help us/Monero frequently, as we don't have anything crazy happening before `int main`, but still helps with program correctness.

## vtnerd | 2025-05-19T23:48:15+00:00
Just some defensive programming. The `enum` value can be cast to any value, and doesn't have to be one of the types defined.

## vtnerd | 2025-05-19T23:48:18+00:00
Same thing here about casting to an `enum`.

## vtnerd | 2025-05-21T17:20:00+00:00
This is not accurate (`std::size` will work on this context). I just did a quick prototype to verify that this works.

Your suggested changes mostly work, but my original suggestion is more desirable as it doesn't require a dummy `enum` value.

## Tzadiko | 2025-05-21T18:05:06+00:00
I still don't understand what you mean here. This method does one think only, that being a lowerbound clamp (Default -> Uniportant). Why would I need to add an upperbound clamp? I understand the need for an upperbound clamp for integer -> FeePriority conversions (since integer can be > FeePriority), but an upperbound clamp doesn't make sense in the context of FeePriority -> FeePriority. 

## Tzadiko | 2025-05-21T18:06:49+00:00
I'm confused on your suggested approach here because its an extension of [this](https://github.com/monero-project/monero/pull/9838#discussion_r2100886290) suggestion, which I didn't understand. There is one string_ for each FeePriority, where each string's index maps to the underlying enum value. I understand that if we add another FeePriority, we'll need to ensure that the developer doesn't forget to add a string. Ideally we catch this at compile time, so I considered a static assert

```cpp
static_assert(static_cast<uint32_t>(feePriorityStrings_.size()) == AsIntegral(fee_priority::LargestPossibleFeePriority), "A fee priority string must exist for each fee priority.");
```

in case the enum is expanded. I don't like the static assert because it introduces another enum value that adds confusion to an already confusing translation between 0-4 and 1-4 (default is mapped to unimportant). Can you be more specific on your suggested approach, please?

## Tzadiko | 2025-05-21T18:13:54+00:00
I don't know what I did to lead me to that conclusion initially. I just tried it with a mock [example](https://godbolt.org/z/W7ndaGa8x) in Godbolt and it worked. I don't know how I feel about reverting it though. What's wrong with being more expressive with a struct (I think you meant 'dummy struct' instead of 'dummy enum')? I think it increases the readability here, which is the primary purpose of this MR.

## Tzadiko | 2025-05-21T18:36:22+00:00
I've made fee_steps static constexpr instead of static const.

## vtnerd | 2025-05-23T02:03:05+00:00
```c++
fee_priority_utlities::clamp(fee_priority(std::numeric_limits<std::uint32_t>::max()));
```

would `return fee_priority::priority;`

So that `clamp` literally clamps all values to the valid range. Whereas currently it just means "do something if ` == fee_priority::default_`.

## vtnerd | 2025-05-23T02:05:22+00:00
I would say just get use to namespaces, because the remainder of the codebase is written in that style.

## vtnerd | 2025-05-23T02:07:42+00:00
I was suggesting possibly clamping `priority` values to the `0...4` range so that `.at(...)` never throws. Since `.at(std::numeric_limits<std::uin32_t>::max())` has defined behavior its not really needed, and perhaps hides potential bugs.

## Tzadiko | 2025-05-27T16:17:09+00:00
I did not know this (`fee_priority(std::numeric_limits<std::uint32_t>::max())`) was possible. Learning new things. 😃 . I get it now, addressed.

## Tzadiko | 2025-05-27T16:51:13+00:00
Done

## Tzadiko | 2025-05-27T16:51:24+00:00
Done

## vtnerd | 2025-06-13T22:40:06+00:00
Its nitpick, but there's still a bunch of camel case in this patch. Typically everything is snake case.

## vtnerd | 2025-06-13T22:40:30+00:00
`as_integral`, etc.

## Tzadiko | 2025-06-14T01:10:06+00:00
See comment below: https://github.com/monero-project/monero/pull/9838#issuecomment-2972073678 Done

## Tzadiko | 2025-06-14T01:10:23+00:00
See comment below: https://github.com/monero-project/monero/pull/9838#issuecomment-2972073678 Done

## vtnerd | 2025-06-14T02:16:44+00:00
Missed this last time. This should be marked `constexpr` as well.

## Tzadiko | 2025-06-15T20:01:07+00:00
Thank you, done. Squashed and pushed.

# Action History
- Created by: Tzadiko | 2025-03-16T17:42:54+00:00
- Merged at: 2025-07-10T12:45:50+00:00
