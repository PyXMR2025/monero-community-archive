---
title: Eliminate dependence on boost::interprocess
source_url: https://github.com/monero-project/monero/pull/8223
author: jeffro256
assignees: []
labels: []
created_at: '2022-03-19T22:27:58+00:00'
updated_at: '2022-04-22T20:02:03+00:00'
type: pull_request
status: merged
closed_at: '2022-04-06T04:20:41+00:00'
merged_at: '2022-04-06T04:20:41+00:00'
---

# Original Description
In this repo, `boost::interprocess` was being used soley to make `uint32_t` operations atomic. So I replaced each instance of
`boost::interprocess::ipcdetail::atomic(...)32` with `std::atomic` methods. I replaced member declarations as applicable. For example, when I needed to change a `volatile uint32_t` into a `std::atomic<uint32_t>`. Sometimes, a member was being used a boolean flag, so I replaced it with `std::atomic<bool>`.

You may notice that I didn't touch `levin_client_async.h`. That is because this file is entirely unused and will be deleted in PR #8211.

When I get more time, I will benchmark this to see if this changed the compile time in any manner.

# Discussion History
## UkoeHB | 2022-03-19T23:59:27+00:00
```suggestion
    const uint32_t local_thr_index = m_thread_index;
    ++m_thread_index;
```

Clarify the code's intent (reader doesn't have to question if post-incrementing was a typo, which might require going into git blame then looking up the obscure code comments of `boost::interprocess::ipcdetail::atomic_inc32` which explains it is a post-increment).

## UkoeHB | 2022-03-20T00:10:19+00:00
Not sure if it needs to be changed but... I had to look up whether post-incrementing an atomic int had any performance implications (like it does for iterators). It doesn't, but someone else might have the same question needlessly.

## UkoeHB | 2022-03-20T00:15:01+00:00
```suggestion
    const uint32_t th_local_index = m_thread_index;
    ++m_thread_index;
```

## jeffro256 | 2022-03-20T16:40:03+00:00
I thought about making the value `const` but it didn't match the rest of the code. Should I go ahead and do it anyways?

## jeffro256 | 2022-03-20T16:40:58+00:00
Good point, I'll put an inline comment

## jeffro256 | 2022-03-20T16:52:49+00:00
Does postfix for iterators take a hit mainly because it has to copy construct itself? If so, it would make sense that `std::atomic<uint32_t>` wouldn't have that issue (except for single 4 byte `mov` instruction`, which is probably optimized away anyways)

## UkoeHB | 2022-03-20T17:12:39+00:00
The main problem here is cognitive load with no functional benefit. Best to do the simplest thing possible, which is a pre-increment.

## jeffro256 | 2022-03-20T17:12:55+00:00
@UkoeHB check out the new commit and lmk what you think. Thanks!

## UkoeHB | 2022-03-20T17:14:49+00:00
If you're touching the line, might as well go all the way.

## UkoeHB | 2022-03-20T17:20:38+00:00
Yes that conveys your intent, thanks

## UkoeHB | 2022-03-20T19:23:48+00:00
I just realized this operation has to be atomic, so post-increment is correct. My bad!

## SChernykh | 2022-03-21T09:44:16+00:00
I'm sorry but
```
const uint32_t local_thr_index = m_thread_index;
++m_thread_index;
```
defeats the purpose of using atomics. Two threads can get the same index executing this code. The whole point of atomics is you do it in one operation!

## SChernykh | 2022-03-21T09:44:52+00:00
Same. It must be done in a single operation, not two lines of code!

## jeffro256 | 2022-03-22T18:17:00+00:00
Is a postfix increment harder to understand than a prefix increment?

## UkoeHB | 2022-03-22T18:23:40+00:00
Doing more things = more things to think about, that's all.

# Action History
- Created by: jeffro256 | 2022-03-19T22:27:58+00:00
- Merged at: 2022-04-06T04:20:41+00:00
