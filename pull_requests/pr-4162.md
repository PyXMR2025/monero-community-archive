---
title: ' account: keep encrypted secret spend key in memory '
source_url: https://github.com/monero-project/monero/pull/4162
author: moneromooo-monero
assignees: []
labels: []
created_at: '2018-07-21T08:23:23+00:00'
updated_at: '2018-07-26T12:22:21+00:00'
type: pull_request
status: closed
closed_at: '2018-07-26T12:22:20+00:00'
merged_at: null
---

# Original Description
The first two patches are needed as base, not merged yet

# Discussion History
## stoffu | 2018-07-23T07:13:17+00:00
Should use `key` instead of `m_cache_key `

## stoffu | 2018-07-23T07:18:27+00:00
The call to `store_keys` on L3183 should come after this line, because `store_keys` assumes that the secret keys are encrypted when it's called.
```diff
@@ -3177,16 +3177,6 @@ bool wallet2::load_keys(const std::string& keys_file_name, const epee::wipeable_
     {
       m_account.decrypt_keys(key);
     }
-    else
-    {
-      // rewrite with encrypted keys, ignore errors
-      bool saved_ret = store_keys(keys_file_name, password, m_watch_only);
-      if (!saved_ret)
-      {
-        // just moan a bit, but not fatal
-        MERROR("Error saving keys file with encrypted keys, not fatal");
-      }
-    }
   }
   const cryptonote::account_keys& keys = m_account.get_keys();
   hw::device &hwdev = m_account.get_device();
@@ -3196,7 +3186,19 @@ bool wallet2::load_keys(const std::string& keys_file_name, const epee::wipeable_
   THROW_WALLET_EXCEPTION_IF(!r, error::invalid_password);
 
   if (r)
+  {
     setup_keys(password);
+    if (!encrypted_secret_keys)
+    {
+      // rewrite with encrypted keys, ignore errors
+      bool saved_ret = store_keys(keys_file_name, password, m_watch_only);
+      if (!saved_ret)
+      {
+        // just moan a bit, but not fatal
+        MERROR("Error saving keys file with encrypted keys, not fatal");
+      }
+    }
+  }
 
   return true;
 }
```


## stoffu | 2018-07-23T07:39:38+00:00
Also I realized that `m_keys_file_locker.reset();` should be called after `store_keys` because it internally locks the keys file. Otherwise, an error message will be shown when opening a legacy wallet file (but it seems harmless):

> ERROR	net.http	src/common/util.cpp:234	Failed to lock tests/data/wallet_9svHk1.keys: Resource temporarily unavailable

## moneromooo-monero | 2018-07-23T09:27:24+00:00
You might have been looking at a previous version of the code ? store_keys is called after encrypting currently, but wasn't previously. Or is it still wrong ?

## moneromooo-monero | 2018-07-23T09:30:54+00:00
Ah nevermind, this part is from a patch that's needed, you can ignore this (the encrypt/decrypt pair), it's only been fixed in the PR this patch is for.

## moneromooo-monero | 2018-07-23T09:32:43+00:00
In fact, I think I might just bundle all the secret key related patches in a single PR, because this is getting really annoying having to cherry-pick needed patches in order for the PR to work.

## stoffu | 2018-07-23T10:06:27+00:00
> I think I might just bundle all the secret key related patches in a single PR

+1

I found the portable wallet serialization unit test failing with the current PR, and came up with the above patch to make it pass the test. Please make sure that the test passes in any case.


# Action History
- Created by: moneromooo-monero | 2018-07-21T08:23:23+00:00
- Closed at: 2018-07-26T12:22:20+00:00
