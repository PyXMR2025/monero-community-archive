---
title: IPv6 support
source_url: https://github.com/monero-project/monero/pull/4851
author: KeeJef
assignees: []
labels: []
created_at: '2018-11-15T03:11:10+00:00'
updated_at: '2019-08-14T20:22:10+00:00'
type: pull_request
status: merged
closed_at: '2019-08-14T20:22:09+00:00'
merged_at: '2019-08-14T20:22:09+00:00'
---

# Original Description
Adds IPv6 support for daemon p2p, daemon RPC, and wallet RPC.

Currently there is no option to bind different ports for
IPv4 and IPv6 for RPC, but that can be changed if necessary.

References this issue https://github.com/monero-project/monero/issues/147

# Discussion History
## sedited | 2018-11-19T14:05:57+00:00
You should not use std::string in a constexpr , constexpr only takes literal types. Travis and build bots catch this when compiling with -Winvalid-constexpr.

## sedited | 2018-11-19T14:11:55+00:00
This trips -Winvalid-constexpr. To me it looks like it's because they have different lifetimes, which is not permitted. 

## moneromooo-monero | 2018-11-27T12:47:36+00:00
All this isn't needed, it's a hack that's due to me not realizing the var naming went into the struct when I added this code. This new IPv6 code doesn't have to deal with that problem, so just a KV_SERIALIZE should do.

## moneromooo-monero | 2018-11-27T12:52:41+00:00
To avoid/mitigate the Sybil problem, this should be the other way, so it's off by default.


## moneromooo-monero | 2018-11-27T12:54:32+00:00
There's a lot of places where indentation is borked. Turns out it's because your editor is adding tabs.

## moneromooo-monero | 2018-11-27T12:55:53+00:00
This looks like it'll fail when given a non IPv4 and non IPv6 address type, since the check is removed.

## moneromooo-monero | 2018-11-27T12:58:34+00:00
Why a separate bool rather than see if any address is given ?

## moneromooo-monero | 2018-11-27T12:59:48+00:00
Stray reformat

## moneromooo-monero | 2019-04-29T07:01:23+00:00
I'm guessing the std::move being removed was a merge error.

## moneromooo-monero | 2019-04-29T07:02:39+00:00
Can you fix the reformatting please ? Makes it hard to see what changed.

## moneromooo-monero | 2019-04-29T07:16:31+00:00
Probably want to match fC etc too. boost::algorithm::iequals does it.

## moneromooo-monero | 2019-04-29T07:17:05+00:00
That's just one short form though isn't it ?

## moneromooo-monero | 2019-04-29T07:19:20+00:00
Why std::string, rather than an array of however number of bytes ? The string means it's never certain to be canonical.

## moneromooo-monero | 2019-04-29T07:25:13+00:00
Those will consider equal IPs different AFAICT, given the non canonical nature of the std::string ip field.

## moneromooo-monero | 2019-04-29T07:26:43+00:00
That seems way underspecified.

## moneromooo-monero | 2019-04-29T07:39:24+00:00
That one seems wrong.

## moneromooo-monero | 2019-04-29T07:41:13+00:00
This all seems to be gratuituous changes, no ?

## moneromooo-monero | 2019-04-29T07:50:34+00:00
That seems to remove any last char, regardless of whether ] or not.

## tewinget | 2019-04-30T01:50:49+00:00
correct, fixed.

## tewinget | 2019-04-30T01:52:33+00:00
The reformatting is because the rest of the file is spaces and this bit was tabs, or so it would seem.  It can be changed back to tabs if necessary, but it seems like all of the non-whitespace changes are highlighted properly in the diff.

## tewinget | 2019-04-30T01:52:43+00:00
addressed.

## tewinget | 2019-04-30T01:53:03+00:00
switched to using boost's address_v6::is_loopback()

## tewinget | 2019-04-30T01:53:27+00:00
changed to using boost's address_v6 under the hood

## tewinget | 2019-04-30T01:53:42+00:00
changed to using boost's address_v6 under the hood

## tewinget | 2019-04-30T01:56:14+00:00
Agreed that it seems that way, but it's just a helper function to avoid a bit of code duplication; the places it is called will handle making sure what it spits out is correct/usable.

## tewinget | 2019-04-30T01:56:22+00:00
correct, fixed.

## tewinget | 2019-04-30T01:57:42+00:00
Seems so, but was done in order to pull get_network_address_host_and_port() (see above) into its own function, as the same logic was used elsewhere in the codebase and should not be duplicated if possible.

## tewinget | 2019-04-30T02:00:01+00:00
If the address was specified with an opening square brace at the beginning and not a closing square brace at the end then it was specified incorrectly.  I reasoned that it would be incredibly tedious to look for all of the possible permutations of incorrect address specification and give hints (e.g. "missing ] character after address") as there are quite a few ways of messing that up.  In this case if the ] is missing then when the code tries to use the address it will fail and message/error accordingly.

## moneromooo-monero | 2019-04-30T10:05:25+00:00
Fair enough, I guess half the lines are changed anyway.

## moneromooo-monero | 2019-04-30T10:06:23+00:00
Looks good, any chance the m_address_str that's still left can be made canonical ? Otherwise stuff that uses the host string as a key (ie, the block list) will still see the same address as different.

## tewinget | 2019-04-30T12:20:17+00:00
It looks a bit weird, but yes (and it should be fine despite the appearance), and done.

## tewinget | 2019-04-30T12:21:46+00:00
That was my thought too, though the changes are small-ish.  Previously for this PR you had asked for less code duplication and this was the cleanest way (without significant refactoring) I could see to have that.

## moneromooo-monero | 2019-04-30T14:17:25+00:00
That all seem dangerous. Is it possible to just remove that m_address_str, and just have the ipv6 var ? host_str calls to_string. No need for this post_deserialize.

## tewinget | 2019-04-30T17:17:20+00:00
Would it be preferable to use boost's address_v6 to make the string address canonical in the case where the address is passed to the constructor and trust that if we're deserializing one it is canonical and that address_v6.to_string() does indeed return at least a consistent (if not canonical, consistent should be fine) string, or to store the address bytes?  I can use the underlying representation address_v6 uses and that should work nicely, but I'll have to add std::array to the list of stl containers supported by KV_SERIALIZE.

## moneromooo-monero | 2019-04-30T18:55:15+00:00
I'm not sure I understand that :)

- having a string seems wrong since several strings can represent the same address
- I assume the boost address_v6 is just 8 bytes and generally behaves sensibly; if not you can just make your own
- For serialization, you can save as a blob assuming the boost object is not a complex thing, which it has no reason to be, but I'm guessing here


## tewinget | 2019-04-30T19:09:53+00:00
re: strings, I agree that that's not the best way to do things, though as I said if address_v6::to_string() is consistent in how it represents an address then there shouldn't be an issue of multiple strings for the same address.

address_v6 uses a std::array of 16 bytes internally.  It does behave sensibly but again I'd need to add it to the list of containers supported by KV_SERIALIZE.  This seems like the best approach, I just thought I'd confirm as much.

The problem with serializing as a different representation (e.g. blob it up before serializing) is that on deserialize the underlying field must be populated, hence the post_serialize above.  I agree that this approach feels clumsy; at the time I hadn't realized that address_v6 exposes the raw bytes, simply misread the header.

I'll go ahead and use the std::array as the underlying representation as it seems like the cleanest approach at this point.

Edit: upon review this will require amending KV_SERIALIZE to handle std::array of size 16 exactly.  A bit awkward but I think still fine.

## moneromooo-monero | 2019-04-30T19:27:36+00:00
Can you not use KV_SERIALIZE_POD_AS_BLOB ?

## tewinget | 2019-04-30T19:30:34+00:00
Ok, nevermind, KV_SERIALIZE did not want to handle std::array and if it's not going to be that simple to tell it to it's probably not worth it.

So the issue at hand is that doing a "post-serialize" function is a bit messy.  I agree with that, but the alternative seems to be running code every time one of the access functions is used, either to check if the struct has been updated since being deserialized or to do what that updating would do every time.

That seems worse, so I'd say it's between post-serialize or trust that address_v6::to_string() will always give the same string for a given address.  It's possible I'm missing something obvious as I am rather quite tired; I'm genuinely not trying to be obtuse.  Given that serializing std::array wasn't a quick win and post-serialize is arguably ugly, I'm inclined to say trust that address_v6::to_string() is consistent.

## tewinget | 2019-04-30T19:31:27+00:00
Should have hit refresh.  Will take a look at KV_SERIALIZE_POD_AS_BLOB, not sure if that will sort it.

## moneromooo-monero | 2019-04-30T23:41:45+00:00
https://paste.debian.net/hidden/f4a22592/ are the changes I'd make. This uses only a address_v6 object. I think there might even be fewer conversions overall. I've only built it, not tried it.

## tewinget | 2019-05-02T09:45:10+00:00
Thanks for that.  There were a couple issues to touch up with that patch but no worries.  Biggest is that address_v6.from_string is a static member function.  Why the compiler allows instance.static_function() is beyond me, but finding that sure was fun.

## moneromooo-monero | 2019-05-02T11:11:35+00:00
Do you actually need a new_connection_ipv6 ? It looks like the same object can be used for both.

## moneromooo-monero | 2019-05-02T11:12:50+00:00
I'd rather this used the boost from_string + is_link_local, since it turns out those exist.

## moneromooo-monero | 2019-05-02T11:14:28+00:00
That function makes me a bit uncomfortable, but I think it's fine to leave as is. I might come back later and see if it can be removed.

## moneromooo-monero | 2019-05-02T11:15:22+00:00
That const_cast should not be needed, but actually was when I tried it. Not sure why... I might look at that later too.

## moneromooo-monero | 2019-05-02T11:15:50+00:00
Can I ask to indent this as the surrounding code ? :)

## moneromooo-monero | 2019-05-02T11:16:51+00:00
This function can also use boost from_string

## tewinget | 2019-05-03T02:13:20+00:00
If we require IPv4 and IPv6 use the same port then yes, I believe it could.  I don't think it's a good idea to have two async_accept (one for each protocol, in case they're bound differently) to be bound to the same new_connection object, but I may be mistaken.

## tewinget | 2019-05-03T02:23:07+00:00
I looked at that as well but I don't think is_link_local does exactly what we want.

https://github.com/boostorg/asio/blob/develop/include/boost/asio/ip/impl/address_v6.ipp#L168

for both is_link_local and is_site_local it's looking if the address is link-local, but I don't think that's what we care about.  I could be mistaken here, but I presume we care about unique-local-addresses in IPv6, i.e. the equivalent of 10.0.0.0/8 or 192.168/16

If link-local is indeed what we care about it can certainly be changed.

## tewinget | 2019-05-03T02:27:57+00:00
That function takes an address that may be specified as "address:port" and separates the two; address_v6.from_string and address_v4.from_string -- ok, they're deprecated, so make_address_vN() -- use inet_pton under the hood which would not play nice with the ":port" part nor the square braces around the address in the case of ipv6.

## tewinget | 2019-05-03T02:30:52+00:00
done

## moneromooo-monero | 2019-05-03T09:51:10+00:00
Looking at the docs, is_site_local is what we want here.

## moneromooo-monero | 2019-05-03T09:51:37+00:00
OK, two is fine then.

## moneromooo-monero | 2019-05-03T09:59:25+00:00
Actually, not sure. I see a claim that site-local allows talking to other machines on the LAN, which is what we want (if there is a "only"). So I don't get the difference between link and site then.

## tewinget | 2019-05-03T12:38:02+00:00
Well, so I'm not sure in practice which would matter.  Could even check for both.

I just know that the wikipedia article compared link-local in IPv6 as the equivalent of 169.254.0.0/16 in IPv4.  It also mentioned something the spec requiring every interface to have a link-local address, though I did not quite understand to what end.

I guess maybe just go with both?

## moneromooo-monero | 2019-05-03T14:36:54+00:00
I think we want site local. The use of it is to detect when a node is not a loopback node, but is still in the LAN, to default to trusted. Site local seems to encompass all the non routable IPv4 address ranges that are meant for LAN only use.

## tewinget | 2019-05-03T16:21:07+00:00
So looking into it on IANA's site, site-local is deprecated as of a 2004 RFC.

RFC 4193 states that Unique Local Unicast addresses are intended for use within a "site" and not intended to be routed on the internet.

RFC 4291 indicates that routers are not intended to forward packets from link-local addresses (fe80::/10) but I guess it could sorta still be used similarly to loopback, though that would be very strange indeed.

I shall add is_link_local to the function, but the current check for unique local unicast should remain.

(edit: changed "I shall add is_site_local" to "add is_link_local", as that is correct)

## moneromooo-monero | 2019-05-03T16:54:52+00:00
OK. Then we'll just wait for someone with an IPv6 LAN to tell us whether it works as expected or not.

# Action History
- Created by: KeeJef | 2018-11-15T03:11:10+00:00
- Merged at: 2019-08-14T20:22:09+00:00
