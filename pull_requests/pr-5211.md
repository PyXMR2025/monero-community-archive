---
title: 'device/trezor: HF10 protocol upgrade'
source_url: https://github.com/monero-project/monero/pull/5211
author: ph4r05
assignees: []
labels: []
created_at: '2019-02-28T13:57:11+00:00'
updated_at: '2019-04-15T13:16:31+00:00'
type: pull_request
status: merged
closed_at: '2019-03-21T12:44:15+00:00'
merged_at: '2019-03-21T12:44:15+00:00'
---

# Original Description
HF10 protocol upgrade for Trezor.
Related to https://github.com/trezor/trezor-core/pull/478

I am sorry for a larger PR, I thought it is easier for you to review all at once as it is easier to see dependencies and why particular choices have been made.

## HF 10
- Most of the changes relate to the deterministic output masks in HF10. Masks are generated when a destination is sent to the Trezor, this affects how BP offloading is done. 
- Transaction Inputs Commitments and outputs commitments balancing is now performed in the signing step of the protocol, thus `pseudo_out`s are updated in the sign step as Trezor adjusts masks so their sums equal. 
- We discontinued support for Borromean range signatures so I removed the related logic from the client protocol code (offloading, verification, serialization).
- `in_memory` variant of the protocol which enabled to do signature a bit faster for standard transactions was removed. Trezor and Monero client code is now cleaner, safer (fewer branches). Speedup was not significant. 
- When all users upgrade to new Monero and Trezor firmwares, support for old protocol (`client_version=0`) can be dropped.
- Key image import after the cold signing imports only key images computed by the Trezor, i.e., key images of spent inputs.
- Minor refactoring of the Trezor client code (consts), passphrase entry refactored.
- key image import only for device-computed inputs

## Get TX key
- Trezor supports `get_tx_key` for decrypting stored tx private keys.
- `wallet2::get_tx_key_device()` calls `get_tx_key` on the Trezor if applicable
- Simplewallet supports `get_tx_key` and `get_tx_proof` on hw device using the `get_tx_key` feature

## Live refresh
- Live refresh feature enables to do the `refresh` with Trezor, i.e., computing key images on the fly. More convenient and efficient for users (`hw_key_image_sync` was required before).
- The first live refresh requires user confirmation on the device.  
- A thread is watching whether live refresh is being computed. If it is not for 30 seconds, it terminates the live refresh process - switches the Trezor state (display changes).

## Wallet API
- Initial Trezor support added so Monero GUI supports Trezor. 
- Wallet API Works with Monero-GUI (https://github.com/monero-project/monero-gui/pull/2019)
- Functional tests of the Wallet::API cold signing and refresh via mocked core RPC daemon (could be also used in different tests later). Mocker RPC daemon is created with the test `cryptonote::core` and runs on a local port. Wallet::API uses RPC daemon.


# Discussion History
## moneromooo-monero | 2019-03-15T16:37:58+00:00
Is this standard HMAC ? If so, it should probably in a hmac.[hc] file so it be used nicely by other stuff in the future.

## moneromooo-monero | 2019-03-15T16:40:25+00:00
This allows keys with size < 32. That loop looks like a two byte key of 0x42 0x00 would behave the same as a one byte key of 0x42. Is that intended ?

## moneromooo-monero | 2019-03-15T16:41:58+00:00
If this is intended to clear memory, please use memwipe instead.

## moneromooo-monero | 2019-03-15T16:43:23+00:00
keccak_update takes a size_t. On 32 bit machines, you might plausibly manage an overflow to 0. Best to use size_t here too then I guess. Even tough you'd have to try to hash 4 GB, so unlikely.

## moneromooo-monero | 2019-03-15T16:44:13+00:00
memwipe

## moneromooo-monero | 2019-03-15T16:45:37+00:00
I'd go for size_t here too.

## ph4r05 | 2019-03-15T18:22:36+00:00
Correct, it is standard HMAC RFC 2104. I've ported it from `src/crypto/blake256.c`checking also against the specification.

I will move it to another file and add comments it is RFC 2104 HMAC.

## ph4r05 | 2019-03-15T18:28:42+00:00
I think this is according to the RFC 2104.

```
B = hash function block length
ipad = the byte 0x36 repeated B times
opad = the byte 0x5C repeated B times.
HMAC(K, text) = H(K XOR opad, H(K XOR ipad, text))
```

Also according to the section 3:
"The key for HMAC can be of any length (keys longer than B bytes are first hashed using H). 
 However, less than L (size of hash output) bytes is strongly discouraged as it would decrease the security strength of the function. "

## moneromooo-monero | 2019-03-16T15:02:06+00:00
Some of those things look like secrets ? If so they should be crypto::secret_key or wipeable_string, which auto wipe on dtor.

## moneromooo-monero | 2019-03-16T15:03:32+00:00
While they're the same size, key derivations would be better stored in a crypto::key_derivation, or it's doing to be massively confusing. Might be better to have two functions in this case (maybe calling the same internal code, I've not seen it yet).

## moneromooo-monero | 2019-03-16T15:11:01+00:00
That seems racy. There is no lock in release, which calls base::release, which calls disconnect, which deletes m_transport and sets it to nullptr.

## moneromooo-monero | 2019-03-16T15:13:29+00:00
Should you not call with 0 at function start ?

## moneromooo-monero | 2019-03-16T15:14:02+00:00
sizeof(buff) ? Guards against later changes :)

## moneromooo-monero | 2019-03-16T15:14:26+00:00
Same

## moneromooo-monero | 2019-03-16T15:16:59+00:00
You probably want to set this before calling live_refresh_start, or you might time out early if unlucky.

## moneromooo-monero | 2019-03-16T15:18:16+00:00
That's probably racy too, though I did not look at this one much :)

## moneromooo-monero | 2019-03-16T15:21:17+00:00
Is signed_tx.key_images guaranteed to have enough size at that place ?

## moneromooo-monero | 2019-03-16T15:26:05+00:00
could be const

## ph4r05 | 2019-03-16T15:34:02+00:00
These are just salts and ciphertexts, they are assumed public in this attacker model so it is maybe ok to be left as strings.

## ph4r05 | 2019-03-16T15:45:31+00:00
Yes, it is set before for-loop
```cpp
signed_tx.key_images.resize(unsigned_tx.transfers.second.size());
```

## ph4r05 | 2019-03-16T16:33:37+00:00
True. I've removed this part and kept just tx_key computation as I noticed the intended use-case for derivations would require a bit change (tx_proof) which is not yet implemented in Trezor.

## ph4r05 | 2019-03-16T16:54:16+00:00
Good catch, I've moved the release after thread join, so it waits for completion.

## ph4r05 | 2019-03-16T17:03:28+00:00
I've set it after the `live_refresh_start` because it sets `m_live_refresh_in_progress` which enables the thread auto-shutdown. The `live_refresh_start` is user-confirmed action so the idea was to set the `m_last_live_refresh_time` after this.

## moneromooo-monero | 2019-03-16T17:06:12+00:00
inactivity (sorry :P)

## ph4r05 | 2019-03-16T17:08:50+00:00
It could be OK, the `live_refresh_finish` has inside `AUTO_LOCK_CMD()`. If there is a race between shutdown thread one locks and finishes.

## moneromooo-monero | 2019-03-16T17:09:08+00:00
Nevermind, I confused m_live_refresh_running and m_live_refresh_in_progress.

## moneromooo-monero | 2019-03-16T17:11:32+00:00
That looks like it's copied from src/common. Can't you use it from there ?

## moneromooo-monero | 2019-03-16T17:12:40+00:00
I suppose it could throw if it does not fit.

## moneromooo-monero | 2019-03-16T17:16:06+00:00
The plaintext will presumably not include the TAG_SIZE bytes ? Or maybe the assert above is technically wrong ?

## moneromooo-monero | 2019-03-16T17:19:15+00:00
Looks like hash is used as a secret key, so you may want to memwipe it.

## moneromooo-monero | 2019-03-16T17:24:40+00:00
plaintext should also be memwiped

## moneromooo-monero | 2019-03-16T17:25:13+00:00
That's going to be super confusing given the chacha iv size is 8 in chacha.h...

## moneromooo-monero | 2019-03-16T17:26:14+00:00
Can this be const ?

## ph4r05 | 2019-03-16T17:47:11+00:00
I've just fixed that 😄 

## ph4r05 | 2019-03-16T17:48:10+00:00
I had to modify it a bit as the string can contain null-byte and this case was not covered by the src/common. I modified it a bit here so it creates a string with the length provided by the json lib

## moneromooo-monero | 2019-03-16T18:48:36+00:00
Does this need to be one in the caller ?

## moneromooo-monero | 2019-03-16T18:49:34+00:00
progresst ?

## moneromooo-monero | 2019-03-16T18:53:44+00:00
There is a get_pruned_tx function in that file which should do the above.

## moneromooo-monero | 2019-03-16T18:57:41+00:00
That seems wrong. Those are supposed index into m_transfers. Are you sure you don't mean to use "= idx" on these two ?

## moneromooo-monero | 2019-03-16T18:58:29+00:00
Is this offset coming from a trusted source (ie, the code) or the device ? It the latter, this should check for overflow.

## ph4r05 | 2019-03-16T19:19:51+00:00
I wanted to discuss this. How to do it correctly. 
`get_tx_key` is const so I cannot just fallback to device in the `wallet::get_tx_key` if it needs to be loaded from the device.

## ph4r05 | 2019-03-16T19:35:43+00:00
I will add a comment it is defined in RFC 7539. It defines 96-bit nonce/IV
Its closed in hw::trezor::crypto::chacha namespace so hopefully shielded enough to avoid confusion with chacha.h

## ph4r05 | 2019-03-16T20:12:18+00:00
Hmm I am not sure how I could use it. I need `transaction_prefix_hash`, the `get_pruned_tx` is maybe a bit more complicated?

## ph4r05 | 2019-03-16T20:14:43+00:00
The offset is from the wallet. It is like the offset that is generated by `wallet2::export_outputs`

## ph4r05 | 2019-03-16T20:18:20+00:00
The idea is that `key_images` can be shorter than `m_transfers`, specified by the offset, like with `export_outputs`.

```cpp
const size_t idx = i + offset;
transfer_details &td = m_transfers[idx];
```

## ph4r05 | 2019-03-16T20:24:37+00:00
Good catch!

## ph4r05 | 2019-03-16T20:24:54+00:00
done

## ph4r05 | 2019-03-16T20:25:09+00:00
done

## ph4r05 | 2019-03-16T20:25:25+00:00
fixed

## ph4r05 | 2019-03-16T20:25:38+00:00
done

## moneromooo-monero | 2019-03-16T21:14:39+00:00
Then there was another instance of this I did not point out since I was not sure I was not missing something since the same thing was done twice.

## moneromooo-monero | 2019-03-16T21:15:55+00:00
Sounds like the device fields should be mutable then. Good old C++ bodge ? :)

## moneromooo-monero | 2019-03-16T21:16:11+00:00
OK, no problem.

## moneromooo-monero | 2019-03-16T21:17:18+00:00
Yes, but here:
m_pub_keys[td.get_public_key()] = i;

That i is going to offset m_transfers, it's supposed to point back to the transfer_details structure. AFAICT, it's going to point to the wrong one here.

## ph4r05 | 2019-03-16T21:20:31+00:00
oh, I missed that. You are right! Thanks!

## ph4r05 | 2019-03-16T21:23:30+00:00
Correct, I've fixed that one also

## ph4r05 | 2019-03-16T21:42:12+00:00
I've changed var names to ki_idx and transfer_idx so it is clear now

## ph4r05 | 2019-03-16T21:45:09+00:00
Soo do you prefer `wallet2::get_tx_key` is not `const` anymore and does the device call if needed? I would like it more. 

Or I can rename `get_tx_key` to `get_tx_key_stored` which would be const, the `get_tx_key`  would do `get_tx_key_stored` first and then query the device. Does that sound OK?

## moneromooo-monero | 2019-03-17T11:30:13+00:00
It does sound OK, thanks. Though "_stored" is not very enlightening as to what it does, but it'll do.

## ph4r05 | 2019-03-17T11:32:10+00:00
Yeah, I sometimes struggle with names, maybe cached? :)

## moneromooo-monero | 2019-03-17T11:33:09+00:00
You can drop that in this file assuming you did not copy any code from them.

## moneromooo-monero | 2019-03-17T11:38:03+00:00
Out of curiosity... why ?

## moneromooo-monero | 2019-03-17T11:47:52+00:00
That is a bit confusing.

## moneromooo-monero | 2019-03-17T11:51:44+00:00
FWIW there are public accessors for these since simplewallet uses them.

## moneromooo-monero | 2019-03-17T12:05:39+00:00
Can't those all be const in the first place ?

## moneromooo-monero | 2019-03-17T12:05:48+00:00
This can be removed

## ph4r05 | 2019-03-17T12:40:39+00:00
Ah thats true, I dont need this particular accessor here. 
`wallet_accessor_test` returns non-const, I need it somewhere else for modifications (tests), but here the const is OK. I will change it 

## ph4r05 | 2019-03-17T12:42:45+00:00
The notify? No particular reason here, I've just noticed the vm change code usually calls the notification. 

## ph4r05 | 2019-03-17T12:43:32+00:00
I mean, the desired functionality is enabled zmq => enabled p2p. Or would it be better to leave this as ordinary setters and check for consistency on start?

## moneromooo-monero | 2019-03-17T14:18:57+00:00
I mean, why random ports ?

## moneromooo-monero | 2019-03-17T14:19:38+00:00
I think it'd certainly be less confusing, but if you prefer if that way I'm ok with it too.

## ph4r05 | 2019-03-17T14:28:05+00:00
I’ve experimented with multiple running daemons but it is probably safer to let user specify the number to avoid possible random clashes

## ph4r05 | 2019-03-18T11:10:20+00:00
done

# Action History
- Created by: ph4r05 | 2019-02-28T13:57:11+00:00
- Merged at: 2019-03-21T12:44:15+00:00
