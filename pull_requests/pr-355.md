---
title: ' cuprated: RPC handlers'
source_url: https://github.com/Cuprate/cuprate/pull/355
author: hinto-janai
assignees: []
labels:
- A-p2p
- A-dependency
- A-workspace
- A-consensus
- A-docs
- A-storage
- A-helper
- A-net
- A-types
- A-rpc
- A-zmq
- A-books
- A-binaries
- A-book-architecture
created_at: '2024-12-05T01:45:06+00:00'
updated_at: '2025-04-08T20:33:13+00:00'
type: pull_request
status: merged
closed_at: '2025-04-08T16:09:43+00:00'
merged_at: '2025-04-08T16:09:43+00:00'
---

# Original Description
### What
Implements the `{json-rpc, binary, json}` handlers in `cuprated`; adds various types and changes some misc things as needed.

### How
See below review.

# Discussion History
## hinto-janai | 2024-12-21T01:09:59+00:00
`cuprate_types::hex::Hex` was moved into a separate crate `cuprate_hex` such that the types could implement epee without running into cyclic dependency or orphan impl problems.

I moved `fixed-bytes` as well since that is used in multiple places outside of networking. 

## hinto-janai | 2024-12-25T19:40:04+00:00
For the time being, I'm making response types in `cuprate-rpc-types` have `serde(default)` since:
1. `monerod` will not serialize fields with empty containers
2. `monerod`'s response types sometimes change depending on branches

`serde(default)` is a bit of a blunt tool to use but I think it works for now. I will think about this more when integrating RPC into `cuprated` and with more tests.

## hinto-janai | 2024-12-25T19:42:50+00:00
### 1/2
This is a new module, `cuprate_types::rpc`. It contains types that are exposed by RPC but are also used internally.

The difference between `cuprate_rpc_types` and `cuprate_types::rpc` is that the latter has more canonical types where it makes sense, e.g. `[u8; 32]` instead of `String`.

## hinto-janai | 2024-12-25T19:44:49+00:00
### 2/2
Since `cuprate_types::rpc` needs to map to `cuprate_rpc_types` eventually, instead of doing it adhoc-ly in places, this file defines a bunch of

```rust
impl From<cuprate_types::rpc::*> for cuprate_rpc_types::*
```
plus some other types from other crates.

## hinto-janai | 2024-12-25T19:52:34+00:00
I think doing [tests like this](https://github.com/Cuprate/cuprate/pull/232#discussion_r1692190753) was a bad tradeoff, it's a bit too noisy and IDE stuff stops working, so a large majority of the changes to `rpc/types/src/{json,bin,other}.rs` are moving the macro tests into normal `#[test] fn()` tests at the bottom of the file.

## hinto-janai | 2024-12-25T19:54:16+00:00
`cuprate-rpc-types` now uses `Hex` and `HexVec` over `String` where it can.

## hinto-janai | 2024-12-25T19:55:54+00:00
All RPC calls involving the address book service are hardcoded to `ClearNet` for now. Can be (easily?) changed later.

## hinto-janai | 2024-12-25T21:27:13+00:00
Lots of fields in RPC are `String` where it is a hexadecimal encoded u128 with a lowercase prefix, e.g. `0x0`

You can use `format!("{var:#x}")` for this although it's a bit obscure so I made this trait so you can do `var.hex_prefix()`. Could remove this if wanted.

## hinto-janai | 2024-12-25T21:37:53+00:00
This function now returns the `txid` for each output due to https://www.getmonero.org/resources/developer-guides/daemon-rpc.html#get_outs.

This is more expensive, the `txid` is not required by default as well. Should this be something like `txid: Option<[u8; 32]>` instead?

## hinto-janai | 2024-12-25T21:43:27+00:00
This was changed to `Vec` due to https://www.getmonero.org/resources/developer-guides/daemon-rpc.html#is_key_image_spent behavior.

```bash
curl http://127.0.0.1:18081/is_key_image_spent -d '{"key_images":["8d1bd8181bf7d857bdb281e0153d84cd55a3fcaa57c3e570f4a49f935850b5e3","8d1bd8181bf7d857bdb281e0153d84cd55a3fcaa57c3e570f4a49f935850b5e3"]}' -H 'Content-Type: application/json'
```
```json
{
  "credits": 0,
  "spent_status": [1,1],
  "status": "OK",
  "top_hash": "",
  "untrusted": false
}
```
If `cuprated` used `HashSet` then it would respond with something like `"spent_status": [1]`. Small detail but I think it could matter with wallets due to indexing code depending on this.

## Boog900 | 2025-01-06T18:36:36+00:00
this seems to only be doing part of the request - finding the split point, we also need to give the next hashes in the chain back. This can be all done with  `BlockchainReadRequest::NextChainEntry`

## Boog900 | 2025-01-06T18:43:42+00:00
>     // FIXME: is there a cheaper way to get this?

yes - get the cumulative difficulty of this block take away the cumulative difficulty of the block before it will give you the difficulty of this block.  Cumulative difficulty should be in the extended header. Also this current method is incorrect as this request takes in a timestamp not height.

Separate but related: I think we should create a specific DB request for as much data as we can as we currently need to call the service multiple times for this request.

This can all be a TODO, no need to do in this PR. 

## Boog900 | 2025-01-06T19:10:51+00:00
I recommend monero-address: https://github.com/serai-dex/serai/tree/develop/networks/monero/wallet/address

## Boog900 | 2025-01-08T02:37:56+00:00
As a TODO, this check should be done in the DB to prevent needing to do multiple DB requests. 




## Boog900 | 2025-01-08T02:54:04+00:00
```suggestion
    let block_weight_limit = usize_to_u64(c.effective_median_weight * 2);
    let block_weight_median = usize_to_u64(c.effective_median_weight);
```

that matches `monerod` behavior but technically it should be this: 

```suggestion
    let block_weight_limit = usize_to_u64(c.median_weight_for_block_reward * 2);
    let block_weight_median = usize_to_u64(c.median_weight_for_block_reward);
```

Both will be the same for the current HF although past HFs these values are different. Should we copy `monerod`'s bad behavior here?

## Boog900 | 2025-01-11T02:09:31+00:00
I don't really like linking directly to monero-core for each function, I guess it helps review so it doesn't have to be removed although eventually these will get out of date

## Boog900 | 2025-01-11T02:14:51+00:00
IMO this function does not make it more clear.

```suggestion
        // <https://github.com/monero-project/monero/blob/cc73fe71162d564ffda8e549b79a350bca53c454/src/rpc/core_rpc_server.cpp#L131-L134>
        let database_size = database_size.div_ceil(5 * 1024 * 1024 * 1024);
```

## Boog900 | 2025-01-11T02:17:52+00:00
```suggestion
        let ip = Ipv4Addr::from(peer.ip.to_le_bytes());
```

## Boog900 | 2025-01-11T02:22:32+00:00
This is the Rx seed hash, it'll have to be a todo, I'll add it to the context in a future PR.

## Boog900 | 2025-01-11T02:25:22+00:00
Same as the other comment, this is actually the correct value but if we are trying to match Monero, the value should be `effective_median_weight`.

## Boog900 | 2025-01-11T02:29:42+00:00
this should spawn on a rayon thread, using `rayon_spawn_async`

## Boog900 | 2025-01-11T02:41:02+00:00
This seems to be returning a list with double the length of the key images? I don't think this is correct. We should have a list the exact same length right

## Boog900 | 2025-01-14T01:43:48+00:00
this should return `true` even if the output is locked but the lock time has passed right?

## Boog900 | 2025-01-14T01:49:10+00:00
(there are lots of places where we could improve performance, could just be left as a wider todo)

## Boog900 | 2025-01-14T01:55:03+00:00
I kinda want to say this should be a separate request, I can imagine it being more efficient to check if any one KI is spent rather than always needing to check every KIs spent status in the blockchain.

## Boog900 | 2025-01-14T01:56:04+00:00
As I said in the other issue I am ok with this for now, I don't  like the idea of this being permanent though 

## hinto-janai | 2025-01-17T00:25:16+00:00
Copy for now, submit patch to `monerod` eventually, then update `cuprated`?

## hinto-janai | 2025-01-17T00:34:14+00:00
I think they are slightly more beneficial than not even when out-of-date. No strong feelings though, I can remove them in another PR if wanted.

## hinto-janai | 2025-01-17T19:47:27+00:00
1/3

Restored old `KeyImagesSpent` request, made new `KeyImagesSpentVec` version, also for txpool.

Request type.

## hinto-janai | 2025-01-17T19:47:42+00:00
2/3

Response type.

## hinto-janai | 2025-01-17T19:48:48+00:00
3/3

DB operation, same as before just added `_vec`.

## hinto-janai | 2025-01-17T19:51:59+00:00
1/2

Fixed timelock check, although now it is `for` due to `async`.

TODO: optimized async iterators?

## hinto-janai | 2025-01-17T19:52:17+00:00
2/2

Timelock check function.

Maybe TODO: should this be a `BlockchainReadRequest`? I feel like this could be used elsewhere.

## hinto-janai | 2025-01-17T21:10:20+00:00
https://github.com/Cuprate/cuprate/pull/355#discussion_r1920670367

## hinto-janai | 2025-01-17T21:10:30+00:00
https://github.com/Cuprate/cuprate/pull/355#discussion_r1920674454

## Boog900 | 2025-01-22T17:15:44+00:00
Outputs unlock time is not just done by looking at the timestamp/block height, it has specific rules, here is the function in Cuprate:

https://github.com/Cuprate/cuprate/blob/9842535e8654f83699373fb29c357dc8badc83ce/consensus/rules/src/transactions.rs#L220-L223

You will need to get the blockchian context for the time for unlock & chain height:

https://github.com/Cuprate/cuprate/blob/9842535e8654f83699373fb29c357dc8badc83ce/consensus/context/src/lib.rs#L138-L141

That only has to be done once, you could reuse the context for each output 

## hinto-janai | 2025-01-29T21:14:52+00:00
@Boog900 ready for review again, although WASM CI for `cuprate-rpc-types` still needs fixing.

## Boog900 | 2025-03-08T19:12:15+00:00
putting the comment here for a lack of a better place, but because the `request` type uses `Bytes` internally it is good to drop it as soon as you can.

What this means is we should extract all the data needed out of the request before doing any async/blocking operations   

## Boog900 | 2025-03-08T19:28:23+00:00
this seems to be incorrect, `monerod` seems to be getting the next hashes in the chain, using the incoming hashes as a representation of the clients current chain, and giving the blocks for those future hashes instead: https://github.com/monero-project/monero/blob/cc73fe71162d564ffda8e549b79a350bca53c454/src/rpc/core_rpc_server.cpp#L733

## Boog900 | 2025-03-08T20:00:55+00:00
@hinto-janai can you add a link to this comment in the code please.

## Boog900 | 2025-03-08T20:08:03+00:00
I would rather this just be a normal subtraction/checked with unwrap. Saturating is confusing IMO

## Boog900 | 2025-03-08T20:20:30+00:00
I don't think this needs to be `saturating_sub` here

## Boog900 | 2025-03-08T20:50:56+00:00
```suggestion
    let (range, expected_len) = {
        let start = u64_to_usize(request.start_height);
        let end = u64_to_usize(request.end_height);
        (start..=end, end - start  + 1)
    };
```

## Boog900 | 2025-03-08T21:08:20+00:00
This IMO is not a great way to do rate limiting, it should be handled with a Semaphore or even better just rate restrict the whole RPC interface. When we have performance characteristics we can look at rate limiting further.

## Boog900 | 2025-03-08T21:37:09+00:00
is this a TODO?

## Boog900 | 2025-03-08T22:11:12+00:00
I am pretty sure this isn't correct the `get(tx_idx)` should use th local index of the tx in the block, right? this currently used the global index.

```suggestion
        let height = u32_to_usize(rct_output.height);
        let tx_idx = u64_to_usize(rct_output.tx_idx);
        
        let miner_tx_id = table_block_infos.get(&height)?.mining_tx_index;
        
        if miner_tx_id == tx_idx {
            let tx_blob = table_tx_blobs.get(&miner_tx_id)?;
            Transaction::read(&mut tx_blob.0.as_slice())?.hash()    
        } else {
            table_block_txs_hashes.get(&height)?[tx_idx - miner_tx_id  - 1)]
        }
```

## Boog900 | 2025-03-08T22:12:18+00:00
Yes I think it should be, this is called ot get outputs when syncing so hashing miner txs/extra lookups is just wasted work.

## Boog900 | 2025-03-08T22:23:01+00:00
I think the line numbers above this are incorrect: https://github.com/monero-project/monero/blob/8d6aff95908e029d6b131638fbbf845e8cff04fc/src/rpc/core_rpc_server_commands_defs.h#L631-L683

## hinto-janai | 2025-03-26T23:41:23+00:00
Agreed, I should have noted that this is temporary. I don't think this endpoint is high priority, is a `FIXME` for later ok?

## hinto-janai | 2025-03-26T23:48:54+00:00
No, this is a `cuprate_types::json` type, it only has `epee` impl due to:

https://github.com/Cuprate/cuprate/blob/3c86c5ed760ccb63d6f3f8b1823d0427c1dbd886/rpc/types/README.md?plain=1#L83-L88

AFAICT only the JSON representation for `Transaction` is used in the (daemon) RPC API. 

## hinto-janai | 2025-03-26T23:53:01+00:00
Added some docs on this.

## SyntheticBird45 | 2025-03-27T00:01:43+00:00
>  it should be handled with a Semaphore or even better just rate restrict the whole RPC interface.

I would ideally prefer this to be a `Layer` as I would like `tower-governor` to be implemented at some point. So the whole RPC interface is better.

## hinto-janai | 2025-03-27T01:09:22+00:00
The issue for `wasm32-unknown-unknown` builds was:
- `cuprate_rpc_types` depends on `cuprate_types` with `json` feature
- `json` feature requires `cuprate_helper::cast`

To fix this, I am making `cuprate_helper::cast` work on 32-bit targets, yet panic when the cast is lossy. I don't think this is the correct long-term solution but it does work for now.

## Boog900 | 2025-04-08T16:02:46+00:00
> is a FIXME for later ok

yes

# Action History
- Created by: hinto-janai | 2024-12-05T01:45:06+00:00
- Merged at: 2025-04-08T16:09:43+00:00
