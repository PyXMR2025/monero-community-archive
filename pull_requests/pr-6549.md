---
title: '[master] MMS: New ''config_checksum'' subcommand'
source_url: https://github.com/monero-project/monero/pull/6549
author: rbrunner7
assignees: []
labels: []
created_at: '2020-05-17T12:11:40+00:00'
updated_at: '2020-06-08T19:12:33+00:00'
type: pull_request
status: merged
closed_at: '2020-06-08T19:12:32+00:00'
merged_at: '2020-06-08T19:12:32+00:00'
---

# Original Description
No description

# Discussion History
## moneromooo-monero | 2020-05-17T12:54:03+00:00
That's trivially collidable. Just use keccak/cn_fast_hash.

## moneromooo-monero | 2020-05-17T12:55:38+00:00
OK, that kinda explains the addition above. I assume you can use a canonical order by sorting on an arbitrary field, no ?

## moneromooo-monero | 2020-05-17T12:56:54+00:00
That will give different hashes on big/little endian. You can use SWAP32LE/SWAP64LE first, then hash the result of it.

## moneromooo-monero | 2020-05-17T12:57:48+00:00
I think you ever only pass cn_fast_hash to add_hash, so my comment above should not apply I think. Not 100% sure.

## moneromooo-monero | 2020-05-17T13:00:59+00:00
That will probably mangle UTF-8 with more than one byte. You want to perform the range checks on the code points, not the individual bytes. See the log sanitization code in my last patch (and you can use tools::utf8canonical to parse it for you).

## rbrunner7 | 2020-05-17T16:14:02+00:00
Done.

## rbrunner7 | 2020-05-17T16:15:58+00:00
Done. I have a very small residual problem left that cutting the string at max length can cut through an UTF-8 sequence and make the whole string invalid that way, but that problem can be ignored for now.

## rbrunner7 | 2020-05-17T16:18:04+00:00
Yes, that's the idea, only summing hashes. Your "sort first, then hash" alternative idea is good and would work, but I think it's safe enough for the purpose as it is now.

## moneromooo-monero | 2020-05-17T17:01:15+00:00
It means ordering is not "hashed in". Not sure if that can be a problem or not. If not, then I think it's good.

## rbrunner7 | 2020-05-17T17:03:39+00:00
Ah, I see. No, the ordering is not among the things that must get enforced / checked for "same". Everybody in fact will have a different order because the signer "me" is fixed to 0 and of course different from wallet to wallet.

## vtnerd | 2020-05-18T08:12:03+00:00
Return by-value if this is always going to assign anyway. Its not re-using the `std::string` memory ever.

## vtnerd | 2020-05-18T08:12:21+00:00
This can be declared `static`.

## vtnerd | 2020-05-18T08:13:07+00:00
All of these become defacto `std::move`s when return-by-value is used.

## vtnerd | 2020-05-18T08:19:32+00:00
if `utf8canonical` took a `boost::string_ref`, then this would reduce the number of copies from 2 to 1. The return type would have to be an explicit template argument though:

```c++
try
{
  const auto filter = [] ... ;
  return tools::utf8canonical<std::string>({text.data(), std::min(text.length(), max_length)}, filter);
}
catch (const std::exception& e)
{
  return "(Illegal UTF-8 string)";
}
```
The code for utf8canonical doesn't really need to change, only the first parameter type. An overload for `sensitive_string` would forward to the main implementation.

## vtnerd | 2020-05-18T19:37:17+00:00
This statement isn't clear (at least to me). Does this mean a n-of-3 setup could be hijacked by one of the parties to not include all 3 pubkeys? Based on how multi-sig crypto works in Monero, this would be my guess.

## rbrunner7 | 2020-05-19T04:56:07+00:00
I am not sure what you mean with "returning by value". Would that be only a modification of those two lines somehow, or also involve the call to `utf8canonical` on the next line?

## vtnerd | 2020-05-19T16:02:32+00:00
Return the `std::string` instead of an out parameter (`sd::string&`). There's no benefit to the current implementation, the memory for `sanitized_text` is never being re-used. The `substr` call is replacing the internal buffer and so is the call to `utf8canonical`.

 All of the calls to the function become "natural" assignments - move assignments or construct-in-place operations (return-value-optimization). Currently a number of calls to this function copy the string 3 times (`substr`, `utf8canonical`, and a copy assignment from temp `std::string`). The `max_length` keeps the copying to a minimum, at least.

## rbrunner7 | 2020-05-24T06:28:02+00:00
Right, the statement alone isn't clear. I hope I will get around to amend the MMS manual with some explanations, but in any case only after the release is broadly available: It's probably not a good idea to reveal how you can cheat before that.

## rbrunner7 | 2020-05-24T06:29:29+00:00
Outside the scope of **this** PR, as the code for `utf8canonical` is only included here for technical reasons.

## rbrunner7 | 2020-05-24T08:28:39+00:00
Done.

## rbrunner7 | 2020-05-24T08:28:55+00:00
Done.

# Action History
- Created by: rbrunner7 | 2020-05-17T12:11:40+00:00
- Merged at: 2020-06-08T19:12:32+00:00
