---
title: 'cuprated: internal signatures required for RPC pt. 2'
source_url: https://github.com/Cuprate/cuprate/pull/320
author: hinto-janai
assignees: []
labels:
- A-p2p
- A-dependency
- A-workspace
- A-consensus
- A-storage
- A-helper
- A-types
- A-rpc
- A-binaries
created_at: '2024-10-18T00:39:30+00:00'
updated_at: '2024-11-01T23:59:22+00:00'
type: pull_request
status: merged
closed_at: '2024-11-01T22:25:55+00:00'
merged_at: '2024-11-01T22:25:55+00:00'
---

# Original Description
### What
Adds more `{function, request, response}` signatures needed by `cuprated`'s RPC system to the `{blockchain, txpool, p2p, blockchain_context, rpc}` APIs.

Continuation of https://github.com/Cuprate/cuprate/pull/297.

This _should_ be all that's needed for JSON-RPC requests, but it may not cover `{binary, other}`.

# Discussion History
## hinto-janai | 2024-10-18T00:41:15+00:00
### Unrelated
Spacing fix.

## hinto-janai | 2024-10-18T00:43:03+00:00
### Unrelated
Lots of the diffs that can be ignored are:
- changing `pub(super)` -> `pub(crate)`
- using `anyhow!()`
- adding more / editing helper functions in `cuprated::rpc::requests`

## hinto-janai | 2024-10-18T00:45:29+00:00
### Unrelated
This file was moved to `p2p/p2p-core/src/types.rs` since there's more types unrelated to bans.

## hinto-janai | 2024-10-18T00:47:40+00:00
### Unrelated
Lots of the diffs are changing the `cuprate_rpc_types::base` constructor from `const fn ok() -> Self` -> `const OK: Self`.

Not really important, I just thought these make more sense as a `const`.

There's also a forgotten field added to `GetFeeEstimateRequest`.

## hinto-janai | 2024-10-18T00:49:35+00:00
### Signatures (blockchain manager)

## hinto-janai | 2024-10-18T00:50:32+00:00
### Signatures (address book)

## hinto-janai | 2024-10-18T00:51:12+00:00
### Signatures (txpool)

## hinto-janai | 2024-10-18T00:51:32+00:00
### Signatures (blockchain)
The `AltChains` response is _slightly_ different from the already existing `AltBlocksInChain`, it also doesn't require you specify a `Chain`, it just returns all alt chains.

Can/should we collapse these into 1 request/response? 

## hinto-janai | 2024-10-18T00:52:04+00:00
### Types (address book / p2p)

## hinto-janai | 2024-10-18T00:54:56+00:00
### Types (RPC)
This is in the `ConnectionInfo` struct produced from the `get_connections` call.

## hinto-janai | 2024-10-18T00:56:40+00:00
### Types (txpool)
This is only _slightly_ different from the previously added `TxEntry`; the `id` field exists instead of `time_in_pool`.

This is used in the `get_miner_data` RPC call.

We could also collapse these into a single struct containing both fields? Slightly heavier but more generic.

## hinto-janai | 2024-10-18T00:58:03+00:00
### Types (everything else)
Used in the `add_aux_pow` RPC call.

## hinto-janai | 2024-10-18T01:02:17+00:00
### TODO
Is it worth re-implementing and supporting these highly specific one-off `monerod` string formats? There's more than this one too.

## hinto-janai | 2024-10-18T01:07:56+00:00
### TODO
I tried replacing `tower::BoxError` with `anyhow::Error`, it's pretty deeply connected within `cuprate_consensus` but I think it works, the only problem was that one of the functions used (I forget which, either a `tower` or `tokio` one) for starting a service (I think) depends directly on the `tower::BoxError` type.

For now this uses `.map_err(|e| anyhow!(e))` which means we're boxing the error twice.

## hinto-janai | 2024-10-18T01:12:35+00:00
### TODO
- For `connection_id`, our currently used `u64` should be fine in place of `monerod`'s `boost::uuids::uuid`, right? The RPC output I think just needs a hex version of the ID.
- What can `{ip,port}` be replaced by `std::net::SocketAddr`? What about i2p/tor?
- What type is `{peer_id,state}`?

Context:
- https://github.com/monero-project/monero/blob/893916ad091a92e765ce3241b94e706ad012b62a/src/cryptonote_protocol/cryptonote_protocol_defs.h#L47
- https://www.getmonero.org/resources/developer-guides/daemon-rpc.html#get_connections

## Boog900 | 2024-10-22T16:38:24+00:00
This can be calculated on the fly in the RPC request handler - it probably doesn't need to go to another service.

## Boog900 | 2024-10-22T16:39:08+00:00
This could also be calculated on the fly in the RPC request handler.

## Boog900 | 2024-10-22T16:41:41+00:00
Probably not - these are mostly for humans IMO.

Probably a good space to add some Cuprate Easter-eggs @SyntheticBird45  

## Boog900 | 2024-10-22T16:48:43+00:00
Actually it will be more efficient to send it to the blockchain cache if we already have the RX VM.

So this should be moved to the blockchain cache service.

## Boog900 | 2024-10-22T17:01:07+00:00
These should go to the block downloader/ syncer - sadly there is no way to interact with them yet. For now just make this request a TODO or add them to the blockchain manager.

## Boog900 | 2024-10-22T17:58:06+00:00
Is this type intentionally duplicated?

## SyntheticBird45 | 2024-10-22T19:47:27+00:00
@hinto-janai `boost::uuids::uuid` is 128 bit not 64 bit.
https://www.boost.org/doc/libs/1_86_0/libs/uuid/doc/html/uuid.html

## SyntheticBird45 | 2024-10-22T19:50:54+00:00
@Boog900 I agree let's do put a surprise there

## Boog900 | 2024-10-22T20:21:52+00:00
> For connection_id, our currently used u64 should be fine in place of monerod's boost::uuids::uuid, right? The RPC output I think just needs a hex version of the ID.

We just the peers address to identify the, we don't have a connection ID. If the address is unknown like an incoming Tor connection we use a u128.

> What can {ip,port} be replaced by std::net::SocketAddr? What about i2p/tor?

Yeah you should be able to pull them from the `std::net::SocketAddr`, if that is what you are asking. I don't know how monerod handles this for Tor.

> What type is {peer_id,state}?

peer_id is a u64 and state is a monerod specific string


## hinto-janai | 2024-10-24T00:04:34+00:00
Yes, one for `cuprate-p2p` to use and one for `cuprate-rpc-types` to use. The RPC version is the same but it has `serde/epee`. I also added another similar enum for [`state`](https://github.com/Cuprate/cuprate/pull/320#discussion_r1811335020) (`ConnectionState`).

I moved them to `cuprate-types` so they can impl `serde/epee`, although, this does mean `cuprate-p2p-core` now pulls in `cuprate-types`.

## hinto-janai | 2024-10-24T00:10:19+00:00
Will be used for the `overview` string in https://github.com/Cuprate/cuprate/pull/320#discussion_r1811063772 and any other string fields we don't support.

## hinto-janai | 2024-10-24T00:11:41+00:00
Should be correct to box this? Use-case is `calc_pow`.

## hinto-janai | 2024-10-24T00:12:08+00:00
`cuprate-p2p-core` now pulls `cuprate_types::AddressType` instead of defining it.

## hinto-janai | 2024-10-24T00:13:36+00:00
I guess we'll just use `FIELD_NOT_SUPPORTED` for fields like this?

Thankfully the RPC type is a hex `String` - I guess for types like `u64` we just use `Default::default()`?

## Boog900 | 2024-10-27T15:02:22+00:00
yeah I am ok with boxing this

## Boog900 | 2024-10-27T15:06:52+00:00
I think it might be best to try mimic the format but just set it to a default value, like `connection_id` is a UUID right? we should just set it to a default UUID and hex encode that.

## Boog900 | 2024-10-27T15:08:21+00:00
Yeah I think we should combine this request on the database side.

## Boog900 | 2024-10-27T15:10:58+00:00
I don't think we should - this request just gives you an overview `AltBlocksInChain` returns the full block blobs

## SyntheticBird45 | 2024-10-27T15:18:19+00:00
I agree with @Boog900 . If I had to debug breakage in migration i would prefer it to be still a default id of some sort rather an another type altogether. Just to avoid a big gasp here.

## Boog900 | 2024-10-27T15:18:47+00:00
```suggestion
use tower::ServiceExt;

use cuprate_pruning::PruningSeed;
use cuprate_rpc_types::misc::{ConnectionInfo, Span};
use cuprate_helper::cast::usize_to_u64;
use cuprate_p2p_core::{
```

## Boog900 | 2024-10-27T15:19:30+00:00
```suggestion
use monero_serai::block::Block;
use tower::{Service, ServiceExt};

use cuprate_helper::cast::u64_to_usize;
use cuprate_consensus_context::{
```

## Boog900 | 2024-10-27T15:20:02+00:00
```suggestion
use monero_serai::block::Block;
use tower::{Service, ServiceExt};

use cuprate_p2p_core::NetworkZone;
use cuprate_rpc_types::misc::Span;
use cuprate_helper::cast::{u64_to_usize, usize_to_u64};
use cuprate_pruning::PruningSeed;
use cuprate_types::{AddAuxPow, AuxPow, HardFork};
```

## hinto-janai | 2024-10-28T21:44:26+00:00
> combine this request on the database side

What does this mean specifically? 1 type/request/response?:

```rust
struct TxEntry {
    pub id: [u8; 32],
    pub weight: u64,
    pub fee: u64,
    pub time_in_pool: Duration,
}
```

## hinto-janai | 2024-10-28T21:59:10+00:00
It's worth noting this is the internal type used by our crates and not the RPC type for serialization - that does have `connection_id: String` where we could insert any value.

We could encode semantics by pulling in `uuid` in `cuprate-p2p-core` but couldn't we do this at the RPC level?

i.e. at this line: https://github.com/Cuprate/cuprate/pull/320/files#diff-8931bba412b83a9ff151c362c29a00a395a66f1a0ffc088fa0b7be3ffbfab064R68

we write `connection_id: String::from("00000000000000000000000000000000")`.

## hinto-janai | 2024-10-29T21:11:43+00:00
@Boog900 @SyntheticBird45 btw I meant this will just be the default value for now, easter egg can replace it whenever.

edit: hmm although actually it's probably better to explain that the field is not supported for end-users.

## Boog900 | 2024-10-29T22:12:26+00:00
Yeah

## Boog900 | 2024-10-29T22:14:31+00:00
Ah ok, I thought this would be returned in RPC requests, if this is just internal and we do replace it with data that decodes into the correct type before sending it over the wire then I am ok with that.

## Boog900 | 2024-11-01T21:51:48+00:00
This is putting the wrong string type in the response right? Should it be a `ConnectionId::DEFAULT_STR` ?

# Action History
- Created by: hinto-janai | 2024-10-18T00:39:30+00:00
- Merged at: 2024-11-01T22:25:55+00:00
