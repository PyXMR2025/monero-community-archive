---
title: '[seraphis] ringct: adjust multiexp'
source_url: https://github.com/monero-project/monero/pull/8679
author: UkoeHB
assignees: []
labels: []
created_at: '2022-12-14T17:43:22+00:00'
updated_at: '2023-05-15T20:55:30+00:00'
type: pull_request
status: merged
closed_at: '2023-04-25T15:10:52+00:00'
merged_at: '2023-04-25T15:10:52+00:00'
---

# Original Description
This is a PR in my 'upstreaming seraphis_lib project', the changes here are not used anywhere yet.

- Refactors `pippenger_cached_data` to use a vector of `ge_cached` directly. This makes `pippenger_cached_data` reusable (I use it in a multiexp utility in the `seraphis_lib`).
- Exposes `ge_p3` versions of the multiexp functions for efficiency when desired.
- Replaces tabs with spaces in the multiexp notes, so they are legible in text editors that don't render tabs consistently (mine shows all the whitespace in the multiexp notes as single spaces).

# Discussion History
## jeffro256 | 2022-12-15T05:23:19+00:00
Since `pippenger_p3` takes ` const&`, you can just pass it as is

## jeffro256 | 2022-12-16T08:17:21+00:00
This definition doesn't exactly match what's in the header because of the lack of `const` qualifiers, so it might produce warnings on some compilers.

## jeffro256 | 2022-12-16T08:19:10+00:00
The old PR did aligned allocs as well. Do you know why?

## UkoeHB | 2022-12-16T17:30:28+00:00
No clue, I just figured it's best to keep that in, in case it's somehow important.

## UkoeHB | 2022-12-16T17:33:32+00:00
Good catch, thanks

## jeffro256 | 2022-12-16T17:41:09+00:00
I think @moneromooo-monero added this feature IIRC, so maybe we ask him

## UkoeHB | 2022-12-16T17:45:41+00:00
Looks like it showed up in a commit called 'some speedups': https://github.com/monero-project/monero/pull/4219

## SChernykh | 2022-12-20T09:59:37+00:00
4096 is the virtual page size on x86, maybe this is why. It will only matter in low memory conditions when swapping starts - then the cache will fit in smaller number of pages. This alignment shouldn't affect performance otherwise.

## SChernykh | 2022-12-20T10:05:02+00:00
The old code had `CHECK_AND_ASSERT_THROW_MES(cache->cached, "Out of memory");` in case of allocation failure, this new code doesn't handle this case. `std::vector::resize` will throw `std::bad_alloc` on allocation failure, it should be handled here.

## SChernykh | 2022-12-20T10:14:27+00:00
On the other hand, the only difference will be what type of assertion will be thrown here: `CHECK_AND_ASSERT_THROW_MES` throws `std::runtime_error` instead of `std::bad_alloc`. I couldn't find places in code that catch explicitly `std::runtime_error`, so it shouldn't matter.

## SChernykh | 2022-12-20T10:21:43+00:00
Actually, I just found `catch (const std::runtime_error& e)` in `zmq_server.cpp` and `simplewallet.cpp`, so it makes sense to catch `std::bad_alloc` here and use the standard `CHECK_AND_ASSERT_THROW_MES`

## UkoeHB | 2022-12-20T15:31:40+00:00
I'm 90% sure this is a `CHECK_AND_ASSERT_THROW_MES()` because A) `aligned_realloc` does not throw an exception and needs to be checked, B) @moneromooo-monero only uses `CHECK_AND_ASSERT_THROW_MES()` and would never write `throw std::bad_alloc;`. I have seen nowhere in the code base where bad allocs are rethrown as runtime errors.

## SChernykh | 2022-12-20T15:53:01+00:00
`std::bad_alloc` is thrown in RandomX code. The other question is, should it be handled in the code at all, or just let everything crash. Probably the latter because out of memory situations are hard to recover from. I think it's fine to not catch it here.

## moneromooo-monero | 2022-12-20T16:45:02+00:00
UkoeHB is right, I seldom send separate exceptions. Sometimes std::rnutime_error, that's it. I'm pretty sure I did not write code that relies on std::bad_alloc here (though of course who knows what anyone else might have done later on).

# Action History
- Created by: UkoeHB | 2022-12-14T17:43:22+00:00
- Merged at: 2023-04-25T15:10:52+00:00
