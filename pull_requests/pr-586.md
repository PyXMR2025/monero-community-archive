---
title: 'cuprated: Add graceful shutdown, pt2: service error propagation'
source_url: https://github.com/Cuprate/cuprate/pull/586
author: redsh4de
assignees: []
labels:
- A-types
- A-binaries
created_at: '2026-02-20T01:08:29+00:00'
updated_at: '2026-05-20T18:25:57+00:00'
type: pull_request
status: open
closed_at: null
merged_at: null
---

# Original Description
### What
Depends on pt1, PR #585

Building on part 1, this replaces the panic-on-error patterns in cuprated's services with error propagation that makes use of the graceful shutdown mechanism

### Why
So we dont panic and insta-crash upon a error, shutdown should be graceful. Internal errors still caused panics before this via PANIC_CRITICAL_SERVICE_ERROR

### Where
- `cuprated`:
  - `blockchain/error.rs` (new):
    | Type | Kind | Variants |
    |---|---|---|
    | `BlockValidationError` | peer-fault (ban) | `AlreadyInMainChain`, `Consensus(ExtendedConsensusError)` |
    | `BlockInternalError` | node-fault (shutdown) | `Service(tower::BoxError)`, `Invariant(&'static str)` |
    | `BlockManagerError` | manager union | `Validation(_)`, `Internal(_)` |
    | `IncomingBlockError` | interface union | `Manager(_)`, `Orphan`, `UnknownTransactions(_, _)`, `TooManyTxs`, `ChannelClosed` |
  
  - `txpool/error.rs` (new):
    | Type | Kind | Variants |
    |---|---|---|
    | `TxValidationError` | peer-fault (ban) | `Parse(io::Error)`, `Consensus(ExtendedConsensusError)`, `DuplicateTransaction`, `RelayRule(RelayRuleError)` |
    | `TxInternalError` | node-fault (shutdown) | `Service(tower::BoxError)` |
    | `IncomingTxError` | union | `Validation(_)`, `Internal(_)` |
  
  - `monitor.rs` - add `TaskExecutor::spawn_critical`, `panic_message` helper.
  - `logging.rs` - return the log guard, hold it until shutdown completes.
  - `constants.rs` - rename `PANIC_CRITICAL_SERVICE_ERROR` to `CRITICAL_SERVICE_ERROR`
  - `lib.rs` - `Node::launch` returns `Result`, `DropGuard` cancels partially-spawned subsystems if init fails, `disarm()` before returning the `Node`.
  - `blockchain.rs` - `check_add_genesis` returns `Result`.
  - `blockchain/manager.rs` - run loop returns `Result`, `spawn_critical` for syncer + manager.
  - `blockchain/manager/handler.rs` - `.expect(...)` -> `?` throughout, handlers return `Result<_, BlockManagerError>` or `Result<_, BlockInternalError>`, `handle_command` routes `Internal` via `?` and `Validation` via response channel, empty-batch -> `Invariant`; `# Panics` -> `# Errors`.
  - `txpool/manager.rs` - uniform DB-error escalation: all 6 handlers (`rerelay_tx`, `promote_tx`, `handle_incoming_tx`, `handle_tx_timeout`, `remove_tx_from_pool`, `new_block`) propagate `TxPoolError` via `?`, P2P-layer errors (diffuse, dandelion route) stay log+continue, `rerelay_tx` short-circuits `NotFound`. New `TxPoolManagerClosed` returned by `TxpoolManagerHandle::new_block`.
  - `txpool/incoming_tx.rs` - `IncomingTxHandler::init` returns `Result`.
  - `tor.rs`, `p2p.rs` - `transport_*_config` / `initialize_clearnet_p2p` / `initialize_tor_p2p` return `Result`, Tor logs+skips on failure.
  - `rpc/server.rs` - `init_rpc_servers` returns `Result`, per-server task uses `spawn_critical`.
  - `rpc/handlers/{helper,json_rpc,other_json}.rs` - coinbase / hardfork / `serde_json` panics -> `Result`.
- `cuprate-types`
    - `TxConversionError` re-exported.

### How
#### 1. spawn_critical wraps each subsystem's future:

  | Outcome | Action |
  |---|---|
  | `Ok(())` | normal exit |
  | `Err(_)` | log w/ subsystem name -> trigger shutdown |
  | panic | log via `panic_message` -> trigger shutdown |

#### 2. Added layered errors.
Per subsystem: a peer-fault `ValidationError`, a node-fault `InternalError`, and a union. `From` impls route (`ExtendedConsensusError::DBErr` -> Internal; else -> Validation). The manager matches between the two:

  | Variant | Route | Caller's response |
  |---|---|---|
  | `Internal(_)` | `?` -> `spawn_critical` | log + graceful shutdown |
  | `Validation(_)` | back via response channel | ban peer / cancel downloader |

# Discussion History
# Action History
- Created by: redsh4de | 2026-02-20T01:08:29+00:00
