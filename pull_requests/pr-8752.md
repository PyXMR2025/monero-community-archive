---
title: 'trezor: support v2.5.2+, more tests, fix chaingen, static libusub fix'
source_url: https://github.com/monero-project/monero/pull/8752
author: ph4r05
assignees: []
labels: []
created_at: '2023-02-28T17:03:24+00:00'
updated_at: '2023-10-26T01:31:24+00:00'
type: pull_request
status: merged
closed_at: '2023-10-26T01:31:23+00:00'
merged_at: '2023-10-26T01:31:23+00:00'
---

# Original Description
- passphrase logic: remove backward compatibility for 2.4.3, code cleanup
- fix LibUSB cmake for static builds on OSX, FreeBSD
- fix Trezor support for FreeBSD, Android. Failt build if Trezor support is enabled but dependencies are missing
- tests: all Trezor tests now work with passphrase logic enabled by default. Passphrase test added with different passphrase. no_passphrase test added, Trezor pin test added. Testing wallet opening with correct and incorrect passphrase. Trezor test chain revamp, cleanup. Smaller chain, chain file versioning added (most of the changes in the PR).
- tests: Trezor tests support TEST_MINING_ENABLED, TEST_MINING_TIMEOUT env vars to change mining-related tests behaviour.
- monero tests: fix tx create from sources, input locking. Originally, creating a synthetic transactions with chaingen could create a transaction with outputs that are still locked in the current block, thus failing chain validation by the daemon. Simple unlock check was added. Some buggy tests were fixed as well as new unlock-checking version of tx creation rejected those, fixes are simple - mostly using correct block after a rewind to construct a transaction

As there are occasionally reported Trezor integration bugs (https://github.com/monero-project/monero-gui/issues/4105), this PR removes some old code, forces users to use the newest Trezor firmware and adds more tests. 

Tests are refactored so the passphrase flow is used for all transactions, besides one special test so we cover all code paths. 

------------------------
## Protobuf

Protobuf v21 is the latest compatible protobuf version, as Protobuf v23 requires C++17 (through its dependency Abseil library). 

To disable Protobuf version check and later compilation test, define `-DUSE_DEVICE_TREZOR_PROTOBUF_TEST=OFF`. But the compilation may fail later with less descriptive error.

To build with unlinked protobuf@21 on OSX:
```shell
CMAKE_PREFIX_PATH=$(find /opt/homebrew/Cellar/protobuf@21 -maxdepth 1 -type d -name "21.*" -print -quit) \
make debug-test-trezor -j8
```

Similarly on other systems, point CMAKE_PREFIX_PATH to Protobuf v21 installation.

OSX install:
```shell
brew install protobuf@21 
brew link protobuf@21
```

MSYS32:
```shell
curl -O https://repo.msys2.org/mingw/mingw64/mingw-w64-x86_64-protobuf-c-1.4.1-1-any.pkg.tar.zst
curl -O https://repo.msys2.org/mingw/mingw64/mingw-w64-x86_64-protobuf-21.9-1-any.pkg.tar.zst
pacman --noconfirm -U mingw-w64-x86_64-protobuf-c-1.4.1-1-any.pkg.tar.zst mingw-w64-x86_64-protobuf-21.9-1-any.pkg.tar.zst
```

# Discussion History
## ph4r05 | 2023-02-28T17:05:15+00:00
optimization: generate blockchain file up to the minimal HF version being tested. Previously was this step performed at runtime after loading the chain file.

## ph4r05 | 2023-03-02T12:32:52+00:00
This test worked previously fine by coincidence. By changing chain structure wallet API created double-spend transactions. We have to scan the wallet. 

The code now demonstrates the correct flow and tests also key image sync via API.

## SChernykh | 2023-06-22T07:38:10+00:00
Can you give a rationale of changing `blk_5` to `blk_5r` here, and for similar other changes in the tests? This code has never been changed before, so for sure the current Trezor changes shouldn't require it. If it's some bug in tests, I'd like to know where it was and why it required such changes.

## SChernykh | 2023-06-22T11:05:37+00:00
> monero tests: fix tx create from sources, input locking. Originally, creating a synthetic transactions with chaingen could create a transaction with outputs that are still locked in the current block, thus failing chain validation by the daemon. Simple unlock check was added. Some buggy tests were fixed as well as new unlock-checking version of tx creation rejected those, fixes are simple - mostly using correct block after a rewind to construct a transaction

I've compiled this PR and I'm playing with tests now, to look how this all works because I didn't touch this code before.

## SChernykh | 2023-06-22T12:05:31+00:00
Having `network_type` as a function parameter and as a class member is confusing, which is why you had to write `this->network_type` here. It's better to rename the class member to `m_network_type` to avoid further confusion.

## SChernykh | 2023-06-22T12:22:20+00:00
The above 2 lines can be changed to `outs.emplace_back(oi.idx, out.key, comm);`

## SChernykh | 2023-06-22T12:43:55+00:00
`abort` is a standard function defined in `cstdlib`, it's better to rename this variable.

## SChernykh | 2023-06-22T13:45:33+00:00
Not a bug that was introduced in this PR, but if this loop iterates more than once, it will use `height` which is never initialized.

## SChernykh | 2023-06-22T13:56:45+00:00
`wallets` should be const reference: `const std::vector<tools::wallet2*>& wallets`

## SChernykh | 2023-06-22T13:56:58+00:00
Same, `wallets` should be const reference: `const std::vector<tools::wallet2*>& wallets`

## ph4r05 | 2023-06-22T19:09:00+00:00
Thanks for starting the review! ❤️ 
 Regarding the blk_5r question - I had to rewind so tests pass after fix of tx lock time.

## SChernykh | 2023-06-23T18:21:04+00:00
The warning below says `Chain deserialization failed` but it's `serialize_chain_to_file` function.

## SChernykh | 2023-06-23T18:31:50+00:00
These two `clear` are redundant, it will be cleared on lines 416-417.

## SChernykh | 2023-06-23T19:03:26+00:00
I'm confused about this `+1` here and in other places. In wallet2.cpp, there is
```
uint64_t unlock_height = td.m_block_height + std::max<uint64_t>(CRYPTONOTE_DEFAULT_TX_SPENDABLE_AGE, CRYPTONOTE_LOCKED_TX_ALLOWED_DELTA_BLOCKS);
```
which means that if a transaction was mined in block `N`, its outputs can be spent in block `N+CRYPTONOTE_DEFAULT_TX_SPENDABLE_AGE`, but here you add additional 1. So, with current settings, a transaction mined in block 2914616 can be spent in block 2914626.
Or is `cur_height` a different height?

## SChernykh | 2023-06-23T19:10:24+00:00
From what I found in `blockchain.cpp`: https://github.com/monero-project/monero/blob/master/src/cryptonote_core/blockchain.cpp#L3337
It should be:
```
  if (hf >= 2)
    return hf >= HF_VERSION_MIN_MIXIN_15 ? 15 : hf >= HF_VERSION_MIN_MIXIN_10 ? 10 : hf >= HF_VERSION_MIN_MIXIN_6 ? 6 : hf >= HF_VERSION_MIN_MIXIN_4 ? 4 : 2;

  return 0;

## SChernykh | 2023-06-23T19:18:13+00:00
If `repeat` is 0, `iters` will also be 0. I would do `repeat ? std::ceil(repeat / (double)per_tx_limit) : 1` to guarantee at least 1 iteration of the loop.

## SChernykh | 2023-06-23T19:28:25+00:00
`m_head`, `m_events`, `m_mining_enabled`, `m_mining_timeout` and perhaps some other fields are missing in this copy constructor.

## SChernykh | 2023-06-23T19:31:45+00:00
`m_no_change_in_tested_tx` is not copied in this function.

## SChernykh | 2023-06-23T19:35:47+00:00
A typo? `last_block`?

## SChernykh | 2023-06-26T14:08:20+00:00
If `capabilities_size()` is 0 (no capabilities at all), this code will leave `on_device` as true. But if there are some capabilities, but none of them are `Capability_PassphraseEntry`, it will set `on_device` to false. I don't know the specifications, but it doesn't seem logical to me.

## SChernykh | 2023-06-26T14:10:24+00:00
I think it's safe to remove `m_features->capabilities_size() > 0` check because if it's empty, the loop inside will not run any iterations, and `on_device` will be set to false.

## SChernykh | 2023-06-26T14:29:14+00:00
`emplace_back` will be more efficient here.

## ph4r05 | 2023-06-26T18:41:14+00:00
in `gen_block_data` the height parameter is `uint64_t &height` and it set immediately (out param), it is ok then, no?

## SChernykh | 2023-06-26T18:45:00+00:00
Ah, yes, I didn't notice it. Then it's ok.

## ph4r05 | 2023-06-26T18:45:51+00:00
Our Trezor integration does not support HF below 8 (i.e., `HF_VERSION_MIN_MIXIN_10`) so we are not using it also in our test code. Is that ok?

## ph4r05 | 2023-06-26T18:50:59+00:00
I've got this warning:
> Clang-Tidy: Parameter 'wallets' is const-qualified in the function declaration; const-qualification of parameters only has an effect in function definitions 

I will move const to .cpp

## ph4r05 | 2023-06-26T18:51:01+00:00
> Clang-Tidy: Parameter 'wallets' is const-qualified in the function declaration; const-qualification of parameters only has an effect in function definitions 

## SChernykh | 2023-06-26T18:56:39+00:00
I think it's ok because these constants will not change, at least for Monero.

## SChernykh | 2023-06-26T18:57:19+00:00
You probably didn't change it in the function definition. It should be the same in both places.

## ph4r05 | 2023-06-26T19:00:29+00:00
This has it's own history 😅 
https://docs.trezor.io/trezor-firmware/common/communication/passphrase-redesign-migration.html

tldr for quite a while there were no capabilities. Our firmware supports only Model TT, which has passphrase entry. Thus the default value=true. 

Then caps were added in new firmwares. When caps are there, we reset `on_device` to false and set back to true only if given cap is present. Thus, it made sense as old firmware -> no caps -> passphrase entry by default.

But now we dont support those old firmares anymore, so I will remove it.

## ph4r05 | 2023-06-26T19:18:59+00:00
`cur_height` in `fill_tx_sources` in 

```cpp
uint64_t head_height = check_unlock_time ? get_block_height(blk_head) : std::numeric_limits<uint64_t>::max() - 1;
// ...
if (fnc_accept && !(fnc_accept.get())({.oi=oi, .cur_height=head_height}))
```

is current height of the head, but we are looking for a transaction spendable in the next block, thus `cur_height + 1`

## ph4r05 | 2023-06-26T19:47:05+00:00
fixed

## ph4r05 | 2023-06-26T19:50:26+00:00
maybe stale IDE analysis. added now

## ph4r05 | 2023-06-26T19:50:30+00:00
maybe stale IDE analysis. added now

## SChernykh | 2023-06-27T14:19:00+00:00
`+ 2` was added in the recent commits, is it to pop more transactions into mempool? The other `rewind_blocks_with_decoys` further down doesn't have this `+ 2`.

## ph4r05 | 2023-06-27T17:12:41+00:00
correct, I had to increase number of transactions to pick from as the last test was failing, so there is enough buffer for that

## selsta | 2023-06-28T13:36:00+00:00
Does this mean every macOS user has to now specifically install and link protobuf@21? This wasn't necessary previously and will break compilation for every Mac user who has the regular protobuf installed.

## selsta | 2023-06-28T13:36:43+00:00
Is it possible to set this to 14? We don't support C++17 yet.

## ph4r05 | 2023-06-28T19:45:55+00:00
let's see :)

## SChernykh | 2023-06-28T19:51:48+00:00
I'm fine with it, as long as it's in `contrib/brew/Brewfile` so MacOS users can build it while following instructions from readme.md

## ph4r05 | 2023-06-28T20:44:31+00:00
> Does this mean every macOS user has to now specifically install and link protobuf@21? This wasn't necessary previously and will break compilation for every Mac user who has the regular protobuf installed.

unfortunately, yes, new protobuf v23 requires newer cmake AFAIK (+some other deps). And we still dont have c++17. Will check what can be done in Brewfile 

## selsta | 2023-06-28T20:45:42+00:00
How does it work right now? I use 23 currently to compile monero.

## selsta | 2023-06-28T20:49:54+00:00
I just want to make sure that there is no other way before breaking compilation for all Mac users.

## selsta | 2023-06-28T20:54:04+00:00
macOS has the latest version of CMake (3.26.4) installed using brew.

## ph4r05 | 2023-06-28T21:50:10+00:00
@selsta I will try and let you know. When I let CI build osx with Protobuf v23 it failed. There may be another locked dependencies

## ph4r05 | 2023-06-29T06:36:06+00:00
@selsta I am having the same problems as runner in GHA, when upgrading with brew, `make clean ; make debug-test-trezor -j8` ends with an error.

```
  /opt/homebrew/include/absl/utility/utility.h:164:12: error: no member named
  'in_place_t' in namespace 'std'

  using std::in_place_t;
```

and this is c++17 feature https://en.cppreference.com/w/cpp/utility/in_place

So unless monero supports c++17 (why not now?) it has to use old protobuf lib.

## SChernykh | 2023-06-29T06:40:14+00:00
Maybe there's some macro definition you can supply to disable c++17 features in that code? I can't check right now.

## SChernykh | 2023-06-29T07:22:40+00:00
@ph4r05 ~~You can add `-DABSL_OPTION_USE_STD_OPTIONAL=0` to cmake command line (or add `add_definitions("-DABSL_OPTION_USE_STD_OPTIONAL=0")` to CMakeLists.txt) to disable C++17 in Abseil.~~

After checking Abseil's cmake files, it's not so simple. But you should still be able to disable C++17 there

## SChernykh | 2023-06-29T07:37:15+00:00
After checking some more, I can see that it should be done by Abseil automatically if you run the compiler with c++14 configured through the command line - `set(CMAKE_CXX_STANDARD 14)` in CMakeLists.txt should work.

## ph4r05 | 2023-06-29T07:50:27+00:00
@SChernykh can I pls resolve this one?

## ph4r05 | 2023-06-29T07:51:16+00:00
@SChernykh can I pls resolve this one?

## ph4r05 | 2023-06-29T07:51:40+00:00
@SChernykh can I pls resolve this one?

## ph4r05 | 2023-06-29T07:51:50+00:00
@SChernykh can I pls resolve this one?

## ph4r05 | 2023-06-29T07:54:58+00:00
@SChernykh `try_compile` fails with `"-DCMAKE_CXX_STANDARD=14"`, does not work. any suggestions?

## ph4r05 | 2023-06-29T08:08:06+00:00
I mean, this is no 5 minute fix 😅  so maybe another PR to make it build with the latest protobuf? It is few hours of work, experimenting. Maybe not worth right now (this may prolong merging of this stuff waay to long now)

## SChernykh | 2023-06-29T08:21:56+00:00
Yes, and other unresolved comments too.

## SChernykh | 2023-06-29T08:22:54+00:00
This whole protobuf/c++17 thing is not directly related to this PR, it compiled fine when you created this PR. So it can be fixed properly in another PR, I don't mind the current solution here.

## ph4r05 | 2023-06-29T08:29:23+00:00
@SChernykh it does not work with C++14, 
```
/opt/homebrew/include/absl/utility/utility.h:164:12: error: no member named 'in_place_t' in namespace 'std'
using std::in_place_t;
```

absl installation from homebrew has it turned on by default. Also `/opt/homebrew/include/absl/base/options.h`

```
#define ABSL_OPTION_USE_STD_OPTIONAL 1
```

meaning it needs C++17. We would have to redefine options somehow. This is too much work for this PR.

May be related to the https://github.com/protocolbuffers/protobuf/issues/12201, I would wait until this issue stabilizes or Monero supports C++17 

## selsta | 2023-06-29T11:12:52+00:00
We usually do it this way: Make a private branch inside your fork for testing and only push working commits to the (separate) PRed branch. Then you can always cancel builds that you don't need anymore here: https://github.com/ph4r05/monero/actions

## selsta | 2023-06-29T11:13:25+00:00
What's even the point of the compile test? Seems it has been failing since forever, see here in latest master: https://github.com/monero-project/monero/actions/runs/5392471097/jobs/9790860150#step:5:145

If you remove this test we can use the latest version of protobuf again without issues.

## ph4r05 | 2023-06-29T11:37:35+00:00
hmm, it is not failing neither for me locally nor for GHA runners. If I remove it and keep latest protobuf, it fails for me locally. Try upgrading protobuf to 23 locally and remove protobuf@21. maybe you have linked still v21?

## ph4r05 | 2023-06-29T11:40:28+00:00
sure, but is something wrong with the proposed change? why extra step of keeping 2 separate branches for one PR? 

## ph4r05 | 2023-06-29T11:41:45+00:00
if you fetch this PR branch and try compiling brew-installed protobuf v23 with c++14 it fails regardless of try_compile result. Does GHA runner give another result? I tried it in this PR, it failed.

## ph4r05 | 2023-06-29T11:42:15+00:00
making it work with latest protobuf is effor worth of hours, IMO for another PR. I dont have time for this rn

## ph4r05 | 2023-06-29T11:44:52+00:00
also ad failing compile test - this PR fixes it, if compile test fails, trezor support is not included, thats one of primary reasons for this PR

## selsta | 2023-06-29T13:17:01+00:00
I will try if I can fix it myself, I said this before but the problem is that this will break compilation and the brew file doesn't fix it, it will just cause conflicts with already installed version of protobuf -> support requests that I will have to deal with :/ I'm just unhappy with it but I can understand if you don't want to spend more time on it currently.

## ph4r05 | 2023-06-29T13:36:27+00:00
OK so in the next PR? :) 
Brewfile should relink protobuf@21 to be the main paths, so when installed, compilation should pass. 



## ph4r05 | 2023-06-29T13:38:54+00:00
Also pls note that this PR makes Trezor support mandatory, if some check fails and Trezor support could not be enabled, build fails. This was not the case before. So if protobufv23 failed on try compile, Trezor support was disabled and compilation passed. We dont want this to happen anymore as official builds sometimes lacked Trezor support (it is still possible to explicitly disable trezor support via cmake option, but by default, it is enabled.)

## ph4r05 | 2023-06-29T13:59:12+00:00
another approach IMO: a) c++17, then lets use new protobuf, b) allow using protobufv21 in not linked version, but then user should override protobuf version that is going to be used - env vars probably

```
cmake -DCMAKE_PREFIX_PATH="/opt/homebrew/Cellar/protobuf@21/21.12;$CMAKE_PREFIX_PATH" .
```

## selsta | 2023-06-29T14:03:01+00:00
If I remember correctly we used C++14 because of using an old XCode version for macOS binaries. We updated the XCode version a year ago so it might be possible for us to switch to C++17. I'll look into it.

## ph4r05 | 2023-06-29T14:07:25+00:00
This also works

```shell
CMAKE_PREFIX_PATH=$(find /opt/homebrew/Cellar/protobuf@21 -maxdepth 1 -type d -name "21.*" -print -quit) \
 make debug-test-trezor -j8
```

## tobtoht | 2023-07-10T18:32:42+00:00
This breaks development builds on Arch Linux, Brew, MSYS2, which all ship Abseil built with C++17.

https://github.com/abseil/abseil-cpp/issues/696#issuecomment-636949929

## ph4r05 | 2023-07-11T07:16:36+00:00
Do you pls have any CI link for this? All tests are passing here. Trezor support is mandatory now, unless it is explicitly turned off by compile switch. 

Also, when compile test fails, is the whole build passing with Trezor?

## tobtoht | 2023-07-11T10:20:21+00:00
Rerun CI. MSYS2 updated to Protobuf 23.4 (from 21.23) 5 days ago.

## ph4r05 | 2023-07-11T15:31:32+00:00
Hmm, what do you propose? Can we pin protobuf to v 21? 
@selsta pls any update on c++17? Thanks!

## selsta | 2023-07-11T15:33:19+00:00
I opened a PR for it but it's unclear if others agree with it: https://github.com/monero-project/monero/pull/8922

## tobtoht | 2023-07-11T16:50:50+00:00
I'd rather not see builds breaking by default on an increasing number of systems.

Some options: 
- Wait for #8922 merge (if accepted) and fix the broken test in CheckTrezor.
- Disable Trezor support by default if Protobuf > 21.

## ph4r05 | 2023-07-12T07:19:54+00:00
>     Disable Trezor support by default if Protobuf > 21.

This would result in official builds without Trezor support. I don't like this, rather pin protobuf to v21 or install from sources

## ph4r05 | 2023-07-12T11:29:53+00:00
AFAIK only win build is broken here. And according to the https://www.msys2.org/docs/package-management/ and https://github.com/msys2/setup-msys2 we can install older protobuf version explicitly. This handles CI case.
 (=official releases).

If user wants to do it on their own, they can a) disable Trezor support if they dont need it b) wait for c++17 support c) follow CI recipe and install compatible protobuf. 

## tobtoht | 2023-07-12T11:47:31+00:00
Official CLI builds are built with `depends` and it [doesn't use](https://github.com/monero-project/monero/blob/master/contrib/depends/packages/native_protobuf.mk#L2) Protobuf > 21. Monero GUI builds are pinned at an older version too.

FYI, the monero-gui Arch Linux [package](https://gitlab.archlinux.org/archlinux/packaging/packages/monero-gui/-/blob/main/PKGBUILD) (which is built against Protobuf 23.4) does not have Trezor support because of the broken test in CheckTrezor. So this should be fixed regardless.

## tobtoht | 2023-07-12T12:57:03+00:00
MSYS2 does not support downgrades. It would be easier to configure CI to 
```
sed -e 's|CMAKE_CXX_STANDARD 14|CMAKE_CXX_STANDARD 17|' -i CMakeLists.txt
``` 
and fix the test.



## tobtoht | 2023-07-12T13:45:22+00:00
|                             | Current state           | With this PR                                                         | 
|-----------------------------|-------------------------|----------------------------------------------------------------------|
| Official builds  (Pb <= 21) | ✅ Trezor support (not enforced) | ✅ Trezor support (enforced by default)                                             |
| Dev builds (Pb <= 21)       | ✅ Trezor support (not enforced) | ✅ Trezor support (enforced by default)                                               |
| Dev builds (Pb > 21) [0]       | ❌ Trezor support, [1]| ❌ **Builds fail**, [2]                   |
| Monero packages (Pb > 21)   | ❌ Trezor support, [1] | ❌ **Builds fail**, [1] |

[0] Currently: Arch Linux (and derivatives), MSYS2, and Homebrew (unless Pb is downgraded).

[1] To enable Trezor support user/maintainers need to set `CMAKE_CXX_STANDARD` to `17` and remove the test in `CheckTrezor.cmake`. (https://paste.debian.net/plainh/6361dd42)

[2] To disable Trezor support users need to manually pass `-DUSE_DEVICE_TREZOR=OFF`. 

A less drastic measure to require Trezor support for official **CLI** builds would be to check if we're building a release build with `if(DEPENDS)` and force it only then. Unfortunately, this doesn't work for the GUI as it doesn't use `depends` for all builds. Alternatively, you can check if `CMAKE_BUILD_TYPE` is `Release`, but this will catch some users building with `make release`, so this isn't ideal.

My point is that I would rather see graceful failure for Protobuf > 21, because it will otherwise result in an influx of support queries.

## ph4r05 | 2023-07-12T16:17:06+00:00
Homebrew should work - it enables to install protobuf@21, there is also a Brewfile updated in this PR accordingly.

## ph4r05 | 2023-07-12T16:22:10+00:00
hmm @tobtoht I see, but what do you suggest? :) I could add a switch to disable compilation test. But is there a way to detect protobuf is > 21?

## selsta | 2023-07-12T16:23:46+00:00
https://cmake.org/cmake/help/latest/module/FindProtobuf.html has a Protobuf_VERSION

## ph4r05 | 2023-07-13T05:52:40+00:00
@selsta @tobtoht I've added a new switch USE_DEVICE_TREZOR_PROTOBUF_TEST, enabled by default. It checks if protobuf > 21, then it fails gracefully. Otherwise continues with the compilation test. When set to OFF, no Protobuf version check and compile test is done. Is this ok?

## SChernykh | 2023-09-27T07:42:37+00:00
`is cannot be compiled` a typo? You can also add "because it requires C++17".


## SChernykh | 2023-09-27T07:43:11+00:00
Nevermind, you mention C++17 further down.

# Action History
- Created by: ph4r05 | 2023-02-28T17:03:24+00:00
- Merged at: 2023-10-26T01:31:23+00:00
