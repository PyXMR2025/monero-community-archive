---
title: 'Epee: CppCheck fixes'
source_url: https://github.com/monero-project/monero/pull/7480
author: mj-xmr
assignees: []
labels: []
created_at: '2021-03-08T21:22:44+00:00'
updated_at: '2023-01-04T09:51:01+00:00'
type: pull_request
status: closed
closed_at: '2023-01-04T09:51:01+00:00'
merged_at: null
---

# Original Description
Fixes some potential memory access bugs and styling, as well as adds braces around one-liner loop iterations.
Current scope: Epee (to keep the branches easier to review)

# Discussion History
## mj-xmr | 2021-03-08T21:36:23+00:00
Here I decided, that we cannot assume, that the file was accessible from the vector at all, which would be actually the reason (or one of them) for falling into this catch scope in the first place.

## mj-xmr | 2021-03-08T21:36:57+00:00
An unsigned value cannot be negative.

## mj-xmr | 2021-03-08T21:38:26+00:00
If `max_log_files` happens to be 0 for some reason, then accessing the files under the condition of <= would crash.

## iamamyth | 2021-03-08T23:00:51+00:00
Would suggest just inlining this in the array indexing operation, rather than having a single-use variable named `temp`.

## iamamyth | 2021-03-08T23:02:41+00:00
Note: The above comment requires moving the `csTmp` assign above the `num_char` assign.

## mj-xmr | 2021-03-09T06:31:18+00:00
What do you think of this change? This will prevent the index from overflowing if the `max_log_files` is greater than `found_files.size()`
I also need an advice. Would simply writing:

`static_cast<int>(found_files.size() - max_log_files);`

be enough? I think not, because we can't assume that the result will be compatible with the `int` type.

## mj-xmr | 2021-03-09T11:32:12+00:00
Done

## iamamyth | 2021-03-09T18:43:55+00:00
I think you're missing the context here, all of this code is inside `if (found_files.size() >= max_log_files)`.

## vtnerd | 2021-03-09T20:33:14+00:00
Did CppCheck blindly recommend to add an `&` here? There's little benefits since its a 16-byte value with a trivial copy-constructor - the compiler is free to optimize as necessary anyway.

## xiphon | 2021-03-09T20:48:21+00:00
Handle `max_log_files` > `found_files.size()` case separately.

You don't need `int` cast at all.


## xiphon | 2021-03-09T20:50:40+00:00
> Here I decided, that we cannot assume, that the file was accessible from the vector at all, which would be actually the reason

How that's possible?

## xiphon | 2021-03-09T20:52:55+00:00
> all of this code is inside if (found_files.size() >= max_log_files).

Ah, yeah. The `max_log_files > found_files.size()` case is already handled.

## mj-xmr | 2021-03-13T07:36:48+00:00
@moneromooo-monero : Here's the potential UB. Replaced [] access with .at().

## mj-xmr | 2021-03-13T07:41:35+00:00
No. This was my own initiative while touching the package.

The compiler optimizes this only in the release mode. I see little reason not to use a const reference, since it doesn't hurt. Especially if `auto` is used, one can't assume, that in future the `sources` remains the same 16-byte value, for which the const reference currently brings no benefit, I admit.

I can still revert it, if it's not convincing for you.

## mj-xmr | 2021-03-13T07:55:35+00:00
What happens if the `found_files` is empty and `max_log_files` is zero?

Then the only line, preventing the code from crashing is [at line 174](https://github.com/monero-project/monero/pull/7480/files#diff-6676c0bac6e2626df8a81502a138684333631486fd1493787b383d37750e9890R174), located whole 30 lines above. So far away, that it seems out of context.
To me this style is misleading, but of course I'm open for suggestions if you think that my fix doesn't look better either.

Shall we settle with just .at() instead of []?

## mj-xmr | 2021-03-13T11:50:47+00:00
Because `.at(i)` throws an exception when `i` is out of the container's bounds. In such case, another call to `.at(i)` would kill the purpose of catching the exception in the first place.

## moneromooo-monero | 2021-03-13T16:10:41+00:00
I don't see it and I'm getting really suspicious it's cargo cult given the accompanying changes. Can you give details of how you end up with an out of bounds here ?

## SChernykh | 2021-03-23T07:41:48+00:00
We should settle at not touching this place at all, given the previous check `if (found_files.size() >= max_log_files)`. Don't just do changes because cppcheck said so, it's a dumb static analyzer. At most, you can replace `i < found_files.size() - max_log_files` with `i  + max_log_files < found_files.size()` if you worry about it.

## SChernykh | 2021-03-23T07:51:55+00:00
This whole function is an abomination of the common sense (not related to your changes though). Why call it `dec_to_hex` and then supply `radix` argument? If it's hex only, remove the argument and rewrite it with just a few lines of code to make 2 hex characters out of a single-byte `char` value. Using `std::string` and `std::reverse` here is a cherry on the top, why? To make it 10 times slower than needed with some memory allocation? Example: http://cpp.sh/9xt2hk

## SChernykh | 2021-03-23T07:53:44+00:00
Just checked: it's used only in 1 place and only with radix = 16, so it makes sense to rewrite it completely

## SChernykh | 2021-03-23T07:55:03+00:00
There's no way `found_files[i]` can be out of range, given the for-loop check above. We lose valuable information (file name) in the log with this change. Not recommended.

# Action History
- Created by: mj-xmr | 2021-03-08T21:22:44+00:00
- Closed at: 2023-01-04T09:51:01+00:00
