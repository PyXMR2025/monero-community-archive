---
title: Include tree root in PoW block hash
source_url: https://github.com/seraphis-migration/monero/pull/22
author: j-berman
assignees: []
labels: []
created_at: '2025-04-05T02:07:01+00:00'
updated_at: '2025-05-16T01:08:32+00:00'
type: pull_request
status: merged
closed_at: '2025-05-16T01:08:32+00:00'
merged_at: '2025-05-16T01:08:32+00:00'
---

# Original Description
As soon as a block is added to the chain, we grow the chain with outputs w/last locked block 9 blocks after that block, i.e. with the earliest possible last locked block for outputs included in the block we just added. We keep the tree ahead of the chain, so that miners can immediately start mining the next block reading the root from the db without needing to do tree building themselves, and maximizing PoW built on top of a tree root for when it is later usable to construct an FCMP++ tx.

# Discussion History
## jeffro256 | 2025-04-08T03:24:49+00:00
Make sure to fix merge conflicts here

## jeffro256 | 2025-04-08T03:25:27+00:00
Make sure to fix merge conflicts here

## jeffro256 | 2025-04-08T03:32:18+00:00
This may be far-fetched, but could an adversary exploit an ambiguity between Helios and Selene tree roots to an SPV wallet (i.e. someone without knowledge of `n_tree_layers`)? Even for something trivial like a set of size 1? In other words, is it guaranteed that `hash_grow()` is collision resistant even for points of the opposite curve ? If not (or we don't consider putting in the brainpower to figure it out), and if the prime field for both curves is strictly less than 2<sup>255</sup>, and we're not storing a x-y parity bit, or if the prime field for both curves is strictly less than 2<sup>254</sup> and we *are* storing a x-y parity bit, then we could store a bit flag for whether or not the point is Helios or Selene. And if we don't have enough space, then we could always place an additional `bool` for the same purpose. Or maybe some other hacky solution like using the `minor_version` field...

## jeffro256 | 2025-04-08T03:49:54+00:00
I'd like if this was calling something other than the "tree hash" (I know it was called tx tree hash before). We are already adding FCMP/curve *trees* in this upgrade, the field we are adding to `block` is called the *tree* root. We add the *tree* root to the *tree* hash, it's just too many *trees*... 🌳 And TBH nowhere in the node code do we care that this hash is a merkle *tree*, just that it binds the transaction list. Additionally, AFAIK, there's no SPV code anywhere yet that actually uses this hash as a merkle *tree*, mainly because Monero node haven't yet stored/served this piece of information efficiently (i.e. we don't yet support header-only sync).

I propose something like "block content hash" because that's how we currently use it and its name is different enough from other terms circulating around in the codebase.

## jeffro256 | 2025-04-08T03:59:33+00:00
Isn't this over *100* quadrillion, not 200? $18^6 * 38^6 = 102{,}408{,}642{,}742{,}358{,}016$. 

## jeffro256 | 2025-04-08T04:22:18+00:00
12 is a good limit. At 14 layers, the number of leaves supported is greater than what can be stored in a 64-bit unsigned integer, so not point ever going higher than that. 13 is unlucky. 

## jeffro256 | 2025-04-08T04:31:14+00:00
When `monerod` support on my Commodore 64? 

## jeffro256 | 2025-04-08T04:32:48+00:00
Nice, this will simplify `finalize_all_proofs_from_transfer_details()` 

## jeffro256 | 2025-04-08T04:38:22+00:00
Note: we could have the tree cache validate the roots provided provided in the block data, but this has the same performance issues for forward sync that we discussed earlier. Probably not worth it if the whole point of `TreeCacheV1` is not to need the daemon to serve tree information. 

## jeffro256 | 2025-04-08T04:40:30+00:00
Much nicer to look at

## jeffro256 | 2025-04-08T04:54:39+00:00
Is this not the same as `major_version`?

## jeffro256 | 2025-04-08T05:19:14+00:00
Same here, should be equal to `major_version`, yeah? And what we care about is the version of the aforementioend block anyways, yeah?

## j-berman | 2025-04-08T18:57:19+00:00
Yep, you're right. Good spot thank you for double checking

## jeffro256 | 2025-04-08T19:15:31+00:00
Just for fun/perspective, we would need to add an average of 205 million outputs (2x April 2025 Monero mainnets worth of outputs) per block for the next ~1890 years before we his this limit *and* the max number of blocks limit. 

## j-berman | 2025-04-10T01:12:22+00:00
This is a good point! For SPV, I think we actually do need to hash and store `n_tree_layers` alongside `fcmp_pp_tree_root` in order to even parse an FCMP++ tx (let alone verify one).

## j-berman | 2025-04-10T01:24:14+00:00
It looks like "current version" could be different from "ideal version" if the new hf isn't voted for using the existing voting mechanism (I haven't looked much into the voting mechanism). It's worth noting this PR's code would break if they differ. I'll look more into the voting mechanism.

## j-berman | 2025-04-10T01:32:28+00:00
Ya, I'm just going to use `major_version` here

## j-berman | 2025-04-18T00:16:33+00:00
Correction: we don't need `n_tree_layers` to parse the tx since it's included as part of the tx (duh), but it's useful in order for SPV nodes to verify FCMP++ proofs, and mitigates potential problems noted using the incorrect layer.

Included it in the latest

## jeffro256 | 2025-05-07T19:39:04+00:00
I think that it would be good to document here exactly which tree root is actually being fetched. Is it the root of the tree of all the normal outputs in this block, plus the others that unlock in exactly 10 blocks, plus previous? Or is it off by N? Etc

## jeffro256 | 2025-05-11T05:53:02+00:00
This isn't strictly related to this PR, but shouldn't this method be named `get_outs_at_last_locked_block_index`? Is this a holdover from when the `reference_block` field in the transaction was a block ID instead of a block index?

## jeffro256 | 2025-05-11T06:00:21+00:00
Sorry, another naming nit. This method is called "advance tree *one* block", but here it gets advanced multiple blocks. Idk what it should be called instead. Also, is there ever a time that this function should work when when `blk_idx` is not equal to height-1?

## jeffro256 | 2025-05-14T05:29:57+00:00
Same as mentioned earlier: it would be good to document exactly which tree root this fetches

## j-berman | 2025-05-14T15:47:12+00:00
I can change any of my usage of `block_id` when I mean `block_idx` to `block_idx`. IIRC I initially did this because I saw `block_id` used in reference to an index elsewhere in the file, and repeated that. I agree `_idx` is better.

## j-berman | 2025-05-14T15:50:58+00:00
> Sorry, another naming nit. This method is called "advance tree one block", but here it gets advanced multiple blocks.

I'll just call it `advance_tree`.

> Also, is there ever a time that this function should work when when `blk_idx` is not equal to height-1?

EDIT: yes, during the migration. Another pre-check that could be useful is making sure `earliest_last_locked_block` is equal to the last block + 1 in the tree meta table (or if `blk_idx == 0`, the table is empty).

~~As the code is structured, no. I'll add an early pre-check to this function.~~

## jeffro256 | 2025-05-15T18:01:39+00:00
This is a good document, but could you please move it to the `BlockchainDB` class, since that defines the API?

# Action History
- Created by: j-berman | 2025-04-05T02:07:01+00:00
- Merged at: 2025-05-16T01:08:32+00:00
