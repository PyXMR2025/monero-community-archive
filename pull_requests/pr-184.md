---
title: tx relay v2
source_url: https://github.com/seraphis-migration/monero/pull/184
author: '0xFFFC0000'
assignees: []
labels: []
created_at: '2025-10-16T19:37:37+00:00'
updated_at: '2026-01-30T17:44:04+00:00'
type: pull_request
status: merged
closed_at: '2026-01-30T17:44:04+00:00'
merged_at: '2026-01-30T17:44:04+00:00'
---

# Original Description
https://github.com/monero-project/monero/pull/9933

# Discussion History
## SChernykh | 2025-11-02T17:12:17+00:00
`elapsed` counts time since first seen, not since `fly()` was called, so it can timeout 1 millisecond after the `fly()` call. Is it indended?

## SChernykh | 2025-11-02T17:16:27+00:00
You don't check the return value of `received_requested` here. If it returns `false`, then tx_hash wasn't received and you shouldn't remove in flight request, no?

## SChernykh | 2025-11-02T17:21:41+00:00
What's the error return value for this function? It returns 1 on all code paths, is it a logical bug here? Also, it just silently drops all tx hashes once the limit is reached. Maybe it makes sense to queue them for later?

## SChernykh | 2025-11-02T17:24:11+00:00
This abandons the in flight request if we received this tx from a block, right? What if this peer already sent this tx to us, and the response will arrive next millisecond? Will it call `remove_flight_request` again? But I guess it's fine because you check if it's `> 0` there.

## SChernykh | 2025-11-02T17:26:27+00:00
This should have a minimum sample size. Right now, if a peer misses its first request, it gets 100% miss ratio and then gets dropped. Not fair, I think.

## SChernykh | 2025-11-02T17:32:54+00:00
I don't understand what's going on at this line. `by_tx_hash` is a reference to something, are you reassigning it? It's an unconventional use of C++ references, so I'm confused. Maybe it's better to give it a new name `by_tx_hash2` or similar to not mix with the above code?

## SChernykh | 2025-11-02T17:37:09+00:00
This doesn't check for max in flight requests limit, so a malicious peer can send a max size packet and squeeze as many tx hashes as possible into it. They can even pick a single tx hash of some "fat" transaction currently in the mempool and make the reply very big.

Edit: an attacker can request the same hash N times (as many times as possible) - the hash of the biggest transaction in the pool. The code doesn't check for duplicate hashes.

Edit 2: 2 MB packet can ask for 65536 tx hashes, and if they all ask for the same 150 KB transaction (max size allowed in the mempool), this is 9.15 GB of data in the `txs` vector. If many peers do this at the same time, they for sure will DoS the node.

## SChernykh | 2025-11-02T17:40:10+00:00
This check means that `in_flight_requests` is not an exact counter and we can underestimate how many request this peer actually has?

## SChernykh | 2025-11-02T17:41:28+00:00
This function is not used anywhere?

## SChernykh | 2025-11-02T20:23:50+00:00
Should return with an error as soon as `txs` vector gets bigger than the max packet size (100 MB?). This is in addition to the duplicate hash checking.

## SChernykh | 2025-11-02T20:25:25+00:00
Is it very hard to change `handle_incoming_tx` and make it return the tx hash as an optional output parameter?

## SChernykh | 2025-11-02T20:43:45+00:00
Why no index here?

## 0xFFFC0000 | 2025-11-02T20:43:59+00:00
That is not my API, I cannot change the design how p2p interacts. 

## 0xFFFC0000 | 2025-11-02T20:45:31+00:00
Read boost multi-index document please. 

## 0xFFFC0000 | 2025-11-02T20:46:17+00:00
That m_p2p convention. Nothing in my power. 

## 0xFFFC0000 | 2025-11-02T20:47:41+00:00
That is request callback. Gets called in predefined intervals. Yes that is intended behaviour. 

## SChernykh | 2025-11-02T20:49:34+00:00
You could at least search the index first to see if you need to run the loop at all.

## 0xFFFC0000 | 2025-11-02T20:50:16+00:00
100MB? That is extremely extremely huge. Many other limitations will kick in before even getting close to that limitation. 

## SChernykh | 2025-11-02T21:00:52+00:00
Read the comment above. This array can grow to over 9 GB if an attacker sends a specially crafted request.

## 0xFFFC0000 | 2025-11-02T21:05:13+00:00
Looking up index via multi_index is not O(1). It is amortized O(n), which means probabilistically it is O(n) 50% of the time. 

Easiest and efficient way to do is a simple go over element. 

## 0xFFFC0000 | 2025-11-02T21:09:39+00:00
That requires a complete change to one of the core APIs of p2p, which is out of scope of this PR. 

## 0xFFFC0000 | 2025-11-02T21:29:03+00:00
It will be treated like what we used to do.

## 0xFFFC0000 | 2025-11-02T21:29:46+00:00
Good point. I will add that.

## 0xFFFC0000 | 2025-11-02T21:38:37+00:00
This part is going to get removed. Nothing important.

## 0xFFFC0000 | 2025-11-02T21:43:33+00:00
I don't understand your point. At this point we already received the transaction. 

The only issue here is when the received tx is not the one we requested from a connection id. Which is a known issue and will be addressed in next update. 

## 0xFFFC0000 | 2025-11-02T21:49:08+00:00
It is roughly exact number. But we should have defense against any network inconsistency in all layers. 

The else clause is basically an assert log error message.

## 0xFFFC0000 | 2025-11-02T21:50:47+00:00
I am in process of changing this class design a little bit. So this either will be removed or will have another design. 

## 0xFFFC0000 | 2025-11-02T21:52:35+00:00
Keep in mind we haven't discussed the maximum number txs that can be requested. We will add a layer of defense to this code. 

## 0xFFFC0000 | 2025-11-04T14:24:05+00:00
Fixed it, thanks.

## 0xFFFC0000 | 2025-11-04T14:24:14+00:00
Fixed it, thanks.

## j-berman | 2025-11-07T08:13:41+00:00
unused function

## j-berman | 2025-11-07T08:43:16+00:00
Looks like you meant to use `arg.t.size()` here

## j-berman | 2025-11-07T08:53:53+00:00
These functions are all unused (lines 165 to 229)

## j-berman | 2025-11-07T09:03:50+00:00
newline

## j-berman | 2025-11-07T09:06:27+00:00
I think it's not a big deal to add a new return by ref for the tx hash in `handle_incoming_tx`, but not a requirement for this PR. Can be handled in a follow-up

## j-berman | 2025-11-11T01:56:42+00:00
nit: I think `NOTIFY_TX_POOL_HASHES` would work a bit better

## j-berman | 2025-11-11T02:36:13+00:00
```suggestion
  , "Set the deadline for waiting for requested transactions (in seconds)"
```

## j-berman | 2025-11-11T02:53:03+00:00
nit: it looks like this is just going to use the default P2P_DEFAULT_REQUEST_TIMEOUT for `m_peer_request_interval` in every case because it'll call the function in the line above immediately before init() runs, which is fine, but looks unintentional and confusing

## j-berman | 2025-11-11T03:26:08+00:00
5s seems like not a lot of time, potentially a cause of uptick in bans when running tx relay v2.. log level 1 filled with `Missed tx announcement more that threshold of the time, dropping peer` would indicate that is the case

E.g. let's say you have 5 128-input txs to receive.. even if you assume 0 latency to receive the tx after requesting it, all the calls to `handle_incoming_tx` could take at least 15s to verify the last tx in that batch and mark it received

It may even make sense to avoid calling `check_tx_request_queue` if `handle_notify_new_transactions` is executing, or synchronize the 2 functions with a lock

## j-berman | 2025-11-11T04:13:55+00:00
Unused `pad` variable

## j-berman | 2025-11-11T04:21:09+00:00
If it's a tx we requested from this peer, looks like it'll still remain in the context's `tx_announcements` as well.

In fact the context's `tx_announcements` struct doesn't shrink anywhere, it will grow forever.

I think there is going to be some trouble keeping these 2 distinct data structures in sync perfectly (the context's stats <> `m_request_manager`). It would be nice to consolidate the logic into one, e.g. maybe move all logic into the `m_request_manager`

## j-berman | 2025-11-11T05:41:46+00:00
Check if in the chain too

## j-berman | 2025-11-11T05:44:39+00:00
Doesn't look like you need both `add_announcement` and `add_request`, and can just consolidate the logic of `add_request` into `add_announcement`

## j-berman | 2025-11-11T06:00:16+00:00
P2P_MAX_IN_FLIGHT_REQUESTS=100 seems pretty low to just stop handling new tx hashes and silently ignore them. The daemon wouldn't be able to handle more than 100 new tx hashes it hasn't seen before per connection. I think instead it would make more sense to queue tx hashes for requesting later on... and the `check_tx_request_queue` could be altered to kick off requests that haven't been kicked off before (edit: in addition to the deadline/stale check)

## 0xFFFC0000 | 2025-11-13T16:08:55+00:00
This part is ugly. I am thinking about how to do the logic between `add_announcement` and `add_request`.

## 0xFFFC0000 | 2025-11-13T16:09:15+00:00
Great point. Thanks. will be addressed in the new push. 

## 0xFFFC0000 | 2025-11-13T16:10:15+00:00
Thanks. Good catch.

## 0xFFFC0000 | 2025-11-13T16:11:06+00:00
Unused right now. But I want to extend the `connection_context` statistics. I will remove it for now.

## 0xFFFC0000 | 2025-11-13T16:11:29+00:00
Thanks, will be fixed in new push. 

## 0xFFFC0000 | 2025-11-13T16:12:24+00:00
Will change this, thanks.

## 0xFFFC0000 | 2025-11-13T16:13:14+00:00
Thanks.

## 0xFFFC0000 | 2025-11-13T16:14:36+00:00
Will try to find a better solution. sadly it is not possible to change the interval in our idle timer caller/scheduler.

## 0xFFFC0000 | 2025-11-13T16:17:12+00:00
Leftover from when we had pad variable. 

## j-berman | 2025-11-13T16:44:53+00:00
EDIT: sorry, I think I see what you're saying (that it's a leftover from an earlier state of the PR and will be removed). Can ignore below.

This is a new function this PR introduces that doesn't need `pad` like the other function in the code base does

## 0xFFFC0000 | 2025-11-13T17:24:19+00:00
> nit: it looks like this is just going to use the default P2P_DEFAULT_REQUEST_TIMEOUT for `m_peer_request_interval` in every case because it'll call the function in the line above immediately before init() runs, which is fine, but looks unintentional and confusing

This is not correct. We are able to change `m_peer_request_interval` dynamically later. 

## j-berman | 2025-11-14T22:40:45+00:00
`arg.t.size()`

## Boog900 | 2026-01-17T12:53:13+00:00
Should this request now respond with `NOTIFY_TX_POOL_HASH`? 

Also, although not done in this PR, I don't _really_ like the idea of responding to requests in multiple messages if the messages trip the packet limit. There are limits on the size of the "pending to send" queue which could trip if multiple big response messages are sent in quick succession. I also think it is a DoS vector. 

The alternative, just truncating the response, isn't great, however I do think it is preferable. 

## Boog900 | 2026-01-17T13:06:42+00:00
I don't think we should have the `|| (m_core.get_target_blockchain_height() > m_core.get_current_blockchain_height())` here.

A node could fake being ahead of the chain and as `get_target_blockchain_height` can be set higher after you have already synced they can prevent this from running until the request for blocks from them timesout.

`is_synchronized` only updates once, when we think we have reached the top of the chain, after that it wont turn `false` again.


## Boog900 | 2026-01-19T13:21:37+00:00
I know I suggested this check to prevent a malicious node from just not relaying all txs, but I do wonder if this could lead to a similar issue like this: https://github.com/monero-project/monero/issues/9335 if the tx-pool is dropping txs too fast.

I don't really know a good solution, maybe we should check txs are still in the pool when the fluff timer fires, as then we reduce the time where a tx can be received then removed while still being sent out? It wouldn't fix this though. 

I don't really think a fix is needed in this PR however I did just want to mention this.

## j-berman | 2026-01-20T18:11:57+00:00
> maybe we should check txs are still in the pool when the fluff timer fires, as then we reduce the time where a tx can be received then removed while still being sent out?

Can you expand on this case?

The one case I can think of where the pool may be dropping txs is if the pool is at capacity (max txpool weight) and higher fee txs are displacing the lower fee ones.

I also don't see a great solution to this causing over-exuberant dropped connections.

This general issue is at least mitigated by this being a failure threshold % of total reqs i.e. success cases should strongly outweigh failures especially as time goes on.

## j-berman | 2026-01-20T18:18:19+00:00
My main concern is that if a node falls behind for whatever reason, it can end up missing a number of tx reqs that are already in the chain (other nodes won't respond to our node's tx reqs because they're in the chain already), causing connection drops while syncing.

I think you provide a good reason for removing that || as is, but I also do think we'll want to monitor this issue and prioritize fixing it if it arises.

Perhaps it makes sense to include logic: if our node falls behind and resumes the sync download, clear the tx requests queue.

## Boog900 | 2026-01-20T18:57:59+00:00
> Can you expand on this case?

This wasn't another case. The way the current code works is once a tx is received it is added to the pool and the fluff queue, IIRC the fluffy queue is average of 2.5 seconds for outbound connections and 5 for inbound. If the tx in the pool is then removed before the fluff timer fires we will tell a peer we have a tx when we don't after the timer fires.

If we instead, after the fluff timer fires, check if we still have the tx before sending it, then the time that the tx can be removed while we still tell the peer with have it is reduced. It's not a fix and its also quite heavy handed, it wouldn't be cheap to check the pool after every fluff fire I wouldn't think.

Thinking about it, a better solution might be to not have a queue and to just ask the tx pool for txs to fluff _after_ the timer fires. Just a thought though. 

> This general issue is at least mitigated by this being a failure threshold % of total reqs i.e. success cases should strongly outweigh failures especially as time goes on.

Yeah I agree, hopefully this shouldn't be an issue.

## Boog900 | 2026-01-20T19:13:12+00:00
Ah, so like receiving tx hashes from nodes that are ahead so we don't get the block notifications but also don't get the tx as it is now in the chain.

We probably should just ignore tx hashes on a per-node bases if the node claims to be too far ahead of our chain, this is what Cuprate does. Also in `handle_notify_tx_pool_hash` we should check if `is_synchronized` is false and ignoring the whole message anyway.

That should help, right?

## j-berman | 2026-01-20T20:16:41+00:00
> a better solution might be to not have a queue and to just ask the tx pool for txs to fluff after the timer fires

This sounds like a good idea to me for a future PR

# Action History
- Created by: 0xFFFC0000 | 2025-10-16T19:37:37+00:00
- Merged at: 2026-01-30T17:44:04+00:00
