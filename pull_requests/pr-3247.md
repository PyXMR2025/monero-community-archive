---
title: 'wallet2: speed up subaddress generation (by about a third)'
source_url: https://github.com/monero-project/monero/pull/3247
author: moneromooo-monero
assignees: []
labels: []
created_at: '2018-02-10T10:42:40+00:00'
updated_at: '2018-02-20T15:46:49+00:00'
type: pull_request
status: merged
closed_at: '2018-02-20T15:46:49+00:00'
merged_at: '2018-02-20T15:46:49+00:00'
---

# Original Description
Whether the speedup is worth it is left as an exercise for the reviewers :)

# Discussion History
## stoffu | 2018-02-12T23:17:06+00:00
The logic design seems to imply that this while-loop is never executed? Not a big deal, though.

## stoffu | 2018-02-12T23:17:17+00:00
The same comment as above.

## stoffu | 2018-02-12T23:26:37+00:00
Maybe add an assert for begin < end?

## moneromooo-monero | 2018-02-13T10:08:39+00:00
This is to match the if (m_subaddresses_inv.count(index2) == 0), which implied the labels and subaddresses could be unmatched (if an address has no label I guess). Admittedly only at start, but the overwrite of an existing should be a no-op. If you think this can't happen I'll remove it, it's just an optimization to avoid calculating already known data.

## stoffu | 2018-02-13T15:30:35+00:00
Ah, looks like the original scheme using `m_subaddresses_inv` was redundant and unnecessary altogether! I think we can safely remove it and eliminate this no-op checking, making the wallet cache smaller. Do you agree, and would you mind doing that here?

## moneromooo-monero | 2018-02-13T16:39:59+00:00
OK

## vtnerd | 2018-02-16T04:35:48+00:00
Why is this aliasing `cryptonote::get_subaddress_public_key` ? If that implementation is slower, can it be updated? Also, this should have always been in a library function outside of wallet 2 that takes the base key.

## stoffu | 2018-02-16T05:06:04+00:00
I find this part a little odd because `begin` is always 0 and not really used. How about this for the sake of clarity?
```c++
      const uint32_t begin = 0;
      const std::vector<crypto::public_key> pkeys = get_subaddress_spend_public_keys(index2.major, begin, end);
      for (index2.minor = begin; index2.minor < end; ++index2.minor)
``` 
Or we could just replace `begin` with 0.

Also, `const` could be added when appropriate.


## stoffu | 2018-02-16T05:06:39+00:00
This could be simply `pkeys[index2.minor]`.

## stoffu | 2018-02-16T05:13:35+00:00
Like above, I'd do something like

```c++
    const std::vector<crypto::public_key> pkeys = get_subaddress_spend_public_keys(index2.major, begin, end);
    for ( ; index2.minor < end; ++index2.minor)
```
Also, please use `const` when appropriate.


## stoffu | 2018-02-16T05:16:27+00:00
Hmm, isn't it possible to optimize even more, by not limiting the subaddress indices to be under the same major index?  I.e. the function signature would look like:

```c++
std::unordered_map<cryptonote::subaddress_index, crypto::public_key> wallet2::get_subaddress_spend_public_keys(const std::unordered_set<cryptonote::subaddress_index> &indices) const
```


## stoffu | 2018-02-16T05:26:12+00:00
The function `cryptonote::get_subaddress_public_key` doesn't exist, and also isn't relevant here because we want the `ge_p3` part, not `crypto::public_key`. But I agree, probably it's better to have a function like this under `libcryptonote_basic`:

```c++
std::unordered_map<cryptonote::subaddress_index, crypto::public_key> cryptonote::get_subaddress_spend_public_keys(const crypto::public_key &spend_key, const std::unordered_set<cryptonote::subaddress_index> &indices)
```

so that wallet2 doesn't need to deal with `ge_*`.

## moneromooo-monero | 2018-02-16T09:18:24+00:00
Yes, this is diminishing returns. "Unneeded" computation went down from 200*50 to 50. If you want to to a level above, you only gain not much, from 50 to 1. I don't think it's worth the bother, but if someone else does it I'd be OK with it.

## moneromooo-monero | 2018-02-16T09:20:11+00:00
Well, begin was not always 0 originally, since it was testing for already calculated data first. It can be changed now, since it always recalculates.

## stoffu | 2018-02-16T09:33:03+00:00
Ah, you're right. Not worth the bother.

## moneromooo-monero | 2018-02-16T09:44:36+00:00
The speedup is from shedding unnecessary conversions, which doesn't apply when calculating one at a time (unless you want to start keeping data in low level types in wallet2).

## stoffu | 2018-02-16T11:53:15+00:00
Maybe bikeshedding, but this would be even shorter: `for (index.minor = begin; index.minor < end; ++index.minor)`.

## stoffu | 2018-02-16T11:55:36+00:00
I think `index2.minor` isn't initialized yet here. We can just use 0 instead, no?

## vtnerd | 2018-02-17T07:24:12+00:00
https://github.com/monero-project/monero/blob/master/src/wallet/wallet2.cpp#L836

Sorry I just typed off the name quickly, it was in `wallet2::get_subaddress_spend_public_key`. This is doing the same thing, but hasn't been removed.

And the speedup came from unpacking the public key once, otherwise its going to be really close. Guess its time to finally introduce `crypto::fast_public_key` (mymonero has been using this specific speedup for wallet scanning for a bit).

What I did was create a new type in `crypto.h` that was equal to the size of `ge_pe3`. Then did a memcpy internally in `crypto.cpp`. I think it was a bit more reusable since it kept the crypto in one spot - unpacking a key once for multiple `scalarmult`s is useful in other contexts. Although in my situation it was doing from an arbitrary base, so it wasn't the identical function.

## vtnerd | 2018-02-17T07:27:37+00:00
Let me re-clarify a bit, if the function is still being used elsewhere (I think it is), you can definitely merge the two paths easily. Theres no reason to alias the remainder of the function, its just an unnecessary duplication.

# Action History
- Created by: moneromooo-monero | 2018-02-10T10:42:40+00:00
- Merged at: 2018-02-20T15:46:49+00:00
