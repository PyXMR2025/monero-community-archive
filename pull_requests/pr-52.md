---
title: 'wallet: complete hot-cold implementation for Carrot/FCMP++'
source_url: https://github.com/seraphis-migration/monero/pull/52
author: jeffro256
assignees: []
labels: []
created_at: '2025-06-01T04:03:14+00:00'
updated_at: '2026-07-01T07:41:44+00:00'
type: pull_request
status: open
closed_at: null
merged_at: null
---

# Original Description
# Carrot/FCMP++ hot-cold wallets

## Design goals

### Protocol goals

- [x] Compact exported Carrot outputs format
- [x] Compact signed Carrot/FCMP++ transaction format (SA/L's and key image associations only)
- [x] Deferred FCMP / BP+ proving to hot wallets at time of submission
- [x] Allows for "state-less" transaction proposals and signing
- [x] Allows for state-less, cold-initiated transaction proposals
- [x] Allows for hot-cold setups for Carrot-key-hierarchy wallets
- [ ] Allows hiding of sender-receiver secrets from hot wallet

### Wallet implementation goals

- [x] New hot wallets backwards compatible with old cold wallets until FCMP++ hard fork activation
- [x] New cold wallets backwards compatible with old hot wallets until FCMP++ hard fork activation
- [x] User experience for hot-cold setups on CLI and RPC is unchanged before and after FCMP++ hard fork activation
- [x] Wallet RPC interface completely backwards compatible until FCMP++ hard fork activation
- [x] Wallet RPC interface only differs in fetching of transaction secret keys after FCMP++ hard fork activation

## Testing

- [x] Importing/exporting outputs, subaddress and coinbase included, legacy key hierarchy
- [ ] Importing/exporting outputs, subaddress and coinbase included, Carrot key hierarchy
- [ ] Carrot-key-hierarchy account signing
- [x] State-less transaction proposals and signing
- [ ] Cold-initiated transaction proposals
  * [x] Unit test
  * [ ] `wallet2` integration tests

## New quirks

- Sender-receiver secrets for Carrot/FCMP++ transactions now need to fetched by the signable transaction hash, not TXID, from the cold wallet. The hot wallet RPC returns the signable transaction hash in the `submit_transfer` method. This is a breaking change.
- Because finalizing transaction proposals into pruned Carrot/FCMP++ transactions requires knowing the private view-incoming key, a hot wallet without a differing private view-incoming key will cryptographically not be able to submit a signed transaction set. This shouldn't be a breaking change since the signed transaction payloads are encrypted using the private view-incoming key anyways.

Depends: #74, #75, #76, #77, #97, #98, #214, #216, #296, #404

Will break this PR up into smaller pieces once the above are merged.

# Discussion History
## ComputeryPony | 2025-07-23T07:44:55+00:00
OUTPUT_EXPORT_FILE_MAGIC

## jeffro256 | 2025-07-23T15:11:10+00:00
Thank you

## j-berman | 2026-06-12T18:58:03+00:00
Why get rid of wipeable string here?

## j-berman | 2026-06-12T22:44:41+00:00
Accidental indents here looks like, see: https://github.com/monero-project/monero/blob/9e817b608b51b138273824afcde2a442b0764359/src/wallet/wallet2.cpp#L7845-L7847



## j-berman | 2026-06-13T01:29:03+00:00
nit: I suggest checking the type here just for sanity

## j-berman | 2026-06-13T02:06:20+00:00
The `validate_key_image_proof` callers know the type of output being proven for. How about just passing in `use_biased_hash_to_point` as a bool?

## j-berman | 2026-06-13T02:16:07+00:00
Could do something like `use_biased_hash_to_point(td.get_output_pair())` here e.g. (not great it would re-calculate the commitment, but avoids yet another helper and the double validation downstream)

## j-berman | 2026-06-13T02:25:00+00:00
I like the decision to reuse serialization logic with dedicated structs here 

## j-berman | 2026-06-13T02:33:17+00:00
nit: could use a wipeable string here and won't need to memwipe

## j-berman | 2026-06-13T02:37:04+00:00
nit suggestion: `encrypt_key_image_proofs` instead of `encrypt_key_images`, and `key_image_proofs` instead of `key_images`? Same for decrypt

## j-berman | 2026-06-13T02:48:24+00:00
Doesn't this mean a new cold wallet won't be able to import outputs from an old hot wallet before the fork, since the old version was 4?

## j-berman | 2026-06-16T02:51:25+00:00
Why's this brand new type using a version > 0 or 1?

Can you also add a comment explaining the purpose of `PASSTHROUGH_VERSION` and how it should be used, maybe in the macro definition of `PASSTHROUGH_VERSION` itself?

## j-berman | 2026-06-17T00:07:47+00:00
nit: numbering ends up off from here on (repeated 8)

## j-berman | 2026-06-17T00:34:24+00:00
I think this function should be private and only callable from internally. Only exported outputs should be passed in from outside. 

Note: this function was originally for importing an old format while there were some v17 wallets still exporting it, and [we exported the new format with all v18 wallets](https://github.com/monero-project/monero/blob/v0.18.0.0/src/wallet/wallet2.h#L1450) ([ref PR](https://github.com/monero-project/monero/pull/8179)). I see this PR is bringing it back by "expanding cold outputs" to be imported here.

## j-berman | 2026-06-17T00:56:55+00:00
I think this function should be private (and note that it's currently only called internally from `sign_tx`)

## j-berman | 2026-06-17T01:31:35+00:00
I think this function should either just re-call `import_outputs(exported_transfer_details_variant)` or expand every output with `wallet::cold::import_cold_output` and call `import_outputs(transfer_details)`

Otherwise we'll have unnecessary duplicated logic 

## j-berman | 2026-06-17T01:36:49+00:00
With all the new logic for expanding the cold outputs via `import_cold_output`, I think we should instead throw here if the key image isn't known and delete everything inside the if / delete everything that `import_cold_output` is already doing. The `td` technically can be const.

## j-berman | 2026-06-17T01:37:09+00:00
Only this `import_outputs` function should be public imo

## jeffro256 | 2026-06-17T15:05:19+00:00
FWIW `boost::get()` will throw `boost::bad_get` if the contained type is wrong. 

## jeffro256 | 2026-06-17T15:07:16+00:00
This is fair since the key image proof has to be updated anyways. 

## jeffro256 | 2026-06-17T16:02:46+00:00
Yes, I don't remember why this is like that though. I'll try to update the code to support v4 and let you know how it goes.

## jeffro256 | 2026-06-17T17:21:54+00:00
I'll go with storing that `bool` inside the `KeyImageProofVariant`

## jeffro256 | 2026-06-17T18:31:19+00:00
I did the latter in latest 

## jeffro256 | 2026-06-17T18:32:01+00:00
Latest should work?

## jeffro256 | 2026-06-17T18:32:27+00:00
Yeah looks like auto indent after copy and pasting

## jeffro256 | 2026-06-17T18:32:37+00:00
Fixed

## j-berman | 2026-06-18T00:21:21+00:00
nit: why store the extra byte on the proof here for something that is expected known at runtime? It's not also storing the onetime address on the proof, seems totally sane to determine the bool at runtime

## j-berman | 2026-06-18T00:55:30+00:00
I think we probably will want another helper to get output type from `td` anyway so may as well implement/use it here too (see [this comment](https://github.com/seraphis-migration/monero/pull/52#pullrequestreview-4520704975))

## j-berman | 2026-06-19T01:21:28+00:00
It looks like there's an inconsistency inside `make_sal_opening_hint_from_transfer_details` for legacy.

[This section](https://github.com/jeffro256/monero/blob/924338ac383ffa831d410ef8364a72508268276a/src/wallet/tx_builder.cpp#L863-L870) uses the additional tx pub key if it's there and the output is received to a subaddress. However, [the normal scan flow](https://github.com/jeffro256/monero/blob/924338ac383ffa831d410ef8364a72508268276a/src/wallet/scanning_tools.cpp#L136-L173) detects receives using **either** tx pub key **or** additional, checking the tx pub key first. So someone could theoretically receive to a subaddress but using the tx pub key for derivation, and `make_sal_opening_hint_from_transfer_details` would still use the additional tx pub key.

## jeffro256 | 2026-06-21T21:57:33+00:00
To differentiate it from `UnsignedPreCarrotTransactionSet` during de-serialization, similar to `VARIANT_TAG`. Its version is 3, 1 more than `UnsignedPreCarrotTransactionSet`. I will document this better

## jeffro256 | 2026-06-22T15:43:41+00:00
The behavior in `make_sal_opening_hint_from_transfer_details()` matches the legacy transaction construction behavior: https://github.com/monero-project/monero/blob/2c48374ecd2449c02bb400e5bcf20b7c6f11649b/src/device/device_default.cpp#L325-L326. I should either modify the scanning code to reject non-standard derivation paths, or `make_sal_opening_hint_from_transfer_details()` will require a view key device. 

## jeffro256 | 2026-06-22T16:35:48+00:00
> nit: why store the extra byte on the proof here for something that is expected known at runtime? It's not also storing the onetime address on the proof, seems totally sane to determine the bool at runtime

Done in latest

## j-berman | 2026-06-22T20:38:49+00:00
Right it's only in the event someone makes a tx using something other than that exact flow that this would surface as a potential issue. The problem is a sender might have done this already, and an honest recipient's wallet2 would have detected it / been able to spend it, and so I think we need to account for that.

Couple more options to consider:
- Restructure the flow to not need to do crypto scanning ops on the opening hint?
- Store an optional `additional_ephemeral_tx_pubkey` on the legacy opening hint as well, then downstream crypto scanning ops on legacy opening hints could use it matching it to the normal scan flow behavior.

## j-berman | 2026-06-23T23:56:34+00:00
Checking my understanding here:

- `SignedFullTransactionSet` can be legacy or Carrot.
  - Legacy: cold wallets construct this when signing.
  - Carrot: hot wallets construct this when importing a signed tx from the cold wallet.
- `SignedCarrotTransactionSetV1`
  - Carrot cold wallets construct this when signing a tx.

And this approach is a result of FCMP++ separating the membership proof and range proof from the signable tx hash.

Looks good to me.

## j-berman | 2026-06-24T01:58:41+00:00
nit: This seems like it would only be reachable potentially for full wallets (since hot wallets set the tx key on pending txs to identity in `finalize_signed_carrot_tx_set_v1_into_full_set`). Do you have a use case in mind for this section?

## j-berman | 2026-06-24T03:09:09+00:00
This doesn't look like something we want to keep as a TODO

## j-berman | 2026-06-24T19:09:46+00:00
Nor this

## j-berman | 2026-06-24T23:17:04+00:00
Context: the cold wallet calls this function to import an unsigned tx. Then after calling this, the cold wallet signs the tx.

For the Carrot flow, this function appears to be doing everything that `sign_carrot_tx_set_v1` then does again after this. Specifically the call to `expand_carrot_transaction_proposal` is duplicated, which seems unnecessary.

## j-berman | 2026-06-25T00:35:56+00:00
Checking my understanding: you "compress" to strictly data the cold wallet needs in order to construct the tx. The "random" fields are re-derived using the `hot_cold_seed`, so those fields don't need to be exported to the cold wallet. So you strip those fields from the proposal, hence "lossy."

I'm finding it a little confusing/hard to see what fields from the tx proposal that the hot wallet really didn't even need in the first place at this stage. Maybe it would be nice if the hot wallet was passing around a type that already didn't have the extraneous types to reduce cognitive load here/room for error, but I get how that might be a significant restructuring.

## j-berman | 2026-06-26T22:19:56+00:00
So in the CLI, a cold wallet importing an unsigned tx set expands the Carrot tx proposals here when importing the unsigned tx set (`accept_loaded_tx` calls `get_transaction_proposals_from_unsigned_tx_set`).

And then it does it again when signing the unsigned tx set (`sign_carrot_tx_set_v1` calls `expand_carrot_transaction_proposal_and_rerandomized_outputs`).

It seems like a major duplication of logic that also adds a lotta surface to the code. Any room here to avoid that duplication/only expand once?

## j-berman | 2026-06-26T22:41:26+00:00
I think makes sense to lock `m_refresh_mutex` at the top of `parse_tx_from_str` to avoid this issue https://github.com/seraphis-migration/monero/issues/164#issuecomment-3408508192

## j-berman | 2026-06-26T23:07:49+00:00
I'm wondering if cold wallets not being able to query for keys using tx id might lead to some disrupted flows. I think this is fine as is, but maybe we'll want signable tx hashes to be a more prominently featured item in the future (displayed in wallets / block explorer / queryable from the wallet) to work around this later on

## j-berman | 2026-06-26T23:17:18+00:00
nit: this could be renamed to `load_tx_from_str` since it's not just parsing anymore for Carrot, or `prepare_tx_from_signed` or something like that

## jeffro256 | 2026-06-27T05:25:36+00:00
Yes, exactly. The reason why Carrot txs are converted into `SignedFullTransactionSet` is mainly to reduce code duplication with legacy code until the `walet2::commit_tx()` path. 

## jeffro256 | 2026-06-27T05:53:06+00:00
This unifies behavior between full and cold wallets: tx keys from both can be fetched using the signable tx hash. So upstream users can switch to only using signable tx hash to fetch tx keys and it'll work for everything. 

## jeffro256 | 2026-07-01T07:36:23+00:00
One of the core design decisions of `CarrotTransactionProposalV1` is that it contains *everything* needed to reconstruct a signable transaction hash, which is what the SA/L proof binds to. This means that e.g. HW devices talking to hot devices can "talk" to each other with a cold signing protocol, and come to a shared 100% exact same description of all parts of a tx that the signature signs for. `HotColdCarrotTransactionProposalV1` does *not* adhere to this. For one, it doesn't have full opening hints like `CarrotTransactionProposalV1` has, rather, it has one-time addresses references to some inferred set of opening hints.  

## jeffro256 | 2026-07-01T07:38:20+00:00
Also, the fact that a view-key device is required to view output information is not ideal for some the call sites elsewhere. In other words, I think that trying to use `HotColdCarrotTransactionProposalV1` everywhere would shift a lot of the mess somewhere else. I like that it is compartamentalized here. 

## jeffro256 | 2026-07-01T07:38:34+00:00
Fixed in latest

## jeffro256 | 2026-07-01T07:38:41+00:00
Fixed in latest

## jeffro256 | 2026-07-01T07:41:44+00:00
Yeah it probably will. It's the one breaking API change AFAIK in this whole chageset. At least one the live side, if you have the TXID, you can lookup its signable tx hash... 

# Action History
- Created by: jeffro256 | 2025-06-01T04:03:14+00:00
