---
title: 'database: Resizes, Shutdown, Flushing'
source_url: https://github.com/Cuprate/cuprate/pull/68
author: hinto-janai
assignees: []
labels:
- A-storage
created_at: '2024-02-18T18:02:03+00:00'
updated_at: '2024-05-27T01:08:40+00:00'
type: pull_request
status: merged
closed_at: '2024-02-25T19:46:36+00:00'
merged_at: '2024-02-25T19:46:36+00:00'
---

# Original Description
## Part 5
https://github.com/Cuprate/cuprate/pull/62 <- Previous Next -> https://github.com/Cuprate/cuprate/pull/77

This PR implements:
- Memory map resizes https://github.com/Cuprate/cuprate/pull/62#discussion_r1492464175
- DB thread-pool shutdown signals/logic https://github.com/Cuprate/cuprate/pull/62#discussion_r1492598811
- `Config` for various runtime `Env` configuration https://github.com/Cuprate/cuprate/pull/62#discussion_r1493038378

# Discussion History
## Boog900 | 2024-02-24T15:42:54+00:00
Would something like this work?


```suggestion
impl<T: Into<usize>> From<T> for ReaderThreads {
    fn from(value: T) -> Self {
        match NonZeroUsize::new(value.into()) {
            Some(n) => Self::Number(n),
            None => Self::One,
        }
    }
}
```

## Boog900 | 2024-02-24T15:45:49+00:00
This function does not rely on the caller doing this, it handles this it self right?

## Boog900 | 2024-02-24T15:48:10+00:00
The database will only shutdown when the Env is dropped so I don't think this is an issue.

## Boog900 | 2024-02-24T15:48:59+00:00
Is there another example of why cuprate_database wont work without the service?

## Boog900 | 2024-02-24T15:58:15+00:00
I feel the wording on this should talk more about how these handles must be dropped eventually so can't be leaked rather than talking about how these handles should not be used after being freed:

```suggestion
// INVARIANT:
// `cuprate_database` depends on the fact that this is the only
// function that hands out the handles. After that, they can be
// cloned, however they must eventually be dropped and shouldn't
// be leaked.
//
// As the reader thread-pool and writer thread both rely on the
// disconnection (drop) of these channels for shutdown behavior,
// leaking these handles could cause data to not get flushed to disk.
```

## Boog900 | 2024-02-24T15:59:23+00:00
```suggestion
//! Upon dropping the [`crate::ConcreteEnv`]:
```

## Boog900 | 2024-02-24T16:05:05+00:00
this is LMDB's file name, I think we should just specify the dir and allow backends to  set file names 

## Boog900 | 2024-02-24T16:10:17+00:00
you could do `TryInto` instead of `Into` and just panic

## hinto-janai | 2024-02-25T13:52:03+00:00
`u16` is the highest unsigned int that can `Into<usize>`, so `ReaderThreads::from(1_u32)` won't work.

The reasoning behind casting is mostly for convenience since `ReaderThreads` must always be at least 1, the behavior for any input <1 is that it gets rounded up to 1.

Although, thinking about it the only input is going to be a `usize` from the user config? Going with `Into<usize>`. 

## hinto-janai | 2024-02-25T13:54:33+00:00
Right, the backend impl handles it, will remove this doc.

## hinto-janai | 2024-02-25T14:05:25+00:00
See `backend/heed/error.rs`. A lot of errors have knowledge of the code in `service`, e.g. `DbsFull` will just panic because _we_ know how many we use so that error should be `unreachable!()` but...

If someone uses `cuprate_database` with no relation to Monero, they could open more sub-DBs than available, in which case they would want that error to return, but instead it will just panic.

So we could add `RuntimeError::DbsFull` even though it will never happen in `service`.

## hinto-janai | 2024-02-25T14:13:31+00:00
Right, it needs to be `cfg_if`'ed based on the backend.

We need the direct PATH to the file because of `Env::disk_size_bytes()`. Do we need this though? Or access to the file in any future function?

## Boog900 | 2024-02-25T14:41:55+00:00
The Env config will hold the file name to the DB, so users would just specify the dir and the backend would add the file name and expose a function `path_to_file` or something, I can't see any part of Cuprate needing the file except for the DB threads.

So this constant should be in the back-end not helper.


## Boog900 | 2024-02-25T14:54:02+00:00
I am ok with sealing tables to prevent people from creating their own.

I don't think the fact we panic on certain errors makes cuprate_database unusable without the service, just that users will need to put more care into how they use it e.g. limit number of active txs.

This should probably be explained somewhere though.



## Boog900 | 2024-02-25T15:00:26+00:00
for docs you can do: `[ConcreteEnv](crate::ConcreteEnv)` instead of having to do this

## hinto-janai | 2024-02-25T17:00:49+00:00
Actually how do feel about doing:
```rust
None => Self::OnePerThread
```
It would match `monerod` and most programs behavior of `0 == maximum value`.

## hinto-janai | 2024-02-25T17:10:37+00:00
Ok, it's now defined in `database/` depending on the backend, and accessible via:
1. `Env::config(&self) -> &Config`
2. `Config::db_file_path(&self) -> &Path`

## hinto-janai | 2024-02-25T17:29:16+00:00
`Table` is now `Sealed` and only implementable on `cuprate_database::tables`.

Added some incomplete docs in `lib.rs` for the non-`service` usage.

## Boog900 | 2024-02-25T19:46:23+00:00
Yeah I think that would make sense, I am going to merge this as is, this can be added to a followup PR

# Action History
- Created by: hinto-janai | 2024-02-18T18:02:03+00:00
- Merged at: 2024-02-25T19:46:36+00:00
