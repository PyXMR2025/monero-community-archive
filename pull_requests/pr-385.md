---
title: 'txpool: don''t re-relay invalid FCMP++ txs'
source_url: https://github.com/seraphis-migration/monero/pull/385
author: j-berman
assignees: []
labels: []
created_at: '2026-05-13T22:56:23+00:00'
updated_at: '2026-05-20T17:50:28+00:00'
type: pull_request
status: open
closed_at: null
merged_at: null
---

# Original Description
After a deep reorg, txs can be rendered invalid (if >10 blocks, FCMP++ tx tree roots can change). A node with a now-invalid tx in its pool will still re-relay that now-invalid tx to its peers in a loop via `get_relayable_transactions`. The node's peers will then drop the node for receiving an invalid tx. Noted in #372 as a cause for poor node connectivity after a deep reorg.

This PR addresses this by checking if the tx about to be re-relayed has been rendered invalid, using the `valid_input_verification_id` stored on the tx. If invalid, we don't re-relay.

This PR repurposes `meta.max_used_block_height` for FCMP++, setting it to the `reference_block` used to construct the FCMP++ tx (expected to be the tip of the chain at tx construction time).

# Discussion History
## j-berman | 2026-05-15T04:51:47+00:00
I don't like using `max_used_block_height` here because `check_tx_inputs` can fail before it gets set (e.g. if the key images are spent in the chain).

I was debating just using `tx.rct_signatures.p.reference_block` here, which should be 0 for non FCMP++ txs.

My primary hangup is if the tx is pruned. But I can't see how `check_tx_inputs` could even be called with a pruned tx, and even if it is pruned, then it *should* still be 0 and will be handled fine?

## j-berman | 2026-05-15T16:44:19+00:00
See the latest. I used `tx.rct_signatures.p.reference_block` to avoid the problem with `max_used_block_height` noted above.

# Action History
- Created by: j-berman | 2026-05-13T22:56:23+00:00
