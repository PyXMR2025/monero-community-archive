---
title: Fast sync part 2
source_url: https://github.com/Cuprate/cuprate/pull/156
author: jomuel
assignees: []
labels:
- A-dependency
- A-workspace
- A-consensus
created_at: '2024-06-09T13:58:29+00:00'
updated_at: '2024-06-14T22:00:18+00:00'
type: pull_request
status: merged
closed_at: '2024-06-14T22:00:18+00:00'
merged_at: '2024-06-14T22:00:18+00:00'
---

# Original Description
Implement the still missing `ValidateBlock` request in `consensus/fast-sync`.
Follow up  to #155. 
Implements #153.
Collaborative work with @dllud.

# Discussion History
## Boog900 | 2024-06-13T15:01:10+00:00
We could sum the outputs here instead of calling `check_miner_tx`:
```suggestion
    let total_fees = txs.iter().map(|tx| tx.fee).sum::<u64>();
    let total_outputs = block
        .miner_tx
        .prefix
        .outputs
        .iter()
        .map(|output| output.amount.unwrap_or(0))
        .sum::<u64>();

    let generated_coins = total_outputs - total_fees;

    let weight = block.miner_tx.weight() + txs.iter().map(|tx| tx.tx_weight).sum::<usize>();
```

## Boog900 | 2024-06-13T15:06:28+00:00
This needs to be pulling them in the order they are in the block:

```suggestion
    let mut verified_txs = Vec::with_capacity(txs.len());
    for tx in &block.txs {
        let tx = txs
            .remove(tx)
            .ok_or(FastSyncError::TxsIncludedWithBlockIncorrect)?;

        let data = TransactionVerificationData::new(tx)?;
        verified_txs.push(VerifiedTransactionInformation {
            tx_blob: data.tx_blob,
            tx_weight: data.tx_weight,
            fee: data.fee,
            tx_hash: data.tx_hash,
            tx: data.tx,
        });
    }
```
We will need to add a new new error variant and make `txs` `mut`

## Boog900 | 2024-06-13T15:08:31+00:00
I think I would rather put `#[allow(clippy::large_enum_variant)]` on this enum than block the block 

## Boog900 | 2024-06-13T15:09:19+00:00
same here 

## Boog900 | 2024-06-13T15:10:34+00:00
Could you add a check that this `height` is equal to the chain height in `block_chain_ctx`

## Boog900 | 2024-06-14T21:33:22+00:00
```suggestion
        block,
```

## Boog900 | 2024-06-14T21:38:55+00:00
```suggestion
        block_blob,
        txs: verified_txs,
        block_hash,
        pow_hash: [0u8; 32],
        height: *height,
        generated_coins,
        weight,
        long_term_weight: block_chain_ctx.next_block_long_term_weight(weight),
        cumulative_difficulty: block_chain_ctx.cumulative_difficulty
            + block_chain_ctx.next_difficulty,
        block,
```

# Action History
- Created by: jomuel | 2024-06-09T13:58:29+00:00
- Merged at: 2024-06-14T22:00:18+00:00
