---
title: 'blockchain_db: add optional k-anonymity to txid fetching'
source_url: https://github.com/monero-project/monero/pull/8958
author: jeffro256
assignees: []
labels: []
created_at: '2023-07-19T19:17:17+00:00'
updated_at: '2023-10-26T01:34:23+00:00'
type: pull_request
status: merged
closed_at: '2023-10-26T01:34:22+00:00'
merged_at: '2023-10-26T01:34:22+00:00'
---

# Original Description
Read more about k-anonymity [here](https://en.wikipedia.org/wiki/K-anonymity). We implement this feature in the monero daemon for transactions by providing a "Txid Template", which is simply a txid with all but `num_matching_bits` bits zeroed out, and the number `num_matching_bits`. We add an operation to `BlockchainLMDB` called `get_txids_loose` which takes a txid template and returns all txids in the database (chain and mempool) that satisfy that template. Thus, a client can ask about a specific transaction from a daemon without revealing the exact transaction they are inquiring about. The client can control the statistical chance that other TXIDs (besides the one in question) match the txid template sent to the daemon up to a power of 2. For example, if a client sets their `num_matching_bits` to 5, then statistically any txid has a 1/(2^5) chance to match. With `num_matching_bits`=10, there is a 1/(2^10) chance, so on and so forth.

# Discussion History
## vtnerd | 2023-07-19T21:23:42+00:00
Does it compare against the first or last bits? Presumably the leading bits, because `GET_BOTH_RANGE` would fail.

## vtnerd | 2023-08-01T14:19:05+00:00
`static_assert` that `sizeof(uint64_t) * 4 == sizeof(crypto::hash)`. I doubt we'll ever change this, but its a nice catch regardless.

## vtnerd | 2023-08-01T14:23:44+00:00
`static_assert` here too.

## vtnerd | 2023-08-01T14:26:36+00:00
For future reference, `std::uint64_t(1)` does the same thing as the `static_cast` but with fewer characters.

## vtnerd | 2023-08-01T14:30:37+00:00
This is technically an aliasing violation. It likely works nearly all the time, but optimizations could theoretical blow this up. I know we do this in a few places, but its suboptimal.

Instead, use a `std::array<std::uint64_t, 4>` with a memcpy from `h` into this array, then a `memcpy` back out for the return address.

## vtnerd | 2023-08-01T14:34:13+00:00
I don't see any direct tests for the compare function.

## vtnerd | 2023-08-01T14:39:35+00:00
Which part is not big endian safe? I saw several swaps that should work .. no? I didn't inspect this part too closely so maybe I missed something.

## jeffro256 | 2023-08-01T15:06:49+00:00
It compares the the bytes in reverse order, and the MSB -> LSB in each byte. It creates an order equal to the LMDB ordering of casting the hash to an array `uint32_t`s and comparing those 8 integers in reverse order. 

## jeffro256 | 2023-08-01T15:08:35+00:00
I actually don't think this is an aliasing violation because `crypto::hash::data` is a `char` array, and `char` pointers are an exception to the strict aliasing rule, but I could be wrong. 

## jeffro256 | 2023-08-01T15:13:41+00:00
The code that I wrote is big-endian safe, but the way that LMDB orders the keys is not: https://github.com/monero-project/monero/blob/00fd416a99686f0956361d1cd0337fe56e58d4a7/src/blockchain_db/lmdb/db_lmdb.cpp#L148-L160 In other words, the behavior of `cryptonote::make_hash32_template` is different from `BlockchainLMDB::compare_hash32` on big-endian systems so the templates will just return garbage (likely nothing). 

## vtnerd | 2023-08-01T17:01:15+00:00
Casting **to** char is always allowed, but casting **from** char has restrictions.

## jeffro256 | 2023-08-01T19:15:07+00:00
How does the new commit look in terms of aliasing?

## vtnerd | 2023-08-01T19:44:39+00:00
You need a `memcpy` here too if abiding by the strict aliasing rules.

## vtnerd | 2023-08-01T19:45:02+00:00
A memcpy is needed here as well (missed this earlier).

## jeffro256 | 2023-08-01T22:26:33+00:00
Yeah you're right... It just feels so wrong to copy these buffers even though it's the exact same thing in memory. 

## jeffro256 | 2023-08-01T22:26:46+00:00
Hopefully the compiler can optimize it away 

# Action History
- Created by: jeffro256 | 2023-07-19T19:17:17+00:00
- Merged at: 2023-10-26T01:34:22+00:00
