---
title: 'wallet: add pending_tx sanity checker'
source_url: https://github.com/monero-project/monero/pull/11076
author: UkoeHB
assignees: []
labels:
- pending review
- wallet
- needs backport
created_at: '2026-08-10T04:44:23+00:00'
updated_at: '2026-09-01T08:17:24+00:00'
type: pull_request
status: open
closed_at: null
merged_at: null
---

# Original Description
`pending_tx` has a lot of internal duplication and repeats info from the wallet.

@jeffro256 I put this in `tx_builder.*` since it seems appropriate, but feel free to suggest an alternative with less merge conflicts.

# Discussion History
## jeffro256 | 2026-08-11T16:49:08+00:00
Yeah one of the main reasons I split the translation units is so that they *don't* depend on `wallet2.h`. If we move `pending_tx` here, then we can remove this include

## UkoeHB | 2026-08-11T18:33:34+00:00
Ideally this PR goes in the release and we can do the refactor later.

## UkoeHB | 2026-08-19T00:36:48+00:00
Moved into `parse_multisig_tx_from_str`

## j-berman | 2026-08-21T16:38:34+00:00
These need to only run for tx version > 1

## j-berman | 2026-08-21T16:39:09+00:00
`tx.rct_signatures.txnFee` is tx v2, but v1 txs are still possible (the sweep unmixable case). v1 tx fee is calculated as `get_inputs_money_amount(tx) - get_outs_money_amount(tx)`, see: https://github.com/monero-project/monero/blob/0aa9c51bd8eb53abb0a7023b5c768d9b46ee18a0/src/cryptonote_core/blockchain.cpp#L1688-L1693 

## UkoeHB | 2026-08-21T19:40:41+00:00
Are pre-rct input amounts recorded explicitly in txs? Right now we have:
```cpp
        CHECK_AND_ASSERT_THROW_MES(src.rct
            ? ext_input_amount == 0
            : ext_input_amount == src.amount,
            "sanity_check_pending_tx: amount extracted from inputs is wrong");
```
But maybe the check should be `ptx.tx.version > 1` instead of `src.rct`.

## j-berman | 2026-08-21T19:58:43+00:00
Yep, you're right. Good spot

## j-berman | 2026-08-21T20:08:11+00:00
Actually spoke too soon. Yes pre-RCT input amounts are recorded explicitly in txs and that original logic was ok. Here's an example tx spending pre-RCT inputs -> RCT outputs: https://xmrchain.net/tx/c22dddbc73076825b163f048122f759cfd983fff9a4bcabefe9f342e7d12017f/1 (EDIT: linked the wrong tx)

It's fine this way too though.


## j-berman | 2026-08-21T20:10:34+00:00
Err, no. New change is actually incorrect because of that. It does need to be:

```
        CHECK_AND_ASSERT_THROW_MES(src.rct
            ? ext_input_amount == 0
            : ext_input_amount == src.amount,
            "sanity_check_pending_tx: amount extracted from inputs is wrong");
```

## j-berman | 2026-08-21T20:14:05+00:00
needs to be reverted to `src.rct`

## UkoeHB | 2026-08-21T22:11:20+00:00
Wouldn't that contradict `collect_transparent_amount_commitments_static` which says transparent amounts are only v1 or coinbase?

## j-berman | 2026-08-21T22:14:16+00:00
That's collecting from the output side, this is the input side

## thomasbuilds | 2026-08-26T06:42:21+00:00
This rejects every sweep_unmixable tx. That path still builds v1 (fake_outs_count = 0, wallet2.cpp:11203) via transfer_selected, which digit-splits dests and adds no change entry when change is 0. Reproduced on this branch with a genuine v1 tx: one dest fails here with or without the split; a two dest v1 tx without the split passes.

## UkoeHB | 2026-08-26T18:19:11+00:00
In that scenario would `splitted_dests.size() == 1`? Or is it adding a dummy output somewhere? If so, where?

## thomasbuilds | 2026-08-26T19:17:11+00:00
There's no dummy output on the v1 path, that's only in `transfer_selected_rct` (9976-9989). `transfer_selected` digit-splits the destination (wallet2.h:1780) and only adds change if `needed_money < found_money` (9802-9806) so for a sweep `splitted_dsts.size()` is the number of nonzero decimal digits of the amount, e.g. 4 for 1.234 XMR. Line 526 assumes the rct rule and expects 2. If the amount happens to have exactly two digits the size check passes and 639-660 catches it instead.

## UkoeHB | 2026-08-26T19:45:27+00:00
Ok for v1 looks like we need to iter splitted dests and accumulate them, then match amounts/keys with dests + change_dest (which also need to be accumulated in case of duplicates). And don't check splitted_dest_repro.

## UkoeHB | 2026-08-28T16:08:52+00:00
@thomasbuilds does latest commit fix your test case?

## thomasbuilds | 2026-08-28T17:20:46+00:00
```suggestion
            auto &amnt = res.first->second;
```

## thomasbuilds | 2026-08-28T17:21:19+00:00
```suggestion
            auto res = acc_dest.try_emplace(dest.addr, 0);
            auto &amnt = res.first->second;
```

## thomasbuilds | 2026-08-28T17:21:35+00:00
```suggestion
            CHECK_AND_ASSERT_THROW_MES(split.second == dest_res->second,
```

## thomasbuilds | 2026-08-28T17:32:18+00:00
No fails with `v1 dest inconsistency (size)`. Check out my suggestions please, which make my test case pass, and the tampered control is still rejected.

## UkoeHB | 2026-08-28T20:31:40+00:00
Thanks, fixed in latest

## thomasbuilds | 2026-08-29T06:12:03+00:00
```suggestion
        const rct::key commitment = rct::commit(src.amount, src.rct ? src.mask : rct::identity());
```

seems that `transfer_selected` never sets `src.mask` for v1 (only `transfer_selected_rct` does, 9961) so it stays zero here and a real sweep is still rejected, including through `parse_tx_from_str`. Scanned pre-RCT outputs always have `m_mask = identity` (wallet2.cpp:2616).

## UkoeHB | 2026-08-29T23:43:03+00:00
Fixed in latest

## thomasbuilds | 2026-08-31T15:01:40+00:00
This used to return false and now throws and the checker also requires `!m_spent && !m_frozen`, so read-only loads of an already-submitted or frozen-input txset (`describe_transfer`, `export_raw_multisig`, re-load) now fail. Keeping the bool contract also fixes the unguarded RPC callers (see review body):

```suggestion
    try
    {
      this->sanity_check_pending_tx(ptx, false, false, std::nullopt);
    }
    catch (const std::exception &e)
    {
      LOG_PRINT_L0("Multisig tx failed sanity check: " << e.what());
      return false;
    }
```

## thomasbuilds | 2026-08-31T15:01:40+00:00
`get_multisig_k` passing only proves `export_multisig` ran (it reads `m_multisig_k`, populated there). `m_key_image_known` is only set by multisig import (`update_multisig_rescan_info`); scan sets it false for multisig (2578). So a cosigner who exported but whose last import doesn't cover these outputs could sign before and now throws `KI is expected but unknown`. multisig.py always imports before signing, so CI won't catch it. If requiring import to sign is intended, the error should say to run `import_multisig_info`; otherwise:

```suggestion
    for (const size_t idx : sd.selected_transfers)
      expect_imported_key_images = expect_imported_key_images && idx < m_transfers.size() && m_transfers[idx].m_key_image_known;
    this->sanity_check_pending_tx(ptx, false, expect_imported_key_images, std::nullopt);
```

## thomasbuilds | 2026-08-31T15:01:40+00:00
Unconditional, but pre-HF15 txs have `txout_to_key` outs, so this rejects valid txsets on chains below HF15 and `\005` files saved before HF15 (format exists since 2020-06 and is still the only accepted version). If dropping those is intended, better make it explicit; this also closes the uninitialized `repro_view_tag` read (other comment) since `use_view_tags` is a file-controlled flag:

```suggestion
    CHECK_AND_ASSERT_THROW_MES(all_are_txout_to_tagged_key,
        "sanity_check_pending_tx: all outputs should have view tags");
    CHECK_AND_ASSERT_THROW_MES(construct.use_view_tags,
        "sanity_check_pending_tx: only view-tag (>= HF15) pending txs can be validated");
```

## thomasbuilds | 2026-08-31T15:01:40+00:00
Stays uninitialized when `construct.use_view_tags` is false (the device only derives the tag under that flag) while line 149 compares it unconditionally, so tagged vouts with the serialized flag cleared compare an indeterminate byte:

```suggestion
        crypto::view_tag repro_view_tag{};
```

## thomasbuilds | 2026-08-31T15:01:40+00:00
`integrated_count` and all payment id checks only run when `original` is non-empty, so `is_integrated = true` with empty `original` skips them and an encrypted payment id can be swapped undetected. All producers set `original` for integrated dests, so requiring it is safe:

```suggestion
        CHECK_AND_ASSERT_THROW_MES(!dest.is_integrated || dest.original.size() > 0,
            "sanity_check_pending_tx: integrated destination missing original address string");

        if (dest.original.size() > 0)
```

An `original` that fails both URI and address parsing still skips the pid binding; maybe also require it to parse when `is_integrated`.

## thomasbuilds | 2026-08-31T15:01:40+00:00
Cannot diverge from the check at 8107: `import_key_images` copies `signed_txs.key_images` verbatim (returns false on oversize, early return above) and `kis.at(i)` already bounds every selected transfer, so this re-checks identical values, and after state was already mutated:

```suggestion
```

## UkoeHB | 2026-08-31T22:34:26+00:00
Lots of good suggestions but this one is bad. It relies on incidental logic instead of local logic which is stronger and more maintainable/robust. Note that this is a *sanity checker* which means it is allowed to check things that are redundant or trivial.

## UkoeHB | 2026-08-31T23:12:11+00:00
Added multisig import enforcement.

## thomasbuilds | 2026-09-01T05:44:52+00:00
Breaks Trezor via the api: the host has no spend key and `compute_key_image` is inactive during construction, so every input of the host template carries the identity key image and the duplicate key image check throws for any tx with 2+ inputs. The check at the end of `cold_sign_tx` already covers the device-signed ptx.

```suggestion
```

## thomasbuilds | 2026-09-01T05:44:52+00:00
This runs before `init(reconstruction = true)`, which rebuilds `ptx.tx` from construction data (`vin` from `multisig_kLRki.ki`, `extra` from `construction_data.extra`, neither checked), so it validates a tx that is then discarded and guarantee A does not hold for the signed one (B depends on it). The check has to run again once `init` returned, as 2a4ac7477 did after the sig loop.

## UkoeHB | 2026-09-01T06:49:42+00:00
Oh sure, let's overload `pending_tx` with every damn spaghetti bs we can think of. Sigh... thanks for pointing these out.

## thomasbuilds | 2026-09-01T08:12:38+00:00
The gate runs per tx after `get_multisig_k` already wiped the earlier txs' nonces, so with an unsynced input in a later tx of a multi-tx set the retry after import fails with `multisig_export_needed`. Run it over all txs first, the in-loop copy can then go:

```suggestion
  for (size_t n = 0; n < exported_txs.m_ptx.size(); ++n)
  {
    for (const size_t idx : exported_txs.m_ptx[n].construction_data.selected_transfers)
    {
      if (idx >= m_transfers.size() || !m_transfers[idx].m_key_image_known)
        THROW_WALLET_EXCEPTION(error::multisig_import_needed);
    }
  }

  for (size_t n = 0; n < exported_txs.m_ptx.size(); ++n)
```

## thomasbuilds | 2026-09-01T08:12:39+00:00
If the online wallet already has every key image imported the export is empty and `import_outputs` is skipped, so an offline wallet restored from seed throws `invalid transfers index` here where master signed from `sd.sources`:

```suggestion
  for (const auto &ptx : txs)
  {
    for (const size_t idx : ptx.selected_transfers)
      THROW_WALLET_EXCEPTION_IF(idx >= m_transfers.size(), error::wallet_internal_error,
        "No record of output " + std::to_string(idx) + " in this wallet, run export_outputs all on the online wallet and import_outputs here");
    this->sanity_check_pending_tx(ptx, false, true, std::nullopt);
  }
```

## thomasbuilds | 2026-09-01T08:12:39+00:00
`unlock_time` is never checked, only the pool rejects nonzero values (blocks bypass it) and the offline side already checks it in `sign_tx`:

```suggestion
    CHECK_AND_ASSERT_THROW_MES(construct.splitted_dsts.size() == ptx.tx.vout.size(),
        "sanity_check_pending_tx: destination vecs are inconsistent");
    CHECK_AND_ASSERT_THROW_MES(ptx.tx.unlock_time == 0 && construct.unlock_time == 0,
        "sanity_check_pending_tx: unlock_time must be zero");
```

## thomasbuilds | 2026-09-01T08:12:39+00:00
```suggestion
```

# Action History
- Created by: UkoeHB | 2026-08-10T04:44:23+00:00
