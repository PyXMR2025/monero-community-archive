---
title: 'wallet2: try to avoid sending back change to the same recipient'
source_url: https://github.com/monero-project/monero/pull/8424
author: moneromooo-monero
assignees: []
labels: []
created_at: '2022-07-06T16:26:40+00:00'
updated_at: '2023-07-11T18:16:54+00:00'
type: pull_request
status: open
closed_at: null
merged_at: null
---

# Original Description
No description

# Discussion History
## jeffro256 | 2023-07-03T16:36:04+00:00
```suggestion
      // if this output is change from a tx that was sent to any address in dsts,
      // we crank relatedness to the max to avoid a "returning customer" inference
      bool returning_customer = false;
      for (const auto &ct: m_confirmed_txs)
      {
        const crypto::hash &txid = ct.first;
        if (txid != candidate.m_txid) continue;
  
        // we found a confirmed transfer corresponding to the output under consideration:
        // we check whether this transfer was to one of the addresses we're sending to
        for (const auto &dst: ct.second.m_dests)
        {
          if (std::find_if(dsts.begin(), dsts.end(), [&dst](const cryptonote::tx_destination_entry &e){ return e.addr== dst.addr; }) != dsts.end())
          {
            MDEBUG("Output is from from a tx sent earlier to one of the destinations, we won't use it unless really necessary");
            relatedness = 1.0f;
            returning_customer = true;
            break;
          }
        }
        if (returning_customer) break;
      }
```

If we add a flag called `returning_customer` and modify the control flow a little bit, we can exit early from the outer loop as well when we sense a returning customer.

## moneromooo-monero | 2023-07-10T19:54:47+00:00
That looks like a large change, but it might be mostly whitespace. In any case, breaking on relatedness == 1.0f is both simpler and more generic. Agreed ?

## jeffro256 | 2023-07-11T18:16:54+00:00
Yeah I like that better actually 

# Action History
- Created by: moneromooo-monero | 2022-07-06T16:26:40+00:00
