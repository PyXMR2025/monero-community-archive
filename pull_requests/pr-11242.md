---
title: 'cryptonote_basic: simplify Hardfork'
source_url: https://github.com/monero-project/monero/pull/11242
author: jeffro256
assignees: []
labels:
- pending review
- daemon
created_at: '2026-09-04T18:08:42+00:00'
updated_at: '2026-09-19T01:49:09+00:00'
type: pull_request
status: open
closed_at: null
merged_at: null
---

# Original Description
- Removes the circular linking dependency between `libcryptonote_basic` and `libblockchain_db` (which causes link-time falures in some debug builds)
- Removes circular logical dependency of `BlockchainDB` on `Hardfork`, and vice versa
- Makes `Hardfork` stateless
- Guts a lot of unused voting logic

This design has been bothering me for a while due to it being fragile and hard to reason about. But I recently got an link-time error while trying to build with debug + ASAN, so now I have an actual excuse to fix it. I'm confident that if we wanted to introduce fork voting logic back into the codebase sometime in the future, that we can implement it a bit better.

# Discussion History
## selsta | 2026-09-09T18:16:28+00:00
`DEFAULT_WINDOW_SIZE` needs to be defined before passing it by reference

## selsta | 2026-09-09T18:25:05+00:00
Blockchain height trunacted to 8 bits?

## selsta | 2026-09-09T18:26:32+00:00
Should `voting` output parameter also be assigned here to match previous behaviour?

## selsta | 2026-09-10T19:10:44+00:00
```suggestion
    const uint8_t version = req.version > 0 ? req.version : blockchain.get_next_hard_fork_version();
```

together with a

```cpp
uint8_t Blockchain::get_next_hard_fork_version() const
{
  const uint8_t current = get_current_hard_fork_version();
  for (const auto &fork : get_hardforks())
    if (fork.version > current)
      return fork.version;
  return current;
}
```

would avoid behaviour change.

## selsta | 2026-09-10T19:11:19+00:00
Should this be visibly deprecated?

## thomasbuilds | 2026-09-15T07:50:02+00:00
`mkblock(hf, h, vote)` sets `major_version` from `hf.get_ideal_version(h)`, which is what `check_for_height` compares against, so the version half of this assert is tautological and the vote half only checks `get_ideal_version(h) <= h+1`; the pre-PR value check (`hf.get(h)`) was dropped instead of converted:

```suggestion
    ASSERT_TRUE(hf.check_for_height(mkblock(hf, h, h+1), h));
    ASSERT_EQ(hf.get_ideal_version(h), h ? h + 1 : 1);
```

Same construction at L124, L130, L168.

## thomasbuilds | 2026-09-15T07:50:02+00:00
Doc still says the check "is made by add" and that a failing add makes the object "reorganize" (L104-L114); both are gone, and `@param height` was dropped.

## thomasbuilds | 2026-09-15T07:50:02+00:00
Brief still says "in use/voted for"; L914 "voted to be enabled" likewise.

## thomasbuilds | 2026-09-15T07:50:02+00:00
`original_version_till_height` is never read anymore (its only reader, `HardFork::get_block_version(uint64_t)`, is deleted). This branch and the leftover braces only pick a dead value; dropping the ctor parameter (the unit tests pass it too), `DEFAULT_ORIGINAL_VERSION_TILL_HEIGHT` and `mainnet_/testnet_hard_fork_version_1_till` leaves `m_hardfork.emplace(1);`.

## thomasbuilds | 2026-09-15T07:50:02+00:00
`threshold` is validated and stored but never read anymore (`get_voted_fork_index` and `get_voting_info` were the readers). Keep only the 3-arg `add_fork` and drop the argument at the four call sites in `Blockchain::init`.

## thomasbuilds | 2026-09-15T07:50:02+00:00
`do_check_for_height` has a single caller (its twin `do_check` is gone), and `check_for_height` takes `lock` right before `get_ideal_version` takes it again. `heights` is only populated during `Blockchain::init` and `get_hardforks()` already returns it unlocked, so the lock and the `do_` split can both go.

## thomasbuilds | 2026-09-15T07:50:02+00:00
Same as the existing inline `get_ideal_hard_fork_version(uint64_t)` (blockchain.h L921), already used as `get_ideal_hard_fork_version(m_db->height())` at L622; the `height == db_height` branch below spells it a third time.

## thomasbuilds | 2026-09-15T07:50:02+00:00
`add_block` now stores `blk.major_version` itself and nothing in this file checks the stored value; e.g. after the two `add_block` calls:

```cpp
  ASSERT_EQ(this->m_blocks[0].first.major_version, this->m_db->get_hard_fork_version(0));
  ASSERT_EQ(this->m_blocks[1].first.major_version, this->m_db->get_hard_fork_version(1));
```

## thomasbuilds | 2026-09-15T07:50:02+00:00
With only this placeholder left, the second `init()` call in `Blockchain::reset_and_set_genesis_block` (blockchain.cpp L719) now does nothing: `heights` is never empty there (`Blockchain::init` populated it at L332).

## jeffro256 | 2026-09-19T01:49:09+00:00
Probably

# Action History
- Created by: jeffro256 | 2026-09-04T18:08:42+00:00
