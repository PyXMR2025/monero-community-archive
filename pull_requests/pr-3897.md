---
title: 'mlog: --max-log-files to set the max number of rotated log files'
source_url: https://github.com/monero-project/monero/pull/3897
author: stoffu
assignees: []
labels: []
created_at: '2018-06-01T03:31:07+00:00'
updated_at: '2018-06-20T19:19:45+00:00'
type: pull_request
status: merged
closed_at: '2018-06-20T19:19:45+00:00'
merged_at: '2018-06-20T19:19:45+00:00'
---

# Original Description
No description

# Discussion History
## moneromooo-monero | 2018-06-01T09:31:28+00:00
That will throw if I/O error, you want the version with an error code parameter (or use a try/catch over the whole thing which is safer I guess)

## moneromooo-monero | 2018-06-01T09:31:54+00:00
Does not test errors

## moneromooo-monero | 2018-06-01T09:34:23+00:00
This is an unsigned comparision, so if found_files.size() is 10, i < some_very_large_number, it should end up dereferencing unallocated memory.
Maybe: for (size_t i = 0; found_files.size() - i > max_log_rotation; ++i) (also guards against huge max_log_rotation)

## moneromooo-monero | 2018-06-01T09:35:04+00:00
Maybe max-log-files is less confusing.

## moneromooo-monero | 2018-06-01T09:37:54+00:00
With > rather than >=, and the "if (found_files.size() > max_log_rotation)" below,  it'll keep bitmonero.log uncounted, so max-log-rotation of 1 wil keep two files.

## stoffu | 2018-06-01T13:10:33+00:00
ok

## stoffu | 2018-06-01T13:10:41+00:00
ok

## stoffu | 2018-06-01T13:14:35+00:00
There's already this check `if (found_files.size() > max_log_files)` just above, so I'm not sure why your concern applies. Or am I missing something?

## stoffu | 2018-06-01T13:14:43+00:00
ok

## stoffu | 2018-06-01T13:14:57+00:00
ok

## moneromooo-monero | 2018-06-01T17:27:50+00:00
If you switch to the ec version, it doesn't throw, it's one xor the other :)

## moneromooo-monero | 2018-06-01T17:28:17+00:00
No, I had missed it.

## stoffu | 2018-06-02T01:30:14+00:00
Ah, I was too lazy to check the reference and simply copied a code snippet from common/util.cpp :)

## stoffu | 2018-06-02T01:43:12+00:00
The intent here is if it failed to obtain the timestamp of a log file, that log file is assigned the least priority to be deleted.

## stoffu | 2018-06-02T01:45:25+00:00
By using `<=` here and using `if (found_files.size() >= max_log_files)` in the above, this code keeps the number of old logs to `max_log_files - 1`, making the total number of kept logs as `max_log_files`.

## vtnerd | 2018-06-13T02:12:04+00:00
This is creating two temporary strings , at `.string()` and at `.substr(...)`.

Drop `.substr(...)` with
```c++
if (... && std::memcmp(filename.data(), filename_base.data(), filename_base.size()) == 0)
```
the size check was/is already done in the first expression. As for the other temp string, I guess it makes sense for the character encodings and what not; `boost::filesystem` is annoying here.

## vtnerd | 2018-06-13T02:16:49+00:00
So ... `time_t` isn't guaranteed to be an integer by the standard. Practically, this might be unheard of at this point though. Adding a 
```c++
static_assert(std::is_integral<time_t>(), "bad time_t")
```
couldn't hurt, as it would at least trap some weird thing going on.

## vtnerd | 2018-06-13T02:20:48+00:00
I think the `boost::system::error_code` overload can still throw FWIW, `std::bad_alloc` for instance. Although, I agree with @moneromooo-monero , usage of the other overload is what you were really going for here. And the object is compatible with `std::ostream` so your error message can just be `<< ec` instead of `<< e.what()`.

## vtnerd | 2018-06-13T02:24:00+00:00
I would just use `boost::filesystem::last_write_time(path, ec)`. The `epee` function adds almost no utility. As a bonus, the log message can have the reason for the failure to retrieve write time via `boost::system::error_code`

## moneromooo-monero | 2018-06-20T07:13:30+00:00
That's also boost::starts_with(filename, filename_base) fwiw (no need to change)

## stoffu | 2018-06-20T07:28:27+00:00
Right, I'll try to remember it.

# Action History
- Created by: stoffu | 2018-06-01T03:31:07+00:00
- Merged at: 2018-06-20T19:19:45+00:00
