---
title: '[proposal] add date and name parameters to wallet URI specification'
source_url: https://github.com/monero-project/monero/issues/10168
author: hbs
assignees: []
labels:
- proposal
created_at: '2025-10-14T19:41:31+00:00'
updated_at: '2026-03-31T21:29:42+00:00'
type: issue
status: open
closed_at: null
---

# Original Description
**NOTE: This is a copy of the original issue filed for [meta](https://github.com/monero-project/meta/issues/1205). Since PR #9992 the wiki is no longer the reference for the URI scheme definition.**

The current [wallet URI specification](https://github.com/monero-project/monero/blob/master/docs/URI_SCHEME.md) allows to specify a restore height or a set of tx ids to scan when defining a wallet.

This proposal suggests to add third possibility to restore a wallet by specifying a start date for restoration.

This feature is available in almost all Monero wallet software, whether on desktop or mobile. The wallet birthday is also part of the [polyseed](https://docs.getmonero.org/mnemonics/polyseed/#wallet-birthday-restore-height) mnemonic scheme.

The date from which to restore the wallet could be specified either as a date in ISO 8601 format (`YYYY-MM-DD`) or as a number of elapsed seconds since the Unix Epoch. The presence of a hyphen in the value would help software decide whether it is a date or a number of seconds being specified.

This proposal suggests to use the query parameter `date` to specify the restore date.

This proposal also suggests to add to the wallet URI specification an optional name to give to the wallet. This name could then be used as a default value for the wallet naming field which exists in most tools.

The proposal suggests to use the URI fragment to specify the wallet name, in a similar way to how `tx_description` is specified for the transaction scheme, though the table describing the elements of the TX scheme are contradicted by the example which specifies `tx_description` as a query parameter and not as the URI fragment.

The final table for the wallet definition scheme would be:

| Parameter      | Type       | Kind | Requires | Description |
|:---------------|:------:    |:-----:|:-------- |:------------|
| `address`      | String     | path | `view_key` |Raw (95-character) address                          |
| `spend_key`    | Hex String | query | `address`, `view_key` | Private spend key of a wallet.                      |
| `view_key`     | Hex String | query | `address`, absence of `seed` | Private view key of a wallet.                       |
| `seed`         | String     | query | absence of `view_key` | URL encoded mnemonic seed to restore a deterministic wallet. |
| `height`       | Long       | query | Absence of `txid` and `date` | Block height when the wallet was created.           |
| `txid`         | String     | query | Absence of `height` and `date` | Transaction ID(s) to scan. Multiple transaction IDs can be provided if separated by a semicolon (`;`). |
| `date`         | String     | query | Absence of `height` and `txid` | Date when the wallet was created, in ISO 8601 format (YYYY-MM-DD) or as a number of seconds elapsed since the Unix Epoch. |
| `label`         | String | query |  | Name to give to the wallet. |

These are valid example of a wallet definition URIs:

```
monero_wallet:467iotZU5tvG26k2xdZWkJ7gwATFVhfbuV3yDoWx5jHoPwxEi4f5BuJQwkP6GpCb1sZvUVB7nbSkgEuW8NKrh9KKRRga5qz?seed=python%20runway%20gossip%20lymph%20hills%20karate%20ruined%20innocent%20ought%20dual%20shipped%20shipped%20sushi%20pyramid%20guys%20entrance%20obedient%20natural%20kiwi%20wobbly%20vixen%20wipeout%20template%20typist%20innocent&height=12345676&label=My%20Wallet
```

```
monero_wallet:467iotZU5tvG26k2xdZWkJ7gwATFVhfbuV3yDoWx5jHoPwxEi4f5BuJQwkP6GpCb1sZvUVB7nbSkgEuW8NKrh9KKRRga5qz?spend_key=029c559cd7669f14e91fd835144916009f8697ab5ac5c7f7c06e1ff869c17b0b&view_key=afaf646edbff3d3bcee8efd3383ffe5d20c947040f74e1110b70ca0fbb0ef90d&date=2025-05-15&label=Monero-Wallet
```

[Edited 2026-03-31 to replace `name` as fragment with `label` as a parameter]


# Discussion History
## FabriLluvia | 2025-12-20T02:52:43+00:00
I agree!

## plowsof | 2026-02-17T10:18:49+00:00
my ignored stance or NACK for `date` remains 

`name`?  `create_wallet` accepts a `filename` parameter. an optional `filename` could be a sane suggestion - unless there are some esoteric URI rules regarding this type of thing im unaware of

## hbs | 2026-02-17T11:16:37+00:00
The more probable use for monero_wallet URIs would be via mobile wallets scanning a QR code, so a name makes probably more sense in that context, an optional filename could indeed be useful for desktop wallets.

For date, sorry indeed I did not reply to your comment, my bad. There is no constraint to restrict the number of parameters, so it is in my opinion clearer to have a dedicated `date` parameter separate from a `height` one so intent can be clearly deducted from the used parameter (with priority rules in the rare cases where both would be specified).

## plowsof | 2026-02-17T12:23:36+00:00
technically the monero-gui accepts a user supplied wallet name and transforms it into the path : defaulting to `$username` or `$username_n` if exists. also checks to remove any unsafe chars for supported OS's. in its config file exist 2 related separate fields:  `account_name` and `wallet_path`, although adding multiple accounts with user supplied names is supported this does not reflect in the settings file. must be stored in the cache, so `name` is nebulous 

some related tracking issues: the monero gui displays an export QR with URI, but importing is not yet implemented, a PR to remove exporting until import is enabled https://github.com/monero-project/monero-gui/pull/3657  

there is also the prompt and push pull request to support multiple recipients which is on-going which concludes with this comment pointing to the spec needing some work to support that feature better https://github.com/monero-project/monero/pull/9830#issuecomment-3676571638


## hbs | 2026-03-30T18:43:52+00:00
Digging into the Monero codebase I found that the `unlock_time` interprets block height < 500,000,000 as an actual block number but any value at or above that as a Unix timestamp with the Epoch as reference. Therefore for the purpose of the URI specification the same convention could be adopted for the `height` parameter. Block 500M will be mined roughly in 3916, that should leave plenty of time to the upcoming core teams to work out a solution to the y3916 problem.

In order to reduce the parameter count, the `height` parameter could also be set to an ISO8601 timestamp which could be detected by the presence of either `-` or `T` in the parameter value.

## hbs | 2026-03-31T21:26:13+00:00
Regarding the wallet *name*, I found out that Cake Wallet is using the value of the `label` parameter, so I suggest this is used instead of `name`.

# Action History
- Created by: hbs | 2025-10-14T19:41:31+00:00
