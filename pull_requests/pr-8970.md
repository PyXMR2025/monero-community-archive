---
title: Read interface for new serialization system
source_url: https://github.com/monero-project/monero/pull/8970
author: vtnerd
assignees: []
labels:
- pending review
- serialization
created_at: '2023-08-11T20:57:17+00:00'
updated_at: '2026-08-20T14:51:31+00:00'
type: pull_request
status: open
closed_at: null
merged_at: null
---

# Original Description
This is the read interface for my proposed serialization system, along with the remainder of the (major) adapters/wrappers that were not in the write interface review. After this review are the implementations (epee, json, msgpack), and then the changes needed to the existing code.

# Discussion History
## jeffro256 | 2023-10-19T19:03:54+00:00
Protected method may cause private field to underflow

## jeffro256 | 2023-10-19T20:49:09+00:00
So are we choosing to support nested arrays now ?

## vtnerd | 2023-10-21T19:00:11+00:00
Iff there's a mismatch in `increment_depth()` calls. Are you advocating for a runtime check here, to guard against that too?

## vtnerd | 2023-10-21T19:03:16+00:00
I thought we always supported nested arrays? This occurs in the ZeroMQ code, and perhaps elsewhere. You can "get-around" this by putting a class in-between like:
```c++
struct nested { std::vector<uint32_t> inner; }
std::vector<nested> outer;
```
instead of
```c++
std::vector<std::vector<uint32_t>>
```
The complexity of the nested case is a little higher than the rest of the code, so I can understand if you think it should be removed.

## jeffro256 | 2023-10-24T18:34:49+00:00
I think the best design would be to make `increment_depth` and `decrement_depth` private, add a runtime check to `decrement_depth`, and then have `start_object` and `start_array` both call `increment_depth`, just like how `end_object` and `end_array` call `decrement_depth`.

This is because a `reader` implementation or any buggy serialization code for any type could cause this value to underflow. Also, this value merely acts (from my PoV) as a DDoS prevention measure. I can't think of when the actual value of `depth_` by itself would be useful to a format implementation. For this reason, I think it would be cleaner and more stable to hide the depth value, and have subclasses call the parent `start/end_object/array` methods as appropriate. 

## jeffro256 | 2023-10-24T18:37:29+00:00
Also, if there happens to be different structural limits we want to impose (e.g. only on objects) and we can do it in this base class, if we force the subclasses to go thru `start/end_object/array` methods, it will be easier to make those adjustments correctly.

## jeffro256 | 2023-10-24T19:04:32+00:00
What is this supposed to return for JSON, CBOR, or any other format with indefinite array lengths?

## jeffro256 | 2023-10-24T19:06:49+00:00
I would mark this as `virtual` in case it is ever needed to be overridden by a subclass. Since we usually use `reader` in a templated context, the compiler should usually be able to de-virtualize this method most of the time anyways. Same with `end_object`. 

## vtnerd | 2023-10-24T23:01:14+00:00
They (JSON, CBOR) `return 0`, and `is_array_end(size_t)` is called for each entry until the end is found. This is why `is_array_end(size_t)` is `virtual` and `end_array()` is not - the derived classes don't need to override the default.

Msgpack and epee return the number of items, and `is_array_end(size_t)` is only invoked once.

## vtnerd | 2023-10-24T23:04:09+00:00
I haven't had a need to `override` for msgpack, epee, protobuf, or JSON (and unlikely for CBOR either). `is_array_end(size_t)` and `key(...)` are both `virtual`, and are used to determine the end whereas these functions are simply used by the base class to track placement in the tree.

## vtnerd | 2023-10-24T23:04:39+00:00
Also, this is trivial to change this if a future format needs to override it.

## vtnerd | 2023-10-24T23:19:33+00:00
I'm leaning towards making this change, but I'll first describe how this is handled in the current design. Its not well documented, a short-coming that needs fixing (or else dropped).

The `from_bytes` template method has the following:
```c++
       R in{std::forward<T>(source)};
       bytes(in, dest);
       in.check_complete();
```
this requires that every reader type have a method called `check_complete()`. This method checks to make sure that the tree is "complete":
```c++
   void json_reader::check_complete() const
   {
     if (depth())
       WIRE_DLOG_THROW(error::rapidjson_e(rapidjson::kParseErrorUnspecificSyntaxError), "Unexpected end");
   }
```
So the potential underflow is intentional - either the next call to `increment_depth()` detects an overflow case or the `check_complete()` detects a non-zero depth.

AFAIK, this achieves the same thing, but with one-less branch per call to `decrement_depth()`. It probably looks better to have the check though, and the extra check is trivial in the design.

---------

I left `increment_depth()` and `decrement_depth()` as `protected` so that "skipping" a field could call them. I _think_ this has never been necessary, as `start_object()` and `end_object()`, etc, have been used instead (so at a minimum I would make the functions `private`).

## vtnerd | 2023-10-24T23:28:41+00:00
After further thought, I will just add the check. It's possible to call `end_object()` enough times to rollback to `0`, which is unintentional, so a runtime-check is needed. I'll also move them to `private`, as I can't recall using them in the derived classes.

## vtnerd | 2023-10-25T00:48:51+00:00
I moved the functions to `private` in the other review, but _might_ revert it. The fallout is that `do_start_object()` and `do_start_array(size_t)` can no longer be de-virtualized.

## selsta | 2026-05-15T21:58:38+00:00
Can this underflow?

## selsta | 2026-05-15T22:09:31+00:00
Could `array_unchecked` track an `elements_read` counter instead of using `dest.size()`, or is "size grows once per decoded element" intended to be part of the array container contract? The `emplace_hint` insertion path would not necessarily guarantee that for set/map-like containers.

## selsta | 2026-05-15T22:16:14+00:00
```suggestion
  if (max_read_depth() < ++depth_)
```

or the `max_read_depth()` comment should say it's exclusive

## selsta | 2026-05-15T22:17:11+00:00
Is it okay if this throws?

## selsta | 2026-05-15T22:18:05+00:00
```suggestion
    * `namespace wire { void read_bytes(reader&, ns::type&); }`
    * `namespace ns { void read_bytes(wire::reader&, type&); }`
```

## vtnerd | 2026-05-16T01:23:34+00:00
Yes this can underflow, but this is intentionally allowed. JSON returns `0` for every `start_array` call, and uses `is_array_end` to signal the end.

An extra guard is added so that once `more` is set to `false`, it remains false forever even after `count` underflows.

## vtnerd | 2026-05-16T01:40:06+00:00
> or is "size grows once per decoded element" intended to be part of the array container contract

This. If the map insertion fails due to a duplicate key, no additional memory was needed, and therefore the ratio between packed and unpacked size remains below `min_element_size`.

## vtnerd | 2026-05-16T01:43:16+00:00
It was written this way to protect against `max_read_depth()` being `size_t::max` (which is arguably unrealistic).

I will tweak the documentation.

## vtnerd | 2026-05-16T01:54:12+00:00
Fixed.

## vtnerd | 2026-05-16T01:57:58+00:00
This should be acceptable, and only possible if `reader` violates the specification of the interface.e

## thomasbuilds | 2026-06-26T17:57:05+00:00
`unpack_field` is pinned to `I=0` (`field_<T, true>`/`field_<T, false>`), but `reset_field` above (L386–398) takes `unsigned I`. Since `set_mapping` propagates `field_.id()` and `try_read` calls `unpack_field` at L455, a `WIRE_FIELD_ID(<non-zero>, …)` field produces `field_<…, true, N>` and matches no overload.

```suggestion
  template<typename R, typename T, unsigned I>
  inline void unpack_field(R& source, wire::field_<T, true, I>& dest)
  {
    bytes(source, dest.get_value());
  }

  template<typename R, typename T, unsigned I>
  inline void unpack_field(R& source, wire::field_<T, false, I>& dest)
  {
    if (!bool(dest.get_value()))
      dest.get_value().emplace();
    bytes(source, *dest.get_value());
  }
```

The same `I=0` pinning affects the write path: `wire_write::field()` in `write.h` and `available()` in `field.h`.

## thomasbuilds | 2026-06-26T18:05:12+00:00
This includes `serialization/wire/epee/fwd.h`, which doesn't exist in any commit of this repo, so read.h can't be `#include`d as-is. It also appears unused

Also the three new sources `wire/asio.cpp`, `wire/basic_value.cpp`, `wire/read.cpp` aren't referenced by any CMakeLists ?

## thomasbuilds | 2026-06-26T18:10:12+00:00
```suggestion
    //! \throw wire::exception if next value not an integer.
```

## thomasbuilds | 2026-06-26T18:10:51+00:00
```suggestion
    the optional on empty behavior. See `wire/traits.h` for more information
```

## thomasbuilds | 2026-06-26T18:21:23+00:00
Writing a `basic_value` holding `nullptr` throws `std::logic_error`. That isn't a `wire::exception`, so it escapes `wire_write::to_bytes`'s error-code path: it's caught only by `catch(...)`, which rethrows instead of returning an error code.

## vtnerd | 2026-06-27T01:36:28+00:00
Yes, if something actually used the `I` parameter, compilation would break here.

I'm currently moving away from the proposal to add integer keys, so the result will be an updated diff dropping the `I` entirely. It does provide some reasonable space savings, but the pack/unpack difference isn't as great as I thought it would be. "Finding" the correct value for a given input seems to dominate the CPU cycles. And worst of all use of the integer keys will likely frustrate downstream projects trying to determine their meaning from our source.

This does mean that this interface will not support Protobuf, unless the integer keys were re-introduced. Its not difficult to re-add them, provided you're clever in C++ templates.

## vtnerd | 2026-06-27T01:37:54+00:00
Yes, must've been an unintentional exclusion when creating this diff.

## vtnerd | 2026-06-27T01:40:08+00:00
Yes, this was intentionally designed this way, but perhaps undesirable. Thinking whether I should move to a wire exception here.

## thomasbuilds | 2026-07-04T15:29:31+00:00
In `replace_serialization` the response id is a `WIRE_OPTIONAL_FIELD`, so a `nullptr` `basic_value` omits the member and this throw is never reached. But omitting the id violates JSON-RPC 2.0: the response id is REQUIRED, and "If there was an error in detecting the id in the Request object (e.g. Parse error/Invalid Request), it MUST be Null". Master currently emits `"id": null` on that path: `FullMessage::getResponse` always writes the id, and `BAD_JSON` passes a default-constructed (null) `rapidjson::Value`, so the port regresses this. A compliant response needs a required field holding null, which is exactly the case that throws here, and `json_writer` has no null output (`json_reader::basic()` already maps `null` to `basic_value{nullptr}`). So rather than changing the exception type, `wire::writer` probably needs null output (at least for JSON), keeping the throw as a `wire::exception` for formats without a null representation like epee binary.

## selsta | 2026-08-20T14:50:01+00:00
This should also require no padding.

## selsta | 2026-08-20T14:51:20+00:00
needs to be `std::string_view`

# Action History
- Created by: vtnerd | 2023-08-11T20:57:17+00:00
