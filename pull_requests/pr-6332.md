---
title: Allow wallet2.h to run in WebAssembly.
source_url: https://github.com/monero-project/monero/pull/6332
author: woodser
assignees: []
labels: []
created_at: '2020-02-11T17:45:17+00:00'
updated_at: '2024-09-11T12:27:02+00:00'
type: pull_request
status: merged
closed_at: '2020-04-21T13:27:23+00:00'
merged_at: '2020-04-21T13:27:23+00:00'
---

# Original Description
- Add contrib/epee/include/net/abstract_http_client.h which
http_client.h extends.
- Replace `simple_http_client` with `abstract_http_client` in wallet2,
message_store, message_transporter, and node_rpc_proxy.
- Skip code incompatible with webassembly using `#if defined
__EMSCRIPTEN__` directives.
- Add ability to import and export wallet file buffers in wallet2
instead of using file system.

Code and collaboration credits: moneromooo, endogenic, mymonero

Feedback is requested on removing the call to `miner::find_nonce_for_given_block` in cryptonote_tx_utils.cpp and changing the default http method from `GET` to `POST` in wallet2.h and http_abstract_invoke.h (otherwise the browser issues `GET` requests which fail).

# Discussion History
## xiphon | 2020-02-16T00:49:05+00:00
```suggestion
  m_http_client(std::move(http_client)),
```

## xiphon | 2020-02-16T00:53:44+00:00
```suggestion
    const std::shared_ptr<epee::net_utils::http::abstract_http_client> m_http_client;
```

## xiphon | 2020-02-16T01:01:52+00:00
```suggestion
boost::optional<keys_file_data> wallet2::get_keys_file_data(const epee::wipeable_string& password, bool watch_only)
```

## xiphon | 2020-02-16T01:08:33+00:00
Would be better to implement `load_keys` overload accepting a buffer as an argument.

## moneromooo-monero | 2020-02-20T13:44:54+00:00
Why add all of these when you can just not define HAVE_MLOCK ? Is this somehow not working ?

## moneromooo-monero | 2020-02-20T13:45:08+00:00
Why these ?

## moneromooo-monero | 2020-02-20T13:45:59+00:00
Can you explain why the switch from GET to POST ? It is probably a good idea in theory since they can have side effects, but are there actual reasons beyond this ?

## moneromooo-monero | 2020-02-20T13:46:49+00:00
return false

## moneromooo-monero | 2020-02-20T13:48:41+00:00
All this massive spam all over the file should be avoided. You can probably do so easily by making m_http_client a reference, initialized in the ctor.

## moneromooo-monero | 2020-02-20T13:50:28+00:00
I've not read this yet, but why is the keys file code changed in this PR ?

## woodser | 2020-02-22T17:54:28+00:00
It is somehow not working (i.e. something is calling one of the mlocker class functions).  I will look into it further.

## woodser | 2020-02-22T17:54:54+00:00
I have updated the indentation to be the same as the lines above it.

## woodser | 2020-02-22T17:55:39+00:00
The browser issues `GET` requests which fail unless these default to `POST`.  I was not able to track how `GET` works at all or where `GET` is overriden with `POST` in monero-project, as the RPC requests require `POST`.

## woodser | 2020-02-22T17:55:53+00:00
Can you clarify?  Do you mean:

```
  #if defined __EMSCRIPTEN__
    return false;
  #endif

## woodser | 2020-02-22T17:56:48+00:00
Making `m_http_client` a reference will require it to be initialized in the constructor.  Since `abstract_http_client` is abstract, the default value could be an instance of `http_simple_client`, but that cannot be initialized in emscripten, so I do not have a suitable default value in that environment and execution fails.

I have updated the PR to use a reference which compiles but does not execute in emscripten because initializing `http_simple_client` in that environment fails.

Please advise.  Thanks.

## woodser | 2020-02-22T17:57:00+00:00
The key file code is changed in order to directly import and export wallet data to and from wallet2.  Otherwise there is no way to save and re-open a wallet in WebAssembly because the file system is not accessible.

## moneromooo-monero | 2020-02-22T18:30:09+00:00
Yes. Otherwise the function returrns success with an invalid block.

## moneromooo-monero | 2020-02-22T18:32:34+00:00
Off the top of my head, you can add a simple object, like:

struct redir {
redir(http_client *c): C(c) {}
operator http_client &() { return *c; }
http_client *C;
};

Then replace http_client *m_http_client with redir m_http_client in wallet2.h.
That should work AFAICT. Add default ctor if needed, and catch NULL if someone uses it before set.

## vtnerd | 2020-02-22T21:29:53+00:00
Two thoughts:

Use a `std::unique_ptr` instead of a raw pointer to clearly designate ownership. Unless `std::shared_ptr` behavior was necessary (it doesn't appear so at a glance).

Adding `*` and `->` stuff isn't really that hard to read, and we're now adding code just to reduce total line changes ... just why?

## vtnerd | 2020-02-22T21:40:31+00:00
Drop `static` from function and add `constexpr` to char array.

## vtnerd | 2020-02-22T21:40:58+00:00
Same thing for this function (-`static` +`constexpr`).

## vtnerd | 2020-02-22T21:41:35+00:00
Take the opportunity to put every function from here down into a cpp. Drop `static inline` from every function signature.

## vtnerd | 2020-02-22T21:42:52+00:00
I think `http_base.h` and `net_parse_helpers.h` can be moved to the cpp (see other changes). `#incude <string>` is also needed (its being included indirectly somehow now).

## vtnerd | 2020-02-22T21:44:19+00:00
Drop `virtual` from this signature, unless there is a need to overload the behavior (doesn't seem so). Also, move to cpp.

## vtnerd | 2020-02-22T21:44:42+00:00
Move to cpp ... I think? @moneromooo-monero 

## vtnerd | 2020-02-22T21:45:55+00:00
Mark it as `class http_simple_client_template final : ...`. This prevents anyone from overriding this class, and allows the compiler to de-virtualize all `this->` virtual method calls within this class.

## moneromooo-monero | 2020-02-22T22:38:55+00:00
To avoid all the diff spam changing lines that don't actually change.

## moneromooo-monero | 2020-02-22T22:41:24+00:00
I don't know offhand if any show up on the profile.


## vtnerd | 2020-02-22T22:44:23+00:00
Seems like poor choice of words on my part - I know why you suggested it. But creating a class to prevent simple line changes seems incorrect. Especially since it hides pointer semantics - the value can be `NULL` but this is hidden at every usage point with an "auto" dereference.

## moneromooo-monero | 2020-02-22T23:53:36+00:00
Not sure if you checked as the current diff does not show them anymore, but it changed every single RPC call for example. It was all over the place in wallet2.cpp.

## vtnerd | 2020-02-23T16:07:04+00:00
This file has no copyright notice. Unfortunately, I think it will need to copy the notice at the top of the original file since this code dates back to the cryptonote fork.

## woodser | 2020-02-24T17:58:38+00:00
I also think it should be based on what is most correct and natural over these line diffs.  I've updated the PR to work with `std::unique_ptr` until moneromooo-monero's suggestion can be implemented for comparison.

## woodser | 2020-02-24T18:47:49+00:00
`epee::net_utils::http::http_simple_client` is extended by `download_client` in `download.cpp` so adding final causes a compile error.  Please advise.  Thanks.

https://github.com/monero-project/monero/blob/39e18902763a5fd6f355294d8b135c8f5bc2ec99/src/common/download.cpp#L94

## woodser | 2020-02-24T18:51:50+00:00
No compile errors when moved to cpp.

## woodser | 2020-02-25T15:24:14+00:00
`mlocker` functions are being called by using the `mlocked` struct like [here](https://github.com/monero-project/monero/blob/39e18902763a5fd6f355294d8b135c8f5bc2ec99/src/wallet/wallet2.cpp#L3923) in `wallet2::setup_keys()`, so currently, individual functions in `mlocker` need guarded.

## moneromooo-monero | 2020-03-01T02:02:13+00:00
Not related to your change, but this does two allocs per byte... Just noting it so it can be changed later if it turns out to be a bottleneck somewhere.

## moneromooo-monero | 2020-03-01T02:03:26+00:00
I don't understand the implication. Can you explain ?

## moneromooo-monero | 2020-03-01T02:12:38+00:00
That's throwy for a function that returns a bool.

## woodser | 2020-03-04T15:46:00+00:00
I made get_cache_file_data() return `boost::optional<wallet2::cache_file_data>` to be consistent with get_keys_file_data(), and an exception is thrown if boost::none is returned.

## woodser | 2020-03-04T15:47:52+00:00
Execution in WebAssembly will fail on `CRITICAL_REGION_LOCAL(mutex())` in mlocker.cpp even if HAVE_MLOCK is not defined because the `mlocked` struct is used externally without checking for this definition.  The current checks in question prevent that.  Do you prefer another way?

## moneromooo-monero | 2020-03-27T15:39:47+00:00
I think you can remove the sleep altogether, I intended to do this for a while.

## moneromooo-monero | 2020-03-27T15:42:04+00:00
That's existing code. Are you adding a copy for some reason ?

## moneromooo-monero | 2020-03-27T15:43:39+00:00
Also existing code. You probably messed up some rebase somewhere.

## moneromooo-monero | 2020-03-27T15:45:13+00:00
If you can't generate the genesis block, you can't even start the wallet2 hash chain. There is a C implementation of Cryptonight. Does it not work ?

## woodser | 2020-03-27T16:41:17+00:00
The wallet2 instances created through webassembly generally work; they're able to get the balance, send, receive, etc.  Is there a problem I can expect to see?

Can you elaborate how to use the C implementation of Cryptonight here?

## moneromooo-monero | 2020-03-28T02:46:00+00:00
They'll get a wrong genesis block hash. I guess it might not matter in practice for the wallet only...

To use the C version of Cryptonight, you ensure the C version is compiled in slow-hash.c. It should be the case if you define NO_AES, looking at the code.

## woodser | 2020-04-01T19:39:46+00:00
That worked.  I've removed this change.

## xiphon | 2020-04-05T02:59:45+00:00
Consider to use `std::shared_ptr` instead

## woodser | 2020-04-05T13:31:33+00:00
A shared pointer would introduce a functional change from current master, which uses a separate http client instance for each class.  The functional tests fail when changed to a shared pointer.

## xiphon | 2020-04-05T13:33:43+00:00
I don't like `virtual abstract_http_client* clone() const = 0;` idea.

## woodser | 2020-04-05T13:49:18+00:00
The message store needs to be initialized with a http client of the same type as wallet2, which the clone() achieves.  Do you suggest any different or better approach?

## woodser | 2020-04-05T14:45:03+00:00
By the way, a shared_ptr was my first choice as well.

## moneromooo-monero | 2020-04-05T14:49:24+00:00
I don't understand why switching to a shared_ptr instead of unique_ptr would break (I don't care which one is used otherwise). Can you expand on this a bit ?

## xiphon | 2020-04-05T15:01:37+00:00
Clone implies we are copying some existing instance state. But actually the message store just needs a new http client instead (not a clone of an existing one).

Off the top of my head, you could pass http client factory to wallet2 and message store.

In the simplest case it could be just a callback returning new `std::unique_ptr<abstract_http_client>` instance.


## xiphon | 2020-04-05T15:04:32+00:00
Ah, i didn't see your guys comments before posting my reply.

## woodser | 2020-04-07T00:24:39+00:00
Ok I added a factory and removed the clone().  I can make name and styling changes if there are any suggestions.

## xiphon | 2020-04-12T10:16:07+00:00
Looks like just an indentation change.

## xiphon | 2020-04-12T10:19:21+00:00
Don't indent the namespace

## xiphon | 2020-04-12T10:26:16+00:00
Why do we need a default (`= std::unique_ptr<epee::net_utils::http::http_simple_client>(new epee::net_utils::http::http_simple_client())`)?

## xiphon | 2020-04-12T10:35:39+00:00
```suggestion
  m_message_store(http_client_factory->create()),
```

## xiphon | 2020-04-12T12:50:47+00:00
I would avoid `dynamic_cast`. On the other hand, if others are OK with it, i could fix this later in a separate PR.

## xiphon | 2020-04-15T14:42:31+00:00
I would drop this default too

## esarachan | 2020-04-16T03:16:52+00:00
NIT: nested namespaces should probably be indented.

## selsta | 2020-04-16T03:21:51+00:00
See https://github.com/monero-project/monero/pull/6332#discussion_r407177508

I don’t think this should get changed again.

# Action History
- Created by: woodser | 2020-02-11T17:45:17+00:00
- Merged at: 2020-04-21T13:27:23+00:00
