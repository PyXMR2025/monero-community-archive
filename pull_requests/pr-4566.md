---
title: 'mining: detailed dashboard for p2pool'
source_url: https://github.com/monero-project/monero-gui/pull/4566
author: rssoc
assignees: []
labels: []
created_at: '2026-02-08T19:29:26+00:00'
updated_at: '2026-08-11T01:56:11+00:00'
type: pull_request
status: open
closed_at: null
merged_at: null
---

# Original Description
This PR implements a [long requested community feature](https://bounties.monero.social/posts/68/6-026m-statistical-mining-dashboard-for-p2pool-in-monero-gui-wallet-2m) intended to educate newcomers on mining concepts and expose them to p2pool.

This adds:
- **A New Dashboard View**: Accessible from "Advanced > Mining" when "P2Pool" is selected as the mining mode. Implements buttons with transitions and theme compatibility, as well as a user interface and experience matching the rest of the program.
- **Educational Resources**: An on-demand explanation for each dashboard field to help educate newcomers on p2pool and mining terminology and concepts.
- **Refined Data Visualization**: Friendly readouts for hashrate, with rolling averages, and mining effort, alongside an "Advanced" section for those who wish to view the raw API data.
- **Synchronization Gating**: Waits until the side and blockchains are synced to address some consistency issues. The initial synchronization takes some time, and the reporting from the API is a bit eager since we're using P2Pools built-in miner which itself doesn't wait.

This is intended to supersede #4182 which was a partial implementation lacking most of the important points above.

### Screenshots
<img width="1215" height="964" alt="1" src="https://github.com/user-attachments/assets/456809a1-985c-4dd4-8536-e12d376e76be" />
<img width="1215" height="964" alt="2 0" src="https://github.com/user-attachments/assets/bda5664d-6eeb-432b-8a7c-0690318c9481" />
<img width="1215" height="964" alt="3" src="https://github.com/user-attachments/assets/9f602007-d605-4606-8ad8-ee36b5043e1b" />
<img width="1215" height="964" alt="4" src="https://github.com/user-attachments/assets/b3c5bccb-352c-4473-9ff1-b1a1f2cc3594" />
<img width="1215" height="964" alt="5" src="https://github.com/user-attachments/assets/4c7e73fb-0cd9-4085-a38a-0b3ca68684a4" />
<img width="1215" height="964" alt="6" src="https://github.com/user-attachments/assets/88302e38-eec6-4e5a-b3f3-3fb78977ce03" />
<img width="1215" height="964" alt="7" src="https://github.com/user-attachments/assets/ff198daa-d127-43cc-8dfe-214f0e89ea1d" />
<img width="1215" height="964" alt="8" src="https://github.com/user-attachments/assets/ae81266e-6c48-4f15-ad88-9d9ddf40bb32" />

# Discussion History
## thomasbuilds | 2026-04-26T18:13:35+00:00
**Issue: `getStatus()` has hidden side effects on EMA / effort state**

`getStatus()` is called on its own cadence by `Mining.qml`, but it now invokes `fetchLocal()`, which mutates `m_hashrate_ema15m/1h/24h`, `m_effort`, `m_round_hashes` and `m_shares_found`. However, `m_lastUpdate` is only assigned in `update()` never in `fetchLocal()`.

Consequences:

- While the user sits on the Mining page, every `getStatus()` tick advances the EMAs but `m_lastUpdate` keeps growing stale, so `secs = m_lastUpdate.secsTo(now)` blows up and `alpha → 1.0` each new sample fully replaces the EMA, defeating the smoothing.
- When the dashboard is then opened, `update()` sees state already corrupted by `getStatus()` calls.
- Two callers (Mining tick + Dashboard 5 s timer) interleave and double-count samples within a window.

**Suggested fix:** split `fetchLocal()` into a *pure* formatter (no member writes) used by `getStatus()`, and keep the stateful EMA/effort step inside `update()` only. Roughly:

```cpp
// pure: just reads files and formats; no member writes
QVariantMap fetchLocalSnapshot() const;

// stateful: snapshot + advance EMAs + set m_lastUpdate
void recomputeAggregates(const QVariantMap& local, const QVariantMap& pool);
```

Then `getStatus()` calls `fetchLocalSnapshot()` and `update()` calls both.

## thomasbuilds | 2026-04-26T18:15:26+00:00
**Issue: holding `const T&` to `P2PoolManager`'s members is a footgun**

`P2PoolStatsProvider` keeps `const bool& m_started` and `const QString& m_p2poolPath` referencing fields owned by `P2PoolManager`. It works today only because the parent owns the provider via `unique_ptr` and outlives it. Any future refactor that:

- moves the provider onto a worker thread,
- copies/moves it,
- or reorders member destruction in `P2PoolManager`,

will silently produce UB the references will dangle or race with writes in `start()`/`exit()` with no compiler warning.

**Suggested fix:** take a back-pointer to the parent and add small accessors, or pass values + setters. For example:

```cpp
explicit P2PoolStatsProvider(P2PoolManager* parent);
// ...
private:
    P2PoolManager* m_manager;  // non-owning, parent guarantees lifetime
    bool started() const { return m_manager->isStarted(); }
    QString poolPath() const { return m_manager->p2poolPath(); }
```

This also keeps the threading story explicit if you ever move stats fetching off the GUI thread.

## thomasbuilds | 2026-04-26T18:18:07+00:00
**Issue: each `update()` re-reads & re-parses the same JSON files ~10×**

Within a single tick:

- `fetchLocal()` reads `local/miner` + `pool/stats`, then calls `isSidechainReady()` which reads them **again**.
- `fetchPool()` reads `pool/stats` + `local/miner` and again calls `isSidechainReady()`.
- `fetchNetwork()` reads `network/stats` and `isNetworkReady()` reads it again.
- `fetchRaw()` reads everything one more time.

That's roughly 10 file opens + JSON parses every 5 s, all redundant.

**Suggested fix:** parse each file once at the top of `update()` and pass the maps down:

```cpp
void P2PoolStatsProvider::update()
{
    const QVariantMap localRaw   = fetch("local/miner");
    const QVariantMap poolRaw    = fetch("pool/stats");
    const QVariantMap poolStats  = poolRaw["pool_statistics"].toMap();
    const QVariantMap networkRaw = fetch("network/stats");

    const bool sidechainReady = poolStats["sidechainHeight"].toULongLong() > 1000
                              && localRaw["block_reward_share_percent"].toInt() != 100;
    const bool networkReady   = networkRaw["height"].toULongLong() > 0;

    auto local   = buildLocal(localRaw, poolStats, sidechainReady);
    auto pool    = buildPool(poolStats, localRaw, sidechainReady);
    auto network = buildNetwork(networkRaw, networkReady);
    auto raw     = buildRaw(localRaw, poolRaw, networkRaw);

    m_lastUpdate = QDateTime::currentDateTime();
    emit p2poolUpdateStats(local, pool, network, raw);
}
```

## rssoc | 2026-04-27T02:22:00+00:00
Hmm...
> While the user sits on the Mining page, every getStatus() tick advances the EMAs but m_lastUpdate keeps growing stale, so secs = m_lastUpdate.secsTo(now) blows up and alpha → 1.0 each new sample fully replaces the EMA, defeating the smoothing.

There is a degradation in the accuracy in the EMA, depending on how long you're not on the dashboard. I don't think most users would notice it, but yeah it does bother me that it is happening. Though I don't like the proposed fix because EMA should be modeled as a property on the miner. Given that nothing's using the `m_lastUpdate` property besides the `fetchLocal()` for EMA calculations, it's probably better if we just rename it to  `m_lastEMAUpdate` and push the maintenance of that into `fetchLocal()`.

> Two callers (Mining tick + Dashboard 5 s timer) interleave and double-count samples within a window.

I don't think this was actually an issue because only one timer is active at a time (depending on your view).

## rssoc | 2026-04-27T02:22:05+00:00
This was an intentional choice by me because:

> Any future refactor that: ...

I don't see these happening., and it's much simpiler to hold a `const T&` when we know it's going to be correct by construction. Writing wrappers to an object that we know will always exist together with the `P2PoolManager` seems a bit premature to me. 

If that changes in the future for any reason, I'd say it's better handled then anyway.

## rssoc | 2026-04-27T02:22:11+00:00
The operating system already caches the files for you, so it's not really re-read each time. This was intentional; I don't think what you'd get out by doing this is worth the complication or the cost in clarity (especially given that this function is only called once every 5 seconds, or so).

## rssoc | 2026-04-27T02:57:33+00:00
> it's probably better if we just rename it to `m_lastEMAUpdate` and push the maintenance of that into `fetchLocal()`.

Done in https://github.com/monero-project/monero-gui/pull/4566/commits/49cb3bffa9896edfc064d97ac23fd4504a195d4d.

## nahuhh | 2026-08-10T20:30:41+00:00
Example, this is a brand new file, it cant have a copyright from 2-12 years before it was created

## nahuhh | 2026-08-10T20:36:30+00:00
Don't know what happened here, but you seemo have replaced spaces with tabs.

the rest of the file uses spaces. Ensure that this wasnt done elsewhere

## rssoc | 2026-08-10T20:47:16+00:00
Yes exactly :-)

I initially put a copyright up to 2026, but as I was looking through the conventions in the rest of the codebase, for one reason or another, everything had their copyright up to 2024 (even if they were created after that year). 

If that was some convention that I did not know about, I didn't want to stray from it - Hence why I made a separate commit bringing them down from "2026" -> "2024" since this was something I wanted some attention on ;-)

## rssoc | 2026-08-10T20:49:26+00:00
Apologies, this was from my Emacs. Good catch as I thought I had already cleaned these out from the commits when I prepared them. 

## nahuhh | 2026-08-10T20:50:05+00:00
IIUC, new files should just have the current year, edited files should have the year bumped

## rssoc | 2026-08-10T21:37:23+00:00
Addressed, thanks. This was the only instance that got through. 

## jpk68 | 2026-08-10T21:37:43+00:00
Nit: it would probably sound better to say "hovering over the label"

## rssoc | 2026-08-10T21:38:35+00:00
I generally agree with you, so I went ahead and dropped that commit & bumped all created and modified files to 2026. Thanks. 

## jpk68 | 2026-08-10T21:38:56+00:00
Nit: something like "P2Pool network" rather than "The Pool" might be more clear

## rssoc | 2026-08-10T21:51:15+00:00
I do like the consistency of "The" in "Your PC", "The Pool", "The Network", maybe: "Your PC", "The P2Pool Network", "The Monero Network"? 

Though "P2Pool Network" is a bit ambiguous in itself, since P2Pool consists of multiple side-chains (mini, nano, etc). The culmination of these could be considered the "network"... 

To the end user, maybe "The Pool" does make the most sense since those statistics are from the actual P2Pool Pool they're actually part of? lmk..

## rssoc | 2026-08-10T21:52:45+00:00
Yeah agreed. Done. 

## jpk68 | 2026-08-10T21:54:54+00:00
Personally, I think "Your machine", "P2Pool network", and "Monero network" would sound the best, but that's just me.

## rssoc | 2026-08-10T22:06:01+00:00
After thinking about it I can see how it makes sense. I also much prefer "Your machine" to "Your PC", so I've adopted it as you say. Also I changed the header text to be less ambiguous as well:

```
qsTr("View detailed statistics about your hardware, the P2Pool, and the overall Monero network.") 
```

## nahuhh | 2026-08-10T22:12:15+00:00
id probably still say "the P2Pool network" (instead of "the P2Pool"), but thats just me

# Action History
- Created by: rssoc | 2026-02-08T19:29:26+00:00
