---
title: 'deployment/README: Dockerise'
source_url: https://github.com/monero-project/monero-site/pull/2454
author: plowsof
assignees: []
labels: []
created_at: '2025-03-22T04:02:41+00:00'
updated_at: '2026-01-10T11:04:05+00:00'
type: pull_request
status: open
closed_at: null
merged_at: null
---

# Original Description
to test:
`git fetch origin`
`git pull origin head pull/2454/head`

1. Navigate to your local `monero-site` git repository.
2. build the Docker container with `docker build --no-cache -t getmonero-container .`
3. to generate sile files to `_site` run `docker run -v $(pwd):/srv/jekyll getmonero-container bundle exec jekyll build` (Output dir can be changed by adding `--destination <output dir>`
    - for getmonero maintainer : confirm file permissions of the `_site` folder do not interfere with deployment, you can pass `--destination _test` if you want to confirm.
5. Then serve with: `docker run -p 4000:4000 -v $(pwd):/srv/jekyll getmonero-container`. If you want, you can speedup this process by loading only the last blog post instead of all of them. Add `bundle exec jekyll serve --limit_posts 1 --host 0.0.0.0` to the command above.
6. Open a browser and go to [http://127.0.0.1:4000](http://127.0.0.1:4000).
7. If all went well, you should see the Monero website and you're ready to make changes.

After running the cmd below, the Gemfile.lock in your local git should remain unchanged (unless there are updates available)
1. `docker run -v $(pwd):/srv/jekyll getmonero-container bundle update`

 [ruby 3.0.1 @](https://hub.docker.com/layers/library/ruby/3.0.1/images/sha256-d1a8c5160d98cdd51000d3ebfc75ccdad5f43c4b319bbd9e0212dfc337c1e4b5)


# Discussion History
## nahuhh | 2025-04-07T01:39:13+00:00
can ones like this be simplified to
```suggestion
      kramdown (~> 2.3.1)
```


## nahuhh | 2025-04-07T01:44:55+00:00
```suggestion
      mercenary (~> 0.3.6)
```


## nahuhh | 2025-04-07T01:45:37+00:00
```suggestion
      rb-fsevent (~> 0.10.3)
      rb-inotify (~> 0.9.10)
```


## plowsof | 2025-04-07T01:59:18+00:00
I have no idea, tbh i don't even know what these symbols mean, i will read this and have a think https://blog.saeloun.com/2022/08/16/understanding-gemfile-and-gemfile-lock/#what-are-the-various-configuration-options-for-a-gem

## nahuhh | 2025-04-07T02:11:39+00:00
AIUI

`~>` means use the full range of the last supplied digit
so `~> 2.1` means use 2.1+ and less than 3.0

and `~> 2.1.0` means use 2.1.0+ and less than 2.2.0

## nahuhh | 2025-04-07T02:12:48+00:00
https://blog.saeloun.com/2022/08/16/understanding-gemfile-and-gemfile-lock/#pessimistically-greater-than-or-equal-to-symbols

## plowsof | 2025-04-07T02:19:06+00:00
yep, i see they are functionally the same, good to know. 

these are generated automatically though by `bundle update`. I just tried this change, then re-ran the update - sadly it ignores the change and re-adds the previous, so this would just make gem updates tedious.

or `docker run -v $(pwd):/srv/jekyll getmonero-container bundle update` for us 

## plowsof | 2025-04-07T10:40:15+00:00
I see i'm not locking the bundler version the Dockerfile uses, i've fixed this with `RUN gem install bundler:2.2.23`  so thanks for that - deleting the entire lock file and regenerating it gives the same result, so we don't actually know what version of ruby/bundler created this lockfile previously, another benefit of Docker

## plowsof | 2025-11-16T11:40:58+00:00
confirmed the logic changes are correct / achieve the same thing but get overwritten by the automated gem updates each time so its best to stay in sync

## fxrstor | 2025-12-01T11:27:12+00:00
```suggestion
2. Build the Docker container with `docker build --no-cache -t getmonero-container .`
```
Just being consistent with grammar in readme.

## plowsof | 2025-12-01T11:32:13+00:00
thanks, i can squash after approval 

# Action History
- Created by: plowsof | 2025-03-22T04:02:41+00:00
