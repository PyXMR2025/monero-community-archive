---
title: 'math: add n_choose_k function, refactor misc_language into src'
source_url: https://github.com/monero-project/monero/pull/7833
author: UkoeHB
assignees: []
labels: []
created_at: '2021-08-05T05:53:12+00:00'
updated_at: '2021-08-20T21:56:22+00:00'
type: pull_request
status: closed
closed_at: '2021-08-12T10:06:44+00:00'
merged_at: null
---

# Original Description
Adds an `n_choose_k<T>()` function for a future PR (multisig key generation). Also adds `factorial<T>()` to support it.

# Discussion History
## vtnerd | 2021-08-05T15:21:56+00:00
`epee` is typically networking related code and stuff to support the networking code. Based on your description, it will not be used by networking code, so move to `src/` somewhere.

## vtnerd | 2021-08-05T15:23:55+00:00
Why make this templated? The difficult part is that since its not used anywhere (right now), I have no idea whether the template is useful or not. It _seems like_ only one type will eventually be necessary.

Each type ends up being its own assembly, so its best to converge where possible.

## UkoeHB | 2021-08-05T19:56:09+00:00
I am ok with moving it, but feel uncertain about `is typically networking related code and stuff to support the networking code`.

I chose this file because it already has `median` and `get_mid`.
- `median` is only used in `src` stuff (and tests) as far as I can tell.
- `get_mid` is used in `median` and `rolling_median`
- `rolling_median`, while defined in `epee`, is not related to networking (added for the dynamic block algorithm).

Would it be better to also refactor these other math functions into `src`?

## UkoeHB | 2021-08-05T19:59:41+00:00
It feels unpleasant to tailor an otherwise generic math function to one use-case.

## vtnerd | 2021-08-05T20:09:09+00:00
Why is it unpleasant? We probably only need to use unsigned integers of a certain width anyway.

## vtnerd | 2021-08-05T20:13:43+00:00
Possibly, if they aren't used in epee elsewhere.

## vtnerd | 2021-08-05T20:15:12+00:00
Why `return 1;` here? This is basically another error case as choose k elements from n only makes sense if this statement is `true`, but why differentiate this instead of using `0` like all the other error cases?

EDIT: I realize my english of `k elements from n` isn't accurate, but I think the gist is understandable.

## vtnerd | 2021-08-05T20:29:31+00:00
`static_assert(!std::is_signed<T>::value, ...);` ?

## vtnerd | 2021-08-05T20:29:59+00:00
Same thing here with an unsigned static assert.

## vtnerd | 2021-08-05T20:32:05+00:00
So if this overflow its `return 0;` ... hmm. I suppose that makes sense as an error case value, a comment on both functions should be added to explain the `return 0` being error cases

## UkoeHB | 2021-08-06T02:12:34+00:00
I changed my mind, `n < k` is also an error case (will update later).

## UkoeHB | 2021-08-06T02:14:08+00:00
My idea was to support all integral types, and just error out if negative numbers are passed in. I suppose in professional grade code you expect very strict use of types, so maybe a stricter interface is appropriate here.

# Action History
- Created by: UkoeHB | 2021-08-05T05:53:12+00:00
- Closed at: 2021-08-12T10:06:44+00:00
