---
title: 'fcmp++: Use 32 byte slices instead of borrowed pointers for OutputBytes'
source_url: https://github.com/seraphis-migration/monero/pull/60
author: j-berman
assignees: []
labels: []
created_at: '2025-06-20T17:52:35+00:00'
updated_at: '2025-07-17T04:28:23+00:00'
type: pull_request
status: merged
closed_at: '2025-07-17T04:28:23+00:00'
merged_at: '2025-07-17T04:28:23+00:00'
---

# Original Description
Stronger type safety and avoids pointers where unnecessary

# Discussion History
## jeffro256 | 2025-06-23T09:02:20+00:00
What's the difference with this function?

## j-berman | 2025-06-23T20:45:45+00:00
I'm thinking we'll want to use byte slices instead of raw pointers for all the types currently passed into `ed25519_point_from_bytes`, and so `ed25519_point_from_bytes` will just do what this function is doing instead

## jeffro256 | 2025-07-02T22:21:05+00:00
Why's that? Isn't the API taking raw pointers anyways? And if we changed the API to *not* take raw pointers, but also provide a (compile-time fixed) length, if the calling user screws it up, they screw it up... IDK doesn't seem to add much safety IMO

## j-berman | 2025-07-04T21:07:36+00:00
> And if we changed the API to not take raw pointers, but also provide a (compile-time fixed) length, if the calling user screws it up, they screw it up

This is what I was thinking. I think at the very least, using fixed 32 byte slices for data that is always going to be a fixed 32 bytes is clearer code that's easier to manage versus using a raw pointer. There are various ways calling users can screw things up in C++, I think this adds a layer of defense by making the code clearer.

## j-berman | 2025-07-04T21:08:50+00:00
I could consolidate all usage of `ed25519_point_from_bytes` into this function in this PR if you agree with that approach (and fix merge conflicts). That way we're sure we don't end up a "2" lingering forever like that wallet thing.

## j-berman | 2025-07-04T21:15:07+00:00
I'll actually probably end up just in-lining `EdwardsPoint::from_bytes` everywhere instead

## j-berman | 2025-07-08T19:05:01+00:00
Thoughts on the goal to replace all raw pointers with byte slices when referencing data of size known at compile time, perhaps for a future PR(s)?

## jeffro256 | 2025-07-08T20:52:03+00:00
At the API level or inside the Rust impl code?

## j-berman | 2025-07-08T21:31:01+00:00
Both, e.g. `signable_tx_hash`, pseudo outs, key images

## jeffro256 | 2025-07-09T17:47:07+00:00
I disagree with changing the API for this, it makes the API clunkier for no safety benefit. We pass some compound structs by pointer, should we also pass the size of the struct as an argument?

## j-berman | 2025-07-09T18:06:08+00:00
Maybe we're saying different things? I'm saying do this everywhere `signable_tx_hash` e.g. is used in `fcmp++.h`:

https://github.com/seraphis-migration/monero/blob/b7f4da1843c3b4cbd7e0814accf8752158a390f7/src/fcmp_pp/fcmp_pp_rust/fcmp%2B%2B.h#L297

And then instead of taking a raw pointer [here](https://github.com/seraphis-migration/monero/blob/b7f4da1843c3b4cbd7e0814accf8752158a390f7/src/fcmp_pp/fcmp_pp_rust/src/lib.rs#L773), use a `[u8; 32]` instead.

I'm proposing do this for all the types that have known fixed size at compile time only, which also lets us remove all the run-time calls to `core::slice::from_raw_parts(x, 32)`.

If you disagree that's fine. I don't have a strong opinion on this. I think this PR is warranted separately because the borrowed pointers seem extra unsafe.

## jeffro256 | 2025-07-17T03:37:31+00:00
Ah okay I see, so in the C API you're not actually passing a fixed size of 32 as a function parameter, just modifying the function signature on the Rust side. Yeah, I'm fine with that. The safety is basically the same IMO, but it does add extra information for the reader. I'd support that change.

> And then instead of taking a raw pointer here, use a `[u8; 32]` instead.

Not to be that guy, but wouldn't it be a `&[u8; 32]`? Rust actually passes arrays by value, unlike C which decays them into pointers.

## j-berman | 2025-07-17T03:58:37+00:00
> Not to be that guy, but wouldn't it be a &[u8; 32]?

You're right. And also nevermind, apparently it's not safe: https://users.rust-lang.org/t/creating-fixed-size-slice-from-pointer-passed-from-c/52717/10

## jeffro256 | 2025-07-17T04:22:34+00:00
I'm sort of confused by the linked post. Is that not a valid way of passing an array from C to Rust? What is "FFI safe"? Maybe it's not *safe* in the Rust sense b/c C doesn't have Rust's borrow checker, but that's about as safe as it gets unless the linked guy is talking about passing the array by value.

## jeffro256 | 2025-07-17T04:27:51+00:00
Like you *could* define a C struct `struct Buf64 { unsigned char buf[64]; };` and it's Rust analogue, and pass the struct containing the array by value. Maybe that's what he means. The downside of that is that that probably entails a stack-to-stack copy at the ABI level. Also it's ugly as hell. AFAIK, having a `&[u8; 32]` parameter on the Rust side is fine as long as you assume the C side passes a pointer to an area of 32 bytes which is readable.

## jeffro256 | 2025-07-17T04:28:17+00:00
But I guess we can continue this discussion elsewhere too.. 

# Action History
- Created by: j-berman | 2025-06-20T17:52:35+00:00
- Merged at: 2025-07-17T04:28:23+00:00
