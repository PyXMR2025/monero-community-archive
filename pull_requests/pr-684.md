---
title: 'RPC: Connection limit and fail-ban protection (hardening pt.2)'
source_url: https://github.com/Cuprate/cuprate/pull/684
author: SyntheticBird45
assignees: []
labels:
- A-binaries
created_at: '2026-08-13T00:17:12+00:00'
updated_at: '2026-08-31T18:21:47+00:00'
type: pull_request
status: open
closed_at: null
merged_at: null
---

# Original Description
### What

This PR follows and is based upon https://github.com/Cuprate/cuprate/pull/675

It adds per-IP type simultaneous connection limits similar to https://github.com/monero-project/monero/pull/9765. This PR extends this mitigation by adding a distinction between loopback addresses and private addresses, as well as enabling user to exclude specific IP addresses from any restriction if needed (main production case would be reverse proxies).

This PR also adds a short-term ban mechanism for IP addresses that repeatedly fails to be served by the hyper stack (axum erros are unaccounted). Said banned IP addresses are immediately dropped until they are unbanned.

# Discussion History
## redsh4de | 2026-08-14T07:25:07+00:00
Add a `is_excluded` accessor so that the exclusion check can move to call sites:

```suggestion
    /// Whether this IP address is excluded from all limits.
    fn is_excluded(&self, remote_addr: IpAddr) -> bool {
        self.excluded_ips.contains(&remote_addr)
    }
```

## redsh4de | 2026-08-14T07:27:15+00:00
The fail-ban path never checks `excluded_ips`, so a reverse proxy - the one IP every client's failures funnel through - is the first thing banned. Gate both `select!` arms instead:

```suggestion
                    let ip = remote_addr.ip();

                    // Excluded IPs bypass the ban and all connection limits.
                    if !self.ip_limit_cache.is_excluded(ip) {
                        // Check the ban, then the total and per-IP connection limits.
                        if self.ip_limit_cache.is_ip_banned(ip)
                            || !self.ip_limit_cache.check_and_track_connection(ip)
                        {
                            continue;
                        }
                    }
```

## redsh4de | 2026-08-14T07:28:19+00:00
```suggestion
                    if self.ip_limit_cache.is_excluded(ip) {
                        continue;
                    }

                    // Untrack connection from IP
                    self.ip_limit_cache.remove_connection(&ip);
```

With the gate in both arms, the internal checks in `check_and_track_connection` and `remove_connection` become redundant and can be removed

## redsh4de | 2026-08-14T07:30:01+00:00
```suggestion
    /// Check the connection limit of the remote IP address, increase it and
    /// return `true` if allowed, `false` if disallowed.
    ///
    /// Excluded IP addresses must be filtered out by the caller.
    fn check_and_track_connection(&mut self, remote_addr: IpAddr) -> bool {
```

## redsh4de | 2026-08-14T07:30:22+00:00
```suggestion
    /// Remove a connection of the remote IP address.
    ///
    /// Excluded IP addresses must be filtered out by the caller.
    fn remove_connection(&mut self, remote_addr: &IpAddr) {
```

## Boog900 | 2026-08-31T18:06:28+00:00
I think this is needed
```suggestion
                    let ip = remote_addr.ip().to_canonical();
```

## Boog900 | 2026-08-31T18:07:55+00:00
We should probably log here when this happens 

# Action History
- Created by: SyntheticBird45 | 2026-08-13T00:17:12+00:00
