---
title: 'cuprated: `killswitch` and `version` module'
source_url: https://github.com/Cuprate/cuprate/pull/365
author: hinto-janai
assignees: []
labels:
- A-dependency
- A-workspace
- A-binaries
- A-constants
created_at: '2025-01-17T14:40:45+00:00'
updated_at: '2025-01-21T19:11:49+00:00'
type: pull_request
status: merged
closed_at: '2025-01-21T19:02:11+00:00'
merged_at: '2025-01-21T19:02:11+00:00'
---

# Original Description
### What
- Adds a killswitch mechanism for releases before v1
- Changes `--version` to output more information in JSON

### Why
```
Tue, Jan 14, 2025, 18:28:22 - hinto: I've also considered not releasing binaries in case `cuprated` exposes major network problems, no binaries would a hacky but real limiter to that problem
Tue, Jan 14, 2025, 18:29:02 - hinto: or if `cuprated` itself is the issue
Tue, Jan 14, 2025, 18:30:04 - SyntheticBird: The `what is important / what should be focused on in Cuprate` is going to be interesting discuss (to re discuss in an extent).
Tue, Jan 14, 2025, 18:30:16 - boog900: yeah we will need to stress that alpha means don't trust with anything critical
Tue, Jan 14, 2025, 18:31:05 - Rucknium: Zcash has a different release philosophy. At least with the old C++ zcashd, old releases had an intentional killswitch after their "expiration date" ev
en without a hard fork. Just to put it in context.
Tue, Jan 14, 2025, 18:32:02 - boog900: Oh yeah that was another idea, we could put a killswitch in the alpha binary to force people to update.
```
```
Thu, Jan 16, 2025, 21:28:13 - hinto: After reading https://github.com/monero-project/monero/pull/9668#issuecomment-2594104964 I think it may be a good idea to open up the security discussion
and gather wider consensus before releasing a v1 `cuprated` binary. If the security scope is being set at a malicious upstream or miscompilation then I think `cuprated` could represent a much
 more real and practical tool to be used by a malicious actor if something like a chain split bug were to be found.\

For reference, there were audits for `reth` and `zebrad` before v1 release:
- https://github.com/paradigmxyz/reth/blob/main/audit/sigma_prime_audit_v2.pdf
- https://www.nccgroup.com/media/bw3nq0vm/_ncc_group_zcashfoundation_e005955_report_2023-06-27_v10.pdf

For the alpha build, releasing build instructions / documentation should be okay without further review...? An argument could be made that it's not difficult to use an alpha `cuprated` for th
e same attacks but then again there's probably cheaper and more profitable ways to attack the network?
Thu, Jan 16, 2025, 21:30:21 - Mαlοri: I agree with the comment
Thu, Jan 16, 2025, 21:30:54 - hinto: Regardless I think the killswitch is a good idea, perhaps just a thread that slowly loops on `assert!(current_timestamp() < some_timestamp)` sounds ok?
Thu, Jan 16, 2025, 21:31:51 - SyntheticBird: fwiw I would like to avoid the killswitch to kill the node at runtime but rather block at next start
Thu, Jan 16, 2025, 21:32:12 - boog900:  no it should kill at runtime IMO
```

# Discussion History
## hinto-janai | 2025-01-17T21:56:49+00:00
1/6

Killswitch module/reasoning.

## hinto-janai | 2025-01-17T22:10:03+00:00
2/6

This module should be removed after v1 release, this helps remind us.

## hinto-janai | 2025-01-17T22:10:09+00:00
3/6

The killswitch timestamp. Upon reaching this timestamp, `cuprated` will `std::process::exit`.

We can decide and change the value before each release.

## hinto-janai | 2025-01-17T22:10:34+00:00
4/6

The killswitch activates in 2 ways:

1. On initial startup
1. During runtime

This line checks if we're already ahead of the timestamp and will exit if so.

> timedatectl set-ntp false
> timedatectl set-time 2025-03-02
> date -u

```
Sun Mar  2 05:00:01 AM UTC 2025
```

> cargo run --bin cuprated

```
killswitch: The killswitch activation timestamp for alpha builds has passed (current_ts: 1740891602, killswitch_activation_timestamp: 1740805200). `cuprated` will now exit. For more details on why this exists, see: <https://github.com/Cuprate/cuprate/pull/365>.
```

## hinto-janai | 2025-01-17T22:10:46+00:00
5/6

Else we exit at runtime when we reach the timestamp.

## hinto-janai | 2025-01-17T22:11:42+00:00
6/6

This guards against a system with a messed up system clock from evading the killswitch.


> timedatectl set-ntp false
> timedatectl set-time 2010-03-06
> date -u

```
Sat Mar  6 05:00:01 AM UTC 2010
```

> cargo run --bin cuprated

```
killswitch: The system clock is too far behind and is not reliable to use (current_ts: 1267851602, killswitch_activation_timestamp: 1740805200). `cuprated` will now exit. For more details on why this exists, see: <https://github.com/Cuprate/cuprate/pull/365>.
```

## hinto-janai | 2025-01-17T22:52:23+00:00
This replaces the `--version` output for:
1. More info
2. JSON (for easier interopt)

I'll eventually add some docs for this in the user book.

This is added in this PR because we can add killswitch info.

```bash
cargo run --bin cuprated -- --version
```
```json
{
  "major_version": 0,
  "minor_version": 0,
  "patch_version": 1,
  "rpc_major_version": 3,
  "rpc_minor_version": 14,
  "rpc_version": 196622,
  "hardfork": 16,
  "blockchain_db_version": 0,
  "semantic_version": "0.0.1",
  "build": "debug",
  "commit": "fb6ec1710b64c8a822aea227c0140449a681b94a",
  "killswitch_timestamp": 1740805200
}
```

## SyntheticBird45 | 2025-01-17T23:00:31+00:00
Is it possible to instead use the [`env` macro](https://doc.rust-lang.org/std/macro.env.html) to collect the timestamp set by [build.rs cargo::rustc-env=VAR=VALUE](https://doc.rust-lang.org/cargo/reference/build-scripts.html#rustc-env) and add the desired period to it. No need to update the timestamp manually after that

## Boog900 | 2025-01-17T23:28:27+00:00
could we make this an async task so we don't need another thread? We would have to move the `init_killswitch` after the tokio runtime is initialized 

## hinto-janai | 2025-01-18T00:21:52+00:00
I'm ok with editing this before releases if they are every 6-8 weeks. FWIW I don't think making it easier to bypass this is good but here's a patch for `env!()` + `build.rs`, thoughts?:
```diff
diff --git a/binaries/cuprated/build.rs b/binaries/cuprated/build.rs
new file mode 100644
index 00000000..ae7129e7
--- /dev/null
+++ b/binaries/cuprated/build.rs
@@ -0,0 +1,20 @@
+fn main() {
+    set_killswitch_timestamp_env();
+}
+
+/// This sets the git `CUPRATED_KILLSWITCH_TIMESTAMP` environment variable.
+fn set_killswitch_timestamp_env() {
+    // If the env variable is set, use it.
+    let timestamp: u64 = if let Some(timestamp) = option_env!("CUPRATED_KILLSWITCH_TIMESTAMP") {
+        timestamp
+            .parse()
+            .expect("Could not parse CUPRATED_KILLSWITCH_TIMESTAMP as u64.")
+    } else {
+        // Else use a fallback.
+        //
+        // Sat Mar 01 2025 05:00:00 GMT+0000
+        1740805200
+    };
+
+    println!("cargo::rustc-env=CUPRATED_KILLSWITCH_TIMESTAMP={timestamp}");
+}
diff --git a/binaries/cuprated/src/killswitch.rs b/binaries/cuprated/src/killswitch.rs
index 7ce29af1..54e0445a 100644
--- a/binaries/cuprated/src/killswitch.rs
+++ b/binaries/cuprated/src/killswitch.rs
@@ -12,7 +12,7 @@
 //! This behavior is limited to an alpha build;
 //! this module will be removed after a stable v1 release.
 
-use std::{process::exit, time::Duration};
+use std::{process::exit, sync::LazyLock, time::Duration};
 
 use cuprate_helper::time::current_unix_timestamp;
 
@@ -25,28 +25,37 @@ const _: () = {
     );
 };
 
+/// A timestamp known to have been passed.
+///
+/// This is an arbitrary timestamp used for
+/// sanity checking the system's clock to make
+/// sure it is not overly behind.
+///
+/// Fri Jan 17 2025 14:19:10 GMT+0000
+const SYSTEM_CLOCK_SANITY_TIMESTAMP: u64 = 1737123550;
+
 /// The killswitch activates if the current timestamp is ahead of this timestamp.
 ///
-/// Sat Mar 01 2025 05:00:00 GMT+0000
-pub const KILLSWITCH_ACTIVATION_TIMESTAMP: u64 = 1740805200;
+/// This env variable is set in `cuprated`'s `build.rs`.
+pub static KILLSWITCH_TIMESTAMP: LazyLock<u64> = LazyLock::new(|| {
+    let timestamp = env!("CUPRATED_KILLSWITCH_TIMESTAMP")
+        .parse()
+        .expect("Could not parse killswitch timestamp as u64");
+
+    assert!(timestamp > SYSTEM_CLOCK_SANITY_TIMESTAMP);
+
+    timestamp
+});
 
 /// Check if the system clock is past a certain timestamp,
 /// if so, exit the entire program.
 fn killswitch() {
-    /// A timestamp known to have been passed.
-    ///
-    /// This is an arbitrary timestamp used for
-    /// sanity checking the system's clock to make
-    /// sure it is not overly behind.
-    ///
-    /// Fri Jan 17 2025 14:19:10 GMT+0000
-    const SYSTEM_CLOCK_SANITY_TIMESTAMP: u64 = 1737123550;
-
     let current_ts = current_unix_timestamp();
 
     // Prints a generic killswitch message.
     let print_killswitch_msg = |msg| {
-        eprintln!("killswitch: {msg}. (current_ts: {current_ts}, killswitch_activation_timestamp: {KILLSWITCH_ACTIVATION_TIMESTAMP}). `cuprated` will now exit. For more details on why this exists, see: <https://github.com/Cuprate/cuprate/pull/365>.");
+        let timestamp =
+        eprintln!("killswitch: {msg}. (current_ts: {current_ts}, killswitch_activation_timestamp: {}). `cuprated` will now exit. For more details on why this exists, see: <https://github.com/Cuprate/cuprate/pull/365>.", *KILLSWITCH_TIMESTAMP);
     };
 
     if current_ts < SYSTEM_CLOCK_SANITY_TIMESTAMP {
@@ -54,13 +63,13 @@ fn killswitch() {
         exit(66);
     }
 
-    if current_ts > KILLSWITCH_ACTIVATION_TIMESTAMP {
+    if current_ts > *KILLSWITCH_TIMESTAMP {
         print_killswitch_msg("The killswitch activation timestamp for alpha builds has passed.");
         exit(88);
     }
 }
 
-/// Spawn a thread that sleeps until the [`KILLSWITCH_ACTIVATION_TIMESTAMP`] activates.
+/// Spawn a thread that sleeps until the [`KILLSWITCH_TIMESTAMP`] activates.
 pub fn init_killswitch() {
     // Check if we should exit immediately.
     killswitch();
@@ -69,7 +78,7 @@ pub fn init_killswitch() {
     std::thread::spawn(|| -> ! {
         // Sleep until killswitch activation.
         let current_ts = current_unix_timestamp();
-        let sleep_duration = Duration::from_secs(KILLSWITCH_ACTIVATION_TIMESTAMP - current_ts);
+        let sleep_duration = Duration::from_secs(*KILLSWITCH_TIMESTAMP - current_ts);
         std::thread::sleep(sleep_duration);
 
         // To account for any miscalculated or drifted sleep time,
diff --git a/binaries/cuprated/src/version.rs b/binaries/cuprated/src/version.rs
index 84540023..97202ea1 100644
--- a/binaries/cuprated/src/version.rs
+++ b/binaries/cuprated/src/version.rs
@@ -10,7 +10,7 @@ use cuprate_types::HardFork;
 
 use crate::{
     constants::{MAJOR_VERSION, MINOR_VERSION, PATCH_VERSION, VERSION},
-    killswitch::KILLSWITCH_ACTIVATION_TIMESTAMP,
+    killswitch::KILLSWITCH_TIMESTAMP,
 };
 
 #[derive(Clone, Debug, PartialEq, Eq, PartialOrd, Ord, Hash, Serialize, Deserialize)]
@@ -56,7 +56,7 @@ impl CupratedVersionInfo {
             semantic_version: VERSION,
             build: BUILD,
             commit: COMMIT,
-            killswitch_timestamp: KILLSWITCH_ACTIVATION_TIMESTAMP,
+            killswitch_timestamp: *KILLSWITCH_TIMESTAMP,
         }
     }
 }
```

## hinto-janai | 2025-01-18T00:40:11+00:00
Are you ok with config/CLI/logging stuff running too? The thread is suspended until exit too so it is light.

## Boog900 | 2025-01-18T01:08:01+00:00
I would be but yeah we may as well just keep the thread.

## Boog900 | 2025-01-18T01:10:26+00:00
IMO keeping it as a fixed constant is better, someone could just recompile the same code to get around this right? 

## hinto-janai | 2025-01-18T01:42:19+00:00
Yes, pull repo, edit const to a high number, build. Or comment out `init_killswitch()`.

## Boog900 | 2025-01-18T01:46:08+00:00
no I mean your patch all they have to do is just run `cargo build` to reset the timer. Yeah with the current code if you really want to you could reset/disable the timer manually but at that point you may as well just pull from the repo again.

## SyntheticBird45 | 2025-01-18T11:27:48+00:00
> FWIW I don't think making it easier to bypass this is good but here's a patch for env!() + build.rs, thoughts?:

I may be dumb on this one but I think you forgot to add the build timestamp and period to `CUPRATED_KILLSWITCH_TIMESTAMP`. `set_killswitch_timestamp_env()` literally just set manually `1740805200` (since `CUPRATED_KILLSWITCH_TIMESTAMP` env variable will never be set)

The idea was that the `else {` branch return `SystemTime::now().duration_since(UNIX_EPOCH).as_sec() + CUPRATED_TESTING_DURATION`.

> no I mean your patch all they have to do is just run cargo build to reset the timer.

Yes. Literally just have to rebuild it to actualize the timer. But now I see how it's not very useful as the alpha will not distribute binaries of any sort, so people will build manually and have different killswitch timestamp (no killswitch timestamp to an extent). PS: Gonna be very funny to see all the cuprate alpha node simultaneously disconnect from p2p network.

> I'm ok with editing this before releases if they are every 6-8 weeks.

This could be re discuss next-meeting fwiw, but as said prior, I find 6 weeks is too short, but 8 weeks seems like a good balance to me.

# Action History
- Created by: hinto-janai | 2025-01-17T14:40:45+00:00
- Merged at: 2025-01-21T19:02:11+00:00
