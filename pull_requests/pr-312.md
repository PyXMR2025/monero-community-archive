---
title: 'cuprated: txpool'
source_url: https://github.com/Cuprate/cuprate/pull/312
author: Boog900
assignees: []
labels:
- A-p2p
- A-dependency
- A-workspace
- A-storage
- A-binaries
created_at: '2024-10-13T01:25:00+00:00'
updated_at: '2024-10-29T15:30:56+00:00'
type: pull_request
status: merged
closed_at: '2024-10-29T15:30:52+00:00'
merged_at: '2024-10-29T15:30:52+00:00'
---

# Original Description

### What
The transaction pool



# Discussion History
## Boog900 | 2024-10-15T19:49:14+00:00
Here is the service that will handle incoming txs

## Boog900 | 2024-10-15T19:51:41+00:00
The `REORG_LOCK` is not enough to prevent  certain situations, we will need a lock for every blockchain mutation. This shouldn't be too bad IMO, every ~2 mins needing to take a write lock, read locks will only need to be taken when multiple services are involved in handling a request, 

## Boog900 | 2024-10-15T19:57:07+00:00
I decided to define a new way to identify txs, we just take the hash of the raw tx blob. This means we don't need to parse txs to find out if we already have them allowing us to discard the tx quicker.

This probably could be removed once all nodes support a protocol like this: https://github.com/monero-project/monero/issues/9334

Note for me later: we need to enforce a minimum tx blob size doing this.

## Boog900 | 2024-10-15T19:58:58+00:00
on a new block we discard txpool txs by key image not by hash, this is to prevent double spends staying in the pool.

## Boog900 | 2024-10-15T20:01:59+00:00
to be clear all this would allow is a double spend tx in the pool, and this would only happen in a very unlucky situation.

So not really an issue however IMO we should aim to be correct under all possible scenarios, being lax here might have consequence elsewhere.    

## Boog900 | 2024-10-15T20:02:51+00:00
Currently uses sha3, however it could be changed to something even more performant? #173 

## hinto-janai | 2024-10-17T00:00:56+00:00
```suggestion
    /// Returns `true` if the tx is in the stem stage.
```

## hinto-janai | 2024-10-17T00:01:19+00:00
```suggestion
    pub const fn is_state_stage(&self) -> bool {
```

## hinto-janai | 2024-10-17T00:06:24+00:00
```suggestion
/// Returns `true` if the transaction with the given hash is in the stem pool.
///
/// # Errors
/// This will return an [`Err`] if the transaction is not in the pool.
```

## hinto-janai | 2024-10-17T00:08:04+00:00
nit: `A request ...` comments are redundant, the type is `TxpoolReadRequest`.

## hinto-janai | 2024-10-17T00:09:22+00:00
Same here, also `TxBlob` should document what the corresponding request type is.

Also, `A response` -> `The response for [...]`

## hinto-janai | 2024-10-17T00:11:38+00:00
```suggestion
    // A closure that returns `true` if a tx with a certain blob hash is unknown.
```

## SyntheticBird45 | 2024-10-22T22:25:05+00:00
You know my answer to this one. Blake3 is the solution to all your hashing problems

## Boog900 | 2024-10-22T23:40:20+00:00
I changed to Blake3

## hinto-janai | 2024-10-25T00:38:46+00:00
Not at this line, but in this function we can enforce the `// From this point on...` comment in this function instead of relying on the programmer(s):

```rust
    // We must remove the block hash from `BLOCKS_BEING_HANDLED`.
    let _guard = {
        struct RemoveFromBlocksBeingHandled {
            block_hash: [u8; 32],
        }
        impl Drop for RemoveFromBlocksBeingHandled {
            fn drop(&mut self) {
                BLOCKS_BEING_HANDLED
                    .lock()
                    .unwrap()
                    .remove(&self.block_hash);
            }
        }
        RemoveFromBlocksBeingHandled { block_hash }
    };

    let (response_tx, response_rx) = oneshot::channel();

    incoming_block_tx
        .send(BlockchainManagerCommand::AddBlock {
            block,
            prepped_txs: txs,
            response_tx,
        })
        .await
        .expect("TODO: don't actually panic here, an err means we are shutting down");

    let res = response_rx
        .await
        .expect("The blockchain manager will always respond")
        .map_err(IncomingBlockError::InvalidBlock);

    res
```

## hinto-janai | 2024-10-25T00:40:22+00:00
Is there a reason this is defined here instead of in `cuprate-p2p`?

## hinto-janai | 2024-10-25T00:43:44+00:00
Can/should be moved to `IncomingTxHandler::init()`?

```rust
impl IncomingTxHandler {
    /// Initialize the [`IncomingTxHandler`].
    #[expect(clippy::significant_drop_tightening)]
    pub fn init(
        clear_net: NetworkInterface<ClearNet>,
        txpool_write_handle: TxpoolWriteHandle,
        txpool_read_handle: TxpoolReadHandle,
        blockchain_context_cache: BlockChainContextService,
        tx_verifier_service: ConcreteTxVerifierService,
    ) -> Self {
        let dandelion_router = dandelion::dandelion_router(clear_net);

        let dandelion_pool_manager = dandelion::start_dandelion_pool_manager(
            dandelion_router,
            txpool_read_handle.clone(),
            txpool_write_handle.clone(),
        );

        Self {
            txs_being_handled: txs_being_handled::TxsBeingHandled::new(),
            blockchain_context_cache,
            dandelion_pool_manager,
            tx_verifier_service,
            txpool_write_handle,
            txpool_read_handle,
        }
    }
}
```

## hinto-janai | 2024-10-25T00:46:52+00:00
If this is eventually exposed, it could/should be an associated constant on the type:

```rust
impl DandelionConfig {
    const CUPRATED_CONFIG: Self = /*...*/
}
```

## hinto-janai | 2024-10-25T00:58:50+00:00
```suggestion
use crate::txpool::dandelion::DandelionTx;
```

## hinto-janai | 2024-10-25T01:05:21+00:00
```suggestion
        let Ok(()) = self
            .clear_net_broadcast_service
            .call(BroadcastRequest::Transaction {
                tx_bytes: req.0 .0,
                direction: None,
                received_from: None,
            })
            .into_inner();
```

## hinto-janai | 2024-10-25T01:06:24+00:00
```suggestion
use crate::{p2p::CrossNetworkInternalPeerId, txpool::dandelion::DandelionTx};
```

## hinto-janai | 2024-10-25T01:12:30+00:00
part 1/2
```suggestion
        handle_incoming_txs(
            req
```

## hinto-janai | 2024-10-25T01:12:43+00:00
part 2/2
```suggestion
/// Handles the incoming txs.
async fn handle_incoming_txs(
    IncomingTxs { txs, state }: IncomingTxs,
```

## hinto-janai | 2024-10-25T01:17:24+00:00
```suggestion
            let tx_blob_hash = transaction_blob_hash(&tx_blob);
```

## hinto-janai | 2024-10-25T01:22:39+00:00
```suggestion
```

## hinto-janai | 2024-10-25T01:28:33+00:00
To be safe, shouldn't this `TxRw::abort().unwrap()` before early returning?

## Boog900 | 2024-10-26T21:40:44+00:00
defining it here means we are more free to change it without it being a breaking change + this isn't used in `cuprate-p2p`.

## Boog900 | 2024-10-26T21:41:20+00:00
I meant exposed to users of `cuprated`, I'll update the comment.

## SyntheticBird45 | 2024-10-29T10:47:32+00:00
I don't understand. What is the benefit of using the drop implementation instead of removing it directly from the LazyLock<Mutex<>>

## SyntheticBird45 | 2024-10-29T10:50:20+00:00
No that would be dangerous. If they want to experiment they can always tweak the constants

## SyntheticBird45 | 2024-10-29T10:51:15+00:00
These two durations and probability could be moved to `cuprate-constants` ?

## Boog900 | 2024-10-29T14:53:23+00:00
To make it harder for people in the future to make a mistake an early return before removing the hash

## Boog900 | 2024-10-29T14:54:07+00:00
Could but this is the only place it is used - IMO not worth it 

# Action History
- Created by: Boog900 | 2024-10-13T01:25:00+00:00
- Merged at: 2024-10-29T15:30:52+00:00
