---
title: '[on hold] Disposable Address using Payment ID'
source_url: https://github.com/monero-project/monero/pull/1345
author: kenshi84
assignees: []
labels: []
created_at: '2016-11-16T10:17:42+00:00'
updated_at: '2017-02-21T10:06:35+00:00'
type: pull_request
status: closed
closed_at: '2017-02-21T10:06:35+00:00'
merged_at: null
---

# Original Description
_**A new idea about sub-addresses was devised which also includes disposable addresses, so this PR should be halted in the meantime.
https://github.com/monero-project/research-lab/issues/7**_

[MRL-0006 draft](https://github.com/monero-project/research-lab/pull/5)

This is an implementation of the **one-time address** idea discussed earlier in [Reddit](https://www.reddit.com/r/Monero/comments/58oizb/). My personal motivation primarily comes from the fact that I must tell my wallet address `(A,B)` to ShapeShift every time I buy XMR there.

To prevent ShapeShift from knowing the total amount of XMR I bought through them, I implemented a scheme where ShapeShift is given a one-time address `(C,D)=(hA,hB)` along with a 256bit payment ID `k` where `h=hash(a,k)`. The two addresses `(A,B)` and `(C,D)` cannot be linked without knowing the secret viewkey `a`. When a wallet (or a watch-only wallet `(a,B)`) processes a new transaction having a 256bit payment ID, it performs the output key derivation also against `(C,D)` to see if the fund belongs to it where the corresponding secret view & spend keys are `(ha,hb)`.

To reduce the additional computational cost incurred in processing new transactions, the wallet generates a one-time address with random `k` such that the following equation holds: `(hash(a,k)-k) mod 256 == 0` which is found by brute force. This trick allows the wallet to ignore all payment IDs not satisfying the equation, reducing the performance impact to roughly 1/256 or 0.4% of the original cost with the naive implementation.

### One-time integrated address
One nice thing about the above scheme is that we don't need to ask ShapeShift to upgrade their software or anything, since the sender's procedure is exactly the same as before (i.e. transfer with 256bit payment ID). However, a downside is that it is cumbersome and error-prone to have to specify the 256bit payment ID separately.

To solve this problem, I took the same approach as the integrated address where a 64bit payment ID is integrated into the address. The integrated address itself cannot be used as-is, though, because the 64bit payment ID is stored in the transaction after being encrypted with the shared secret `rA=aR`. In our case, the sender would use the shared secret `rC` which equals to `hash(a,k)aR` to encrypt `k`; i.e., the receiver needs to know `k` in order to decrypt the encrypted `k`, which is impossible. Therefore, in order for our scheme to work, the integrated 64bit payment ID `k` should be stored in the transaction *unencrypted*. This necessitates the introduction of a new type of address similar to the integrated address, where the sender puts `k` into the transaction data without encryption. When a wallet sees a new transaction with a 64bit payment ID, it treats the value as potentially the unencrypted `k` and performs the additional key derivation step as described above.

Two new commands `onetime_address_classic` and `onetime_address` are introduced. As is the case with the current practice, it's definitely discouraged to reuse the same one-time address (i.e. the same unencrypted payment ID) which obviously leads to linkages among transactions.

### Example
```
[wallet 49Nppc]: address
49Nppc2PHjDhbrhwW8UmtVGAW5RPKKBX1cbYEUASk6qdGVoZVaM1hot86ZPnKiVhb5E2svSDc2zLobHVTKkscczhSkYbERz
[wallet 49Nppc]: integrated_address
Random payment ID: <6cf06151b36851e7>
Matching integrated address: 4K5VqQqstzjhbrhwW8UmtVGAW5RPKKBX1cbYEUASk6qdGVoZVaM1hot86ZPnKiVhb5E2svSDc2zLobHVTKkscczhfD1omn2xS5WT718GZ4
[wallet 49Nppc]: onetime_address_classic
One-time address: 
432u5kgFw8X9p6KwUYbxhB255Qjad7Cj9JSdx1FVgkzq1efmiruMw19JrcADDGFpJFbqKiTYbmKsp1KeHGaVRKWVPREHoZC
Payment ID: 
<6685e62a0906106b072a844d4147362e6df380ba1601d7f6f03bbcdfcfb8c784>
IMPORTANT: If the sender forgets to attach the payment ID, you will N
OT see the fund!
[wallet 49Nppc]: onetime_address
One-time integrated address: 
4UEteoX8YitA4AKwMRN6hr4Tb8V6JM6BFXM86Sv9bk2eRLYtn4xkMkF47L92HcJFTXQVE1dUzQ5aRDoJBPXhm3PWbiYNg6hbn2gLE7XnUB
Payment ID integrated in the above: <8e0321662140e5aa>
```

Notice how the real address is included in the integrated address, while one-time addresses are entirely different from the real address.


### ~~Known issue (on Mac OS X 10.11.6)~~
~~If you create a watch-only wallet of an address which received funds using one-time addresses and do `import_key_images`, the wallet almost always crashes with an annoying error `"incorrect checksum for freed object - object was probably modified after being freed."` Funny thing is that it *occasionally* succeeds in importing key images (maybe 5% chance) on testnet (no hope with mainnet). I have absolutely no idea how to fix this.~~
**Bug fixed (26 Dec 2016)**

----
### ToDo list:
- agree on design choices
-- ~~whether to abandon `onetime_address_classic` or not~~ &rarr; **abandoned**
-- ~~whether to generate pIDs randomly or deterministically~~ &rarr; **randomly**
-- ~~terminology (one-time address vs disposable address vs ...)~~ &rarr; **use disposable**
- ~~replace scalar multiplication by scalar addition (suggested by Luigi)~~ &rarr;**done**
- ~~cold wallet & offline signing~~ &rarr;**bug fixed**
- ~~mechanism to prevent outputs received by one-time addresses from being spent together~~ &rarr; **future work** (seems highly related to the 'churning' concept proposed by knaccc)
-- http://termbin.com/639f
- ~~make it work with RPC~~ &rarr; **done**
- make it work with GUI
- unit tests?
- testing period on testnet

# Discussion History
## kenshi84 | 2016-11-17T04:21:51+00:00
I'm worried about potential inconsistencies in the code outside simplewallet that uses `wallet2::commit_tx(ptx)`, specifically:

src/wallet/api/pending_transaction.cpp 
src/wallet/wallet_rpc_server.cpp 

I've never touched the RPC server etc., so I have no idea what side effects I might have caused.


## kenshi84 | 2016-11-18T05:17:03+00:00
Note: this prevents the encryption for the one-time mode.


## moneromooo-monero | 2016-11-19T10:07:56+00:00
why remove this ?


## moneromooo-monero | 2016-11-19T10:11:50+00:00
That seems wrong. The wallet tx creation should set this if it creates such a tx. The flag is passed to it already.


## moneromooo-monero | 2016-11-19T10:12:14+00:00
same comment as above


## moneromooo-monero | 2016-11-19T10:24:06+00:00
I'd rather see two booleans (received, and onetime, eg), makes things a lot more understandable


## moneromooo-monero | 2016-11-19T10:27:21+00:00
Does not set has_\* to false if nothing found in extra


## moneromooo-monero | 2016-11-19T10:29:14+00:00
Could use 2*HASH_SIZE as the other code does. Also some static_assert that HASH_SIZE equals size of keys and scalars would be nice, since all this code assumes it.


## moneromooo-monero | 2016-11-19T10:30:23+00:00
I think 0.4% of non-onetime payment ids will also trigger this. This will just cause the onetime test to be done, but be otherwise fine, right ?


## moneromooo-monero | 2016-11-19T10:43:32+00:00
needs to be cref


## moneromooo-monero | 2016-11-19T10:45:15+00:00
Loads of gratuitous reformatting here and below. Please undo.


## moneromooo-monero | 2016-11-19T10:50:50+00:00
This stops caring about errors.


## moneromooo-monero | 2016-11-19T10:52:14+00:00
Please don't add arbitrary reformatting


## moneromooo-monero | 2016-11-19T10:56:41+00:00
his should probably init to null_hash if nothing found, just to be clean


## moneromooo-monero | 2016-11-19T10:57:16+00:00
This should also initialize to null_hash id not found


## moneromooo-monero | 2016-11-19T11:02:10+00:00
init to null_hash if nothing found


## moneromooo-monero | 2016-11-19T11:02:46+00:00
Did this error out if you did not add :: ?


## kenshi84 | 2016-11-19T13:01:55+00:00
Because the same thing appears at [#L1011](https://github.com/kenshi84/monero/blob/db8cfa220a99ba6d61f2a087b2db1c6585507c25/src/simplewallet/simplewallet.cpp#L1011). Isn't this line redundant?


## kenshi84 | 2016-11-19T13:11:54+00:00
Yes, that's intentional. The purpose of this equation is to reduce the set of payment IDs to be tested for this one-time scheme to 0.4% of the entire space of 64bit payment IDs.


## kenshi84 | 2016-11-19T13:14:04+00:00
has_\* are set to false by default, see [L626](https://github.com/kenshi84/monero/blob/db8cfa220a99ba6d61f2a087b2db1c6585507c25/src/wallet/wallet2.cpp#L626)


## kenshi84 | 2016-11-19T13:21:02+00:00
Sorry, I'll undo them.
(I personally like to use spaces instead of tabs for indentation, nvm)


## kenshi84 | 2016-11-19T13:32:03+00:00
OK I'll change it.


## kenshi84 | 2016-11-19T13:32:27+00:00
sorry


## kenshi84 | 2016-11-19T14:08:22+00:00
I did this because I thought simply assigning `ec_point` from one variable to another wouldn't copy the `char data[32]`, but now I realized it does. So this should be unnecessary, it can be simply written as `dst = src`. Correct?

I'm so used to using `std::vector` or `std::array` and didn't familiarize myself enough with raw arrays... (I'm surprised because raw arrays themselves cannot be copied directly.)


## kenshi84 | 2016-11-19T14:13:21+00:00
OK


## kenshi84 | 2016-11-19T14:13:36+00:00
ok


## kenshi84 | 2016-11-19T14:15:20+00:00
ok


## kenshi84 | 2016-11-19T14:21:48+00:00
Yes, because I defined the function `crypto::hash_to_scalar()` in crypto.h and crypto-tests.h has a global scope function `::hash_to_scalar()`, so the compiler gets confused which to use due to `using namespace crypto`.


## kenshi84 | 2016-11-19T14:24:48+00:00
ok


## kenshi84 | 2016-11-19T14:38:53+00:00
Could be. I did this in order to avoid decrypting payment ID in `wallet2::get_payment_id()` when in the one-time mode, but actually I don't really know how `wallet2::commit_tx()` works. In that function, `payment_id` seems to be used only in `wallet2::add_unconfirmed_tx()`, but what's the purpose of this function?


## kenshi84 | 2016-11-19T14:44:02+00:00
ok


## moneromooo-monero | 2016-11-19T22:02:12+00:00
It looks like it is.


## moneromooo-monero | 2016-11-19T22:04:54+00:00
This should be irrelevant to get_payment_id. Functions like create_transactions_2 fill up the ptx set, and should set the is_onetime from the passed parameter. This is done just before in the timeline, so get_payment_id does not need to change.
add_unconfirmed_tx saves data about an outgoing tx that cannot be recovered from the blockchain (for examaple, destination addresses). It could also save short payment_id, since this is not decryptable after the fact by the sender (except if the sender is the receiver).


## moneromooo-monero | 2016-11-19T22:06:44+00:00
I much prefer spaces too, but I prefer even more small changes, they're easier to review, cause less conflicts, etc.


## moneromooo-monero | 2016-11-19T22:08:39+00:00
I see it. Very strange.


## moneromooo-monero | 2016-11-19T22:09:13+00:00
dst = src would be fine, yes. IIRC, copying of structs/classes defaults to memcpy, which is correct in this case.


## kenshi84 | 2016-11-20T01:31:47+00:00
Ah, I see what you mean. Then I guess I need to put `ptx.is_onetime = is_onetime;` into the end of `wallet2::transfer<T>()` and `wallet2::transfer_selected()` and `wallet2::transfer_selected_rct()`, correct?

BTW, overloads to `wallet2::transfer()` without args `cryptonote::transaction tx` and `pending_tx ptx` seem to be not used anywhere. Aren't they redundant?


## kenshi84 | 2016-11-20T02:24:24+00:00
I'm surprised that boost::bind produces error when the number of function arguments is more than 9. Seriously? :/
As a workaround, I'll introduce a new struct `wallet2::is_received_info` containing two bools.


## kenshi84 | 2016-11-20T02:39:42+00:00
ok


## kenshi84 | 2016-11-21T13:44:38+00:00
Minor note: shouldn't the second arg be const reference as `const crypto::public_key& pub`?

## moneromooo-monero | 2016-11-23T21:10:14+00:00
It should.

## kenshi84 | 2017-01-15T13:28:40+00:00
I screwed up here previously by doing

    memset(&pID_info.payment_id32 + 8, 0, 24);

Lesson: be careful with data types when taking addresses and adding offsets to them!

## kenshi84 | 2017-01-15T13:33:14+00:00
I simply passed `false` to the `is_onetime` parameter of `construct_tx_and_get_tx_key ` in this and other tests for now. Will look into the test code later, and probably will write some tests for this one-time scheme.

## kenshi84 | 2017-01-15T13:34:38+00:00
`construct_tx` seems to be used only by tests, and here I assume it's safe to pass `false` to the `is_onetime` parameter of `construct_tx_and_get_tx_key`. Will look into the test code later.

## moneromooo-monero | 2017-01-15T13:50:12+00:00
It is a thin convenience shell so I could avoid modifying all the tests with boilerplate when I introduced the new paraemeter, and false should be fine, yes.

## moneromooo-monero | 2017-01-21T09:14:13+00:00
Are those two lines the same as: secret_key_to_public_key ?

## moneromooo-monero | 2017-01-21T09:22:49+00:00
Are those casts really needed ? onetime_c_real is already a ec_scalar.

For luigi: I'm not sure I know what I'm talking about here, but might this need a constant salt, like for the encrypted payment ids ?

## moneromooo-monero | 2017-01-21T09:30:37+00:00
Would sending a tx to a one time address and (either a different one time address or a non one time address) be OK ? If not, there needs to be checks to prevent this, similar to different payment ids are forbidden.

## moneromooo-monero | 2017-01-21T09:32:02+00:00
Same check should be made in the GUI (or, better, to the wallet2 API, but I'm not sure it can tell). Jaquee or myself will do it later, just noting it down for reference.

## moneromooo-monero | 2017-01-21T09:37:50+00:00
Do we want to keep this long hash version ?

## moneromooo-monero | 2017-01-21T09:40:33+00:00
Should test for error

## moneromooo-monero | 2017-01-21T09:43:38+00:00
else received.one_time = false;

## moneromooo-monero | 2017-01-21T09:55:48+00:00
misc not is not initialized if no extra nonce found

## moneromooo-monero | 2017-01-21T09:57:17+00:00
You could just always use data when you need a pointer. Also avoids the extra cast to unsigned char* which you  have in the first part of the patch.

## moneromooo-monero | 2017-01-21T09:59:51+00:00
It's not set to NULL otherwise (except maybe in the caller, but you don't really want to put that burden on every single caller)

## moneromooo-monero | 2017-01-21T10:05:20+00:00
This seems to assume you can't receive both in a single tx, which is not true

## moneromooo-monero | 2017-01-21T10:35:56+00:00
This seems wrong. I think It should be:
    if (in_ephemeral.pub != pkey)
    {
      in_ephemeral = in_ephemeral2;
      ki = ki2;
    }

## moneromooo-monero | 2017-01-21T10:38:39+00:00
This bit of code is here three times. It should be moved to its own function.


## kenshi84 | 2017-01-21T10:49:45+00:00
No, because this can be called in the watch-only mode where `ack.m_spend_secret_key` is set to zero. The second line is needed to recognize incoming transfers.

## kenshi84 | 2017-01-21T10:52:47+00:00
You're right, these casts are unnecessary.

## kenshi84 | 2017-01-21T10:57:31+00:00
It's not OK to provide multiple one-time integrated addresses as destinations. I think such cases are already rejected by the current mechanism for preventing use of multiple integrated addresses as destinations or use of explicit payment ID for a tx with an integrated address being one of the destinations.

## kenshi84 | 2017-01-21T10:59:15+00:00
Maybe not :)
Even if this feature is dropped officially, any motivated user can easily bring it back and use their customized software.

## kenshi84 | 2017-01-21T11:07:03+00:00
It's default initialized as false, defined in wallet2.h:
```
struct is_received_info {
  bool standard = false;
  bool onetime = false;
};
```
Is this a bad practice?

## moneromooo-monero | 2017-01-21T11:15:31+00:00
No, it is not bad practice per se. I can't see where that code is because github won't let me  see more context, but if it's not a local variable (and thus you know it was initialized), then it should be set, because the caller may have done whatever with it before. If it's a local var declared just above, then it's fine as is. But in general, I like inits to be explicit when a test is done. Saves on uncertainty and bugs if/when code changes elsewhere.

## kenshi84 | 2017-01-21T11:46:49+00:00
Well, like above with `is_received_info`, all fields of `pID_info` are default initialized as:
```
struct payment_id_info {
  tx_extra_nonce extra_nonce;
  bool has_payment_id32 = false;
  bool has_payment_id8  = false;
  crypto::hash  payment_id32 = null_hash ;
  crypto::hash8 payment_id8  = null_hash8;
  crypto::secret_key* onetime_c = nullptr;
  crypto::secret_key  onetime_c_real;
};
```
Should I stop this and use your style?

## kenshi84 | 2017-01-21T12:08:18+00:00
No, it doesn't make such an assumption. Why do you think it implies that?

FYI, `received.standard` and `received.onetime` cannot be both true simultaneously for the same tx and the same output index.

## kenshi84 | 2017-01-21T12:10:35+00:00
ok

## kenshi84 | 2017-01-21T12:23:50+00:00
ok

## kenshi84 | 2017-01-21T12:24:29+00:00
ok

## kenshi84 | 2017-01-21T12:41:26+00:00
Cast `(unsigned char*)` here is really necessary. If I use `key.data`, the compiler emits an error: `no known conversion from 'char [32]' to 'unsigned char *'`

## kenshi84 | 2017-01-21T12:44:19+00:00
I stop using default initialization of structs' member data.

## kenshi84 | 2017-01-21T13:20:34+00:00
The repetitive code is moved to `set_unencrypted_payment_id_from_tx_extra(const vector<uint8_t>&, tx_source_entry&)` in `cryptonote_format_util`.

## kenshi84 | 2017-01-21T13:32:05+00:00
@moneromooo-monero @luigi1111 
Please confirm that I got this right.

## moneromooo-monero | 2017-01-22T11:12:35+00:00
is_onetime is function level, and will get used to decide whether to decrypt the payment id or not. So if you receive both a onetime output and a non onetime output, is_onetime gets set to true and the payment id doesn't get decrypted. I suppose this will "never happen" in practice, since there should be checks in the wallet to never create such txes, but they're valid nonetheless.

## kenshi84 | 2017-01-22T12:11:35+00:00
If I transfer funds to a certain address and also to its one-time address simultaneously (which is totally legit to do), the receiver will see both non-onetime and onetime outputs in this transaction. If the one-time integrated address was used, the payment ID stored in the wallet would be the integrated short payment ID as-is (without decryption). What problem do you see?

## moneromooo-monero | 2017-01-22T13:59:14+00:00
That the payment id would not decrypted if there was a non-onetime output sent with one.

## kenshi84 | 2017-01-22T14:40:28+00:00
You mean the short payment ID carried by an integrated address? That case is irrelevant here, since one transaction can contain only one payment ID. If the set of destination addresses of a transaction contains a one-time integrated address, it cannot contain any integrated addresses. The same goes with the 'classic' one-time address with long payment ID (even though it's not officially supported anymore). The decryption of short payment ID is always unnecessary when there are any one-time outputs in the transaction. Am I misunderstanding something?

## moneromooo-monero | 2017-01-22T18:37:07+00:00
No, this is correct, assuming all senders have such checks. Their problem if they don't I suppose. Would be nice to ensure this is indeed checked for, but I guess I'll have to pull and go check then.

## kenshi84 | 2017-01-23T01:09:19+00:00
Is it possible at all to construct a transaction that contains multiple payment IDs? I thought it's illegal at the protocol level.

## moneromooo-monero | 2017-01-23T08:42:34+00:00
It is not illegal at the protocol level. There is just code in the wallet (not the daemon) to validate a transfer's parameters before it is made, and while it might already prevent sending to a one time address and using a payment id, that's what I am trying to say needs to be checked.

## kenshi84 | 2017-01-23T11:46:21+00:00
Wow, that's surprising. In the current implementation, payment IDs are extracted on the per-transaction basis using `parse_tx_extra` and `find_tx_extra_field_by_type` etc. If any weird wallet implementation put multiple payment IDs into a single tx, the official wallet implementation will only recognize one of them (which one? I wonder), so such attempts would be largely useless in my opinion. Do you still think it needs to be checked? And how can it be checked?

## moneromooo-monero | 2017-01-23T13:21:30+00:00
The wallet will see only the first one now. The whole tx_extra is not checked for consensus (apart from it deserializing correctly as a string). I don't think it should be part of the consensus without thinking it through first. This field should be able to be used to carry "arbitrary" metadata, which could enable nice things like colored coins (I'm handwaving here).

## kenshi84 | 2017-01-23T13:34:08+00:00
Still I'm confused, what should be done here after all? Should we consider cases where multiple payment IDs are stored in a given transaction?

## moneromooo-monero | 2017-01-23T13:51:33+00:00
Not really. Just make sure that such txes cannot be made by the current implementation.

## luigi1111 | 2017-01-26T17:56:13+00:00
Looks ok.

## luigi1111 | 2017-01-26T18:04:24+00:00
This breaks the refresh optimization (the reason it's called "precomp"). I would prefer that we didn't regress performance there, even if it's somewhat less of a concern with RingCT (only 2 outputs in most cases).

# Action History
- Created by: kenshi84 | 2016-11-16T10:17:42+00:00
- Closed at: 2017-02-21T10:06:35+00:00
