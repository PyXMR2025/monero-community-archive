---
title: 'FCMP++: CurveTrees class & implementation of ed25519 outputs -> curve tree
  leaves function'
source_url: https://github.com/monero-project/monero/pull/10360
author: j-berman
assignees: []
labels:
- fcmp++
created_at: '2026-03-11T23:07:15+00:00'
updated_at: '2026-08-19T03:00:31+00:00'
type: pull_request
status: open
closed_at: null
merged_at: null
---

# Original Description
Builds on top of:
- #10359

The `outputs_to_leaves` function introduced in this PR takes in outputs that enter the chain, and converts them into valid leaves prepared to be inserted to the curve trees merkle tree.	We do not add invalid outputs to
the tree, so `outputs_to_leaves` checks outputs for validity and only includes the valid outputs in the final response.

The `CurveTrees` class is a useful object for updating the curve trees merkle tree. It is instantiated with the widths of each layer in the tree, as well as the curve objects themselves.

"Unified Outputs" are the set of all outputs in the chain (pre-RCT, RCT, and Carrot). Each output has a `unified_id`, which is the ordered index given to the output upon entering the chain (when the tx that creates the output enters a block in the chain). We use the `unified_id` to determine the order to insert a batch of outputs into the curve tree merkle tree.

_____

[Trail of Bits](https://github.com/trailofbits/publications/blob/73e52c96cef59d42201ff5546090b76ca67a8014/reviews/2026-07-magicgrants-monerofcmp%2B%2Bcrypto-securityreview.pdf) and Cypher Stack ([section 12](https://github.com/cypherstack/fcmp-impl-audit/blob/d7de33d08c48a3c249629e47f71ea72c938a3404/FCMP_Code_Implementation_Audit.pdf)) audited an earlier state of this PR. Neither identified issues with this PR. However, since both pointed out that if `fe_batch_invert` is passed a 0, it could lead to an erroneous result, I made sure to utilize the updated API for `fe_batch_invert` that returns a success response on success, and throw if returning a failure response. `fe_batch_invert` was included in #10111 and fails if it's passed a 0.

# Discussion History
## UkoeHB | 2026-06-05T22:23:01+00:00
```suggestion
    // Output's unique id in the chain.
    // When a batch of enotes is added to the tree, the batch
    // is internally sorted by unique ids, which correspond with
    // the order the enotes entered the chain. Batch-added enotes
    // may not be globally ordered because e.g. timelocked
    // enotes were added to 'late batches'. Batch refers to
    // enotes that become unlocked/spendable at the same
    // time.
```

## jeffro256 | 2026-08-18T21:00:32+00:00
nit: out of alphabetical order

## j-berman | 2026-08-18T23:25:11+00:00
Fixed

# Action History
- Created by: j-berman | 2026-03-11T23:07:15+00:00
