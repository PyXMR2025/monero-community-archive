---
title: Add proper status / error codes to the Wallet API
source_url: https://github.com/monero-project/monero/issues/10343
author: rbrunner7
assignees: []
labels:
- proposal
created_at: '2026-03-01T09:20:51+00:00'
updated_at: '2026-03-14T13:42:06+00:00'
type: issue
status: open
closed_at: null
---

# Original Description
With *Wallet API* I mean [wallet2_api.h](https://github.com/monero-project/monero/blob/master/src/wallet/api/wallet2_api.h) and the implementation behind it, an API that lets you implement wallet apps that is on a higher level of abstraction than `wallet2.h`. The Monero GUI wallet app uses this, as an example.

There is a WIP PR to significantly expand the Wallet API [here](https://github.com/monero-project/monero/pull/9464), with a goal of making it a full / feature complete replacement for using `wallet2` directly. That expanded API is used in another PR [here](https://github.com/monero-project/monero/pull/10233) to refactor the code of the Monero CLI wallet app to completely stop using `wallet2` directly and using the Wallet API exclusively. That work is quite far along already.

Long-term goal is to stop using `wallet2` in any Monero wallet app, making the way free to re-implement the wallet code from the ground up and get rid of the "spaghetti code" in `wallet2.cpp`. Who knows, maybe we will even be able to use Rust wallet code that became ready and battle-tested in the meantime and spare that implementation work.

Work is currently underway to make the same migration for the Monero RPC wallet server, and it's here where a significant problem surfaced:

The RPC interface is designed to give back detailed error codes, to allow clients to react in sensible ways to problems that occur. You find a list of the codes here: [wallet/wallet_rpc_server_error_codes.h](https://github.com/monero-project/monero/blob/master/src/wallet/wallet_rpc_server_error_codes.h)

Unfortunately this collides with the current design of the Wallet API which beside textual error info only gives back [these status values](https://github.com/monero-project/monero/blob/7fba8bb466bd13221a1bbe47f8c6aa1cbeb226e2/src/wallet/api/wallet2_api.h#L429) as result of the last call:

    enum Status {
        Status_Ok,
        Status_Error,
        Status_Critical
    };

I think it's important for a Wallet RPC server based on the Wallet API to continue giving detailed error codes, like it does now. That in turn would mean to modify the Wallet API code somehow to provide enough info to callers to allow that in a clean way; hacks like parsing the returned error *texts* to deduct error codes from them do not look promising at all to me.

After checking the current Wallet API code I think that implementing proper error codes would need some work, but be quite doable, because it's not a complex job, just some "grunt work".

I have a proposal how to implement this in a way that does not break the current API. The basic idea: There would be an extended `Status` enum along these lines:

    enum Status {
        Status_Ok,
        Status_Error,
        Status_Critical,

        Status_Wrong_Address,
        Status_Daemon_Is_Busy,
        Status_Not_Enough_Money,
        Status_Watch_Only,
        Status_Tx_Too_Large,
        ....
    };

The Wallet API would get a *returned status value mode*, with two values *simple values* and *detailed values*, and a call to switch between the two. Default is *simple values*, where only *Ok*, *Error* and *Critical* are returned, like now. The RPC wallet server would set the mode to *detailed values* and generate its error codes based on the returned "extended" codes.

# Discussion History
## SNeedlewoods | 2026-03-10T18:38:32+00:00
Thanks for writing this issue, I think the extended error codes are a great idea, though in the meantime I went with a different approach that I'd like to share for comparison.
The idea as TLDR: Store relevant exceptions with `setStatusError()` and make them accessible by adding a `recentException()` method to the API.
Or in code:
Add to `wallet/api/wallet.cpp`
``` c++
std::exception_ptr WalletImpl::recentException() const {
    boost::lock_guard<boost::mutex> l(m_statusMutex);
    return m_exceptionPtr;
}
```
Change in `wallet/api/wallet.cpp`
``` diff
-void WalletImpl::setStatusError(const std::string& message) const
+void WalletImpl::setStatusError(const std::string& message,
+                                std::exception_ptr exception /*= nullptr */) const
 {
-    setStatus(Status_Error, message);
+    setStatus(Status_Error, message, exception);
 }

-void WalletImpl::setStatusCritical(const std::string& message) const
+void WalletImpl::setStatusCritical(const std::string& message,
+                                   std::exception_ptr exception /*= nullptr */) const
 {
-    setStatus(Status_Critical, message);
+    setStatus(Status_Critical, message, exception);
 }

-void WalletImpl::setStatus(int status, const std::string& message) const
+void WalletImpl::setStatus(int status, const std::string& message,
+                           std::exception_ptr exception /*= nullptr */) const
 {
     boost::lock_guard<boost::mutex> l(m_statusMutex);
     m_status = status;
     m_errorString = message;
+    m_exceptionPtr = exception ? exception : m_exceptionPtr;
 }
 void WalletImpl::clearStatus() const
 {
     boost::lock_guard<boost::mutex> l(m_statusMutex);
     m_status = Status_Ok;
     m_errorString.clear();
+    m_exceptionPtr = nullptr;
 }
```
Then a method in the Wallet API implementation could look something like this:
``` c++
void WalletImpl::doStuff()
{
  clearStatus();
  try { m_wallet->do_stuff(); }
  catch (const std::exception &e) {
    setStatusError(string(tr("failed to do stuff: ")) + e.what(), std::current_exception());
  }
}
```
and in `wallet/wallet_rpc_server.cpp` you'd call it like this:
``` c++
  try 
  {
    m_wallet_impl->doStuff();
    THROW_WALLET_EXCEPTION_ON_API_ERROR();
  }
  catch (const std::exception &e)
  {
    handle_rpc_exception(std::current_exception(), er, WALLET_RPC_ERROR_CODE_UNKNOWN_ERROR);
    return false;
  }
```
with `THROW_WALLET_EXCEPTION_ON_API_ERROR()` is defined like this:
``` c++
#define THROW_WALLET_EXCEPTION_ON_API_ERROR() \
    do \
    { \
      int error_code; \
      std::string error_message; \
      m_wallet_impl->statusWithErrorString(error_code, error_message); \
      if (error_code != Wallet::Status::Status_Ok) \
      { \
        const std::exception_ptr &eptr = m_wallet_impl->recentException(); \
        if (eptr) { std::rethrow_exception(eptr); } \
        else \
        { \
          LOG_ERROR("Wallet API error: " << error_message); \
          tools::error::throw_wallet_ex<tools::error::wallet_internal_error>(std::string(__FILE__ ":" STRINGIZE(__LINE__)), error_message); \
        } \
      } \
    } while (0)
```


If no one sees a problem with this approach I'd continue that way, just one question then: Which of the following two ideas would you prefer?

1. we could just add `std::current_exception` to `setStatusError()`. (IIUIC this works because the wallet errors we care about inherit from `std::exception` and are therefore catched with the generic `catch (const std::exception &e)` or even `catch (...)`, luckily we don't lose the information about the actual exception if we store and later rethrow `std::current_exception`.)
I prefer this more generic idea, but to be clear, I would not add `current_exception` to every `setStatusError()` inside a `catch`, but only to the ones I identified as necessary for the error-codes in the wallet-rpc. And for the ones which already have a separate `catch` (e.g. [daemon_busy](https://github.com/monero-project/monero/blob/b9998fc9e1c280d30e4b40ba983b1964eb256d66/src/wallet/api/wallet.cpp#L1716)) I would use that.
``` c++
try { m_wallet->some_function(); }
catch (const std::exception &e)
{
    LOG_ERROR(__FUNCTION__ << " error: " << e.what());
    setStatusError(string(tr("Failed to do stuff: ")) + e.what(), std::current_exception());
    return;
}
```

2. Only add `std::current_exception` to `setStatusError()` for exceptions that we actually need in the wallet rpc, to achieve this we often need to add another `catch` block.
This would be easier to use for the wallet-rpc, because we don't have to filter through `m_wallet_impl->recentException()`, we know we care about it if it's not a `nullptr`.
``` c++
try { m_wallet->some_function(); }
catch (const tools::error::some_exception_we_care_about &e)
{
    LOG_ERROR(__FUNCTION__ << " error: " << e.what());
    setStatusError(string(tr("Failed to do stuff: ")) + e.what(), std::current_exception());
    return;
}
catch (const std::exception &e)
{
    LOG_ERROR(__FUNCTION__ << " error: " << e.what());
    setStatusError(string(tr("Failed to do stuff: ")) + e.what());
    return;
}
```

___________
According to my investigation, out of the 53 [wallet rpc error codes](https://github.com/monero-project/monero/blob/master/src/wallet/wallet_rpc_server_error_codes.h), only the following 14 error codes need to be handled/propagated by the Wallet API:
```
WALLET_RPC_ERROR_CODE_DAEMON_IS_BUSY
WALLET_RPC_ERROR_CODE_WRONG_SIGNATURE
WALLET_RPC_ERROR_CODE_ACCOUNT_INDEX_OUT_OF_BOUNDS
WALLET_RPC_ERROR_CODE_ADDRESS_INDEX_OUT_OF_BOUNDS
WALLET_RPC_ERROR_CODE_TX_NOT_POSSIBLE
WALLET_RPC_ERROR_CODE_NOT_ENOUGH_MONEY
WALLET_RPC_ERROR_CODE_NOT_ENOUGH_OUTS_TO_MIX
WALLET_RPC_ERROR_CODE_ZERO_DESTINATION
WALLET_RPC_ERROR_CODE_WALLET_ALREADY_EXISTS
WALLET_RPC_ERROR_CODE_INVALID_PASSWORD
WALLET_RPC_ERROR_CODE_NOT_ENOUGH_UNLOCKED_MONEY
WALLET_RPC_ERROR_CODE_NO_DAEMON_CONNECTION
WALLET_RPC_ERROR_CODE_ZERO_AMOUNT
WALLET_RPC_ERROR_CODE_NONZERO_UNLOCK_TIME
```

## rbrunner7 | 2026-03-13T12:53:10+00:00
I have mixed feelings about this solution.

One the one hand, I think it works, only little new code is necessary to implement it, and the way of processing the exceptions in the RCP server does not radically change.

On the other hand, I do see downsides.

Let us first look from "the outside", i.e. let's look at the Wallet API as an API, and how that API is designed, what its "philosophy" is. It currently has a very simple, you could say even trivial, error reporting based on codes and messages. I would say from a API design point of view nothing is wrong except one thing: the codes are not detailed enough, at least for the RPC server.

How would you react, in an ideal case, as an API designer? Would you now, to put it a bit bluntly, double the complexity of the error handling part of the API, by in a way adding exceptions to the codes? Maybe not.

When I came up with my "extended status code" idea, I was searching for a way that only adds minimal additional complexity *to the API*. Granted, catching all those wallet2 expections internally in the Wallet API code and translating them to error codes is more work than your approach, but isn't it the job of a library to guard clients from as much "trouble" as possible, and offer an interface that is as easy as possible?

If we zoom out further, let us think about one of the important reasons why we do the whole laborious exercise in the first place: We want to fully hide wallet2, to gain the freedom to later completely replace it, maybe with something totally differnt.

Here I see a certain danger with your solution, that it lets "shine through" some aspects of wallet2, which we really want to avoid IMHO.

You don't know, maybe we even replace it with something in Rust, which may not work with C++ style exceptions at all. Translating some error codes coming from the Rust code into the Wallet API error codes would be trivial, however.

## vtnerd | 2026-03-13T15:40:45+00:00
I would advocate for adding a new `enum` rather than adding to the existing one. This way clients can still determine the severity of the error, as according to the backend.

If necessary, we could map `std::error_code` or `std::exception_ptr` to C/Rust - `std::error_code` will be much easier:

```c
// header
struct error_code_t { void* category, int value; };
error_code_t get_error(...);
const char* get_message(error_code_t* code);

// cpp
namespace
{
  error_code_t do_get_error_code(std::error_code code, ...) noexcept
  {
    return error_code_t{code.category(), code.value()};
  }

   const char* do_get_message(error_code* code) noexcept
  {
     try
     {
         const std::error_code code_ = {code.value, *static_cast<const std::error_category*>(code.category)};
         return strcpy(code_.message().c_str());
     }
     catch(...)
     {
        return nullptr;
     }
  }
}

error_code_t get_error_code(...)
{
   return do_get_error_code(...);
}

const char* get_message(error_code_t* code)
{
  return do_get_message(code);
}
```

# Action History
- Created by: rbrunner7 | 2026-03-01T09:20:51+00:00
