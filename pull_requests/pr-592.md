---
title: 'cuprated: embeddable node library with `Node::launch()`'
source_url: https://github.com/Cuprate/cuprate/pull/592
author: redsh4de
assignees: []
labels:
- A-dependency
- A-consensus
- A-binaries
created_at: '2026-03-16T15:41:11+00:00'
updated_at: '2026-04-27T08:22:14+00:00'
type: pull_request
status: open
closed_at: null
merged_at: null
---

# Original Description
### What

Extracts `cuprated` into an embeddable library (`lib.rs`/`main.rs` pattern). Takes inspiration from the API proposed in #554

Closes #516

Note to reviewers: This does not implement graceful shutdown / panic removal yet, those have been deferred to #585. For the final state with all changes, skip to #590  

### Why

To enable embedding the node in other applications

### Where

- `cuprated`:
  - `Cargo.toml` - explicit `[lib]` + `[[bin]]` targets
  - `lib.rs` - new: `Node` struct, `NodeContext` for shared state, `Node::launch()`, public service handles
  - `main.rs` - simplified to CLI wrapper calling `Node::launch()`
  - `commands.rs` - actor pattern rewrite: `CommandHandle` with mpsc/oneshot, `send_command()` API
  - `config.rs` - decoupled from CLI args, `find_config()` returns `Result<Option<Config>>`, dry_run_check() returns `Vec<DryRunResult>`, CLI presentation moved to `main.rs`
  - `blockchain/syncer.rs` - `SyncNotify` replaced by `Syncer` + `SyncerHandle`
  - `blockchain/interface.rs` - free functions + statics (`COMMAND_TX`, `BLOCKS_BEING_HANDLED`) converted to `BlockchainManagerHandle` struct with owned channel + lock 
  - `version.rs` - add `Default` impl (thanks clippy)
  - Removed mutable statics in favor of `NodeContext` for multi-instance support
- `consensus/fast-sync`:
  - Removed global FAST_SYNC_HASHES static
  - `set_fast_sync_hashes` removed; `fast_sync_stop_height` and `validate_entries` now take hashes as a parameter from NodeContext

### How

`Node::launch(config)` in `lib.rs` handles all initialization and returns a `Node` with service handles that serve as the API to the embeder:

```rust
pub struct Node {
    pub context: BlockchainContextService,
    pub blockchain: BlockchainReadHandle,
    pub txpool: TxpoolReadHandle,
    pub clearnet: NetworkInterface<ClearNet>,
    pub tor: Option<oneshot::Receiver<NetworkInterface<Tor>>>,
    pub syncer: SyncerHandle,
    pub command: CommandHandle,
}
```

The previous mutable statics were moved into `NodeContext`, making the node safe to run multiple times in a single process.
```rust
pub struct NodeContext {
    pub network: Network,
    pub fast_sync_hashes: &'static [[u8; 32]], /// was static
    pub reorg_lock: Arc<RwLock<()>>, // was static
    pub blockchain_manager: BlockchainManagerHandle, // was composed of statics
    pub blockchain_read: BlockchainReadHandle,
    pub blockchain_context: BlockchainContextService,
    pub txpool_read: TxpoolReadHandle,
    pub syncer: SyncerHandle,
    pub start_instant: std::time::SystemTime,
    pub start_instant_unix: u64,
}
```

`BlockchainManagerHandle` replaces the COMMAND_TX  and BLOCKS_BEING_HANDLED statics from `interface.rs`, wrapping them into a struct with `new()`, `is_block_being_handled()`, and `handle_incoming_block()` methods.

The Tor interface arrives via oneshot after sync, similar to the `dandelion.rs` pattern.


# Discussion History
## sneurlax | 2026-03-19T03:21:58+00:00
statics::init_lazylock_statics() twice--once in main.rs, once here.  What should actually be responsible for this?

## sneurlax | 2026-03-19T03:24:15+00:00
won't this panic if the db is corrupt, misconfigured, etc? maybe locked?

inspect_err logs it then panics anyways? 

## sneurlax | 2026-03-19T03:25:05+00:00
same thing here, will this panic if the db is corrup tor locked?

## sneurlax | 2026-03-19T03:26:57+00:00
this naked unwrap can cause a panic w no useful msg if consensus init fails

## redsh4de | 2026-03-19T06:21:46+00:00
Ideally this should be called by the embedder imo.

However, it's kept twice because if it's called a second time, it's a no-op, and if the embedder doesnt do it the library will take care of it

## redsh4de | 2026-03-19T06:23:16+00:00
It will - the graceful shutdown PRs that are dependant on this branch gradually introduce error propagation and get rid of the expects

They are split into parts so that its easier to review, as the changes there are quite verbose

## redsh4de | 2026-03-19T06:23:56+00:00
Same as prior, deferred to the other three graceful shutdown PRs

## redsh4de | 2026-03-19T06:46:00+00:00
Deferred to PR #586 

## SyntheticBird45 | 2026-03-19T23:41:48+00:00
Why 8 ? This is no useful in cuprated case and If multiple commands are supposed to be ok for the caller then this should be a parameter somewhere.
@Boog900 opinion?

## redsh4de | 2026-03-20T08:44:42+00:00
Ty for the catch, this should be changed to 1 now to match `main`

The 8 carried over from when i had a seperate buffered output channel, doesnt apply now when a dedicated response channel is made by the request

## SyntheticBird45 | 2026-03-20T10:24:51+00:00
You replaced the `inspect` but not the `inspect_err`.

## SyntheticBird45 | 2026-03-20T10:47:34+00:00
It should be placed after with `mod macros;` the rest of local module imports

## redsh4de | 2026-03-20T11:30:43+00:00
Removed `inspect_err` in order to have `load_config()` in main.rs be responsible for the error handling:

```rust
    //...
    let config = if let Some(config_file) = &args.config_file {
        Config::read_from_path(config_file).unwrap_or_else(|e| {
            eprintln_red(&format!("Failed to read config from file: {e}"));
            std::process::exit(1);
        })
    } else if let Some(config) = find_config().unwrap_or_else(|e| {
        eprintln_red(&format!("Failed to read config: {e}"));
        std::process::exit(1);
    }) {
```

## SyntheticBird45 | 2026-03-20T17:02:22+00:00
Why did you remove `['BlockchainManager']` (`'` = `) ?

## redsh4de | 2026-03-20T17:44:15+00:00
Something about the split made `RUSTDOCFLAGS='-D warnings' cargo doc --workspace --all-features` not like the links anymore if `--document-private-items` isn't passed

This is what im getting with them kept:
```bash
error: public documentation for `init_blockchain_manager` links to private item `BlockchainManager`
  --> binaries/cuprated/src/blockchain/manager.rs:42:33
   |
42 | /// This function sets up the [`BlockchainManager`] and the [`syncer`] so that the functions in [`interface`](super::interface)
   |                                 ^^^^^^^^^^^^^^^^^ this item is private
   |
   = note: this link will resolve properly if you pass `--document-private-items`
   = note: `-D rustdoc::private-intra-doc-links` implied by `-D warnings`
   = help: to override `-D warnings` add `#[allow(rustdoc::private_intra_doc_links)]`

error: public documentation for `init_blockchain_manager` links to private item `syncer`
  --> binaries/cuprated/src/blockchain/manager.rs:42:63
   |
42 | /// This function sets up the [`BlockchainManager`] and the [`syncer`] so that the functions in [`interface`](super::interface)
   |                                                               ^^^^^^ this item is private
   |
   = note: this link will resolve properly if you pass `--document-private-items`

error: could not document `cuprated`
```


## redsh4de | 2026-03-20T17:51:13+00:00
Got it, i overcorrected - the backticks werent the issue, the brackets were

# Action History
- Created by: redsh4de | 2026-03-16T15:41:11+00:00
